syntax = "proto3";

// Provider: googlecloud 8
package terraform.googlecloud.resources.v8;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// GoogleComputeAddress version is 0
message GoogleComputeAddress {
  // The static external IP address represented by this resource.
  //
  // The IP address must be inside the specified subnetwork,
  //
  // if any. Set by the API if undefined.
  optional string address = 1;

  // The unique numeric identifier for the resource. This identifier is defined by the
  // server.
  optional string address_id = 2 [json_name = "address_id"];

  // The type of address to reserve.
  //
  // Note: if you set this argument's value as 'INTERNAL' you need to leave the
  // 'network_tier' argument unset in that resource block. Default value: "EXTERNAL"
  // Possible values: ["INTERNAL", "EXTERNAL"]
  optional string address_type = 3 [json_name = "address_type"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 5 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 6;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 7 [json_name = "effective_labels"];

  optional string id = 8;

  // Reference to the source of external IPv4/IPv6 addresses, like a
  // PublicDelegatedPrefix(PDP) for BYOIP.
  //
  // If an IPv4 PDP is used, the PDP must support enhanced IPv4 allocations.
  //
  // If an IPv6 PDP is used, the PDP must be in EXTERNAL_IPV6_FORWARDING_RULE_CREATION
  // mode.
  //
  // Use one of the following formats to specify a PDP when reserving an external IPv4
  // address using BYOIP.
  //
  // Full resource URL, as in:
  //
  // 1/projects/{{projectId}}/regions/{{region}}/publicDelegatedPrefixes/{{pdp-name}}'
  //
  // Partial URL, as in:
  //
  // * 'projects/{{projectId}}/regions/region/publicDelegatedPrefixes/{{pdp-name}}'
  //
  // * 'regions/{{region}}/publicDelegatedPrefixes/{{pdp-name}}'
  optional string ip_collection = 9 [json_name = "ip_collection"];

  // The IP Version that will be used by this address. The default value is 'IPV4'.
  // Possible values: ["IPV4", "IPV6"]
  optional string ip_version = 10 [json_name = "ip_version"];

  // The endpoint type of this address, which should be VM or NETLB. This is
  //
  // used for deciding which type of endpoint this address can be used after
  //
  // the external IPv6 address reservation. Possible values: ["VM", "NETLB"]
  optional string ipv6_endpoint_type = 11 [json_name = "ipv6_endpoint_type"];

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 12 [json_name = "label_fingerprint"];

  // Labels to apply to this address.  A list of key->value pairs.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 13;

  // Name of the resource. The name must be 1-63 characters long, and
  //
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  //
  // which means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 14;

  // The URL of the network in which to reserve the address. This field
  //
  // can only be used with INTERNAL type with the VPC_PEERING and
  //
  // IPSEC_INTERCONNECT purposes.
  optional string network = 15;

  // The networking tier used for configuring this address. If this field is not
  //
  // specified, it is assumed to be PREMIUM.
  //
  // This argument should not be used when configuring Internal addresses, because
  // [network tier cannot be set for internal traffic; it's always
  // Premium](https://cloud.google.com/network-tiers/docs/overview). Possible values:
  // ["PREMIUM", "STANDARD"]
  optional string network_tier = 16 [json_name = "network_tier"];

  // The prefix length if the resource represents an IP range.
  optional int64 prefix_length = 17 [json_name = "prefix_length"];

  optional string project = 18;

  // The purpose of this resource, which can be one of the following values.
  //
  //
  // * GCE_ENDPOINT for addresses that are used by VM instances, alias IP
  //
  // ranges, load balancers, and similar resources.
  //
  //
  // * SHARED_LOADBALANCER_VIP for an address that can be used by multiple
  //
  // internal load balancers.
  //
  //
  // * VPC_PEERING for addresses that are reserved for VPC peer networks.
  //
  //
  // * IPSEC_INTERCONNECT for addresses created from a private IP range that
  //
  // are reserved for a VLAN attachment in an HA VPN over Cloud Interconnect
  //
  // configuration. These addresses are regional resources.
  //
  //
  // * PRIVATE_SERVICE_CONNECT for a private network address that is used to
  //
  // configure Private Service Connect. Only global internal addresses can use
  //
  // this purpose.
  //
  //
  // This should only be set when using an Internal address.
  optional string purpose = 19;

  // The Region in which the created address should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 20;

  optional string self_link = 21 [json_name = "self_link"];

  // The URL of the subnetwork in which to reserve the address. If an IP
  //
  // address is specified, it must be within the subnetwork's IP range.
  //
  // This field can only be used with INTERNAL type with
  //
  // GCE_ENDPOINT/DNS_RESOLVER purposes.
  optional string subnetwork = 22;

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 23 [json_name = "terraform_labels"];

  // The URLs of the resources that are using this address.
  repeated string users = 24;

  repeated Timeouts timeouts = 25;

  google.protobuf.Value for_each = 26 [json_name = "for_each"];

  repeated string depends_on = 27 [json_name = "depends_on"];

  optional int32 count = 28;

  optional string provider = 29;

  terraform.v1.Lifecycle lifecycle = 30;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeAttachedDisk version is 0
message GoogleComputeAttachedDisk {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // Specifies a unique device name of your choice that is reflected into the
  // /dev/disk/by-id/google-* tree of a Linux operating system running within the
  // instance. This name can be used to reference the device for mounting, resizing,
  // and so on, from within the instance. If not specified, the server chooses a
  // default device name to apply to this disk, in the form persistent-disks-x, where
  // x is a number assigned by Google Compute Engine.
  optional string device_name = 2 [json_name = "device_name"];

  // name or self_link of the disk that will be attached.
  optional string disk = 3;

  optional string id = 4;

  // name or self_link of the compute instance that the disk will be attached to. If
  // the self_link is provided then zone and project are extracted from the self link.
  // If only the name is used then zone and project must be defined as properties on
  // the resource or provider.
  optional string instance = 5;

  // The disk interface used for attaching this disk. One of SCSI or NVME. (This field
  // is only used for specific cases, please don't specify this field without advice
  // from Google.)
  optional string interface = 6;

  // The mode in which to attach this disk, either READ_WRITE or READ_ONLY. If not
  // specified, the default is to attach the disk in READ_WRITE mode.
  optional string mode = 7;

  // The project that the referenced compute instance is a part of. If instance is
  // referenced by its self_link the project defined in the link will take precedence.
  optional string project = 8;

  // The zone that the referenced compute instance is located within. If instance is
  // referenced by its self_link the zone defined in the link will take precedence.
  optional string zone = 9;

  repeated Timeouts timeouts = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  optional int32 count = 13;

  optional string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeAutoscaler version is 0
message GoogleComputeAutoscaler {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  optional string id = 4;

  // Name of the resource. The name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 5;

  optional string project = 6;

  optional string self_link = 7 [json_name = "self_link"];

  // URL of the managed instance group that this autoscaler will scale.
  optional string target = 8;

  // URL of the zone where the instance group resides.
  optional string zone = 9;

  AutoscalingPolicy autoscaling_policy = 10 [json_name = "autoscaling_policy"];

  repeated Timeouts timeouts = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  optional int32 count = 14;

  optional string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message AutoscalingPolicy {
    // The number of seconds that the autoscaler should wait before it
    //
    // starts collecting information from a new instance. This prevents
    //
    // the autoscaler from collecting information when the instance is
    //
    // initializing, during which the collected usage would not be
    //
    // reliable. The default time autoscaler waits is 60 seconds.
    //
    //
    // Virtual machine initialization times might vary because of
    //
    // numerous factors. We recommend that you test how long an
    //
    // instance may take to initialize. To do this, create an instance
    //
    // and time the startup process.
    optional int64 cooldown_period = 1 [json_name = "cooldown_period"];

    // The maximum number of instances that the autoscaler can scale up
    //
    // to. This is required when creating or updating an autoscaler. The
    //
    // maximum number of replicas should not be lower than minimal number
    //
    // of replicas.
    optional int64 max_replicas = 2 [json_name = "max_replicas"];

    // The minimum number of replicas that the autoscaler can scale down
    //
    // to. This cannot be less than 0. If not provided, autoscaler will
    //
    // choose a default value depending on maximum number of instances
    //
    // allowed.
    optional int64 min_replicas = 3 [json_name = "min_replicas"];

    // Defines operating mode for this policy.
    optional string mode = 4;

    // The number of seconds that the autoscaler waits for load stabilization
    //
    // before making scale-in decisions.
    //
    //
    // This might appear as a delay in scaling in but it is an important mechanism
    //
    // for your application to not have fluctuating size due to short term load
    //
    // fluctuations.
    optional int64 stabilization_period = 5 [json_name = "stabilization_period"];

    CpuUtilization cpu_utilization = 6 [json_name = "cpu_utilization"];

    LoadBalancingUtilization load_balancing_utilization = 7 [json_name = "load_balancing_utilization"];

    repeated Metric metric = 8;

    ScaleInControl scale_in_control = 9 [json_name = "scale_in_control"];

    repeated ScalingSchedules scaling_schedules = 10 [json_name = "scaling_schedules"];

    message CpuUtilization {
      // Indicates whether predictive autoscaling based on CPU metric is enabled. Valid
      // values are:
      //
      //
      // - NONE (default). No predictive method is used. The autoscaler scales the group
      // to meet current demand based on real-time metrics.
      //
      //
      // - OPTIMIZE_AVAILABILITY. Predictive autoscaling improves availability by
      // monitoring daily and weekly load patterns and scaling out ahead of anticipated
      // demand.
      optional string predictive_method = 1 [json_name = "predictive_method"];

      // The target CPU utilization that the autoscaler should maintain.
      //
      // Must be a float value in the range (0, 1]. If not specified, the
      //
      // default is 0.6.
      //
      //
      // If the CPU level is below the target utilization, the autoscaler
      //
      // scales down the number of instances until it reaches the minimum
      //
      // number of instances you specified or until the average CPU of
      //
      // your instances reaches the target utilization.
      //
      //
      // If the average CPU is above the target utilization, the autoscaler
      //
      // scales up until it reaches the maximum number of instances you
      //
      // specified or until the average utilization reaches the target
      //
      // utilization.
      optional int64 target = 2;
    }

    message LoadBalancingUtilization {
      // Fraction of backend capacity utilization (set in HTTP(s) load
      //
      // balancing configuration) that autoscaler should maintain. Must
      //
      // be a positive float value. If not defined, the default is 0.8.
      optional int64 target = 1;
    }

    message Metric {
      // A filter string to be used as the filter string for
      //
      // a Stackdriver Monitoring TimeSeries.list API call.
      //
      // This filter is used to select a specific TimeSeries for
      //
      // the purpose of autoscaling and to determine whether the metric
      //
      // is exporting per-instance or per-group data.
      //
      //
      // You can only use the AND operator for joining selectors.
      //
      // You can only use direct equality comparison operator (=) without
      //
      // any functions for each selector.
      //
      // You can specify the metric in both the filter string and in the
      //
      // metric field. However, if specified in both places, the metric must
      //
      // be identical.
      //
      //
      // The monitored resource type determines what kind of values are
      //
      // expected for the metric. If it is a gce_instance, the autoscaler
      //
      // expects the metric to include a separate TimeSeries for each
      //
      // instance in a group. In such a case, you cannot filter on resource
      //
      // labels.
      //
      //
      // If the resource type is any other value, the autoscaler expects
      //
      // this metric to contain values that apply to the entire autoscaled
      //
      // instance group and resource label filtering can be performed to
      //
      // point autoscaler at the correct TimeSeries to scale upon.
      //
      // This is called a per-group metric for the purpose of autoscaling.
      //
      //
      // If not specified, the type defaults to gce_instance.
      //
      //
      // You should provide a filter that is selective enough to pick just
      //
      // one TimeSeries for the autoscaled group or for each of the instances
      //
      // (if you are using gce_instance resource type). If multiple
      //
      // TimeSeries are returned upon the query execution, the autoscaler
      //
      // will sum their respective values to obtain its scaling value.
      optional string filter = 1;

      // The identifier (type) of the Stackdriver Monitoring metric.
      //
      // The metric cannot have negative values.
      //
      //
      // The metric must have a value type of INT64 or DOUBLE.
      optional string name = 2;

      // If scaling is based on a per-group metric value that represents the
      //
      // total amount of work to be done or resource usage, set this value to
      //
      // an amount assigned for a single instance of the scaled group.
      //
      // The autoscaler will keep the number of instances proportional to the
      //
      // value of this metric, the metric itself should not change value due
      //
      // to group resizing.
      //
      //
      // For example, a good metric to use with the target is
      //
      // 'pubsub.googleapis.com/subscription/num_undelivered_messages'
      //
      // or a custom metric exporting the total number of requests coming to
      //
      // your instances.
      //
      //
      // A bad example would be a metric exporting an average or median
      //
      // latency, since this value can't include a chunk assignable to a
      //
      // single instance, it could be better used with utilization_target
      //
      // instead.
      optional int64 single_instance_assignment = 3 [json_name = "single_instance_assignment"];

      // The target value of the metric that autoscaler should
      //
      // maintain. This must be a positive value. A utilization
      //
      // metric scales number of virtual machines handling requests
      //
      // to increase or decrease proportionally to the metric.
      //
      //
      // For example, a good metric to use as a utilizationTarget is
      //
      // www.googleapis.com/compute/instance/network/received_bytes_count.
      //
      // The autoscaler will work to keep this value constant for each
      //
      // of the instances.
      optional int64 target = 4;

      // Defines how target utilization value is expressed for a
      //
      // Stackdriver Monitoring metric. Possible values: ["GAUGE", "DELTA_PER_SECOND",
      // "DELTA_PER_MINUTE"]
      optional string type = 5;
    }

    message ScaleInControl {
      // How long back autoscaling should look when computing recommendations
      //
      // to include directives regarding slower scale down, as described above.
      optional int64 time_window_sec = 1 [json_name = "time_window_sec"];

      MaxScaledInReplicas max_scaled_in_replicas = 2 [json_name = "max_scaled_in_replicas"];

      message MaxScaledInReplicas {
        // Specifies a fixed number of VM instances. This must be a positive
        //
        // integer.
        optional int64 fixed = 1;

        // Specifies a percentage of instances between 0 to 100%, inclusive.
        //
        // For example, specify 80 for 80%.
        optional int64 percent = 2;
      }
    }

    message ScalingSchedules {
      // A description of a scaling schedule.
      optional string description = 1;

      // A boolean value that specifies if a scaling schedule can influence autoscaler
      // recommendations. If set to true, then a scaling schedule has no effect.
      optional bool disabled = 2;

      // The duration of time intervals (in seconds) for which this scaling schedule will
      // be running. The minimum allowed value is 300.
      optional int64 duration_sec = 3 [json_name = "duration_sec"];

      // Minimum number of VM instances that autoscaler will recommend in time intervals
      // starting according to schedule.
      optional int64 min_required_replicas = 4 [json_name = "min_required_replicas"];

      optional string name = 5;

      // The start timestamps of time intervals when this scaling schedule should provide
      // a scaling signal. This field uses the extended cron format (with an optional year
      // field).
      optional string schedule = 6;

      // The time zone to be used when interpreting the schedule. The value of this field
      // must be a time zone name from the tz database:
      // http://en.wikipedia.org/wiki/Tz_database.
      optional string time_zone = 7 [json_name = "time_zone"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeBackendBucket version is 0
message GoogleComputeBackendBucket {
  // Cloud Storage bucket name.
  optional string bucket_name = 1 [json_name = "bucket_name"];

  // Compress text responses using Brotli or gzip compression, based on the client's
  // Accept-Encoding header. Possible values: ["AUTOMATIC", "DISABLED"]
  optional string compression_mode = 2 [json_name = "compression_mode"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Headers that the HTTP/S load balancer should add to proxied responses.
  repeated string custom_response_headers = 4 [json_name = "custom_response_headers"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 5 [json_name = "deletion_policy"];

  // An optional textual description of the resource; provided by the
  //
  // client when the resource is created.
  optional string description = 6;

  // The security policy associated with this backend bucket.
  optional string edge_security_policy = 7 [json_name = "edge_security_policy"];

  // If true, enable Cloud CDN for this BackendBucket.
  //
  // Note: This cannot be set to true when loadBalancingScheme is set to
  // INTERNAL_MANAGED.
  optional bool enable_cdn = 8 [json_name = "enable_cdn"];

  optional string id = 9;

  // The value can only be INTERNAL_MANAGED for cross-region internal layer 7 load
  // balancer.
  //
  // If loadBalancingScheme is not specified, the backend bucket can be used by
  // classic global external load balancers, or global application external load
  // balancers, or both.
  //
  // Important: CDN cannot be enabled (enableCdn cannot be set to true) when
  // loadBalancingScheme is set to INTERNAL_MANAGED. Possible values:
  // ["INTERNAL_MANAGED"]
  optional string load_balancing_scheme = 10 [json_name = "load_balancing_scheme"];

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the
  //
  // last character, which cannot be a dash.
  optional string name = 11;

  optional string project = 12;

  optional string self_link = 13 [json_name = "self_link"];

  CdnPolicy cdn_policy = 14 [json_name = "cdn_policy"];

  Params params = 15;

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message CdnPolicy {
    // Specifies the cache setting for all responses from this backend.
    //
    // The possible values are: USE_ORIGIN_HEADERS, FORCE_CACHE_ALL and CACHE_ALL_STATIC
    // Possible values: ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
    optional string cache_mode = 1 [json_name = "cache_mode"];

    // Specifies the maximum allowed TTL for cached content served by this origin. When
    // the
    //
    // 'cache_mode' is set to "USE_ORIGIN_HEADERS", you must omit this field.
    optional int64 client_ttl = 2 [json_name = "client_ttl"];

    // Specifies the default TTL for cached content served by this origin for responses
    //
    // that do not have an existing valid TTL (max-age or s-max-age). When the
    // 'cache_mode'
    //
    // is set to "USE_ORIGIN_HEADERS", you must omit this field.
    optional int64 default_ttl = 3 [json_name = "default_ttl"];

    // Specifies the maximum allowed TTL for cached content served by this origin. When
    // the
    //
    // 'cache_mode' is set to "USE_ORIGIN_HEADERS", you must omit this field.
    optional int64 max_ttl = 4 [json_name = "max_ttl"];

    // Negative caching allows per-status code TTLs to be set, in order to apply
    // fine-grained caching for common errors or redirects.
    optional bool negative_caching = 5 [json_name = "negative_caching"];

    // If true then Cloud CDN will combine multiple concurrent cache fill requests into
    // a small number of requests to the origin.
    optional bool request_coalescing = 6 [json_name = "request_coalescing"];

    // Serve existing content from the cache (if available) when revalidating content
    // with the origin, or when an error is encountered when refreshing the cache.
    optional int64 serve_while_stale = 7 [json_name = "serve_while_stale"];

    // Maximum number of seconds the response to a signed URL request will
    //
    // be considered fresh. After this time period,
    //
    // the response will be revalidated before being served.
    //
    // When serving responses to signed URL requests,
    //
    // Cloud CDN will internally behave as though
    //
    // all responses from this backend had a "Cache-Control: public,
    //
    // max-age=[TTL]" header, regardless of any existing Cache-Control
    //
    // header. The actual headers served in responses will not be altered.
    optional int64 signed_url_cache_max_age_sec = 8 [json_name = "signed_url_cache_max_age_sec"];

    repeated BypassCacheOnRequestHeaders bypass_cache_on_request_headers = 9 [json_name = "bypass_cache_on_request_headers"];

    CacheKeyPolicy cache_key_policy = 10 [json_name = "cache_key_policy"];

    repeated NegativeCachingPolicy negative_caching_policy = 11 [json_name = "negative_caching_policy"];

    message BypassCacheOnRequestHeaders {
      // The header field name to match on when bypassing cache. Values are
      // case-insensitive.
      optional string header_name = 1 [json_name = "header_name"];
    }

    message CacheKeyPolicy {
      // Allows HTTP request headers (by name) to be used in the
      //
      // cache key.
      repeated string include_http_headers = 1 [json_name = "include_http_headers"];

      // Names of query string parameters to include in cache keys.
      //
      // Default parameters are always included. '&' and '=' will
      //
      // be percent encoded and not treated as delimiters.
      repeated string query_string_whitelist = 2 [json_name = "query_string_whitelist"];
    }

    message NegativeCachingPolicy {
      // The HTTP status code to define a TTL against. Only HTTP status codes 300, 301,
      // 308, 404, 405, 410, 421, 451 and 501
      //
      // can be specified as values, and you cannot specify a status code more than once.
      optional int64 code = 1;

      // The TTL (in seconds) for which to cache responses with the corresponding status
      // code. The maximum allowed value is 1800s
      //
      // (30 minutes), noting that infrequently accessed objects may be evicted from the
      // cache before the defined TTL.
      optional int64 ttl = 2;
    }
  }

  message Params {
    // Resource manager tags to be bound to the backend bucket. Tag keys and values have
    // the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeBackendBucketSignedUrlKey version is 0
message GoogleComputeBackendBucketSignedUrlKey {
  // The backend bucket this signed URL key belongs.
  optional string backend_bucket = 1 [json_name = "backend_bucket"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  optional string id = 3;

  // 128-bit key value used for signing the URL. The key value must be a
  //
  // valid RFC 4648 Section 5 base64url encoded string.
  optional string key_value = 4 [json_name = "key_value"];

  // Name of the signed URL key.
  optional string name = 5;

  optional string project = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeBackendService version is 1
message GoogleComputeBackendService {
  // Lifetime of cookies in seconds if session_affinity is
  //
  // GENERATED_COOKIE. If set to 0, the cookie is non-persistent and lasts
  //
  // only until the end of the browser session (or equivalent). The
  //
  // maximum allowed value for TTL is one day.
  //
  //
  // When the load balancing scheme is INTERNAL, this field is not used.
  optional int64 affinity_cookie_ttl_sec = 1 [json_name = "affinity_cookie_ttl_sec"];

  // Compress text responses using Brotli or gzip compression, based on the client's
  // Accept-Encoding header. Possible values: ["AUTOMATIC", "DISABLED"]
  optional string compression_mode = 2 [json_name = "compression_mode"];

  // Time for which instance will be drained (not accept new
  //
  // connections, but still work to finish started).
  optional int64 connection_draining_timeout_sec = 3 [json_name = "connection_draining_timeout_sec"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // Headers that the HTTP/S load balancer should add to proxied
  //
  // requests.
  repeated string custom_request_headers = 5 [json_name = "custom_request_headers"];

  // Headers that the HTTP/S load balancer should add to proxied
  //
  // responses.
  repeated string custom_response_headers = 6 [json_name = "custom_response_headers"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 7 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 8;

  // The resource URL for the edge security policy associated with this backend
  // service.
  optional string edge_security_policy = 9 [json_name = "edge_security_policy"];

  // If true, enable Cloud CDN for this BackendService.
  optional bool enable_cdn = 10 [json_name = "enable_cdn"];

  // Specifies the canary migration state. Possible values are PREPARE,
  // TEST_BY_PERCENTAGE, and
  //
  // TEST_ALL_TRAFFIC.
  //
  //
  // To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
  // changed to
  //
  // PREPARE. The state must be changed to TEST_ALL_TRAFFIC before the
  // loadBalancingScheme can be
  //
  // changed to EXTERNAL_MANAGED. Optionally, the TEST_BY_PERCENTAGE state can be used
  // to migrate
  //
  // traffic by percentage using externalManagedMigrationTestingPercentage.
  //
  //
  // Rolling back a migration requires the states to be set in reverse order. So
  // changing the
  //
  // scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to be set to
  // TEST_ALL_TRAFFIC at
  //
  // the same time. Optionally, the TEST_BY_PERCENTAGE state can be used to migrate
  // some traffic
  //
  // back to EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL.
  // Possible values: ["PREPARE", "TEST_BY_PERCENTAGE", "TEST_ALL_TRAFFIC"]
  optional string external_managed_migration_state = 11 [json_name = "external_managed_migration_state"];

  // Determines the fraction of requests that should be processed by the Global
  // external
  //
  // Application Load Balancer.
  //
  //
  // The value of this field must be in the range [0, 100].
  //
  //
  // Session affinity options will slightly affect this routing behavior, for more
  // details,
  //
  // see: Session Affinity.
  //
  //
  // This value can only be set if the loadBalancingScheme in the backend service is
  // set to
  //
  // EXTERNAL (when using the Classic ALB) and the migration state is
  // TEST_BY_PERCENTAGE.
  optional int64 external_managed_migration_testing_percentage = 12 [
    json_name = "external_managed_migration_testing_percentage"
  ];

  // Fingerprint of this resource. A hash of the contents stored in this
  //
  // object. This field is used in optimistic locking.
  optional string fingerprint = 13;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional int64 generated_id = 14 [json_name = "generated_id"];

  // The set of URLs to the HttpHealthCheck or HttpsHealthCheck resource
  //
  // for health checking this BackendService. Currently at most one health
  //
  // check can be specified.
  //
  //
  // A health check must be specified unless the backend service uses an internet
  //
  // or serverless NEG as a backend.
  //
  //
  // For internal load balancing, a URL to a HealthCheck resource must be specified
  // instead.
  repeated string health_checks = 15 [json_name = "health_checks"];

  optional string id = 16;

  // Specifies preference of traffic to the backend (from the proxy and from the
  // client for proxyless gRPC). Possible values: ["IPV4_ONLY", "PREFER_IPV6",
  // "IPV6_ONLY"]
  optional string ip_address_selection_policy = 17 [json_name = "ip_address_selection_policy"];

  // Indicates whether the backend service will be used with internal or
  //
  // external load balancing. A backend service created for one type of
  //
  // load balancing cannot be used with the other. For more information, refer to
  //
  // [Choosing a load
  // balancer](https://cloud.google.com/load-balancing/docs/backend-service). Default
  // value: "EXTERNAL_MANAGED" Possible values: ["EXTERNAL", "INTERNAL_SELF_MANAGED",
  // "INTERNAL_MANAGED", "EXTERNAL_MANAGED"]
  optional string load_balancing_scheme = 18 [json_name = "load_balancing_scheme"];

  // The load balancing algorithm used within the scope of the locality.
  //
  // The possible values are:
  //
  //
  // * 'ROUND_ROBIN': This is a simple policy in which each healthy backend
  //
  // is selected in round robin order.
  //
  //
  // * 'LEAST_REQUEST': An O(1) algorithm which selects two random healthy
  //
  // hosts and picks the host which has fewer active requests.
  //
  //
  // * 'RING_HASH': The ring/modulo hash load balancer implements consistent
  //
  // hashing to backends. The algorithm has the property that the
  //
  // addition/removal of a host from a set of N hosts only affects
  //
  // 1/N of the requests.
  //
  //
  // * 'RANDOM': The load balancer selects a random healthy host.
  //
  //
  // * 'ORIGINAL_DESTINATION': Backend host is selected based on the client
  //
  // connection metadata, i.e., connections are opened
  //
  // to the same address as the destination address of
  //
  // the incoming connection before the connection
  //
  // was redirected to the load balancer.
  //
  //
  // * 'MAGLEV': used as a drop in replacement for the ring hash load balancer.
  //
  // Maglev is not as stable as ring hash but has faster table lookup
  //
  // build times and host selection times. For more information about
  //
  // Maglev, refer to https://ai.google/research/pubs/pub44824
  //
  //
  // * 'WEIGHTED_MAGLEV': Per-instance weighted Load Balancing via health check
  //
  // reported weights. Only applicable to loadBalancingScheme
  //
  // EXTERNAL. If set, the Backend Service must
  //
  // configure a non legacy HTTP-based Health Check, and
  //
  // health check replies are expected to contain
  //
  // non-standard HTTP response header field
  //
  // X-Load-Balancing-Endpoint-Weight to specify the
  //
  // per-instance weights. If set, Load Balancing is weight
  //
  // based on the per-instance weights reported in the last
  //
  // processed health check replies, as long as every
  //
  // instance either reported a valid weight or had
  //
  // UNAVAILABLE_WEIGHT. Otherwise, Load Balancing remains
  //
  // equal-weight.
  //
  //
  // * 'WEIGHTED_ROUND_ROBIN': Per-endpoint weighted round-robin Load Balancing using
  // weights computed
  //
  // from Backend reported Custom Metrics. If set, the Backend Service
  //
  // responses are expected to contain non-standard HTTP response header field
  //
  // X-Endpoint-Load-Metrics. The reported metrics
  //
  // to use for computing the weights are specified via the
  //
  // backends[].customMetrics fields.
  //
  //
  // locality_lb_policy is applicable to either:
  //
  //
  // * A regional backend service with the service_protocol set to HTTP, HTTPS, HTTP2
  // or H2C,
  //
  // and loadBalancingScheme set to INTERNAL_MANAGED.
  //
  // * A global backend service with the load_balancing_scheme set to
  // INTERNAL_SELF_MANAGED.
  //
  // * A regional backend service with loadBalancingScheme set to EXTERNAL (External
  // Network
  //
  // Load Balancing). Only MAGLEV and WEIGHTED_MAGLEV values are possible for External
  //
  // Network Load Balancing. The default is MAGLEV.
  //
  //
  // If session_affinity is not NONE, and locality_lb_policy is not set to MAGLEV,
  // WEIGHTED_MAGLEV,
  //
  // or RING_HASH, session affinity settings will not take effect.
  //
  //
  // Only ROUND_ROBIN and RING_HASH are supported when the backend service is
  // referenced
  //
  // by a URL map that is bound to target gRPC proxy that has validate_for_proxyless
  //
  // field set to true. Possible values: ["ROUND_ROBIN", "LEAST_REQUEST", "RING_HASH",
  // "RANDOM", "ORIGINAL_DESTINATION", "MAGLEV", "WEIGHTED_MAGLEV",
  // "WEIGHTED_ROUND_ROBIN"]
  optional string locality_lb_policy = 19 [json_name = "locality_lb_policy"];

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 20;

  // Name of backend port. The same name should appear in the instance
  //
  // groups referenced by this service. Required when the load balancing
  //
  // scheme is EXTERNAL.
  optional string port_name = 21 [json_name = "port_name"];

  optional string project = 22;

  // The protocol this BackendService uses to communicate with backends.
  //
  // The default is HTTP. Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP
  //
  // or GRPC. Refer to the documentation for the load balancers or for Traffic
  // Director
  //
  // for more information. Must be set to GRPC when the backend service is referenced
  //
  // by a URL map that is bound to target gRPC proxy. Possible values: ["HTTP",
  // "HTTPS", "HTTP2", "TCP", "SSL", "UDP", "GRPC", "UNSPECIFIED", "H2C"]
  optional string protocol = 23;

  // The security policy associated with this backend service.
  optional string security_policy = 24 [json_name = "security_policy"];

  optional string self_link = 25 [json_name = "self_link"];

  // URL to networkservices.ServiceLbPolicy resource.
  //
  // Can only be set if load balancing scheme is EXTERNAL, EXTERNAL_MANAGED,
  // INTERNAL_MANAGED or INTERNAL_SELF_MANAGED and the scope is global.
  optional string service_lb_policy = 26 [json_name = "service_lb_policy"];

  // Type of session affinity to use. The default is NONE. Session affinity is
  //
  // not applicable if the protocol is UDP. Possible values: ["NONE", "CLIENT_IP",
  // "CLIENT_IP_PORT_PROTO", "CLIENT_IP_PROTO", "GENERATED_COOKIE", "HEADER_FIELD",
  // "HTTP_COOKIE", "STRONG_COOKIE_AFFINITY"]
  optional string session_affinity = 27 [json_name = "session_affinity"];

  // The backend service timeout has a different meaning depending on the type of load
  // balancer.
  //
  // For more information see, [Backend service
  // ttings](https://cloud.google.com/compute/docs/reference/rest/v1/backendServices).
  //
  // The default is 30 seconds.
  //
  // The full range of timeout values allowed goes from 1 through 2,147,483,647
  // seconds.
  optional int64 timeout_sec = 28 [json_name = "timeout_sec"];

  repeated Backend backend = 29;

  CdnPolicy cdn_policy = 30 [json_name = "cdn_policy"];

  CircuitBreakers circuit_breakers = 31 [json_name = "circuit_breakers"];

  ConsistentHash consistent_hash = 32 [json_name = "consistent_hash"];

  repeated CustomMetrics custom_metrics = 33 [json_name = "custom_metrics"];

  Iap iap = 34;

  repeated LocalityLbPolicies locality_lb_policies = 35 [json_name = "locality_lb_policies"];

  LogConfig log_config = 36 [json_name = "log_config"];

  MaxStreamDuration max_stream_duration = 37 [json_name = "max_stream_duration"];

  OutlierDetection outlier_detection = 38 [json_name = "outlier_detection"];

  Params params = 39;

  SecuritySettings security_settings = 40 [json_name = "security_settings"];

  StrongSessionAffinityCookie strong_session_affinity_cookie = 41 [json_name = "strong_session_affinity_cookie"];

  repeated Timeouts timeouts = 42;

  TlsSettings tls_settings = 43 [json_name = "tls_settings"];

  google.protobuf.Value for_each = 44 [json_name = "for_each"];

  repeated string depends_on = 45 [json_name = "depends_on"];

  optional int32 count = 46;

  optional string provider = 47;

  terraform.v1.Lifecycle lifecycle = 48;

  message Backend {
    // Specifies the balancing mode for this backend.
    //
    //
    // For global HTTP(S) or TCP/SSL load balancing, the default is
    //
    // UTILIZATION. Valid values are UTILIZATION, RATE (for HTTP(S)),
    //
    // CUSTOM_METRICS (for HTTP(s)) and CONNECTION (for TCP/SSL).
    //
    //
    // See the [Backend Services
    // iew](https://cloud.google.com/load-balancing/docs/backend-service#balancing-mode)
    //
    // for an explanation of load balancing modes. Default value: "UTILIZATION" Possible
    // values: ["UTILIZATION", "RATE", "CONNECTION", "CUSTOM_METRICS", "IN_FLIGHT"]
    optional string balancing_mode = 1 [json_name = "balancing_mode"];

    // A multiplier applied to the group's maximum servicing capacity
    //
    // (based on UTILIZATION, RATE or CONNECTION).
    //
    //
    // Default value is 1, which means the group will serve up to 100%
    //
    // of its configured capacity (depending on balancingMode). A
    //
    // setting of 0 means the group is completely drained, offering
    //
    // 0% of its available Capacity. Valid range is [0.0,1.0].
    optional int64 capacity_scaler = 2 [json_name = "capacity_scaler"];

    // An optional description of this resource.
    //
    // Provide this property when you create the resource.
    optional string description = 3;

    // The fully-qualified URL of an Instance Group or Network Endpoint
    //
    // Group resource. In case of instance group this defines the list
    //
    // of instances that serve traffic. Member virtual machine
    //
    // instances from each instance group must live in the same zone as
    //
    // the instance group itself. No two backends in a backend service
    //
    // are allowed to use same Instance Group resource.
    //
    //
    // For Network Endpoint Groups this defines list of endpoints. All
    //
    // endpoints of Network Endpoint Group must be hosted on instances
    //
    // located in the same zone as the Network Endpoint Group.
    //
    //
    // Backend services cannot mix Instance Group and
    //
    // Network Endpoint Group backends.
    //
    //
    // Note that you must specify an Instance Group or Network Endpoint
    //
    // Group resource using the fully-qualified URL, rather than a
    //
    // partial URL.
    optional string group = 4;

    // The max number of simultaneous connections for the group. Can
    //
    // be used with either CONNECTION or UTILIZATION balancing modes.
    //
    //
    // For CONNECTION mode, either maxConnections or one
    //
    // of maxConnectionsPerInstance or maxConnectionsPerEndpoint,
    //
    // as appropriate for group type, must be set.
    optional int64 max_connections = 5 [json_name = "max_connections"];

    // The max number of simultaneous connections that a single backend
    //
    // network endpoint can handle. This is used to calculate the
    //
    // capacity of the group. Can be used in either CONNECTION or
    //
    // UTILIZATION balancing modes.
    //
    //
    // For CONNECTION mode, either
    //
    // maxConnections or maxConnectionsPerEndpoint must be set.
    optional int64 max_connections_per_endpoint = 6 [json_name = "max_connections_per_endpoint"];

    // The max number of simultaneous connections that a single
    //
    // backend instance can handle. This is used to calculate the
    //
    // capacity of the group. Can be used in either CONNECTION or
    //
    // UTILIZATION balancing modes.
    //
    //
    // For CONNECTION mode, either maxConnections or
    //
    // maxConnectionsPerInstance must be set.
    optional int64 max_connections_per_instance = 7 [json_name = "max_connections_per_instance"];

    // The max requests per second (RPS) of the group.
    //
    //
    // Can be used with either RATE or UTILIZATION balancing modes,
    //
    // but required if RATE mode. For RATE mode, either maxRate or one
    //
    // of maxRatePerInstance or maxRatePerEndpoint, as appropriate for
    //
    // group type, must be set.
    optional int64 max_rate = 8 [json_name = "max_rate"];

    // The max requests per second (RPS) that a single backend network
    //
    // endpoint can handle. This is used to calculate the capacity of
    //
    // the group. Can be used in either balancing mode. For RATE mode,
    //
    // either maxRate or maxRatePerEndpoint must be set.
    optional int64 max_rate_per_endpoint = 9 [json_name = "max_rate_per_endpoint"];

    // The max requests per second (RPS) that a single backend
    //
    // instance can handle. This is used to calculate the capacity of
    //
    // the group. Can be used in either balancing mode. For RATE mode,
    //
    // either maxRate or maxRatePerInstance must be set.
    optional int64 max_rate_per_instance = 10 [json_name = "max_rate_per_instance"];

    // Used when balancingMode is UTILIZATION. This ratio defines the
    //
    // CPU utilization target for the group. Valid range is [0.0, 1.0].
    optional int64 max_utilization = 11 [json_name = "max_utilization"];

    // This field indicates whether this backend should be fully utilized before sending
    // traffic to backends
    //
    // with default preference. This field cannot be set when loadBalancingScheme is set
    // to 'EXTERNAL'. The possible values are:
    //
    // - PREFERRED: Backends with this preference level will be filled up to their
    // capacity limits first,
    //
    // based on RTT.
    //
    // - DEFAULT: If preferred backends don't have enough capacity, backends in this
    // layer would be used and
    //
    // traffic would be assigned based on the load balancing algorithm you use. This is
    // the default Possible values: ["PREFERRED", "DEFAULT"]
    optional string preference = 12;

    repeated CustomMetrics custom_metrics = 13 [json_name = "custom_metrics"];

    message CustomMetrics {
      // If true, the metric data is collected and reported to Cloud
      //
      // Monitoring, but is not used for load balancing.
      optional bool dry_run = 1 [json_name = "dry_run"];

      // Optional parameter to define a target utilization for the Custom Metrics
      //
      // balancing mode. The valid range is <code>[0.0, 1.0]</code>.
      optional int64 max_utilization = 2 [json_name = "max_utilization"];

      // Name of a custom utilization signal. The name must be 1-64 characters
      //
      // long and match the regular expression [a-z]([-_.a-z0-9]*[a-z0-9])? which
      //
      // means the first character must be a lowercase letter, and all following
      //
      // characters must be a dash, period, underscore, lowercase letter, or
      //
      // digit, except the last character, which cannot be a dash, period, or
      //
      // underscore. For usage guidelines, see Custom Metrics balancing mode. This
      //
      // field can only be used for a global or regional backend service with the
      //
      // loadBalancingScheme set to <code>EXTERNAL_MANAGED</code>,
      //
      // <code>INTERNAL_MANAGED</code> <code>INTERNAL_SELF_MANAGED</code>.
      optional string name = 3;
    }
  }

  message CdnPolicy {
    // Specifies the cache setting for all responses from this backend.
    //
    // The possible values are: USE_ORIGIN_HEADERS, FORCE_CACHE_ALL and CACHE_ALL_STATIC
    // Possible values: ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
    optional string cache_mode = 1 [json_name = "cache_mode"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    optional int64 client_ttl = 2 [json_name = "client_ttl"];

    // Specifies the default TTL for cached content served by this origin for responses
    //
    // that do not have an existing valid TTL (max-age or s-max-age).
    optional int64 default_ttl = 3 [json_name = "default_ttl"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    optional int64 max_ttl = 4 [json_name = "max_ttl"];

    // Negative caching allows per-status code TTLs to be set, in order to apply
    // fine-grained caching for common errors or redirects.
    optional bool negative_caching = 5 [json_name = "negative_caching"];

    // If true then Cloud CDN will combine multiple concurrent cache fill requests into
    // a small number of requests
    //
    // to the origin.
    optional bool request_coalescing = 6 [json_name = "request_coalescing"];

    // Serve existing content from the cache (if available) when revalidating content
    // with the origin, or when an error is encountered when refreshing the cache.
    optional int64 serve_while_stale = 7 [json_name = "serve_while_stale"];

    // Maximum number of seconds the response to a signed URL request
    //
    // will be considered fresh, defaults to 1hr (3600s). After this
    //
    // time period, the response will be revalidated before
    //
    // being served.
    //
    //
    // When serving responses to signed URL requests, Cloud CDN will
    //
    // internally behave as though all responses from this backend had a
    //
    // "Cache-Control: public, max-age=[TTL]" header, regardless of any
    //
    // existing Cache-Control header. The actual headers served in
    //
    // responses will not be altered.
    optional int64 signed_url_cache_max_age_sec = 8 [json_name = "signed_url_cache_max_age_sec"];

    repeated BypassCacheOnRequestHeaders bypass_cache_on_request_headers = 9 [json_name = "bypass_cache_on_request_headers"];

    CacheKeyPolicy cache_key_policy = 10 [json_name = "cache_key_policy"];

    repeated NegativeCachingPolicy negative_caching_policy = 11 [json_name = "negative_caching_policy"];

    message BypassCacheOnRequestHeaders {
      // The header field name to match on when bypassing cache. Values are
      // case-insensitive.
      optional string header_name = 1 [json_name = "header_name"];
    }

    message CacheKeyPolicy {
      // If true requests to different hosts will be cached separately.
      optional bool include_host = 1 [json_name = "include_host"];

      // Allows HTTP request headers (by name) to be used in the
      //
      // cache key.
      repeated string include_http_headers = 2 [json_name = "include_http_headers"];

      // Names of cookies to include in cache keys.
      repeated string include_named_cookies = 3 [json_name = "include_named_cookies"];

      // If true, http and https requests will be cached separately.
      optional bool include_protocol = 4 [json_name = "include_protocol"];

      // If true, include query string parameters in the cache key
      //
      // according to query_string_whitelist and
      //
      // query_string_blacklist. If neither is set, the entire query
      //
      // string will be included.
      //
      //
      // If false, the query string will be excluded from the cache
      //
      // key entirely.
      optional bool include_query_string = 5 [json_name = "include_query_string"];

      // Names of query string parameters to exclude in cache keys.
      //
      //
      // All other parameters will be included. Either specify
      //
      // query_string_whitelist or query_string_blacklist, not both.
      //
      // '&' and '=' will be percent encoded and not treated as
      //
      // delimiters.
      repeated string query_string_blacklist = 6 [json_name = "query_string_blacklist"];

      // Names of query string parameters to include in cache keys.
      //
      //
      // All other parameters will be excluded. Either specify
      //
      // query_string_whitelist or query_string_blacklist, not both.
      //
      // '&' and '=' will be percent encoded and not treated as
      //
      // delimiters.
      repeated string query_string_whitelist = 7 [json_name = "query_string_whitelist"];
    }

    message NegativeCachingPolicy {
      // The HTTP status code to define a TTL against. Only HTTP status codes 300, 301,
      // 308, 404, 405, 410, 421, 451 and 501
      //
      // can be specified as values, and you cannot specify a status code more than once.
      optional int64 code = 1;

      // The TTL (in seconds) for which to cache responses with the corresponding status
      // code. The maximum allowed value is 1800s
      //
      // (30 minutes), noting that infrequently accessed objects may be evicted from the
      // cache before the defined TTL.
      optional int64 ttl = 2;
    }
  }

  message CircuitBreakers {
    // The maximum number of connections to the backend cluster.
    //
    // Defaults to 1024.
    optional int64 max_connections = 1 [json_name = "max_connections"];

    // The maximum number of pending requests to the backend cluster.
    //
    // Defaults to 1024.
    optional int64 max_pending_requests = 2 [json_name = "max_pending_requests"];

    // The maximum number of parallel requests to the backend cluster.
    //
    // Defaults to 1024.
    optional int64 max_requests = 3 [json_name = "max_requests"];

    // Maximum requests for a single backend connection. This parameter
    //
    // is respected by both the HTTP/1.1 and HTTP/2 implementations. If
    //
    // not specified, there is no limit. Setting this parameter to 1
    //
    // will effectively disable keep alive.
    optional int64 max_requests_per_connection = 4 [json_name = "max_requests_per_connection"];

    // The maximum number of parallel retries to the backend cluster.
    //
    // Defaults to 3.
    optional int64 max_retries = 5 [json_name = "max_retries"];
  }

  message ConsistentHash {
    // The hash based on the value of the specified header field.
    //
    // This field is applicable if the sessionAffinity is set to HEADER_FIELD.
    optional string http_header_name = 1 [json_name = "http_header_name"];

    // The minimum number of virtual nodes to use for the hash ring.
    //
    // Larger ring sizes result in more granular load
    //
    // distributions. If the number of hosts in the load balancing pool
    //
    // is larger than the ring size, each host will be assigned a single
    //
    // virtual node.
    //
    // Defaults to 1024.
    optional int64 minimum_ring_size = 2 [json_name = "minimum_ring_size"];

    HttpCookie http_cookie = 3 [json_name = "http_cookie"];

    message HttpCookie {
      // Name of the cookie.
      optional string name = 1;

      // Path to set for the cookie.
      optional string path = 2;

      Ttl ttl = 3;

      message Ttl {
        // Span of time that's a fraction of a second at nanosecond
        //
        // resolution. Durations less than one second are represented
        //
        // with a 0 seconds field and a positive nanos field. Must
        //
        // be from 0 to 999,999,999 inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second.
        //
        // Must be from 0 to 315,576,000,000 inclusive.
        optional int64 seconds = 2;
      }
    }
  }

  message CustomMetrics {
    // If true, the metric data is not used for load balancing.
    optional bool dry_run = 1 [json_name = "dry_run"];

    // Name of a custom utilization signal. The name must be 1-64 characters
    //
    // long and match the regular expression [a-z]([-_.a-z0-9]*[a-z0-9])? which
    //
    // means the first character must be a lowercase letter, and all following
    //
    // characters must be a dash, period, underscore, lowercase letter, or
    //
    // digit, except the last character, which cannot be a dash, period, or
    //
    // underscore. For usage guidelines, see Custom Metrics balancing mode. This
    //
    // field can only be used for a global or regional backend service with the
    //
    // loadBalancingScheme set to <code>EXTERNAL_MANAGED</code>,
    //
    // <code>INTERNAL_MANAGED</code> <code>INTERNAL_SELF_MANAGED</code>.
    optional string name = 2;
  }

  message Iap {
    // Whether the serving infrastructure will authenticate and authorize all incoming
    // requests.
    optional bool enabled = 1;

    // OAuth2 Client ID for IAP
    optional string oauth2_client_id = 2 [json_name = "oauth2_client_id"];

    // OAuth2 Client ID for IAP
    optional string oauth2_client_id_wo = 3 [json_name = "oauth2_client_id_wo"];

    // Triggers update of 'oauth2_client_id_wo' write-only. Increment this value when an
    // update to 'oauth2_client_id_wo' is needed. For more info see [updating write-only
    // ders/google/guides/using_write_only_arguments.html#updating-write-only-arguments)
    optional string oauth2_client_id_wo_version = 4 [json_name = "oauth2_client_id_wo_version"];

    // OAuth2 Client Secret for IAP
    optional string oauth2_client_secret = 5 [json_name = "oauth2_client_secret"];

    // OAuth2 Client Secret SHA-256 for IAP
    optional string oauth2_client_secret_sha256 = 6 [json_name = "oauth2_client_secret_sha256"];

    // OAuth2 Client Secret for IAP
    optional string oauth2_client_secret_wo = 7 [json_name = "oauth2_client_secret_wo"];

    // Triggers update of 'oauth2_client_secret_wo' write-only. Increment this value
    // when an update to 'oauth2_client_secret_wo' is needed. For more info see
    // [updating write-only
    // ders/google/guides/using_write_only_arguments.html#updating-write-only-arguments)
    optional string oauth2_client_secret_wo_version = 8 [json_name = "oauth2_client_secret_wo_version"];
  }

  message LocalityLbPolicies {
    CustomPolicy custom_policy = 1 [json_name = "custom_policy"];

    Policy policy = 2;

    message CustomPolicy {
      // An optional, arbitrary JSON object with configuration data, understood
      //
      // by a locally installed custom policy implementation.
      optional string data = 1;

      // Identifies the custom policy.
      //
      //
      // The value should match the type the custom implementation is registered
      //
      // with on the gRPC clients. It should follow protocol buffer
      //
      // message naming conventions and include the full path (e.g.
      //
      // myorg.CustomLbPolicy). The maximum length is 256 characters.
      //
      //
      // Note that specifying the same custom policy more than once for a
      //
      // backend is not a valid configuration and will be rejected.
      optional string name = 2;
    }

    message Policy {
      // The name of a locality load balancer policy to be used. The value
      //
      // should be one of the predefined ones as supported by localityLbPolicy,
      //
      // although at the moment only ROUND_ROBIN is supported.
      //
      //
      // This field should only be populated when the customPolicy field is not
      //
      // used.
      //
      //
      // Note that specifying the same policy more than once for a backend is
      //
      // not a valid configuration and will be rejected.
      //
      //
      // The possible values are:
      //
      //
      // * 'ROUND_ROBIN': This is a simple policy in which each healthy backend
      //
      // is selected in round robin order.
      //
      //
      // * 'LEAST_REQUEST': An O(1) algorithm which selects two random healthy
      //
      // hosts and picks the host which has fewer active requests.
      //
      //
      // * 'RING_HASH': The ring/modulo hash load balancer implements consistent
      //
      // hashing to backends. The algorithm has the property that the
      //
      // addition/removal of a host from a set of N hosts only affects
      //
      // 1/N of the requests.
      //
      //
      // * 'RANDOM': The load balancer selects a random healthy host.
      //
      //
      // * 'ORIGINAL_DESTINATION': Backend host is selected based on the client
      //
      // connection metadata, i.e., connections are opened
      //
      // to the same address as the destination address of
      //
      // the incoming connection before the connection
      //
      // was redirected to the load balancer.
      //
      //
      // * 'MAGLEV': used as a drop in replacement for the ring hash load balancer.
      //
      // Maglev is not as stable as ring hash but has faster table lookup
      //
      // build times and host selection times. For more information about
      //
      // Maglev, refer to https://ai.google/research/pubs/pub44824 Possible values:
      // ["ROUND_ROBIN", "LEAST_REQUEST", "RING_HASH", "RANDOM", "ORIGINAL_DESTINATION",
      // "MAGLEV"]
      optional string name = 1;
    }
  }

  message LogConfig {
    // Whether to enable logging for the load balancer traffic served by this backend
    // service.
    optional bool enable = 1;

    // This field can only be specified if logging is enabled for this backend service
    // and "logConfig.optionalMode"
    //
    // was set to CUSTOM. Contains a list of optional fields you want to include in the
    // logs.
    //
    // For example: serverInstance, serverGkeDetails.cluster,
    // serverGkeDetails.pod.podNamespace
    //
    // For example: orca_load_report, tls.protocol
    repeated string optional_fields = 2 [json_name = "optional_fields"];

    // Specifies the optional logging mode for the load balancer traffic.
    //
    // Supported values: INCLUDE_ALL_OPTIONAL, EXCLUDE_ALL_OPTIONAL, CUSTOM. Possible
    // values: ["INCLUDE_ALL_OPTIONAL", "EXCLUDE_ALL_OPTIONAL", "CUSTOM"]
    optional string optional_mode = 3 [json_name = "optional_mode"];

    // This field can only be specified if logging is enabled for this backend service.
    // The value of
    //
    // the field must be in [0, 1]. This configures the sampling rate of requests to the
    // load balancer
    //
    // where 1.0 means all logged requests are reported and 0.0 means no logged requests
    // are reported.
    //
    // The default value is 1.0.
    optional int64 sample_rate = 4 [json_name = "sample_rate"];

    repeated RequestHeaders request_headers = 5 [json_name = "request_headers"];

    repeated ResponseHeaders response_headers = 6 [json_name = "response_headers"];

    message RequestHeaders {
      // The header name to match on for logging.
      optional string header_name = 1 [json_name = "header_name"];
    }

    message ResponseHeaders {
      // The header name to match on for logging.
      optional string header_name = 1 [json_name = "header_name"];
    }
  }

  message MaxStreamDuration {
    // Span of time that's a fraction of a second at nanosecond resolution.
    //
    // Durations less than one second are represented with a 0 seconds field and a
    // positive nanos field.
    //
    // Must be from 0 to 999,999,999 inclusive.
    optional int64 nanos = 1;

    // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
    // inclusive. (int64 format)
    optional string seconds = 2;
  }

  message OutlierDetection {
    // Number of errors before a host is ejected from the connection pool. When the
    //
    // backend host is accessed over HTTP, a 5xx return code qualifies as an error.
    //
    // Defaults to 5.
    optional int64 consecutive_errors = 1 [json_name = "consecutive_errors"];

    // The number of consecutive gateway failures (502, 503, 504 status or connection
    //
    // errors that are mapped to one of those status codes) before a consecutive
    //
    // gateway failure ejection occurs. Defaults to 5.
    optional int64 consecutive_gateway_failure = 2 [json_name = "consecutive_gateway_failure"];

    // The percentage chance that a host will be actually ejected when an outlier
    //
    // status is detected through consecutive 5xx. This setting can be used to disable
    //
    // ejection or to ramp it up slowly. Defaults to 100.
    optional int64 enforcing_consecutive_errors = 3 [json_name = "enforcing_consecutive_errors"];

    // The percentage chance that a host will be actually ejected when an outlier
    //
    // status is detected through consecutive gateway failures. This setting can be
    //
    // used to disable ejection or to ramp it up slowly. Defaults to 0.
    optional int64 enforcing_consecutive_gateway_failure = 4 [
      json_name = "enforcing_consecutive_gateway_failure"
    ];

    // The percentage chance that a host will be actually ejected when an outlier
    //
    // status is detected through success rate statistics. This setting can be used to
    //
    // disable ejection or to ramp it up slowly. Defaults to 100.
    optional int64 enforcing_success_rate = 5 [json_name = "enforcing_success_rate"];

    // Maximum percentage of hosts in the load balancing pool for the backend service
    //
    // that can be ejected. Defaults to 10%.
    optional int64 max_ejection_percent = 6 [json_name = "max_ejection_percent"];

    // The number of hosts in a cluster that must have enough request volume to detect
    //
    // success rate outliers. If the number of hosts is less than this setting, outlier
    //
    // detection via success rate statistics is not performed for any host in the
    //
    // cluster. Defaults to 5.
    optional int64 success_rate_minimum_hosts = 7 [json_name = "success_rate_minimum_hosts"];

    // The minimum number of total requests that must be collected in one interval (as
    //
    // defined by the interval duration above) to include this host in success rate
    //
    // based outlier detection. If the volume is lower than this setting, outlier
    //
    // detection via success rate statistics is not performed for that host. Defaults
    //
    // to 100.
    optional int64 success_rate_request_volume = 8 [json_name = "success_rate_request_volume"];

    // This factor is used to determine the ejection threshold for success rate outlier
    //
    // ejection. The ejection threshold is the difference between the mean success
    //
    // rate, and the product of this factor and the standard deviation of the mean
    //
    // success rate: mean - (stdev * success_rate_stdev_factor). This factor is divided
    //
    // by a thousand to get a double. That is, if the desired factor is 1.9, the
    //
    // runtime value should be 1900. Defaults to 1900.
    optional int64 success_rate_stdev_factor = 9 [json_name = "success_rate_stdev_factor"];

    BaseEjectionTime base_ejection_time = 10 [json_name = "base_ejection_time"];

    Interval interval = 11;

    message BaseEjectionTime {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      //
      // less than one second are represented with a 0 'seconds' field and a positive
      //
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      //
      // inclusive.
      optional int64 seconds = 2;
    }

    message Interval {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      //
      // less than one second are represented with a 0 'seconds' field and a positive
      //
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      //
      // inclusive.
      optional int64 seconds = 2;
    }
  }

  message Params {
    // Resource manager tags to be bound to the backend service. Tag keys and values
    // have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message SecuritySettings {
    // ClientTlsPolicy is a resource that specifies how a client should authenticate
    //
    // connections to backends of a service. This resource itself does not affect
    //
    // configuration unless it is attached to a backend service resource.
    optional string client_tls_policy = 1 [json_name = "client_tls_policy"];

    // A list of alternate names to verify the subject identity in the certificate.
    //
    // If specified, the client will verify that the server certificate's subject
    //
    // alt name matches one of the specified values.
    repeated string subject_alt_names = 2 [json_name = "subject_alt_names"];

    AwsV4Authentication aws_v4_authentication = 3 [json_name = "aws_v4_authentication"];

    message AwsV4Authentication {
      // The access key used for s3 bucket authentication.
      //
      // Required for updating or creating a backend that uses AWS v4 signature
      // authentication, but will not be returned as part of the configuration when
      // queried with a REST API GET request.
      optional string access_key = 1 [json_name = "access_key"];

      // The identifier of an access key used for s3 bucket authentication.
      optional string access_key_id = 2 [json_name = "access_key_id"];

      // The optional version identifier for the access key. You can use this to keep
      // track of different iterations of your access key.
      optional string access_key_version = 3 [json_name = "access_key_version"];

      // The name of the cloud region of your origin. This is a free-form field with the
      // name of the region your cloud uses to host your origin.
      //
      // For example, "us-east-1" for AWS or "us-ashburn-1" for OCI.
      optional string origin_region = 4 [json_name = "origin_region"];
    }
  }

  message StrongSessionAffinityCookie {
    // Name of the cookie.
    optional string name = 1;

    // Path to set for the cookie.
    optional string path = 2;

    Ttl ttl = 3;

    message Ttl {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message TlsSettings {
    // Reference to the BackendAuthenticationConfig resource from the
    // networksecurity.googleapis.com namespace.
    //
    // Can be used in authenticating TLS connections to the backend, as specified by the
    // authenticationMode field.
    //
    // Can only be specified if authenticationMode is not NONE.
    optional string authentication_config = 1 [json_name = "authentication_config"];

    // Server Name Indication - see RFC3546 section 3.1. If set, the load balancer sends
    // this string as the SNI hostname in the
    //
    // TLS connection to the backend, and requires that this string match a Subject
    // Alternative Name (SAN) in the backend's
    //
    // server certificate. With a Regional Internet NEG backend, if the SNI is specified
    // here, the load balancer uses it
    //
    // regardless of whether the Regional Internet NEG is specified with FQDN or IP
    // address and port.
    optional string sni = 2;

    repeated SubjectAltNames subject_alt_names = 3 [json_name = "subject_alt_names"];

    message SubjectAltNames {
      // The SAN specified as a DNS Name.
      optional string dns_name = 1 [json_name = "dns_name"];

      // The SAN specified as a URI.
      optional string uniform_resource_identifier = 2 [json_name = "uniform_resource_identifier"];
    }
  }
}

// GoogleComputeBackendServiceSignedUrlKey version is 0
message GoogleComputeBackendServiceSignedUrlKey {
  // The backend service this signed URL key belongs.
  optional string backend_service = 1 [json_name = "backend_service"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  optional string id = 3;

  // 128-bit key value used for signing the URL. The key value must be a
  //
  // valid RFC 4648 Section 5 base64url encoded string.
  optional string key_value = 4 [json_name = "key_value"];

  // Name of the signed URL key.
  optional string name = 5;

  optional string project = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeBulkPerInstanceConfig version is 0
message GoogleComputeBulkPerInstanceConfig {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The instance group manager this instance config is part of.
  optional string instance_group_manager = 3 [json_name = "instance_group_manager"];

  optional string project = 4;

  // Zone where the containing instance group manager is located
  optional string zone = 5;

  repeated Instances instances = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Instances {
    // The name for this per-instance config and its corresponding instance.
    optional string name = 1;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeCrossSiteNetwork version is 0
message GoogleComputeCrossSiteNetwork {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 2;

  optional string id = 3;

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be
  //
  // 1-63 characters long, and comply with RFC1035. Specifically, the name must be
  // 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first
  //
  // character must be a lowercase letter, and all following characters must be a
  // dash,
  //
  // lowercase letter, or digit, except the last character, which cannot be a dash.
  optional string name = 4;

  optional string project = 5;

  repeated Timeouts timeouts = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeDisk version is 0
message GoogleComputeDisk {
  // The access mode of the disk.
  //
  // For example:
  //
  // * READ_WRITE_SINGLE: The default AccessMode, means the disk can be attached to
  // single instance in RW mode.
  //
  // * READ_WRITE_MANY: The AccessMode means the disk can be attached to multiple
  // instances in RW mode.
  //
  // * READ_ONLY_SINGLE: The AccessMode means the disk can be attached to multiple
  // instances in RO mode.
  //
  // The AccessMode is only valid for Hyperdisk disk types.
  optional string access_mode = 1 [json_name = "access_mode"];

  // The architecture of the disk. Values include 'X86_64', 'ARM64'.
  optional string architecture = 2;

  // If set to true, a snapshot of the disk will be created before it is destroyed.
  //
  // If your disk is encrypted with customer managed encryption keys these will be
  // reused for the snapshot creation.
  //
  // The name of the snapshot by default will be '{{disk-name}}-YYYYMMDD-HHmm'
  optional bool create_snapshot_before_destroy = 3 [json_name = "create_snapshot_before_destroy"];

  // This will set a custom name prefix for the snapshot that's created when the disk
  // is deleted.
  optional string create_snapshot_before_destroy_prefix = 4 [
    json_name = "create_snapshot_before_destroy_prefix"
  ];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 5 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 6 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 7;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string disk_id = 8 [json_name = "disk_id"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 9 [json_name = "effective_labels"];

  // Whether this disk is using confidential compute mode.
  //
  // Note: Only supported on hyperdisk skus, disk_encryption_key is required when
  // setting to true
  optional bool enable_confidential_compute = 10 [json_name = "enable_confidential_compute"];

  optional string id = 11;

  // The image from which to initialize this disk. This can be
  //
  // one of: the image's 'self_link', 'projects/{project}/global/images/{image}',
  //
  // 'projects/{project}/global/images/family/{family}', 'global/images/{image}',
  //
  // 'global/images/family/{family}', 'family/{family}', '{project}/{family}',
  //
  // '{project}/{image}', '{family}', or '{image}'. If referred by family, the
  //
  // images names must include the family name. If they don't, use the
  //
  // [google_compute_image data source](/docs/providers/google/d/compute_image.html).
  //
  // For instance, the image 'centos-6-v20180104' includes its family name 'centos-6'.
  //
  // These images can be referred by family name here.
  optional string image = 12;

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 13 [json_name = "label_fingerprint"];

  // Labels to apply to this disk.  A list of key->value pairs.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 14;

  // Last attach timestamp in RFC3339 text format.
  optional string last_attach_timestamp = 15 [json_name = "last_attach_timestamp"];

  // Last detach timestamp in RFC3339 text format.
  optional string last_detach_timestamp = 16 [json_name = "last_detach_timestamp"];

  // Any applicable license URI.
  repeated string licenses = 17;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 18;

  // Physical block size of the persistent disk, in bytes. If not present
  //
  // in a request, a default value is used. Currently supported sizes
  //
  // are 4096 and 16384, other sizes may be added in the future.
  //
  // If an unsupported value is requested, the error message will list
  //
  // the supported values for the caller's project.
  optional int64 physical_block_size_bytes = 19 [json_name = "physical_block_size_bytes"];

  optional string project = 20;

  // Indicates how many IOPS must be provisioned for the disk.
  //
  // Note: Updating currently is only supported by hyperdisk skus without the need to
  // delete and recreate the disk, hyperdisk
  //
  // allows for an update of IOPS every 4 hours. To update your hyperdisk more
  // frequently, you'll need to manually delete and recreate it
  optional int64 provisioned_iops = 21 [json_name = "provisioned_iops"];

  // Indicates how much Throughput must be provisioned for the disk.
  //
  // Note: Updating currently is only supported by hyperdisk skus without the need to
  // delete and recreate the disk, hyperdisk
  //
  // allows for an update of Throughput every 4 hours. To update your hyperdisk more
  // frequently, you'll need to manually delete and recreate it
  optional int64 provisioned_throughput = 22 [json_name = "provisioned_throughput"];

  optional string self_link = 23 [json_name = "self_link"];

  // Size of the persistent disk, specified in GB. You can specify this
  //
  // field when creating a persistent disk using the 'image' or
  //
  // 'snapshot' parameter, or specify it alone to create an empty
  //
  // persistent disk.
  //
  //
  // If you specify this field along with 'image' or 'snapshot',
  //
  // the value must not be less than the size of the image
  //
  // or the size of the snapshot.
  //
  //
  // ~>**NOTE** If you change the size, Terraform updates the disk size
  //
  // if upsizing is detected but recreates the disk if downsizing is requested.
  //
  // You can add 'lifecycle.prevent_destroy' in the config to prevent destroying
  //
  // and recreating.
  optional int64 size = 24;

  // The source snapshot used to create this disk. You can provide this as
  //
  // a partial or full URL to the resource. If the snapshot is in another
  //
  // project than this disk, you must supply a full URL. For example, the
  //
  // following are valid values:
  //
  //
  // https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot'
  //
  // * 'projects/project/global/snapshots/snapshot'
  //
  // * 'global/snapshots/snapshot'
  //
  // * 'snapshot'
  optional string snapshot = 25;

  // The source disk used to create this disk. You can provide this as a partial or
  // full URL to the resource.
  //
  // For example, the following are valid values:
  //
  //
  // ttps://www.googleapis.com/compute/v1/projects/{project}/zones/{zone}/disks/{disk}
  //
  // ://www.googleapis.com/compute/v1/projects/{project}/regions/{region}/disks/{disk}
  //
  // * projects/{project}/zones/{zone}/disks/{disk}
  //
  // * projects/{project}/regions/{region}/disks/{disk}
  //
  // * zones/{zone}/disks/{disk}
  //
  // * regions/{region}/disks/{disk}
  optional string source_disk = 26 [json_name = "source_disk"];

  // The ID value of the disk used to create this image. This value may
  //
  // be used to determine whether the image was taken from the current
  //
  // or a previous instance of a given disk name.
  optional string source_disk_id = 27 [json_name = "source_disk_id"];

  // The ID value of the image used to create this disk. This value
  //
  // identifies the exact image that was used to create this persistent
  //
  // disk. For example, if you created the persistent disk from an image
  //
  // that was later deleted and recreated under the same name, the source
  //
  // image ID would identify the exact version of the image that was used.
  optional string source_image_id = 28 [json_name = "source_image_id"];

  // The source instant snapshot used to create this disk. You can provide this as a
  // partial or full URL to the resource.
  //
  // For example, the following are valid values:
  //
  //
  // apis.com/compute/v1/projects/project/zones/zone/instantSnapshots/instantSnapshot'
  //
  // * 'projects/project/zones/zone/instantSnapshots/instantSnapshot'
  //
  // * 'zones/zone/instantSnapshots/instantSnapshot'
  optional string source_instant_snapshot = 29 [json_name = "source_instant_snapshot"];

  // The unique ID of the instant snapshot used to create this disk. This value
  // identifies
  //
  // the exact instant snapshot that was used to create this persistent disk.
  //
  // For example, if you created the persistent disk from an instant snapshot that was
  // later
  //
  // deleted and recreated under the same name, the source instant snapshot ID would
  // identify
  //
  // the exact version of the instant snapshot that was used.
  optional string source_instant_snapshot_id = 30 [json_name = "source_instant_snapshot_id"];

  // The unique ID of the snapshot used to create this disk. This value
  //
  // identifies the exact snapshot that was used to create this persistent
  //
  // disk. For example, if you created the persistent disk from a snapshot
  //
  // that was later deleted and recreated under the same name, the source
  //
  // snapshot ID would identify the exact version of the snapshot that was
  //
  // used.
  optional string source_snapshot_id = 31 [json_name = "source_snapshot_id"];

  // The full Google Cloud Storage URI where the disk image is stored.
  //
  // This file must be a gzip-compressed tarball whose name ends in .tar.gz or virtual
  // machine disk whose name ends in vmdk.
  //
  // Valid URIs may start with gs:// or https://storage.googleapis.com/.
  //
  // This flag is not optimized for creating multiple disks from a source storage
  // object.
  //
  // To create many disks from a source storage object, use gcloud compute images
  // import instead.
  optional string source_storage_object = 32 [json_name = "source_storage_object"];

  // The URL or the name of the storage pool in which the new disk is created.
  //
  // For example:
  //
  // gleapis.com/compute/v1/projects/{project}/zones/{zone}/storagePools/{storagePool}
  //
  // * /projects/{project}/zones/{zone}/storagePools/{storagePool}
  //
  // * /zones/{zone}/storagePools/{storagePool}
  //
  // * /{storagePool}
  optional string storage_pool = 33 [json_name = "storage_pool"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 34 [json_name = "terraform_labels"];

  // URL of the disk type resource describing which disk type to use to
  //
  // create the disk. Provide this when creating the disk.
  optional string type = 35;

  // Links to the users of the disk (attached instances) in form:
  //
  // project/zones/zone/instances/instance
  repeated string users = 36;

  // A reference to the zone where the disk resides.
  optional string zone = 37;

  AsyncPrimaryDisk async_primary_disk = 38 [json_name = "async_primary_disk"];

  DiskEncryptionKey disk_encryption_key = 39 [json_name = "disk_encryption_key"];

  repeated GuestOsFeatures guest_os_features = 40 [json_name = "guest_os_features"];

  Params params = 41;

  SourceImageEncryptionKey source_image_encryption_key = 42 [json_name = "source_image_encryption_key"];

  SourceSnapshotEncryptionKey source_snapshot_encryption_key = 43 [json_name = "source_snapshot_encryption_key"];

  repeated Timeouts timeouts = 44;

  google.protobuf.Value for_each = 45 [json_name = "for_each"];

  repeated string depends_on = 46 [json_name = "depends_on"];

  optional int32 count = 47;

  optional string provider = 48;

  terraform.v1.Lifecycle lifecycle = 49;

  message AsyncPrimaryDisk {
    // Primary disk for asynchronous disk replication.
    optional string disk = 1;
  }

  message DiskEncryptionKey {
    // The self link of the encryption key used to encrypt the disk. Also called
    // KmsKeyName
    //
    // in the cloud console. Your project's Compute Engine System service account
    //
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    //
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    //
    // See
    // isks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    //
    // If absent, the Compute Engine Service Agent service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit
    //
    // customer-supplied encryption key to either encrypt or decrypt
    //
    // this resource. You can provide either the rawKey or the rsaEncryptedKey.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    //
    // encryption key that protects this resource.
    optional string sha256 = 5;
  }

  message GuestOsFeatures {
    // The type of supported feature. Read [Enabling guest operating system
    // com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features)
    // to see a list of available options.
    optional string type = 1;
  }

  message Params {
    // Resource manager tags to be bound to the disk. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message SourceImageEncryptionKey {
    // The self link of the encryption key used to encrypt the disk. Also called
    // KmsKeyName
    //
    // in the cloud console. Your project's Compute Engine System service account
    //
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    //
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    //
    // See
    // isks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    //
    // If absent, the Compute Engine Service Agent service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    //
    // encryption key that protects this resource.
    optional string sha256 = 4;
  }

  message SourceSnapshotEncryptionKey {
    // The self link of the encryption key used to encrypt the disk. Also called
    // KmsKeyName
    //
    // in the cloud console. Your project's Compute Engine System service account
    //
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    //
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    //
    // See
    // isks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    //
    // If absent, the Compute Engine Service Agent service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    //
    // encryption key that protects this resource.
    optional string sha256 = 4;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeDiskAsyncReplication version is 0
message GoogleComputeDiskAsyncReplication {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // Primary disk for asynchronous replication.
  optional string primary_disk = 3 [json_name = "primary_disk"];

  SecondaryDisk secondary_disk = 4 [json_name = "secondary_disk"];

  repeated Timeouts timeouts = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message SecondaryDisk {
    // Secondary disk for asynchronous replication.
    optional string disk = 1;

    // Output-only. Status of replication on the secondary disk.
    optional string state = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeDiskIamBinding version is 0
message GoogleComputeDiskIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeDiskIamMember version is 0
message GoogleComputeDiskIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeDiskIamPolicy version is 0
message GoogleComputeDiskIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  optional string zone = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeDiskResourcePolicyAttachment version is 0
message GoogleComputeDiskResourcePolicyAttachment {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // The name of the disk in which the resource policies are attached to.
  optional string disk = 2;

  optional string id = 3;

  // The resource policy to be attached to the disk for scheduling snapshot
  //
  // creation. Do not specify the self link.
  optional string name = 4;

  optional string project = 5;

  // A reference to the zone where the disk resides.
  optional string zone = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeExternalVpnGateway version is 0
message GoogleComputeExternalVpnGateway {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 2;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 3 [json_name = "effective_labels"];

  optional string id = 4;

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 5 [json_name = "label_fingerprint"];

  // Labels for the external VPN gateway resource.
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 6;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 7;

  optional string project = 8;

  // Indicates the redundancy type of this external VPN gateway Possible values:
  // ["FOUR_IPS_REDUNDANCY", "SINGLE_IP_INTERNALLY_REDUNDANT", "TWO_IPS_REDUNDANCY"]
  optional string redundancy_type = 9 [json_name = "redundancy_type"];

  optional string self_link = 10 [json_name = "self_link"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 11 [json_name = "terraform_labels"];

  repeated Interface interface = 12;

  Params params = 13;

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Interface {
    // The numeric ID for this interface. Allowed values are based on the redundancy
    // type
    //
    // of this external VPN gateway
    //
    // * '0 - SINGLE_IP_INTERNALLY_REDUNDANT'
    //
    // * '0, 1 - TWO_IPS_REDUNDANCY'
    //
    // * '0, 1, 2, 3 - FOUR_IPS_REDUNDANCY'
    optional int64 id = 1;

    // IP address of the interface in the external VPN gateway.
    //
    // Only IPv4 is supported. This IP address can be either from
    //
    // your on-premise gateway or another Cloud provider's VPN gateway,
    //
    // it cannot be an IP address from Google Compute Engine.
    optional string ip_address = 2 [json_name = "ip_address"];

    // IPv6 address of the interface in the external VPN gateway. This IPv6
    //
    // address can be either from your on-premise gateway or another Cloud
    //
    // provider's VPN gateway, it cannot be an IP address from Google Compute
    //
    // Engine. Must specify an IPv6 address (not IPV4-mapped) using any format
    //
    // described in RFC 4291 (e.g. 2001:db8:0:0:2d9:51:0:0). The output format
    //
    // is RFC 5952 format (e.g. 2001:db8::2d9:51:0:0).
    optional string ipv6_address = 3 [json_name = "ipv6_address"];
  }

  message Params {
    // Resource manager tags to be bound to the ExternalVpnGateway. Tag keys and values
    // have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeFirewall version is 1
message GoogleComputeFirewall {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 3;

  // If destination ranges are specified, the firewall will apply only to
  //
  // traffic that has destination IP address in these ranges. These ranges
  //
  // must be expressed in CIDR format. IPv4 or IPv6 ranges are supported.
  repeated string destination_ranges = 4 [json_name = "destination_ranges"];

  // Direction of traffic to which this firewall applies; default is
  //
  // INGRESS. Note: For INGRESS traffic, one of 'source_ranges',
  //
  // 'source_tags' or 'source_service_accounts' is required. Possible values:
  // ["INGRESS", "EGRESS"]
  optional string direction = 5;

  // Denotes whether the firewall rule is disabled, i.e not applied to the
  //
  // network it is associated with. When set to true, the firewall rule is
  //
  // not enforced and the network behaves as if it did not exist. If this
  //
  // is unspecified, the firewall rule will be enabled.
  optional bool disabled = 6;

  // This field denotes whether to enable logging for a particular firewall rule. If
  // logging is enabled, logs will be exported to Stackdriver.
  optional bool enable_logging = 7 [json_name = "enable_logging"];

  optional string id = 8;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 9;

  // The name or self_link of the network to attach this firewall to.
  optional string network = 10;

  // Priority for this rule. This is an integer between 0 and 65535, both
  //
  // inclusive. When not specified, the value assumed is 1000. Relative
  //
  // priorities determine precedence of conflicting rules. Lower value of
  //
  // priority implies higher precedence (eg, a rule with priority 0 has
  //
  // higher precedence than a rule with priority 1). DENY rules take
  //
  // precedence over ALLOW rules having equal priority.
  optional int64 priority = 11;

  optional string project = 12;

  optional string self_link = 13 [json_name = "self_link"];

  // If source ranges are specified, the firewall will apply only to
  //
  // traffic that has source IP address in these ranges. These ranges must
  //
  // be expressed in CIDR format. One or both of sourceRanges and
  //
  // sourceTags may be set. If both properties are set, the firewall will
  //
  // apply to traffic that has source IP address within sourceRanges OR the
  //
  // source IP that belongs to a tag listed in the sourceTags property. The
  //
  // connection does not need to match both properties for the firewall to
  //
  // apply. IPv4 or IPv6 ranges are supported. For INGRESS traffic, one of
  //
  // 'source_ranges', 'source_tags' or 'source_service_accounts' is required.
  repeated string source_ranges = 14 [json_name = "source_ranges"];

  // If source service accounts are specified, the firewall will apply only
  //
  // to traffic originating from an instance with a service account in this
  //
  // list. Source service accounts cannot be used to control traffic to an
  //
  // instance's external IP address because service accounts are associated
  //
  // with an instance, not an IP address. sourceRanges can be set at the
  //
  // same time as sourceServiceAccounts. If both are set, the firewall will
  //
  // apply to traffic that has source IP address within sourceRanges OR the
  //
  // source IP belongs to an instance with service account listed in
  //
  // sourceServiceAccount. The connection does not need to match both
  //
  // properties for the firewall to apply. sourceServiceAccounts cannot be
  //
  // used at the same time as sourceTags or targetTags. For INGRESS traffic,
  //
  // one of 'source_ranges', 'source_tags' or 'source_service_accounts' is required.
  repeated string source_service_accounts = 15 [json_name = "source_service_accounts"];

  // If source tags are specified, the firewall will apply only to traffic
  //
  // with source IP that belongs to a tag listed in source tags. Source
  //
  // tags cannot be used to control traffic to an instance's external IP
  //
  // address. Because tags are associated with an instance, not an IP
  //
  // address. One or both of sourceRanges and sourceTags may be set. If
  //
  // both properties are set, the firewall will apply to traffic that has
  //
  // source IP address within sourceRanges OR the source IP that belongs to
  //
  // a tag listed in the sourceTags property. The connection does not need
  //
  // to match both properties for the firewall to apply. For INGRESS traffic,
  //
  // one of 'source_ranges', 'source_tags' or 'source_service_accounts' is required.
  repeated string source_tags = 16 [json_name = "source_tags"];

  // A list of service accounts indicating sets of instances located in the
  //
  // network that may make network connections as specified in allowed[].
  //
  // targetServiceAccounts cannot be used at the same time as targetTags or
  //
  // sourceTags. If neither targetServiceAccounts nor targetTags are
  //
  // specified, the firewall rule applies to all instances on the specified
  //
  // network.
  repeated string target_service_accounts = 17 [json_name = "target_service_accounts"];

  // A list of instance tags indicating sets of instances located in the
  //
  // network that may make network connections as specified in allowed[].
  //
  // If no targetTags are specified, the firewall rule applies to all
  //
  // instances on the specified network.
  repeated string target_tags = 18 [json_name = "target_tags"];

  repeated Allow allow = 19;

  repeated Deny deny = 20;

  LogConfig log_config = 21 [json_name = "log_config"];

  Params params = 22;

  repeated Timeouts timeouts = 23;

  google.protobuf.Value for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  optional int32 count = 26;

  optional string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message Allow {
    // An optional list of ports to which this rule applies. This field
    //
    // is only applicable for UDP or TCP protocol. Each entry must be
    //
    // either an integer or a range. If not specified, this rule
    //
    // applies to connections through any port.
    //
    //
    // Example inputs include: [22], [80, 443], and
    //
    // ["12345-12349"].
    repeated string ports = 1;

    // The IP protocol to which this rule applies. The protocol type is
    //
    // required when creating a firewall rule. This value can either be
    //
    // one of the following well known protocol strings (tcp, udp,
    //
    // icmp, esp, ah, sctp, ipip, all), or the IP protocol number.
    optional string protocol = 2;
  }

  message Deny {
    // An optional list of ports to which this rule applies. This field
    //
    // is only applicable for UDP or TCP protocol. Each entry must be
    //
    // either an integer or a range. If not specified, this rule
    //
    // applies to connections through any port.
    //
    //
    // Example inputs include: [22], [80, 443], and
    //
    // ["12345-12349"].
    repeated string ports = 1;

    // The IP protocol to which this rule applies. The protocol type is
    //
    // required when creating a firewall rule. This value can either be
    //
    // one of the following well known protocol strings (tcp, udp,
    //
    // icmp, esp, ah, sctp, ipip, all), or the IP protocol number.
    optional string protocol = 2;
  }

  message LogConfig {
    // This field denotes whether to include or exclude metadata for firewall logs.
    // Possible values: ["EXCLUDE_ALL_METADATA", "INCLUDE_ALL_METADATA"]
    optional string metadata = 1;
  }

  message Params {
    // Resource manager tags to be bound to the firewall. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456. The field is ignored when empty.
    //
    // The field is immutable and causes resource replacement when mutated. This field
    // is only
    //
    // set at create time and modifying this field after creation will trigger
    // recreation.
    //
    // To apply tags to an existing resource, see the google_tags_tag_binding resource.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeFirewallPolicy version is 0
message GoogleComputeFirewallPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 3;

  // Fingerprint of the resource. This field is used internally during updates of this
  // resource.
  optional string fingerprint = 4;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string firewall_policy_id = 5 [json_name = "firewall_policy_id"];

  optional string id = 6;

  // Name of the resource. It is a numeric ID allocated by GCP which uniquely
  // identifies the Firewall Policy.
  optional string name = 7;

  // The parent of the firewall policy.
  optional string parent = 8;

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed
  // a set number of tuples.
  optional int64 rule_tuple_count = 9 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  optional string self_link = 10 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  optional string self_link_with_id = 11 [json_name = "self_link_with_id"];

  // User-provided name of the Organization firewall policy. The name should be unique
  // in the organization in which the firewall policy is created.
  //
  // This field is not applicable to network firewall policies. This name must be set
  // on creation and cannot be changed. The name must be 1-63 characters long, and
  // comply with RFC1035.
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  // expression [a-z]([-a-z0-9]*[a-z0-9])? which means the first character must be a
  // lowercase letter, and all following characters must be a dash, lowercase letter,
  // or digit, except the last character, which cannot be a dash.
  optional string short_name = 12 [json_name = "short_name"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeFirewallPolicyAssociation version is 0
message GoogleComputeFirewallPolicyAssociation {
  // The target that the firewall policy is attached to.
  optional string attachment_target = 1 [json_name = "attachment_target"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // The firewall policy of the resource.
  //
  //
  // This field can be updated to refer to a different Firewall Policy, which will
  // create a new association from that new
  //
  // firewall policy with the flag to override the existing attachmentTarget's policy
  // association.
  //
  //
  // **Note** Due to potential risks with this operation it is *highly* recommended to
  // use the 'create_before_destroy' life cycle option
  //
  // on your exisiting firewall policy so as to prevent a situation where your
  // attachment target has no associated policy.
  optional string firewall_policy = 3 [json_name = "firewall_policy"];

  optional string id = 4;

  // The name for an association.
  optional string name = 5;

  // The short name of the firewall policy of the association.
  optional string short_name = 6 [json_name = "short_name"];

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeFirewallPolicyIamBinding version is 0
message GoogleComputeFirewallPolicyIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string role = 5;

  Condition condition = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeFirewallPolicyIamMember version is 0
message GoogleComputeFirewallPolicyIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string role = 5;

  Condition condition = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeFirewallPolicyIamPolicy version is 0
message GoogleComputeFirewallPolicyIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  optional int32 count = 7;

  optional string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;
}

// GoogleComputeFirewallPolicyRule version is 0
message GoogleComputeFirewallPolicyRule {
  // The Action to perform when the client connection triggers the rule. Valid actions
  // are "allow", "deny", "goto_next" and "apply_security_profile_group".
  optional string action = 1;

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description for this resource.
  optional string description = 4;

  // The direction in which this rule applies. Possible values: ["INGRESS", "EGRESS"]
  optional string direction = 5;

  // Denotes whether the firewall policy rule is disabled.
  //
  // When set to true, the firewall policy rule is not enforced and traffic behaves as
  // if it did not exist.
  //
  // If this is unspecified, the firewall policy rule will be enabled.
  optional bool disabled = 6;

  // Denotes whether to enable logging for a particular rule.
  //
  // If logging is enabled, logs will be exported to the configured export destination
  // in Stackdriver.
  //
  // Logs may be exported to BigQuery or Pub/Sub.
  //
  // Note: you cannot enable logging on "goto_next" rules.
  optional bool enable_logging = 7 [json_name = "enable_logging"];

  // The firewall policy of the resource.
  optional string firewall_policy = 8 [json_name = "firewall_policy"];

  optional string id = 9;

  // Type of the resource. Always 'compute#firewallPolicyRule' for firewall policy
  // rules
  optional string kind = 10;

  // An integer indicating the priority of a rule in the list.
  //
  // The priority must be a positive value between 0 and 2147483647.
  //
  // Rules are evaluated from highest to lowest priority where 0 is the highest
  // priority and 2147483647 is the lowest prority.
  optional int64 priority = 11;

  // Calculation of the complexity of a single firewall policy rule.
  optional int64 rule_tuple_count = 12 [json_name = "rule_tuple_count"];

  // A fully-qualified URL of a SecurityProfile resource instance.
  //
  // Example:
  // ts/{project}/locations/{location}/securityProfileGroups/my-security-profile-group
  //
  // Must be specified if action = 'apply_security_profile_group' and cannot be
  // specified for other actions.
  optional string security_profile_group = 13 [json_name = "security_profile_group"];

  // A list of network resource URLs to which this rule applies.
  //
  // This field allows you to control which network's VMs get this rule.
  //
  // If this field is left blank, all VMs within the organization will receive the
  // rule.
  repeated string target_resources = 14 [json_name = "target_resources"];

  // A list of service accounts indicating the sets of instances that are applied with
  // this rule.
  repeated string target_service_accounts = 15 [json_name = "target_service_accounts"];

  // Boolean flag indicating if the traffic should be TLS decrypted.
  //
  // Can be set only if action = 'apply_security_profile_group' and cannot be set for
  // other actions.
  optional bool tls_inspect = 16 [json_name = "tls_inspect"];

  Match match = 17;

  repeated TargetSecureTags target_secure_tags = 18 [json_name = "target_secure_tags"];

  repeated Timeouts timeouts = 19;

  google.protobuf.Value for_each = 20 [json_name = "for_each"];

  repeated string depends_on = 21 [json_name = "depends_on"];

  optional int32 count = 22;

  optional string provider = 23;

  terraform.v1.Lifecycle lifecycle = 24;

  message Match {
    // Address groups which should be matched against the traffic destination. Maximum
    // number of destination address groups is 10.
    repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

    // Fully Qualified Domain Name (FQDN) which should be matched against traffic
    // destination. Maximum number of destination fqdn allowed is 100.
    repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

    // CIDR IP address range. Maximum number of destination CIDR IP ranges allowed is
    // 5000.
    repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

    // Network context of the traffic destination. Possible values: ["UNSPECIFIED",
    // "INTERNET", "INTRA_VPC", "NON_INTERNET", "VPC_NETWORKS"]
    optional string dest_network_context = 4 [json_name = "dest_network_context"];

    // Region codes whose IP addresses will be used to match for destination of traffic.
    // Should be specified as 2 letter country code defined as per ISO 3166 alpha-2
    // country codes. ex."US" Maximum number of dest region codes allowed is 5000.
    repeated string dest_region_codes = 5 [json_name = "dest_region_codes"];

    // Names of Network Threat Intelligence lists. The IPs in these lists will be
    // matched against traffic destination.
    repeated string dest_threat_intelligences = 6 [json_name = "dest_threat_intelligences"];

    // Address groups which should be matched against the traffic source. Maximum number
    // of source address groups is 10.
    repeated string src_address_groups = 7 [json_name = "src_address_groups"];

    // Fully Qualified Domain Name (FQDN) which should be matched against traffic
    // source. Maximum number of source fqdn allowed is 100.
    repeated string src_fqdns = 8 [json_name = "src_fqdns"];

    // CIDR IP address range. Maximum number of source CIDR IP ranges allowed is 5000.
    repeated string src_ip_ranges = 9 [json_name = "src_ip_ranges"];

    // Network context of the traffic source. Possible values: ["UNSPECIFIED",
    // "INTERNET", "INTRA_VPC", "NON_INTERNET", "VPC_NETWORKS"]
    optional string src_network_context = 10 [json_name = "src_network_context"];

    // Networks of the traffic source. It can be either a full or partial url.
    repeated string src_networks = 11 [json_name = "src_networks"];

    // Region codes whose IP addresses will be used to match for source of traffic.
    // Should be specified as 2 letter country code defined as per ISO 3166 alpha-2
    // country codes. ex."US" Maximum number of source region codes allowed is 5000.
    repeated string src_region_codes = 12 [json_name = "src_region_codes"];

    // Names of Network Threat Intelligence lists. The IPs in these lists will be
    // matched against traffic source.
    repeated string src_threat_intelligences = 13 [json_name = "src_threat_intelligences"];

    repeated Layer4Configs layer4_configs = 14 [json_name = "layer4_configs"];

    repeated SrcSecureTags src_secure_tags = 15 [json_name = "src_secure_tags"];

    message Layer4Configs {
      // The IP protocol to which this rule applies. The protocol type is required when
      // creating a firewall rule.
      //
      // This value can either be one of the following well known protocol strings (tcp,
      // udp, icmp, esp, ah, ipip, sctp), or the IP protocol number.
      optional string ip_protocol = 1 [json_name = "ip_protocol"];

      // An optional list of ports to which this rule applies. This field is only
      // applicable for UDP or TCP protocol. Each entry must be either an integer or a
      // range. If not specified, this rule applies to connections through any port.
      //
      // Example inputs include: ["22"], ["80","443"], and ["12345-12349"].
      repeated string ports = 2;
    }

    message SrcSecureTags {
      // Name of the secure tag, created with TagManager's TagValue API.
      optional string name = 1;

      // State of the secure tag, either EFFECTIVE or INEFFECTIVE. A secure tag is
      // INEFFECTIVE when it is deleted or its network is deleted.
      optional string state = 2;
    }
  }

  message TargetSecureTags {
    // Name of the secure tag, created with TagManager's TagValue API.
    optional string name = 1;

    // State of the secure tag, either EFFECTIVE or INEFFECTIVE. A secure tag is
    // INEFFECTIVE when it is deleted or its network is deleted.
    optional string state = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeFirewallPolicyWithRules version is 0
message GoogleComputeFirewallPolicyWithRules {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Fingerprint of the resource. This field is used internally during updates of this
  // resource.
  optional string fingerprint = 4;

  optional string id = 5;

  // The parent of this FirewallPolicy in the Cloud Resource Hierarchy.
  //
  // Format: organizations/{organization_id} or folders/{folder_id}
  optional string parent = 6;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string policy_id = 7 [json_name = "policy_id"];

  // A list of pre-define firewall policy rules.
  repeated PredefinedRules predefined_rules = 8 [json_name = "predefined_rules"];

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed
  // a set number of tuples.
  optional int64 rule_tuple_count = 9 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  optional string self_link = 10 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  optional string self_link_with_id = 11 [json_name = "self_link_with_id"];

  // A textual name of the security policy.
  optional string short_name = 12 [json_name = "short_name"];

  repeated Rule rule = 13;

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message PredefinedRules {
    // action: string
    optional string action = 1;

    // description: string
    optional string description = 2;

    // direction: string
    optional string direction = 3;

    // disabled: bool
    optional bool disabled = 4;

    // enable_logging: bool
    optional bool enable_logging = 5 [json_name = "enable_logging"];

    // match: list of object
    repeated Match match = 6;

    // priority: number
    optional int64 priority = 7;

    // rule_name: string
    optional string rule_name = 8 [json_name = "rule_name"];

    // security_profile_group: string
    optional string security_profile_group = 9 [json_name = "security_profile_group"];

    // target_resources: list of string
    repeated string target_resources = 10 [json_name = "target_resources"];

    // target_secure_tag: list of object
    repeated TargetSecureTag target_secure_tag = 11 [json_name = "target_secure_tag"];

    // target_service_accounts: list of string
    repeated string target_service_accounts = 12 [json_name = "target_service_accounts"];

    // tls_inspect: bool
    optional bool tls_inspect = 13 [json_name = "tls_inspect"];

    message Match {
      // dest_address_groups: list of string
      repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

      // dest_fqdns: list of string
      repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

      // dest_ip_ranges: list of string
      repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

      // dest_region_codes: list of string
      repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

      // dest_threat_intelligences: list of string
      repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

      // layer4_config: list of object
      repeated Layer4Config layer4_config = 6 [json_name = "layer4_config"];

      // src_address_groups: list of string
      repeated string src_address_groups = 7 [json_name = "src_address_groups"];

      // src_fqdns: list of string
      repeated string src_fqdns = 8 [json_name = "src_fqdns"];

      // src_ip_ranges: list of string
      repeated string src_ip_ranges = 9 [json_name = "src_ip_ranges"];

      // src_region_codes: list of string
      repeated string src_region_codes = 10 [json_name = "src_region_codes"];

      // src_secure_tag: list of object
      repeated SrcSecureTag src_secure_tag = 11 [json_name = "src_secure_tag"];

      // src_threat_intelligences: list of string
      repeated string src_threat_intelligences = 12 [json_name = "src_threat_intelligences"];

      message Layer4Config {
        // ip_protocol: string
        optional string ip_protocol = 1 [json_name = "ip_protocol"];

        // ports: list of string
        repeated string ports = 2;
      }

      message SrcSecureTag {
        // name: string
        optional string name = 1;

        // state: string
        optional string state = 2;
      }
    }

    message TargetSecureTag {
      // name: string
      optional string name = 1;

      // state: string
      optional string state = 2;
    }
  }

  message Rule {
    // The Action to perform when the client connection triggers the rule. Can currently
    // be either
    //
    // "allow", "deny", "apply_security_profile_group" or "goto_next".
    optional string action = 1;

    // A description of the rule.
    optional string description = 2;

    // The direction in which this rule applies. If unspecified an INGRESS rule is
    // created. Possible values: ["INGRESS", "EGRESS"]
    optional string direction = 3;

    // Denotes whether the firewall policy rule is disabled. When set to true,
    //
    // the firewall policy rule is not enforced and traffic behaves as if it did
    //
    // not exist. If this is unspecified, the firewall policy rule will be
    //
    // enabled.
    optional bool disabled = 4;

    // Denotes whether to enable logging for a particular rule.
    //
    // If logging is enabled, logs will be exported to the
    //
    // configured export destination in Stackdriver.
    optional bool enable_logging = 5 [json_name = "enable_logging"];

    // An integer indicating the priority of a rule in the list. The priority must be a
    // value
    //
    // between 0 and 2147483647. Rules are evaluated from highest to lowest priority
    // where 0 is the
    //
    // highest priority and 2147483647 is the lowest priority.
    optional int64 priority = 6;

    // An optional name for the rule. This field is not a unique identifier
    //
    // and can be updated.
    optional string rule_name = 7 [json_name = "rule_name"];

    // A fully-qualified URL of a SecurityProfile resource instance.
    //
    // Example:
    //
    // ts/{project}/locations/{location}/securityProfileGroups/my-security-profile-group
    //
    // Must be specified if action is 'apply_security_profile_group'.
    optional string security_profile_group = 8 [json_name = "security_profile_group"];

    // A list of network resource URLs to which this rule applies.
    //
    // This field allows you to control which network's VMs get
    //
    // this rule. If this field is left blank, all VMs
    //
    // within the organization will receive the rule.
    repeated string target_resources = 9 [json_name = "target_resources"];

    // A list of service accounts indicating the sets of
    //
    // instances that are applied with this rule.
    repeated string target_service_accounts = 10 [json_name = "target_service_accounts"];

    // Boolean flag indicating if the traffic should be TLS decrypted.
    //
    // It can be set only if action = 'apply_security_profile_group' and cannot be set
    // for other actions.
    optional bool tls_inspect = 11 [json_name = "tls_inspect"];

    Match match = 12;

    repeated TargetSecureTag target_secure_tag = 13 [json_name = "target_secure_tag"];

    message Match {
      // Address groups which should be matched against the traffic destination.
      //
      // Maximum number of destination address groups is 10.
      repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

      // Fully Qualified Domain Name (FQDN) which should be matched against
      //
      // traffic destination. Maximum number of destination fqdn allowed is 100.
      repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

      // Destination IP address range in CIDR format. Required for
      //
      // EGRESS rules.
      repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

      // Region codes whose IP addresses will be used to match for destination
      //
      // of traffic. Should be specified as 2 letter country code defined as per
      //
      // ISO 3166 alpha-2 country codes. ex."US"
      //
      // Maximum number of destination region codes allowed is 5000.
      repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

      // Names of Network Threat Intelligence lists.
      //
      // The IPs in these lists will be matched against traffic destination.
      repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

      // Address groups which should be matched against the traffic source.
      //
      // Maximum number of source address groups is 10.
      repeated string src_address_groups = 6 [json_name = "src_address_groups"];

      // Fully Qualified Domain Name (FQDN) which should be matched against
      //
      // traffic source. Maximum number of source fqdn allowed is 100.
      repeated string src_fqdns = 7 [json_name = "src_fqdns"];

      // Source IP address range in CIDR format. Required for
      //
      // INGRESS rules.
      repeated string src_ip_ranges = 8 [json_name = "src_ip_ranges"];

      // Region codes whose IP addresses will be used to match for source
      //
      // of traffic. Should be specified as 2 letter country code defined as per
      //
      // ISO 3166 alpha-2 country codes. ex."US"
      //
      // Maximum number of source region codes allowed is 5000.
      repeated string src_region_codes = 9 [json_name = "src_region_codes"];

      // Names of Network Threat Intelligence lists.
      //
      // The IPs in these lists will be matched against traffic source.
      repeated string src_threat_intelligences = 10 [json_name = "src_threat_intelligences"];

      repeated Layer4Config layer4_config = 11 [json_name = "layer4_config"];

      repeated SrcSecureTag src_secure_tag = 12 [json_name = "src_secure_tag"];

      message Layer4Config {
        // The IP protocol to which this rule applies. The protocol
        //
        // type is required when creating a firewall rule.
        //
        // This value can either be one of the following well
        //
        // known protocol strings (tcp, udp, icmp, esp, ah, ipip, sctp),
        //
        // or the IP protocol number.
        optional string ip_protocol = 1 [json_name = "ip_protocol"];

        // An optional list of ports to which this rule applies. This field
        //
        // is only applicable for UDP or TCP protocol. Each entry must be
        //
        // either an integer or a range. If not specified, this rule
        //
        // applies to connections through any port.
        //
        // Example inputs include: ["22"], ["80","443"], and
        //
        // ["12345-12349"].
        repeated string ports = 2;
      }

      message SrcSecureTag {
        // Name of the secure tag, created with TagManager's TagValue API.
        //
        // @pattern tagValues/[0-9]+
        optional string name = 1;

        // [Output Only] State of the secure tag, either 'EFFECTIVE' or
        //
        // 'INEFFECTIVE'. A secure tag is 'INEFFECTIVE' when it is deleted
        //
        // or its network is deleted.
        optional string state = 2;
      }
    }

    message TargetSecureTag {
      // Name of the secure tag, created with TagManager's TagValue API.
      //
      // @pattern tagValues/[0-9]+
      optional string name = 1;

      // [Output Only] State of the secure tag, either 'EFFECTIVE' or
      //
      // 'INEFFECTIVE'. A secure tag is 'INEFFECTIVE' when it is deleted
      //
      // or its network is deleted.
      optional string state = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeForwardingRule version is 0
message GoogleComputeForwardingRule {
  // The 'ports', 'portRange', and 'allPorts' fields are mutually exclusive.
  //
  // Only packets addressed to ports in the specified range will be forwarded
  //
  // to the backends configured with this forwarding rule.
  //
  //
  // The 'allPorts' field has the following limitations:
  //
  // * It requires that the forwarding rule 'IPProtocol' be TCP, UDP, SCTP, or
  //
  // L3_DEFAULT.
  //
  // * It's applicable only to the following products: internal passthrough
  //
  // Network Load Balancers, backend service-based external passthrough Network
  //
  // Load Balancers, and internal and external protocol forwarding.
  //
  // * Set this field to true to allow packets addressed to any port or packets
  //
  // lacking destination port information (for example, UDP fragments after the
  //
  // first fragment) to be forwarded to the backends configured with this
  //
  // forwarding rule. The L3_DEFAULT protocol requires 'allPorts' be set to
  //
  // true.
  optional bool all_ports = 1 [json_name = "all_ports"];

  // This field is used along with the 'backend_service' field for
  //
  // internal load balancing or with the 'target' field for internal
  //
  // TargetInstance.
  //
  //
  // If the field is set to 'TRUE', clients can access ILB from all
  //
  // regions.
  //
  //
  // Otherwise only allows access from clients in the same region as the
  //
  // internal load balancer.
  optional bool allow_global_access = 2 [json_name = "allow_global_access"];

  // This is used in PSC consumer ForwardingRule to control whether the PSC endpoint
  // can be accessed from another region.
  optional bool allow_psc_global_access = 3 [json_name = "allow_psc_global_access"];

  // Identifies the backend service to which the forwarding rule sends traffic.
  //
  //
  // Required for Internal TCP/UDP Load Balancing and Network Load Balancing;
  //
  // must be omitted for all other load balancer types.
  optional string backend_service = 4 [json_name = "backend_service"];

  // [Output Only] The URL for the corresponding base Forwarding Rule. By base
  // Forwarding Rule, we mean the Forwarding Rule that has the same IP address,
  // protocol, and port settings with the current Forwarding Rule, but without
  // sourceIPRanges specified. Always empty if the current Forwarding Rule does not
  // have sourceIPRanges specified.
  optional string base_forwarding_rule = 5 [json_name = "base_forwarding_rule"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 6 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 7 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 8;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 9 [json_name = "effective_labels"];

  // The unique identifier number for the resource. This identifier is defined by the
  // server.
  optional int64 forwarding_rule_id = 10 [json_name = "forwarding_rule_id"];

  optional string id = 11;

  // IP address for which this forwarding rule accepts traffic. When a client
  //
  // sends traffic to this IP address, the forwarding rule directs the traffic
  //
  // to the referenced 'target' or 'backendService'.
  //
  //
  // While creating a forwarding rule, specifying an 'IPAddress' is
  //
  // required under the following circumstances:
  //
  //
  // * When the 'target' is set to 'targetGrpcProxy' and
  //
  // 'validateForProxyless' is set to 'true', the
  //
  // 'IPAddress' should be set to '0.0.0.0'.
  //
  // * When the 'target' is a Private Service Connect Google APIs
  //
  // bundle, you must specify an 'IPAddress'.
  //
  //
  // Otherwise, you can optionally specify an IP address that references an
  //
  // existing static (reserved) IP address resource. When omitted, Google Cloud
  //
  // assigns an ephemeral IP address.
  //
  //
  // Use one of the following formats to specify an IP address while creating a
  //
  // forwarding rule:
  //
  //
  // * IP address number, as in '100.1.2.3'
  //
  // * IPv6 address range, as in '2600:1234::/96'
  //
  // * Full resource URL, as in
  //
  // gleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name'
  //
  // * Partial URL or by name, as in:
  //
  // * 'projects/project_id/regions/region/addresses/address-name'
  //
  // * 'regions/region/addresses/address-name'
  //
  // * 'global/addresses/address-name'
  //
  // * 'address-name'
  //
  //
  // The forwarding rule's 'target' or 'backendService',
  //
  // and in most cases, also the 'loadBalancingScheme', determine the
  //
  // type of IP address that you can use. For detailed information, see
  //
  // [IP address
  //
  // ogle.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  //
  //
  // When reading an 'IPAddress', the API always returns the IP
  //
  // address number.
  optional string ip_address = 12 [json_name = "ip_address"];

  // Resource reference of a PublicDelegatedPrefix. The PDP must be a sub-PDP
  //
  // in EXTERNAL_IPV6_FORWARDING_RULE_CREATION mode.
  //
  // Use one of the following formats to specify a sub-PDP when creating an
  //
  // IPv6 NetLB forwarding rule using BYOIP:
  //
  // Full resource URL, as in:
  //
  // ojects/{{projectId}}/regions/{{region}}/publicDelegatedPrefixes/{{sub-pdp-name}}'
  //
  // Partial URL, as in:
  //
  // 'projects/{{projectId}}/regions/region/publicDelegatedPrefixes/{{sub-pdp-name}}'
  //
  // * 'regions/{{region}}/publicDelegatedPrefixes/{{sub-pdp-name}}'
  optional string ip_collection = 13 [json_name = "ip_collection"];

  // The IP protocol to which this rule applies.
  //
  //
  // For protocol forwarding, valid
  //
  // options are 'TCP', 'UDP', 'ESP',
  //
  // 'AH', 'SCTP', 'ICMP' and
  //
  // 'L3_DEFAULT'.
  //
  //
  // The valid IP protocols are different for different load balancing products
  //
  // as described in [Load balancing
  //
  // m/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends).
  //
  //
  // A Forwarding Rule with protocol L3_DEFAULT can attach with target instance or
  //
  // backend service with UNSPECIFIED protocol.
  //
  // A forwarding rule with "L3_DEFAULT" IPProtocal cannot be attached to a backend
  // service with TCP or UDP. Possible values: ["TCP", "UDP", "ESP", "AH", "SCTP",
  // "ICMP", "L3_DEFAULT"]
  optional string ip_protocol = 14 [json_name = "ip_protocol"];

  // The IP address version that will be used by this forwarding rule.
  //
  // Valid options are IPV4 and IPV6.
  //
  //
  // If not set, the IPv4 address will be used by default. Possible values: ["IPV4",
  // "IPV6"]
  optional string ip_version = 15 [json_name = "ip_version"];

  // Indicates whether or not this load balancer can be used as a collector for
  //
  // packet mirroring. To prevent mirroring loops, instances behind this
  //
  // load balancer will not have their traffic mirrored even if a
  //
  // 'PacketMirroring' rule applies to them.
  //
  //
  // This can only be set to true for load balancers that have their
  //
  // 'loadBalancingScheme' set to 'INTERNAL'.
  optional bool is_mirroring_collector = 16 [json_name = "is_mirroring_collector"];

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 17 [json_name = "label_fingerprint"];

  // Labels to apply to this forwarding rule.  A list of key->value pairs.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 18;

  // Specifies the forwarding rule type.
  //
  //
  // Note that an empty string value ('""') is also supported for some use
  //
  // cases, for example PSC (private service connection) regional forwarding
  //
  // rules.
  //
  //
  // For more information about forwarding rules, refer to
  //
  // [Forwarding rule
  // concepts](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts).
  // Default value: "EXTERNAL" Possible values: ["EXTERNAL", "EXTERNAL_MANAGED",
  // "INTERNAL", "INTERNAL_MANAGED"]
  optional string load_balancing_scheme = 19 [json_name = "load_balancing_scheme"];

  // Name of the resource; provided by the client when the resource is created.
  //
  // The name must be 1-63 characters long, and comply with
  //
  // [RFC1035](https://www.ietf.org/rfc/rfc1035.txt).
  //
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  //
  // expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  //
  // character must be a lowercase letter, and all following characters must
  //
  // be a dash, lowercase letter, or digit, except the last character, which
  //
  // cannot be a dash.
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  //
  // APIs, the forwarding rule name must be a 1-20 characters string with
  //
  // lowercase letters and numbers and must start with a letter.
  optional string name = 20;

  // This field is not used for external load balancing.
  //
  //
  // For Internal TCP/UDP Load Balancing, this field identifies the network that
  //
  // the load balanced IP should belong to for this Forwarding Rule.
  //
  // If the subnetwork is specified, the network of the subnetwork will be used.
  //
  // If neither subnetwork nor this field is specified, the default network will
  //
  // be used.
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  //
  // APIs, a network must be provided.
  optional string network = 21;

  // This signifies the networking tier used for configuring
  //
  // this load balancer and can only take the following values:
  //
  // 'PREMIUM', 'STANDARD'.
  //
  //
  // For regional ForwardingRule, the valid values are 'PREMIUM' and
  //
  // 'STANDARD'. For GlobalForwardingRule, the valid value is
  //
  // 'PREMIUM'.
  //
  //
  // If this field is not specified, it is assumed to be 'PREMIUM'.
  //
  // If 'IPAddress' is specified, this value must be equal to the
  //
  // networkTier of the Address. Possible values: ["PREMIUM", "STANDARD"]
  optional string network_tier = 22 [json_name = "network_tier"];

  // This is used in PSC consumer ForwardingRule to control whether it should try to
  // auto-generate a DNS zone or not. Non-PSC forwarding rules do not use this field.
  optional bool no_automate_dns_zone = 23 [json_name = "no_automate_dns_zone"];

  // The 'ports', 'portRange', and 'allPorts' fields are mutually exclusive.
  //
  // Only packets addressed to ports in the specified range will be forwarded
  //
  // to the backends configured with this forwarding rule.
  //
  //
  // The 'portRange' field has the following limitations:
  //
  // * It requires that the forwarding rule 'IPProtocol' be TCP, UDP, or SCTP,
  //
  // and
  //
  // * It's applicable only to the following products: external passthrough
  //
  // Network Load Balancers, internal and external proxy Network Load
  //
  // Balancers, internal and external Application Load Balancers, external
  //
  // protocol forwarding, and Classic VPN.
  //
  // * Some products have restrictions on what ports can be used. See
  //
  // [port
  // loud.google.com/load-balancing/docs/forwarding-rule-concepts#port_specifications)
  //
  // for details.
  //
  //
  // For external forwarding rules, two or more forwarding rules cannot use the
  //
  // same '[IPAddress, IPProtocol]' pair, and cannot have overlapping
  //
  // 'portRange's.
  //
  //
  // For internal forwarding rules within the same VPC network, two or more
  //
  // forwarding rules cannot use the same '[IPAddress, IPProtocol]' pair, and
  //
  // cannot have overlapping 'portRange's.
  //
  //
  // @pattern: \d+(?:-\d+)?
  optional string port_range = 24 [json_name = "port_range"];

  // The 'ports', 'portRange', and 'allPorts' fields are mutually exclusive.
  //
  // Only packets addressed to ports in the specified range will be forwarded
  //
  // to the backends configured with this forwarding rule.
  //
  //
  // The 'ports' field has the following limitations:
  //
  // * It requires that the forwarding rule 'IPProtocol' be TCP, UDP, or SCTP,
  //
  // and
  //
  // * It's applicable only to the following products: internal passthrough
  //
  // Network Load Balancers, backend service-based external passthrough Network
  //
  // Load Balancers, and internal protocol forwarding.
  //
  // * You can specify a list of up to five ports by number, separated by
  //
  // commas. The ports can be contiguous or discontiguous.
  //
  //
  // For external forwarding rules, two or more forwarding rules cannot use the
  //
  // same '[IPAddress, IPProtocol]' pair if they share at least one port
  //
  // number.
  //
  //
  // For internal forwarding rules within the same VPC network, two or more
  //
  // forwarding rules cannot use the same '[IPAddress, IPProtocol]' pair if
  //
  // they share at least one port number.
  //
  //
  // @pattern: \d+(?:-\d+)?
  repeated string ports = 25;

  optional string project = 26;

  // The PSC connection id of the PSC Forwarding Rule.
  optional string psc_connection_id = 27 [json_name = "psc_connection_id"];

  // The PSC connection status of the PSC Forwarding Rule. Possible values:
  // 'STATUS_UNSPECIFIED', 'PENDING', 'ACCEPTED', 'REJECTED', 'CLOSED'
  optional string psc_connection_status = 28 [json_name = "psc_connection_status"];

  // This is used in PSC consumer ForwardingRule to make terraform recreate the
  // ForwardingRule when the status is closed
  optional bool recreate_closed_psc = 29 [json_name = "recreate_closed_psc"];

  // A reference to the region where the regional forwarding rule resides.
  //
  //
  // This field is not applicable to global forwarding rules.
  optional string region = 30;

  optional string self_link = 31 [json_name = "self_link"];

  // An optional prefix to the service name for this Forwarding Rule.
  //
  // If specified, will be the first label of the fully qualified service
  //
  // name.
  //
  //
  // The label must be 1-63 characters long, and comply with RFC1035.
  //
  // Specifically, the label must be 1-63 characters long and match the
  //
  // regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  //
  // character must be a lowercase letter, and all following characters
  //
  // must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  //
  //
  // This field is only used for INTERNAL load balancing.
  optional string service_label = 32 [json_name = "service_label"];

  // The internal fully qualified service name for this Forwarding Rule.
  //
  //
  // This field is only used for INTERNAL load balancing.
  optional string service_name = 33 [json_name = "service_name"];

  // If not empty, this Forwarding Rule will only forward the traffic when the source
  // IP address matches one of the IP addresses or CIDR ranges set here. Note that a
  // Forwarding Rule can only have up to 64 source IP ranges, and this field can only
  // be used with a regional Forwarding Rule whose scheme is EXTERNAL. Each
  // sourceIpRange entry should be either an IP address (for example, 1.2.3.4) or a
  // CIDR range (for example, 1.2.3.0/24).
  repeated string source_ip_ranges = 34 [json_name = "source_ip_ranges"];

  // This field identifies the subnetwork that the load balanced IP should
  //
  // belong to for this Forwarding Rule, used in internal load balancing and
  //
  // network load balancing with IPv6.
  //
  //
  // If the network specified is in auto subnet mode, this field is optional.
  //
  // However, a subnetwork must be specified if the network is in custom subnet
  //
  // mode or when creating external forwarding rule with IPv6.
  optional string subnetwork = 35;

  // The URL of the target resource to receive the matched traffic.  For
  //
  // regional forwarding rules, this target must be in the same region as the
  //
  // forwarding rule. For global forwarding rules, this target must be a global
  //
  // load balancing resource.
  //
  //
  // The forwarded traffic must be of a type appropriate to the target object.
  //
  // *  For load balancers, see the "Target" column in [Port
  // ogle.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to managed
  // services, the target must be a service attachment.
  optional string target = 36;

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 37 [json_name = "terraform_labels"];

  ServiceDirectoryRegistrations service_directory_registrations = 38 [json_name = "service_directory_registrations"];

  repeated Timeouts timeouts = 39;

  google.protobuf.Value for_each = 40 [json_name = "for_each"];

  repeated string depends_on = 41 [json_name = "depends_on"];

  optional int32 count = 42;

  optional string provider = 43;

  terraform.v1.Lifecycle lifecycle = 44;

  message ServiceDirectoryRegistrations {
    // Service Directory namespace to register the forwarding rule under.
    optional string namespace = 1;

    // Service Directory service to register the forwarding rule under.
    optional string service = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeGlobalAddress version is 0
message GoogleComputeGlobalAddress {
  // The IP address or beginning of the address range represented by this
  //
  // resource. This can be supplied as an input to reserve a specific
  //
  // address or omitted to allow GCP to choose a valid one for you.
  optional string address = 1;

  // The type of the address to reserve.
  //
  //
  // * EXTERNAL indicates public/external single IP address.
  //
  // * INTERNAL indicates internal IP ranges belonging to some network. Default value:
  // "EXTERNAL" Possible values: ["EXTERNAL", "INTERNAL"]
  optional string address_type = 2 [json_name = "address_type"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 5;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 6 [json_name = "effective_labels"];

  optional string id = 7;

  // The IP Version that will be used by this address. The default value is 'IPV4'.
  // Possible values: ["IPV4", "IPV6"]
  optional string ip_version = 8 [json_name = "ip_version"];

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 9 [json_name = "label_fingerprint"];

  // Labels to apply to this address.  A list of key->value pairs.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 10;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 11;

  // The URL of the network in which to reserve the IP range. The IP range
  //
  // must be in RFC1918 space. The network cannot be deleted if there are
  //
  // any reserved IP ranges referring to it.
  //
  //
  // This should only be set when using an Internal address.
  optional string network = 12;

  // The prefix length of the IP range. If not present, it means the
  //
  // address field is a single IP address.
  //
  //
  // This field is not applicable to addresses with addressType=INTERNAL
  //
  // when purpose=PRIVATE_SERVICE_CONNECT
  optional int64 prefix_length = 13 [json_name = "prefix_length"];

  optional string project = 14;

  // The purpose of the resource. Possible values include:
  //
  //
  // * VPC_PEERING - for peer networks
  //
  //
  // * PRIVATE_SERVICE_CONNECT - for
  // ([Beta](../guides/provider_versions.html.markdown)) only) Private Service Connect
  // networks
  optional string purpose = 15;

  optional string self_link = 16 [json_name = "self_link"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 17 [json_name = "terraform_labels"];

  repeated Timeouts timeouts = 18;

  google.protobuf.Value for_each = 19 [json_name = "for_each"];

  repeated string depends_on = 20 [json_name = "depends_on"];

  optional int32 count = 21;

  optional string provider = 22;

  terraform.v1.Lifecycle lifecycle = 23;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeGlobalForwardingRule version is 0
message GoogleComputeGlobalForwardingRule {
  // [Output Only] The URL for the corresponding base Forwarding Rule. By base
  // Forwarding Rule, we mean the Forwarding Rule that has the same IP address,
  // protocol, and port settings with the current Forwarding Rule, but without
  // sourceIPRanges specified. Always empty if the current Forwarding Rule does not
  // have sourceIPRanges specified.
  optional string base_forwarding_rule = 1 [json_name = "base_forwarding_rule"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 3;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 4 [json_name = "effective_labels"];

  // Specifies the canary migration state for the backend buckets attached to this
  // forwarding rule.
  //
  // Possible values are PREPARE, TEST_BY_PERCENTAGE, and TEST_ALL_TRAFFIC.
  //
  //
  // To begin the migration from EXTERNAL to EXTERNAL_MANAGED, the state must be
  // changed to
  //
  // PREPARE. The state must be changed to TEST_ALL_TRAFFIC before the
  // loadBalancingScheme can be
  //
  // changed to EXTERNAL_MANAGED. Optionally, the TEST_BY_PERCENTAGE state can be used
  // to migrate
  //
  // traffic to backend buckets attached to this forwarding rule by percentage using
  //
  // externalManagedBackendBucketMigrationTestingPercentage.
  //
  //
  // Rolling back a migration requires the states to be set in reverse order. So
  // changing the
  //
  // scheme from EXTERNAL_MANAGED to EXTERNAL requires the state to be set to
  // TEST_ALL_TRAFFIC at
  //
  // the same time. Optionally, the TEST_BY_PERCENTAGE state can be used to migrate
  // some traffic
  //
  // back to EXTERNAL or PREPARE can be used to migrate all traffic back to EXTERNAL.
  // Possible values: ["PREPARE", "TEST_BY_PERCENTAGE", "TEST_ALL_TRAFFIC"]
  optional string external_managed_backend_bucket_migration_state = 5 [
    json_name = "external_managed_backend_bucket_migration_state"
  ];

  // Determines the fraction of requests to backend buckets that should be processed
  // by the Global
  //
  // external Application Load Balancer.
  //
  //
  // The value of this field must be in the range [0, 100].
  //
  //
  // This value can only be set if the loadBalancingScheme in the forwarding rule is
  // set to
  //
  // EXTERNAL (when using the Classic ALB) and the migration state is
  // TEST_BY_PERCENTAGE.
  optional int64 external_managed_backend_bucket_migration_testing_percentage = 6 [
    json_name = "external_managed_backend_bucket_migration_testing_percentage"
  ];

  // The unique identifier number for the resource. This identifier is defined by the
  // server.
  optional int64 forwarding_rule_id = 7 [json_name = "forwarding_rule_id"];

  optional string id = 8;

  // IP address for which this forwarding rule accepts traffic. When a client
  //
  // sends traffic to this IP address, the forwarding rule directs the traffic
  //
  // to the referenced 'target'.
  //
  //
  // While creating a forwarding rule, specifying an 'IPAddress' is
  //
  // required under the following circumstances:
  //
  //
  // * When the 'target' is set to 'targetGrpcProxy' and
  //
  // 'validateForProxyless' is set to 'true', the
  //
  // 'IPAddress' should be set to '0.0.0.0'.
  //
  // * When the 'target' is a Private Service Connect Google APIs
  //
  // bundle, you must specify an 'IPAddress'.
  //
  //
  // Otherwise, you can optionally specify an IP address that references an
  //
  // existing static (reserved) IP address resource. When omitted, Google Cloud
  //
  // assigns an ephemeral IP address.
  //
  //
  // Use one of the following formats to specify an IP address while creating a
  //
  // forwarding rule:
  //
  //
  // * IP address number, as in '100.1.2.3'
  //
  // * IPv6 address range, as in '2600:1234::/96'
  //
  // * Full resource URL, as in
  //
  // gleapis.com/compute/v1/projects/project_id/regions/region/addresses/address-name'
  //
  // * Partial URL or by name, as in:
  //
  // * 'projects/project_id/regions/region/addresses/address-name'
  //
  // * 'regions/region/addresses/address-name'
  //
  // * 'global/addresses/address-name'
  //
  // * 'address-name'
  //
  //
  // The forwarding rule's 'target',
  //
  // and in most cases, also the 'loadBalancingScheme', determine the
  //
  // type of IP address that you can use. For detailed information, see
  //
  // [IP address
  //
  // ogle.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  //
  //
  // When reading an 'IPAddress', the API always returns the IP
  //
  // address number.
  optional string ip_address = 9 [json_name = "ip_address"];

  // The IP protocol to which this rule applies.
  //
  //
  // For protocol forwarding, valid
  //
  // options are 'TCP', 'UDP', 'ESP',
  //
  // 'AH', 'SCTP', 'ICMP' and
  //
  // 'L3_DEFAULT'.
  //
  //
  // The valid IP protocols are different for different load balancing products
  //
  // as described in [Load balancing
  //
  // m/load-balancing/docs/features#protocols_from_the_load_balancer_to_the_backends).
  // Possible values: ["TCP", "UDP", "ESP", "AH", "SCTP", "ICMP"]
  optional string ip_protocol = 10 [json_name = "ip_protocol"];

  // The IP Version that will be used by this global forwarding rule. Possible values:
  // ["IPV4", "IPV6"]
  optional string ip_version = 11 [json_name = "ip_version"];

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 12 [json_name = "label_fingerprint"];

  // Labels to apply to this forwarding rule.  A list of key->value pairs.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 13;

  // Specifies the forwarding rule type.
  //
  //
  // For more information about forwarding rules, refer to
  //
  // [Forwarding rule
  // concepts](https://cloud.google.com/load-balancing/docs/forwarding-rule-concepts).
  // Default value: "EXTERNAL_MANAGED" Possible values: ["EXTERNAL",
  // "EXTERNAL_MANAGED", "INTERNAL_MANAGED", "INTERNAL_SELF_MANAGED"]
  optional string load_balancing_scheme = 14 [json_name = "load_balancing_scheme"];

  // Name of the resource; provided by the client when the resource is created.
  //
  // The name must be 1-63 characters long, and comply with
  //
  // [RFC1035](https://www.ietf.org/rfc/rfc1035.txt).
  //
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  //
  // expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  //
  // character must be a lowercase letter, and all following characters must
  //
  // be a dash, lowercase letter, or digit, except the last character, which
  //
  // cannot be a dash.
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  //
  // APIs, the forwarding rule name must be a 1-20 characters string with
  //
  // lowercase letters and numbers and must start with a letter.
  optional string name = 15;

  // This field is not used for external load balancing.
  //
  //
  // For Internal TCP/UDP Load Balancing, this field identifies the network that
  //
  // the load balanced IP should belong to for this Forwarding Rule.
  //
  // If the subnetwork is specified, the network of the subnetwork will be used.
  //
  // If neither subnetwork nor this field is specified, the default network will
  //
  // be used.
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to Google
  //
  // APIs, a network must be provided.
  optional string network = 16;

  // This signifies the networking tier used for configuring
  //
  // this load balancer and can only take the following values:
  //
  // 'PREMIUM', 'STANDARD'.
  //
  //
  // For regional ForwardingRule, the valid values are 'PREMIUM' and
  //
  // 'STANDARD'. For GlobalForwardingRule, the valid value is
  //
  // 'PREMIUM'.
  //
  //
  // If this field is not specified, it is assumed to be 'PREMIUM'.
  //
  // If 'IPAddress' is specified, this value must be equal to the
  //
  // networkTier of the Address. Possible values: ["PREMIUM", "STANDARD"]
  optional string network_tier = 17 [json_name = "network_tier"];

  // This is used in PSC consumer ForwardingRule to control whether it should try to
  // auto-generate a DNS zone or not. Non-PSC forwarding rules do not use this field.
  optional bool no_automate_dns_zone = 18 [json_name = "no_automate_dns_zone"];

  // The 'portRange' field has the following limitations:
  //
  // * It requires that the forwarding rule 'IPProtocol' be TCP, UDP, or SCTP,
  //
  // and
  //
  // * It's applicable only to the following products: external passthrough
  //
  // Network Load Balancers, internal and external proxy Network Load
  //
  // Balancers, internal and external Application Load Balancers, external
  //
  // protocol forwarding, and Classic VPN.
  //
  // * Some products have restrictions on what ports can be used. See
  //
  // [port
  // loud.google.com/load-balancing/docs/forwarding-rule-concepts#port_specifications)
  //
  // for details.
  //
  //
  // For external forwarding rules, two or more forwarding rules cannot use the
  //
  // same '[IPAddress, IPProtocol]' pair, and cannot have overlapping
  //
  // 'portRange's.
  //
  //
  // For internal forwarding rules within the same VPC network, two or more
  //
  // forwarding rules cannot use the same '[IPAddress, IPProtocol]' pair, and
  //
  // cannot have overlapping 'portRange's.
  //
  //
  // @pattern: \d+(?:-\d+)?
  optional string port_range = 19 [json_name = "port_range"];

  optional string project = 20;

  // The PSC connection id of the PSC Forwarding Rule.
  optional string psc_connection_id = 21 [json_name = "psc_connection_id"];

  // The PSC connection status of the PSC Forwarding Rule. Possible values:
  // 'STATUS_UNSPECIFIED', 'PENDING', 'ACCEPTED', 'REJECTED', 'CLOSED'
  optional string psc_connection_status = 22 [json_name = "psc_connection_status"];

  optional string self_link = 23 [json_name = "self_link"];

  // If not empty, this Forwarding Rule will only forward the traffic when the source
  // IP address matches one of the IP addresses or CIDR ranges set here. Note that a
  // Forwarding Rule can only have up to 64 source IP ranges, and this field can only
  // be used with a regional Forwarding Rule whose scheme is EXTERNAL. Each
  // sourceIpRange entry should be either an IP address (for example, 1.2.3.4) or a
  // CIDR range (for example, 1.2.3.0/24).
  repeated string source_ip_ranges = 24 [json_name = "source_ip_ranges"];

  // This field identifies the subnetwork that the load balanced IP should
  //
  // belong to for this Forwarding Rule, used in internal load balancing and
  //
  // network load balancing with IPv6.
  //
  //
  // If the network specified is in auto subnet mode, this field is optional.
  //
  // However, a subnetwork must be specified if the network is in custom subnet
  //
  // mode or when creating external forwarding rule with IPv6.
  optional string subnetwork = 25;

  // The URL of the target resource to receive the matched traffic.  For
  //
  // regional forwarding rules, this target must be in the same region as the
  //
  // forwarding rule. For global forwarding rules, this target must be a global
  //
  // load balancing resource.
  //
  //
  // The forwarded traffic must be of a type appropriate to the target object.
  //
  // *  For load balancers, see the "Target" column in [Port
  // ogle.com/load-balancing/docs/forwarding-rule-concepts#ip_address_specifications).
  //
  // *  For Private Service Connect forwarding rules that forward traffic to Google
  // APIs, provide the name of a supported Google API bundle:
  //
  // *  'vpc-sc' - [ APIs that support VPC Service
  // Controls](https://cloud.google.com/vpc-service-controls/docs/supported-products).
  //
  // *  'all-apis' - [All supported Google
  // APIs](https://cloud.google.com/vpc/docs/private-service-connect#supported-apis).
  //
  //
  // For Private Service Connect forwarding rules that forward traffic to managed
  // services, the target must be a service attachment.
  optional string target = 26;

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 27 [json_name = "terraform_labels"];

  repeated MetadataFilters metadata_filters = 28 [json_name = "metadata_filters"];

  ServiceDirectoryRegistrations service_directory_registrations = 29 [json_name = "service_directory_registrations"];

  repeated Timeouts timeouts = 30;

  google.protobuf.Value for_each = 31 [json_name = "for_each"];

  repeated string depends_on = 32 [json_name = "depends_on"];

  optional int32 count = 33;

  optional string provider = 34;

  terraform.v1.Lifecycle lifecycle = 35;

  message MetadataFilters {
    // Specifies how individual filterLabel matches within the list of
    //
    // filterLabels contribute towards the overall metadataFilter match.
    //
    //
    // MATCH_ANY - At least one of the filterLabels must have a matching
    //
    // label in the provided metadata.
    //
    // MATCH_ALL - All filterLabels must have matching labels in the
    //
    // provided metadata. Possible values: ["MATCH_ANY", "MATCH_ALL"]
    optional string filter_match_criteria = 1 [json_name = "filter_match_criteria"];

    repeated FilterLabels filter_labels = 2 [json_name = "filter_labels"];

    message FilterLabels {
      // Name of the metadata label. The length must be between
      //
      // 1 and 1024 characters, inclusive.
      optional string name = 1;

      // The value that the label must match. The value has a maximum
      //
      // length of 1024 characters.
      optional string value = 2;
    }
  }

  message ServiceDirectoryRegistrations {
    // Service Directory namespace to register the forwarding rule under.
    optional string namespace = 1;

    // [Optional] Service Directory region to register this global forwarding rule
    // under.
    //
    // Default to "us-central1". Only used for PSC for Google APIs. All PSC for
    //
    // Google APIs Forwarding Rules on the same network should use the same Service
    //
    // Directory region.
    optional string service_directory_region = 2 [json_name = "service_directory_region"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeGlobalNetworkEndpoint version is 0
message GoogleComputeGlobalNetworkEndpoint {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // Fully qualified domain name of network endpoint.
  //
  // This can only be specified when network_endpoint_type of the NEG is
  // INTERNET_FQDN_PORT.
  optional string fqdn = 2;

  // The global network endpoint group this endpoint is part of.
  optional string global_network_endpoint_group = 3 [json_name = "global_network_endpoint_group"];

  optional string id = 4;

  // IPv4 address external endpoint.
  optional string ip_address = 5 [json_name = "ip_address"];

  // Port number of the external endpoint.
  optional int64 port = 6;

  optional string project = 7;

  repeated Timeouts timeouts = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeGlobalNetworkEndpointGroup version is 0
message GoogleComputeGlobalNetworkEndpointGroup {
  // The default port used if the port number is not specified in the
  //
  // network endpoint.
  optional int64 default_port = 1 [json_name = "default_port"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 3;

  optional string id = 4;

  // Name of the resource; provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 5;

  // Type of network endpoints in this network endpoint group. Possible values:
  // ["INTERNET_IP_PORT", "INTERNET_FQDN_PORT"]
  optional string network_endpoint_type = 6 [json_name = "network_endpoint_type"];

  optional string project = 7;

  optional string self_link = 8 [json_name = "self_link"];

  repeated Timeouts timeouts = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  optional int32 count = 12;

  optional string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeGlobalVmExtensionPolicy version is 0
message GoogleComputeGlobalVmExtensionPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // The unique identifier for the resource.
  optional string id = 4;

  // Type of the resource.
  optional string kind = 5;

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be 1-63 characters long and match the regular expression
  // '^[a-z]([-a-z0-9]{0,61}[a-z0-9])?$' to comply with RFC1035.
  optional string name = 6;

  // Used to resolve conflicts when multiple policies are active. Defaults to 0.
  optional int64 priority = 7;

  optional string project = 8;

  // The scoped resource status.
  optional string scoped_resource_status = 9 [json_name = "scoped_resource_status"];

  // Server-defined fully-qualified URL for this resource.
  optional string self_link = 10 [json_name = "self_link"];

  // Update timestamp in RFC3339 text format.
  optional string update_timestamp = 11 [json_name = "update_timestamp"];

  repeated ExtensionPolicies extension_policies = 12 [json_name = "extension_policies"];

  repeated InstanceSelectors instance_selectors = 13 [json_name = "instance_selectors"];

  RolloutOperation rollout_operation = 14 [json_name = "rollout_operation"];

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  optional int32 count = 18;

  optional string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message ExtensionPolicies {
    optional string extension_name = 1 [json_name = "extension_name"];

    // The version pinning for the extension.
    optional string pinned_version = 2 [json_name = "pinned_version"];

    // String configuration payload.
    optional string string_config = 3 [json_name = "string_config"];
  }

  message InstanceSelectors {
    LabelSelector label_selector = 1 [json_name = "label_selector"];

    message LabelSelector {
      // Labels as key value pairs.
      map<string, string> inclusion_labels = 1 [json_name = "inclusion_labels"];
    }
  }

  message RolloutOperation {
    // Rollout status.
    repeated RolloutStatus rollout_status = 1 [json_name = "rollout_status"];

    RolloutInput rollout_input = 2 [json_name = "rollout_input"];

    message RolloutStatus {
      // current_rollouts: list of object
      repeated CurrentRollouts current_rollouts = 1 [json_name = "current_rollouts"];

      // previous_rollout: list of object
      repeated PreviousRollout previous_rollout = 2 [json_name = "previous_rollout"];

      message CurrentRollouts {
        // location_rollout_status: set of object
        repeated LocationRolloutStatus location_rollout_status = 1 [json_name = "location_rollout_status"];

        // rollout: string
        optional string rollout = 2;

        // rollout_plan: string
        optional string rollout_plan = 3 [json_name = "rollout_plan"];

        // state: string
        optional string state = 4;

        message LocationRolloutStatus {
          // location_name: string
          optional string location_name = 1 [json_name = "location_name"];

          // state: string
          optional string state = 2;
        }
      }

      message PreviousRollout {
        // location_rollout_status: set of object
        repeated LocationRolloutStatus location_rollout_status = 1 [json_name = "location_rollout_status"];

        // rollout: string
        optional string rollout = 2;

        // rollout_plan: string
        optional string rollout_plan = 3 [json_name = "rollout_plan"];

        // state: string
        optional string state = 4;

        message LocationRolloutStatus {
          // location_name: string
          optional string location_name = 1 [json_name = "location_name"];

          // state: string
          optional string state = 2;
        }
      }
    }

    message RolloutInput {
      // Specifies the behavior of the rollout if a conflict is detected.
      optional string conflict_behavior = 1 [json_name = "conflict_behavior"];

      // The name of the rollout plan.
      optional string name = 2;

      // Specifies the predefined rollout plan for the policy.
      optional string predefined_rollout_plan = 3 [json_name = "predefined_rollout_plan"];

      // The UUID that identifies a policy rollout retry attempt. It should only be set
      // when retrying an existing rollout. Updating this field along with other policy
      // fields (description, extension_policies, instance_selectors, priority) in the
      // same plan will return an error.
      optional string retry_uuid = 4 [json_name = "retry_uuid"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeHaVpnGateway version is 1
message GoogleComputeHaVpnGateway {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 2;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 3 [json_name = "effective_labels"];

  // The IP family of the gateway IPs for the HA-VPN gateway interfaces. If not
  // specified, IPV4 will be used. Default value: "IPV4" Possible values: ["IPV4",
  // "IPV6"]
  optional string gateway_ip_version = 4 [json_name = "gateway_ip_version"];

  optional string id = 5;

  // A fingerprint for the labels being applied to this VpnGateway, which is
  // essentially a hash
  //
  // of the labels set used for optimistic locking. The fingerprint is initially
  // generated by
  //
  // Compute Engine and changes after every request to modify or update labels.
  //
  // You must always provide an up-to-date fingerprint hash in order to update or
  // change labels,
  //
  // otherwise the request will fail with error 412 conditionNotMet.
  optional string label_fingerprint = 6 [json_name = "label_fingerprint"];

  // Labels for this resource. These can only be added or modified by the setLabels
  // method.
  //
  // Each label key/value pair must comply with RFC1035. Label values may be empty.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 7;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 8;

  // The network this VPN gateway is accepting traffic for.
  optional string network = 9;

  optional string project = 10;

  // The region this gateway should sit in.
  optional string region = 11;

  optional string self_link = 12 [json_name = "self_link"];

  // The stack type for this VPN gateway to identify the IP protocols that are
  // enabled.
  //
  // If not specified, IPV4_ONLY will be used. Default value: "IPV4_ONLY" Possible
  // values: ["IPV4_ONLY", "IPV4_IPV6", "IPV6_ONLY"]
  optional string stack_type = 13 [json_name = "stack_type"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 14 [json_name = "terraform_labels"];

  Params params = 15;

  repeated Timeouts timeouts = 16;

  repeated VpnInterfaces vpn_interfaces = 17 [json_name = "vpn_interfaces"];

  google.protobuf.Value for_each = 18 [json_name = "for_each"];

  repeated string depends_on = 19 [json_name = "depends_on"];

  optional int32 count = 20;

  optional string provider = 21;

  terraform.v1.Lifecycle lifecycle = 22;

  message Params {
    // Resource manager tags to be bound to the HaVpnGateway. Tag keys and values have
    // the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message VpnInterfaces {
    // The numeric ID of this VPN gateway interface.
    optional int64 id = 1;

    // URL of the interconnect attachment resource. When the value
    //
    // of this field is present, the VPN Gateway will be used for
    //
    // IPsec-encrypted Cloud Interconnect; all Egress or Ingress
    //
    // traffic for this VPN Gateway interface will go through the
    //
    // specified interconnect attachment resource.
    //
    //
    // Not currently available publicly.
    optional string interconnect_attachment = 2 [json_name = "interconnect_attachment"];

    // The external IP address for this VPN gateway interface.
    optional string ip_address = 3 [json_name = "ip_address"];
  }
}

// GoogleComputeHealthCheck version is 0
message GoogleComputeHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  //
  // seconds.
  optional int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 4;

  // A so-far unhealthy instance will be marked healthy after this many
  //
  // consecutive successes. The default value is 2.
  optional int64 healthy_threshold = 5 [json_name = "healthy_threshold"];

  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the
  //
  // last character, which cannot be a dash.
  optional string name = 7;

  optional string project = 8;

  optional string self_link = 9 [json_name = "self_link"];

  // The list of cloud regions from which health checks are performed. If
  //
  // any regions are specified, then exactly 3 regions should be specified.
  //
  // The region names must be valid names of Google Cloud regions. This can
  //
  // only be set for global health check. If this list is non-empty, then
  //
  // there are restrictions on what other health check fields are supported
  //
  // and what other resources can use this health check:
  //
  //
  // * SSL, HTTP2, and GRPC protocols are not supported.
  //
  //
  // * The TCP request field is not supported.
  //
  //
  // * The proxyHeader field for HTTP, HTTPS, and TCP is not supported.
  //
  //
  // * The checkIntervalSec field must be at least 30.
  //
  //
  // * The health check cannot be used with BackendService nor with managed
  //
  // instance group auto-healing.
  repeated string source_regions = 10 [json_name = "source_regions"];

  // How long (in seconds) to wait before claiming failure.
  //
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  //
  // greater value than checkIntervalSec.
  optional int64 timeout_sec = 11 [json_name = "timeout_sec"];

  // The type of the health check. One of HTTP, HTTPS, TCP, or SSL.
  optional string type = 12;

  // A so-far healthy instance will be marked unhealthy after this many
  //
  // consecutive failures. The default value is 2.
  optional int64 unhealthy_threshold = 13 [json_name = "unhealthy_threshold"];

  GrpcHealthCheck grpc_health_check = 14 [json_name = "grpc_health_check"];

  GrpcTlsHealthCheck grpc_tls_health_check = 15 [json_name = "grpc_tls_health_check"];

  Http2HealthCheck http2_health_check = 16 [json_name = "http2_health_check"];

  HttpHealthCheck http_health_check = 17 [json_name = "http_health_check"];

  HttpsHealthCheck https_health_check = 18 [json_name = "https_health_check"];

  LogConfig log_config = 19 [json_name = "log_config"];

  SslHealthCheck ssl_health_check = 20 [json_name = "ssl_health_check"];

  TcpHealthCheck tcp_health_check = 21 [json_name = "tcp_health_check"];

  repeated Timeouts timeouts = 22;

  google.protobuf.Value for_each = 23 [json_name = "for_each"];

  repeated string depends_on = 24 [json_name = "depends_on"];

  optional int32 count = 25;

  optional string provider = 26;

  terraform.v1.Lifecycle lifecycle = 27;

  message GrpcHealthCheck {
    // The gRPC service name for the health check.
    //
    // The value of grpcServiceName has the following meanings by convention:
    //
    // - Empty serviceName means the overall status of all services at the backend.
    //
    // - Non-empty serviceName means the health of that gRPC service, as defined by the
    // owner of the service.
    //
    // The grpcServiceName can only be ASCII.
    optional string grpc_service_name = 1 [json_name = "grpc_service_name"];

    // The port number for the health check request.
    //
    // Must be specified if portName and portSpecification are not set
    //
    // or if port_specification is USE_FIXED_PORT. Valid values are 1 through 65535.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, gRPC health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];
  }

  message GrpcTlsHealthCheck {
    // The gRPC service name for the health check.
    //
    // The value of grpcServiceName has the following meanings by convention:
    //
    // - Empty serviceName means the overall status of all services at the backend.
    //
    // - Non-empty serviceName means the health of that gRPC service, as defined by the
    // owner of the service.
    //
    // The grpcServiceName can only be ASCII.
    optional string grpc_service_name = 1 [json_name = "grpc_service_name"];

    // The port number for the health check request.
    //
    // Must be specified if port_specification is USE_FIXED_PORT. Valid values are 1
    // through 65535.
    optional int64 port = 2;

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': Not supported for GRPC with TLS health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, gRPC with TLS health check follows behavior specified in the
    // 'port' field. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 3 [json_name = "port_specification"];
  }

  message Http2HealthCheck {
    // The value of the host header in the HTTP2 health check request.
    //
    // If left empty (default value), the public IP on behalf of which this health
    //
    // check is performed will be used.
    optional string host = 1;

    // The TCP port number for the HTTP2 health check request.
    //
    // The default value is 443.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, HTTP2 health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP2 health check request.
    //
    // The default value is /.
    optional string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 7;
  }

  message HttpHealthCheck {
    // The value of the host header in the HTTP health check request.
    //
    // If left empty (default value), the public IP on behalf of which this health
    //
    // check is performed will be used.
    optional string host = 1;

    // The TCP port number for the HTTP health check request.
    //
    // The default value is 80.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, HTTP health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP health check request.
    //
    // The default value is /.
    optional string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 7;
  }

  message HttpsHealthCheck {
    // The value of the host header in the HTTPS health check request.
    //
    // If left empty (default value), the public IP on behalf of which this health
    //
    // check is performed will be used.
    optional string host = 1;

    // The TCP port number for the HTTPS health check request.
    //
    // The default value is 443.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, HTTPS health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTPS health check request.
    //
    // The default value is /.
    optional string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 7;
  }

  message LogConfig {
    // Indicates whether or not to export logs. This is false by default,
    //
    // which means no health check logging will be done.
    optional bool enable = 1;
  }

  message SslHealthCheck {
    // The TCP port number for the SSL health check request.
    //
    // The default value is 443.
    optional int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, SSL health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the SSL connection has been
    //
    // established (default value is empty). If both request and response are
    //
    // empty, the connection establishment alone will indicate health. The request
    //
    // data can only be ASCII.
    optional string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 6;
  }

  message TcpHealthCheck {
    // The TCP port number for the TCP health check request.
    //
    // The default value is 443.
    optional int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, TCP health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the TCP connection has been
    //
    // established (default value is empty). If both request and response are
    //
    // empty, the connection establishment alone will indicate health. The request
    //
    // data can only be ASCII.
    optional string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 6;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeHttpHealthCheck version is 0
message GoogleComputeHttpHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  //
  // seconds.
  optional int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 4;

  // A so-far unhealthy instance will be marked healthy after this many
  //
  // consecutive successes. The default value is 2.
  optional int64 healthy_threshold = 5 [json_name = "healthy_threshold"];

  // The value of the host header in the HTTP health check request. If
  //
  // left empty (default value), the public IP on behalf of which this
  //
  // health check is performed will be used.
  optional string host = 6;

  optional string id = 7;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the
  //
  // last character, which cannot be a dash.
  optional string name = 8;

  // The TCP port number for the HTTP health check request.
  //
  // The default value is 80.
  optional int64 port = 9;

  optional string project = 10;

  // The request path of the HTTP health check request.
  //
  // The default value is /.
  optional string request_path = 11 [json_name = "request_path"];

  optional string self_link = 12 [json_name = "self_link"];

  // How long (in seconds) to wait before claiming failure.
  //
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  //
  // greater value than checkIntervalSec.
  optional int64 timeout_sec = 13 [json_name = "timeout_sec"];

  // A so-far healthy instance will be marked unhealthy after this many
  //
  // consecutive failures. The default value is 2.
  optional int64 unhealthy_threshold = 14 [json_name = "unhealthy_threshold"];

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  optional int32 count = 18;

  optional string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeHttpsHealthCheck version is 0
message GoogleComputeHttpsHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  //
  // seconds.
  optional int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 4;

  // A so-far unhealthy instance will be marked healthy after this many
  //
  // consecutive successes. The default value is 2.
  optional int64 healthy_threshold = 5 [json_name = "healthy_threshold"];

  // The value of the host header in the HTTPS health check request. If
  //
  // left empty (default value), the public IP on behalf of which this
  //
  // health check is performed will be used.
  optional string host = 6;

  optional string id = 7;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the
  //
  // last character, which cannot be a dash.
  optional string name = 8;

  // The TCP port number for the HTTPS health check request.
  //
  // The default value is 443.
  optional int64 port = 9;

  optional string project = 10;

  // The request path of the HTTPS health check request.
  //
  // The default value is /.
  optional string request_path = 11 [json_name = "request_path"];

  optional string self_link = 12 [json_name = "self_link"];

  // How long (in seconds) to wait before claiming failure.
  //
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  //
  // greater value than checkIntervalSec.
  optional int64 timeout_sec = 13 [json_name = "timeout_sec"];

  // A so-far healthy instance will be marked unhealthy after this many
  //
  // consecutive failures. The default value is 2.
  optional int64 unhealthy_threshold = 14 [json_name = "unhealthy_threshold"];

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  optional int32 count = 18;

  optional string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeImage version is 0
message GoogleComputeImage {
  // Size of the image tar.gz archive stored in Google Cloud Storage (in
  //
  // bytes).
  optional int64 archive_size_bytes = 1 [json_name = "archive_size_bytes"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 4;

  // Size of the image when restored onto a persistent disk (in GB).
  optional int64 disk_size_gb = 5 [json_name = "disk_size_gb"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 6 [json_name = "effective_labels"];

  // The name of the image family to which this image belongs. You can
  //
  // create disks by specifying an image family instead of a specific
  //
  // image name. The image family always returns its latest image that is
  //
  // not deprecated. The name of the image family must comply with
  //
  // RFC1035.
  optional string family = 7;

  optional string id = 8;

  // The fingerprint used for optimistic locking of this resource. Used
  //
  // internally during updates.
  optional string label_fingerprint = 9 [json_name = "label_fingerprint"];

  // Labels to apply to this Image.
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 10;

  // Any applicable license URI.
  repeated string licenses = 11;

  // Name of the resource; provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the
  //
  // last character, which cannot be a dash.
  optional string name = 12;

  optional string project = 13;

  optional string self_link = 14 [json_name = "self_link"];

  // The source disk to create this image based on.
  //
  // You must provide either this property or the
  //
  // rawDisk.source property but not both to create an image.
  optional string source_disk = 15 [json_name = "source_disk"];

  // URL of the source image used to create this image. In order to create an image,
  // you must provide the full or partial
  //
  // URL of one of the following:
  //
  //
  // * The selfLink URL
  //
  // * This property
  //
  // * The rawDisk.source URL
  //
  // * The sourceDisk URL
  optional string source_image = 16 [json_name = "source_image"];

  // URL of the source snapshot used to create this image.
  //
  //
  // In order to create an image, you must provide the full or partial URL of one of
  // the following:
  //
  //
  // * The selfLink URL
  //
  // * This property
  //
  // * The sourceImage URL
  //
  // * The rawDisk.source URL
  //
  // * The sourceDisk URL
  optional string source_snapshot = 17 [json_name = "source_snapshot"];

  // Cloud Storage bucket storage location of the image
  //
  // (regional or multi-regional).
  //
  // Reference link: https://cloud.google.com/compute/docs/reference/rest/v1/images
  repeated string storage_locations = 18 [json_name = "storage_locations"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 19 [json_name = "terraform_labels"];

  repeated GuestOsFeatures guest_os_features = 20 [json_name = "guest_os_features"];

  ImageEncryptionKey image_encryption_key = 21 [json_name = "image_encryption_key"];

  Params params = 22;

  RawDisk raw_disk = 23 [json_name = "raw_disk"];

  ShieldedInstanceInitialState shielded_instance_initial_state = 24 [json_name = "shielded_instance_initial_state"];

  SourceDiskEncryptionKey source_disk_encryption_key = 25 [json_name = "source_disk_encryption_key"];

  SourceImageEncryptionKey source_image_encryption_key = 26 [json_name = "source_image_encryption_key"];

  SourceSnapshotEncryptionKey source_snapshot_encryption_key = 27 [json_name = "source_snapshot_encryption_key"];

  repeated Timeouts timeouts = 28;

  google.protobuf.Value for_each = 29 [json_name = "for_each"];

  repeated string depends_on = 30 [json_name = "depends_on"];

  optional int32 count = 31;

  optional string provider = 32;

  terraform.v1.Lifecycle lifecycle = 33;

  message GuestOsFeatures {
    // The type of supported feature. Read [Enabling guest operating system
    // com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features)
    // to see a list of available options. Possible values: ["MULTI_IP_SUBNET",
    // "SECURE_BOOT", "SEV_CAPABLE", "UEFI_COMPATIBLE", "VIRTIO_SCSI_MULTIQUEUE",
    // "WINDOWS", "GVNIC", "IDPF", "SEV_LIVE_MIGRATABLE", "SEV_SNP_CAPABLE",
    // "SUSPEND_RESUME_COMPATIBLE", "TDX_CAPABLE", "SEV_LIVE_MIGRATABLE_V2",
    // "SNP_SVSM_CAPABLE"]
    optional string type = 1;
  }

  message ImageEncryptionKey {
    // The self link of the encryption key that is stored in Google Cloud
    //
    // KMS.
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account being used for the encryption request for the
    //
    // given KMS key. If absent, the Compute Engine default service
    //
    // account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
  }

  message Params {
    // Resource manager tags to be bound to the image. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys and values can be either in
    // numeric format,
    //
    // such as tagKeys/{tag_key_id} and tagValues/{tag_value_id} or in namespaced format
    // such as
    //
    // {org_id|projectId}/{tag_key_short_name} and {tag_value_short_name}. The field is
    // ignored when empty.
    //
    // The field is immutable and causes resource replacement when mutated. This field
    // is only
    //
    // set at create time and modifying this field after creation will trigger
    // recreation.
    //
    // To apply tags to an existing resource, see the google_tags_tag_binding resource.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message RawDisk {
    // The format used to encode and transmit the block device, which
    //
    // should be TAR. This is just a container and transmission format
    //
    // and not a runtime format. Provided by the client when the disk
    //
    // image is created. Default value: "TAR" Possible values: ["TAR"]
    optional string container_type = 1 [json_name = "container_type"];

    // An optional SHA1 checksum of the disk image before unpackaging.
    //
    // This is provided by the client when the disk image is created.
    optional string sha1 = 2;

    // The full Google Cloud Storage URL where disk storage is stored
    //
    // You must provide either this property or the sourceDisk property
    //
    // but not both.
    optional string source = 3;
  }

  message ShieldedInstanceInitialState {
    repeated Dbs dbs = 1;

    repeated Dbxs dbxs = 2;

    repeated Keks keks = 3;

    Pk pk = 4;

    message Dbs {
      // The raw content in the secure keys file.
      //
      //
      // A base64-encoded string.
      optional string content = 1;

      // The file type of source file.
      optional string file_type = 2 [json_name = "file_type"];
    }

    message Dbxs {
      // The raw content in the secure keys file.
      //
      //
      // A base64-encoded string.
      optional string content = 1;

      // The file type of source file.
      optional string file_type = 2 [json_name = "file_type"];
    }

    message Keks {
      // The raw content in the secure keys file.
      //
      //
      // A base64-encoded string.
      optional string content = 1;

      // The file type of source file.
      optional string file_type = 2 [json_name = "file_type"];
    }

    message Pk {
      // The raw content in the secure keys file.
      //
      //
      // A base64-encoded string.
      optional string content = 1;

      // The file type of source file.
      optional string file_type = 2 [json_name = "file_type"];
    }
  }

  message SourceDiskEncryptionKey {
    // The self link of the encryption key used to decrypt this resource. Also called
    // KmsKeyName
    //
    // in the cloud console. Your project's Compute Engine System service account
    //
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    //
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    //
    // See
    // isks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account being used for the encryption request for the
    //
    // given KMS key. If absent, the Compute Engine default service
    //
    // account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit
    //
    // customer-supplied encryption key to either encrypt or decrypt
    //
    // this resource. You can provide either the rawKey or the rsaEncryptedKey.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
  }

  message SourceImageEncryptionKey {
    // The self link of the encryption key used to decrypt this resource. Also called
    // KmsKeyName
    //
    // in the cloud console. Your project's Compute Engine System service account
    //
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    //
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    //
    // See
    // isks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account being used for the encryption request for the
    //
    // given KMS key. If absent, the Compute Engine default service
    //
    // account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit
    //
    // customer-supplied encryption key to either encrypt or decrypt
    //
    // this resource. You can provide either the rawKey or the rsaEncryptedKey.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
  }

  message SourceSnapshotEncryptionKey {
    // The self link of the encryption key used to decrypt this resource. Also called
    // KmsKeyName
    //
    // in the cloud console. Your project's Compute Engine System service account
    //
    // ('service-{{PROJECT_NUMBER}}@compute-system.iam.gserviceaccount.com') must have
    //
    // 'roles/cloudkms.cryptoKeyEncrypterDecrypter' to use this feature.
    //
    // See
    // isks/customer-managed-encryption#encrypt_a_new_persistent_disk_with_your_own_keys
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account being used for the encryption request for the
    //
    // given KMS key. If absent, the Compute Engine default service
    //
    // account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit
    //
    // customer-supplied encryption key to either encrypt or decrypt
    //
    // this resource. You can provide either the rawKey or the rsaEncryptedKey.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeImageIamBinding version is 0
message GoogleComputeImageIamBinding {
  optional string etag = 1;

  optional string id = 2;

  optional string image = 3;

  repeated string members = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeImageIamMember version is 0
message GoogleComputeImageIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string image = 3;

  optional string member = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeImageIamPolicy version is 0
message GoogleComputeImageIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string image = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// GoogleComputeInstance version is 6
message GoogleComputeInstance {
  // If true, allows Terraform to stop the instance to update its properties. If you
  // try to update a property that requires stopping the instance without setting this
  // field, the update will fail.
  optional bool allow_stopping_for_update = 1 [json_name = "allow_stopping_for_update"];

  // Whether sending and receiving of packets with non-matching source or destination
  // IPs is allowed.
  optional bool can_ip_forward = 2 [json_name = "can_ip_forward"];

  // The CPU platform used by this instance.
  optional string cpu_platform = 3 [json_name = "cpu_platform"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // 					Current status of the instance.
  //
  // 					This could be one of the following values: PROVISIONING, STAGING, RUNNING,
  // STOPPING, SUSPENDING, SUSPENDED, REPAIRING, and TERMINATED.
  //
  // 					For more information about the status of the instance, see [Instance life
  // cycle](https://cloud.google.com/compute/docs/instances/instance-life-cycle).
  optional string current_status = 5 [json_name = "current_status"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 6 [json_name = "deletion_policy"];

  // Whether deletion protection is enabled on this instance.
  optional bool deletion_protection = 7 [json_name = "deletion_protection"];

  // A brief description of the resource.
  optional string description = 8;

  // Desired status of the instance. Either "RUNNING", "SUSPENDED" or "TERMINATED".
  optional string desired_status = 9 [json_name = "desired_status"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 10 [json_name = "effective_labels"];

  // Whether the instance has virtual displays enabled.
  optional bool enable_display = 11 [json_name = "enable_display"];

  // A custom hostname for the instance. Must be a fully qualified DNS name and
  // RFC-1035-valid. Valid format is a series of labels 1-63 characters long matching
  // the regular expression [a-z]([-a-z0-9]*[a-z0-9]), concatenated with periods. The
  // entire hostname must not exceed 253 characters. Changing this forces a new
  // resource to be created.
  optional string hostname = 12;

  optional string id = 13;

  // The server-assigned unique identifier of this instance.
  optional string instance_id = 14 [json_name = "instance_id"];

  // Action to be taken when a customer's encryption key is revoked. Supports "STOP"
  // and "NONE", with "NONE" being the default.
  optional string key_revocation_action_type = 15 [json_name = "key_revocation_action_type"];

  // The unique fingerprint of the labels.
  optional string label_fingerprint = 16 [json_name = "label_fingerprint"];

  // A set of key/value label pairs assigned to the instance.
  //
  //
  // 				**Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // 				Please refer to the field 'effective_labels' for all of the labels present on
  // the resource.
  map<string, string> labels = 17;

  // The machine type to create.
  optional string machine_type = 18 [json_name = "machine_type"];

  // Metadata key/value pairs made available within the instance.
  map<string, string> metadata = 19;

  // The unique fingerprint of the metadata.
  optional string metadata_fingerprint = 20 [json_name = "metadata_fingerprint"];

  // Metadata startup scripts made available within the instance.
  optional string metadata_startup_script = 21 [json_name = "metadata_startup_script"];

  // The minimum CPU platform specified for the VM instance.
  optional string min_cpu_platform = 22 [json_name = "min_cpu_platform"];

  // The name of the instance. One of name or self_link must be provided.
  optional string name = 23;

  // The ID of the project in which the resource belongs. If self_link is provided,
  // this value is ignored. If neither self_link nor project are provided, the
  // provider project is used.
  optional string project = 24;

  // A list of self_links of resource policies to attach to the instance. Currently a
  // max of 1 resource policy is supported.
  repeated string resource_policies = 25 [json_name = "resource_policies"];

  // The URI of the created resource.
  optional string self_link = 26 [json_name = "self_link"];

  // The list of tags attached to the instance.
  repeated string tags = 27;

  // The unique fingerprint of the tags.
  optional string tags_fingerprint = 28 [json_name = "tags_fingerprint"];

  // The combination of labels configured directly on the resource and default labels
  // configured on the provider.
  map<string, string> terraform_labels = 29 [json_name = "terraform_labels"];

  // The zone of the instance. If self_link is provided, this value is ignored. If
  // neither self_link nor zone are provided, the provider zone is used.
  optional string zone = 30;

  AdvancedMachineFeatures advanced_machine_features = 31 [json_name = "advanced_machine_features"];

  repeated AttachedDisk attached_disk = 32 [json_name = "attached_disk"];

  BootDisk boot_disk = 33 [json_name = "boot_disk"];

  ConfidentialInstanceConfig confidential_instance_config = 34 [json_name = "confidential_instance_config"];

  repeated GuestAccelerator guest_accelerator = 35 [json_name = "guest_accelerator"];

  InstanceEncryptionKey instance_encryption_key = 36 [json_name = "instance_encryption_key"];

  repeated NetworkInterface network_interface = 37 [json_name = "network_interface"];

  NetworkPerformanceConfig network_performance_config = 38 [json_name = "network_performance_config"];

  Params params = 39;

  ReservationAffinity reservation_affinity = 40 [json_name = "reservation_affinity"];

  Scheduling scheduling = 41;

  repeated ScratchDisk scratch_disk = 42 [json_name = "scratch_disk"];

  ServiceAccount service_account = 43 [json_name = "service_account"];

  ShieldedInstanceConfig shielded_instance_config = 44 [json_name = "shielded_instance_config"];

  repeated Timeouts timeouts = 45;

  WorkloadIdentityConfig workload_identity_config = 46 [json_name = "workload_identity_config"];

  google.protobuf.Value for_each = 47 [json_name = "for_each"];

  repeated string depends_on = 48 [json_name = "depends_on"];

  optional int32 count = 49;

  optional string provider = 50;

  terraform.v1.Lifecycle lifecycle = 51;

  message AdvancedMachineFeatures {
    // Whether to enable nested virtualization or not.
    optional bool enable_nested_virtualization = 1 [json_name = "enable_nested_virtualization"];

    // Whether to enable UEFI networking for the instance.
    optional bool enable_uefi_networking = 2 [json_name = "enable_uefi_networking"];

    // The PMU is a hardware component within the CPU core that monitors how the
    // processor runs code. Valid values for the level of PMU are "STANDARD",
    // "ENHANCED", and "ARCHITECTURAL".
    optional string performance_monitoring_unit = 3 [json_name = "performance_monitoring_unit"];

    // The number of threads per physical core. To disable simultaneous multithreading
    // (SMT) set this to 1. If unset, the maximum number of threads supported per core
    // by the underlying processor is assumed.
    optional int64 threads_per_core = 4 [json_name = "threads_per_core"];

    // Turbo frequency mode to use for the instance. Currently supported modes is
    // "ALL_CORE_MAX".
    optional string turbo_mode = 5 [json_name = "turbo_mode"];

    // The number of physical cores to expose to an instance. Multiply by the number of
    // threads per core to compute the total number of virtual CPUs to expose to the
    // instance. If unset, the number of cores is inferred from the instance\'s nominal
    // CPU count and the underlying platform\'s SMT width.
    optional int64 visible_core_count = 6 [json_name = "visible_core_count"];
  }

  message AttachedDisk {
    // Name with which the attached disk is accessible under /dev/disk/by-id/
    optional string device_name = 1 [json_name = "device_name"];

    // A 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_rsa and
    // disk_encryption_key_raw may be set.
    optional string disk_encryption_key_raw = 2 [json_name = "disk_encryption_key_raw"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
    // encryption key to either encrypt or decrypt this resource. Only one of
    // kms_key_self_link, disk_encryption_key_rsa and disk_encryption_key_raw may be
    // set.
    optional string disk_encryption_key_rsa = 3 [json_name = "disk_encryption_key_rsa"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key
    // that protects this resource.
    optional string disk_encryption_key_sha256 = 4 [json_name = "disk_encryption_key_sha256"];

    // The service account being used for the encryption request for the given KMS key.
    // If absent, the Compute Engine default service account is used
    optional string disk_encryption_service_account = 5 [json_name = "disk_encryption_service_account"];

    // Whether to force attach the regional disk even if it's currently attached to
    // another instance. If you try to force attach a zonal disk to an instance, you
    // will receive an error. Setting this parameter cause VM recreation.
    optional bool force_attach = 6 [json_name = "force_attach"];

    // The self_link of the encryption key that is stored in Google Cloud KMS to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_rsa and
    // disk_encryption_key_raw may be set.
    optional string kms_key_self_link = 7 [json_name = "kms_key_self_link"];

    // Read/write mode for the disk. One of "READ_ONLY" or "READ_WRITE".
    optional string mode = 8;

    // The name or self_link of the disk attached to this instance.
    optional string source = 9;
  }

  message BootDisk {
    // Whether the disk will be auto-deleted when the instance is deleted.
    optional bool auto_delete = 1 [json_name = "auto_delete"];

    // Name with which attached disk will be accessible under /dev/disk/by-id/
    optional string device_name = 2 [json_name = "device_name"];

    // A 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_raw and
    // disk_encryption_key_rsa may be set.
    optional string disk_encryption_key_raw = 3 [json_name = "disk_encryption_key_raw"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
    // encryption key to either encrypt or decrypt this resource. Only one of
    // kms_key_self_link, disk_encryption_key_raw and disk_encryption_key_rsa may be
    // set.
    optional string disk_encryption_key_rsa = 4 [json_name = "disk_encryption_key_rsa"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key
    // that protects this resource.
    optional string disk_encryption_key_sha256 = 5 [json_name = "disk_encryption_key_sha256"];

    // The service account being used for the encryption request for the given KMS key.
    // If absent, the Compute Engine default service account is used
    optional string disk_encryption_service_account = 6 [json_name = "disk_encryption_service_account"];

    // Whether to force attach the regional disk even if it's currently attached to
    // another instance. If you try to force attach a zonal disk to an instance, you
    // will receive an error. Setting this parameter cause VM recreation.
    optional bool force_attach = 7 [json_name = "force_attach"];

    // A list of features to enable on the guest operating system. Applicable only for
    // bootable images.
    repeated string guest_os_features = 8 [json_name = "guest_os_features"];

    // The disk interface used for attaching this disk. One of SCSI or NVME. (This field
    // is shared with attached_disk and only used for specific cases, please don't
    // specify this field without advice from Google.)
    optional string interface = 9;

    // The self_link of the encryption key that is stored in Google Cloud KMS to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_raw and
    // disk_encryption_key_rsa may be set.
    optional string kms_key_self_link = 10 [json_name = "kms_key_self_link"];

    // Read/write mode for the disk. One of "READ_ONLY" or "READ_WRITE".
    optional string mode = 11;

    // The name or self_link of the disk attached to this instance.
    optional string source = 12;

    InitializeParams initialize_params = 13 [json_name = "initialize_params"];

    message InitializeParams {
      // The architecture of the disk. One of "X86_64" or "ARM64".
      optional string architecture = 1;

      // A flag to enable confidential compute mode on boot disk
      optional bool enable_confidential_compute = 2 [json_name = "enable_confidential_compute"];

      // The image from which this disk was initialised.
      optional string image = 3;

      // A set of key/value label pairs assigned to the disk.
      map<string, string> labels = 4;

      // Indicates how many IOPS to provision for the disk. This sets the number of I/O
      // operations per second that the disk can handle.
      optional int64 provisioned_iops = 5 [json_name = "provisioned_iops"];

      // Indicates how much throughput to provision for the disk. This sets the number of
      // throughput mb per second that the disk can handle.
      optional int64 provisioned_throughput = 6 [json_name = "provisioned_throughput"];

      // A list of short names or self_links of zones in which to create a regional disk.
      repeated string replica_zones = 7 [json_name = "replica_zones"];

      // A map of resource manager tags. Resource manager tag keys and values have the
      // same definition as resource manager tags. Keys must be in the format
      // tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is
      // ignored (both PUT & PATCH) when empty.
      map<string, string> resource_manager_tags = 8 [json_name = "resource_manager_tags"];

      // A list of self_links of resource policies to attach to the instance's boot disk.
      // Modifying this list will cause the instance to recreate. Currently a max of 1
      // resource policy is supported.
      repeated string resource_policies = 9 [json_name = "resource_policies"];

      // The size of the image in gigabytes.
      optional int64 size = 10;

      // The snapshot from which this disk was initialised.
      optional string snapshot = 11;

      // The URL of the storage pool in which the new disk is created
      optional string storage_pool = 12 [json_name = "storage_pool"];

      // The Google Compute Engine disk type. Such as pd-standard, pd-ssd or pd-balanced.
      optional string type = 13;

      SourceImageEncryptionKey source_image_encryption_key = 14 [json_name = "source_image_encryption_key"];

      SourceSnapshotEncryptionKey source_snapshot_encryption_key = 15 [json_name = "source_snapshot_encryption_key"];

      message SourceImageEncryptionKey {
        // The self link of the encryption key that is stored in Google Cloud KMS. Only one
        // of kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

        // The service account being used for the encryption request for the given KMS key.
        // If absent, the Compute Engine default service account is used.
        optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

        // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
        // to either encrypt or decrypt this resource. Only one of kms_key_self_link,
        // rsa_encrypted_key and raw_key may be set.
        optional string raw_key = 3 [json_name = "raw_key"];

        // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
        // encryption key to either encrypt or decrypt this resource. Only one of
        // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

        // The SHA256 hash of the encryption key used to encrypt this disk.
        optional string sha256 = 5;
      }

      message SourceSnapshotEncryptionKey {
        // The self link of the encryption key that is stored in Google Cloud KMS. Only one
        // of kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

        // The service account being used for the encryption request for the given KMS key.
        // If absent, the Compute Engine default service account is used.
        optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

        // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
        // to either encrypt or decrypt this resource. Only one of kms_key_self_link,
        // rsa_encrypted_key and raw_key may be set.
        optional string raw_key = 3 [json_name = "raw_key"];

        // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
        // encryption key to either encrypt or decrypt this resource. Only one of
        // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

        // The SHA256 hash of the encryption key used to encrypt this disk.
        optional string sha256 = 5;
      }
    }
  }

  message ConfidentialInstanceConfig {
    // 								The confidential computing technology the instance uses.
    //
    // 								SEV is an AMD feature. TDX is an Intel feature. One of the following
    //
    // 								values is required: SEV, SEV_SNP, TDX. If SEV_SNP, min_cpu_platform =
    //
    // 								"AMD Milan" is currently required.
    optional string confidential_instance_type = 1 [json_name = "confidential_instance_type"];

    // Defines whether the instance should have confidential compute enabled. Field will
    // be deprecated in a future release
    optional bool enable_confidential_compute = 2 [json_name = "enable_confidential_compute"];
  }

  message GuestAccelerator {
    // The number of the guest accelerator cards exposed to this instance.
    optional int64 count = 1;

    // The accelerator type resource exposed to this instance. E.g. nvidia-tesla-k80.
    optional string type = 2;
  }

  message InstanceEncryptionKey {
    // The self link of the encryption key that is stored in Google Cloud KMS.
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account being used for the encryption request for the given KMS key.
    // If absent, the Compute Engine default service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // The SHA256 hash of the customer's encryption key.
    optional string sha256 = 3;
  }

  message NetworkInterface {
    // Indicates whether igmp query is enabled on the network interface or not. If
    // enabled, also indicates the version of IGMP supported.
    optional string igmp_query = 1 [json_name = "igmp_query"];

    // The prefix length of the primary internal IPv6 range.
    optional int64 internal_ipv6_prefix_length = 2 [json_name = "internal_ipv6_prefix_length"];

    // One of EXTERNAL, INTERNAL to indicate whether the IP can be accessed from the
    // Internet. This field is always inherited from its subnetwork.
    optional string ipv6_access_type = 3 [json_name = "ipv6_access_type"];

    // An IPv6 internal network address for this network interface. If not specified,
    // Google Cloud will automatically assign an internal IPv6 address from the
    // instance's subnetwork.
    optional string ipv6_address = 4 [json_name = "ipv6_address"];

    // The name of the interface
    optional string name = 5;

    // The name or self_link of the network attached to this interface.
    optional string network = 6;

    // The URL of the network attachment that this interface should connect to in the
    // following format:
    // rojectNumber}/regions/{region_name}/networkAttachments/{network_attachment_name}.
    optional string network_attachment = 7 [json_name = "network_attachment"];

    // The private IP address assigned to the instance.
    optional string network_ip = 8 [json_name = "network_ip"];

    // The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET,
    // IDPF, MRDMA, IRDMA and IDPF
    optional string nic_type = 9 [json_name = "nic_type"];

    // Name of the parent network interface of a dynamic network interface.
    optional string parent_nic_name = 10 [json_name = "parent_nic_name"];

    // The networking queue count that's specified by users for the network interface.
    // Both Rx and Tx queues will be set to this number. It will be empty if not
    // specified.
    optional int64 queue_count = 11 [json_name = "queue_count"];

    // The stack type for this network interface to identify whether the IPv6 feature is
    // enabled or not. If not specified, IPV4_ONLY will be used.
    optional string stack_type = 12 [json_name = "stack_type"];

    // The name or self_link of the subnetwork attached to this interface.
    optional string subnetwork = 13;

    // The project in which the subnetwork belongs.
    optional string subnetwork_project = 14 [json_name = "subnetwork_project"];

    // VLAN tag of a dynamic network interface, must be an integer in the range from 2
    // to 255 inclusively.
    optional int64 vlan = 15;

    repeated AccessConfig access_config = 16 [json_name = "access_config"];

    repeated AliasIpRange alias_ip_range = 17 [json_name = "alias_ip_range"];

    repeated Ipv6AccessConfig ipv6_access_config = 18 [json_name = "ipv6_access_config"];

    message AccessConfig {
      // The IP address that is be 1:1 mapped to the instance's network ip.
      optional string nat_ip = 1 [json_name = "nat_ip"];

      // The networking tier used for configuring this instance. One of PREMIUM or
      // STANDARD.
      optional string network_tier = 2 [json_name = "network_tier"];

      // The DNS domain name for the public PTR record.
      optional string public_ptr_domain_name = 3 [json_name = "public_ptr_domain_name"];
    }

    message AliasIpRange {
      // The IP CIDR range represented by this alias IP range.
      optional string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

      // The subnetwork secondary range name specifying the secondary range from which to
      // allocate the IP CIDR range for this alias IP range.
      optional string subnetwork_range_name = 2 [json_name = "subnetwork_range_name"];
    }

    message Ipv6AccessConfig {
      // The first IPv6 address of the external IPv6 range associated with this instance,
      // prefix length is stored in externalIpv6PrefixLength in ipv6AccessConfig. To use a
      // static external IP address, it must be unused and in the same region as the
      // instance's zone. If not specified, Google Cloud will automatically assign an
      // external IPv6 address from the instance's subnetwork.
      optional string external_ipv6 = 1 [json_name = "external_ipv6"];

      // The prefix length of the external IPv6 range.
      optional string external_ipv6_prefix_length = 2 [json_name = "external_ipv6_prefix_length"];

      // The name of this access configuration. In ipv6AccessConfigs, the recommended name
      // is External IPv6.
      optional string name = 3;

      // The service-level to be provided for IPv6 traffic when the subnet has an external
      // subnet. Only PREMIUM tier is valid for IPv6
      optional string network_tier = 4 [json_name = "network_tier"];

      // The domain name to be used when creating DNSv6 records for the external IPv6
      // ranges.
      optional string public_ptr_domain_name = 5 [json_name = "public_ptr_domain_name"];
    }
  }

  message NetworkPerformanceConfig {
    // The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT
    optional string total_egress_bandwidth_tier = 1 [json_name = "total_egress_bandwidth_tier"];
  }

  message Params {
    // A map of resource manager tags. Resource manager tag keys and values have the
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is
    // ignored (both PUT & PATCH) when empty.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message ReservationAffinity {
    // The type of reservation from which this instance can consume resources.
    optional string type = 1;

    SpecificReservation specific_reservation = 2 [json_name = "specific_reservation"];

    message SpecificReservation {
      // Corresponds to the label key of a reservation resource. To target a
      // SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as
      // the key and specify the name of your reservation as the only value.
      optional string key = 1;

      // Corresponds to the label values of a reservation resource.
      repeated string values = 2;
    }
  }

  message Scheduling {
    // Specifies if the instance should be restarted if it was terminated by Compute
    // Engine (not a user).
    optional bool automatic_restart = 1 [json_name = "automatic_restart"];

    // Specifies the availability domain, which this instance should be scheduled on.
    optional int64 availability_domain = 2 [json_name = "availability_domain"];

    // Specify the time in seconds for host error detection, the value must be within
    // the range of [90, 330] with the increment of 30, if unset, the default behavior
    // of host error recovery will be used.
    optional int64 host_error_timeout_seconds = 3 [json_name = "host_error_timeout_seconds"];

    // Specifies the action GCE should take when SPOT VM is preempted.
    optional string instance_termination_action = 4 [json_name = "instance_termination_action"];

    optional int64 min_node_cpus = 5 [json_name = "min_node_cpus"];

    // Describes maintenance behavior for the instance. One of MIGRATE or TERMINATE,
    optional string on_host_maintenance = 6 [json_name = "on_host_maintenance"];

    // Whether the instance is preemptible.
    optional bool preemptible = 7;

    // Describes the desired provisioning model for the instance. Possible values are
    // STANDARD, SPOT, FLEX_START, and RESERVATION_BOUND. For STANDARD, resources are
    // provisioned immediately. For SPOT, resources are offered at a discount compared
    // to standard pricing but may be preempted. For FLEX_START, resources are offered
    // at a discount with flexible start times. For RESERVATION_BOUND, the instance is
    // bound to a specific reservation and will only consume capacity from that
    // reservation.
    optional string provisioning_model = 8 [json_name = "provisioning_model"];

    // Specifies the timestamp, when the instance will be terminated,
    //
    // in RFC3339 text format. If specified, the instance termination action
    //
    // will be performed at the termination time.
    optional string termination_time = 9 [json_name = "termination_time"];

    LocalSsdRecoveryTimeout local_ssd_recovery_timeout = 10 [json_name = "local_ssd_recovery_timeout"];

    MaxRunDuration max_run_duration = 11 [json_name = "max_run_duration"];

    repeated NodeAffinities node_affinities = 12 [json_name = "node_affinities"];

    OnInstanceStopAction on_instance_stop_action = 13 [json_name = "on_instance_stop_action"];

    message LocalSsdRecoveryTimeout {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message MaxRunDuration {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message NodeAffinities {
      optional string key = 1;

      optional string operator = 2;

      repeated string values = 3;
    }

    message OnInstanceStopAction {
      // If true, the contents of any attached Local SSD disks will be discarded.
      optional bool discard_local_ssd = 1 [json_name = "discard_local_ssd"];
    }
  }

  message ScratchDisk {
    // Name with which the attached disk is accessible under /dev/disk/by-id/
    optional string device_name = 1 [json_name = "device_name"];

    // The disk interface used for attaching this disk. One of SCSI or NVME.
    optional string interface = 2;

    // The size of the disk in gigabytes. One of 375 or 3000.
    optional int64 size = 3;
  }

  message ServiceAccount {
    // The service account e-mail address.
    optional string email = 1;

    // A list of service scopes.
    repeated string scopes = 2;
  }

  message ShieldedInstanceConfig {
    // Whether integrity monitoring is enabled for the instance.
    optional bool enable_integrity_monitoring = 1 [json_name = "enable_integrity_monitoring"];

    // Whether secure boot is enabled for the instance.
    optional bool enable_secure_boot = 2 [json_name = "enable_secure_boot"];

    // Whether the instance uses vTPM.
    optional bool enable_vtpm = 3 [json_name = "enable_vtpm"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message WorkloadIdentityConfig {
    // Identity SPIFFE id.
    optional string identity = 1;

    // Specifies whether identity certificates are enabled.
    optional bool identity_certificate_enabled = 2 [json_name = "identity_certificate_enabled"];
  }
}

// GoogleComputeInstanceFromTemplate version is 0
message GoogleComputeInstanceFromTemplate {
  // If true, allows Terraform to stop the instance to update its properties. If you
  // try to update a property that requires stopping the instance without setting this
  // field, the update will fail.
  optional bool allow_stopping_for_update = 1 [json_name = "allow_stopping_for_update"];

  // Whether sending and receiving of packets with non-matching source or destination
  // IPs is allowed.
  optional bool can_ip_forward = 2 [json_name = "can_ip_forward"];

  // The CPU platform used by this instance.
  optional string cpu_platform = 3 [json_name = "cpu_platform"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // 					Current status of the instance.
  //
  // 					This could be one of the following values: PROVISIONING, STAGING, RUNNING,
  // STOPPING, SUSPENDING, SUSPENDED, REPAIRING, and TERMINATED.
  //
  // 					For more information about the status of the instance, see [Instance life
  // cycle](https://cloud.google.com/compute/docs/instances/instance-life-cycle).
  optional string current_status = 5 [json_name = "current_status"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 6 [json_name = "deletion_policy"];

  // Whether deletion protection is enabled on this instance.
  optional bool deletion_protection = 7 [json_name = "deletion_protection"];

  // A brief description of the resource.
  optional string description = 8;

  // Desired status of the instance. Either "RUNNING", "SUSPENDED" or "TERMINATED".
  optional string desired_status = 9 [json_name = "desired_status"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 10 [json_name = "effective_labels"];

  // Whether the instance has virtual displays enabled.
  optional bool enable_display = 11 [json_name = "enable_display"];

  // A custom hostname for the instance. Must be a fully qualified DNS name and
  // RFC-1035-valid. Valid format is a series of labels 1-63 characters long matching
  // the regular expression [a-z]([-a-z0-9]*[a-z0-9]), concatenated with periods. The
  // entire hostname must not exceed 253 characters. Changing this forces a new
  // resource to be created.
  optional string hostname = 12;

  optional string id = 13;

  // The server-assigned unique identifier of this instance.
  optional string instance_id = 14 [json_name = "instance_id"];

  // Action to be taken when a customer's encryption key is revoked. Supports "STOP"
  // and "NONE", with "NONE" being the default.
  optional string key_revocation_action_type = 15 [json_name = "key_revocation_action_type"];

  // The unique fingerprint of the labels.
  optional string label_fingerprint = 16 [json_name = "label_fingerprint"];

  // A set of key/value label pairs assigned to the instance.
  //
  //
  // 				**Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // 				Please refer to the field 'effective_labels' for all of the labels present on
  // the resource.
  map<string, string> labels = 17;

  // The machine type to create.
  optional string machine_type = 18 [json_name = "machine_type"];

  // Metadata key/value pairs made available within the instance.
  map<string, string> metadata = 19;

  // The unique fingerprint of the metadata.
  optional string metadata_fingerprint = 20 [json_name = "metadata_fingerprint"];

  // Metadata startup scripts made available within the instance.
  optional string metadata_startup_script = 21 [json_name = "metadata_startup_script"];

  // The minimum CPU platform specified for the VM instance.
  optional string min_cpu_platform = 22 [json_name = "min_cpu_platform"];

  // The name of the instance. One of name or self_link must be provided.
  optional string name = 23;

  // The ID of the project in which the resource belongs. If self_link is provided,
  // this value is ignored. If neither self_link nor project are provided, the
  // provider project is used.
  optional string project = 24;

  // A list of self_links of resource policies to attach to the instance. Currently a
  // max of 1 resource policy is supported.
  repeated string resource_policies = 25 [json_name = "resource_policies"];

  // The URI of the created resource.
  optional string self_link = 26 [json_name = "self_link"];

  // Name or self link of an instance template to create the instance based on.
  optional string source_instance_template = 27 [json_name = "source_instance_template"];

  // The list of tags attached to the instance.
  repeated string tags = 28;

  // The unique fingerprint of the tags.
  optional string tags_fingerprint = 29 [json_name = "tags_fingerprint"];

  // The combination of labels configured directly on the resource and default labels
  // configured on the provider.
  map<string, string> terraform_labels = 30 [json_name = "terraform_labels"];

  // The zone of the instance. If self_link is provided, this value is ignored. If
  // neither self_link nor zone are provided, the provider zone is used.
  optional string zone = 31;

  AdvancedMachineFeatures advanced_machine_features = 32 [json_name = "advanced_machine_features"];

  repeated AttachedDisk attached_disk = 33 [json_name = "attached_disk"];

  BootDisk boot_disk = 34 [json_name = "boot_disk"];

  ConfidentialInstanceConfig confidential_instance_config = 35 [json_name = "confidential_instance_config"];

  repeated GuestAccelerator guest_accelerator = 36 [json_name = "guest_accelerator"];

  InstanceEncryptionKey instance_encryption_key = 37 [json_name = "instance_encryption_key"];

  repeated NetworkInterface network_interface = 38 [json_name = "network_interface"];

  NetworkPerformanceConfig network_performance_config = 39 [json_name = "network_performance_config"];

  Params params = 40;

  ReservationAffinity reservation_affinity = 41 [json_name = "reservation_affinity"];

  Scheduling scheduling = 42;

  repeated ScratchDisk scratch_disk = 43 [json_name = "scratch_disk"];

  ServiceAccount service_account = 44 [json_name = "service_account"];

  ShieldedInstanceConfig shielded_instance_config = 45 [json_name = "shielded_instance_config"];

  repeated Timeouts timeouts = 46;

  WorkloadIdentityConfig workload_identity_config = 47 [json_name = "workload_identity_config"];

  google.protobuf.Value for_each = 48 [json_name = "for_each"];

  repeated string depends_on = 49 [json_name = "depends_on"];

  optional int32 count = 50;

  optional string provider = 51;

  terraform.v1.Lifecycle lifecycle = 52;

  message AdvancedMachineFeatures {
    // Whether to enable nested virtualization or not.
    optional bool enable_nested_virtualization = 1 [json_name = "enable_nested_virtualization"];

    // Whether to enable UEFI networking for the instance.
    optional bool enable_uefi_networking = 2 [json_name = "enable_uefi_networking"];

    // The PMU is a hardware component within the CPU core that monitors how the
    // processor runs code. Valid values for the level of PMU are "STANDARD",
    // "ENHANCED", and "ARCHITECTURAL".
    optional string performance_monitoring_unit = 3 [json_name = "performance_monitoring_unit"];

    // The number of threads per physical core. To disable simultaneous multithreading
    // (SMT) set this to 1. If unset, the maximum number of threads supported per core
    // by the underlying processor is assumed.
    optional int64 threads_per_core = 4 [json_name = "threads_per_core"];

    // Turbo frequency mode to use for the instance. Currently supported modes is
    // "ALL_CORE_MAX".
    optional string turbo_mode = 5 [json_name = "turbo_mode"];

    // The number of physical cores to expose to an instance. Multiply by the number of
    // threads per core to compute the total number of virtual CPUs to expose to the
    // instance. If unset, the number of cores is inferred from the instance\'s nominal
    // CPU count and the underlying platform\'s SMT width.
    optional int64 visible_core_count = 6 [json_name = "visible_core_count"];
  }

  message AttachedDisk {
    // Name with which the attached disk is accessible under /dev/disk/by-id/
    optional string device_name = 1 [json_name = "device_name"];

    // A 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_rsa and
    // disk_encryption_key_raw may be set.
    optional string disk_encryption_key_raw = 2 [json_name = "disk_encryption_key_raw"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
    // encryption key to either encrypt or decrypt this resource. Only one of
    // kms_key_self_link, disk_encryption_key_rsa and disk_encryption_key_raw may be
    // set.
    optional string disk_encryption_key_rsa = 3 [json_name = "disk_encryption_key_rsa"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key
    // that protects this resource.
    optional string disk_encryption_key_sha256 = 4 [json_name = "disk_encryption_key_sha256"];

    // The service account being used for the encryption request for the given KMS key.
    // If absent, the Compute Engine default service account is used
    optional string disk_encryption_service_account = 5 [json_name = "disk_encryption_service_account"];

    // Whether to force attach the regional disk even if it's currently attached to
    // another instance. If you try to force attach a zonal disk to an instance, you
    // will receive an error. Setting this parameter cause VM recreation.
    optional bool force_attach = 6 [json_name = "force_attach"];

    // The self_link of the encryption key that is stored in Google Cloud KMS to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_rsa and
    // disk_encryption_key_raw may be set.
    optional string kms_key_self_link = 7 [json_name = "kms_key_self_link"];

    // Read/write mode for the disk. One of "READ_ONLY" or "READ_WRITE".
    optional string mode = 8;

    // The name or self_link of the disk attached to this instance.
    optional string source = 9;
  }

  message BootDisk {
    // Whether the disk will be auto-deleted when the instance is deleted.
    optional bool auto_delete = 1 [json_name = "auto_delete"];

    // Name with which attached disk will be accessible under /dev/disk/by-id/
    optional string device_name = 2 [json_name = "device_name"];

    // A 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_raw and
    // disk_encryption_key_rsa may be set.
    optional string disk_encryption_key_raw = 3 [json_name = "disk_encryption_key_raw"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
    // encryption key to either encrypt or decrypt this resource. Only one of
    // kms_key_self_link, disk_encryption_key_raw and disk_encryption_key_rsa may be
    // set.
    optional string disk_encryption_key_rsa = 4 [json_name = "disk_encryption_key_rsa"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied encryption key
    // that protects this resource.
    optional string disk_encryption_key_sha256 = 5 [json_name = "disk_encryption_key_sha256"];

    // The service account being used for the encryption request for the given KMS key.
    // If absent, the Compute Engine default service account is used
    optional string disk_encryption_service_account = 6 [json_name = "disk_encryption_service_account"];

    // Whether to force attach the regional disk even if it's currently attached to
    // another instance. If you try to force attach a zonal disk to an instance, you
    // will receive an error. Setting this parameter cause VM recreation.
    optional bool force_attach = 7 [json_name = "force_attach"];

    // A list of features to enable on the guest operating system. Applicable only for
    // bootable images.
    repeated string guest_os_features = 8 [json_name = "guest_os_features"];

    // The disk interface used for attaching this disk. One of SCSI or NVME. (This field
    // is shared with attached_disk and only used for specific cases, please don't
    // specify this field without advice from Google.)
    optional string interface = 9;

    // The self_link of the encryption key that is stored in Google Cloud KMS to encrypt
    // this disk. Only one of kms_key_self_link, disk_encryption_key_raw and
    // disk_encryption_key_rsa may be set.
    optional string kms_key_self_link = 10 [json_name = "kms_key_self_link"];

    // Read/write mode for the disk. One of "READ_ONLY" or "READ_WRITE".
    optional string mode = 11;

    // The name or self_link of the disk attached to this instance.
    optional string source = 12;

    InitializeParams initialize_params = 13 [json_name = "initialize_params"];

    message InitializeParams {
      // The architecture of the disk. One of "X86_64" or "ARM64".
      optional string architecture = 1;

      // A flag to enable confidential compute mode on boot disk
      optional bool enable_confidential_compute = 2 [json_name = "enable_confidential_compute"];

      // The image from which this disk was initialised.
      optional string image = 3;

      // A set of key/value label pairs assigned to the disk.
      map<string, string> labels = 4;

      // Indicates how many IOPS to provision for the disk. This sets the number of I/O
      // operations per second that the disk can handle.
      optional int64 provisioned_iops = 5 [json_name = "provisioned_iops"];

      // Indicates how much throughput to provision for the disk. This sets the number of
      // throughput mb per second that the disk can handle.
      optional int64 provisioned_throughput = 6 [json_name = "provisioned_throughput"];

      // A list of short names or self_links of zones in which to create a regional disk.
      repeated string replica_zones = 7 [json_name = "replica_zones"];

      // A map of resource manager tags. Resource manager tag keys and values have the
      // same definition as resource manager tags. Keys must be in the format
      // tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is
      // ignored (both PUT & PATCH) when empty.
      map<string, string> resource_manager_tags = 8 [json_name = "resource_manager_tags"];

      // A list of self_links of resource policies to attach to the instance's boot disk.
      // Modifying this list will cause the instance to recreate. Currently a max of 1
      // resource policy is supported.
      repeated string resource_policies = 9 [json_name = "resource_policies"];

      // The size of the image in gigabytes.
      optional int64 size = 10;

      // The snapshot from which this disk was initialised.
      optional string snapshot = 11;

      // The URL of the storage pool in which the new disk is created
      optional string storage_pool = 12 [json_name = "storage_pool"];

      // The Google Compute Engine disk type. Such as pd-standard, pd-ssd or pd-balanced.
      optional string type = 13;

      SourceImageEncryptionKey source_image_encryption_key = 14 [json_name = "source_image_encryption_key"];

      SourceSnapshotEncryptionKey source_snapshot_encryption_key = 15 [json_name = "source_snapshot_encryption_key"];

      message SourceImageEncryptionKey {
        // The self link of the encryption key that is stored in Google Cloud KMS. Only one
        // of kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

        // The service account being used for the encryption request for the given KMS key.
        // If absent, the Compute Engine default service account is used.
        optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

        // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
        // to either encrypt or decrypt this resource. Only one of kms_key_self_link,
        // rsa_encrypted_key and raw_key may be set.
        optional string raw_key = 3 [json_name = "raw_key"];

        // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
        // encryption key to either encrypt or decrypt this resource. Only one of
        // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

        // The SHA256 hash of the encryption key used to encrypt this disk.
        optional string sha256 = 5;
      }

      message SourceSnapshotEncryptionKey {
        // The self link of the encryption key that is stored in Google Cloud KMS. Only one
        // of kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

        // The service account being used for the encryption request for the given KMS key.
        // If absent, the Compute Engine default service account is used.
        optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

        // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
        // to either encrypt or decrypt this resource. Only one of kms_key_self_link,
        // rsa_encrypted_key and raw_key may be set.
        optional string raw_key = 3 [json_name = "raw_key"];

        // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
        // encryption key to either encrypt or decrypt this resource. Only one of
        // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
        optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

        // The SHA256 hash of the encryption key used to encrypt this disk.
        optional string sha256 = 5;
      }
    }
  }

  message ConfidentialInstanceConfig {
    // 								The confidential computing technology the instance uses.
    //
    // 								SEV is an AMD feature. TDX is an Intel feature. One of the following
    //
    // 								values is required: SEV, SEV_SNP, TDX. If SEV_SNP, min_cpu_platform =
    //
    // 								"AMD Milan" is currently required.
    optional string confidential_instance_type = 1 [json_name = "confidential_instance_type"];

    // Defines whether the instance should have confidential compute enabled. Field will
    // be deprecated in a future release
    optional bool enable_confidential_compute = 2 [json_name = "enable_confidential_compute"];
  }

  message GuestAccelerator {
    // The number of the guest accelerator cards exposed to this instance.
    optional int64 count = 1;

    // The accelerator type resource exposed to this instance. E.g. nvidia-tesla-k80.
    optional string type = 2;
  }

  message InstanceEncryptionKey {
    // The self link of the encryption key that is stored in Google Cloud KMS.
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account being used for the encryption request for the given KMS key.
    // If absent, the Compute Engine default service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // The SHA256 hash of the customer's encryption key.
    optional string sha256 = 3;
  }

  message NetworkInterface {
    // Indicates whether igmp query is enabled on the network interface or not. If
    // enabled, also indicates the version of IGMP supported.
    optional string igmp_query = 1 [json_name = "igmp_query"];

    // The prefix length of the primary internal IPv6 range.
    optional int64 internal_ipv6_prefix_length = 2 [json_name = "internal_ipv6_prefix_length"];

    // One of EXTERNAL, INTERNAL to indicate whether the IP can be accessed from the
    // Internet. This field is always inherited from its subnetwork.
    optional string ipv6_access_type = 3 [json_name = "ipv6_access_type"];

    // An IPv6 internal network address for this network interface. If not specified,
    // Google Cloud will automatically assign an internal IPv6 address from the
    // instance's subnetwork.
    optional string ipv6_address = 4 [json_name = "ipv6_address"];

    // The name of the interface
    optional string name = 5;

    // The name or self_link of the network attached to this interface.
    optional string network = 6;

    // The URL of the network attachment that this interface should connect to in the
    // following format:
    // rojectNumber}/regions/{region_name}/networkAttachments/{network_attachment_name}.
    optional string network_attachment = 7 [json_name = "network_attachment"];

    // The private IP address assigned to the instance.
    optional string network_ip = 8 [json_name = "network_ip"];

    // The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET,
    // IDPF, MRDMA, IRDMA and IDPF
    optional string nic_type = 9 [json_name = "nic_type"];

    // Name of the parent network interface of a dynamic network interface.
    optional string parent_nic_name = 10 [json_name = "parent_nic_name"];

    // The networking queue count that's specified by users for the network interface.
    // Both Rx and Tx queues will be set to this number. It will be empty if not
    // specified.
    optional int64 queue_count = 11 [json_name = "queue_count"];

    // The stack type for this network interface to identify whether the IPv6 feature is
    // enabled or not. If not specified, IPV4_ONLY will be used.
    optional string stack_type = 12 [json_name = "stack_type"];

    // The name or self_link of the subnetwork attached to this interface.
    optional string subnetwork = 13;

    // The project in which the subnetwork belongs.
    optional string subnetwork_project = 14 [json_name = "subnetwork_project"];

    // VLAN tag of a dynamic network interface, must be an integer in the range from 2
    // to 255 inclusively.
    optional int64 vlan = 15;

    repeated AccessConfig access_config = 16 [json_name = "access_config"];

    repeated AliasIpRange alias_ip_range = 17 [json_name = "alias_ip_range"];

    repeated Ipv6AccessConfig ipv6_access_config = 18 [json_name = "ipv6_access_config"];

    message AccessConfig {
      // The IP address that is be 1:1 mapped to the instance's network ip.
      optional string nat_ip = 1 [json_name = "nat_ip"];

      // The networking tier used for configuring this instance. One of PREMIUM or
      // STANDARD.
      optional string network_tier = 2 [json_name = "network_tier"];

      // The DNS domain name for the public PTR record.
      optional string public_ptr_domain_name = 3 [json_name = "public_ptr_domain_name"];
    }

    message AliasIpRange {
      // The IP CIDR range represented by this alias IP range.
      optional string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

      // The subnetwork secondary range name specifying the secondary range from which to
      // allocate the IP CIDR range for this alias IP range.
      optional string subnetwork_range_name = 2 [json_name = "subnetwork_range_name"];
    }

    message Ipv6AccessConfig {
      // The first IPv6 address of the external IPv6 range associated with this instance,
      // prefix length is stored in externalIpv6PrefixLength in ipv6AccessConfig. To use a
      // static external IP address, it must be unused and in the same region as the
      // instance's zone. If not specified, Google Cloud will automatically assign an
      // external IPv6 address from the instance's subnetwork.
      optional string external_ipv6 = 1 [json_name = "external_ipv6"];

      // The prefix length of the external IPv6 range.
      optional string external_ipv6_prefix_length = 2 [json_name = "external_ipv6_prefix_length"];

      // The name of this access configuration. In ipv6AccessConfigs, the recommended name
      // is External IPv6.
      optional string name = 3;

      // The service-level to be provided for IPv6 traffic when the subnet has an external
      // subnet. Only PREMIUM tier is valid for IPv6
      optional string network_tier = 4 [json_name = "network_tier"];

      // The domain name to be used when creating DNSv6 records for the external IPv6
      // ranges.
      optional string public_ptr_domain_name = 5 [json_name = "public_ptr_domain_name"];
    }
  }

  message NetworkPerformanceConfig {
    // The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT
    optional string total_egress_bandwidth_tier = 1 [json_name = "total_egress_bandwidth_tier"];
  }

  message Params {
    // A map of resource manager tags. Resource manager tag keys and values have the
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is
    // ignored (both PUT & PATCH) when empty.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message ReservationAffinity {
    // The type of reservation from which this instance can consume resources.
    optional string type = 1;

    SpecificReservation specific_reservation = 2 [json_name = "specific_reservation"];

    message SpecificReservation {
      // Corresponds to the label key of a reservation resource. To target a
      // SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as
      // the key and specify the name of your reservation as the only value.
      optional string key = 1;

      // Corresponds to the label values of a reservation resource.
      repeated string values = 2;
    }
  }

  message Scheduling {
    // Specifies if the instance should be restarted if it was terminated by Compute
    // Engine (not a user).
    optional bool automatic_restart = 1 [json_name = "automatic_restart"];

    // Specifies the availability domain, which this instance should be scheduled on.
    optional int64 availability_domain = 2 [json_name = "availability_domain"];

    // Specify the time in seconds for host error detection, the value must be within
    // the range of [90, 330] with the increment of 30, if unset, the default behavior
    // of host error recovery will be used.
    optional int64 host_error_timeout_seconds = 3 [json_name = "host_error_timeout_seconds"];

    // Specifies the action GCE should take when SPOT VM is preempted.
    optional string instance_termination_action = 4 [json_name = "instance_termination_action"];

    optional int64 min_node_cpus = 5 [json_name = "min_node_cpus"];

    // Describes maintenance behavior for the instance. One of MIGRATE or TERMINATE,
    optional string on_host_maintenance = 6 [json_name = "on_host_maintenance"];

    // Whether the instance is preemptible.
    optional bool preemptible = 7;

    // Describes the desired provisioning model for the instance. Possible values are
    // STANDARD, SPOT, FLEX_START, and RESERVATION_BOUND. For STANDARD, resources are
    // provisioned immediately. For SPOT, resources are offered at a discount compared
    // to standard pricing but may be preempted. For FLEX_START, resources are offered
    // at a discount with flexible start times. For RESERVATION_BOUND, the instance is
    // bound to a specific reservation and will only consume capacity from that
    // reservation.
    optional string provisioning_model = 8 [json_name = "provisioning_model"];

    // Specifies the timestamp, when the instance will be terminated,
    //
    // in RFC3339 text format. If specified, the instance termination action
    //
    // will be performed at the termination time.
    optional string termination_time = 9 [json_name = "termination_time"];

    LocalSsdRecoveryTimeout local_ssd_recovery_timeout = 10 [json_name = "local_ssd_recovery_timeout"];

    MaxRunDuration max_run_duration = 11 [json_name = "max_run_duration"];

    repeated NodeAffinities node_affinities = 12 [json_name = "node_affinities"];

    OnInstanceStopAction on_instance_stop_action = 13 [json_name = "on_instance_stop_action"];

    message LocalSsdRecoveryTimeout {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message MaxRunDuration {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message NodeAffinities {
      optional string key = 1;

      optional string operator = 2;

      repeated string values = 3;
    }

    message OnInstanceStopAction {
      // If true, the contents of any attached Local SSD disks will be discarded.
      optional bool discard_local_ssd = 1 [json_name = "discard_local_ssd"];
    }
  }

  message ScratchDisk {
    // Name with which the attached disk is accessible under /dev/disk/by-id/
    optional string device_name = 1 [json_name = "device_name"];

    // The disk interface used for attaching this disk. One of SCSI or NVME.
    optional string interface = 2;

    // The size of the disk in gigabytes. One of 375 or 3000.
    optional int64 size = 3;
  }

  message ServiceAccount {
    // The service account e-mail address.
    optional string email = 1;

    // A list of service scopes.
    repeated string scopes = 2;
  }

  message ShieldedInstanceConfig {
    // Whether integrity monitoring is enabled for the instance.
    optional bool enable_integrity_monitoring = 1 [json_name = "enable_integrity_monitoring"];

    // Whether secure boot is enabled for the instance.
    optional bool enable_secure_boot = 2 [json_name = "enable_secure_boot"];

    // Whether the instance uses vTPM.
    optional bool enable_vtpm = 3 [json_name = "enable_vtpm"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message WorkloadIdentityConfig {
    // Identity SPIFFE id.
    optional string identity = 1;

    // Specifies whether identity certificates are enabled.
    optional bool identity_certificate_enabled = 2 [json_name = "identity_certificate_enabled"];
  }
}

// GoogleComputeInstanceGroup version is 2
message GoogleComputeInstanceGroup {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional textual description of the instance group.
  optional string description = 2;

  optional string id = 3;

  // The list of instances in the group, in self_link format. When adding instances
  // they must all be in the same network and zone as the instance group.
  repeated string instances = 4;

  // The name of the instance group. Must be 1-63 characters long and comply with
  // RFC1035. Supported characters include lowercase letters, numbers, and hyphens.
  optional string name = 5;

  // The URL of the network the instance group is in. If this is different from the
  // network where the instances are in, the creation fails. Defaults to the network
  // where the instances are in (if neither network nor instances is specified, this
  // field will be blank).
  optional string network = 6;

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 7;

  // The URI of the created resource.
  optional string self_link = 8 [json_name = "self_link"];

  // The number of instances in the group.
  optional int64 size = 9;

  // The zone that this instance group should be created in.
  optional string zone = 10;

  repeated NamedPort named_port = 11 [json_name = "named_port"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message NamedPort {
    // The name which the port will be mapped to.
    optional string name = 1;

    // The port number to map the name to.
    optional int64 port = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeInstanceGroupManager version is 0
message GoogleComputeInstanceGroupManager {
  // The base instance name to use for instances in this group. The value must be a
  // valid RFC1035 name. Supported characters are lowercase letters, numbers, and
  // hyphens (-). Instances are named by appending a hyphen and a random
  // four-character string to the base instance name.
  optional string base_instance_name = 1 [json_name = "base_instance_name"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional textual description of the instance group manager.
  optional string description = 4;

  // The fingerprint of the instance group manager.
  optional string fingerprint = 5;

  optional string id = 6;

  // The full URL of the instance group created by the manager.
  optional string instance_group = 7 [json_name = "instance_group"];

  // The unique identifier number for the resource. This identifier is defined by the
  // server.
  optional int64 instance_group_manager_id = 8 [json_name = "instance_group_manager_id"];

  // Pagination behavior of the listManagedInstances API method for this managed
  // instance group. Valid values are: "PAGELESS", "PAGINATED". If PAGELESS (default),
  // Pagination is disabled for the group's listManagedInstances API method.
  // maxResults and pageToken query parameters are ignored and all instances are
  // returned in a single response. If PAGINATED, pagination is enabled, maxResults
  // and pageToken query parameters are respected.
  optional string list_managed_instances_results = 9 [json_name = "list_managed_instances_results"];

  // The name of the instance group manager. Must be 1-63 characters long and comply
  // with RFC1035. Supported characters include lowercase letters, numbers, and
  // hyphens.
  optional string name = 10;

  optional string operation = 11;

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 12;

  // The URL of the created resource.
  optional string self_link = 13 [json_name = "self_link"];

  // The status of this managed instance group.
  repeated Status status = 14;

  // The full URL of all target pools to which new instances in the group are added.
  // Updating the target pools attribute does not affect existing instances.
  repeated string target_pools = 15 [json_name = "target_pools"];

  // The target number of running instances for this managed instance group. This
  // value should always be explicitly set unless this resource is attached to an
  // autoscaler, in which case it should never be set. Defaults to 0.
  optional int64 target_size = 16 [json_name = "target_size"];

  // The target number of stopped instances for this managed instance group.
  optional int64 target_stopped_size = 17 [json_name = "target_stopped_size"];

  // The target number of suspended instances for this managed instance group.
  optional int64 target_suspended_size = 18 [json_name = "target_suspended_size"];

  // Whether to wait for all instances to be created/updated before returning. Note
  // that if this is set to true and the operation does not succeed, Terraform will
  // continue trying until it times out.
  optional bool wait_for_instances = 19 [json_name = "wait_for_instances"];

  // When used with wait_for_instances specifies the status to wait for. When STABLE
  // is specified this resource will wait until the instances are stable before
  // returning. When UPDATED is set, it will wait for the version target to be reached
  // and any per instance configs to be effective and all instances configs to be
  // effective as well as all instances to be stable before returning.
  optional string wait_for_instances_status = 20 [json_name = "wait_for_instances_status"];

  // The zone that instances in this group should be created in.
  optional string zone = 21;

  AllInstancesConfig all_instances_config = 22 [json_name = "all_instances_config"];

  AutoHealingPolicies auto_healing_policies = 23 [json_name = "auto_healing_policies"];

  InstanceLifecyclePolicy instance_lifecycle_policy = 24 [json_name = "instance_lifecycle_policy"];

  repeated NamedPort named_port = 25 [json_name = "named_port"];

  ResourcePolicies resource_policies = 26 [json_name = "resource_policies"];

  StandbyPolicy standby_policy = 27 [json_name = "standby_policy"];

  repeated StatefulDisk stateful_disk = 28 [json_name = "stateful_disk"];

  repeated StatefulExternalIp stateful_external_ip = 29 [json_name = "stateful_external_ip"];

  repeated StatefulInternalIp stateful_internal_ip = 30 [json_name = "stateful_internal_ip"];

  repeated TargetSizePolicy target_size_policy = 31 [json_name = "target_size_policy"];

  repeated Timeouts timeouts = 32;

  UpdatePolicy update_policy = 33 [json_name = "update_policy"];

  repeated Version version = 34;

  google.protobuf.Value for_each = 35 [json_name = "for_each"];

  repeated string depends_on = 36 [json_name = "depends_on"];

  optional int32 count = 37;

  optional string provider = 38;

  terraform.v1.Lifecycle lifecycle = 39;

  message Status {
    // all_instances_config: list of object
    repeated AllInstancesConfig all_instances_config = 1 [json_name = "all_instances_config"];

    // is_stable: bool
    optional bool is_stable = 2 [json_name = "is_stable"];

    // stateful: list of object
    repeated Stateful stateful = 3;

    // version_target: list of object
    repeated VersionTarget version_target = 4 [json_name = "version_target"];

    message AllInstancesConfig {
      // current_revision: string
      optional string current_revision = 1 [json_name = "current_revision"];

      // effective: bool
      optional bool effective = 2;
    }

    message Stateful {
      // has_stateful_config: bool
      optional bool has_stateful_config = 1 [json_name = "has_stateful_config"];

      // per_instance_configs: list of object
      repeated PerInstanceConfigs per_instance_configs = 2 [json_name = "per_instance_configs"];

      message PerInstanceConfigs {
        // all_effective: bool
        optional bool all_effective = 1 [json_name = "all_effective"];
      }
    }

    message VersionTarget {
      // is_reached: bool
      optional bool is_reached = 1 [json_name = "is_reached"];
    }
  }

  message AllInstancesConfig {
    // The label key-value pairs that you want to patch onto the instance,
    map<string, string> labels = 1;

    // The metadata key-value pairs that you want to patch onto the instance. For more
    // information, see Project and instance metadata,
    map<string, string> metadata = 2;
  }

  message AutoHealingPolicies {
    // The health check resource that signals autohealing.
    optional string health_check = 1 [json_name = "health_check"];

    // The number of seconds that the managed instance group waits before it applies
    // autohealing policies to new instances or recently recreated instances. Between 0
    // and 3600.
    optional int64 initial_delay_sec = 2 [json_name = "initial_delay_sec"];
  }

  message InstanceLifecyclePolicy {
    // Specifies the action that a MIG performs on a failed VM. If the value of the
    // "on_failed_health_check" field is DEFAULT_ACTION, then the same action also
    // applies to the VMs on which your application fails a health check. Valid values
    // are: REPAIR, DO_NOTHING. If REPAIR (default), then MIG automatically repairs a
    // failed VM by recreating it. For more information, see about repairing VMs in a
    // MIG. If DO_NOTHING, then MIG does not repair a failed VM.
    optional string default_action_on_failure = 1 [json_name = "default_action_on_failure"];

    // Specifies whether to apply the group's latest configuration when repairing a VM.
    // Valid options are: YES, NO. If YES and you updated the group's instance template
    // or per-instance configurations after the VM was created, then these changes are
    // applied when VM is repaired. If NO (default), then updates are applied in
    // accordance with the group's update policy type.
    optional string force_update_on_repair = 2 [json_name = "force_update_on_repair"];

    // Specifies the action that a MIG performs on an unhealthy VM. A VM is marked as
    // unhealthy when the application running on that VM fails a health check. Valid
    // values are: DEFAULT_ACTION, DO_NOTHING, REPAIR. If DEFAULT_ACTION (default), then
    // MIG uses the same action configured for the  "default_action_on_failure" field.
    // If DO_NOTHING, then MIG does not repair unhealthy VM. If REPAIR, then MIG
    // automatically repairs an unhealthy VM by recreating it.
    optional string on_failed_health_check = 3 [json_name = "on_failed_health_check"];

    OnRepair on_repair = 4 [json_name = "on_repair"];

    message OnRepair {
      // Specifies whether the MIG can change a VM's zone during a repair. If "YES", MIG
      // can select a different zone for the VM during a repair. Else if "NO", MIG cannot
      // change a VM's zone during a repair. The default value of allow_changing_zone is
      // "NO".
      optional string allow_changing_zone = 1 [json_name = "allow_changing_zone"];
    }
  }

  message NamedPort {
    // The name of the port.
    optional string name = 1;

    // The port number.
    optional int64 port = 2;
  }

  message ResourcePolicies {
    // The URL of the workload policy that is specified for this managed instance group.
    // It can be a full or partial URL.
    optional string workload_policy = 1 [json_name = "workload_policy"];
  }

  message StandbyPolicy {
    // Specifies the number of seconds that the MIG should wait to suspend or stop a VM
    // after that VM was created. The initial delay gives the initialization script the
    // time to prepare your VM for a quick scale out. The value of initial delay must be
    // between 0 and 3600 seconds. The default value is 0.
    optional int64 initial_delay_sec = 1 [json_name = "initial_delay_sec"];

    // Defines how a MIG resumes or starts VMs from a standby pool when the group scales
    // out. The default mode is "MANUAL".
    optional string mode = 2;
  }

  message StatefulDisk {
    // A value that prescribes what should happen to the stateful disk when the VM
    // instance is deleted. The available options are NEVER and
    // ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the disk when the VM is deleted,
    // but do not delete the disk. ON_PERMANENT_INSTANCE_DELETION will delete the
    // stateful disk when the VM is permanently deleted from the instance group. The
    // default is NEVER.
    optional string delete_rule = 1 [json_name = "delete_rule"];

    // The device name of the disk to be attached.
    optional string device_name = 2 [json_name = "device_name"];
  }

  message StatefulExternalIp {
    // A value that prescribes what should happen to an associated static Address
    // resource when a VM instance is permanently deleted. The available options are
    // NEVER and ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the IP when the VM is
    // deleted, but do not delete the address resource. ON_PERMANENT_INSTANCE_DELETION
    // will delete the stateful address when the VM is permanently deleted from the
    // instance group. The default is NEVER.
    optional string delete_rule = 1 [json_name = "delete_rule"];

    // The network interface name
    optional string interface_name = 2 [json_name = "interface_name"];
  }

  message StatefulInternalIp {
    // A value that prescribes what should happen to an associated static Address
    // resource when a VM instance is permanently deleted. The available options are
    // NEVER and ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the IP when the VM is
    // deleted, but do not delete the address resource. ON_PERMANENT_INSTANCE_DELETION
    // will delete the stateful address when the VM is permanently deleted from the
    // instance group. The default is NEVER.
    optional string delete_rule = 1 [json_name = "delete_rule"];

    // The network interface name
    optional string interface_name = 2 [json_name = "interface_name"];
  }

  message TargetSizePolicy {
    // The mode of target size policy based on which the MIG creates its VMs
    // individually or all at once.
    optional string mode = 1;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message UpdatePolicy {
    // Specifies a fixed number of VM instances. This must be a positive integer.
    // Conflicts with max_surge_percent. Both cannot be 0
    optional int64 max_surge_fixed = 1 [json_name = "max_surge_fixed"];

    // Specifies a percentage of instances between 0 to 100%, inclusive. For example,
    // specify 80 for 80%. Conflicts with max_surge_fixed.
    optional int64 max_surge_percent = 2 [json_name = "max_surge_percent"];

    // Specifies a fixed number of VM instances. This must be a positive integer.
    optional int64 max_unavailable_fixed = 3 [json_name = "max_unavailable_fixed"];

    // Specifies a percentage of instances between 0 to 100%, inclusive. For example,
    // specify 80 for 80%.
    optional int64 max_unavailable_percent = 4 [json_name = "max_unavailable_percent"];

    // Minimal action to be taken on an instance. You can specify either NONE to forbid
    // any actions, REFRESH to update without stopping instances, RESTART to restart
    // existing instances or REPLACE to delete and create new instances from the target
    // template. If you specify a REFRESH, the Updater will attempt to perform that
    // action only. However, if the Updater determines that the minimal action you
    // specify is not enough to perform the update, it might perform a more disruptive
    // action.
    optional string minimal_action = 5 [json_name = "minimal_action"];

    // Most disruptive action that is allowed to be taken on an instance. You can
    // specify either NONE to forbid any actions, REFRESH to allow actions that do not
    // need instance restart, RESTART to allow actions that can be applied without
    // instance replacing or REPLACE to allow all possible actions. If the Updater
    // determines that the minimal update action needed is more disruptive than most
    // disruptive allowed action you specify it will not perform the update at all.
    optional string most_disruptive_allowed_action = 6 [json_name = "most_disruptive_allowed_action"];

    // The instance replacement method for managed instance groups. Valid values are:
    // "RECREATE", "SUBSTITUTE". If SUBSTITUTE (default), the group replaces VM
    // instances with new instances that have randomly generated names. If RECREATE,
    // instance names are preserved.  You must also set max_unavailable_fixed or
    // max_unavailable_percent to be greater than 0.
    optional string replacement_method = 7 [json_name = "replacement_method"];

    // The type of update process. You can specify either PROACTIVE so that the instance
    // group manager proactively executes actions in order to bring instances to their
    // target versions or OPPORTUNISTIC so that no action is proactively executed but
    // the update will be performed as part of other actions (for example, resizes or
    // recreateInstances calls).
    optional string type = 8;
  }

  message Version {
    // The full URL to an instance template from which all new instances of this version
    // will be created.
    optional string instance_template = 1 [json_name = "instance_template"];

    // Version name.
    optional string name = 2;

    TargetSize target_size = 3 [json_name = "target_size"];

    message TargetSize {
      // The number of instances which are managed for this version. Conflicts with
      // percent.
      optional int64 fixed = 1;

      // The number of instances (calculated as percentage) which are managed for this
      // version. Conflicts with fixed. Note that when using percent, rounding will be in
      // favor of explicitly set target_size values; a managed instance group with 2
      // instances and 2 versions, one of which has a target_size.percent of 60 will
      // create 2 instances of that version.
      optional int64 percent = 2;
    }
  }
}

// GoogleComputeInstanceGroupMembership version is 0
message GoogleComputeInstanceGroupMembership {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // An instance being added to the InstanceGroup
  optional string instance = 3;

  // Represents an Instance Group resource name that the instance belongs to.
  optional string instance_group = 4 [json_name = "instance_group"];

  optional string project = 5;

  // A reference to the zone where the instance group resides.
  optional string zone = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeInstanceGroupNamedPort version is 0
message GoogleComputeInstanceGroupNamedPort {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // The name of the instance group.
  optional string group = 2;

  optional string id = 3;

  // The name for this named port. The name must be 1-63 characters
  //
  // long, and comply with RFC1035.
  optional string name = 4;

  // The port number, which can be a value between 1 and 65535.
  optional int64 port = 5;

  optional string project = 6;

  // The zone of the instance group.
  optional string zone = 7;

  repeated Timeouts timeouts = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeInstanceIamBinding version is 0
message GoogleComputeInstanceIamBinding {
  optional string etag = 1;

  optional string id = 2;

  optional string instance_name = 3 [json_name = "instance_name"];

  repeated string members = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeInstanceIamMember version is 0
message GoogleComputeInstanceIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string instance_name = 3 [json_name = "instance_name"];

  optional string member = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeInstanceIamPolicy version is 0
message GoogleComputeInstanceIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string instance_name = 3 [json_name = "instance_name"];

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  optional string zone = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeInstanceSettings version is 0
message GoogleComputeInstanceSettings {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string fingerprint = 2;

  optional string id = 3;

  optional string project = 4;

  // A reference to the zone where the machine resides.
  optional string zone = 5;

  Metadata metadata = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Metadata {
    // A metadata key/value items map. The total size of all keys and values must be
    // less than 512KB
    map<string, string> items = 1;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeInstanceTemplate version is 1
message GoogleComputeInstanceTemplate {
  // Whether to allow sending and receiving of packets with non-matching source or
  // destination IPs. This defaults to false.
  optional bool can_ip_forward = 1 [json_name = "can_ip_forward"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // A brief description of this resource.
  optional string description = 3;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 4 [json_name = "effective_labels"];

  optional string id = 5;

  // A description of the instance.
  optional string instance_description = 6 [json_name = "instance_description"];

  // Action to be taken when a customer's encryption key is revoked. Supports "STOP"
  // and "NONE", with "NONE" being the default.
  optional string key_revocation_action_type = 7 [json_name = "key_revocation_action_type"];

  // A set of key/value label pairs to assign to instances created from this template.
  //
  //
  // 				**Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // 				Please refer to the field 'effective_labels' for all of the labels present on
  // the resource.
  map<string, string> labels = 8;

  // The machine type to create. To create a machine with a custom type (such as
  // extended memory), format the value like custom-VCPUS-MEM_IN_MB like
  // custom-6-20480 for 6 vCPU and 20GB of RAM.
  optional string machine_type = 9 [json_name = "machine_type"];

  // Metadata key/value pairs to make available from within instances created from
  // this template.
  map<string, string> metadata = 10;

  // The unique fingerprint of the metadata.
  optional string metadata_fingerprint = 11 [json_name = "metadata_fingerprint"];

  // An alternative to using the startup-script metadata key, mostly to match the
  // compute_instance resource. This replaces the startup-script metadata key on the
  // created instance and thus the two mechanisms are not allowed to be used
  // simultaneously.
  optional string metadata_startup_script = 12 [json_name = "metadata_startup_script"];

  // Specifies a minimum CPU platform. Applicable values are the friendly names of CPU
  // platforms, such as Intel Haswell or Intel Skylake.
  optional string min_cpu_platform = 13 [json_name = "min_cpu_platform"];

  // The name of the instance template. If you leave this blank, Terraform will
  // auto-generate a unique name.
  optional string name = 14;

  // Creates a unique name beginning with the specified prefix. Conflicts with name.
  // Max length is 54 characters. Prefixes with lengths longer than 37 characters will
  // use a shortened UUID that will be more prone to collisions.
  optional string name_prefix = 15 [json_name = "name_prefix"];

  // The ID of the template in numeric format.
  optional string numeric_id = 16 [json_name = "numeric_id"];

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 17;

  // An instance template is a global resource that is not bound to a zone or a
  // region. However, you can still specify some regional resources in an instance
  // template, which restricts the template to the region where that resource resides.
  // For example, a custom subnetwork resource is tied to a specific region. Defaults
  // to the region of the Provider if no value is given.
  optional string region = 18;

  // A map of resource manager tags.
  //
  // 				Resource manager tag keys and values have the same definition as resource
  // manager tags.
  //
  // 				Keys must be in the format tagKeys/{tag_key_id}, and values are in the format
  // tagValues/456.
  //
  // 				The field is ignored (both PUT & PATCH) when empty.
  map<string, string> resource_manager_tags = 19 [json_name = "resource_manager_tags"];

  // A list of self_links of resource policies to attach to the instance. Currently a
  // max of 1 resource policy is supported.
  repeated string resource_policies = 20 [json_name = "resource_policies"];

  // The URI of the created resource.
  optional string self_link = 21 [json_name = "self_link"];

  // A special URI of the created resource that uniquely identifies this instance
  // template.
  optional string self_link_unique = 22 [json_name = "self_link_unique"];

  // Tags to attach to the instance.
  repeated string tags = 23;

  // The unique fingerprint of the tags.
  optional string tags_fingerprint = 24 [json_name = "tags_fingerprint"];

  // The combination of labels configured directly on the resource and default labels
  // configured on the provider.
  map<string, string> terraform_labels = 25 [json_name = "terraform_labels"];

  AdvancedMachineFeatures advanced_machine_features = 26 [json_name = "advanced_machine_features"];

  ConfidentialInstanceConfig confidential_instance_config = 27 [json_name = "confidential_instance_config"];

  repeated Disk disk = 28;

  repeated GuestAccelerator guest_accelerator = 29 [json_name = "guest_accelerator"];

  repeated NetworkInterface network_interface = 30 [json_name = "network_interface"];

  NetworkPerformanceConfig network_performance_config = 31 [json_name = "network_performance_config"];

  ReservationAffinity reservation_affinity = 32 [json_name = "reservation_affinity"];

  Scheduling scheduling = 33;

  ServiceAccount service_account = 34 [json_name = "service_account"];

  ShieldedInstanceConfig shielded_instance_config = 35 [json_name = "shielded_instance_config"];

  repeated Timeouts timeouts = 36;

  WorkloadIdentityConfig workload_identity_config = 37 [json_name = "workload_identity_config"];

  google.protobuf.Value for_each = 38 [json_name = "for_each"];

  repeated string depends_on = 39 [json_name = "depends_on"];

  optional int32 count = 40;

  optional string provider = 41;

  terraform.v1.Lifecycle lifecycle = 42;

  message AdvancedMachineFeatures {
    // Whether to enable nested virtualization or not.
    optional bool enable_nested_virtualization = 1 [json_name = "enable_nested_virtualization"];

    // Whether to enable UEFI networking or not.
    optional bool enable_uefi_networking = 2 [json_name = "enable_uefi_networking"];

    // The PMU is a hardware component within the CPU core that monitors how the
    // processor runs code. Valid values for the level of PMU are "STANDARD",
    // "ENHANCED", and "ARCHITECTURAL".
    optional string performance_monitoring_unit = 3 [json_name = "performance_monitoring_unit"];

    // The number of threads per physical core. To disable simultaneous multithreading
    // (SMT) set this to 1. If unset, the maximum number of threads supported per core
    // by the underlying processor is assumed.
    optional int64 threads_per_core = 4 [json_name = "threads_per_core"];

    // Turbo frequency mode to use for the instance. Currently supported modes is
    // "ALL_CORE_MAX".
    optional string turbo_mode = 5 [json_name = "turbo_mode"];

    // The number of physical cores to expose to an instance. Multiply by the number of
    // threads per core to compute the total number of virtual CPUs to expose to the
    // instance. If unset, the number of cores is inferred from the instance\'s nominal
    // CPU count and the underlying platform\'s SMT width.
    optional int64 visible_core_count = 6 [json_name = "visible_core_count"];
  }

  message ConfidentialInstanceConfig {
    // 								The confidential computing technology the instance uses.
    //
    // 								SEV is an AMD feature. TDX is an Intel feature. One of the following
    //
    // 								values is required: SEV, SEV_SNP, TDX. If SEV_SNP, min_cpu_platform =
    //
    // 								"AMD Milan" is currently required.
    optional string confidential_instance_type = 1 [json_name = "confidential_instance_type"];

    // Defines whether the instance should have confidential compute enabled. Field will
    // be deprecated in a future release.
    optional bool enable_confidential_compute = 2 [json_name = "enable_confidential_compute"];
  }

  message Disk {
    // The architecture of the image. Allowed values are ARM64 or X86_64.
    optional string architecture = 1;

    // Whether or not the disk should be auto-deleted. This defaults to true.
    optional bool auto_delete = 2 [json_name = "auto_delete"];

    // Indicates that this is a boot disk.
    optional bool boot = 3;

    // A unique device name that is reflected into the /dev/ tree of a Linux operating
    // system running within the instance. If not specified, the server chooses a
    // default device name to apply to this disk.
    optional string device_name = 4 [json_name = "device_name"];

    // Name of the disk. When not provided, this defaults to the name of the instance.
    optional string disk_name = 5 [json_name = "disk_name"];

    // The size of the image in gigabytes. If not specified, it will inherit the size of
    // its base image. For SCRATCH disks, the size must be one of 375, 3000, 3500, 7000
    // or 14000 GB, with a default of 375 GB.
    optional int64 disk_size_gb = 6 [json_name = "disk_size_gb"];

    // The Google Compute Engine disk type. Such as "pd-ssd", "local-ssd", "pd-balanced"
    // or "pd-standard".
    optional string disk_type = 7 [json_name = "disk_type"];

    // A list of features to enable on the guest operating system. Applicable only for
    // bootable images.
    repeated string guest_os_features = 8 [json_name = "guest_os_features"];

    // Specifies the disk interface to use for attaching this disk.
    optional string interface = 9;

    // A set of key/value label pairs to assign to disks,
    map<string, string> labels = 10;

    // The mode in which to attach this disk, either READ_WRITE or READ_ONLY. If you are
    // attaching or creating a boot disk, this must read-write mode.
    optional string mode = 11;

    // Indicates how many IOPS to provision for the disk. This sets the number of I/O
    // operations per second that the disk can handle. For more details, see the
    // [Extreme persistent disk
    // cumentation](https://cloud.google.com/compute/docs/disks/extreme-persistent-disk)
    // or the [Hyperdisk
    // documentation](https://cloud.google.com/compute/docs/disks/hyperdisks) depending
    // on the selected disk_type.
    optional int64 provisioned_iops = 12 [json_name = "provisioned_iops"];

    // Indicates how much throughput to provision for the disk, in MB/s. This sets the
    // amount of data that can be read or written from the disk per second. Values must
    // greater than or equal to 1. For more details, see the [Hyperdisk
    // documentation](https://cloud.google.com/compute/docs/disks/hyperdisks).
    optional int64 provisioned_throughput = 13 [json_name = "provisioned_throughput"];

    // A map of resource manager tags. Resource manager tag keys and values have the
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is
    // ignored (both PUT & PATCH) when empty.
    map<string, string> resource_manager_tags = 14 [json_name = "resource_manager_tags"];

    // A list (short name or id) of resource policies to attach to this disk. Currently
    // a max of 1 resource policy is supported.
    repeated string resource_policies = 15 [json_name = "resource_policies"];

    // The name (not self_link) of the disk (such as those managed by
    // google_compute_disk) to attach. ~> Note: Either source or source_image is
    // required when creating a new instance except for when creating a local SSD.
    optional string source = 16;

    // The image from which to initialize this disk. This can be one of: the image's
    // self_link, projects/{project}/global/images/{image},
    // projects/{project}/global/images/family/{family}, global/images/{image},
    // global/images/family/{family}, family/{family}, {project}/{family},
    // {project}/{image}, {family}, or {image}. ~> Note: Either source or source_image
    // is required when creating a new instance except for when creating a local SSD.
    optional string source_image = 17 [json_name = "source_image"];

    // The source snapshot to create this disk. When creating
    //
    // a new instance, one of initializeParams.sourceSnapshot,
    //
    // initializeParams.sourceImage, or disks.source is
    //
    // required except for local SSD.
    optional string source_snapshot = 18 [json_name = "source_snapshot"];

    // The self_link or ID of the Storage Pool to create this disk in.
    optional string storage_pool = 19 [json_name = "storage_pool"];

    // The type of Google Compute Engine disk, can be either "SCRATCH" or "PERSISTENT".
    optional string type = 20;

    DiskEncryptionKey disk_encryption_key = 21 [json_name = "disk_encryption_key"];

    SourceImageEncryptionKey source_image_encryption_key = 22 [json_name = "source_image_encryption_key"];

    SourceSnapshotEncryptionKey source_snapshot_encryption_key = 23 [json_name = "source_snapshot_encryption_key"];

    message DiskEncryptionKey {
      // The self link of the encryption key that is stored in Google Cloud KMS.
      optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption request for the given KMS key.
      // If absent, the Compute Engine default service account is used.
      optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];
    }

    message SourceImageEncryptionKey {
      // The self link of the encryption key that is stored in
      //
      // Google Cloud KMS. Only one of kms_key_self_link, rsa_encrypted_key and raw_key
      // may be set.
      optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption
      //
      // request for the given KMS key. If absent, the Compute
      //
      // Engine default service account is used.
      optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

      // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
      // to either encrypt or decrypt this resource. Only one of kms_key_self_link,
      // rsa_encrypted_key and raw_key may be set.
      optional string raw_key = 3 [json_name = "raw_key"];

      // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
      // encryption key to either encrypt or decrypt this resource. Only one of
      // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
      optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
    }

    message SourceSnapshotEncryptionKey {
      // The self link of the encryption key that is stored in
      //
      // Google Cloud KMS. Only one of kms_key_self_link, rsa_encrypted_key and raw_key
      // may be set.
      optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption
      //
      // request for the given KMS key. If absent, the Compute
      //
      // Engine default service account is used.
      optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

      // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
      // to either encrypt or decrypt this resource. Only one of kms_key_self_link,
      // rsa_encrypted_key and raw_key may be set.
      optional string raw_key = 3 [json_name = "raw_key"];

      // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
      // encryption key to either encrypt or decrypt this resource. Only one of
      // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
      optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
    }
  }

  message GuestAccelerator {
    // The number of the guest accelerator cards exposed to this instance.
    optional int64 count = 1;

    // The accelerator type resource to expose to this instance. E.g. nvidia-tesla-k80.
    optional string type = 2;
  }

  message NetworkInterface {
    // Indicates whether igmp query is enabled on the network interface or not. If
    // enabled, also indicates the version of IGMP supported.
    optional string igmp_query = 1 [json_name = "igmp_query"];

    // The prefix length of the primary internal IPv6 range.
    optional int64 internal_ipv6_prefix_length = 2 [json_name = "internal_ipv6_prefix_length"];

    // One of EXTERNAL, INTERNAL to indicate whether the IP can be accessed from the
    // Internet. This field is always inherited from its subnetwork.
    optional string ipv6_access_type = 3 [json_name = "ipv6_access_type"];

    // An IPv6 internal network address for this network interface. If not specified,
    // Google Cloud will automatically assign an internal IPv6 address from the
    // instance's subnetwork.
    optional string ipv6_address = 4 [json_name = "ipv6_address"];

    // The name of the network_interface.
    optional string name = 5;

    // The name or self_link of the network to attach this interface to. Use network
    // attribute for Legacy or Auto subnetted networks and subnetwork for custom
    // subnetted networks.
    optional string network = 6;

    // The URL of the network attachment that this interface should connect to in the
    // following format:
    // rojectNumber}/regions/{region_name}/networkAttachments/{network_attachment_name}.
    optional string network_attachment = 7 [json_name = "network_attachment"];

    // The private IP address to assign to the instance. If empty, the address will be
    // automatically assigned.
    optional string network_ip = 8 [json_name = "network_ip"];

    // The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET,
    // MRDMA, IRDMA and IDPF
    optional string nic_type = 9 [json_name = "nic_type"];

    // Name of the parent network interface of a dynamic network interface.
    optional string parent_nic_name = 10 [json_name = "parent_nic_name"];

    // The networking queue count that's specified by users for the network interface.
    // Both Rx and Tx queues will be set to this number. It will be empty if not
    // specified.
    optional int64 queue_count = 11 [json_name = "queue_count"];

    // The stack type for this network interface to identify whether the IPv6 feature is
    // enabled or not. If not specified, IPV4_ONLY will be used.
    optional string stack_type = 12 [json_name = "stack_type"];

    // The name of the subnetwork to attach this interface to. The subnetwork must exist
    // in the same region this instance will be created in. Either network or subnetwork
    // must be provided.
    optional string subnetwork = 13;

    // The ID of the project in which the subnetwork belongs. If it is not provided, the
    // provider project is used.
    optional string subnetwork_project = 14 [json_name = "subnetwork_project"];

    // VLAN tag of a dynamic network interface, must be an integer in the range from 2
    // to 255 inclusively.
    optional int64 vlan = 15;

    repeated AccessConfig access_config = 16 [json_name = "access_config"];

    repeated AliasIpRange alias_ip_range = 17 [json_name = "alias_ip_range"];

    repeated Ipv6AccessConfig ipv6_access_config = 18 [json_name = "ipv6_access_config"];

    message AccessConfig {
      // The IP address that will be 1:1 mapped to the instance's network ip. If not
      // given, one will be generated.
      optional string nat_ip = 1 [json_name = "nat_ip"];

      // The networking tier used for configuring this instance template. This field can
      // take the following values: PREMIUM, STANDARD, FIXED_STANDARD. If this field is
      // not specified, it is assumed to be PREMIUM.
      optional string network_tier = 2 [json_name = "network_tier"];

      // The DNS domain name for the public PTR record.The DNS domain name for the public
      // PTR record.
      optional string public_ptr_domain_name = 3 [json_name = "public_ptr_domain_name"];
    }

    message AliasIpRange {
      // The IP CIDR range represented by this alias IP range. This IP CIDR range must
      // belong to the specified subnetwork and cannot contain IP addresses reserved by
      // system or used by other network interfaces. At the time of writing only a netmask
      // (e.g. /24) may be supplied, with a CIDR format resulting in an API error.
      optional string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

      // The subnetwork secondary range name specifying the secondary range from which to
      // allocate the IP CIDR range for this alias IP range. If left unspecified, the
      // primary range of the subnetwork will be used.
      optional string subnetwork_range_name = 2 [json_name = "subnetwork_range_name"];
    }

    message Ipv6AccessConfig {
      // The first IPv6 address of the external IPv6 range associated with this instance,
      // prefix length is stored in externalIpv6PrefixLength in ipv6AccessConfig. The
      // field is output only, an IPv6 address from a subnetwork associated with the
      // instance will be allocated dynamically.
      optional string external_ipv6 = 1 [json_name = "external_ipv6"];

      // The prefix length of the external IPv6 range.
      optional string external_ipv6_prefix_length = 2 [json_name = "external_ipv6_prefix_length"];

      // The name of this access configuration.
      optional string name = 3;

      // The service-level to be provided for IPv6 traffic when the subnet has an external
      // subnet. Only PREMIUM tier is valid for IPv6
      optional string network_tier = 4 [json_name = "network_tier"];

      // The domain name to be used when creating DNSv6 records for the external IPv6
      // ranges.
      optional string public_ptr_domain_name = 5 [json_name = "public_ptr_domain_name"];
    }
  }

  message NetworkPerformanceConfig {
    // The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT
    optional string total_egress_bandwidth_tier = 1 [json_name = "total_egress_bandwidth_tier"];
  }

  message ReservationAffinity {
    // The type of reservation from which this instance can consume resources.
    optional string type = 1;

    SpecificReservation specific_reservation = 2 [json_name = "specific_reservation"];

    message SpecificReservation {
      // Corresponds to the label key of a reservation resource. To target a
      // SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as
      // the key and specify the name of your reservation as the only value.
      optional string key = 1;

      // Corresponds to the label values of a reservation resource.
      repeated string values = 2;
    }
  }

  message Scheduling {
    // Specifies whether the instance should be automatically restarted if it is
    // terminated by Compute Engine (not terminated by a user). This defaults to true.
    optional bool automatic_restart = 1 [json_name = "automatic_restart"];

    // Specifies the availability domain, which this instance should be scheduled on.
    optional int64 availability_domain = 2 [json_name = "availability_domain"];

    // Specify the time in seconds for host error detection, the value must be within
    // the range of [90, 330] with the increment of 30, if unset, the default behavior
    // of host error recovery will be used.
    optional int64 host_error_timeout_seconds = 3 [json_name = "host_error_timeout_seconds"];

    // Specifies the action GCE should take when SPOT VM is preempted.
    optional string instance_termination_action = 4 [json_name = "instance_termination_action"];

    // Minimum number of cpus for the instance.
    optional int64 min_node_cpus = 5 [json_name = "min_node_cpus"];

    // Defines the maintenance behavior for this instance.
    optional string on_host_maintenance = 6 [json_name = "on_host_maintenance"];

    // Allows instance to be preempted. This defaults to false.
    optional bool preemptible = 7;

    // Describes the desired provisioning model for the instance. Possible values are
    // STANDARD, SPOT, FLEX_START, and RESERVATION_BOUND. For STANDARD, resources are
    // provisioned immediately. For SPOT, resources are offered at a discount compared
    // to standard pricing but may be preempted. For FLEX_START, resources are offered
    // at a discount with flexible start times. For RESERVATION_BOUND, the instance is
    // bound to a specific reservation and will only consume capacity from that
    // reservation.
    optional string provisioning_model = 8 [json_name = "provisioning_model"];

    // Specifies the timestamp, when the instance will be terminated,
    //
    // in RFC3339 text format. If specified, the instance termination action
    //
    // will be performed at the termination time.
    optional string termination_time = 9 [json_name = "termination_time"];

    repeated LocalSsdRecoveryTimeout local_ssd_recovery_timeout = 10 [json_name = "local_ssd_recovery_timeout"];

    MaxRunDuration max_run_duration = 11 [json_name = "max_run_duration"];

    repeated NodeAffinities node_affinities = 12 [json_name = "node_affinities"];

    OnInstanceStopAction on_instance_stop_action = 13 [json_name = "on_instance_stop_action"];

    message LocalSsdRecoveryTimeout {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message MaxRunDuration {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message NodeAffinities {
      optional string key = 1;

      optional string operator = 2;

      repeated string values = 3;
    }

    message OnInstanceStopAction {
      // If true, the contents of any attached Local SSD disks will be discarded.
      optional bool discard_local_ssd = 1 [json_name = "discard_local_ssd"];
    }
  }

  message ServiceAccount {
    // The service account e-mail address. If not given, the default Google Compute
    // Engine service account is used.
    optional string email = 1;

    // A list of service scopes. Both OAuth2 URLs and gcloud short names are supported.
    // To allow full access to all Cloud APIs, use the cloud-platform scope.
    repeated string scopes = 2;
  }

  message ShieldedInstanceConfig {
    // Compare the most recent boot measurements to the integrity policy baseline and
    // return a pair of pass/fail results depending on whether they match or not.
    // Defaults to true.
    optional bool enable_integrity_monitoring = 1 [json_name = "enable_integrity_monitoring"];

    // Verify the digital signature of all boot components, and halt the boot process if
    // signature verification fails. Defaults to false.
    optional bool enable_secure_boot = 2 [json_name = "enable_secure_boot"];

    // Use a virtualized trusted platform module, which is a specialized computer chip
    // you can use to encrypt objects like keys and certificates. Defaults to true.
    optional bool enable_vtpm = 3 [json_name = "enable_vtpm"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }

  message WorkloadIdentityConfig {
    // Identity SPIFFE id.
    optional string identity = 1;

    // Specifies whether identity certificates are enabled.
    optional bool identity_certificate_enabled = 2 [json_name = "identity_certificate_enabled"];
  }
}

// GoogleComputeInstanceTemplateIamBinding version is 0
message GoogleComputeInstanceTemplateIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeInstanceTemplateIamMember version is 0
message GoogleComputeInstanceTemplateIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeInstanceTemplateIamPolicy version is 0
message GoogleComputeInstanceTemplateIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// GoogleComputeInstantSnapshot version is 0
message GoogleComputeInstantSnapshot {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Size of the snapshot, specified in GB.
  optional int64 disk_size_gb = 4 [json_name = "disk_size_gb"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 5 [json_name = "effective_labels"];

  optional string id = 6;

  // The fingerprint used for optimistic locking of this resource. Used
  //
  // internally during updates.
  optional string label_fingerprint = 7 [json_name = "label_fingerprint"];

  // Labels to apply to this InstantSnapshot.
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 8;

  // Name of the resource; provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 9;

  optional string project = 10;

  optional string self_link = 11 [json_name = "self_link"];

  // A reference to the disk used to create this instant snapshot.
  optional string source_disk = 12 [json_name = "source_disk"];

  // The ID value of the disk used to create this InstantSnapshot.
  optional string source_disk_id = 13 [json_name = "source_disk_id"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 14 [json_name = "terraform_labels"];

  // A reference to the zone where the disk is located.
  optional string zone = 15;

  Params params = 16;

  repeated Timeouts timeouts = 17;

  google.protobuf.Value for_each = 18 [json_name = "for_each"];

  repeated string depends_on = 19 [json_name = "depends_on"];

  optional int32 count = 20;

  optional string provider = 21;

  terraform.v1.Lifecycle lifecycle = 22;

  message Params {
    // Resource manager tags to be bound to the instant snapshot. Tag keys and values
    // have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeInstantSnapshotIamBinding version is 0
message GoogleComputeInstantSnapshotIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeInstantSnapshotIamMember version is 0
message GoogleComputeInstantSnapshotIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeInstantSnapshotIamPolicy version is 0
message GoogleComputeInstantSnapshotIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  optional string zone = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeInterconnect version is 0
message GoogleComputeInterconnect {
  // Administrative status of the interconnect. When this is set to true, the
  // Interconnect is
  //
  // functional and can carry traffic. When set to false, no packets can be carried
  // over the
  //
  // interconnect and no BGP routes are exchanged over it. By default, the status is
  // set to true.
  optional bool admin_enabled = 1 [json_name = "admin_enabled"];

  // [Output Only] List of features that are available on this Interconnect connection
  // based on the provisioned hardware and configuration.
  //
  // Possible values include:
  //
  // - 'IF_MACSEC': Indicates the Interconnect connection is provisioned on MACsec
  // capable hardware ports. If this feature is not present, the connection does not
  // support MACsec, and any attempt to enable it will fail.
  //
  // - 'IF_L2_FORWARDING': Indicates the Interconnect connection can be used for Layer
  // 2 (L2) traffic forwarding. If not present, the connection cannot be used with L2
  // forwarding attachments.
  //
  // - 'IF_CROSS_SITE_NETWORK': Indicates the Interconnect connection is provisioned
  // for Cross-Site Networking.
  //
  // Note: 'MACSEC' is a legacy value and has the same meaning as 'IF_MACSEC'.
  repeated string available_features = 2 [json_name = "available_features"];

  // A list of CircuitInfo objects, that describe the individual circuits in this LAG.
  repeated CircuitInfos circuit_infos = 3 [json_name = "circuit_infos"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // Customer name, to put in the Letter of Authorization as the party authorized to
  // request a
  //
  // crossconnect. This field is required for Dedicated and Partner Interconnect,
  // should not be specified
  //
  // for cross-cloud interconnect.
  optional string customer_name = 5 [json_name = "customer_name"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 6 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 7;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 8 [json_name = "effective_labels"];

  // URL of the InterconnectLocation object that represents where this connection is
  // to be provisioned.
  //
  // Specifies the location inside Google's Networks.
  optional string effective_location = 9 [json_name = "effective_location"];

  // A list of outages expected for this Interconnect.
  repeated ExpectedOutages expected_outages = 10 [json_name = "expected_outages"];

  // IP address configured on the Google side of the Interconnect link.
  //
  // This can be used only for ping tests.
  optional string google_ip_address = 11 [json_name = "google_ip_address"];

  // Google reference ID to be used when raising support tickets with Google or
  // otherwise to debug
  //
  // backend connectivity issues.
  optional string google_reference_id = 12 [json_name = "google_reference_id"];

  optional string id = 13;

  // A list of the URLs of all InterconnectAttachments configured to use this
  // Interconnect.
  repeated string interconnect_attachments = 14 [json_name = "interconnect_attachments"];

  // URLs of InterconnectGroups that include this Interconnect.
  //
  // Order is arbitrary and items are unique.
  repeated string interconnect_groups = 15 [json_name = "interconnect_groups"];

  // Type of interconnect. Note that a value IT_PRIVATE has been deprecated in favor
  // of DEDICATED.
  //
  // Can take one of the following values:
  //
  // - PARTNER: A partner-managed interconnection shared between customers though a
  // partner.
  //
  // - DEDICATED: A dedicated physical interconnection with the customer. Possible
  // values: ["DEDICATED", "PARTNER", "IT_PRIVATE"]
  optional string interconnect_type = 16 [json_name = "interconnect_type"];

  // A fingerprint for the labels being applied to this Interconnect, which is
  // essentially a hash
  //
  // of the labels set used for optimistic locking. The fingerprint is initially
  // generated by
  //
  // Compute Engine and changes after every request to modify or update labels.
  //
  // You must always provide an up-to-date fingerprint hash in order to update or
  // change labels,
  //
  // otherwise the request will fail with error 412 conditionNotMet.
  optional string label_fingerprint = 17 [json_name = "label_fingerprint"];

  // Labels for this resource. These can only be added or modified by the setLabels
  //
  // method. Each label key/value pair must comply with RFC1035. Label values may be
  // empty.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 18;

  // Type of link requested. Note that this field indicates the speed of each of the
  // links in the
  //
  // bundle, not the speed of the entire bundle. Can take one of the following values:
  //
  // - LINK_TYPE_ETHERNET_10G_LR: A 10G Ethernet with LR optics.
  //
  // - LINK_TYPE_ETHERNET_100G_LR: A 100G Ethernet with LR optics.
  //
  // - LINK_TYPE_ETHERNET_400G_LR4: A 400G Ethernet with LR4 optics Possible values:
  // ["LINK_TYPE_ETHERNET_10G_LR", "LINK_TYPE_ETHERNET_100G_LR",
  // "LINK_TYPE_ETHERNET_400G_LR4"]
  optional string link_type = 19 [json_name = "link_type"];

  // URL of the InterconnectLocation object that represents where this connection is
  // requested to be provisioned.
  //
  // Specifies the location inside Google's Networks.
  optional string location = 20;

  // Enable or disable MACsec on this Interconnect connection.
  //
  // MACsec enablement fails if the MACsec object is not specified.
  optional bool macsec_enabled = 21 [json_name = "macsec_enabled"];

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be
  //
  // 1-63 characters long, and comply with RFC1035. Specifically, the name must be
  // 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first
  //
  // character must be a lowercase letter, and all following characters must be a
  // dash,
  //
  // lowercase letter, or digit, except the last character, which cannot be a dash.
  optional string name = 22;

  // Email address to contact the customer NOC for operations and maintenance
  // notifications
  //
  // regarding this Interconnect. If specified, this will be used for notifications in
  // addition to
  //
  // all other forms described, such as Cloud Monitoring logs alerting and Cloud
  // Notifications.
  //
  // This field is required for users who sign up for Cloud Interconnect using
  // workforce identity
  //
  // federation.
  optional string noc_contact_email = 23 [json_name = "noc_contact_email"];

  // The current status of this Interconnect's functionality, which can take one of
  // the following:
  //
  // - OS_ACTIVE: A valid Interconnect, which is turned up and is ready to use.
  // Attachments may
  //
  // be provisioned on this Interconnect.
  //
  // - OS_UNPROVISIONED: An Interconnect that has not completed turnup. No attachments
  // may be
  //
  // provisioned on this Interconnect.
  //
  // - OS_UNDER_MAINTENANCE: An Interconnect that is undergoing internal maintenance.
  // No
  //
  // attachments may be provisioned or updated on this Interconnect.
  optional string operational_status = 24 [json_name = "operational_status"];

  // IP address configured on the customer side of the Interconnect link.
  //
  // The customer should configure this IP address during turnup when prompted by
  // Google NOC.
  //
  // This can be used only for ping tests.
  optional string peer_ip_address = 25 [json_name = "peer_ip_address"];

  optional string project = 26;

  // Number of links actually provisioned in this interconnect.
  optional int64 provisioned_link_count = 27 [json_name = "provisioned_link_count"];

  // Indicates that this is a Cross-Cloud Interconnect. This field specifies the
  // location outside
  //
  // of Google's network that the interconnect is connected to.
  optional string remote_location = 28 [json_name = "remote_location"];

  // List of features to request for this Interconnect connection. This field is only
  // applicable during Interconnect creation and cannot be modified later.
  //
  // Possible values include:
  //
  // - 'IF_MACSEC': Provisions the connection on hardware ports that support MACsec
  // (Media Access Control Security). If not specified, the system may allocate
  // non-MACsec capable ports if available.
  //
  // - 'IF_L2_FORWARDING': Provisions the connection for Layer 2 (L2) traffic
  // forwarding. If not specified, the connection defaults to Layer 3 (L3) traffic
  // forwarding.
  //
  // - 'IF_CROSS_SITE_NETWORK': Provisions the connection exclusively for Cross-Site
  // Networking.
  //
  // Note: 'MACSEC' is a legacy value for compatibility reasons and has the same
  // effect as 'IF_MACSEC'. 'IF_MACSEC' is preferred. Possible values: ["MACSEC",
  // "CROSS_SITE_NETWORK", "IF_MACSEC", "IF_L2_FORWARDING"]
  repeated string requested_features = 29 [json_name = "requested_features"];

  // Target number of physical links in the link bundle, as requested by the customer.
  optional int64 requested_link_count = 30 [json_name = "requested_link_count"];

  // Reserved for future use.
  optional bool satisfies_pzs = 31 [json_name = "satisfies_pzs"];

  // The current state of Interconnect functionality, which can take one of the
  // following values:
  //
  // - ACTIVE: The Interconnect is valid, turned up and ready to use.
  //
  // Attachments may be provisioned on this Interconnect.
  //
  // - UNPROVISIONED: The Interconnect has not completed turnup. No attachments may b
  //
  // provisioned on this Interconnect.
  //
  // - UNDER_MAINTENANCE: The Interconnect is undergoing internal maintenance. No
  // attachments may
  //
  // be provisioned or updated on this Interconnect.
  optional string state = 32;

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 33 [json_name = "terraform_labels"];

  // A list of the URLs of all CrossSiteNetwork WireGroups configured to use this
  // Interconnect. The Interconnect cannot be deleted if this list is non-empty.
  repeated string wire_groups = 34 [json_name = "wire_groups"];

  Macsec macsec = 35;

  Params params = 36;

  repeated Timeouts timeouts = 37;

  google.protobuf.Value for_each = 38 [json_name = "for_each"];

  repeated string depends_on = 39 [json_name = "depends_on"];

  optional int32 count = 40;

  optional string provider = 41;

  terraform.v1.Lifecycle lifecycle = 42;

  message CircuitInfos {
    // customer_demarc_id: string
    optional string customer_demarc_id = 1 [json_name = "customer_demarc_id"];

    // google_circuit_id: string
    optional string google_circuit_id = 2 [json_name = "google_circuit_id"];

    // google_demarc_id: string
    optional string google_demarc_id = 3 [json_name = "google_demarc_id"];
  }

  message ExpectedOutages {
    // affected_circuits: list of string
    repeated string affected_circuits = 1 [json_name = "affected_circuits"];

    // description: string
    optional string description = 2;

    // end_time: string
    optional string end_time = 3 [json_name = "end_time"];

    // issue_type: string
    optional string issue_type = 4 [json_name = "issue_type"];

    // name: string
    optional string name = 5;

    // source: string
    optional string source = 6;

    // start_time: string
    optional string start_time = 7 [json_name = "start_time"];

    // state: string
    optional string state = 8;
  }

  message Macsec {
    // If set to true, the Interconnect connection is configured with a should-secure
    //
    // MACsec security policy, that allows the Google router to fallback to cleartext
    //
    // traffic if the MKA session cannot be established. By default, the Interconnect
    //
    // connection is configured with a must-secure security policy that drops all
    // traffic
    //
    // if the MKA session cannot be established with your router.
    optional bool fail_open = 1 [json_name = "fail_open"];

    repeated PreSharedKeys pre_shared_keys = 2 [json_name = "pre_shared_keys"];

    message PreSharedKeys {
      // If set to true, the Interconnect connection is configured with a should-secure
      //
      // MACsec security policy, that allows the Google router to fallback to cleartext
      //
      // traffic if the MKA session cannot be established. By default, the Interconnect
      //
      // connection is configured with a must-secure security policy that drops all
      // traffic
      //
      // if the MKA session cannot be established with your router.
      optional bool fail_open = 1 [json_name = "fail_open"];

      // A name for this pre-shared key. The name must be 1-63 characters long, and
      //
      // comply with RFC1035. Specifically, the name must be 1-63 characters long and
      // match
      //
      // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
      // character
      //
      // must be a lowercase letter, and all following characters must be a dash,
      // lowercase
      //
      // letter, or digit, except the last character, which cannot be a dash.
      optional string name = 2;

      // A RFC3339 timestamp on or after which the key is valid. startTime can be in the
      //
      // future. If the keychain has a single key, startTime can be omitted. If the
      // keychain
      //
      // has multiple keys, startTime is mandatory for each key. The start times of keys
      // must
      //
      // be in increasing order. The start times of two consecutive keys must be at least
      // hours apart.
      optional string start_time = 3 [json_name = "start_time"];
    }
  }

  message Params {
    // Resource manager tags to be bound to the interconnect. Tag keys and values have
    // the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeInterconnectAttachment version is 0
message GoogleComputeInterconnectAttachment {
  // Whether the VLAN attachment is enabled or disabled.  When using
  //
  // PARTNER type this will Pre-Activate the interconnect attachment
  optional bool admin_enabled = 1 [json_name = "admin_enabled"];

  // URL of the AttachmentGroup that includes this Attachment.
  optional string attachment_group = 2 [json_name = "attachment_group"];

  // Provisioned bandwidth capacity for the interconnect attachment.
  //
  // For attachments of type DEDICATED, the user can set the bandwidth.
  //
  // For attachments of type PARTNER, the Google Partner that is operating the
  // interconnect must set the bandwidth.
  //
  // Output only for PARTNER type, mutable for PARTNER_PROVIDER and DEDICATED,
  //
  // Defaults to BPS_10G Possible values: ["BPS_50M", "BPS_100M", "BPS_200M",
  // "BPS_300M", "BPS_400M", "BPS_500M", "BPS_1G", "BPS_2G", "BPS_5G", "BPS_10G",
  // "BPS_20G", "BPS_50G", "BPS_100G", "BPS_400G"]
  optional string bandwidth = 3;

  // Single IPv4 address + prefix length to be configured on the cloud router
  // interface for this
  //
  // interconnect attachment. Example: 203.0.113.1/29
  optional string candidate_cloud_router_ip_address = 4 [json_name = "candidate_cloud_router_ip_address"];

  // Single IPv6 address + prefix length to be configured on the cloud router
  // interface for this
  //
  // interconnect attachment. Example: 2001:db8::1/125
  optional string candidate_cloud_router_ipv6_address = 5 [json_name = "candidate_cloud_router_ipv6_address"];

  // Single IPv4 address + prefix length to be configured on the customer router
  // interface for this
  //
  // interconnect attachment. Example: 203.0.113.2/29
  optional string candidate_customer_router_ip_address = 6 [json_name = "candidate_customer_router_ip_address"];

  // Single IPv6 address + prefix length to be configured on the customer router
  // interface for this
  //
  // interconnect attachment. Example: 2001:db8::2/125
  optional string candidate_customer_router_ipv6_address = 7 [
    json_name = "candidate_customer_router_ipv6_address"
  ];

  // Up to 16 candidate prefixes that can be used to restrict the allocation
  //
  // of cloudRouterIpAddress and customerRouterIpAddress for this attachment.
  //
  // All prefixes must be within link-local address space (169.254.0.0/16)
  //
  // and must be /29 or shorter (/28, /27, etc). Google will attempt to select
  //
  // an unused /29 from the supplied candidate prefix(es). The request will
  //
  // fail if all possible /29s are in use on Google's edge. If not supplied,
  //
  // Google will randomly select an unused /29 from all of link-local space.
  repeated string candidate_subnets = 8 [json_name = "candidate_subnets"];

  // IPv4 address + prefix length to be configured on Cloud Router
  //
  // Interface for this interconnect attachment.
  optional string cloud_router_ip_address = 9 [json_name = "cloud_router_ip_address"];

  // IPv6 address + prefix length to be configured on Cloud Router
  //
  // Interface for this interconnect attachment.
  optional string cloud_router_ipv6_address = 10 [json_name = "cloud_router_ipv6_address"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 11 [json_name = "creation_timestamp"];

  // IPv4 address + prefix length to be configured on the customer
  //
  // router subinterface for this interconnect attachment.
  optional string customer_router_ip_address = 12 [json_name = "customer_router_ip_address"];

  // IPv6 address + prefix length to be configured on the customer
  //
  // router subinterface for this interconnect attachment.
  optional string customer_router_ipv6_address = 13 [json_name = "customer_router_ipv6_address"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 14 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 15;

  // Desired availability domain for the attachment. Only available for type
  //
  // PARTNER, at creation time. For improved reliability, customers should
  //
  // configure a pair of attachments with one per availability domain. The
  //
  // selected availability domain will be provided to the Partner via the
  //
  // pairing key so that the provisioned circuit will lie in the specified
  //
  // domain. If not specified, the value will default to AVAILABILITY_DOMAIN_ANY.
  optional string edge_availability_domain = 16 [json_name = "edge_availability_domain"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 17 [json_name = "effective_labels"];

  // Indicates the user-supplied encryption option of this interconnect
  //
  // attachment. Can only be specified at attachment creation for PARTNER or
  //
  // DEDICATED attachments.
  //
  // * NONE - This is the default value, which means that the VLAN attachment
  //
  // carries unencrypted traffic. VMs are able to send traffic to, or receive
  //
  // traffic from, such a VLAN attachment.
  //
  // * IPSEC - The VLAN attachment carries only encrypted traffic that is
  //
  // encrypted by an IPsec device, such as an HA VPN gateway or third-party
  //
  // IPsec VPN. VMs cannot directly send traffic to, or receive traffic from,
  //
  // such a VLAN attachment. To use HA VPN over Cloud Interconnect, the VLAN
  //
  // attachment must be created with this option. Default value: "NONE" Possible
  // values: ["NONE", "IPSEC"]
  optional string encryption = 18;

  // Google reference ID, to be used when raising support tickets with
  //
  // Google or otherwise to debug backend connectivity issues.
  optional string google_reference_id = 19 [json_name = "google_reference_id"];

  optional string id = 20;

  // URL of the underlying Interconnect object that this attachment's
  //
  // traffic will traverse through. Required if type is DEDICATED, must not
  //
  // be set if type is PARTNER.
  optional string interconnect = 21;

  // URL of addresses that have been reserved for the interconnect attachment,
  //
  // Used only for interconnect attachment that has the encryption option as
  //
  // IPSEC.
  //
  // The addresses must be RFC 1918 IP address ranges. When creating HA VPN
  //
  // gateway over the interconnect attachment, if the attachment is configured
  //
  // to use an RFC 1918 IP address, then the VPN gateway's IP address will be
  //
  // allocated from the IP address range specified here.
  //
  // For example, if the HA VPN gateway's interface 0 is paired to this
  //
  // interconnect attachment, then an RFC 1918 IP address for the VPN gateway
  //
  // interface 0 will be allocated from the IP address specified for this
  //
  // interconnect attachment.
  //
  // If this field is not specified for interconnect attachment that has
  //
  // encryption option as IPSEC, later on when creating HA VPN gateway on this
  //
  // interconnect attachment, the HA VPN gateway's IP address will be
  //
  // allocated from regional external IP address pool.
  repeated string ipsec_internal_addresses = 22 [json_name = "ipsec_internal_addresses"];

  // A fingerprint for the labels being applied to this Interconnect, which is
  // essentially a hash
  //
  // of the labels set used for optimistic locking. The fingerprint is initially
  // generated by
  //
  // Compute Engine and changes after every request to modify or update labels.
  //
  // You must always provide an up-to-date fingerprint hash in order to update or
  // change labels,
  //
  // otherwise the request will fail with error 412 conditionNotMet.
  optional string label_fingerprint = 23 [json_name = "label_fingerprint"];

  // Labels for this resource. These can only be added or modified by the setLabels
  //
  // method. Each label key/value pair must comply with RFC1035. Label values may be
  // empty.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 24;

  // Maximum Transmission Unit (MTU), in bytes, of packets passing through this
  // interconnect attachment.
  //
  // Valid values are 1440, 1460, 1500, and 8896. If not specified, the value will
  // default to 1440.
  optional string mtu = 25;

  // Name of the resource. Provided by the client when the resource is created. The
  //
  // name must be 1-63 characters long, and comply with RFC1035. Specifically, the
  //
  // name must be 1-63 characters long and match the regular expression
  //
  // '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a
  //
  // lowercase letter, and all following characters must be a dash, lowercase
  //
  // letter, or digit, except the last character, which cannot be a dash.
  optional string name = 26;

  // [Output only for type PARTNER. Not present for DEDICATED]. The opaque
  //
  // identifier of an PARTNER attachment used to initiate provisioning with
  //
  // a selected partner. Of the form "XXXXX/region/domain"
  optional string pairing_key = 27 [json_name = "pairing_key"];

  // [Output only for type PARTNER. Not present for DEDICATED]. Optional
  //
  // BGP ASN for the router that should be supplied by a layer 3 Partner if
  //
  // they configured BGP on behalf of the customer.
  optional string partner_asn = 28 [json_name = "partner_asn"];

  // Information specific to an InterconnectAttachment. This property
  //
  // is populated if the interconnect that this is attached to is of type DEDICATED.
  repeated PrivateInterconnectInfo private_interconnect_info = 29 [json_name = "private_interconnect_info"];

  optional string project = 30;

  // Region where the regional interconnect attachment resides.
  optional string region = 31;

  // URL of the cloud router to be used for dynamic routing. This router must be in
  //
  // the same region as this InterconnectAttachment. The InterconnectAttachment will
  //
  // automatically connect the Interconnect to the network & region within which the
  //
  // Cloud Router is configured.
  optional string router = 32;

  optional string self_link = 33 [json_name = "self_link"];

  // The stack type for this interconnect attachment to identify whether the IPv6
  //
  // feature is enabled or not. If not specified, IPV4_ONLY will be used.
  //
  // This field can be both set at interconnect attachments creation and update
  //
  // interconnect attachment operations. Possible values: ["IPV4_IPV6", "IPV4_ONLY"]
  optional string stack_type = 34 [json_name = "stack_type"];

  // [Output Only] The current state of this attachment's functionality.
  optional string state = 35;

  // Length of the IPv4 subnet mask. Allowed values: 29 (default), 30. The default
  // value is 29,
  //
  // except for Cross-Cloud Interconnect connections that use an
  // InterconnectRemoteLocation with a
  //
  // constraints.subnetLengthRange.min equal to 30. For example, connections that use
  // an Azure
  //
  // remote location fall into this category. In these cases, the default value is 30,
  // and
  //
  // requesting 29 returns an error. Where both 29 and 30 are allowed, 29 is
  // preferred, because it
  //
  // gives Google Cloud Support more debugging visibility.
  optional int64 subnet_length = 36 [json_name = "subnet_length"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 37 [json_name = "terraform_labels"];

  // The type of InterconnectAttachment you wish to create. Defaults to
  //
  // DEDICATED. Possible values: ["DEDICATED", "PARTNER", "PARTNER_PROVIDER",
  // "L2_DEDICATED"]
  optional string type = 38;

  // The IEEE 802.1Q VLAN tag for this attachment, in the range 2-4094. When
  //
  // using PARTNER type this will be managed upstream.
  optional int64 vlan_tag8021q = 39 [json_name = "vlan_tag8021q"];

  L2Forwarding l2_forwarding = 40 [json_name = "l2_forwarding"];

  Params params = 41;

  repeated Timeouts timeouts = 42;

  google.protobuf.Value for_each = 43 [json_name = "for_each"];

  repeated string depends_on = 44 [json_name = "depends_on"];

  optional int32 count = 45;

  optional string provider = 46;

  terraform.v1.Lifecycle lifecycle = 47;

  message PrivateInterconnectInfo {
    // tag8021q: number
    optional int64 tag8021q = 1;
  }

  message L2Forwarding {
    // The default appliance IP address.
    optional string default_appliance_ip_address = 1 [json_name = "default_appliance_ip_address"];

    // URL of the network to which this attachment belongs.
    optional string network = 2;

    // The tunnel endpoint IP address.
    optional string tunnel_endpoint_ip_address = 3 [json_name = "tunnel_endpoint_ip_address"];

    repeated ApplianceMappings appliance_mappings = 4 [json_name = "appliance_mappings"];

    GeneveHeader geneve_header = 5 [json_name = "geneve_header"];

    message ApplianceMappings {
      // The appliance IP address.
      optional string appliance_ip_address = 1 [json_name = "appliance_ip_address"];

      // The name of this appliance mapping rule.
      optional string name = 2;

      // The VLAN tag.
      optional string vlan_id = 3 [json_name = "vlan_id"];

      repeated InnerVlanToApplianceMappings inner_vlan_to_appliance_mappings = 4 [json_name = "inner_vlan_to_appliance_mappings"];

      message InnerVlanToApplianceMappings {
        // The inner appliance IP address.
        optional string inner_appliance_ip_address = 1 [json_name = "inner_appliance_ip_address"];

        // List of inner VLAN tags.
        repeated string inner_vlan_tags = 2 [json_name = "inner_vlan_tags"];
      }
    }

    message GeneveHeader {
      // VNI is a 24-bit unique virtual network identifier.
      optional int64 vni = 1;
    }
  }

  message Params {
    // Resource manager tags to be bound to the interconnect attachment. Tag keys and
    // values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeInterconnectAttachmentGroup version is 0
message GoogleComputeInterconnectAttachmentGroup {
  // The redundancy this group is configured to support. The way a
  //
  // user queries what SLA their Attachment gets is by looking at this field of
  //
  // the Attachment's AttachmentGroup.
  repeated Configured configured = 1;

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 4;

  optional string id = 5;

  // The URL of an InterconnectGroup that groups these Attachments'
  //
  // Interconnects. Customers do not need to set this unless directed by
  //
  // Google Support.
  optional string interconnect_group = 6 [json_name = "interconnect_group"];

  // An analysis of the logical layout of Attachments in this
  //
  // group. Every Attachment in the group is shown once in this structure.
  repeated LogicalStructure logical_structure = 7 [json_name = "logical_structure"];

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be
  //
  // 1-63 characters long, and comply with RFC1035. Specifically, the name must be
  // 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first
  //
  // character must be a lowercase letter, and all following characters must be a
  // dash,
  //
  // lowercase letter, or digit, except the last character, which cannot be a dash.
  optional string name = 8;

  optional string project = 9;

  repeated Attachments attachments = 10;

  Intent intent = 11;

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Configured {
    // availability_sla: list of object
    repeated AvailabilitySla availability_sla = 1 [json_name = "availability_sla"];

    message AvailabilitySla {
      // effective_sla: string
      optional string effective_sla = 1 [json_name = "effective_sla"];

      // intended_sla_blockers: list of object
      repeated IntendedSlaBlockers intended_sla_blockers = 2 [json_name = "intended_sla_blockers"];

      message IntendedSlaBlockers {
        // attachments: list of string
        repeated string attachments = 1;

        // blocker_type: string
        optional string blocker_type = 2 [json_name = "blocker_type"];

        // documentation_link: string
        optional string documentation_link = 3 [json_name = "documentation_link"];

        // explanation: string
        optional string explanation = 4;

        // metros: list of string
        repeated string metros = 5;

        // regions: list of string
        repeated string regions = 6;

        // zones: list of string
        repeated string zones = 7;
      }
    }
  }

  message LogicalStructure {
    // regions: list of object
    repeated Regions regions = 1;

    message Regions {
      // metros: list of object
      repeated Metros metros = 1;

      // region: string
      optional string region = 2;

      message Metros {
        // facilities: list of object
        repeated Facilities facilities = 1;

        // metro: string
        optional string metro = 2;

        message Facilities {
          // facility: string
          optional string facility = 1;

          // zones: list of object
          repeated Zones zones = 2;

          message Zones {
            // attachments: list of string
            repeated string attachments = 1;

            // zone: string
            optional string zone = 2;
          }
        }
      }
    }
  }

  message Attachments {
    optional string attachment = 1;

    optional string name = 2;
  }

  message Intent {
    // Which SLA the user intends this group to support. Possible values:
    // ["PRODUCTION_NON_CRITICAL", "PRODUCTION_CRITICAL", "NO_SLA",
    // "AVAILABILITY_SLA_UNSPECIFIED"]
    optional string availability_sla = 1 [json_name = "availability_sla"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeInterconnectGroup version is 0
message GoogleComputeInterconnectGroup {
  // The status of the group as configured. This has the same
  //
  // structure as the operational field reported by the OperationalStatus
  //
  // method, but does not take into account the operational status of each
  //
  // resource.
  repeated Configured configured = 1;

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 4;

  optional string id = 5;

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be
  //
  // 1-63 characters long, and comply with RFC1035. Specifically, the name must be
  // 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first
  //
  // character must be a lowercase letter, and all following characters must be a
  // dash,
  //
  // lowercase letter, or digit, except the last character, which cannot be a dash.
  optional string name = 6;

  // An analysis of the physical layout of Interconnects in this
  //
  // group. Every Interconnect in the group is shown once in this structure.
  repeated PhysicalStructure physical_structure = 7 [json_name = "physical_structure"];

  optional string project = 8;

  Intent intent = 9;

  repeated Interconnects interconnects = 10;

  repeated Timeouts timeouts = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  optional int32 count = 14;

  optional string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Configured {
    // topology_capability: list of object
    repeated TopologyCapability topology_capability = 1 [json_name = "topology_capability"];

    message TopologyCapability {
      // intended_capability_blockers: list of object
      repeated IntendedCapabilityBlockers intended_capability_blockers = 1 [json_name = "intended_capability_blockers"];

      // supported_sla: string
      optional string supported_sla = 2 [json_name = "supported_sla"];

      message IntendedCapabilityBlockers {
        // blocker_type: string
        optional string blocker_type = 1 [json_name = "blocker_type"];

        // documentation_link: string
        optional string documentation_link = 2 [json_name = "documentation_link"];

        // explanation: string
        optional string explanation = 3;

        // facilities: list of string
        repeated string facilities = 4;

        // interconnects: list of string
        repeated string interconnects = 5;

        // metros: list of string
        repeated string metros = 6;

        // zones: list of string
        repeated string zones = 7;
      }
    }
  }

  message PhysicalStructure {
    // metros: list of object
    repeated Metros metros = 1;

    message Metros {
      // facilities: list of object
      repeated Facilities facilities = 1;

      // metro: string
      optional string metro = 2;

      message Facilities {
        // facility: string
        optional string facility = 1;

        // zones: list of object
        repeated Zones zones = 2;

        message Zones {
          // interconnects: list of string
          repeated string interconnects = 1;

          // zone: string
          optional string zone = 2;
        }
      }
    }
  }

  message Intent {
    // The reliability the user intends this group to be capable of, in terms
    //
    // of the Interconnect product SLAs. Possible values: ["PRODUCTION_NON_CRITICAL",
    // "PRODUCTION_CRITICAL", "NO_SLA", "AVAILABILITY_SLA_UNSPECIFIED"]
    optional string topology_capability = 1 [json_name = "topology_capability"];
  }

  message Interconnects {
    // The URL of an Interconnect in this group. All Interconnects in the group are
    // unique.
    optional string interconnect = 1;

    optional string name = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeManagedSslCertificate version is 0
message GoogleComputeManagedSslCertificate {
  // The unique identifier for the resource.
  optional int64 certificate_id = 1 [json_name = "certificate_id"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  // Expire time of the certificate in RFC3339 text format.
  optional string expire_time = 5 [json_name = "expire_time"];

  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  //
  //
  // These are in the same namespace as the managed SSL certificates.
  optional string name = 7;

  optional string project = 8;

  optional string self_link = 9 [json_name = "self_link"];

  // Domains associated with the certificate via Subject Alternative Name.
  repeated string subject_alternative_names = 10 [json_name = "subject_alternative_names"];

  // Enum field whose value is always 'MANAGED' - used to signal to the API
  //
  // which type this is. Default value: "MANAGED" Possible values: ["MANAGED"]
  optional string type = 11;

  Managed managed = 12;

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Managed {
    // Domains for which a managed SSL certificate will be valid.  Currently,
    //
    // there can be up to 100 domains in this list.
    repeated string domains = 1;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeNetwork version is 0
message GoogleComputeNetwork {
  // When set to 'true', the network is created in "auto subnet mode" and
  //
  // it will create a subnet for each region automatically across the
  //
  // '10.128.0.0/9' address range.
  //
  //
  // When set to 'false', the network is created in "custom subnet mode" so
  //
  // the user can explicitly connect subnetwork resources.
  optional bool auto_create_subnetworks = 1 [json_name = "auto_create_subnetworks"];

  // Enables/disables the comparison of MED across routes with different Neighbor
  // ASNs.
  //
  // This value can only be set if the --bgp-best-path-selection-mode is STANDARD
  optional bool bgp_always_compare_med = 2 [json_name = "bgp_always_compare_med"];

  // The BGP best selection algorithm to be employed. MODE can be LEGACY or STANDARD.
  // Possible values: ["LEGACY", "STANDARD"]
  optional string bgp_best_path_selection_mode = 3 [json_name = "bgp_best_path_selection_mode"];

  // Choice of the behavior of inter-regional cost and MED in the BPS algorithm.
  // Possible values: ["DEFAULT", "ADD_COST_TO_MED"]
  optional string bgp_inter_region_cost = 4 [json_name = "bgp_inter_region_cost"];

  // If set to 'true', the 'bgp_always_compare_med' field will be cleared.
  //
  // If set to 'false' (the default), 'bgp_always_compare_med' will be set to the
  // value specified in the configuration.
  optional bool delete_bgp_always_compare_med = 5 [json_name = "delete_bgp_always_compare_med"];

  // If set to 'true', default routes ('0.0.0.0/0') will be deleted
  //
  // immediately after network creation. Defaults to 'false'.
  optional bool delete_default_routes_on_create = 6 [json_name = "delete_default_routes_on_create"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 7 [json_name = "deletion_policy"];

  // An optional description of this resource. The resource must be
  //
  // recreated to modify this field.
  optional string description = 8;

  // Enable ULA internal ipv6 on this network. Enabling this feature will assign
  //
  // a /48 from google defined ULA prefix fd20::/20.
  optional bool enable_ula_internal_ipv6 = 9 [json_name = "enable_ula_internal_ipv6"];

  // The gateway address for default routing out of the network. This value
  //
  // is selected by GCP.
  optional string gateway_ipv4 = 10 [json_name = "gateway_ipv4"];

  optional string id = 11;

  // When enabling ula internal ipv6, caller optionally can specify the /48 range
  //
  // they want from the google defined ULA prefix fd20::/20. The input must be a
  //
  // valid /48 ULA IPv6 address and must be within the fd20::/20. Operation will
  //
  // fail if the speficied /48 is already in used by another resource.
  //
  // If the field is not speficied, then a /48 range will be randomly allocated from
  // fd20::/20 and returned via this field.
  optional string internal_ipv6_range = 12 [json_name = "internal_ipv6_range"];

  // Maximum Transmission Unit in bytes. The default value is 1460 bytes.
  //
  // The minimum value for this field is 1300 and the maximum value is 8896 bytes
  // (jumbo frames).
  //
  // Note that packets larger than 1500 bytes (standard Ethernet) can be subject to
  // TCP-MSS clamping or dropped
  //
  // with an ICMP 'Fragmentation-Needed' message if the packets are routed to the
  // Internet or other VPCs
  //
  // with varying MTUs.
  optional int64 mtu = 13;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 14;

  // Set the order that Firewall Rules and Firewall Policies are evaluated. Default
  // value: "AFTER_CLASSIC_FIREWALL" Possible values: ["BEFORE_CLASSIC_FIREWALL",
  // "AFTER_CLASSIC_FIREWALL"]
  optional string network_firewall_policy_enforcement_order = 15 [
    json_name = "network_firewall_policy_enforcement_order"
  ];

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string network_id = 16 [json_name = "network_id"];

  // A full or partial URL of the network profile to apply to this network.
  //
  // This field can be set only at resource creation time. For example, the
  //
  // following are valid URLs:
  //
  // com/compute/v1/projects/{projectId}/global/networkProfiles/{network_profile_name}
  //
  // * projects/{projectId}/global/networkProfiles/{network_profile_name}
  optional string network_profile = 17 [json_name = "network_profile"];

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string numeric_id = 18 [json_name = "numeric_id"];

  optional string project = 19;

  // The network-wide routing mode to use. If set to 'REGIONAL', this
  //
  // network's cloud routers will only advertise routes with subnetworks
  //
  // of this network in the same region as the router. If set to 'GLOBAL',
  //
  // this network's cloud routers will advertise routes with all
  //
  // subnetworks of this network, across regions. Possible values: ["REGIONAL",
  // "GLOBAL"]
  optional string routing_mode = 20 [json_name = "routing_mode"];

  optional string self_link = 21 [json_name = "self_link"];

  Params params = 22;

  repeated Timeouts timeouts = 23;

  google.protobuf.Value for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  optional int32 count = 26;

  optional string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message Params {
    // Resource manager tags to be bound to the network. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNetworkAttachment version is 0
message GoogleComputeNetworkAttachment {
  // An array of connections for all the producers connected to this network
  // attachment.
  repeated ConnectionEndpoints connection_endpoints = 1 [json_name = "connection_endpoints"];

  // The connection preference of service attachment. The value can be set to
  // ACCEPT_AUTOMATIC. An ACCEPT_AUTOMATIC service attachment is one that always
  // accepts the connection from consumer forwarding rules. Possible values:
  // ["ACCEPT_AUTOMATIC", "ACCEPT_MANUAL", "INVALID"]
  optional string connection_preference = 2 [json_name = "connection_preference"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 5;

  // Fingerprint of this resource. A hash of the contents stored in this object. This
  //
  // field is used in optimistic locking. An up-to-date fingerprint must be provided
  // in order to patch.
  optional string fingerprint = 6;

  // The unique identifier for the resource type. The server generates this
  // identifier.
  optional string id = 7;

  // Type of the resource.
  optional string kind = 8;

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be 1-63 characters long, and comply with RFC1035. Specifically, the
  // name must be 1-63 characters long and match the regular expression
  // [a-z]([-a-z0-9]*[a-z0-9])? which means the first character must be a lowercase
  // letter, and all following characters must be a dash, lowercase letter, or digit,
  // except the last character, which cannot be a dash.
  optional string name = 9;

  // The URL of the network which the Network Attachment belongs to. Practically it is
  // inferred by fetching the network of the first subnetwork associated.
  //
  // Because it is required that all the subnetworks must be from the same network, it
  // is assured that the Network Attachment belongs to the same network as all the
  // subnetworks.
  optional string network = 10;

  // Projects that are allowed to connect to this network attachment. The project can
  // be specified using its id or number.
  repeated string producer_accept_lists = 11 [json_name = "producer_accept_lists"];

  // Projects that are not allowed to connect to this network attachment. The project
  // can be specified using its id or number.
  repeated string producer_reject_lists = 12 [json_name = "producer_reject_lists"];

  optional string project = 13;

  // URL of the region where the network attachment resides. This field applies only
  // to the region resource. You must specify this field as part of the HTTP request
  // URL. It is not settable as a field in the request body.
  optional string region = 14;

  // Server-defined URL for the resource.
  optional string self_link = 15 [json_name = "self_link"];

  // Server-defined URL for this resource's resource id.
  optional string self_link_with_id = 16 [json_name = "self_link_with_id"];

  // An array of URLs where each entry is the URL of a subnet provided by the service
  // consumer to use for endpoints in the producers that connect to this network
  // attachment.
  repeated string subnetworks = 17;

  repeated Timeouts timeouts = 18;

  google.protobuf.Value for_each = 19 [json_name = "for_each"];

  repeated string depends_on = 20 [json_name = "depends_on"];

  optional int32 count = 21;

  optional string provider = 22;

  terraform.v1.Lifecycle lifecycle = 23;

  message ConnectionEndpoints {
    // ip_address: string
    optional string ip_address = 1 [json_name = "ip_address"];

    // project_id_or_num: string
    optional string project_id_or_num = 2 [json_name = "project_id_or_num"];

    // secondary_ip_cidr_ranges: string
    optional string secondary_ip_cidr_ranges = 3 [json_name = "secondary_ip_cidr_ranges"];

    // status: string
    optional string status = 4;

    // subnetwork: string
    optional string subnetwork = 5;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNetworkEndpoint version is 0
message GoogleComputeNetworkEndpoint {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The name for a specific VM instance that the IP address belongs to.
  //
  // This is required for network endpoints of type GCE_VM_IP_PORT.
  //
  // The instance must be in the same zone of network endpoint group.
  optional string instance = 3;

  // IPv4 address of network endpoint. The IP address must belong
  //
  // to a VM in GCE (either the primary IP or as part of an aliased IP
  //
  // range).
  optional string ip_address = 4 [json_name = "ip_address"];

  // The network endpoint group this endpoint is part of.
  optional string network_endpoint_group = 5 [json_name = "network_endpoint_group"];

  // Port number of network endpoint.
  //
  // **Note** 'port' is required unless the Network Endpoint Group is created
  //
  // with the type of 'GCE_VM_IP'
  optional int64 port = 6;

  optional string project = 7;

  // Zone where the containing network endpoint group is located.
  optional string zone = 8;

  repeated Timeouts timeouts = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  optional int32 count = 12;

  optional string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeNetworkEndpointGroup version is 0
message GoogleComputeNetworkEndpointGroup {
  // The default port used if the port number is not specified in the
  //
  // network endpoint.
  optional int64 default_port = 1 [json_name = "default_port"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 3;

  // The uniquely generated identifier for the resource. This identifier is defined by
  // the server.
  optional int64 generated_id = 4 [json_name = "generated_id"];

  optional string id = 5;

  // Name of the resource; provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 6;

  // The network to which all network endpoints in the NEG belong.
  //
  // Uses "default" project network if unspecified.
  optional string network = 7;

  // Type of network endpoints in this network endpoint group.
  //
  // NON_GCP_PRIVATE_IP_PORT is used for hybrid connectivity network
  //
  // endpoint groups (see https://cloud.google.com/load-balancing/docs/hybrid).
  //
  // Note that NON_GCP_PRIVATE_IP_PORT can only be used with Backend Services
  //
  // that 1) have the following load balancing schemes: EXTERNAL, EXTERNAL_MANAGED,
  //
  // INTERNAL_MANAGED, and INTERNAL_SELF_MANAGED and 2) support the RATE or
  //
  // CONNECTION balancing modes.
  //
  //
  // Possible values include: GCE_VM_IP, GCE_VM_IP_PORT, NON_GCP_PRIVATE_IP_PORT,
  // INTERNET_IP_PORT, INTERNET_FQDN_PORT, SERVERLESS, and PRIVATE_SERVICE_CONNECT.
  // Default value: "GCE_VM_IP_PORT" Possible values: ["GCE_VM_IP", "GCE_VM_IP_PORT",
  // "NON_GCP_PRIVATE_IP_PORT", "INTERNET_IP_PORT", "INTERNET_FQDN_PORT",
  // "SERVERLESS", "PRIVATE_SERVICE_CONNECT", "GCE_VM_IP_DEDICATED_BACKEND"]
  optional string network_endpoint_type = 8 [json_name = "network_endpoint_type"];

  optional string project = 9;

  optional string self_link = 10 [json_name = "self_link"];

  // Number of network endpoints in the network endpoint group.
  optional int64 size = 11;

  // Optional subnetwork to which all network endpoints in the NEG belong.
  optional string subnetwork = 12;

  // Zone where the network endpoint group is located.
  optional string zone = 13;

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeNetworkEndpoints version is 0
message GoogleComputeNetworkEndpoints {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The network endpoint group these endpoints are part of.
  optional string network_endpoint_group = 3 [json_name = "network_endpoint_group"];

  optional string project = 4;

  // Zone where the containing network endpoint group is located.
  optional string zone = 5;

  repeated NetworkEndpoints network_endpoints = 6 [json_name = "network_endpoints"];

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message NetworkEndpoints {
    // The name for a specific VM instance that the IP address belongs to.
    //
    // This is required for network endpoints of type GCE_VM_IP_PORT.
    //
    // The instance must be in the same zone as the network endpoint group.
    optional string instance = 1;

    // IPv4 address of network endpoint. The IP address must belong
    //
    // to a VM in GCE (either the primary IP or as part of an aliased IP
    //
    // range).
    //
    // **Note** 'ip_address' is required unless the Network Endpoint Group is created
    // with the type of 'GCE_VM_IP_DEDICATED_BACKEND'
    optional string ip_address = 2 [json_name = "ip_address"];

    // Port number of network endpoint.
    //
    // **Note** 'port' is required unless the Network Endpoint Group is created
    //
    // with the type of 'GCE_VM_IP'
    optional int64 port = 3;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNetworkFirewallPolicy version is 0
message GoogleComputeNetworkFirewallPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 3;

  // Fingerprint of the resource. This field is used internally during updates of this
  // resource.
  optional string fingerprint = 4;

  optional string id = 5;

  // User-provided name of the Network firewall policy. The name should be unique in
  // the project in which the firewall policy is created. The name must be 1-63
  // characters long, and comply with RFC1035. Specifically, the name must be 1-63
  // characters long and match the regular expression [a-z]([-a-z0-9]*[a-z0-9])? which
  // means the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last character,
  // which cannot be a dash.
  optional string name = 6;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string network_firewall_policy_id = 7 [json_name = "network_firewall_policy_id"];

  // Policy type is used to determine which resources (networks) the policy can be
  // associated with.
  //
  // A policy can be associated with a network only if the network has the matching
  // policyType in its network profile.
  //
  // Different policy types may support some of the Firewall Rules features. Possible
  // values: ["VPC_POLICY"]
  optional string policy_type = 8 [json_name = "policy_type"];

  optional string project = 9;

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed
  // a set number of tuples.
  optional int64 rule_tuple_count = 10 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  optional string self_link = 11 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  optional string self_link_with_id = 12 [json_name = "self_link_with_id"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNetworkFirewallPolicyAssociation version is 0
message GoogleComputeNetworkFirewallPolicyAssociation {
  // The target that the firewall policy is attached to.
  optional string attachment_target = 1 [json_name = "attachment_target"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // The firewall policy of the resource.
  optional string firewall_policy = 3 [json_name = "firewall_policy"];

  optional string id = 4;

  // The name for an association.
  optional string name = 5;

  optional string project = 6;

  // The short name of the firewall policy of the association.
  optional string short_name = 7 [json_name = "short_name"];

  repeated Timeouts timeouts = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeNetworkFirewallPolicyIamBinding version is 0
message GoogleComputeNetworkFirewallPolicyIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeNetworkFirewallPolicyIamMember version is 0
message GoogleComputeNetworkFirewallPolicyIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeNetworkFirewallPolicyIamPolicy version is 0
message GoogleComputeNetworkFirewallPolicyIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// GoogleComputeNetworkFirewallPolicyRule version is 0
message GoogleComputeNetworkFirewallPolicyRule {
  // The Action to perform when the client connection triggers the rule. Valid actions
  // are "allow", "deny", "goto_next" and "apply_security_profile_group".
  optional string action = 1;

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description for this resource.
  optional string description = 4;

  // The direction in which this rule applies. Possible values: ["INGRESS", "EGRESS"]
  optional string direction = 5;

  // Denotes whether the firewall policy rule is disabled.
  //
  // When set to true, the firewall policy rule is not enforced and traffic behaves as
  // if it did not exist.
  //
  // If this is unspecified, the firewall policy rule will be enabled.
  optional bool disabled = 6;

  // Denotes whether to enable logging for a particular rule.
  //
  // If logging is enabled, logs will be exported to the configured export destination
  // in Stackdriver.
  //
  // Logs may be exported to BigQuery or Pub/Sub.
  //
  // Note: you cannot enable logging on "goto_next" rules.
  optional bool enable_logging = 7 [json_name = "enable_logging"];

  // The firewall policy of the resource.
  optional string firewall_policy = 8 [json_name = "firewall_policy"];

  optional string id = 9;

  // Type of the resource. Always 'compute#firewallPolicyRule' for firewall policy
  // rules
  optional string kind = 10;

  // An integer indicating the priority of a rule in the list.
  //
  // The priority must be a positive value between 0 and 2147483647.
  //
  // Rules are evaluated from highest to lowest priority where 0 is the highest
  // priority and 2147483647 is the lowest prority.
  optional int64 priority = 11;

  optional string project = 12;

  // An optional name for the rule. This field is not a unique identifier and can be
  // updated.
  optional string rule_name = 13 [json_name = "rule_name"];

  // Calculation of the complexity of a single firewall policy rule.
  optional int64 rule_tuple_count = 14 [json_name = "rule_tuple_count"];

  // A fully-qualified URL of a SecurityProfile resource instance.
  //
  // Example:
  // ts/{project}/locations/{location}/securityProfileGroups/my-security-profile-group
  //
  // Must be specified if action = 'apply_security_profile_group' and cannot be
  // specified for other actions.
  optional string security_profile_group = 15 [json_name = "security_profile_group"];

  // A list of forwarding rules to which this rule applies.
  //
  // This field allows you to control which load balancers get this rule.
  //
  // For example, the following are valid values:
  //
  // .googleapis.com/compute/v1/projects/project/global/forwardingRules/forwardingRule
  //
  // pis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule
  //
  // - projects/project/global/forwardingRules/forwardingRule
  //
  // - projects/project/regions/region/forwardingRules/forwardingRule
  repeated string target_forwarding_rules = 16 [json_name = "target_forwarding_rules"];

  // A list of service accounts indicating the sets of instances that are applied with
  // this rule.
  repeated string target_service_accounts = 17 [json_name = "target_service_accounts"];

  // Target types of the firewall policy rule.
  //
  // Default value is INSTANCES.
  //
  // When target_type is INTERNAL_MANAGED_LB, target_forwarding_rules must be set
  // Possible values: ["INSTANCES", "INTERNAL_MANAGED_LB"]
  optional string target_type = 18 [json_name = "target_type"];

  // Boolean flag indicating if the traffic should be TLS decrypted.
  //
  // Can be set only if action = 'apply_security_profile_group' and cannot be set for
  // other actions.
  optional bool tls_inspect = 19 [json_name = "tls_inspect"];

  Match match = 20;

  repeated TargetSecureTags target_secure_tags = 21 [json_name = "target_secure_tags"];

  repeated Timeouts timeouts = 22;

  google.protobuf.Value for_each = 23 [json_name = "for_each"];

  repeated string depends_on = 24 [json_name = "depends_on"];

  optional int32 count = 25;

  optional string provider = 26;

  terraform.v1.Lifecycle lifecycle = 27;

  message Match {
    // Address groups which should be matched against the traffic destination. Maximum
    // number of destination address groups is 10.
    repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

    // Fully Qualified Domain Name (FQDN) which should be matched against traffic
    // destination. Maximum number of destination fqdn allowed is 100.
    repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

    // CIDR IP address range. Maximum number of destination CIDR IP ranges allowed is
    // 5000.
    repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

    // Network context of the traffic destination. Possible values: ["UNSPECIFIED",
    // "INTERNET", "INTRA_VPC", "NON_INTERNET", "VPC_NETWORKS"]
    optional string dest_network_context = 4 [json_name = "dest_network_context"];

    // Region codes whose IP addresses will be used to match for destination of traffic.
    // Should be specified as 2 letter country code defined as per ISO 3166 alpha-2
    // country codes. ex."US" Maximum number of dest region codes allowed is 5000.
    repeated string dest_region_codes = 5 [json_name = "dest_region_codes"];

    // Names of Network Threat Intelligence lists. The IPs in these lists will be
    // matched against traffic destination.
    repeated string dest_threat_intelligences = 6 [json_name = "dest_threat_intelligences"];

    // Address groups which should be matched against the traffic source. Maximum number
    // of source address groups is 10.
    repeated string src_address_groups = 7 [json_name = "src_address_groups"];

    // Fully Qualified Domain Name (FQDN) which should be matched against traffic
    // source. Maximum number of source fqdn allowed is 100.
    repeated string src_fqdns = 8 [json_name = "src_fqdns"];

    // CIDR IP address range. Maximum number of source CIDR IP ranges allowed is 5000.
    repeated string src_ip_ranges = 9 [json_name = "src_ip_ranges"];

    // Network context of the traffic source. Possible values: ["UNSPECIFIED",
    // "INTERNET", "INTRA_VPC", "NON_INTERNET", "VPC_NETWORKS"]
    optional string src_network_context = 10 [json_name = "src_network_context"];

    // Networks of the traffic source. It can be either a full or partial url.
    repeated string src_networks = 11 [json_name = "src_networks"];

    // Region codes whose IP addresses will be used to match for source of traffic.
    // Should be specified as 2 letter country code defined as per ISO 3166 alpha-2
    // country codes. ex."US" Maximum number of source region codes allowed is 5000.
    repeated string src_region_codes = 12 [json_name = "src_region_codes"];

    // Names of Network Threat Intelligence lists. The IPs in these lists will be
    // matched against traffic source.
    repeated string src_threat_intelligences = 13 [json_name = "src_threat_intelligences"];

    repeated Layer4Configs layer4_configs = 14 [json_name = "layer4_configs"];

    repeated SrcSecureTags src_secure_tags = 15 [json_name = "src_secure_tags"];

    message Layer4Configs {
      // The IP protocol to which this rule applies. The protocol type is required when
      // creating a firewall rule.
      //
      // This value can either be one of the following well known protocol strings (tcp,
      // udp, icmp, esp, ah, ipip, sctp), or the IP protocol number.
      optional string ip_protocol = 1 [json_name = "ip_protocol"];

      // An optional list of ports to which this rule applies. This field is only
      // applicable for UDP or TCP protocol. Each entry must be either an integer or a
      // range. If not specified, this rule applies to connections through any port.
      //
      // Example inputs include: ["22"], ["80","443"], and ["12345-12349"].
      repeated string ports = 2;
    }

    message SrcSecureTags {
      // Name of the secure tag, created with TagManager's TagValue API.
      optional string name = 1;

      // State of the secure tag, either EFFECTIVE or INEFFECTIVE. A secure tag is
      // INEFFECTIVE when it is deleted or its network is deleted.
      optional string state = 2;
    }
  }

  message TargetSecureTags {
    // Name of the secure tag, created with TagManager's TagValue API.
    optional string name = 1;

    // State of the secure tag, either EFFECTIVE or INEFFECTIVE. A secure tag is
    // INEFFECTIVE when it is deleted or its network is deleted.
    optional string state = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNetworkFirewallPolicyWithRules version is 0
message GoogleComputeNetworkFirewallPolicyWithRules {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Fingerprint of the resource. This field is used internally during updates of this
  // resource.
  optional string fingerprint = 4;

  optional string id = 5;

  // User-provided name of the Network firewall policy.
  //
  // The name should be unique in the project in which the firewall policy is created.
  //
  // The name must be 1-63 characters long, and comply with RFC1035. Specifically,
  //
  // the name must be 1-63 characters long and match the regular expression
  // [a-z]([-a-z0-9]*[a-z0-9])?
  //
  // which means the first character must be a lowercase letter, and all following
  // characters must be a dash,
  //
  // lowercase letter, or digit, except the last character, which cannot be a dash.
  optional string name = 6;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string network_firewall_policy_id = 7 [json_name = "network_firewall_policy_id"];

  // Policy type is used to determine which resources (networks) the policy can be
  // associated with.
  //
  // A policy can be associated with a network only if the network has the matching
  // policyType in its network profile.
  //
  // Different policy types may support some of the Firewall Rules features. Possible
  // values: ["VPC_POLICY"]
  optional string policy_type = 8 [json_name = "policy_type"];

  // A list of firewall policy pre-defined rules.
  repeated PredefinedRules predefined_rules = 9 [json_name = "predefined_rules"];

  optional string project = 10;

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed
  // a set number of tuples.
  optional int64 rule_tuple_count = 11 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  optional string self_link = 12 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  optional string self_link_with_id = 13 [json_name = "self_link_with_id"];

  repeated Rule rule = 14;

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  optional int32 count = 18;

  optional string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message PredefinedRules {
    // action: string
    optional string action = 1;

    // description: string
    optional string description = 2;

    // direction: string
    optional string direction = 3;

    // disabled: bool
    optional bool disabled = 4;

    // enable_logging: bool
    optional bool enable_logging = 5 [json_name = "enable_logging"];

    // match: list of object
    repeated Match match = 6;

    // priority: number
    optional int64 priority = 7;

    // rule_name: string
    optional string rule_name = 8 [json_name = "rule_name"];

    // security_profile_group: string
    optional string security_profile_group = 9 [json_name = "security_profile_group"];

    // target_secure_tag: list of object
    repeated TargetSecureTag target_secure_tag = 10 [json_name = "target_secure_tag"];

    // target_service_accounts: list of string
    repeated string target_service_accounts = 11 [json_name = "target_service_accounts"];

    // tls_inspect: bool
    optional bool tls_inspect = 12 [json_name = "tls_inspect"];

    message Match {
      // dest_address_groups: list of string
      repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

      // dest_fqdns: list of string
      repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

      // dest_ip_ranges: list of string
      repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

      // dest_region_codes: list of string
      repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

      // dest_threat_intelligences: list of string
      repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

      // layer4_config: list of object
      repeated Layer4Config layer4_config = 6 [json_name = "layer4_config"];

      // src_address_groups: list of string
      repeated string src_address_groups = 7 [json_name = "src_address_groups"];

      // src_fqdns: list of string
      repeated string src_fqdns = 8 [json_name = "src_fqdns"];

      // src_ip_ranges: list of string
      repeated string src_ip_ranges = 9 [json_name = "src_ip_ranges"];

      // src_region_codes: list of string
      repeated string src_region_codes = 10 [json_name = "src_region_codes"];

      // src_secure_tag: list of object
      repeated SrcSecureTag src_secure_tag = 11 [json_name = "src_secure_tag"];

      // src_threat_intelligences: list of string
      repeated string src_threat_intelligences = 12 [json_name = "src_threat_intelligences"];

      message Layer4Config {
        // ip_protocol: string
        optional string ip_protocol = 1 [json_name = "ip_protocol"];

        // ports: list of string
        repeated string ports = 2;
      }

      message SrcSecureTag {
        // name: string
        optional string name = 1;

        // state: string
        optional string state = 2;
      }
    }

    message TargetSecureTag {
      // name: string
      optional string name = 1;

      // state: string
      optional string state = 2;
    }
  }

  message Rule {
    // The Action to perform when the client connection triggers the rule. Can currently
    // be either
    //
    // "allow", "deny", "apply_security_profile_group" or "goto_next".
    optional string action = 1;

    // A description of the rule.
    optional string description = 2;

    // The direction in which this rule applies. If unspecified an INGRESS rule is
    // created. Possible values: ["INGRESS", "EGRESS"]
    optional string direction = 3;

    // Denotes whether the firewall policy rule is disabled. When set to true,
    //
    // the firewall policy rule is not enforced and traffic behaves as if it did
    //
    // not exist. If this is unspecified, the firewall policy rule will be
    //
    // enabled.
    optional bool disabled = 4;

    // Denotes whether to enable logging for a particular rule.
    //
    // If logging is enabled, logs will be exported to the
    //
    // configured export destination in Stackdriver.
    optional bool enable_logging = 5 [json_name = "enable_logging"];

    // An integer indicating the priority of a rule in the list. The priority must be a
    // value
    //
    // between 0 and 2147483647. Rules are evaluated from highest to lowest priority
    // where 0 is the
    //
    // highest priority and 2147483647 is the lowest priority.
    optional int64 priority = 6;

    // An optional name for the rule. This field is not a unique identifier
    //
    // and can be updated.
    optional string rule_name = 7 [json_name = "rule_name"];

    // A fully-qualified URL of a SecurityProfile resource instance.
    //
    // Example:
    //
    // ts/{project}/locations/{location}/securityProfileGroups/my-security-profile-group
    //
    // Must be specified if action is 'apply_security_profile_group'.
    optional string security_profile_group = 8 [json_name = "security_profile_group"];

    // A list of service accounts indicating the sets of
    //
    // instances that are applied with this rule.
    repeated string target_service_accounts = 9 [json_name = "target_service_accounts"];

    // Boolean flag indicating if the traffic should be TLS decrypted.
    //
    // It can be set only if action = 'apply_security_profile_group' and cannot be set
    // for other actions.
    optional bool tls_inspect = 10 [json_name = "tls_inspect"];

    Match match = 11;

    repeated TargetSecureTag target_secure_tag = 12 [json_name = "target_secure_tag"];

    message Match {
      // Address groups which should be matched against the traffic destination.
      //
      // Maximum number of destination address groups is 10.
      repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

      // Fully Qualified Domain Name (FQDN) which should be matched against
      //
      // traffic destination. Maximum number of destination fqdn allowed is 100.
      repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

      // Destination IP address range in CIDR format. Required for
      //
      // EGRESS rules.
      repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

      // Region codes whose IP addresses will be used to match for destination
      //
      // of traffic. Should be specified as 2 letter country code defined as per
      //
      // ISO 3166 alpha-2 country codes. ex."US"
      //
      // Maximum number of destination region codes allowed is 5000.
      repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

      // Names of Network Threat Intelligence lists.
      //
      // The IPs in these lists will be matched against traffic destination.
      repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

      // Address groups which should be matched against the traffic source.
      //
      // Maximum number of source address groups is 10.
      repeated string src_address_groups = 6 [json_name = "src_address_groups"];

      // Fully Qualified Domain Name (FQDN) which should be matched against
      //
      // traffic source. Maximum number of source fqdn allowed is 100.
      repeated string src_fqdns = 7 [json_name = "src_fqdns"];

      // Source IP address range in CIDR format. Required for
      //
      // INGRESS rules.
      repeated string src_ip_ranges = 8 [json_name = "src_ip_ranges"];

      // Region codes whose IP addresses will be used to match for source
      //
      // of traffic. Should be specified as 2 letter country code defined as per
      //
      // ISO 3166 alpha-2 country codes. ex."US"
      //
      // Maximum number of source region codes allowed is 5000.
      repeated string src_region_codes = 9 [json_name = "src_region_codes"];

      // Names of Network Threat Intelligence lists.
      //
      // The IPs in these lists will be matched against traffic source.
      repeated string src_threat_intelligences = 10 [json_name = "src_threat_intelligences"];

      repeated Layer4Config layer4_config = 11 [json_name = "layer4_config"];

      repeated SrcSecureTag src_secure_tag = 12 [json_name = "src_secure_tag"];

      message Layer4Config {
        // The IP protocol to which this rule applies. The protocol
        //
        // type is required when creating a firewall rule.
        //
        // This value can either be one of the following well
        //
        // known protocol strings (tcp, udp, icmp, esp, ah, ipip, sctp),
        //
        // or the IP protocol number.
        optional string ip_protocol = 1 [json_name = "ip_protocol"];

        // An optional list of ports to which this rule applies. This field
        //
        // is only applicable for UDP or TCP protocol. Each entry must be
        //
        // either an integer or a range. If not specified, this rule
        //
        // applies to connections through any port.
        //
        // Example inputs include: ["22"], ["80","443"], and
        //
        // ["12345-12349"].
        repeated string ports = 2;
      }

      message SrcSecureTag {
        // Name of the secure tag, created with TagManager's TagValue API.
        //
        // @pattern tagValues/[0-9]+
        optional string name = 1;

        // [Output Only] State of the secure tag, either 'EFFECTIVE' or
        //
        // 'INEFFECTIVE'. A secure tag is 'INEFFECTIVE' when it is deleted
        //
        // or its network is deleted.
        optional string state = 2;
      }
    }

    message TargetSecureTag {
      // Name of the secure tag, created with TagManager's TagValue API.
      //
      // @pattern tagValues/[0-9]+
      optional string name = 1;

      // [Output Only] State of the secure tag, either 'EFFECTIVE' or
      //
      // 'INEFFECTIVE'. A secure tag is 'INEFFECTIVE' when it is deleted
      //
      // or its network is deleted.
      optional string state = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNetworkPeering version is 0
message GoogleComputeNetworkPeering {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // Whether to export the custom routes to the peer network. Defaults to false.
  optional bool export_custom_routes = 2 [json_name = "export_custom_routes"];

  optional bool export_subnet_routes_with_public_ip = 3 [json_name = "export_subnet_routes_with_public_ip"];

  optional string id = 4;

  // Whether to export the custom routes from the peer network. Defaults to false.
  optional bool import_custom_routes = 5 [json_name = "import_custom_routes"];

  optional bool import_subnet_routes_with_public_ip = 6 [json_name = "import_subnet_routes_with_public_ip"];

  // Name of the peering.
  optional string name = 7;

  // The primary network of the peering.
  optional string network = 8;

  // The peer network in the peering. The peer network may belong to a different
  // project.
  optional string peer_network = 9 [json_name = "peer_network"];

  // Which IP version(s) of traffic and routes are allowed to be imported or exported
  // between peer networks. The default value is IPV4_ONLY. Possible values:
  // ["IPV4_ONLY", "IPV4_IPV6"]
  optional string stack_type = 10 [json_name = "stack_type"];

  // State for the peering, either ACTIVE or INACTIVE. The peering is ACTIVE when
  // there's a matching configuration in the peer network.
  optional string state = 11;

  // Details about the current state of the peering.
  optional string state_details = 12 [json_name = "state_details"];

  // The update strategy determines the semantics for updates and deletes to the
  // peering connection configuration. The default value is INDEPENDENT. Possible
  // values: ["INDEPENDENT", "CONSENSUS"]
  optional string update_strategy = 13 [json_name = "update_strategy"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNetworkPeeringRoutesConfig version is 0
message GoogleComputeNetworkPeeringRoutesConfig {
  // Whether to export the custom routes to the peer network.
  optional bool export_custom_routes = 1 [json_name = "export_custom_routes"];

  // Whether subnet routes with public IP range are exported.
  //
  // IPv4 special-use ranges are always exported to peers and
  //
  // are not controlled by this field.
  optional bool export_subnet_routes_with_public_ip = 2 [json_name = "export_subnet_routes_with_public_ip"];

  optional string id = 3;

  // Whether to import the custom routes to the peer network.
  optional bool import_custom_routes = 4 [json_name = "import_custom_routes"];

  // Whether subnet routes with public IP range are imported.
  //
  // IPv4 special-use ranges are always imported from peers and
  //
  // are not controlled by this field.
  optional bool import_subnet_routes_with_public_ip = 5 [json_name = "import_subnet_routes_with_public_ip"];

  // The name of the primary network for the peering.
  optional string network = 6;

  // Name of the peering.
  optional string peering = 7;

  optional string project = 8;

  repeated Timeouts timeouts = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  optional int32 count = 12;

  optional string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNodeGroup version is 0
message GoogleComputeNodeGroup {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional textual description of the resource.
  optional string description = 3;

  optional string id = 4;

  // The initial number of nodes in the node group. One of 'initial_size' or
  // 'autoscaling_policy' must be configured on resource creation.
  optional int64 initial_size = 5 [json_name = "initial_size"];

  // Specifies how to handle instances when a node in the group undergoes maintenance.
  // Set to one of: DEFAULT, RESTART_IN_PLACE, or MIGRATE_WITHIN_NODE_GROUP. The
  // default value is DEFAULT.
  optional string maintenance_policy = 6 [json_name = "maintenance_policy"];

  // Name of the resource.
  optional string name = 7;

  // The URL of the node template to which this node group belongs.
  optional string node_template = 8 [json_name = "node_template"];

  optional string project = 9;

  optional string self_link = 10 [json_name = "self_link"];

  // The total number of nodes in the node group.
  optional int64 size = 11;

  // Zone where this node group is located
  optional string zone = 12;

  AutoscalingPolicy autoscaling_policy = 13 [json_name = "autoscaling_policy"];

  MaintenanceWindow maintenance_window = 14 [json_name = "maintenance_window"];

  ShareSettings share_settings = 15 [json_name = "share_settings"];

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message AutoscalingPolicy {
    // Maximum size of the node group. Set to a value less than or equal
    //
    // to 100 and greater than or equal to min-nodes.
    optional int64 max_nodes = 1 [json_name = "max_nodes"];

    // Minimum size of the node group. Must be less
    //
    // than or equal to max-nodes. The default value is 0.
    optional int64 min_nodes = 2 [json_name = "min_nodes"];

    // The autoscaling mode. Set to one of the following:
    //
    // - OFF: Disables the autoscaler.
    //
    // - ON: Enables scaling in and scaling out.
    //
    // - ONLY_SCALE_OUT: Enables only scaling out.
    //
    // You must use this mode if your node groups are configured to
    //
    // restart their hosted VMs on minimal servers. Possible values: ["OFF", "ON",
    // "ONLY_SCALE_OUT"]
    optional string mode = 3;
  }

  message MaintenanceWindow {
    // instances.start time of the window. This must be in UTC format that resolves to
    // one of 00:00, 04:00, 08:00, 12:00, 16:00, or 20:00. For example, both 13:00-5 and
    // 08:00 are valid.
    optional string start_time = 1 [json_name = "start_time"];
  }

  message ShareSettings {
    // Node group sharing type. Possible values: ["ORGANIZATION", "SPECIFIC_PROJECTS",
    // "LOCAL"]
    optional string share_type = 1 [json_name = "share_type"];

    repeated ProjectMap project_map = 2 [json_name = "project_map"];

    message ProjectMap {
      optional string id = 1;

      // The project id/number should be the same as the key of this project config in the
      // project map.
      optional string project_id = 2 [json_name = "project_id"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeNodeTemplate version is 0
message GoogleComputeNodeTemplate {
  // CPU overcommit. Default value: "NONE" Possible values: ["ENABLED", "NONE"]
  optional string cpu_overcommit_type = 1 [json_name = "cpu_overcommit_type"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional textual description of the resource.
  optional string description = 4;

  optional string id = 5;

  // Name of the resource.
  optional string name = 6;

  // Labels to use for node affinity, which will be used in
  //
  // instance scheduling.
  map<string, string> node_affinity_labels = 7 [json_name = "node_affinity_labels"];

  // Node type to use for nodes group that are created from this template.
  //
  // Only one of nodeTypeFlexibility and nodeType can be specified.
  optional string node_type = 8 [json_name = "node_type"];

  optional string project = 9;

  // Region where nodes using the node template will be created.
  //
  // If it is not provided, the provider region is used.
  optional string region = 10;

  optional string self_link = 11 [json_name = "self_link"];

  repeated Accelerators accelerators = 12;

  repeated Disks disks = 13;

  NodeTypeFlexibility node_type_flexibility = 14 [json_name = "node_type_flexibility"];

  ServerBinding server_binding = 15 [json_name = "server_binding"];

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message Accelerators {
    // The number of the guest accelerator cards exposed to this
    //
    // node template.
    optional int64 accelerator_count = 1 [json_name = "accelerator_count"];

    // Full or partial URL of the accelerator type resource to expose
    //
    // to this node template.
    optional string accelerator_type = 2 [json_name = "accelerator_type"];
  }

  message Disks {
    // Specifies the number of such disks.
    optional int64 disk_count = 1 [json_name = "disk_count"];

    // Specifies the size of the disk in base-2 GB.
    optional int64 disk_size_gb = 2 [json_name = "disk_size_gb"];

    // Specifies the desired disk type on the node. This disk type must be a local
    // storage type (e.g.: local-ssd). Note that for nodeTemplates, this should be the
    // name of the disk type and not its URL.
    optional string disk_type = 3 [json_name = "disk_type"];
  }

  message NodeTypeFlexibility {
    // Number of virtual CPUs to use.
    optional string cpus = 1;

    // Use local SSD
    optional string local_ssd = 2 [json_name = "local_ssd"];

    // Physical memory available to the node, defined in MB.
    optional string memory = 3;
  }

  message ServerBinding {
    // Type of server binding policy. If 'RESTART_NODE_ON_ANY_SERVER',
    //
    // nodes using this template will restart on any physical server
    //
    // following a maintenance event.
    //
    //
    // If 'RESTART_NODE_ON_MINIMAL_SERVER', nodes using this template
    //
    // will restart on the same physical server following a maintenance
    //
    // event, instead of being live migrated to or restarted on a new
    //
    // physical server. This option may be useful if you are using
    //
    // software licenses tied to the underlying server characteristics
    //
    // such as physical sockets or cores, to avoid the need for
    //
    // additional licenses when maintenance occurs. However, VMs on such
    //
    // nodes will experience outages while maintenance is applied. Possible values:
    // ["RESTART_NODE_ON_ANY_SERVER", "RESTART_NODE_ON_MINIMAL_SERVERS"]
    optional string type = 1;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeOrganizationSecurityPolicy version is 0
message GoogleComputeOrganizationSecurityPolicy {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // A textual description for the organization security policy.
  optional string description = 2;

  // User-provided name of the organization security policy. The name should be unique
  // in the organization in which the security policy is created. This should only be
  // used when SecurityPolicyType is FIREWALL.
  optional string display_name = 3 [json_name = "display_name"];

  // Fingerprint of this resource. This field is used internally during
  //
  // updates of this resource.
  optional string fingerprint = 4;

  optional string id = 5;

  // The parent of this OrganizationSecurityPolicy in the Cloud Resource Hierarchy.
  //
  // Format: organizations/{organization_id} or folders/{folder_id}
  optional string parent = 6;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string policy_id = 7 [json_name = "policy_id"];

  // User-provided name of the organization security policy. The name should be unique
  // in the organization in which the security policy is created. This should only be
  // used when SecurityPolicyType is CLOUD_ARMOR.
  optional string short_name = 8 [json_name = "short_name"];

  // The type indicates the intended use of the security policy. This field can be set
  // only at resource creation time.
  //
  //
  // **NOTE** : 'FIREWALL' type is deprecated and will be removed in a future major
  // release. Please use 'google_compute_firewall_policy' instead." Possible values:
  // ["FIREWALL", "CLOUD_ARMOR", "CLOUD_ARMOR_EDGE", "CLOUD_ARMOR_INTERNAL_SERVICE",
  // "CLOUD_ARMOR_NETWORK"]
  optional string type = 9;

  AdvancedOptionsConfig advanced_options_config = 10 [json_name = "advanced_options_config"];

  repeated Timeouts timeouts = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  optional int32 count = 14;

  optional string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message AdvancedOptionsConfig {
    // JSON body parsing. Supported values include: "DISABLED", "STANDARD",
    // "STANDARD_WITH_GRAPHQL". Possible values: ["DISABLED", "STANDARD",
    // "STANDARD_WITH_GRAPHQL"]
    optional string json_parsing = 1 [json_name = "json_parsing"];

    // Logging level. Supported values include: "NORMAL", "VERBOSE". Possible values:
    // ["NORMAL", "VERBOSE"]
    optional string log_level = 2 [json_name = "log_level"];

    // The maximum request size chosen by the customer with Waf enabled. Values
    // supported are "8KB", "16KB", "32KB", "48KB" and "64KB".
    //
    // Values are case insensitive. Possible values: ["8KB", "16KB", "32KB", "48KB",
    // "64KB"]
    optional string request_body_inspection_size = 3 [json_name = "request_body_inspection_size"];

    // An optional list of case-insensitive request header names to use for resolving
    // the client source IP address.
    repeated string user_ip_request_headers = 4 [json_name = "user_ip_request_headers"];

    JsonCustomConfig json_custom_config = 5 [json_name = "json_custom_config"];

    message JsonCustomConfig {
      // A list of content types to be parsed as JSON.
      repeated string content_types = 1 [json_name = "content_types"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeOrganizationSecurityPolicyAssociation version is 0
message GoogleComputeOrganizationSecurityPolicyAssociation {
  // The resource that the security policy is attached to.
  optional string attachment_id = 1 [json_name = "attachment_id"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // The display name of the security policy of the association.
  optional string display_name = 3 [json_name = "display_name"];

  // A list of folders to exclude from the security policy.
  repeated string excluded_folders = 4 [json_name = "excluded_folders"];

  // A list of projects to exclude from the security policy.
  repeated string excluded_projects = 5 [json_name = "excluded_projects"];

  optional string id = 6;

  // The name for an association.
  optional string name = 7;

  // The security policy ID of the association.
  optional string policy_id = 8 [json_name = "policy_id"];

  repeated Timeouts timeouts = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  optional int32 count = 12;

  optional string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeOrganizationSecurityPolicyRule version is 0
message GoogleComputeOrganizationSecurityPolicyRule {
  // The Action to perform when the client connection triggers the rule. Valid actions
  // are:
  //
  // "allow": allow access to target.
  //
  // "deny": deny access to target.
  //
  // "goto_next": forward the request to the next hierarchical policy for evaluation.
  //
  // "redirect": redirect to a different target. Parameters for this action can be
  // configured via redirectOptions. Only EXTERNAL_302 redirect type is supported for
  // organization security policies.
  optional string action = 1;

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // A description of the rule.
  optional string description = 3;

  optional string id = 4;

  // The ID of the OrganizationSecurityPolicy this rule applies to.
  optional string policy_id = 5 [json_name = "policy_id"];

  // If set to true, the specified action is not enforced.
  optional bool preview = 6;

  // An integer indicating the priority of a rule in the list. The priority must be a
  // value
  //
  // between 0 and 2147483647. Rules are evaluated from highest to lowest priority
  // where 0 is the
  //
  // highest priority and 2147483647 is the lowest prority.
  optional int64 priority = 7;

  HeaderAction header_action = 8 [json_name = "header_action"];

  Match match = 9;

  PreconfiguredWafConfig preconfigured_waf_config = 10 [json_name = "preconfigured_waf_config"];

  RedirectOptions redirect_options = 11 [json_name = "redirect_options"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message HeaderAction {
    repeated RequestHeadersToAdds request_headers_to_adds = 1 [json_name = "request_headers_to_adds"];

    message RequestHeadersToAdds {
      // The name of the header to set.
      optional string header_name = 1 [json_name = "header_name"];

      // The value to set the named header to.
      optional string header_value = 2 [json_name = "header_value"];
    }
  }

  message Match {
    // A description of the rule.
    optional string description = 1;

    // Preconfigured versioned expression. For organization security policy rules,
    //
    // the only supported type is "SRC_IPS_V1".
    //
    // **NOTE** : 'FIREWALL' type is deprecated. Please use
    // 'google_compute_firewall_policy_rule' resource instead.
    optional string versioned_expr = 2 [json_name = "versioned_expr"];

    Config config = 3;

    Expr expr = 4;

    message Config {
      // Source IP address range in CIDR format. Required for
      //
      // INGRESS rules.
      repeated string src_ip_ranges = 1 [json_name = "src_ip_ranges"];
    }

    message Expr {
      // Textual representation of an expression in Common Expression Language syntax. The
      // application context of the containing message determines which well-known feature
      // set of CEL is supported.
      optional string expression = 1;
    }
  }

  message PreconfiguredWafConfig {
    repeated Exclusion exclusion = 1;

    message Exclusion {
      // A list of target rule IDs under the WAF rule set to apply the preconfigured WAF
      // exclusion.
      //
      // If omitted, it refers to all the rule IDs under the WAF rule set.
      repeated string target_rule_ids = 1 [json_name = "target_rule_ids"];

      // Target WAF rule set to apply the preconfigured WAF exclusion.
      optional string target_rule_set = 2 [json_name = "target_rule_set"];

      repeated RequestCookie request_cookie = 3 [json_name = "request_cookie"];

      repeated RequestHeader request_header = 4 [json_name = "request_header"];

      repeated RequestQueryParam request_query_param = 5 [json_name = "request_query_param"];

      repeated RequestUri request_uri = 6 [json_name = "request_uri"];

      message RequestCookie {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestHeader {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestQueryParam {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestUri {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }
    }
  }

  message RedirectOptions {
    // Target for the redirect action. This is required if the type is EXTERNAL_302.
    optional string target = 1;

    // Type of the redirect action. For organization security policies, only
    // EXTERNAL_302 is supported.
    optional string type = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputePacketMirroring version is 0
message GoogleComputePacketMirroring {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // A human-readable description of the rule.
  optional string description = 2;

  // Indicates whether or not this packet mirroring takes effect. If set to FALSE,
  // this packet mirroring
  //
  // policy will not be enforced on the network. The default is TRUE. Possible values:
  // ["TRUE", "FALSE"]
  optional string enable = 3;

  optional string id = 4;

  // The name of the packet mirroring rule
  optional string name = 5;

  // Since only one rule can be active at a time, priority is
  //
  // used to break ties in the case of two rules that apply to
  //
  // the same instances.
  optional int64 priority = 6;

  optional string project = 7;

  // The Region in which the created address should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 8;

  CollectorIlb collector_ilb = 9 [json_name = "collector_ilb"];

  Filter filter = 10;

  MirroredResources mirrored_resources = 11 [json_name = "mirrored_resources"];

  Network network = 12;

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message CollectorIlb {
    // The URL of the forwarding rule.
    optional string url = 1;
  }

  message Filter {
    // IP CIDR ranges that apply as a filter on the source (ingress) or
    //
    // destination (egress) IP in the IP header. Only IPv4 is supported.
    repeated string cidr_ranges = 1 [json_name = "cidr_ranges"];

    // Direction of traffic to mirror. Default value: "BOTH" Possible values:
    // ["INGRESS", "EGRESS", "BOTH"]
    optional string direction = 2;

    // Possible IP protocols including tcp, udp, icmp and esp
    repeated string ip_protocols = 3 [json_name = "ip_protocols"];
  }

  message MirroredResources {
    // All instances with these tags will be mirrored.
    repeated string tags = 1;

    repeated Instances instances = 2;

    repeated Subnetworks subnetworks = 3;

    message Instances {
      // The URL of the instances where this rule should be active.
      optional string url = 1;
    }

    message Subnetworks {
      // The URL of the subnetwork where this rule should be active.
      optional string url = 1;
    }
  }

  message Network {
    // The full self_link URL of the network where this rule is active.
    optional string url = 1;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputePerInstanceConfig version is 0
message GoogleComputePerInstanceConfig {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The instance group manager this instance config is part of.
  optional string instance_group_manager = 3 [json_name = "instance_group_manager"];

  // The minimal action to perform on the instance during an update.
  //
  // Default is 'NONE'. Possible values are:
  //
  // * REPLACE
  //
  // * RESTART
  //
  // * REFRESH
  //
  // * NONE
  optional string minimal_action = 4 [json_name = "minimal_action"];

  // The most disruptive action to perform on the instance during an update.
  //
  // Default is 'REPLACE'. Possible values are:
  //
  // * REPLACE
  //
  // * RESTART
  //
  // * REFRESH
  //
  // * NONE
  optional string most_disruptive_allowed_action = 5 [json_name = "most_disruptive_allowed_action"];

  // The name for this per-instance config and its corresponding instance.
  optional string name = 6;

  optional string project = 7;

  // When true, deleting this config will immediately remove the underlying instance.
  //
  // When false, deleting this config will use the behavior as determined by
  // remove_instance_on_destroy.
  optional bool remove_instance_on_destroy = 8 [json_name = "remove_instance_on_destroy"];

  // When true, deleting this config will immediately remove any specified state from
  // the underlying instance.
  //
  // When false, deleting this config will *not* immediately remove any state from the
  // underlying instance.
  //
  // State will be removed on the next instance recreation or update.
  optional bool remove_instance_state_on_destroy = 9 [json_name = "remove_instance_state_on_destroy"];

  // Zone where the containing instance group manager is located
  optional string zone = 10;

  PreservedState preserved_state = 11 [json_name = "preserved_state"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message PreservedState {
    // Preserved metadata defined for this instance. This is a list of key->value pairs.
    map<string, string> metadata = 1;

    repeated Disk disk = 2;

    repeated ExternalIp external_ip = 3 [json_name = "external_ip"];

    repeated InternalIp internal_ip = 4 [json_name = "internal_ip"];

    message Disk {
      // A value that prescribes what should happen to the stateful disk when the VM
      // instance is deleted.
      //
      // The available options are 'NEVER' and 'ON_PERMANENT_INSTANCE_DELETION'.
      //
      // 'NEVER' - detach the disk when the VM is deleted, but do not delete the disk.
      //
      // 'ON_PERMANENT_INSTANCE_DELETION' will delete the stateful disk when the VM is
      // permanently
      //
      // deleted from the instance group. Default value: "NEVER" Possible values:
      // ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      optional string delete_rule = 1 [json_name = "delete_rule"];

      // A unique device name that is reflected into the /dev/ tree of a Linux operating
      // system running within the instance.
      optional string device_name = 2 [json_name = "device_name"];

      // The mode of the disk. Default value: "READ_WRITE" Possible values: ["READ_ONLY",
      // "READ_WRITE"]
      optional string mode = 3;

      // The URI of an existing persistent disk to attach under the specified device-name
      // in the format
      //
      // 'projects/project-id/zones/zone/disks/disk-name'.
      optional string source = 4;
    }

    message ExternalIp {
      // These stateful IPs will never be released during autohealing, update or VM
      // instance recreate operations. This flag is used to configure if the IP
      // reservation should be deleted after it is no longer used by the group, e.g. when
      // the given instance or the whole group is deleted. Default value: "NEVER" Possible
      // values: ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      optional string auto_delete = 1 [json_name = "auto_delete"];

      optional string interface_name = 2 [json_name = "interface_name"];

      IpAddress ip_address = 3 [json_name = "ip_address"];

      message IpAddress {
        // The URL of the reservation for this IP address.
        optional string address = 1;
      }
    }

    message InternalIp {
      // These stateful IPs will never be released during autohealing, update or VM
      // instance recreate operations. This flag is used to configure if the IP
      // reservation should be deleted after it is no longer used by the group, e.g. when
      // the given instance or the whole group is deleted. Default value: "NEVER" Possible
      // values: ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      optional string auto_delete = 1 [json_name = "auto_delete"];

      optional string interface_name = 2 [json_name = "interface_name"];

      IpAddress ip_address = 3 [json_name = "ip_address"];

      message IpAddress {
        // The URL of the reservation for this IP address.
        optional string address = 1;
      }
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputePreviewFeature version is 0
message GoogleComputePreviewFeature {
  // The activation status of the preview feature. Possible values: ["ENABLED",
  // "ACTIVATION_STATE_UNSPECIFIED"]
  optional string activation_status = 1 [json_name = "activation_status"];

  optional string id = 2;

  // The name of the preview feature.
  optional string name = 3;

  optional string project = 4;

  RolloutOperation rollout_operation = 5 [json_name = "rollout_operation"];

  repeated Timeouts timeouts = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message RolloutOperation {
    RolloutInput rollout_input = 1 [json_name = "rollout_input"];

    message RolloutInput {
      // Predefined rollout plans. Possible values: ["ROLLOUT_PLAN_FAST_ROLLOUT"]
      optional string predefined_rollout_plan = 1 [json_name = "predefined_rollout_plan"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeProjectCloudArmorTier version is 0
message GoogleComputeProjectCloudArmorTier {
  // Managed protection tier to be set. Possible values: ["CA_STANDARD",
  // "CA_ENTERPRISE_PAYGO", "CA_ENTERPRISE_ANNUAL"]
  optional string cloud_armor_tier = 1 [json_name = "cloud_armor_tier"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  optional string id = 3;

  optional string project = 4;

  repeated Timeouts timeouts = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeProjectDefaultNetworkTier version is 0
message GoogleComputeProjectDefaultNetworkTier {
  optional string id = 1;

  // The default network tier to be configured for the project. This field can take
  // the following values: PREMIUM or STANDARD.
  optional string network_tier = 2 [json_name = "network_tier"];

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 3;

  repeated Timeouts timeouts = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  optional int32 count = 7;

  optional string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Timeouts {
    optional string create = 1;
  }
}

// GoogleComputeProjectMetadata version is 0
message GoogleComputeProjectMetadata {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // A series of key value pairs.
  map<string, string> metadata = 3;

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 4;

  repeated Timeouts timeouts = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeProjectMetadataItem version is 0
message GoogleComputeProjectMetadataItem {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The metadata key to set.
  optional string key = 3;

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 4;

  // The value to set for the given metadata key.
  optional string value = 5;

  repeated Timeouts timeouts = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputePublicAdvertisedPrefix version is 0
message GoogleComputePublicAdvertisedPrefix {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 2;

  // The IPv4 address to be used for reverse DNS verification.
  optional string dns_verification_ip = 3 [json_name = "dns_verification_ip"];

  optional string id = 4;

  // The address range, in CIDR format, represented by this public advertised prefix.
  optional string ip_cidr_range = 5 [json_name = "ip_cidr_range"];

  // The internet access type for IPv6 Public Advertised Prefixes. It can be
  //
  // set to one of following:
  //
  // * EXTERNAL: Default access type. The prefix will be announced to the
  //
  // internet. All children PDPs will have access type as EXTERNAL.
  //
  // * INTERNAL: The prefix won’t be announced to the internet. Prefix will
  //
  // be used privately within Google Cloud. All children PDPs will have
  //
  // access type as INTERNAL. Possible values: ["EXTERNAL", "INTERNAL"]
  optional string ipv6_access_type = 6 [json_name = "ipv6_access_type"];

  // Name of the resource. The name must be 1-63 characters long, and
  //
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  //
  // which means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 7;

  // Specifies how child public delegated prefix will be scoped. pdpScope
  //
  // must be one of: GLOBAL, REGIONAL
  //
  // * REGIONAL: The public delegated prefix is regional only. The
  //
  // provisioning will take a few minutes.
  //
  // * GLOBAL: The public delegated prefix is global only. The provisioning
  //
  // will take ~4 weeks. Possible values: ["GLOBAL", "REGIONAL"]
  optional string pdp_scope = 8 [json_name = "pdp_scope"];

  optional string project = 9;

  optional string self_link = 10 [json_name = "self_link"];

  // Output Only. The shared secret to be used for reverse DNS verification.
  optional string shared_secret = 11 [json_name = "shared_secret"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputePublicDelegatedPrefix version is 0
message GoogleComputePublicDelegatedPrefix {
  // The allocatable prefix length supported by this public delegated prefix. This
  // field is optional and cannot be set for prefixes in DELEGATION mode. It cannot be
  // set for IPv4 prefixes either, and it always defaults to 32.
  optional int64 allocatable_prefix_length = 1 [json_name = "allocatable_prefix_length"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Whether this PublicDelegatedPrefix supports enhanced IPv4 allocations.
  //
  // Applicable for IPv4 PDPs only.
  optional bool enable_enhanced_ipv4_allocation = 4 [json_name = "enable_enhanced_ipv4_allocation"];

  optional string id = 5;

  // The IP address range, in CIDR format, represented by this public delegated
  // prefix.
  optional string ip_cidr_range = 6 [json_name = "ip_cidr_range"];

  // The internet access type for IPv6 Public Delegated Prefixes. Inherited
  //
  // from parent prefix and can be one of following:
  //
  // * EXTERNAL: The prefix will be announced to the internet. All children
  //
  // PDPs will have access type as EXTERNAL.
  //
  // * INTERNAL: The prefix won’t be announced to the internet. Prefix will
  //
  // be used privately within Google Cloud. All children PDPs will have
  //
  // access type as INTERNAL.
  optional string ipv6_access_type = 7 [json_name = "ipv6_access_type"];

  // If true, the prefix will be live migrated.
  optional bool is_live_migration = 8 [json_name = "is_live_migration"];

  // Specifies the mode of this IPv6 PDP. MODE must be one of:
  //
  // * DELEGATION
  //
  // * EXTERNAL_IPV6_FORWARDING_RULE_CREATION
  //
  // * EXTERNAL_IPV6_SUBNETWORK_CREATION
  //
  // * INTERNAL_IPV6_SUBNETWORK_CREATION Possible values: ["DELEGATION",
  // "EXTERNAL_IPV6_FORWARDING_RULE_CREATION", "EXTERNAL_IPV6_SUBNETWORK_CREATION",
  // "INTERNAL_IPV6_SUBNETWORK_CREATION"]
  optional string mode = 9;

  // Name of the resource. The name must be 1-63 characters long, and
  //
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  //
  // which means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 10;

  // The URL of parent prefix. Either PublicAdvertisedPrefix or PublicDelegatedPrefix.
  optional string parent_prefix = 11 [json_name = "parent_prefix"];

  optional string project = 12;

  // List of sub public delegated fixes for BYO IP functionality.
  //
  // Each item in this array represents a sub prefix that can be
  //
  // used to create addresses or further allocations.
  repeated PublicDelegatedSubPrefixs public_delegated_sub_prefixs = 13 [json_name = "public_delegated_sub_prefixs"];

  // A region where the prefix will reside.
  optional string region = 14;

  optional string self_link = 15 [json_name = "self_link"];

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message PublicDelegatedSubPrefixs {
    // allocatable_prefix_length: number
    optional int64 allocatable_prefix_length = 1 [json_name = "allocatable_prefix_length"];

    // delegatee_project: string
    optional string delegatee_project = 2 [json_name = "delegatee_project"];

    // description: string
    optional string description = 3;

    // enable_enhanced_ipv4_allocation: bool
    optional bool enable_enhanced_ipv4_allocation = 4 [json_name = "enable_enhanced_ipv4_allocation"];

    // ip_cidr_range: string
    optional string ip_cidr_range = 5 [json_name = "ip_cidr_range"];

    // ipv6_access_type: string
    optional string ipv6_access_type = 6 [json_name = "ipv6_access_type"];

    // is_address: bool
    optional bool is_address = 7 [json_name = "is_address"];

    // mode: string
    optional string mode = 8;

    // name: string
    optional string name = 9;

    // region: string
    optional string region = 10;

    // status: string
    optional string status = 11;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionAutoscaler version is 0
message GoogleComputeRegionAutoscaler {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  optional string id = 4;

  // Name of the resource. The name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 5;

  optional string project = 6;

  // URL of the region where the instance group resides.
  optional string region = 7;

  optional string self_link = 8 [json_name = "self_link"];

  // URL of the managed instance group that this autoscaler will scale.
  optional string target = 9;

  AutoscalingPolicy autoscaling_policy = 10 [json_name = "autoscaling_policy"];

  repeated Timeouts timeouts = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  optional int32 count = 14;

  optional string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message AutoscalingPolicy {
    // The number of seconds that the autoscaler should wait before it
    //
    // starts collecting information from a new instance. This prevents
    //
    // the autoscaler from collecting information when the instance is
    //
    // initializing, during which the collected usage would not be
    //
    // reliable. The default time autoscaler waits is 60 seconds.
    //
    //
    // Virtual machine initialization times might vary because of
    //
    // numerous factors. We recommend that you test how long an
    //
    // instance may take to initialize. To do this, create an instance
    //
    // and time the startup process.
    optional int64 cooldown_period = 1 [json_name = "cooldown_period"];

    // The maximum number of instances that the autoscaler can scale up
    //
    // to. This is required when creating or updating an autoscaler. The
    //
    // maximum number of replicas should not be lower than minimal number
    //
    // of replicas.
    optional int64 max_replicas = 2 [json_name = "max_replicas"];

    // The minimum number of replicas that the autoscaler can scale down
    //
    // to. This cannot be less than 0. If not provided, autoscaler will
    //
    // choose a default value depending on maximum number of instances
    //
    // allowed.
    optional int64 min_replicas = 3 [json_name = "min_replicas"];

    // Defines operating mode for this policy.
    optional string mode = 4;

    // The number of seconds that the autoscaler waits for load stabilization
    //
    // before making scale-in decisions.
    //
    //
    // This might appear as a delay in scaling in but it is an important mechanism
    //
    // for your application to not have fluctuating size due to short term load
    //
    // fluctuations.
    optional int64 stabilization_period = 5 [json_name = "stabilization_period"];

    CpuUtilization cpu_utilization = 6 [json_name = "cpu_utilization"];

    LoadBalancingUtilization load_balancing_utilization = 7 [json_name = "load_balancing_utilization"];

    repeated Metric metric = 8;

    ScaleInControl scale_in_control = 9 [json_name = "scale_in_control"];

    repeated ScalingSchedules scaling_schedules = 10 [json_name = "scaling_schedules"];

    message CpuUtilization {
      // Indicates whether predictive autoscaling based on CPU metric is enabled. Valid
      // values are:
      //
      //
      // - NONE (default). No predictive method is used. The autoscaler scales the group
      // to meet current demand based on real-time metrics.
      //
      //
      // - OPTIMIZE_AVAILABILITY. Predictive autoscaling improves availability by
      // monitoring daily and weekly load patterns and scaling out ahead of anticipated
      // demand.
      optional string predictive_method = 1 [json_name = "predictive_method"];

      // The target CPU utilization that the autoscaler should maintain.
      //
      // Must be a float value in the range (0, 1]. If not specified, the
      //
      // default is 0.6.
      //
      //
      // If the CPU level is below the target utilization, the autoscaler
      //
      // scales down the number of instances until it reaches the minimum
      //
      // number of instances you specified or until the average CPU of
      //
      // your instances reaches the target utilization.
      //
      //
      // If the average CPU is above the target utilization, the autoscaler
      //
      // scales up until it reaches the maximum number of instances you
      //
      // specified or until the average utilization reaches the target
      //
      // utilization.
      optional int64 target = 2;
    }

    message LoadBalancingUtilization {
      // Fraction of backend capacity utilization (set in HTTP(s) load
      //
      // balancing configuration) that autoscaler should maintain. Must
      //
      // be a positive float value. If not defined, the default is 0.8.
      optional int64 target = 1;
    }

    message Metric {
      // A filter string to be used as the filter string for
      //
      // a Stackdriver Monitoring TimeSeries.list API call.
      //
      // This filter is used to select a specific TimeSeries for
      //
      // the purpose of autoscaling and to determine whether the metric
      //
      // is exporting per-instance or per-group data.
      //
      //
      // You can only use the AND operator for joining selectors.
      //
      // You can only use direct equality comparison operator (=) without
      //
      // any functions for each selector.
      //
      // You can specify the metric in both the filter string and in the
      //
      // metric field. However, if specified in both places, the metric must
      //
      // be identical.
      //
      //
      // The monitored resource type determines what kind of values are
      //
      // expected for the metric. If it is a gce_instance, the autoscaler
      //
      // expects the metric to include a separate TimeSeries for each
      //
      // instance in a group. In such a case, you cannot filter on resource
      //
      // labels.
      //
      //
      // If the resource type is any other value, the autoscaler expects
      //
      // this metric to contain values that apply to the entire autoscaled
      //
      // instance group and resource label filtering can be performed to
      //
      // point autoscaler at the correct TimeSeries to scale upon.
      //
      // This is called a per-group metric for the purpose of autoscaling.
      //
      //
      // If not specified, the type defaults to gce_instance.
      //
      //
      // You should provide a filter that is selective enough to pick just
      //
      // one TimeSeries for the autoscaled group or for each of the instances
      //
      // (if you are using gce_instance resource type). If multiple
      //
      // TimeSeries are returned upon the query execution, the autoscaler
      //
      // will sum their respective values to obtain its scaling value.
      optional string filter = 1;

      // The identifier (type) of the Stackdriver Monitoring metric.
      //
      // The metric cannot have negative values.
      //
      //
      // The metric must have a value type of INT64 or DOUBLE.
      optional string name = 2;

      // If scaling is based on a per-group metric value that represents the
      //
      // total amount of work to be done or resource usage, set this value to
      //
      // an amount assigned for a single instance of the scaled group.
      //
      // The autoscaler will keep the number of instances proportional to the
      //
      // value of this metric, the metric itself should not change value due
      //
      // to group resizing.
      //
      //
      // For example, a good metric to use with the target is
      //
      // 'pubsub.googleapis.com/subscription/num_undelivered_messages'
      //
      // or a custom metric exporting the total number of requests coming to
      //
      // your instances.
      //
      //
      // A bad example would be a metric exporting an average or median
      //
      // latency, since this value can't include a chunk assignable to a
      //
      // single instance, it could be better used with utilization_target
      //
      // instead.
      optional int64 single_instance_assignment = 3 [json_name = "single_instance_assignment"];

      // The target value of the metric that autoscaler should
      //
      // maintain. This must be a positive value. A utilization
      //
      // metric scales number of virtual machines handling requests
      //
      // to increase or decrease proportionally to the metric.
      //
      //
      // For example, a good metric to use as a utilizationTarget is
      //
      // www.googleapis.com/compute/instance/network/received_bytes_count.
      //
      // The autoscaler will work to keep this value constant for each
      //
      // of the instances.
      optional int64 target = 4;

      // Defines how target utilization value is expressed for a
      //
      // Stackdriver Monitoring metric. Possible values: ["GAUGE", "DELTA_PER_SECOND",
      // "DELTA_PER_MINUTE"]
      optional string type = 5;
    }

    message ScaleInControl {
      // How long back autoscaling should look when computing recommendations
      //
      // to include directives regarding slower scale down, as described above.
      optional int64 time_window_sec = 1 [json_name = "time_window_sec"];

      MaxScaledInReplicas max_scaled_in_replicas = 2 [json_name = "max_scaled_in_replicas"];

      message MaxScaledInReplicas {
        // Specifies a fixed number of VM instances. This must be a positive
        //
        // integer.
        optional int64 fixed = 1;

        // Specifies a percentage of instances between 0 to 100%, inclusive.
        //
        // For example, specify 80 for 80%.
        optional int64 percent = 2;
      }
    }

    message ScalingSchedules {
      // A description of a scaling schedule.
      optional string description = 1;

      // A boolean value that specifies if a scaling schedule can influence autoscaler
      // recommendations. If set to true, then a scaling schedule has no effect.
      optional bool disabled = 2;

      // The duration of time intervals (in seconds) for which this scaling schedule will
      // be running. The minimum allowed value is 300.
      optional int64 duration_sec = 3 [json_name = "duration_sec"];

      // Minimum number of VM instances that autoscaler will recommend in time intervals
      // starting according to schedule.
      optional int64 min_required_replicas = 4 [json_name = "min_required_replicas"];

      optional string name = 5;

      // The start timestamps of time intervals when this scaling schedule should provide
      // a scaling signal. This field uses the extended cron format (with an optional year
      // field).
      optional string schedule = 6;

      // The time zone to be used when interpreting the schedule. The value of this field
      // must be a time zone name from the tz database:
      // http://en.wikipedia.org/wiki/Tz_database.
      optional string time_zone = 7 [json_name = "time_zone"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionBackendService version is 1
message GoogleComputeRegionBackendService {
  // Lifetime of cookies in seconds if session_affinity is
  //
  // GENERATED_COOKIE. If set to 0, the cookie is non-persistent and lasts
  //
  // only until the end of the browser session (or equivalent). The
  //
  // maximum allowed value for TTL is one day.
  //
  //
  // When the load balancing scheme is INTERNAL, this field is not used.
  optional int64 affinity_cookie_ttl_sec = 1 [json_name = "affinity_cookie_ttl_sec"];

  // Time for which instance will be drained (not accept new
  //
  // connections, but still work to finish started).
  optional int64 connection_draining_timeout_sec = 2 [json_name = "connection_draining_timeout_sec"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 5;

  // If true, enable Cloud CDN for this RegionBackendService.
  optional bool enable_cdn = 6 [json_name = "enable_cdn"];

  // Fingerprint of this resource. A hash of the contents stored in this
  //
  // object. This field is used in optimistic locking.
  optional string fingerprint = 7;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional int64 generated_id = 8 [json_name = "generated_id"];

  // The set of URLs to HealthCheck resources for health checking
  //
  // this RegionBackendService. Currently at most one health
  //
  // check can be specified.
  //
  //
  // A health check must be specified unless the backend service uses an internet
  //
  // or serverless NEG as a backend.
  repeated string health_checks = 9 [json_name = "health_checks"];

  optional string id = 10;

  // Specifies preference of traffic to the backend (from the proxy and from the
  // client for proxyless gRPC). Possible values: ["IPV4_ONLY", "PREFER_IPV6",
  // "IPV6_ONLY"]
  optional string ip_address_selection_policy = 11 [json_name = "ip_address_selection_policy"];

  // Indicates what kind of load balancing this regional backend service
  //
  // will be used for. A backend service created for one type of load
  //
  // balancing cannot be used with the other(s). For more information, refer to
  //
  // [Choosing a load
  // balancer](https://cloud.google.com/load-balancing/docs/backend-service). Default
  // value: "INTERNAL" Possible values: ["EXTERNAL", "EXTERNAL_MANAGED", "INTERNAL",
  // "INTERNAL_MANAGED", "INTERNAL_SELF_MANAGED"]
  optional string load_balancing_scheme = 12 [json_name = "load_balancing_scheme"];

  // The load balancing algorithm used within the scope of the locality.
  //
  // The possible values are:
  //
  //
  // * 'ROUND_ROBIN': This is a simple policy in which each healthy backend
  //
  // is selected in round robin order.
  //
  //
  // * 'LEAST_REQUEST': An O(1) algorithm which selects two random healthy
  //
  // hosts and picks the host which has fewer active requests.
  //
  //
  // * 'RING_HASH': The ring/modulo hash load balancer implements consistent
  //
  // hashing to backends. The algorithm has the property that the
  //
  // addition/removal of a host from a set of N hosts only affects
  //
  // 1/N of the requests.
  //
  //
  // * 'RANDOM': The load balancer selects a random healthy host.
  //
  //
  // * 'ORIGINAL_DESTINATION': Backend host is selected based on the client
  //
  // connection metadata, i.e., connections are opened
  //
  // to the same address as the destination address of
  //
  // the incoming connection before the connection
  //
  // was redirected to the load balancer.
  //
  //
  // * 'MAGLEV': used as a drop in replacement for the ring hash load balancer.
  //
  // Maglev is not as stable as ring hash but has faster table lookup
  //
  // build times and host selection times. For more information about
  //
  // Maglev, refer to https://ai.google/research/pubs/pub44824
  //
  //
  // * 'WEIGHTED_MAGLEV': Per-instance weighted Load Balancing via health check
  //
  // reported weights. Only applicable to loadBalancingScheme
  //
  // EXTERNAL. If set, the Backend Service must
  //
  // configure a non legacy HTTP-based Health Check, and
  //
  // health check replies are expected to contain
  //
  // non-standard HTTP response header field
  //
  // X-Load-Balancing-Endpoint-Weight to specify the
  //
  // per-instance weights. If set, Load Balancing is weight
  //
  // based on the per-instance weights reported in the last
  //
  // processed health check replies, as long as every
  //
  // instance either reported a valid weight or had
  //
  // UNAVAILABLE_WEIGHT. Otherwise, Load Balancing remains
  //
  // equal-weight.
  //
  //
  // * 'WEIGHTED_ROUND_ROBIN': Per-endpoint weighted round-robin Load Balancing using
  // weights computed
  //
  // from Backend reported Custom Metrics. If set, the Backend Service
  //
  // responses are expected to contain non-standard HTTP response header field
  //
  // X-Endpoint-Load-Metrics. The reported metrics
  //
  // to use for computing the weights are specified via the
  //
  // backends[].customMetrics fields.
  //
  //
  // locality_lb_policy is applicable to either:
  //
  //
  // * A regional backend service with the service_protocol set to HTTP, HTTPS, HTTP2
  // or H2C,
  //
  // and loadBalancingScheme set to INTERNAL_MANAGED.
  //
  // * A global backend service with the load_balancing_scheme set to
  // INTERNAL_SELF_MANAGED.
  //
  // * A regional backend service with loadBalancingScheme set to EXTERNAL (External
  // Network
  //
  // Load Balancing). Only MAGLEV and WEIGHTED_MAGLEV values are possible for External
  //
  // Network Load Balancing. The default is MAGLEV.
  //
  //
  // If session_affinity is not NONE, and locality_lb_policy is not set to MAGLEV,
  // WEIGHTED_MAGLEV,
  //
  // or RING_HASH, session affinity settings will not take effect.
  //
  //
  // Only ROUND_ROBIN and RING_HASH are supported when the backend service is
  // referenced
  //
  // by a URL map that is bound to target gRPC proxy that has validate_for_proxyless
  //
  // field set to true. Possible values: ["ROUND_ROBIN", "LEAST_REQUEST", "RING_HASH",
  // "RANDOM", "ORIGINAL_DESTINATION", "MAGLEV", "WEIGHTED_MAGLEV",
  // "WEIGHTED_ROUND_ROBIN"]
  optional string locality_lb_policy = 13 [json_name = "locality_lb_policy"];

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 14;

  // The URL of the network to which this backend service belongs.
  //
  // This field must be set for Internal Passthrough Network Load Balancers when the
  // haPolicy is enabled, and for External Passthrough Network Load Balancers when the
  // haPolicy fastIpMove is enabled.
  //
  // This field can only be specified when the load balancing scheme is set to
  // INTERNAL, or when the load balancing scheme is set to EXTERNAL and haPolicy
  // fastIpMove is enabled.
  //
  // Changes to this field force recreation of the resource.
  optional string network = 15;

  // A named port on a backend instance group representing the port for
  //
  // communication to the backend VMs in that group. Required when the
  //
  // loadBalancingScheme is EXTERNAL, EXTERNAL_MANAGED, INTERNAL_MANAGED, or
  // INTERNAL_SELF_MANAGED
  //
  // and the backends are instance groups. The named port must be defined on each
  //
  // backend instance group. This parameter has no meaning if the backends are NEGs.
  // API sets a
  //
  // default of "http" if not given.
  //
  // Must be omitted when the loadBalancingScheme is INTERNAL (Internal TCP/UDP Load
  // Balancing).
  optional string port_name = 16 [json_name = "port_name"];

  optional string project = 17;

  // The protocol this BackendService uses to communicate with backends.
  //
  // The default is HTTP. Possible values are HTTP, HTTPS, HTTP2, H2C, TCP, SSL, UDP
  //
  // or GRPC. Refer to the documentation for the load balancers or for Traffic
  // Director
  //
  // for more information. Possible values: ["HTTP", "HTTPS", "HTTP2", "TCP", "SSL",
  // "UDP", "GRPC", "UNSPECIFIED", "H2C"]
  optional string protocol = 18;

  // The Region in which the created backend service should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 19;

  // The security policy associated with this backend service.
  optional string security_policy = 20 [json_name = "security_policy"];

  optional string self_link = 21 [json_name = "self_link"];

  // Type of session affinity to use. The default is NONE. Session affinity is
  //
  // not applicable if the protocol is UDP. Possible values: ["NONE", "CLIENT_IP",
  // "CLIENT_IP_PORT_PROTO", "CLIENT_IP_PROTO", "GENERATED_COOKIE", "HEADER_FIELD",
  // "HTTP_COOKIE", "CLIENT_IP_NO_DESTINATION", "STRONG_COOKIE_AFFINITY"]
  optional string session_affinity = 22 [json_name = "session_affinity"];

  // The backend service timeout has a different meaning depending on the type of load
  // balancer.
  //
  // For more information see, [Backend service
  // ttings](https://cloud.google.com/compute/docs/reference/rest/v1/backendServices).
  //
  // The default is 30 seconds.
  //
  // The full range of timeout values allowed goes from 1 through 2,147,483,647
  // seconds.
  optional int64 timeout_sec = 23 [json_name = "timeout_sec"];

  repeated Backend backend = 24;

  CdnPolicy cdn_policy = 25 [json_name = "cdn_policy"];

  CircuitBreakers circuit_breakers = 26 [json_name = "circuit_breakers"];

  ConnectionTrackingPolicy connection_tracking_policy = 27 [json_name = "connection_tracking_policy"];

  ConsistentHash consistent_hash = 28 [json_name = "consistent_hash"];

  repeated CustomMetrics custom_metrics = 29 [json_name = "custom_metrics"];

  FailoverPolicy failover_policy = 30 [json_name = "failover_policy"];

  HaPolicy ha_policy = 31 [json_name = "ha_policy"];

  Iap iap = 32;

  LogConfig log_config = 33 [json_name = "log_config"];

  NetworkPassThroughLbTrafficPolicy network_pass_through_lb_traffic_policy = 34 [
    json_name = "network_pass_through_lb_traffic_policy"
  ];

  OutlierDetection outlier_detection = 35 [json_name = "outlier_detection"];

  Params params = 36;

  StrongSessionAffinityCookie strong_session_affinity_cookie = 37 [json_name = "strong_session_affinity_cookie"];

  repeated Timeouts timeouts = 38;

  TlsSettings tls_settings = 39 [json_name = "tls_settings"];

  google.protobuf.Value for_each = 40 [json_name = "for_each"];

  repeated string depends_on = 41 [json_name = "depends_on"];

  optional int32 count = 42;

  optional string provider = 43;

  terraform.v1.Lifecycle lifecycle = 44;

  message Backend {
    // Specifies the balancing mode for this backend.
    //
    //
    // See the [Backend Services
    // iew](https://cloud.google.com/load-balancing/docs/backend-service#balancing-mode)
    //
    // for an explanation of load balancing modes. Default value: "UTILIZATION" Possible
    // values: ["UTILIZATION", "RATE", "CONNECTION", "CUSTOM_METRICS"]
    optional string balancing_mode = 1 [json_name = "balancing_mode"];

    // A multiplier applied to the group's maximum servicing capacity
    //
    // (based on UTILIZATION, RATE or CONNECTION).
    //
    //
    // ~>**NOTE**: This field cannot be set for
    //
    // INTERNAL region backend services (default loadBalancingScheme),
    //
    // but is required for non-INTERNAL backend service. The total
    //
    // capacity_scaler for all backends must be non-zero.
    //
    //
    // A setting of 0 means the group is completely drained, offering
    //
    // 0% of its available Capacity. Valid range is [0.0,1.0].
    optional int64 capacity_scaler = 2 [json_name = "capacity_scaler"];

    // An optional description of this resource.
    //
    // Provide this property when you create the resource.
    optional string description = 3;

    // This field designates whether this is a failover backend. More
    //
    // than one failover backend can be configured for a given RegionBackendService.
    optional bool failover = 4;

    // The fully-qualified URL of an Instance Group or Network Endpoint
    //
    // Group resource. In case of instance group this defines the list
    //
    // of instances that serve traffic. Member virtual machine
    //
    // instances from each instance group must live in the same zone as
    //
    // the instance group itself. No two backends in a backend service
    //
    // are allowed to use same Instance Group resource.
    //
    //
    // For Network Endpoint Groups this defines list of endpoints. All
    //
    // endpoints of Network Endpoint Group must be hosted on instances
    //
    // located in the same zone as the Network Endpoint Group.
    //
    //
    // Backend services cannot mix Instance Group and
    //
    // Network Endpoint Group backends.
    //
    //
    // When the 'load_balancing_scheme' is INTERNAL, only instance groups
    //
    // are supported.
    //
    //
    // Note that you must specify an Instance Group or Network Endpoint
    //
    // Group resource using the fully-qualified URL, rather than a
    //
    // partial URL.
    optional string group = 5;

    // The max number of simultaneous connections for the group. Can
    //
    // be used with either CONNECTION or UTILIZATION balancing modes.
    //
    // Cannot be set for INTERNAL backend services.
    //
    //
    // For CONNECTION mode, either maxConnections or one
    //
    // of maxConnectionsPerInstance or maxConnectionsPerEndpoint,
    //
    // as appropriate for group type, must be set.
    optional int64 max_connections = 6 [json_name = "max_connections"];

    // The max number of simultaneous connections that a single backend
    //
    // network endpoint can handle. Cannot be set
    //
    // for INTERNAL backend services.
    //
    //
    // This is used to calculate the capacity of the group. Can be
    //
    // used in either CONNECTION or UTILIZATION balancing modes. For
    //
    // CONNECTION mode, either maxConnections or
    //
    // maxConnectionsPerEndpoint must be set.
    optional int64 max_connections_per_endpoint = 7 [json_name = "max_connections_per_endpoint"];

    // The max number of simultaneous connections that a single
    //
    // backend instance can handle. Cannot be set for INTERNAL backend
    //
    // services.
    //
    //
    // This is used to calculate the capacity of the group.
    //
    // Can be used in either CONNECTION or UTILIZATION balancing modes.
    //
    // For CONNECTION mode, either maxConnections or
    //
    // maxConnectionsPerInstance must be set.
    optional int64 max_connections_per_instance = 8 [json_name = "max_connections_per_instance"];

    // The max requests per second (RPS) of the group. Cannot be set
    //
    // for INTERNAL backend services.
    //
    //
    // Can be used with either RATE or UTILIZATION balancing modes,
    //
    // but required if RATE mode. Either maxRate or one
    //
    // of maxRatePerInstance or maxRatePerEndpoint, as appropriate for
    //
    // group type, must be set.
    optional int64 max_rate = 9 [json_name = "max_rate"];

    // The max requests per second (RPS) that a single backend network
    //
    // endpoint can handle. This is used to calculate the capacity of
    //
    // the group. Can be used in either balancing mode. For RATE mode,
    //
    // either maxRate or maxRatePerEndpoint must be set. Cannot be set
    //
    // for INTERNAL backend services.
    optional int64 max_rate_per_endpoint = 10 [json_name = "max_rate_per_endpoint"];

    // The max requests per second (RPS) that a single backend
    //
    // instance can handle. This is used to calculate the capacity of
    //
    // the group. Can be used in either balancing mode. For RATE mode,
    //
    // either maxRate or maxRatePerInstance must be set. Cannot be set
    //
    // for INTERNAL backend services.
    optional int64 max_rate_per_instance = 11 [json_name = "max_rate_per_instance"];

    // Used when balancingMode is UTILIZATION. This ratio defines the
    //
    // CPU utilization target for the group. Valid range is [0.0, 1.0].
    //
    // Cannot be set for INTERNAL backend services.
    optional int64 max_utilization = 12 [json_name = "max_utilization"];

    repeated CustomMetrics custom_metrics = 13 [json_name = "custom_metrics"];

    message CustomMetrics {
      // If true, the metric data is collected and reported to Cloud
      //
      // Monitoring, but is not used for load balancing.
      optional bool dry_run = 1 [json_name = "dry_run"];

      // Optional parameter to define a target utilization for the Custom Metrics
      //
      // balancing mode. The valid range is <code>[0.0, 1.0]</code>.
      optional int64 max_utilization = 2 [json_name = "max_utilization"];

      // Name of a custom utilization signal. The name must be 1-64 characters
      //
      // long and match the regular expression [a-z]([-_.a-z0-9]*[a-z0-9])? which
      //
      // means the first character must be a lowercase letter, and all following
      //
      // characters must be a dash, period, underscore, lowercase letter, or
      //
      // digit, except the last character, which cannot be a dash, period, or
      //
      // underscore. For usage guidelines, see Custom Metrics balancing mode. This
      //
      // field can only be used for a global or regional backend service with the
      //
      // loadBalancingScheme set to <code>EXTERNAL_MANAGED</code>,
      //
      // <code>INTERNAL_MANAGED</code> <code>INTERNAL_SELF_MANAGED</code>.
      optional string name = 3;
    }
  }

  message CdnPolicy {
    // Specifies the cache setting for all responses from this backend.
    //
    // The possible values are: USE_ORIGIN_HEADERS, FORCE_CACHE_ALL and CACHE_ALL_STATIC
    // Possible values: ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
    optional string cache_mode = 1 [json_name = "cache_mode"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    optional int64 client_ttl = 2 [json_name = "client_ttl"];

    // Specifies the default TTL for cached content served by this origin for responses
    //
    // that do not have an existing valid TTL (max-age or s-max-age).
    optional int64 default_ttl = 3 [json_name = "default_ttl"];

    // Specifies the maximum allowed TTL for cached content served by this origin.
    optional int64 max_ttl = 4 [json_name = "max_ttl"];

    // Negative caching allows per-status code TTLs to be set, in order to apply
    // fine-grained caching for common errors or redirects.
    optional bool negative_caching = 5 [json_name = "negative_caching"];

    // Serve existing content from the cache (if available) when revalidating content
    // with the origin, or when an error is encountered when refreshing the cache.
    optional int64 serve_while_stale = 6 [json_name = "serve_while_stale"];

    // Maximum number of seconds the response to a signed URL request
    //
    // will be considered fresh, defaults to 1hr (3600s). After this
    //
    // time period, the response will be revalidated before
    //
    // being served.
    //
    //
    // When serving responses to signed URL requests, Cloud CDN will
    //
    // internally behave as though all responses from this backend had a
    //
    // "Cache-Control: public, max-age=[TTL]" header, regardless of any
    //
    // existing Cache-Control header. The actual headers served in
    //
    // responses will not be altered.
    optional int64 signed_url_cache_max_age_sec = 7 [json_name = "signed_url_cache_max_age_sec"];

    CacheKeyPolicy cache_key_policy = 8 [json_name = "cache_key_policy"];

    repeated NegativeCachingPolicy negative_caching_policy = 9 [json_name = "negative_caching_policy"];

    message CacheKeyPolicy {
      // If true requests to different hosts will be cached separately.
      optional bool include_host = 1 [json_name = "include_host"];

      // Names of cookies to include in cache keys.
      repeated string include_named_cookies = 2 [json_name = "include_named_cookies"];

      // If true, http and https requests will be cached separately.
      optional bool include_protocol = 3 [json_name = "include_protocol"];

      // If true, include query string parameters in the cache key
      //
      // according to query_string_whitelist and
      //
      // query_string_blacklist. If neither is set, the entire query
      //
      // string will be included.
      //
      //
      // If false, the query string will be excluded from the cache
      //
      // key entirely.
      optional bool include_query_string = 4 [json_name = "include_query_string"];

      // Names of query string parameters to exclude in cache keys.
      //
      //
      // All other parameters will be included. Either specify
      //
      // query_string_whitelist or query_string_blacklist, not both.
      //
      // '&' and '=' will be percent encoded and not treated as
      //
      // delimiters.
      repeated string query_string_blacklist = 5 [json_name = "query_string_blacklist"];

      // Names of query string parameters to include in cache keys.
      //
      //
      // All other parameters will be excluded. Either specify
      //
      // query_string_whitelist or query_string_blacklist, not both.
      //
      // '&' and '=' will be percent encoded and not treated as
      //
      // delimiters.
      repeated string query_string_whitelist = 6 [json_name = "query_string_whitelist"];
    }

    message NegativeCachingPolicy {
      // The HTTP status code to define a TTL against. Only HTTP status codes 300, 301,
      // 308, 404, 405, 410, 421, 451 and 501
      //
      // can be specified as values, and you cannot specify a status code more than once.
      optional int64 code = 1;
    }
  }

  message CircuitBreakers {
    // The maximum number of connections to the backend cluster.
    //
    // Defaults to 1024.
    optional int64 max_connections = 1 [json_name = "max_connections"];

    // The maximum number of pending requests to the backend cluster.
    //
    // Defaults to 1024.
    optional int64 max_pending_requests = 2 [json_name = "max_pending_requests"];

    // The maximum number of parallel requests to the backend cluster.
    //
    // Defaults to 1024.
    optional int64 max_requests = 3 [json_name = "max_requests"];

    // Maximum requests for a single backend connection. This parameter
    //
    // is respected by both the HTTP/1.1 and HTTP/2 implementations. If
    //
    // not specified, there is no limit. Setting this parameter to 1
    //
    // will effectively disable keep alive.
    optional int64 max_requests_per_connection = 4 [json_name = "max_requests_per_connection"];

    // The maximum number of parallel retries to the backend cluster.
    //
    // Defaults to 3.
    optional int64 max_retries = 5 [json_name = "max_retries"];
  }

  message ConnectionTrackingPolicy {
    // Specifies connection persistence when backends are unhealthy.
    //
    //
    // If set to 'DEFAULT_FOR_PROTOCOL', the existing connections persist on
    //
    // unhealthy backends only for connection-oriented protocols (TCP and SCTP)
    //
    // and only if the Tracking Mode is PER_CONNECTION (default tracking mode)
    //
    // or the Session Affinity is configured for 5-tuple. They do not persist
    //
    // for UDP.
    //
    //
    // If set to 'NEVER_PERSIST', after a backend becomes unhealthy, the existing
    //
    // connections on the unhealthy backend are never persisted on the unhealthy
    //
    // backend. They are always diverted to newly selected healthy backends
    //
    // (unless all backends are unhealthy).
    //
    //
    // If set to 'ALWAYS_PERSIST', existing connections always persist on
    //
    // unhealthy backends regardless of protocol and session affinity. It is
    //
    // generally not recommended to use this mode overriding the default. Default value:
    // "DEFAULT_FOR_PROTOCOL" Possible values: ["DEFAULT_FOR_PROTOCOL", "NEVER_PERSIST",
    // "ALWAYS_PERSIST"]
    optional string connection_persistence_on_unhealthy_backends = 1 [
      json_name = "connection_persistence_on_unhealthy_backends"
    ];

    // Enable Strong Session Affinity for Network Load Balancing. This option is not
    // available publicly.
    optional bool enable_strong_affinity = 2 [json_name = "enable_strong_affinity"];

    // Specifies how long to keep a Connection Tracking entry while there is
    //
    // no matching traffic (in seconds).
    //
    //
    // For L4 ILB the minimum(default) is 10 minutes and maximum is 16 hours.
    //
    //
    // For NLB the minimum(default) is 60 seconds and the maximum is 16 hours.
    optional int64 idle_timeout_sec = 3 [json_name = "idle_timeout_sec"];

    // Specifies the key used for connection tracking. There are two options:
    //
    // 'PER_CONNECTION': The Connection Tracking is performed as per the
    //
    // Connection Key (default Hash Method) for the specific protocol.
    //
    //
    // 'PER_SESSION': The Connection Tracking is performed as per the
    //
    // configured Session Affinity. It matches the configured Session Affinity. Default
    // value: "PER_CONNECTION" Possible values: ["PER_CONNECTION", "PER_SESSION"]
    optional string tracking_mode = 4 [json_name = "tracking_mode"];
  }

  message ConsistentHash {
    // The hash based on the value of the specified header field.
    //
    // This field is applicable if the sessionAffinity is set to HEADER_FIELD.
    optional string http_header_name = 1 [json_name = "http_header_name"];

    // The minimum number of virtual nodes to use for the hash ring.
    //
    // Larger ring sizes result in more granular load
    //
    // distributions. If the number of hosts in the load balancing pool
    //
    // is larger than the ring size, each host will be assigned a single
    //
    // virtual node.
    //
    // Defaults to 1024.
    optional int64 minimum_ring_size = 2 [json_name = "minimum_ring_size"];

    HttpCookie http_cookie = 3 [json_name = "http_cookie"];

    message HttpCookie {
      // Name of the cookie.
      optional string name = 1;

      // Path to set for the cookie.
      optional string path = 2;

      Ttl ttl = 3;

      message Ttl {
        // Span of time that's a fraction of a second at nanosecond
        //
        // resolution. Durations less than one second are represented
        //
        // with a 0 seconds field and a positive nanos field. Must
        //
        // be from 0 to 999,999,999 inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second.
        //
        // Must be from 0 to 315,576,000,000 inclusive.
        optional int64 seconds = 2;
      }
    }
  }

  message CustomMetrics {
    // If true, the metric data is not used for load balancing.
    optional bool dry_run = 1 [json_name = "dry_run"];

    // Name of a custom utilization signal. The name must be 1-64 characters
    //
    // long and match the regular expression [a-z]([-_.a-z0-9]*[a-z0-9])? which
    //
    // means the first character must be a lowercase letter, and all following
    //
    // characters must be a dash, period, underscore, lowercase letter, or
    //
    // digit, except the last character, which cannot be a dash, period, or
    //
    // underscore. For usage guidelines, see Custom Metrics balancing mode. This
    //
    // field can only be used for a global or regional backend service with the
    //
    // loadBalancingScheme set to <code>EXTERNAL_MANAGED</code>,
    //
    // <code>INTERNAL_MANAGED</code> <code>INTERNAL_SELF_MANAGED</code>.
    optional string name = 2;
  }

  message FailoverPolicy {
    // On failover or failback, this field indicates whether connection drain
    //
    // will be honored. Setting this to true has the following effect: connections
    //
    // to the old active pool are not drained. Connections to the new active pool
    //
    // use the timeout of 10 min (currently fixed). Setting to false has the
    //
    // following effect: both old and new connections will have a drain timeout
    //
    // of 10 min.
    //
    // This can be set to true only if the protocol is TCP.
    //
    // The default is false.
    optional bool disable_connection_drain_on_failover = 1 [json_name = "disable_connection_drain_on_failover"];

    // This option is used only when no healthy VMs are detected in the primary
    //
    // and backup instance groups. When set to true, traffic is dropped. When
    //
    // set to false, new connections are sent across all VMs in the primary group.
    //
    // The default is false.
    optional bool drop_traffic_if_unhealthy = 2 [json_name = "drop_traffic_if_unhealthy"];

    // The value of the field must be in [0, 1]. If the ratio of the healthy
    //
    // VMs in the primary backend is at or below this number, traffic arriving
    //
    // at the load-balanced IP will be directed to the failover backend.
    //
    // In case where 'failoverRatio' is not set or all the VMs in the backup
    //
    // backend are unhealthy, the traffic will be directed back to the primary
    //
    // backend in the "force" mode, where traffic will be spread to the healthy
    //
    // VMs with the best effort, or to all VMs when no VM is healthy.
    //
    // This field is only used with l4 load balancing.
    optional int64 failover_ratio = 3 [json_name = "failover_ratio"];
  }

  message HaPolicy {
    // Specifies whether fast IP move is enabled, and if so, the mechanism to achieve
    // it.
    //
    // Supported values are:
    //
    //
    // * 'DISABLED': Fast IP Move is disabled. You can only use the haPolicy.leader API
    // to
    //
    // update the leader.
    //
    //
    // * 'GARP_RA': Provides a method to very quickly define a new network endpoint as
    // the
    //
    // leader. This method is faster than updating the leader using the
    //
    // haPolicy.leader API. Fast IP move works as follows: The VM hosting the
    //
    // network endpoint that should become the new leader sends either a
    //
    // Gratuitous ARP (GARP) packet (IPv4) or an ICMPv6 Router Advertisement(RA)
    //
    // packet (IPv6). Google Cloud immediately but temporarily associates the
    //
    // forwarding rule IP address with that VM, and both new and in-flight packets
    //
    // are quickly delivered to that VM. Possible values: ["DISABLED", "GARP_RA"]
    optional string fast_ip_move = 1 [json_name = "fast_ip_move"];

    Leader leader = 2;

    message Leader {
      // A fully-qualified URL of the zonal Network Endpoint Group (NEG) that the leader
      // is
      //
      // attached to.
      optional string backend_group = 1 [json_name = "backend_group"];

      NetworkEndpoint network_endpoint = 2 [json_name = "network_endpoint"];

      message NetworkEndpoint {
        // The name of the VM instance of the leader network endpoint. The instance must
        //
        // already be attached to the NEG specified in the haPolicy.leader.backendGroup.
        optional string instance = 1;
      }
    }
  }

  message Iap {
    // Whether the serving infrastructure will authenticate and authorize all incoming
    // requests.
    optional bool enabled = 1;

    // OAuth2 Client ID for IAP
    optional string oauth2_client_id = 2 [json_name = "oauth2_client_id"];

    // OAuth2 Client Secret for IAP
    optional string oauth2_client_secret = 3 [json_name = "oauth2_client_secret"];

    // OAuth2 Client Secret SHA-256 for IAP
    optional string oauth2_client_secret_sha256 = 4 [json_name = "oauth2_client_secret_sha256"];
  }

  message LogConfig {
    // Whether to enable logging for the load balancer traffic served by this backend
    // service.
    optional bool enable = 1;

    // Specifies the fields to include in logging. This field can only be specified if
    // logging is enabled for this backend service.
    repeated string optional_fields = 2 [json_name = "optional_fields"];

    // Specifies the optional logging mode for the load balancer traffic.
    //
    // Supported values: INCLUDE_ALL_OPTIONAL, EXCLUDE_ALL_OPTIONAL, CUSTOM. Possible
    // values: ["INCLUDE_ALL_OPTIONAL", "EXCLUDE_ALL_OPTIONAL", "CUSTOM"]
    optional string optional_mode = 3 [json_name = "optional_mode"];

    // This field can only be specified if logging is enabled for this backend service.
    // The value of
    //
    // the field must be in [0, 1]. This configures the sampling rate of requests to the
    // load balancer
    //
    // where 1.0 means all logged requests are reported and 0.0 means no logged requests
    // are reported.
    //
    // The default value is 1.0.
    optional int64 sample_rate = 4 [json_name = "sample_rate"];

    repeated RequestHeaders request_headers = 5 [json_name = "request_headers"];

    repeated ResponseHeaders response_headers = 6 [json_name = "response_headers"];

    message RequestHeaders {
      // The header name to match on for logging.
      optional string header_name = 1 [json_name = "header_name"];
    }

    message ResponseHeaders {
      // The header name to match on for logging.
      optional string header_name = 1 [json_name = "header_name"];
    }
  }

  message NetworkPassThroughLbTrafficPolicy {
    ZonalAffinity zonal_affinity = 1 [json_name = "zonal_affinity"];

    message ZonalAffinity {
      // This field indicates whether zonal affinity is enabled or not. Default value:
      // "ZONAL_AFFINITY_DISABLED" Possible values: ["ZONAL_AFFINITY_DISABLED",
      // "ZONAL_AFFINITY_SPILL_CROSS_ZONE", "ZONAL_AFFINITY_STAY_WITHIN_ZONE"]
      optional string spillover = 1;

      // The value of the field must be in [0, 1]. When the ratio of the count of healthy
      // backend endpoints in a zone
      //
      // to the count of backend endpoints in that same zone is equal to or above this
      // threshold, the load balancer
      //
      // distributes new connections to all healthy endpoints in the local zone only. When
      // the ratio of the count
      //
      // of healthy backend endpoints in a zone to the count of backend endpoints in that
      // same zone is below this
      //
      // threshold, the load balancer distributes all new connections to all healthy
      // endpoints across all zones.
      optional int64 spillover_ratio = 2 [json_name = "spillover_ratio"];
    }
  }

  message OutlierDetection {
    // Number of errors before a host is ejected from the connection pool. When the
    //
    // backend host is accessed over HTTP, a 5xx return code qualifies as an error.
    //
    // Defaults to 5.
    optional int64 consecutive_errors = 1 [json_name = "consecutive_errors"];

    // The number of consecutive gateway failures (502, 503, 504 status or connection
    //
    // errors that are mapped to one of those status codes) before a consecutive
    //
    // gateway failure ejection occurs. Defaults to 5.
    optional int64 consecutive_gateway_failure = 2 [json_name = "consecutive_gateway_failure"];

    // The percentage chance that a host will be actually ejected when an outlier
    //
    // status is detected through consecutive 5xx. This setting can be used to disable
    //
    // ejection or to ramp it up slowly. Defaults to 100.
    optional int64 enforcing_consecutive_errors = 3 [json_name = "enforcing_consecutive_errors"];

    // The percentage chance that a host will be actually ejected when an outlier
    //
    // status is detected through consecutive gateway failures. This setting can be
    //
    // used to disable ejection or to ramp it up slowly. Defaults to 0.
    optional int64 enforcing_consecutive_gateway_failure = 4 [
      json_name = "enforcing_consecutive_gateway_failure"
    ];

    // The percentage chance that a host will be actually ejected when an outlier
    //
    // status is detected through success rate statistics. This setting can be used to
    //
    // disable ejection or to ramp it up slowly. Defaults to 100.
    optional int64 enforcing_success_rate = 5 [json_name = "enforcing_success_rate"];

    // Maximum percentage of hosts in the load balancing pool for the backend service
    //
    // that can be ejected. Defaults to 10%.
    optional int64 max_ejection_percent = 6 [json_name = "max_ejection_percent"];

    // The number of hosts in a cluster that must have enough request volume to detect
    //
    // success rate outliers. If the number of hosts is less than this setting, outlier
    //
    // detection via success rate statistics is not performed for any host in the
    //
    // cluster. Defaults to 5.
    optional int64 success_rate_minimum_hosts = 7 [json_name = "success_rate_minimum_hosts"];

    // The minimum number of total requests that must be collected in one interval (as
    //
    // defined by the interval duration above) to include this host in success rate
    //
    // based outlier detection. If the volume is lower than this setting, outlier
    //
    // detection via success rate statistics is not performed for that host. Defaults
    //
    // to 100.
    optional int64 success_rate_request_volume = 8 [json_name = "success_rate_request_volume"];

    // This factor is used to determine the ejection threshold for success rate outlier
    //
    // ejection. The ejection threshold is the difference between the mean success
    //
    // rate, and the product of this factor and the standard deviation of the mean
    //
    // success rate: mean - (stdev * success_rate_stdev_factor). This factor is divided
    //
    // by a thousand to get a double. That is, if the desired factor is 1.9, the
    //
    // runtime value should be 1900. Defaults to 1900.
    optional int64 success_rate_stdev_factor = 9 [json_name = "success_rate_stdev_factor"];

    BaseEjectionTime base_ejection_time = 10 [json_name = "base_ejection_time"];

    Interval interval = 11;

    message BaseEjectionTime {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      //
      // less than one second are represented with a 0 'seconds' field and a positive
      //
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      //
      // inclusive.
      optional int64 seconds = 2;
    }

    message Interval {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      //
      // less than one second are represented with a 0 'seconds' field and a positive
      //
      // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      //
      // inclusive.
      optional int64 seconds = 2;
    }
  }

  message Params {
    // Resource manager tags to be bound to the region backend service. Tag keys and
    // values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message StrongSessionAffinityCookie {
    // Name of the cookie.
    optional string name = 1;

    // Path to set for the cookie.
    optional string path = 2;

    Ttl ttl = 3;

    message Ttl {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message TlsSettings {
    // Reference to the BackendAuthenticationConfig resource from the
    // networksecurity.googleapis.com namespace.
    //
    // Can be used in authenticating TLS connections to the backend, as specified by the
    // authenticationMode field.
    //
    // Can only be specified if authenticationMode is not NONE.
    optional string authentication_config = 1 [json_name = "authentication_config"];

    // Server Name Indication - see RFC3546 section 3.1. If set, the load balancer sends
    // this string as the SNI hostname in the
    //
    // TLS connection to the backend, and requires that this string match a Subject
    // Alternative Name (SAN) in the backend's
    //
    // server certificate. With a Regional Internet NEG backend, if the SNI is specified
    // here, the load balancer uses it
    //
    // regardless of whether the Regional Internet NEG is specified with FQDN or IP
    // address and port.
    optional string sni = 2;

    repeated SubjectAltNames subject_alt_names = 3 [json_name = "subject_alt_names"];

    message SubjectAltNames {
      // The SAN specified as a DNS Name.
      optional string dns_name = 1 [json_name = "dns_name"];

      // The SAN specified as a URI.
      optional string uniform_resource_identifier = 2 [json_name = "uniform_resource_identifier"];
    }
  }
}

// GoogleComputeRegionCommitment version is 0
message GoogleComputeRegionCommitment {
  // Specifies whether to enable automatic renewal for the commitment.
  //
  // The default value is false if not specified.
  //
  // If the field is set to true, the commitment will be automatically renewed for
  // either
  //
  // one or three years according to the terms of the existing commitment.
  optional bool auto_renew = 1 [json_name = "auto_renew"];

  // The category of the commitment. Category MACHINE specifies commitments composed
  // of
  //
  // machine resources such as VCPU or MEMORY, listed in resources. Category LICENSE
  //
  // specifies commitments composed of software licenses, listed in licenseResources.
  //
  // Note that only MACHINE commitments should have a Type specified. Possible values:
  // ["LICENSE", "MACHINE"]
  optional string category = 2;

  // Unique identifier for the resource.
  optional int64 commitment_id = 3 [json_name = "commitment_id"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // An optional description of this resource.
  optional string description = 5;

  // Commitment end time in RFC3339 text format.
  optional string end_timestamp = 6 [json_name = "end_timestamp"];

  // Specifies the already existing reservations to attach to the Commitment. This
  // field will suppress
  //
  // diffs that change the value from empty to non-empty. To force changing this field
  // from empty to non-empty,
  //
  // change another field at the same time.
  optional string existing_reservations = 7 [json_name = "existing_reservations"];

  optional string id = 8;

  // Name of the resource. The name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 9;

  // The plan for this commitment, which determines duration and discount rate.
  //
  // The currently supported plans are TWELVE_MONTH (1 year), and THIRTY_SIX_MONTH (3
  // years). Possible values: ["TWELVE_MONTH", "THIRTY_SIX_MONTH"]
  optional string plan = 10;

  optional string project = 11;

  // URL of the region where this commitment may be used.
  optional string region = 12;

  optional string self_link = 13 [json_name = "self_link"];

  // Commitment start time in RFC3339 text format.
  optional string start_timestamp = 14 [json_name = "start_timestamp"];

  // Status of the commitment with regards to eventual expiration
  //
  // (each commitment has an end date defined).
  optional string status = 15;

  // A human-readable explanation of the status.
  optional string status_message = 16 [json_name = "status_message"];

  // The type of commitment, which affects the discount rate and the eligible
  // resources.
  //
  // The type could be one of the following value: 'MEMORY_OPTIMIZED',
  // 'ACCELERATOR_OPTIMIZED',
  //
  // 'GENERAL_PURPOSE', 'GENERAL_PURPOSE_N2', 'GENERAL_PURPOSE_N2D',
  // 'GENERAL_PURPOSE_E2',
  //
  // 'GENERAL_PURPOSE_T2D', 'GENERAL_PURPOSE_C3', 'COMPUTE_OPTIMIZED_C2',
  // 'COMPUTE_OPTIMIZED_C2D' and
  //
  // 'GRAPHICS_OPTIMIZED_G2'
  optional string type = 17;

  LicenseResource license_resource = 18 [json_name = "license_resource"];

  Params params = 19;

  repeated Resources resources = 20;

  repeated Timeouts timeouts = 21;

  google.protobuf.Value for_each = 22 [json_name = "for_each"];

  repeated string depends_on = 23 [json_name = "depends_on"];

  optional int32 count = 24;

  optional string provider = 25;

  terraform.v1.Lifecycle lifecycle = 26;

  message LicenseResource {
    // The number of licenses purchased.
    optional string amount = 1;

    // Specifies the core range of the instance for which this license applies.
    optional string cores_per_license = 2 [json_name = "cores_per_license"];

    // Any applicable license URI.
    optional string license = 3;
  }

  message Params {
    // Resource manager tags to be bound to the commitment. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Resources {
    // Name of the accelerator type resource. Applicable only when the type is
    // ACCELERATOR.
    optional string accelerator_type = 1 [json_name = "accelerator_type"];

    // The amount of the resource purchased (in a type-dependent unit,
    //
    // such as bytes). For vCPUs, this can just be an integer. For memory,
    //
    // this must be provided in MB. Memory must be a multiple of 256 MB,
    //
    // with up to 6.5GB of memory per every vCPU.
    optional string amount = 2;

    // Type of resource for which this commitment applies.
    //
    // Possible values are VCPU, MEMORY, LOCAL_SSD, and ACCELERATOR.
    optional string type = 3;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionCompositeHealthCheck version is 0
message GoogleComputeRegionCompositeHealthCheck {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you
  //
  // create the resource.
  optional string description = 3;

  // Fingerprint of this resource. A hash of the contents stored in this object.
  //
  // This field is used in optimistic locking. This field will be ignored when
  //
  // inserting a 'CompositeHealthCheck'. An up-to-date fingerprint
  //
  // must be provided in order to patch the CompositeHealthCheck; Otherwise,
  //
  // the request will fail with error '412 conditionNotMet'. To see
  //
  // the latest fingerprint, make a 'get()' request to retrieve the
  //
  // CompositeHealthCheck.
  optional string fingerprint = 4;

  // URL to the destination resource. Must be set. Must be a
  //
  // ForwardingRule. The ForwardingRule must have
  //
  // load balancing scheme INTERNAL or
  //
  // INTERNAL_MANAGED and must be regional and in the same region
  //
  // as the CompositeHealthCheck (cross-region deployment for
  //
  // INTERNAL_MANAGED is not supported). Can be mutated.
  optional string health_destination = 5 [json_name = "health_destination"];

  // URLs to the HealthSource resources whose results are AND'ed.
  //
  // I.e. he aggregated result is is HEALTHY only if all sources
  //
  // are HEALTHY. Must have at least 1. Must not have more than 10.
  //
  // Must be regional and in the same region as the
  //
  // CompositeHealthCheck. Can be mutated.
  repeated string health_sources = 6 [json_name = "health_sources"];

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string id = 7;

  // Name of the resource. Provided by the client when the resource is created.
  //
  // The name must be 1-63 characters long, and comply with RFC1035.
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  //
  // expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  //
  // character must be a lowercase letter, and all following characters must
  //
  // be a dash, lowercase letter, or digit, except the last character, which
  //
  // cannot be a dash.
  optional string name = 8;

  optional string project = 9;

  // URL of the region where the composite health check resides.
  optional string region = 10;

  // Server-defined URL with id for the resource.
  optional string self_link_with_id = 11 [json_name = "self_link_with_id"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionDisk version is 0
message GoogleComputeRegionDisk {
  // The access mode of the disk.
  //
  // For example:
  //
  // * READ_WRITE_SINGLE: The default AccessMode, means the disk can be attached to
  // single instance in RW mode.
  //
  // * READ_WRITE_MANY: The AccessMode means the disk can be attached to multiple
  // instances in RW mode.
  //
  // * READ_ONLY_SINGLE: The AccessMode means the disk can be attached to multiple
  // instances in RO mode.
  //
  // The AccessMode is only valid for Hyperdisk disk types.
  optional string access_mode = 1 [json_name = "access_mode"];

  // If set to true, a snapshot of the disk will be created before it is destroyed.
  //
  // If your disk is encrypted with customer managed encryption keys these will be
  // reused for the snapshot creation.
  //
  // The name of the snapshot by default will be '{{disk-name}}-YYYYMMDD-HHmm'
  optional bool create_snapshot_before_destroy = 2 [json_name = "create_snapshot_before_destroy"];

  // This will set a custom name prefix for the snapshot that's created when the disk
  // is deleted.
  optional string create_snapshot_before_destroy_prefix = 3 [
    json_name = "create_snapshot_before_destroy_prefix"
  ];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 4 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 5 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 6;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string disk_id = 7 [json_name = "disk_id"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 8 [json_name = "effective_labels"];

  optional string id = 9;

  // The image from which to initialize this disk. This can be
  //
  // one of: the image's 'self_link', 'projects/{project}/global/images/{image}',
  //
  // 'projects/{project}/global/images/family/{family}', 'global/images/{image}',
  //
  // 'global/images/family/{family}', 'family/{family}', '{project}/{family}',
  //
  // '{project}/{image}', '{family}', or '{image}'. If referred by family, the
  //
  // images names must include the family name. If they don't, use the
  //
  // [google_compute_image data source](/docs/providers/google/d/compute_image.html).
  //
  // For instance, the image 'centos-6-v20180104' includes its family name 'centos-6'.
  //
  // These images can be referred by family name here.
  optional string image = 10;

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 11 [json_name = "label_fingerprint"];

  // Labels to apply to this disk.  A list of key->value pairs.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 12;

  // Last attach timestamp in RFC3339 text format.
  optional string last_attach_timestamp = 13 [json_name = "last_attach_timestamp"];

  // Last detach timestamp in RFC3339 text format.
  optional string last_detach_timestamp = 14 [json_name = "last_detach_timestamp"];

  // Any applicable license URI.
  repeated string licenses = 15;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 16;

  // Physical block size of the persistent disk, in bytes. If not present
  //
  // in a request, a default value is used. Currently supported sizes
  //
  // are 4096 and 16384, other sizes may be added in the future.
  //
  // If an unsupported value is requested, the error message will list
  //
  // the supported values for the caller's project.
  optional int64 physical_block_size_bytes = 17 [json_name = "physical_block_size_bytes"];

  optional string project = 18;

  // Indicates how many IOPS to provision for the disk. This sets the number of I/O
  // operations per second
  //
  // that the disk can handle. Values must be between 10,000 and 120,000.
  //
  // For more details, see the Extreme persistent disk
  // umentation](https://cloud.google.com/compute/docs/disks/extreme-persistent-disk).
  optional int64 provisioned_iops = 19 [json_name = "provisioned_iops"];

  // Indicates how much throughput to provision for the disk. This sets the number of
  // throughput
  //
  // mb per second that the disk can handle. Values must be greater than or equal to
  // 1.
  optional int64 provisioned_throughput = 20 [json_name = "provisioned_throughput"];

  // A reference to the region where the disk resides.
  optional string region = 21;

  // URLs of the zones where the disk should be replicated to.
  repeated string replica_zones = 22 [json_name = "replica_zones"];

  optional string self_link = 23 [json_name = "self_link"];

  // Size of the persistent disk, specified in GB. You can specify this
  //
  // field when creating a persistent disk using the sourceImage or
  //
  // sourceSnapshot parameter, or specify it alone to create an empty
  //
  // persistent disk.
  //
  //
  // If you specify this field along with sourceImage or sourceSnapshot,
  //
  // the value of sizeGb must not be less than the size of the sourceImage
  //
  // or the size of the snapshot.
  optional int64 size = 24;

  // The source snapshot used to create this disk. You can provide this as
  //
  // a partial or full URL to the resource. For example, the following are
  //
  // valid values:
  //
  //
  // https://www.googleapis.com/compute/v1/projects/project/global/snapshots/snapshot'
  //
  // * 'projects/project/global/snapshots/snapshot'
  //
  // * 'global/snapshots/snapshot'
  //
  // * 'snapshot'
  optional string snapshot = 25;

  // The source disk used to create this disk. You can provide this as a partial or
  // full URL to the resource.
  //
  // For example, the following are valid values:
  //
  //
  // ttps://www.googleapis.com/compute/v1/projects/{project}/zones/{zone}/disks/{disk}
  //
  // ://www.googleapis.com/compute/v1/projects/{project}/regions/{region}/disks/{disk}
  //
  // * projects/{project}/zones/{zone}/disks/{disk}
  //
  // * projects/{project}/regions/{region}/disks/{disk}
  //
  // * zones/{zone}/disks/{disk}
  //
  // * regions/{region}/disks/{disk}
  optional string source_disk = 26 [json_name = "source_disk"];

  // The ID value of the disk used to create this image. This value may
  //
  // be used to determine whether the image was taken from the current
  //
  // or a previous instance of a given disk name.
  optional string source_disk_id = 27 [json_name = "source_disk_id"];

  // The ID value of the image used to create this disk. This value
  //
  // identifies the exact image that was used to create this persistent
  //
  // disk. For example, if you created the persistent disk from an image
  //
  // that was later deleted and recreated under the same name, the source
  //
  // image ID would identify the exact version of the image that was used.
  optional string source_image_id = 28 [json_name = "source_image_id"];

  // The unique ID of the snapshot used to create this disk. This value
  //
  // identifies the exact snapshot that was used to create this persistent
  //
  // disk. For example, if you created the persistent disk from a snapshot
  //
  // that was later deleted and recreated under the same name, the source
  //
  // snapshot ID would identify the exact version of the snapshot that was
  //
  // used.
  optional string source_snapshot_id = 29 [json_name = "source_snapshot_id"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 30 [json_name = "terraform_labels"];

  // URL of the disk type resource describing which disk type to use to
  //
  // create the disk. Provide this when creating the disk.
  optional string type = 31;

  // Links to the users of the disk (attached instances) in form:
  //
  // project/zones/zone/instances/instance
  repeated string users = 32;

  AsyncPrimaryDisk async_primary_disk = 33 [json_name = "async_primary_disk"];

  DiskEncryptionKey disk_encryption_key = 34 [json_name = "disk_encryption_key"];

  repeated GuestOsFeatures guest_os_features = 35 [json_name = "guest_os_features"];

  SourceImageEncryptionKey source_image_encryption_key = 36 [json_name = "source_image_encryption_key"];

  SourceSnapshotEncryptionKey source_snapshot_encryption_key = 37 [json_name = "source_snapshot_encryption_key"];

  repeated Timeouts timeouts = 38;

  google.protobuf.Value for_each = 39 [json_name = "for_each"];

  repeated string depends_on = 40 [json_name = "depends_on"];

  optional int32 count = 41;

  optional string provider = 42;

  terraform.v1.Lifecycle lifecycle = 43;

  message AsyncPrimaryDisk {
    // Primary disk for asynchronous disk replication.
    optional string disk = 1;
  }

  message DiskEncryptionKey {
    // The name of the encryption key that is stored in Google Cloud KMS.
    optional string kms_key_name = 1 [json_name = "kms_key_name"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 2 [json_name = "raw_key"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit
    //
    // customer-supplied encryption key to either encrypt or decrypt
    //
    // this resource. You can provide either the rawKey or the rsaEncryptedKey.
    optional string rsa_encrypted_key = 3 [json_name = "rsa_encrypted_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    //
    // encryption key that protects this resource.
    optional string sha256 = 4;
  }

  message GuestOsFeatures {
    // The type of supported feature. Read [Enabling guest operating system
    // com/compute/docs/images/create-delete-deprecate-private-images#guest-os-features)
    // to see a list of available options. Possible values: ["MULTI_IP_SUBNET",
    // "SECURE_BOOT", "SEV_CAPABLE", "UEFI_COMPATIBLE", "VIRTIO_SCSI_MULTIQUEUE",
    // "WINDOWS", "GVNIC", "SEV_LIVE_MIGRATABLE", "SEV_SNP_CAPABLE",
    // "SUSPEND_RESUME_COMPATIBLE", "TDX_CAPABLE", "SEV_LIVE_MIGRATABLE_V2",
    // "SNP_SVSM_CAPABLE"]
    optional string type = 1;
  }

  message SourceImageEncryptionKey {
    // The name of the encryption key that is stored in Google Cloud KMS.
    optional string kms_key_name = 1 [json_name = "kms_key_name"];

    // The service account used for the encryption request for the given KMS key.
    //
    // If absent, the Compute Engine Service Agent service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit
    //
    // customer-supplied encryption key to either encrypt or decrypt
    //
    // this resource. You can provide either the rawKey or the rsaEncryptedKey.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    //
    // encryption key that protects this resource.
    optional string sha256 = 5;
  }

  message SourceSnapshotEncryptionKey {
    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 1 [json_name = "raw_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    //
    // encryption key that protects this resource.
    optional string sha256 = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionDiskIamBinding version is 0
message GoogleComputeRegionDiskIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string region = 6;

  optional string role = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeRegionDiskIamMember version is 0
message GoogleComputeRegionDiskIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string region = 6;

  optional string role = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeRegionDiskIamPolicy version is 0
message GoogleComputeRegionDiskIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  optional string region = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeRegionDiskResourcePolicyAttachment version is 0
message GoogleComputeRegionDiskResourcePolicyAttachment {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // The name of the regional disk in which the resource policies are attached to.
  optional string disk = 2;

  optional string id = 3;

  // The resource policy to be attached to the disk for scheduling snapshot
  //
  // creation. Do not specify the self link.
  optional string name = 4;

  optional string project = 5;

  // A reference to the region where the disk resides.
  optional string region = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionHealthAggregationPolicy version is 0
message GoogleComputeRegionHealthAggregationPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you
  //
  // create the resource.
  optional string description = 3;

  // Fingerprint of this resource. A hash of the contents stored in this object.
  //
  // This field is used in optimistic locking. This field will be ignored when
  //
  // inserting a 'HealthAggregationPolicy'. An up-to-date fingerprint
  //
  // must be provided in order to patch the RegionHealthAggregationPolicy; Otherwise,
  //
  // the request will fail with error '412 conditionNotMet'. To see
  //
  // the latest fingerprint, make a 'get()' request to retrieve the
  //
  // RegionHealthAggregationPolicy.
  optional string fingerprint = 4;

  // Can only be set if the 'policyType' field is
  //
  // 'BACKEND_SERVICE_POLICY'. Specifies the threshold (as a
  //
  // percentage) of healthy endpoints required in order to consider the
  //
  // aggregated health result HEALTHY. Defaults to '60'. Must be in
  //
  // range [0, 100]. Not applicable if the 'policyType' field is
  //
  // 'DNB_PUBLIC_IP_POLICY'. Can be mutated. This field is optional,
  //
  // and will be set to the default if unspecified. Note that both this
  //
  // threshold and 'minHealthyThreshold' must be satisfied in order
  //
  // for HEALTHY to be the aggregated result. "Endpoints" refers to network
  //
  // endpoints within a Network Endpoint Group or instances within an Instance
  //
  // Group.
  optional int64 healthy_percent_threshold = 5 [json_name = "healthy_percent_threshold"];

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string id = 6;

  // Can only be set if the 'policyType' field is
  //
  // 'BACKEND_SERVICE_POLICY'. Specifies the minimum number of
  //
  // healthy endpoints required in order to consider the aggregated health
  //
  // result HEALTHY. Defaults to '1'. Must be positive. Not
  //
  // applicable if the 'policyType' field is
  //
  // 'DNB_PUBLIC_IP_POLICY'. Can be mutated. This field is optional,
  //
  // and will be set to the default if unspecified. Note that both this
  //
  // threshold and 'healthyPercentThreshold' must be satisfied in
  //
  // order for HEALTHY to be the aggregated result. "Endpoints" refers to
  //
  // network endpoints within a Network Endpoint Group or instances within an
  //
  // Instance Group.
  optional int64 min_healthy_threshold = 7 [json_name = "min_healthy_threshold"];

  // Name of the resource. Provided by the client when the resource is created.
  //
  // The name must be 1-63 characters long, and comply with RFC1035.
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  //
  // expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  //
  // character must be a lowercase letter, and all following characters must
  //
  // be a dash, lowercase letter, or digit, except the last character, which
  //
  // cannot be a dash.
  optional string name = 8;

  // Specifies the type of the healthAggregationPolicy. The only allowed value
  //
  // for global resources is 'DNS_PUBLIC_IP_POLICY'. The only allowed
  //
  // value for regional resources is 'BACKEND_SERVICE_POLICY'. Must
  //
  // be specified when the healthAggregationPolicy is created, and cannot be
  //
  // mutated. Default value: "BACKEND_SERVICE_POLICY" Possible values:
  // ["DNS_PUBLIC_IP_POLICY", "BACKEND_SERVICE_POLICY"]
  optional string policy_type = 9 [json_name = "policy_type"];

  optional string project = 10;

  // URL of the region where the health aggregation policy resides.
  optional string region = 11;

  // Server-defined URL with id for the resource.
  optional string self_link_with_id = 12 [json_name = "self_link_with_id"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionHealthCheck version is 0
message GoogleComputeRegionHealthCheck {
  // How often (in seconds) to send a health check. The default value is 5
  //
  // seconds.
  optional int64 check_interval_sec = 1 [json_name = "check_interval_sec"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 4;

  // The unique identifier number for the resource. This identifier is defined by the
  // server.
  optional int64 health_check_id = 5 [json_name = "health_check_id"];

  // A so-far unhealthy instance will be marked healthy after this many
  //
  // consecutive successes. The default value is 2.
  optional int64 healthy_threshold = 6 [json_name = "healthy_threshold"];

  optional string id = 7;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the
  //
  // last character, which cannot be a dash.
  optional string name = 8;

  optional string project = 9;

  // The Region in which the created health check should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 10;

  optional string self_link = 11 [json_name = "self_link"];

  // How long (in seconds) to wait before claiming failure.
  //
  // The default value is 5 seconds.  It is invalid for timeoutSec to have
  //
  // greater value than checkIntervalSec.
  optional int64 timeout_sec = 12 [json_name = "timeout_sec"];

  // The type of the health check. One of HTTP, HTTP2, HTTPS, TCP, or SSL.
  optional string type = 13;

  // A so-far healthy instance will be marked unhealthy after this many
  //
  // consecutive failures. The default value is 2.
  optional int64 unhealthy_threshold = 14 [json_name = "unhealthy_threshold"];

  GrpcHealthCheck grpc_health_check = 15 [json_name = "grpc_health_check"];

  GrpcTlsHealthCheck grpc_tls_health_check = 16 [json_name = "grpc_tls_health_check"];

  Http2HealthCheck http2_health_check = 17 [json_name = "http2_health_check"];

  HttpHealthCheck http_health_check = 18 [json_name = "http_health_check"];

  HttpsHealthCheck https_health_check = 19 [json_name = "https_health_check"];

  LogConfig log_config = 20 [json_name = "log_config"];

  SslHealthCheck ssl_health_check = 21 [json_name = "ssl_health_check"];

  TcpHealthCheck tcp_health_check = 22 [json_name = "tcp_health_check"];

  repeated Timeouts timeouts = 23;

  google.protobuf.Value for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  optional int32 count = 26;

  optional string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message GrpcHealthCheck {
    // The gRPC service name for the health check.
    //
    // The value of grpcServiceName has the following meanings by convention:
    //
    //
    // * Empty serviceName means the overall status of all services at the backend.
    //
    // * Non-empty serviceName means the health of that gRPC service, as defined by the
    // owner of the service.
    //
    //
    // The grpcServiceName can only be ASCII.
    optional string grpc_service_name = 1 [json_name = "grpc_service_name"];

    // The port number for the health check request.
    //
    // Must be specified if portName and portSpecification are not set
    //
    // or if port_specification is USE_FIXED_PORT. Valid values are 1 through 65535.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, gRPC health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];
  }

  message GrpcTlsHealthCheck {
    // The gRPC service name for the health check.
    //
    // The value of grpcServiceName has the following meanings by convention:
    //
    //
    // * Empty serviceName means the overall status of all services at the backend.
    //
    // * Non-empty serviceName means the health of that gRPC service, as defined by the
    // owner of the service.
    //
    //
    // The grpcServiceName can only be ASCII.
    optional string grpc_service_name = 1 [json_name = "grpc_service_name"];

    // The port number for the health check request.
    //
    // Must be specified if port_specification is USE_FIXED_PORT. Valid values are 1
    // through 65535.
    optional int64 port = 2;

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': Not supported for GRPC with TLS health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, gRPC health check follows behavior specified in the 'port'
    // field. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"]
    optional string port_specification = 3 [json_name = "port_specification"];
  }

  message Http2HealthCheck {
    // The value of the host header in the HTTP2 health check request.
    //
    // If left empty (default value), the public IP on behalf of which this health
    //
    // check is performed will be used.
    optional string host = 1;

    // The TCP port number for the HTTP2 health check request.
    //
    // The default value is 443.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, HTTP2 health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP2 health check request.
    //
    // The default value is /.
    optional string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 7;
  }

  message HttpHealthCheck {
    // The value of the host header in the HTTP health check request.
    //
    // If left empty (default value), the public IP on behalf of which this health
    //
    // check is performed will be used.
    optional string host = 1;

    // The TCP port number for the HTTP health check request.
    //
    // The default value is 80.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, HTTP health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTP health check request.
    //
    // The default value is /.
    optional string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 7;
  }

  message HttpsHealthCheck {
    // The value of the host header in the HTTPS health check request.
    //
    // If left empty (default value), the public IP on behalf of which this health
    //
    // check is performed will be used.
    optional string host = 1;

    // The TCP port number for the HTTPS health check request.
    //
    // The default value is 443.
    optional int64 port = 2;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 3 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, HTTPS health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 4 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 5 [json_name = "proxy_header"];

    // The request path of the HTTPS health check request.
    //
    // The default value is /.
    optional string request_path = 6 [json_name = "request_path"];

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 7;
  }

  message LogConfig {
    // Indicates whether or not to export logs. This is false by default,
    //
    // which means no health check logging will be done.
    optional bool enable = 1;
  }

  message SslHealthCheck {
    // The TCP port number for the SSL health check request.
    //
    // The default value is 443.
    optional int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, SSL health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the SSL connection has been
    //
    // established (default value is empty). If both request and response are
    //
    // empty, the connection establishment alone will indicate health. The request
    //
    // data can only be ASCII.
    optional string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 6;
  }

  message TcpHealthCheck {
    // The TCP port number for the TCP health check request.
    //
    // The default value is 80.
    optional int64 port = 1;

    // Port name as defined in InstanceGroup#NamedPort#name. If both port and
    //
    // port_name are defined, port takes precedence.
    optional string port_name = 2 [json_name = "port_name"];

    // Specifies how port is selected for health checking, can be one of the
    //
    // following values:
    //
    //
    // * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.
    //
    //
    // * 'USE_NAMED_PORT': The 'portName' is used for health checking.
    //
    //
    // * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
    //
    // network endpoint is used for health checking. For other backends, the
    //
    // port or named port specified in the Backend Service is used for health
    //
    // checking.
    //
    //
    // If not specified, TCP health check follows behavior specified in 'port' and
    //
    // 'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT",
    // "USE_SERVING_PORT"]
    optional string port_specification = 3 [json_name = "port_specification"];

    // Specifies the type of proxy header to append before sending data to the
    //
    // backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
    optional string proxy_header = 4 [json_name = "proxy_header"];

    // The application data to send once the TCP connection has been
    //
    // established (default value is empty). If both request and response are
    //
    // empty, the connection establishment alone will indicate health. The request
    //
    // data can only be ASCII.
    optional string request = 5;

    // The bytes to match against the beginning of the response data. If left empty
    //
    // (the default value), any response will indicate health. The response data
    //
    // can only be ASCII.
    optional string response = 6;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionHealthSource version is 0
message GoogleComputeRegionHealthSource {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you
  //
  // create the resource.
  optional string description = 3;

  // Fingerprint of this resource. A hash of the contents stored in this object.
  //
  // This field is used in optimistic locking.
  optional string fingerprint = 4;

  // URL to the 'HealthAggregationPolicy' resource. Must be set. Must
  //
  // be regional and in the same region as the 'HealthSource'. Can be
  //
  // mutated.
  optional string health_aggregation_policy = 5 [json_name = "health_aggregation_policy"];

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is created.
  //
  // The name must be 1-63 characters long, and comply with RFC1035.
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  //
  // expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first
  //
  // character must be a lowercase letter, and all following characters must
  //
  // be a dash, lowercase letter, or digit, except the last character, which
  //
  // cannot be a dash.
  optional string name = 7;

  optional string project = 8;

  // URL of the region where the health source resides.
  optional string region = 9;

  // Server-defined URL with id for the resource.
  optional string self_link_with_id = 10 [json_name = "self_link_with_id"];

  // Specifies the type of the 'HealthSource'. The only allowed value
  //
  // is 'BACKEND_SERVICE'. Must be specified when the
  //
  // 'HealthSource' is created, and cannot be mutated. Possible values:
  // ["BACKEND_SERVICE"]
  optional string source_type = 11 [json_name = "source_type"];

  // URLs to the source resources. Must be size 1. Must be a
  //
  // 'BackendService' if the 'sourceType' is 'BACKEND_SERVICE'. The
  //
  // 'BackendService' must have load balancing scheme
  //
  // 'INTERNAL' or 'INTERNAL_MANAGED' and must be regional
  //
  // and in the same region as the 'HealthSource' (cross-region
  //
  // deployment for 'INTERNAL_MANAGED' is not supported). The
  //
  // 'BackendService' may use only IGs, MIGs, or NEGs of type
  //
  // 'GCE_VM_IP' or 'GCE_VM_IP_PORT'. The
  //
  // 'BackendService' may not use 'haPolicy'. Can be
  //
  // mutated.
  repeated string sources = 12;

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionInstanceGroupManager version is 0
message GoogleComputeRegionInstanceGroupManager {
  // The base instance name to use for instances in this group. The value must be a
  // valid RFC1035 name. Supported characters are lowercase letters, numbers, and
  // hyphens (-). Instances are named by appending a hyphen and a random
  // four-character string to the base instance name.
  optional string base_instance_name = 1 [json_name = "base_instance_name"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional textual description of the instance group manager.
  optional string description = 4;

  // The shape to which the group converges either proactively or on resize events
  // (depending on the value set in updatePolicy.instanceRedistributionType).
  optional string distribution_policy_target_shape = 5 [json_name = "distribution_policy_target_shape"];

  // The distribution policy for this managed instance group. You can specify one or
  // more values.
  repeated string distribution_policy_zones = 6 [json_name = "distribution_policy_zones"];

  // The fingerprint of the instance group manager.
  optional string fingerprint = 7;

  optional string id = 8;

  // The full URL of the instance group created by the manager.
  optional string instance_group = 9 [json_name = "instance_group"];

  // The unique identifier number for the resource. This identifier is defined by the
  // server.
  optional int64 instance_group_manager_id = 10 [json_name = "instance_group_manager_id"];

  // Pagination behavior of the listManagedInstances API method for this managed
  // instance group. Valid values are: "PAGELESS", "PAGINATED". If PAGELESS (default),
  // Pagination is disabled for the group's listManagedInstances API method.
  // maxResults and pageToken query parameters are ignored and all instances are
  // returned in a single response. If PAGINATED, pagination is enabled, maxResults
  // and pageToken query parameters are respected.
  optional string list_managed_instances_results = 11 [json_name = "list_managed_instances_results"];

  // The name of the instance group manager. Must be 1-63 characters long and comply
  // with RFC1035. Supported characters include lowercase letters, numbers, and
  // hyphens.
  optional string name = 12;

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 13;

  // The region where the managed instance group resides.
  optional string region = 14;

  // The URL of the created resource.
  optional string self_link = 15 [json_name = "self_link"];

  // The status of this managed instance group.
  repeated Status status = 16;

  // The full URL of all target pools to which new instances in the group are added.
  // Updating the target pools attribute does not affect existing instances.
  repeated string target_pools = 17 [json_name = "target_pools"];

  // The target number of running instances for this managed instance group. This
  // value should always be explicitly set unless this resource is attached to an
  // autoscaler, in which case it should never be set. Defaults to 0.
  optional int64 target_size = 18 [json_name = "target_size"];

  // The target number of stopped instances for this managed instance group.
  optional int64 target_stopped_size = 19 [json_name = "target_stopped_size"];

  // The target number of suspended instances for this managed instance group.
  optional int64 target_suspended_size = 20 [json_name = "target_suspended_size"];

  // Whether to wait for all instances to be created/updated before returning. Note
  // that if this is set to true and the operation does not succeed, Terraform will
  // continue trying until it times out.
  optional bool wait_for_instances = 21 [json_name = "wait_for_instances"];

  // When used with wait_for_instances specifies the status to wait for. When STABLE
  // is specified this resource will wait until the instances are stable before
  // returning. When UPDATED is set, it will wait for the version target to be reached
  // and any per instance configs to be effective and all instances configs to be
  // effective as well as all instances to be stable before returning.
  optional string wait_for_instances_status = 22 [json_name = "wait_for_instances_status"];

  AllInstancesConfig all_instances_config = 23 [json_name = "all_instances_config"];

  AutoHealingPolicies auto_healing_policies = 24 [json_name = "auto_healing_policies"];

  InstanceFlexibilityPolicy instance_flexibility_policy = 25 [json_name = "instance_flexibility_policy"];

  InstanceLifecyclePolicy instance_lifecycle_policy = 26 [json_name = "instance_lifecycle_policy"];

  repeated NamedPort named_port = 27 [json_name = "named_port"];

  ResourcePolicies resource_policies = 28 [json_name = "resource_policies"];

  StandbyPolicy standby_policy = 29 [json_name = "standby_policy"];

  repeated StatefulDisk stateful_disk = 30 [json_name = "stateful_disk"];

  repeated StatefulExternalIp stateful_external_ip = 31 [json_name = "stateful_external_ip"];

  repeated StatefulInternalIp stateful_internal_ip = 32 [json_name = "stateful_internal_ip"];

  repeated TargetSizePolicy target_size_policy = 33 [json_name = "target_size_policy"];

  repeated Timeouts timeouts = 34;

  UpdatePolicy update_policy = 35 [json_name = "update_policy"];

  repeated Version version = 36;

  google.protobuf.Value for_each = 37 [json_name = "for_each"];

  repeated string depends_on = 38 [json_name = "depends_on"];

  optional int32 count = 39;

  optional string provider = 40;

  terraform.v1.Lifecycle lifecycle = 41;

  message Status {
    // all_instances_config: list of object
    repeated AllInstancesConfig all_instances_config = 1 [json_name = "all_instances_config"];

    // is_stable: bool
    optional bool is_stable = 2 [json_name = "is_stable"];

    // stateful: list of object
    repeated Stateful stateful = 3;

    // version_target: list of object
    repeated VersionTarget version_target = 4 [json_name = "version_target"];

    message AllInstancesConfig {
      // current_revision: string
      optional string current_revision = 1 [json_name = "current_revision"];

      // effective: bool
      optional bool effective = 2;
    }

    message Stateful {
      // has_stateful_config: bool
      optional bool has_stateful_config = 1 [json_name = "has_stateful_config"];

      // per_instance_configs: list of object
      repeated PerInstanceConfigs per_instance_configs = 2 [json_name = "per_instance_configs"];

      message PerInstanceConfigs {
        // all_effective: bool
        optional bool all_effective = 1 [json_name = "all_effective"];
      }
    }

    message VersionTarget {
      // is_reached: bool
      optional bool is_reached = 1 [json_name = "is_reached"];
    }
  }

  message AllInstancesConfig {
    // The label key-value pairs that you want to patch onto the instance,
    map<string, string> labels = 1;

    // The metadata key-value pairs that you want to patch onto the instance. For more
    // information, see Project and instance metadata,
    map<string, string> metadata = 2;
  }

  message AutoHealingPolicies {
    // The health check resource that signals autohealing.
    optional string health_check = 1 [json_name = "health_check"];

    // The number of seconds that the managed instance group waits before it applies
    // autohealing policies to new instances or recently recreated instances. Between 0
    // and 3600.
    optional int64 initial_delay_sec = 2 [json_name = "initial_delay_sec"];
  }

  message InstanceFlexibilityPolicy {
    repeated InstanceSelections instance_selections = 1 [json_name = "instance_selections"];

    message InstanceSelections {
      // Full machine-type names, e.g. "n1-standard-16"
      repeated string machine_types = 1 [json_name = "machine_types"];

      // Instance selection name.
      optional string name = 2;

      // Preference of this instance selection. Lower number means higher preference. MIG
      // will first try to create a VM based on the machine-type with lowest rank and
      // fallback to next rank based on availability. Machine types and instance
      // selections with the same rank have the same preference.
      optional int64 rank = 3;
    }
  }

  message InstanceLifecyclePolicy {
    // Specifies the action that a MIG performs on a failed VM. If the value of the
    // "on_failed_health_check" field is DEFAULT_ACTION, then the same action also
    // applies to the VMs on which your application fails a health check. Valid values
    // are: REPAIR, DO_NOTHING. If REPAIR (default), then MIG automatically repairs a
    // failed VM by recreating it. For more information, see about repairing VMs in a
    // MIG. If DO_NOTHING, then MIG does not repair a failed VM.
    optional string default_action_on_failure = 1 [json_name = "default_action_on_failure"];

    // Specifies whether to apply the group's latest configuration when repairing a VM.
    // Valid options are: YES, NO. If YES and you updated the group's instance template
    // or per-instance configurations after the VM was created, then these changes are
    // applied when VM is repaired. If NO (default), then updates are applied in
    // accordance with the group's update policy type.
    optional string force_update_on_repair = 2 [json_name = "force_update_on_repair"];

    // Specifies the action that a MIG performs on an unhealthy VM. A VM is marked as
    // unhealthy when the application running on that VM fails a health check. Valid
    // values are: DEFAULT_ACTION, DO_NOTHING, REPAIR. If DEFAULT_ACTION (default), then
    // MIG uses the same action configured for the  "default_action_on_failure" field.
    // If DO_NOTHING, then MIG does not repair unhealthy VM. If REPAIR, then MIG
    // automatically repairs an unhealthy VM by recreating it.
    optional string on_failed_health_check = 3 [json_name = "on_failed_health_check"];

    OnRepair on_repair = 4 [json_name = "on_repair"];

    message OnRepair {
      // Specifies whether the MIG can change a VM's zone during a repair. If "YES", MIG
      // can select a different zone for the VM during a repair. Else if "NO", MIG cannot
      // change a VM's zone during a repair. The default value of allow_changing_zone is
      // "NO".
      optional string allow_changing_zone = 1 [json_name = "allow_changing_zone"];
    }
  }

  message NamedPort {
    // The name of the port.
    optional string name = 1;

    // The port number.
    optional int64 port = 2;
  }

  message ResourcePolicies {
    // The URL of the workload policy that is specified for this managed instance group.
    // It can be a full or partial URL.
    optional string workload_policy = 1 [json_name = "workload_policy"];
  }

  message StandbyPolicy {
    // Specifies the number of seconds that the MIG should wait to suspend or stop a VM
    // after that VM was created. The initial delay gives the initialization script the
    // time to prepare your VM for a quick scale out. The value of initial delay must be
    // between 0 and 3600 seconds. The default value is 0.
    optional int64 initial_delay_sec = 1 [json_name = "initial_delay_sec"];

    // Defines how a MIG resumes or starts VMs from a standby pool when the group scales
    // out. The default mode is "MANUAL".
    optional string mode = 2;
  }

  message StatefulDisk {
    // A value that prescribes what should happen to the stateful disk when the VM
    // instance is deleted. The available options are NEVER and
    // ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the disk when the VM is deleted,
    // but do not delete the disk. ON_PERMANENT_INSTANCE_DELETION will delete the
    // stateful disk when the VM is permanently deleted from the instance group. The
    // default is NEVER.
    optional string delete_rule = 1 [json_name = "delete_rule"];

    // The device name of the disk to be attached.
    optional string device_name = 2 [json_name = "device_name"];
  }

  message StatefulExternalIp {
    // A value that prescribes what should happen to an associated static Address
    // resource when a VM instance is permanently deleted. The available options are
    // NEVER and ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the IP when the VM is
    // deleted, but do not delete the address resource. ON_PERMANENT_INSTANCE_DELETION
    // will delete the stateful address when the VM is permanently deleted from the
    // instance group. The default is NEVER.
    optional string delete_rule = 1 [json_name = "delete_rule"];

    // The network interface name
    optional string interface_name = 2 [json_name = "interface_name"];
  }

  message StatefulInternalIp {
    // A value that prescribes what should happen to an associated static Address
    // resource when a VM instance is permanently deleted. The available options are
    // NEVER and ON_PERMANENT_INSTANCE_DELETION. NEVER - detach the IP when the VM is
    // deleted, but do not delete the address resource. ON_PERMANENT_INSTANCE_DELETION
    // will delete the stateful address when the VM is permanently deleted from the
    // instance group. The default is NEVER.
    optional string delete_rule = 1 [json_name = "delete_rule"];

    // The network interface name
    optional string interface_name = 2 [json_name = "interface_name"];
  }

  message TargetSizePolicy {
    // The mode of target size policy based on which the MIG creates its VMs
    // individually or all at once.
    optional string mode = 1;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message UpdatePolicy {
    // The instance redistribution policy for regional managed instance groups. Valid
    // values are: "PROACTIVE", "NONE". If PROACTIVE (default), the group attempts to
    // maintain an even distribution of VM instances across zones in the region. If
    // NONE, proactive redistribution is disabled.
    optional string instance_redistribution_type = 1 [json_name = "instance_redistribution_type"];

    // Specifies a fixed number of VM instances. This must be a positive integer.
    // Conflicts with max_surge_percent. Both cannot be 0
    optional int64 max_surge_fixed = 2 [json_name = "max_surge_fixed"];

    // Specifies a percentage of instances between 0 to 100%, inclusive. For example,
    // specify 80 for 80%. Conflicts with max_surge_fixed.
    optional int64 max_surge_percent = 3 [json_name = "max_surge_percent"];

    // Specifies a fixed number of VM instances. This must be a positive integer.
    optional int64 max_unavailable_fixed = 4 [json_name = "max_unavailable_fixed"];

    // Specifies a percentage of instances between 0 to 100%, inclusive. For example,
    // specify 80 for 80%.
    optional int64 max_unavailable_percent = 5 [json_name = "max_unavailable_percent"];

    // Minimal action to be taken on an instance. You can specify either NONE to forbid
    // any actions, REFRESH to update without stopping instances, RESTART to restart
    // existing instances or REPLACE to delete and create new instances from the target
    // template. If you specify a REFRESH, the Updater will attempt to perform that
    // action only. However, if the Updater determines that the minimal action you
    // specify is not enough to perform the update, it might perform a more disruptive
    // action.
    optional string minimal_action = 6 [json_name = "minimal_action"];

    // Most disruptive action that is allowed to be taken on an instance. You can
    // specify either NONE to forbid any actions, REFRESH to allow actions that do not
    // need instance restart, RESTART to allow actions that can be applied without
    // instance replacing or REPLACE to allow all possible actions. If the Updater
    // determines that the minimal update action needed is more disruptive than most
    // disruptive allowed action you specify it will not perform the update at all.
    optional string most_disruptive_allowed_action = 7 [json_name = "most_disruptive_allowed_action"];

    // The instance replacement method for regional managed instance groups. Valid
    // values are: "RECREATE", "SUBSTITUTE". If SUBSTITUTE (default), the group replaces
    // VM instances with new instances that have randomly generated names. If RECREATE,
    // instance names are preserved.  You must also set max_unavailable_fixed or
    // max_unavailable_percent to be greater than 0.
    optional string replacement_method = 8 [json_name = "replacement_method"];

    // The type of update process. You can specify either PROACTIVE so that the instance
    // group manager proactively executes actions in order to bring instances to their
    // target versions or OPPORTUNISTIC so that no action is proactively executed but
    // the update will be performed as part of other actions (for example, resizes or
    // recreateInstances calls).
    optional string type = 9;
  }

  message Version {
    // The full URL to an instance template from which all new instances of this version
    // will be created.
    optional string instance_template = 1 [json_name = "instance_template"];

    // Version name.
    optional string name = 2;

    TargetSize target_size = 3 [json_name = "target_size"];

    message TargetSize {
      // The number of instances which are managed for this version. Conflicts with
      // percent.
      optional int64 fixed = 1;

      // The number of instances (calculated as percentage) which are managed for this
      // version. Conflicts with fixed. Note that when using percent, rounding will be in
      // favor of explicitly set target_size values; a managed instance group with 2
      // instances and 2 versions, one of which has a target_size.percent of 60 will
      // create 2 instances of that version.
      optional int64 percent = 2;
    }
  }
}

// GoogleComputeRegionInstanceTemplate version is 1
message GoogleComputeRegionInstanceTemplate {
  // Whether to allow sending and receiving of packets with non-matching source or
  // destination IPs. This defaults to false.
  optional bool can_ip_forward = 1 [json_name = "can_ip_forward"];

  // The time at which the instance was created in RFC 3339 format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // A brief description of this resource.
  optional string description = 4;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 5 [json_name = "effective_labels"];

  optional string id = 6;

  // A description of the instance.
  optional string instance_description = 7 [json_name = "instance_description"];

  // Action to be taken when a customer's encryption key is revoked. Supports "STOP"
  // and "NONE", with "NONE" being the default.
  optional string key_revocation_action_type = 8 [json_name = "key_revocation_action_type"];

  // A set of key/value label pairs to assign to instances created from this template,
  //
  //
  // 				**Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // 				Please refer to the field 'effective_labels' for all of the labels present on
  // the resource.
  map<string, string> labels = 9;

  // The machine type to create. To create a machine with a custom type (such as
  // extended memory), format the value like custom-VCPUS-MEM_IN_MB like
  // custom-6-20480 for 6 vCPU and 20GB of RAM.
  optional string machine_type = 10 [json_name = "machine_type"];

  // Metadata key/value pairs to make available from within instances created from
  // this template.
  map<string, string> metadata = 11;

  // The unique fingerprint of the metadata.
  optional string metadata_fingerprint = 12 [json_name = "metadata_fingerprint"];

  // An alternative to using the startup-script metadata key, mostly to match the
  // compute_instance resource. This replaces the startup-script metadata key on the
  // created instance and thus the two mechanisms are not allowed to be used
  // simultaneously.
  optional string metadata_startup_script = 13 [json_name = "metadata_startup_script"];

  // Specifies a minimum CPU platform. Applicable values are the friendly names of CPU
  // platforms, such as Intel Haswell or Intel Skylake.
  optional string min_cpu_platform = 14 [json_name = "min_cpu_platform"];

  // The name of the instance template. If you leave this blank, Terraform will
  // auto-generate a unique name.
  optional string name = 15;

  // Creates a unique name beginning with the specified prefix. Conflicts with name.
  optional string name_prefix = 16 [json_name = "name_prefix"];

  // The ID of the template in numeric format.
  optional string numeric_id = 17 [json_name = "numeric_id"];

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 18;

  // The region in which the instance template is located. If it is not provided, the
  // provider region is used.
  optional string region = 19;

  // A map of resource manager tags.
  //
  // 				Resource manager tag keys and values have the same definition as resource
  // manager tags. Keys must be in the format tagKeys/{tag_key_id}, and values are in
  // the format tagValues/456. The field is ignored (both PUT & PATCH) when empty.
  map<string, string> resource_manager_tags = 20 [json_name = "resource_manager_tags"];

  // A list of self_links of resource policies to attach to the instance. Currently a
  // max of 1 resource policy is supported.
  repeated string resource_policies = 21 [json_name = "resource_policies"];

  // The URI of the created resource.
  optional string self_link = 22 [json_name = "self_link"];

  // Tags to attach to the instance.
  repeated string tags = 23;

  // The unique fingerprint of the tags.
  optional string tags_fingerprint = 24 [json_name = "tags_fingerprint"];

  // The combination of labels configured directly on the resource and default labels
  // configured on the provider.
  map<string, string> terraform_labels = 25 [json_name = "terraform_labels"];

  AdvancedMachineFeatures advanced_machine_features = 26 [json_name = "advanced_machine_features"];

  ConfidentialInstanceConfig confidential_instance_config = 27 [json_name = "confidential_instance_config"];

  repeated Disk disk = 28;

  repeated GuestAccelerator guest_accelerator = 29 [json_name = "guest_accelerator"];

  repeated NetworkInterface network_interface = 30 [json_name = "network_interface"];

  NetworkPerformanceConfig network_performance_config = 31 [json_name = "network_performance_config"];

  ReservationAffinity reservation_affinity = 32 [json_name = "reservation_affinity"];

  Scheduling scheduling = 33;

  ServiceAccount service_account = 34 [json_name = "service_account"];

  ShieldedInstanceConfig shielded_instance_config = 35 [json_name = "shielded_instance_config"];

  repeated Timeouts timeouts = 36;

  WorkloadIdentityConfig workload_identity_config = 37 [json_name = "workload_identity_config"];

  google.protobuf.Value for_each = 38 [json_name = "for_each"];

  repeated string depends_on = 39 [json_name = "depends_on"];

  optional int32 count = 40;

  optional string provider = 41;

  terraform.v1.Lifecycle lifecycle = 42;

  message AdvancedMachineFeatures {
    // Whether to enable nested virtualization or not.
    optional bool enable_nested_virtualization = 1 [json_name = "enable_nested_virtualization"];

    // Whether to enable UEFI networking or not.
    optional bool enable_uefi_networking = 2 [json_name = "enable_uefi_networking"];

    // The PMU is a hardware component within the CPU core that monitors how the
    // processor runs code. Valid values for the level of PMU are "STANDARD",
    // "ENHANCED", and "ARCHITECTURAL".
    optional string performance_monitoring_unit = 3 [json_name = "performance_monitoring_unit"];

    // The number of threads per physical core. To disable simultaneous multithreading
    // (SMT) set this to 1. If unset, the maximum number of threads supported per core
    // by the underlying processor is assumed.
    optional int64 threads_per_core = 4 [json_name = "threads_per_core"];

    // Turbo frequency mode to use for the instance. Currently supported modes is
    // "ALL_CORE_MAX".
    optional string turbo_mode = 5 [json_name = "turbo_mode"];

    // The number of physical cores to expose to an instance. Multiply by the number of
    // threads per core to compute the total number of virtual CPUs to expose to the
    // instance. If unset, the number of cores is inferred from the instance\'s nominal
    // CPU count and the underlying platform\'s SMT width.
    optional int64 visible_core_count = 6 [json_name = "visible_core_count"];
  }

  message ConfidentialInstanceConfig {
    // 								The confidential computing technology the instance uses.
    //
    // 								SEV is an AMD feature. TDX is an Intel feature. One of the following
    //
    // 								values is required: SEV, SEV_SNP, TDX. If SEV_SNP, min_cpu_platform =
    //
    // 								"AMD Milan" is currently required.
    optional string confidential_instance_type = 1 [json_name = "confidential_instance_type"];

    // Defines whether the instance should have confidential compute enabled. Field will
    // be deprecated in a future release.
    optional bool enable_confidential_compute = 2 [json_name = "enable_confidential_compute"];
  }

  message Disk {
    // The architecture of the image. Allowed values are ARM64 or X86_64.
    optional string architecture = 1;

    // Whether or not the disk should be auto-deleted. This defaults to true.
    optional bool auto_delete = 2 [json_name = "auto_delete"];

    // Indicates that this is a boot disk.
    optional bool boot = 3;

    // A unique device name that is reflected into the /dev/ tree of a Linux operating
    // system running within the instance. If not specified, the server chooses a
    // default device name to apply to this disk.
    optional string device_name = 4 [json_name = "device_name"];

    // Name of the disk. When not provided, this defaults to the name of the instance.
    optional string disk_name = 5 [json_name = "disk_name"];

    // The size of the image in gigabytes. If not specified, it will inherit the size of
    // its base image. For SCRATCH disks, the size must be one of 375, 3000, 3500, 7000
    // or 14000 GB, with a default of 375 GB.
    optional int64 disk_size_gb = 6 [json_name = "disk_size_gb"];

    // The Google Compute Engine disk type. Such as "pd-ssd", "local-ssd", "pd-balanced"
    // or "pd-standard".
    optional string disk_type = 7 [json_name = "disk_type"];

    // A list of features to enable on the guest operating system. Applicable only for
    // bootable images.
    repeated string guest_os_features = 8 [json_name = "guest_os_features"];

    // Specifies the disk interface to use for attaching this disk.
    optional string interface = 9;

    // A set of key/value label pairs to assign to disks,
    map<string, string> labels = 10;

    // The mode in which to attach this disk, either READ_WRITE or READ_ONLY. If you are
    // attaching or creating a boot disk, this must read-write mode.
    optional string mode = 11;

    // Indicates how many IOPS to provision for the disk. This sets the number of I/O
    // operations per second that the disk can handle. For more details, see the
    // [Extreme persistent disk
    // cumentation](https://cloud.google.com/compute/docs/disks/extreme-persistent-disk)
    // or the [Hyperdisk
    // documentation](https://cloud.google.com/compute/docs/disks/hyperdisks) depending
    // on the selected disk_type.
    optional int64 provisioned_iops = 12 [json_name = "provisioned_iops"];

    // Indicates how much throughput to provision for the disk, in MB/s. This sets the
    // amount of data that can be read or written from the disk per second. Values must
    // greater than or equal to 1. For more details, see the [Hyperdisk
    // documentation](https://cloud.google.com/compute/docs/disks/hyperdisks).
    optional int64 provisioned_throughput = 13 [json_name = "provisioned_throughput"];

    // A map of resource manager tags. Resource manager tag keys and values have the
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is
    // ignored (both PUT & PATCH) when empty.
    map<string, string> resource_manager_tags = 14 [json_name = "resource_manager_tags"];

    // A list (short name or id) of resource policies to attach to this disk. Currently
    // a max of 1 resource policy is supported.
    repeated string resource_policies = 15 [json_name = "resource_policies"];

    // The name (not self_link) of the disk (such as those managed by
    // google_compute_disk) to attach. ~> Note: Either source or source_image is
    // required when creating a new instance except for when creating a local SSD.
    optional string source = 16;

    // The image from which to initialize this disk. This can be one of: the image's
    // self_link, projects/{project}/global/images/{image},
    // projects/{project}/global/images/family/{family}, global/images/{image},
    // global/images/family/{family}, family/{family}, {project}/{family},
    // {project}/{image}, {family}, or {image}. ~> Note: Either source or source_image
    // is required when creating a new instance except for when creating a local SSD.
    optional string source_image = 17 [json_name = "source_image"];

    // The source snapshot to create this disk. When creating
    //
    // a new instance, one of initializeParams.sourceSnapshot,
    //
    // initializeParams.sourceImage, or disks.source is
    //
    // required except for local SSD.
    optional string source_snapshot = 18 [json_name = "source_snapshot"];

    // The self_link or ID of the Storage Pool to create this disk in.
    optional string storage_pool = 19 [json_name = "storage_pool"];

    // The type of Google Compute Engine disk, can be either "SCRATCH" or "PERSISTENT".
    optional string type = 20;

    DiskEncryptionKey disk_encryption_key = 21 [json_name = "disk_encryption_key"];

    SourceImageEncryptionKey source_image_encryption_key = 22 [json_name = "source_image_encryption_key"];

    SourceSnapshotEncryptionKey source_snapshot_encryption_key = 23 [json_name = "source_snapshot_encryption_key"];

    message DiskEncryptionKey {
      // The self link of the encryption key that is stored in Google Cloud KMS.
      optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption request for the given KMS key.
      // If absent, the Compute Engine default service account is used.
      optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];
    }

    message SourceImageEncryptionKey {
      // The self link of the encryption key that is stored in
      //
      // Google Cloud KMS. Only one of kms_key_self_link, rsa_encrypted_key and raw_key
      // may be set.
      optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption
      //
      // request for the given KMS key. If absent, the Compute
      //
      // Engine default service account is used.
      optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

      // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
      // to either encrypt or decrypt this resource.  Only one of kms_key_self_link,
      // rsa_encrypted_key and raw_key may be set.
      optional string raw_key = 3 [json_name = "raw_key"];

      // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
      // encryption key to either encrypt or decrypt this resource.  Only one of
      // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
      optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
    }

    message SourceSnapshotEncryptionKey {
      // The self link of the encryption key that is stored in
      //
      // Google Cloud KMS. Only one of kms_key_self_link, rsa_encrypted_key and raw_key
      // may be set.
      optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

      // The service account being used for the encryption
      //
      // request for the given KMS key. If absent, the Compute
      //
      // Engine default service account is used.
      optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

      // Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64
      // to either encrypt or decrypt this resource. Only one of kms_key_self_link,
      // rsa_encrypted_key and raw_key may be set.
      optional string raw_key = 3 [json_name = "raw_key"];

      // Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied
      // encryption key to either encrypt or decrypt this resource.  Only one of
      // kms_key_self_link, rsa_encrypted_key and raw_key may be set.
      optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
    }
  }

  message GuestAccelerator {
    // The number of the guest accelerator cards exposed to this instance.
    optional int64 count = 1;

    // The accelerator type resource to expose to this instance. E.g. nvidia-tesla-k80.
    optional string type = 2;
  }

  message NetworkInterface {
    // Indicates whether igmp query is enabled on the network interface or not. If
    // enabled, also indicates the version of IGMP supported.
    optional string igmp_query = 1 [json_name = "igmp_query"];

    // The prefix length of the primary internal IPv6 range.
    optional int64 internal_ipv6_prefix_length = 2 [json_name = "internal_ipv6_prefix_length"];

    // One of EXTERNAL, INTERNAL to indicate whether the IP can be accessed from the
    // Internet. This field is always inherited from its subnetwork.
    optional string ipv6_access_type = 3 [json_name = "ipv6_access_type"];

    // An IPv6 internal network address for this network interface. If not specified,
    // Google Cloud will automatically assign an internal IPv6 address from the
    // instance's subnetwork.
    optional string ipv6_address = 4 [json_name = "ipv6_address"];

    // The name of the network_interface.
    optional string name = 5;

    // The name or self_link of the network to attach this interface to. Use network
    // attribute for Legacy or Auto subnetted networks and subnetwork for custom
    // subnetted networks.
    optional string network = 6;

    // The URL of the network attachment that this interface should connect to in the
    // following format:
    // rojectNumber}/regions/{region_name}/networkAttachments/{network_attachment_name}.
    optional string network_attachment = 7 [json_name = "network_attachment"];

    // The private IP address to assign to the instance. If empty, the address will be
    // automatically assigned.
    optional string network_ip = 8 [json_name = "network_ip"];

    // The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET,
    // MRDMA, IRDMA and IDPF
    optional string nic_type = 9 [json_name = "nic_type"];

    // Name of the parent network interface of a dynamic network interface.
    optional string parent_nic_name = 10 [json_name = "parent_nic_name"];

    // The networking queue count that's specified by users for the network interface.
    // Both Rx and Tx queues will be set to this number. It will be empty if not
    // specified.
    optional int64 queue_count = 11 [json_name = "queue_count"];

    // The stack type for this network interface to identify whether the IPv6 feature is
    // enabled or not. If not specified, IPV4_ONLY will be used.
    optional string stack_type = 12 [json_name = "stack_type"];

    // The name of the subnetwork to attach this interface to. The subnetwork must exist
    // in the same region this instance will be created in. Either network or subnetwork
    // must be provided.
    optional string subnetwork = 13;

    // The ID of the project in which the subnetwork belongs. If it is not provided, the
    // provider project is used.
    optional string subnetwork_project = 14 [json_name = "subnetwork_project"];

    // VLAN tag of a dynamic network interface, must be an integer in the range from 2
    // to 255 inclusively.
    optional int64 vlan = 15;

    repeated AccessConfig access_config = 16 [json_name = "access_config"];

    repeated AliasIpRange alias_ip_range = 17 [json_name = "alias_ip_range"];

    repeated Ipv6AccessConfig ipv6_access_config = 18 [json_name = "ipv6_access_config"];

    message AccessConfig {
      // The IP address that will be 1:1 mapped to the instance's network ip. If not
      // given, one will be generated.
      optional string nat_ip = 1 [json_name = "nat_ip"];

      // The networking tier used for configuring this instance template. This field can
      // take the following values: PREMIUM, STANDARD, FIXED_STANDARD. If this field is
      // not specified, it is assumed to be PREMIUM.
      optional string network_tier = 2 [json_name = "network_tier"];

      // The DNS domain name for the public PTR record.The DNS domain name for the public
      // PTR record.
      optional string public_ptr_domain_name = 3 [json_name = "public_ptr_domain_name"];
    }

    message AliasIpRange {
      // The IP CIDR range represented by this alias IP range. This IP CIDR range must
      // belong to the specified subnetwork and cannot contain IP addresses reserved by
      // system or used by other network interfaces. At the time of writing only a netmask
      // (e.g. /24) may be supplied, with a CIDR format resulting in an API error.
      optional string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

      // The subnetwork secondary range name specifying the secondary range from which to
      // allocate the IP CIDR range for this alias IP range. If left unspecified, the
      // primary range of the subnetwork will be used.
      optional string subnetwork_range_name = 2 [json_name = "subnetwork_range_name"];
    }

    message Ipv6AccessConfig {
      // The first IPv6 address of the external IPv6 range associated with this instance,
      // prefix length is stored in externalIpv6PrefixLength in ipv6AccessConfig. The
      // field is output only, an IPv6 address from a subnetwork associated with the
      // instance will be allocated dynamically.
      optional string external_ipv6 = 1 [json_name = "external_ipv6"];

      // The prefix length of the external IPv6 range.
      optional string external_ipv6_prefix_length = 2 [json_name = "external_ipv6_prefix_length"];

      // The name of this access configuration.
      optional string name = 3;

      // The service-level to be provided for IPv6 traffic when the subnet has an external
      // subnet. Only PREMIUM tier is valid for IPv6
      optional string network_tier = 4 [json_name = "network_tier"];

      // The domain name to be used when creating DNSv6 records for the external IPv6
      // ranges.
      optional string public_ptr_domain_name = 5 [json_name = "public_ptr_domain_name"];
    }
  }

  message NetworkPerformanceConfig {
    // The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT
    optional string total_egress_bandwidth_tier = 1 [json_name = "total_egress_bandwidth_tier"];
  }

  message ReservationAffinity {
    // The type of reservation from which this instance can consume resources.
    optional string type = 1;

    SpecificReservation specific_reservation = 2 [json_name = "specific_reservation"];

    message SpecificReservation {
      // Corresponds to the label key of a reservation resource. To target a
      // SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as
      // the key and specify the name of your reservation as the only value.
      optional string key = 1;

      // Corresponds to the label values of a reservation resource.
      repeated string values = 2;
    }
  }

  message Scheduling {
    // Specifies whether the instance should be automatically restarted if it is
    // terminated by Compute Engine (not terminated by a user). This defaults to true.
    optional bool automatic_restart = 1 [json_name = "automatic_restart"];

    // Specifies the availability domain, which this instance should be scheduled on.
    optional int64 availability_domain = 2 [json_name = "availability_domain"];

    // Specify the time in seconds for host error detection, the value must be within
    // the range of [90, 330] with the increment of 30, if unset, the default behavior
    // of host error recovery will be used.
    optional int64 host_error_timeout_seconds = 3 [json_name = "host_error_timeout_seconds"];

    // Specifies the action GCE should take when SPOT VM is preempted.
    optional string instance_termination_action = 4 [json_name = "instance_termination_action"];

    // Minimum number of cpus for the instance.
    optional int64 min_node_cpus = 5 [json_name = "min_node_cpus"];

    // Defines the maintenance behavior for this instance.
    optional string on_host_maintenance = 6 [json_name = "on_host_maintenance"];

    // Allows instance to be preempted. This defaults to false.
    optional bool preemptible = 7;

    // Describes the desired provisioning model for the instance. Possible values are
    // STANDARD, SPOT, FLEX_START, and RESERVATION_BOUND. For STANDARD, resources are
    // provisioned immediately. For SPOT, resources are offered at a discount compared
    // to standard pricing but may be preempted. For FLEX_START, resources are offered
    // at a discount with flexible start times. For RESERVATION_BOUND, the instance is
    // bound to a specific reservation and will only consume capacity from that
    // reservation.
    optional string provisioning_model = 8 [json_name = "provisioning_model"];

    // Specifies the timestamp, when the instance will be terminated,
    //
    // in RFC3339 text format. If specified, the instance termination action
    //
    // will be performed at the termination time.
    optional string termination_time = 9 [json_name = "termination_time"];

    repeated LocalSsdRecoveryTimeout local_ssd_recovery_timeout = 10 [json_name = "local_ssd_recovery_timeout"];

    MaxRunDuration max_run_duration = 11 [json_name = "max_run_duration"];

    repeated NodeAffinities node_affinities = 12 [json_name = "node_affinities"];

    OnInstanceStopAction on_instance_stop_action = 13 [json_name = "on_instance_stop_action"];

    message LocalSsdRecoveryTimeout {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message MaxRunDuration {
      // Span of time that's a fraction of a second at nanosecond
      //
      // resolution. Durations less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must
      //
      // be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second.
      //
      // Must be from 0 to 315,576,000,000 inclusive.
      optional int64 seconds = 2;
    }

    message NodeAffinities {
      optional string key = 1;

      optional string operator = 2;

      repeated string values = 3;
    }

    message OnInstanceStopAction {
      // If true, the contents of any attached Local SSD disks will be discarded.
      optional bool discard_local_ssd = 1 [json_name = "discard_local_ssd"];
    }
  }

  message ServiceAccount {
    // The service account e-mail address. If not given, the default Google Compute
    // Engine service account is used.
    optional string email = 1;

    // A list of service scopes. Both OAuth2 URLs and gcloud short names are supported.
    // To allow full access to all Cloud APIs, use the cloud-platform scope.
    repeated string scopes = 2;
  }

  message ShieldedInstanceConfig {
    // Compare the most recent boot measurements to the integrity policy baseline and
    // return a pair of pass/fail results depending on whether they match or not.
    // Defaults to true.
    optional bool enable_integrity_monitoring = 1 [json_name = "enable_integrity_monitoring"];

    // Verify the digital signature of all boot components, and halt the boot process if
    // signature verification fails. Defaults to false.
    optional bool enable_secure_boot = 2 [json_name = "enable_secure_boot"];

    // Use a virtualized trusted platform module, which is a specialized computer chip
    // you can use to encrypt objects like keys and certificates. Defaults to true.
    optional bool enable_vtpm = 3 [json_name = "enable_vtpm"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }

  message WorkloadIdentityConfig {
    // Identity SPIFFE id.
    optional string identity = 1;

    // Specifies whether identity certificates are enabled.
    optional bool identity_certificate_enabled = 2 [json_name = "identity_certificate_enabled"];
  }
}

// GoogleComputeRegionInstantSnapshot version is 0
message GoogleComputeRegionInstantSnapshot {
  // The architecture of the instant snapshot.
  optional string architecture = 1;

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  // Size of the snapshot, specified in GB.
  optional int64 disk_size_gb = 5 [json_name = "disk_size_gb"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 6 [json_name = "effective_labels"];

  optional string id = 7;

  // The fingerprint used for optimistic locking of this resource. Used
  //
  // internally during updates.
  optional string label_fingerprint = 8 [json_name = "label_fingerprint"];

  // Labels to apply to this InstantSnapshot.
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 9;

  // Name of the resource; provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 10;

  optional string project = 11;

  // A reference to the region where the disk is located.
  optional string region = 12;

  // Resource status for the instant snapshot.
  repeated ResourceStatus resource_status = 13 [json_name = "resource_status"];

  optional string self_link = 14 [json_name = "self_link"];

  // The source disk used to create this instant snapshot. You can provide this as a
  // partial or full URL to the resource.
  optional string source_disk = 15 [json_name = "source_disk"];

  // The ID value of the disk used to create this InstantSnapshot.
  optional string source_disk_id = 16 [json_name = "source_disk_id"];

  // URL of the source instant snapshot group.
  optional string source_instant_snapshot_group = 17 [json_name = "source_instant_snapshot_group"];

  // The unique ID of the source instant snapshot group.
  optional string source_instant_snapshot_group_id = 18 [json_name = "source_instant_snapshot_group_id"];

  // The status of the instant snapshot.
  optional string status = 19;

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 20 [json_name = "terraform_labels"];

  Params params = 21;

  repeated Timeouts timeouts = 22;

  google.protobuf.Value for_each = 23 [json_name = "for_each"];

  repeated string depends_on = 24 [json_name = "depends_on"];

  optional int32 count = 25;

  optional string provider = 26;

  terraform.v1.Lifecycle lifecycle = 27;

  message ResourceStatus {
    // storage_size_bytes: number
    optional int64 storage_size_bytes = 1 [json_name = "storage_size_bytes"];
  }

  message Params {
    // Resource manager tags to be bound to the instant snapshot. Tag keys and values
    // have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionInstantSnapshotIamBinding version is 0
message GoogleComputeRegionInstantSnapshotIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string region = 6;

  optional string role = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeRegionInstantSnapshotIamMember version is 0
message GoogleComputeRegionInstantSnapshotIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string region = 6;

  optional string role = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeRegionInstantSnapshotIamPolicy version is 0
message GoogleComputeRegionInstantSnapshotIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  optional string region = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeRegionNetworkEndpoint version is 0
message GoogleComputeRegionNetworkEndpoint {
  // Client destination port for the 'GCE_VM_IP_PORTMAP' NEG.
  optional int64 client_destination_port = 1 [json_name = "client_destination_port"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // Fully qualified domain name of network endpoint.
  //
  //
  // This can only be specified when network_endpoint_type of the NEG is
  // INTERNET_FQDN_PORT.
  optional string fqdn = 3;

  optional string id = 4;

  // The name for a specific VM instance that the IP address belongs to.
  //
  // This is required for network endpoints of type GCE_VM_IP_PORTMAP.
  optional string instance = 5;

  // IPv4 address external endpoint.
  //
  //
  // This can only be specified when network_endpoint_type of the NEG is
  // INTERNET_IP_PORT.
  optional string ip_address = 6 [json_name = "ip_address"];

  // The unique identifier number for the resource. This identifier is defined by the
  // server.
  optional int64 network_endpoint_id = 7 [json_name = "network_endpoint_id"];

  // Port number of network endpoint.
  optional int64 port = 8;

  optional string project = 9;

  // Region where the containing network endpoint group is located.
  optional string region = 10;

  // The network endpoint group this endpoint is part of.
  optional string region_network_endpoint_group = 11 [json_name = "region_network_endpoint_group"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionNetworkEndpointGroup version is 0
message GoogleComputeRegionNetworkEndpointGroup {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 2;

  optional string id = 3;

  // Name of the resource; provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 4;

  // This field is only used for PSC and INTERNET NEGs.
  //
  //
  // The URL of the network to which all network endpoints in the NEG belong. Uses
  //
  // "default" project network if unspecified.
  optional string network = 5;

  // Type of network endpoints in this network endpoint group. Defaults to SERVERLESS.
  // Default value: "SERVERLESS" Possible values: ["SERVERLESS",
  // "PRIVATE_SERVICE_CONNECT", "INTERNET_IP_PORT", "INTERNET_FQDN_PORT",
  // "GCE_VM_IP_PORTMAP"]
  optional string network_endpoint_type = 6 [json_name = "network_endpoint_type"];

  optional string project = 7;

  // This field is only used for PSC and INTERNET NEGs.
  //
  //
  // The target service url used to set up private service connection to
  //
  // a Google API or a PSC Producer Service Attachment.
  optional string psc_target_service = 8 [json_name = "psc_target_service"];

  // A reference to the region where the regional NEGs reside.
  optional string region = 9;

  optional string self_link = 10 [json_name = "self_link"];

  // This field is only used for PSC NEGs.
  //
  //
  // Optional URL of the subnetwork to which all network endpoints in the NEG belong.
  optional string subnetwork = 11;

  AppEngine app_engine = 12 [json_name = "app_engine"];

  CloudFunction cloud_function = 13 [json_name = "cloud_function"];

  CloudRun cloud_run = 14 [json_name = "cloud_run"];

  PscData psc_data = 15 [json_name = "psc_data"];

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message AppEngine {
    // Optional serving service.
    //
    // The service name must be 1-63 characters long, and comply with RFC1035.
    //
    // Example value: "default", "my-service".
    optional string service = 1;

    // A template to parse service and version fields from a request URL.
    //
    // URL mask allows for routing to multiple App Engine services without
    //
    // having to create multiple Network Endpoint Groups and backend services.
    //
    //
    // For example, the request URLs "foo1-dot-appname.appspot.com/v1" and
    //
    // "foo1-dot-appname.appspot.com/v2" can be backed by the same Serverless NEG with
    //
    // URL mask "-dot-appname.appspot.com/". The URL mask will parse
    //
    // them to { service = "foo1", version = "v1" } and { service = "foo1", version =
    // "v2" } respectively.
    optional string url_mask = 2 [json_name = "url_mask"];

    // Optional serving version.
    //
    // The version must be 1-63 characters long, and comply with RFC1035.
    //
    // Example value: "v1", "v2".
    optional string version = 3;
  }

  message CloudFunction {
    // A user-defined name of the Cloud Function.
    //
    // The function name is case-sensitive and must be 1-63 characters long.
    //
    // Example value: "func1".
    optional string function = 1;

    // A template to parse function field from a request URL. URL mask allows
    //
    // for routing to multiple Cloud Functions without having to create
    //
    // multiple Network Endpoint Groups and backend services.
    //
    //
    // For example, request URLs "mydomain.com/function1" and "mydomain.com/function2"
    //
    // can be backed by the same Serverless NEG with URL mask "/". The URL mask
    //
    // will parse them to { function = "function1" } and { function = "function2" }
    // respectively.
    optional string url_mask = 2 [json_name = "url_mask"];
  }

  message CloudRun {
    // Cloud Run service is the main resource of Cloud Run.
    //
    // The service must be 1-63 characters long, and comply with RFC1035.
    //
    // Example value: "run-service".
    optional string service = 1;

    // Cloud Run tag represents the "named-revision" to provide
    //
    // additional fine-grained traffic routing information.
    //
    // The tag must be 1-63 characters long, and comply with RFC1035.
    //
    // Example value: "revision-0010".
    optional string tag = 2;

    // A template to parse service and tag fields from a request URL.
    //
    // URL mask allows for routing to multiple Run services without having
    //
    // to create multiple network endpoint groups and backend services.
    //
    //
    // For example, request URLs "foo1.domain.com/bar1" and "foo1.domain.com/bar2"
    //
    // an be backed by the same Serverless Network Endpoint Group (NEG) with
    //
    // URL mask ".domain.com/". The URL mask will parse them to { service="bar1",
    // tag="foo1" }
    //
    // and { service="bar2", tag="foo2" } respectively.
    optional string url_mask = 3 [json_name = "url_mask"];
  }

  message PscData {
    // The PSC producer port to use when consumer PSC NEG connects to a producer. If
    //
    // this flag isn't specified for a PSC NEG with endpoint type
    //
    // private-service-connect, then PSC NEG will be connected to a first port in the
    //
    // available PSC producer port range.
    optional string producer_port = 1 [json_name = "producer_port"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionNetworkFirewallPolicy version is 0
message GoogleComputeRegionNetworkFirewallPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 3;

  // Fingerprint of the resource. This field is used internally during updates of this
  // resource.
  optional string fingerprint = 4;

  optional string id = 5;

  // User-provided name of the Network firewall policy. The name should be unique in
  // the project in which the firewall policy is created. The name must be 1-63
  // characters long, and comply with RFC1035. Specifically, the name must be 1-63
  // characters long and match the regular expression [a-z]([-a-z0-9]*[a-z0-9])? which
  // means the first character must be a lowercase letter, and all following
  // characters must be a dash, lowercase letter, or digit, except the last character,
  // which cannot be a dash.
  optional string name = 6;

  // Policy type is used to determine which resources (networks) the policy can be
  // associated with.
  //
  // A policy can be associated with a network only if the network has the matching
  // policyType in its network profile.
  //
  // Different policy types may support some of the Firewall Rules features. Possible
  // values: ["VPC_POLICY", "RDMA_ROCE_POLICY", "RDMA_FALCON_POLICY", "ULL_POLICY"]
  optional string policy_type = 7 [json_name = "policy_type"];

  optional string project = 8;

  // The region of this resource.
  optional string region = 9;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string region_network_firewall_policy_id = 10 [json_name = "region_network_firewall_policy_id"];

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed
  // a set number of tuples.
  optional int64 rule_tuple_count = 11 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  optional string self_link = 12 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  optional string self_link_with_id = 13 [json_name = "self_link_with_id"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionNetworkFirewallPolicyAssociation version is 0
message GoogleComputeRegionNetworkFirewallPolicyAssociation {
  // The target that the firewall policy is attached to.
  optional string attachment_target = 1 [json_name = "attachment_target"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // The firewall policy of the resource.
  optional string firewall_policy = 3 [json_name = "firewall_policy"];

  optional string id = 4;

  // The name for an association.
  optional string name = 5;

  optional string project = 6;

  // The location of this resource.
  optional string region = 7;

  // The short name of the firewall policy of the association.
  optional string short_name = 8 [json_name = "short_name"];

  repeated Timeouts timeouts = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  optional int32 count = 12;

  optional string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionNetworkFirewallPolicyIamBinding version is 0
message GoogleComputeRegionNetworkFirewallPolicyIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string region = 6;

  optional string role = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeRegionNetworkFirewallPolicyIamMember version is 0
message GoogleComputeRegionNetworkFirewallPolicyIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string region = 6;

  optional string role = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeRegionNetworkFirewallPolicyIamPolicy version is 0
message GoogleComputeRegionNetworkFirewallPolicyIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  optional string region = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeRegionNetworkFirewallPolicyRule version is 0
message GoogleComputeRegionNetworkFirewallPolicyRule {
  // The Action to perform when the client connection triggers the rule. Valid actions
  // are "allow", "deny", "goto_next" and "apply_security_profile_group".
  optional string action = 1;

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description for this resource.
  optional string description = 4;

  // The direction in which this rule applies. Possible values: ["INGRESS", "EGRESS"]
  optional string direction = 5;

  // Denotes whether the firewall policy rule is disabled.
  //
  // When set to true, the firewall policy rule is not enforced and traffic behaves as
  // if it did not exist.
  //
  // If this is unspecified, the firewall policy rule will be enabled.
  optional bool disabled = 6;

  // Denotes whether to enable logging for a particular rule.
  //
  // If logging is enabled, logs will be exported to the configured export destination
  // in Stackdriver.
  //
  // Logs may be exported to BigQuery or Pub/Sub.
  //
  // Note: you cannot enable logging on "goto_next" rules.
  optional bool enable_logging = 7 [json_name = "enable_logging"];

  // The firewall policy of the resource.
  optional string firewall_policy = 8 [json_name = "firewall_policy"];

  optional string id = 9;

  // Type of the resource. Always 'compute#firewallPolicyRule' for firewall policy
  // rules
  optional string kind = 10;

  // An integer indicating the priority of a rule in the list.
  //
  // The priority must be a positive value between 0 and 2147483647.
  //
  // Rules are evaluated from highest to lowest priority where 0 is the highest
  // priority and 2147483647 is the lowest prority.
  optional int64 priority = 11;

  optional string project = 12;

  // The location of this resource.
  optional string region = 13;

  // An optional name for the rule. This field is not a unique identifier and can be
  // updated.
  optional string rule_name = 14 [json_name = "rule_name"];

  // Calculation of the complexity of a single firewall policy rule.
  optional int64 rule_tuple_count = 15 [json_name = "rule_tuple_count"];

  // A fully-qualified URL of a SecurityProfile resource instance.
  //
  // Example:
  // ts/{project}/locations/{location}/securityProfileGroups/my-security-profile-group
  //
  // Must be specified if action = 'apply_security_profile_group' and cannot be
  // specified for other actions.
  //
  //
  // Security Profile Group and Firewall Policy Rule must be in the same scope.
  optional string security_profile_group = 16 [json_name = "security_profile_group"];

  // A list of forwarding rules to which this rule applies.
  //
  // This field allows you to control which load balancers get this rule.
  //
  // For example, the following are valid values:
  //
  // .googleapis.com/compute/v1/projects/project/global/forwardingRules/forwardingRule
  //
  // pis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule
  //
  // - projects/project/global/forwardingRules/forwardingRule
  //
  // - projects/project/regions/region/forwardingRules/forwardingRule
  repeated string target_forwarding_rules = 17 [json_name = "target_forwarding_rules"];

  // A list of service accounts indicating the sets of instances that are applied with
  // this rule.
  repeated string target_service_accounts = 18 [json_name = "target_service_accounts"];

  // Target types of the firewall policy rule.
  //
  // Default value is INSTANCES.
  //
  // When target_type is INTERNAL_MANAGED_LB, target_forwarding_rules must be set
  // Possible values: ["INSTANCES", "INTERNAL_MANAGED_LB"]
  optional string target_type = 19 [json_name = "target_type"];

  // Boolean flag indicating if the traffic should be TLS decrypted.
  //
  // Can be set only if action = 'apply_security_profile_group' and cannot be set for
  // other actions.
  optional bool tls_inspect = 20 [json_name = "tls_inspect"];

  Match match = 21;

  repeated TargetSecureTags target_secure_tags = 22 [json_name = "target_secure_tags"];

  repeated Timeouts timeouts = 23;

  google.protobuf.Value for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  optional int32 count = 26;

  optional string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message Match {
    // Address groups which should be matched against the traffic destination. Maximum
    // number of destination address groups is 10.
    repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

    // Fully Qualified Domain Name (FQDN) which should be matched against traffic
    // destination. Maximum number of destination fqdn allowed is 100.
    repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

    // CIDR IP address range. Maximum number of destination CIDR IP ranges allowed is
    // 5000.
    repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

    // Network context of the traffic destination. Possible values: ["UNSPECIFIED",
    // "INTERNET", "INTRA_VPC", "NON_INTERNET", "VPC_NETWORKS"]
    optional string dest_network_context = 4 [json_name = "dest_network_context"];

    // Region codes whose IP addresses will be used to match for destination of traffic.
    // Should be specified as 2 letter country code defined as per ISO 3166 alpha-2
    // country codes. ex."US" Maximum number of dest region codes allowed is 5000.
    repeated string dest_region_codes = 5 [json_name = "dest_region_codes"];

    // Names of Network Threat Intelligence lists. The IPs in these lists will be
    // matched against traffic destination.
    repeated string dest_threat_intelligences = 6 [json_name = "dest_threat_intelligences"];

    // Address groups which should be matched against the traffic source. Maximum number
    // of source address groups is 10.
    repeated string src_address_groups = 7 [json_name = "src_address_groups"];

    // Fully Qualified Domain Name (FQDN) which should be matched against traffic
    // source. Maximum number of source fqdn allowed is 100.
    repeated string src_fqdns = 8 [json_name = "src_fqdns"];

    // CIDR IP address range. Maximum number of source CIDR IP ranges allowed is 5000.
    repeated string src_ip_ranges = 9 [json_name = "src_ip_ranges"];

    // Network context of the traffic source. Possible values: ["UNSPECIFIED",
    // "INTERNET", "INTRA_VPC", "NON_INTERNET", "VPC_NETWORKS"]
    optional string src_network_context = 10 [json_name = "src_network_context"];

    // Networks of the traffic source. It can be either a full or partial url.
    repeated string src_networks = 11 [json_name = "src_networks"];

    // Region codes whose IP addresses will be used to match for source of traffic.
    // Should be specified as 2 letter country code defined as per ISO 3166 alpha-2
    // country codes. ex."US" Maximum number of source region codes allowed is 5000.
    repeated string src_region_codes = 12 [json_name = "src_region_codes"];

    // Names of Network Threat Intelligence lists. The IPs in these lists will be
    // matched against traffic source.
    repeated string src_threat_intelligences = 13 [json_name = "src_threat_intelligences"];

    repeated Layer4Configs layer4_configs = 14 [json_name = "layer4_configs"];

    repeated SrcSecureTags src_secure_tags = 15 [json_name = "src_secure_tags"];

    message Layer4Configs {
      // The IP protocol to which this rule applies. The protocol type is required when
      // creating a firewall rule.
      //
      // This value can either be one of the following well known protocol strings (tcp,
      // udp, icmp, esp, ah, ipip, sctp), or the IP protocol number.
      optional string ip_protocol = 1 [json_name = "ip_protocol"];

      // An optional list of ports to which this rule applies. This field is only
      // applicable for UDP or TCP protocol. Each entry must be either an integer or a
      // range. If not specified, this rule applies to connections through any port.
      //
      // Example inputs include: ["22"], ["80","443"], and ["12345-12349"].
      repeated string ports = 2;
    }

    message SrcSecureTags {
      // Name of the secure tag, created with TagManager's TagValue API.
      optional string name = 1;

      // State of the secure tag, either EFFECTIVE or INEFFECTIVE. A secure tag is
      // INEFFECTIVE when it is deleted or its network is deleted.
      optional string state = 2;
    }
  }

  message TargetSecureTags {
    // Name of the secure tag, created with TagManager's TagValue API.
    optional string name = 1;

    // State of the secure tag, either EFFECTIVE or INEFFECTIVE. A secure tag is
    // INEFFECTIVE when it is deleted or its network is deleted.
    optional string state = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionNetworkFirewallPolicyWithRules version is 0
message GoogleComputeRegionNetworkFirewallPolicyWithRules {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Fingerprint of the resource. This field is used internally during updates of this
  // resource.
  optional string fingerprint = 4;

  optional string id = 5;

  // User-provided name of the Network firewall policy.
  //
  // The name should be unique in the project in which the firewall policy is created.
  //
  // The name must be 1-63 characters long, and comply with RFC1035. Specifically,
  //
  // the name must be 1-63 characters long and match the regular expression
  // [a-z]([-a-z0-9]*[a-z0-9])?
  //
  // which means the first character must be a lowercase letter, and all following
  // characters must be a dash,
  //
  // lowercase letter, or digit, except the last character, which cannot be a dash.
  optional string name = 6;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string network_firewall_policy_id = 7 [json_name = "network_firewall_policy_id"];

  // Policy type is used to determine which resources (networks) the policy can be
  // associated with.
  //
  // A policy can be associated with a network only if the network has the matching
  // policyType in its network profile.
  //
  // Different policy types may support some of the Firewall Rules features. Possible
  // values: ["VPC_POLICY", "RDMA_ROCE_POLICY", "RDMA_FALCON_POLICY", "ULL_POLICY"]
  optional string policy_type = 8 [json_name = "policy_type"];

  // A list of firewall policy pre-defined rules.
  repeated PredefinedRules predefined_rules = 9 [json_name = "predefined_rules"];

  optional string project = 10;

  // The region of this resource.
  optional string region = 11;

  // Total count of all firewall policy rule tuples. A firewall policy can not exceed
  // a set number of tuples.
  optional int64 rule_tuple_count = 12 [json_name = "rule_tuple_count"];

  // Server-defined URL for the resource.
  optional string self_link = 13 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  optional string self_link_with_id = 14 [json_name = "self_link_with_id"];

  repeated Rule rule = 15;

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message PredefinedRules {
    // action: string
    optional string action = 1;

    // description: string
    optional string description = 2;

    // direction: string
    optional string direction = 3;

    // disabled: bool
    optional bool disabled = 4;

    // enable_logging: bool
    optional bool enable_logging = 5 [json_name = "enable_logging"];

    // match: list of object
    repeated Match match = 6;

    // priority: number
    optional int64 priority = 7;

    // rule_name: string
    optional string rule_name = 8 [json_name = "rule_name"];

    // security_profile_group: string
    optional string security_profile_group = 9 [json_name = "security_profile_group"];

    // target_secure_tag: list of object
    repeated TargetSecureTag target_secure_tag = 10 [json_name = "target_secure_tag"];

    // target_service_accounts: list of string
    repeated string target_service_accounts = 11 [json_name = "target_service_accounts"];

    // tls_inspect: bool
    optional bool tls_inspect = 12 [json_name = "tls_inspect"];

    message Match {
      // dest_address_groups: list of string
      repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

      // dest_fqdns: list of string
      repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

      // dest_ip_ranges: list of string
      repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

      // dest_region_codes: list of string
      repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

      // dest_threat_intelligences: list of string
      repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

      // layer4_config: list of object
      repeated Layer4Config layer4_config = 6 [json_name = "layer4_config"];

      // src_address_groups: list of string
      repeated string src_address_groups = 7 [json_name = "src_address_groups"];

      // src_fqdns: list of string
      repeated string src_fqdns = 8 [json_name = "src_fqdns"];

      // src_ip_ranges: list of string
      repeated string src_ip_ranges = 9 [json_name = "src_ip_ranges"];

      // src_region_codes: list of string
      repeated string src_region_codes = 10 [json_name = "src_region_codes"];

      // src_secure_tag: list of object
      repeated SrcSecureTag src_secure_tag = 11 [json_name = "src_secure_tag"];

      // src_threat_intelligences: list of string
      repeated string src_threat_intelligences = 12 [json_name = "src_threat_intelligences"];

      message Layer4Config {
        // ip_protocol: string
        optional string ip_protocol = 1 [json_name = "ip_protocol"];

        // ports: list of string
        repeated string ports = 2;
      }

      message SrcSecureTag {
        // name: string
        optional string name = 1;

        // state: string
        optional string state = 2;
      }
    }

    message TargetSecureTag {
      // name: string
      optional string name = 1;

      // state: string
      optional string state = 2;
    }
  }

  message Rule {
    // The Action to perform when the client connection triggers the rule. Can currently
    // be either
    //
    // "allow", "deny", "apply_security_profile_group" or "goto_next".
    optional string action = 1;

    // A description of the rule.
    optional string description = 2;

    // The direction in which this rule applies. If unspecified an INGRESS rule is
    // created. Possible values: ["INGRESS", "EGRESS"]
    optional string direction = 3;

    // Denotes whether the firewall policy rule is disabled. When set to true,
    //
    // the firewall policy rule is not enforced and traffic behaves as if it did
    //
    // not exist. If this is unspecified, the firewall policy rule will be
    //
    // enabled.
    optional bool disabled = 4;

    // Denotes whether to enable logging for a particular rule.
    //
    // If logging is enabled, logs will be exported to the
    //
    // configured export destination in Stackdriver.
    optional bool enable_logging = 5 [json_name = "enable_logging"];

    // An integer indicating the priority of a rule in the list. The priority must be a
    // value
    //
    // between 0 and 2147483647. Rules are evaluated from highest to lowest priority
    // where 0 is the
    //
    // highest priority and 2147483647 is the lowest priority.
    optional int64 priority = 6;

    // An optional name for the rule. This field is not a unique identifier
    //
    // and can be updated.
    optional string rule_name = 7 [json_name = "rule_name"];

    // A fully-qualified URL of a SecurityProfile resource instance.
    //
    // Example:
    //
    // ts/{project}/locations/{location}/securityProfileGroups/my-security-profile-group
    //
    // Must be specified if action is 'apply_security_profile_group'.
    optional string security_profile_group = 8 [json_name = "security_profile_group"];

    // A list of forwarding rules to which this rule applies.
    //
    // This field allows you to control which load balancers get this rule.
    //
    // For example, the following are valid values:
    //
    // .googleapis.com/compute/v1/projects/project/global/forwardingRules/forwardingRule
    //
    // pis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule
    //
    // - projects/project/global/forwardingRules/forwardingRule
    //
    // - projects/project/regions/region/forwardingRules/forwardingRule
    repeated string target_forwarding_rules = 9 [json_name = "target_forwarding_rules"];

    // A list of service accounts indicating the sets of
    //
    // instances that are applied with this rule.
    repeated string target_service_accounts = 10 [json_name = "target_service_accounts"];

    // Target types of the firewall policy rule.
    //
    // Default value is INSTANCES.
    //
    // When target_type is INTERNAL_MANAGED_LB, target_forwarding_rules must be set
    // Possible values: ["INSTANCES", "INTERNAL_MANAGED_LB"]
    optional string target_type = 11 [json_name = "target_type"];

    // Boolean flag indicating if the traffic should be TLS decrypted.
    //
    // It can be set only if action = 'apply_security_profile_group' and cannot be set
    // for other actions.
    optional bool tls_inspect = 12 [json_name = "tls_inspect"];

    Match match = 13;

    repeated TargetSecureTag target_secure_tag = 14 [json_name = "target_secure_tag"];

    message Match {
      // Address groups which should be matched against the traffic destination.
      //
      // Maximum number of destination address groups is 10.
      repeated string dest_address_groups = 1 [json_name = "dest_address_groups"];

      // Fully Qualified Domain Name (FQDN) which should be matched against
      //
      // traffic destination. Maximum number of destination fqdn allowed is 100.
      repeated string dest_fqdns = 2 [json_name = "dest_fqdns"];

      // Destination IP address range in CIDR format. Required for
      //
      // EGRESS rules.
      repeated string dest_ip_ranges = 3 [json_name = "dest_ip_ranges"];

      // Region codes whose IP addresses will be used to match for destination
      //
      // of traffic. Should be specified as 2 letter country code defined as per
      //
      // ISO 3166 alpha-2 country codes. ex."US"
      //
      // Maximum number of destination region codes allowed is 5000.
      repeated string dest_region_codes = 4 [json_name = "dest_region_codes"];

      // Names of Network Threat Intelligence lists.
      //
      // The IPs in these lists will be matched against traffic destination.
      repeated string dest_threat_intelligences = 5 [json_name = "dest_threat_intelligences"];

      // Address groups which should be matched against the traffic source.
      //
      // Maximum number of source address groups is 10.
      repeated string src_address_groups = 6 [json_name = "src_address_groups"];

      // Fully Qualified Domain Name (FQDN) which should be matched against
      //
      // traffic source. Maximum number of source fqdn allowed is 100.
      repeated string src_fqdns = 7 [json_name = "src_fqdns"];

      // Source IP address range in CIDR format. Required for
      //
      // INGRESS rules.
      repeated string src_ip_ranges = 8 [json_name = "src_ip_ranges"];

      // Region codes whose IP addresses will be used to match for source
      //
      // of traffic. Should be specified as 2 letter country code defined as per
      //
      // ISO 3166 alpha-2 country codes. ex."US"
      //
      // Maximum number of source region codes allowed is 5000.
      repeated string src_region_codes = 9 [json_name = "src_region_codes"];

      // Names of Network Threat Intelligence lists.
      //
      // The IPs in these lists will be matched against traffic source.
      repeated string src_threat_intelligences = 10 [json_name = "src_threat_intelligences"];

      repeated Layer4Config layer4_config = 11 [json_name = "layer4_config"];

      repeated SrcSecureTag src_secure_tag = 12 [json_name = "src_secure_tag"];

      message Layer4Config {
        // The IP protocol to which this rule applies. The protocol
        //
        // type is required when creating a firewall rule.
        //
        // This value can either be one of the following well
        //
        // known protocol strings (tcp, udp, icmp, esp, ah, ipip, sctp),
        //
        // or the IP protocol number.
        optional string ip_protocol = 1 [json_name = "ip_protocol"];

        // An optional list of ports to which this rule applies. This field
        //
        // is only applicable for UDP or TCP protocol. Each entry must be
        //
        // either an integer or a range. If not specified, this rule
        //
        // applies to connections through any port.
        //
        // Example inputs include: ["22"], ["80","443"], and
        //
        // ["12345-12349"].
        repeated string ports = 2;
      }

      message SrcSecureTag {
        // Name of the secure tag, created with TagManager's TagValue API.
        //
        // @pattern tagValues/[0-9]+
        optional string name = 1;

        // [Output Only] State of the secure tag, either 'EFFECTIVE' or
        //
        // 'INEFFECTIVE'. A secure tag is 'INEFFECTIVE' when it is deleted
        //
        // or its network is deleted.
        optional string state = 2;
      }
    }

    message TargetSecureTag {
      // Name of the secure tag, created with TagManager's TagValue API.
      //
      // @pattern tagValues/[0-9]+
      optional string name = 1;

      // [Output Only] State of the secure tag, either 'EFFECTIVE' or
      //
      // 'INEFFECTIVE'. A secure tag is 'INEFFECTIVE' when it is deleted
      //
      // or its network is deleted.
      optional string state = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionPerInstanceConfig version is 0
message GoogleComputeRegionPerInstanceConfig {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The minimal action to perform on the instance during an update.
  //
  // Default is 'NONE'. Possible values are:
  //
  // * REPLACE
  //
  // * RESTART
  //
  // * REFRESH
  //
  // * NONE
  optional string minimal_action = 3 [json_name = "minimal_action"];

  // The most disruptive action to perform on the instance during an update.
  //
  // Default is 'REPLACE'. Possible values are:
  //
  // * REPLACE
  //
  // * RESTART
  //
  // * REFRESH
  //
  // * NONE
  optional string most_disruptive_allowed_action = 4 [json_name = "most_disruptive_allowed_action"];

  // The name for this per-instance config and its corresponding instance.
  optional string name = 5;

  optional string project = 6;

  // Region where the containing instance group manager is located
  optional string region = 7;

  // The region instance group manager this instance config is part of.
  optional string region_instance_group_manager = 8 [json_name = "region_instance_group_manager"];

  // When true, deleting this config will immediately remove the underlying instance.
  //
  // When false, deleting this config will use the behavior as determined by
  // remove_instance_on_destroy.
  optional bool remove_instance_on_destroy = 9 [json_name = "remove_instance_on_destroy"];

  // When true, deleting this config will immediately remove any specified state from
  // the underlying instance.
  //
  // When false, deleting this config will *not* immediately remove any state from the
  // underlying instance.
  //
  // State will be removed on the next instance recreation or update.
  optional bool remove_instance_state_on_destroy = 10 [json_name = "remove_instance_state_on_destroy"];

  PreservedState preserved_state = 11 [json_name = "preserved_state"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message PreservedState {
    // Preserved metadata defined for this instance. This is a list of key->value pairs.
    map<string, string> metadata = 1;

    repeated Disk disk = 2;

    repeated ExternalIp external_ip = 3 [json_name = "external_ip"];

    repeated InternalIp internal_ip = 4 [json_name = "internal_ip"];

    message Disk {
      // A value that prescribes what should happen to the stateful disk when the VM
      // instance is deleted.
      //
      // The available options are 'NEVER' and 'ON_PERMANENT_INSTANCE_DELETION'.
      //
      // 'NEVER' - detach the disk when the VM is deleted, but do not delete the disk.
      //
      // 'ON_PERMANENT_INSTANCE_DELETION' will delete the stateful disk when the VM is
      // permanently
      //
      // deleted from the instance group. Default value: "NEVER" Possible values:
      // ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      optional string delete_rule = 1 [json_name = "delete_rule"];

      // A unique device name that is reflected into the /dev/ tree of a Linux operating
      // system running within the instance.
      optional string device_name = 2 [json_name = "device_name"];

      // The mode of the disk. Default value: "READ_WRITE" Possible values: ["READ_ONLY",
      // "READ_WRITE"]
      optional string mode = 3;

      // The URI of an existing persistent disk to attach under the specified device-name
      // in the format
      //
      // 'projects/project-id/zones/zone/disks/disk-name'.
      optional string source = 4;
    }

    message ExternalIp {
      // These stateful IPs will never be released during autohealing, update or VM
      // instance recreate operations. This flag is used to configure if the IP
      // reservation should be deleted after it is no longer used by the group, e.g. when
      // the given instance or the whole group is deleted. Default value: "NEVER" Possible
      // values: ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      optional string auto_delete = 1 [json_name = "auto_delete"];

      optional string interface_name = 2 [json_name = "interface_name"];

      IpAddress ip_address = 3 [json_name = "ip_address"];

      message IpAddress {
        // The URL of the reservation for this IP address.
        optional string address = 1;
      }
    }

    message InternalIp {
      // These stateful IPs will never be released during autohealing, update or VM
      // instance recreate operations. This flag is used to configure if the IP
      // reservation should be deleted after it is no longer used by the group, e.g. when
      // the given instance or the whole group is deleted. Default value: "NEVER" Possible
      // values: ["NEVER", "ON_PERMANENT_INSTANCE_DELETION"]
      optional string auto_delete = 1 [json_name = "auto_delete"];

      optional string interface_name = 2 [json_name = "interface_name"];

      IpAddress ip_address = 3 [json_name = "ip_address"];

      message IpAddress {
        // The URL of the reservation for this IP address.
        optional string address = 1;
      }
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionResizeRequest version is 0
message GoogleComputeRegionResizeRequest {
  // The creation timestamp for this resize request in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resize-request.
  optional string description = 3;

  optional string id = 4;

  // The reference of the regional instance group manager this ResizeRequest is a part
  // of.
  optional string instance_group_manager = 5 [json_name = "instance_group_manager"];

  // The name of this resize request. The name must be 1-63 characters long, and
  // comply with RFC1035.
  optional string name = 6;

  optional string project = 7;

  // The reference of the compute region scoping this request. If it is not provided,
  // the provider region is used.
  optional string region = 8;

  // The number of instances to be created by this resize request. The group's target
  // size will be increased by this number.
  optional int64 resize_by = 9 [json_name = "resize_by"];

  // Current state of the request.
  optional string state = 10;

  // Status of the request.
  repeated Status status = 11;

  RequestedRunDuration requested_run_duration = 12 [json_name = "requested_run_duration"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Status {
    // error: list of object
    repeated Error error = 1;

    // last_attempt: list of object
    repeated LastAttempt last_attempt = 2 [json_name = "last_attempt"];

    message Error {
      // errors: list of object
      repeated Errors errors = 1;

      message Errors {
        // code: string
        optional string code = 1;

        // error_details: list of object
        repeated ErrorDetails error_details = 2 [json_name = "error_details"];

        // location: string
        optional string location = 3;

        // message: string
        optional string message = 4;

        message ErrorDetails {
          // error_info: list of object
          repeated ErrorInfo error_info = 1 [json_name = "error_info"];

          // help: list of object
          repeated Help help = 2;

          // localized_message: list of object
          repeated LocalizedMessage localized_message = 3 [json_name = "localized_message"];

          // quota_info: list of object
          repeated QuotaInfo quota_info = 4 [json_name = "quota_info"];

          message ErrorInfo {
            // domain: string
            optional string domain = 1;

            // metadatas: map of string
            map<string, string> metadatas = 2;

            // reason: string
            optional string reason = 3;
          }

          message Help {
            // links: list of object
            repeated Links links = 1;

            message Links {
              // description: string
              optional string description = 1;

              // url: string
              optional string url = 2;
            }
          }

          message LocalizedMessage {
            // locale: string
            optional string locale = 1;

            // message: string
            optional string message = 2;
          }

          message QuotaInfo {
            // dimensions: map of string
            map<string, string> dimensions = 1;

            // future_limit: number
            optional int64 future_limit = 2 [json_name = "future_limit"];

            // limit: number
            optional int64 limit = 3;

            // limit_name: string
            optional string limit_name = 4 [json_name = "limit_name"];

            // metric_name: string
            optional string metric_name = 5 [json_name = "metric_name"];

            // rollout_status: string
            optional string rollout_status = 6 [json_name = "rollout_status"];
          }
        }
      }
    }

    message LastAttempt {
      // error: list of object
      repeated Error error = 1;

      message Error {
        // errors: list of object
        repeated Errors errors = 1;

        message Errors {
          // code: string
          optional string code = 1;

          // error_details: list of object
          repeated ErrorDetails error_details = 2 [json_name = "error_details"];

          // location: string
          optional string location = 3;

          // message: string
          optional string message = 4;

          message ErrorDetails {
            // error_info: list of object
            repeated ErrorInfo error_info = 1 [json_name = "error_info"];

            // help: list of object
            repeated Help help = 2;

            // localized_message: list of object
            repeated LocalizedMessage localized_message = 3 [json_name = "localized_message"];

            // quota_info: list of object
            repeated QuotaInfo quota_info = 4 [json_name = "quota_info"];

            message ErrorInfo {
              // domain: string
              optional string domain = 1;

              // metadatas: map of string
              map<string, string> metadatas = 2;

              // reason: string
              optional string reason = 3;
            }

            message Help {
              // links: list of object
              repeated Links links = 1;

              message Links {
                // description: string
                optional string description = 1;

                // url: string
                optional string url = 2;
              }
            }

            message LocalizedMessage {
              // locale: string
              optional string locale = 1;

              // message: string
              optional string message = 2;
            }

            message QuotaInfo {
              // dimensions: map of string
              map<string, string> dimensions = 1;

              // future_limit: number
              optional int64 future_limit = 2 [json_name = "future_limit"];

              // limit: number
              optional int64 limit = 3;

              // limit_name: string
              optional string limit_name = 4 [json_name = "limit_name"];

              // metric_name: string
              optional string metric_name = 5 [json_name = "metric_name"];

              // rollout_status: string
              optional string rollout_status = 6 [json_name = "rollout_status"];
            }
          }
        }
      }
    }
  }

  message RequestedRunDuration {
    // Span of time that's a fraction of a second at nanosecond resolution. Durations
    // less than one second are represented with a 0 seconds field and a positive nanos
    // field. Must be from 0 to 999,999,999 inclusive.
    optional int64 nanos = 1;

    // Span of time at a resolution of a second. Must be from 600 to 604800 inclusive.
    // Note: minimum and maximum allowed range for requestedRunDuration is 10 minutes
    // (600 seconds) and 7 days(604800 seconds) correspondingly.
    optional string seconds = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionSecurityPolicy version is 0
message GoogleComputeRegionSecurityPolicy {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 2;

  // Fingerprint of this resource. This field is used internally during
  //
  // updates of this resource.
  optional string fingerprint = 3;

  optional string id = 4;

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be 1-63 characters long, and comply with RFC1035.
  //
  // Specifically, the name must be 1-63 characters long and match the regular
  // expression [a-z]([-a-z0-9]*[a-z0-9])? which means the first character must be a
  // lowercase letter, and all following characters must be a dash, lowercase letter,
  // or digit, except the last character, which cannot be a dash.
  optional string name = 5;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string policy_id = 6 [json_name = "policy_id"];

  optional string project = 7;

  // The Region in which the created Region Security Policy should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 8;

  // Server-defined URL for the resource.
  optional string self_link = 9 [json_name = "self_link"];

  // Server-defined URL for this resource with the resource id.
  optional string self_link_with_policy_id = 10 [json_name = "self_link_with_policy_id"];

  // The type indicates the intended use of the security policy.
  //
  // - CLOUD_ARMOR: Cloud Armor backend security policies can be configured to filter
  // incoming HTTP requests targeting backend services. They filter requests before
  // they hit the origin servers.
  //
  // - CLOUD_ARMOR_EDGE: Cloud Armor edge security policies can be configured to
  // filter incoming HTTP requests targeting backend services (including Cloud
  // CDN-enabled) as well as backend buckets (Cloud Storage). They filter requests
  // before the request is served from Google's cache.
  //
  // - CLOUD_ARMOR_NETWORK: Cloud Armor network policies can be configured to filter
  // packets targeting network load balancing resources such as backend services,
  // target pools, target instances, and instances with external IPs. They filter
  // requests before the request is served from the application.
  //
  // This field can be set only at resource creation time. Possible values:
  // ["CLOUD_ARMOR", "CLOUD_ARMOR_EDGE", "CLOUD_ARMOR_NETWORK"]
  optional string type = 11;

  AdvancedOptionsConfig advanced_options_config = 12 [json_name = "advanced_options_config"];

  DdosProtectionConfig ddos_protection_config = 13 [json_name = "ddos_protection_config"];

  repeated Rules rules = 14;

  repeated Timeouts timeouts = 15;

  repeated UserDefinedFields user_defined_fields = 16 [json_name = "user_defined_fields"];

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message AdvancedOptionsConfig {
    // JSON body parsing. Supported values include: "DISABLED", "STANDARD",
    // "STANDARD_WITH_GRAPHQL". Possible values: ["DISABLED", "STANDARD",
    // "STANDARD_WITH_GRAPHQL"]
    optional string json_parsing = 1 [json_name = "json_parsing"];

    // Logging level. Supported values include: "NORMAL", "VERBOSE". Possible values:
    // ["NORMAL", "VERBOSE"]
    optional string log_level = 2 [json_name = "log_level"];

    // An optional list of case-insensitive request header names to use for resolving
    // the callers client IP address.
    repeated string user_ip_request_headers = 3 [json_name = "user_ip_request_headers"];

    JsonCustomConfig json_custom_config = 4 [json_name = "json_custom_config"];

    message JsonCustomConfig {
      // A list of custom Content-Type header values to apply the JSON parsing.
      repeated string content_types = 1 [json_name = "content_types"];
    }
  }

  message DdosProtectionConfig {
    // Google Cloud Armor offers the following options to help protect systems against
    // DDoS attacks:
    //
    // - STANDARD: basic always-on protection for network load balancers, protocol
    // forwarding, or VMs with public IP addresses.
    //
    // - ADVANCED: additional protections for Managed Protection Plus subscribers who
    // use network load balancers, protocol forwarding, or VMs with public IP addresses.
    //
    // - ADVANCED_PREVIEW: flag to enable the security policy in preview mode. Possible
    // values: ["ADVANCED", "ADVANCED_PREVIEW", "STANDARD"]
    optional string ddos_protection = 1 [json_name = "ddos_protection"];
  }

  message Rules {
    // The Action to perform when the rule is matched. The following are the valid
    // actions:
    //
    //
    // * allow: allow access to target.
    //
    //
    // * deny(STATUS): deny access to target, returns the HTTP response code specified.
    // Valid values for STATUS are 403, 404, and 502.
    //
    //
    // * rate_based_ban: limit client traffic to the configured threshold and ban the
    // client if the traffic exceeds the threshold. Configure parameters for this action
    // in RateLimitOptions. Requires rateLimitOptions to be set.
    //
    //
    // * redirect: redirect to a different target. This can either be an internal
    // reCAPTCHA redirect, or an external URL-based redirect via a 302 response.
    // Parameters for this action can be configured via redirectOptions. This action is
    // only supported in Global Security Policies of type CLOUD_ARMOR.
    //
    //
    // * throttle: limit client traffic to the configured threshold. Configure
    // parameters for this action in rateLimitOptions. Requires rateLimitOptions to be
    // set for this.
    optional string action = 1;

    // An optional description of this resource. Provide this property when you create
    // the resource.
    optional string description = 2;

    // If set to true, the specified action is not enforced.
    optional bool preview = 3;

    // An integer indicating the priority of a rule in the list.
    //
    // The priority must be a positive value between 0 and 2147483647.
    //
    // Rules are evaluated from highest to lowest priority where 0 is the highest
    // priority and 2147483647 is the lowest priority.
    optional int64 priority = 4;

    Match match = 5;

    NetworkMatch network_match = 6 [json_name = "network_match"];

    PreconfiguredWafConfig preconfigured_waf_config = 7 [json_name = "preconfigured_waf_config"];

    RateLimitOptions rate_limit_options = 8 [json_name = "rate_limit_options"];

    message Match {
      // Preconfigured versioned expression. If this field is specified, config must also
      // be specified.
      //
      // Available preconfigured expressions along with their requirements are: SRC_IPS_V1
      // - must specify the corresponding srcIpRange field in config. Possible values:
      // ["SRC_IPS_V1"]
      optional string versioned_expr = 1 [json_name = "versioned_expr"];

      Config config = 2;

      Expr expr = 3;

      message Config {
        // CIDR IP address range. Maximum number of srcIpRanges allowed is 10.
        repeated string src_ip_ranges = 1 [json_name = "src_ip_ranges"];
      }

      message Expr {
        // Textual representation of an expression in Common Expression Language syntax. The
        // application context of the containing message determines which well-known feature
        // set of CEL is supported.
        optional string expression = 1;
      }
    }

    message NetworkMatch {
      // Destination IPv4/IPv6 addresses or CIDR prefixes, in standard text format.
      repeated string dest_ip_ranges = 1 [json_name = "dest_ip_ranges"];

      // Destination port numbers for TCP/UDP/SCTP. Each element can be a 16-bit unsigned
      // decimal number (e.g. "80") or range (e.g. "0-1023").
      repeated string dest_ports = 2 [json_name = "dest_ports"];

      // IPv4 protocol / IPv6 next header (after extension headers). Each element can be
      // an 8-bit unsigned decimal number (e.g. "6"), range (e.g. "253-254"), or one of
      // the following protocol names: "tcp", "udp", "icmp", "esp", "ah", "ipip", or
      // "sctp".
      repeated string ip_protocols = 3 [json_name = "ip_protocols"];

      // BGP Autonomous System Number associated with the source IP address.
      repeated int64 src_asns = 4 [json_name = "src_asns"];

      // Source IPv4/IPv6 addresses or CIDR prefixes, in standard text format.
      repeated string src_ip_ranges = 5 [json_name = "src_ip_ranges"];

      // Source port numbers for TCP/UDP/SCTP. Each element can be a 16-bit unsigned
      // decimal number (e.g. "80") or range (e.g. "0-1023").
      repeated string src_ports = 6 [json_name = "src_ports"];

      // Two-letter ISO 3166-1 alpha-2 country code associated with the source IP address.
      repeated string src_region_codes = 7 [json_name = "src_region_codes"];

      repeated UserDefinedFields user_defined_fields = 8 [json_name = "user_defined_fields"];

      message UserDefinedFields {
        // Name of the user-defined field, as given in the definition.
        optional string name = 1;

        // Matching values of the field. Each element can be a 32-bit unsigned decimal or
        // hexadecimal (starting with "0x") number (e.g. "64") or range (e.g.
        // "0x400-0x7ff").
        repeated string values = 2;
      }
    }

    message PreconfiguredWafConfig {
      repeated Exclusion exclusion = 1;

      message Exclusion {
        // A list of target rule IDs under the WAF rule set to apply the preconfigured WAF
        // exclusion.
        //
        // If omitted, it refers to all the rule IDs under the WAF rule set.
        repeated string target_rule_ids = 1 [json_name = "target_rule_ids"];

        // Target WAF rule set to apply the preconfigured WAF exclusion.
        optional string target_rule_set = 2 [json_name = "target_rule_set"];

        repeated RequestCookie request_cookie = 3 [json_name = "request_cookie"];

        repeated RequestHeader request_header = 4 [json_name = "request_header"];

        repeated RequestQueryParam request_query_param = 5 [json_name = "request_query_param"];

        repeated RequestUri request_uri = 6 [json_name = "request_uri"];

        message RequestCookie {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value.
          //
          // Available options:
          //
          // EQUALS: The operator matches if the field value equals the specified value.
          //
          // STARTS_WITH: The operator matches if the field value starts with the specified
          // value.
          //
          // ENDS_WITH: The operator matches if the field value ends with the specified value.
          //
          // CONTAINS: The operator matches if the field value contains the specified value.
          //
          // EQUALS_ANY: The operator matches if the field value is any value. Possible
          // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation.
          //
          // The field value must be given if the field operator is not EQUALS_ANY, and cannot
          // be given if the field operator is EQUALS_ANY.
          optional string value = 2;
        }

        message RequestHeader {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value.
          //
          // Available options:
          //
          // EQUALS: The operator matches if the field value equals the specified value.
          //
          // STARTS_WITH: The operator matches if the field value starts with the specified
          // value.
          //
          // ENDS_WITH: The operator matches if the field value ends with the specified value.
          //
          // CONTAINS: The operator matches if the field value contains the specified value.
          //
          // EQUALS_ANY: The operator matches if the field value is any value. Possible
          // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation.
          //
          // The field value must be given if the field operator is not EQUALS_ANY, and cannot
          // be given if the field operator is EQUALS_ANY.
          optional string value = 2;
        }

        message RequestQueryParam {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value.
          //
          // Available options:
          //
          // EQUALS: The operator matches if the field value equals the specified value.
          //
          // STARTS_WITH: The operator matches if the field value starts with the specified
          // value.
          //
          // ENDS_WITH: The operator matches if the field value ends with the specified value.
          //
          // CONTAINS: The operator matches if the field value contains the specified value.
          //
          // EQUALS_ANY: The operator matches if the field value is any value. Possible
          // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation.
          //
          // The field value must be given if the field operator is not EQUALS_ANY, and cannot
          // be given if the field operator is EQUALS_ANY.
          optional string value = 2;
        }

        message RequestUri {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value.
          //
          // Available options:
          //
          // EQUALS: The operator matches if the field value equals the specified value.
          //
          // STARTS_WITH: The operator matches if the field value starts with the specified
          // value.
          //
          // ENDS_WITH: The operator matches if the field value ends with the specified value.
          //
          // CONTAINS: The operator matches if the field value contains the specified value.
          //
          // EQUALS_ANY: The operator matches if the field value is any value. Possible
          // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation.
          //
          // The field value must be given if the field operator is not EQUALS_ANY, and cannot
          // be given if the field operator is EQUALS_ANY.
          optional string value = 2;
        }
      }
    }

    message RateLimitOptions {
      // Can only be specified if the action for the rule is "rate_based_ban".
      //
      // If specified, determines the time (in seconds) the traffic will continue to be
      // banned by the rate limit after the rate falls below the threshold.
      optional int64 ban_duration_sec = 1 [json_name = "ban_duration_sec"];

      // Action to take for requests that are under the configured rate limit threshold.
      //
      // Valid option is "allow" only.
      optional string conform_action = 2 [json_name = "conform_action"];

      // Determines the key to enforce the rateLimitThreshold on. Possible values are:
      //
      // * ALL: A single rate limit threshold is applied to all the requests matching this
      // rule. This is the default value if "enforceOnKey" is not configured.
      //
      // * IP: The source IP address of the request is the key. Each IP has this limit
      // enforced separately.
      //
      // * HTTP_HEADER: The value of the HTTP header whose name is configured under
      // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
      // header value. If no such header is present in the request, the key type defaults
      // to ALL.
      //
      // * XFF_IP: The first IP address (i.e. the originating client IP address) specified
      // in the list of IPs under X-Forwarded-For HTTP header. If no such header is
      // present or the value is not a valid IP, the key defaults to the source IP address
      // of the request i.e. key type IP.
      //
      // * HTTP_COOKIE: The value of the HTTP cookie whose name is configured under
      // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
      // cookie value. If no such cookie is present in the request, the key type defaults
      // to ALL.
      //
      // * HTTP_PATH: The URL path of the HTTP request. The key value is truncated to the
      // first 128 bytes.
      //
      // * SNI: Server name indication in the TLS session of the HTTPS request. The key
      // value is truncated to the first 128 bytes. The key type defaults to ALL on a HTTP
      // session.
      //
      // * REGION_CODE: The country/region from which the request originates.
      //
      // * TLS_JA3_FINGERPRINT: JA3 TLS/SSL fingerprint if the client connects using
      // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
      //
      // * TLS_JA4_FINGERPRINT: JA4 TLS/SSL fingerprint if the client connects using
      // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
      //
      // * USER_IP: The IP address of the originating client, which is resolved based on
      // "userIpRequestHeaders" configured with the security policy. If there is no
      // "userIpRequestHeaders" configuration or an IP address cannot be resolved from it,
      // the key type defaults to IP. Possible values: ["ALL", "IP", "HTTP_HEADER",
      // "XFF_IP", "HTTP_COOKIE", "HTTP_PATH", "SNI", "REGION_CODE",
      // "TLS_JA3_FINGERPRINT", "TLS_JA4_FINGERPRINT", "USER_IP"]
      optional string enforce_on_key = 3 [json_name = "enforce_on_key"];

      // Rate limit key name applicable only for the following key types:
      //
      // HTTP_HEADER -- Name of the HTTP header whose value is taken as the key value.
      //
      // HTTP_COOKIE -- Name of the HTTP cookie whose value is taken as the key value.
      optional string enforce_on_key_name = 4 [json_name = "enforce_on_key_name"];

      // Action to take for requests that are above the configured rate limit threshold,
      // to deny with a specified HTTP response code.
      //
      // Valid options are deny(STATUS), where valid values for STATUS are 403, 404, 429,
      // and 502.
      optional string exceed_action = 5 [json_name = "exceed_action"];

      BanThreshold ban_threshold = 6 [json_name = "ban_threshold"];

      repeated EnforceOnKeyConfigs enforce_on_key_configs = 7 [json_name = "enforce_on_key_configs"];

      RateLimitThreshold rate_limit_threshold = 8 [json_name = "rate_limit_threshold"];

      message BanThreshold {
        // Number of HTTP(S) requests for calculating the threshold.
        optional int64 count = 1;

        // Interval over which the threshold is computed.
        optional int64 interval_sec = 2 [json_name = "interval_sec"];
      }

      message EnforceOnKeyConfigs {
        // Rate limit key name applicable only for the following key types:
        //
        // HTTP_HEADER -- Name of the HTTP header whose value is taken as the key value.
        //
        // HTTP_COOKIE -- Name of the HTTP cookie whose value is taken as the key value.
        optional string enforce_on_key_name = 1 [json_name = "enforce_on_key_name"];

        // Determines the key to enforce the rateLimitThreshold on. Possible values are:
        //
        // * ALL: A single rate limit threshold is applied to all the requests matching this
        // rule. This is the default value if "enforceOnKeyConfigs" is not configured.
        //
        // * IP: The source IP address of the request is the key. Each IP has this limit
        // enforced separately.
        //
        // * HTTP_HEADER: The value of the HTTP header whose name is configured under
        // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
        // header value. If no such header is present in the request, the key type defaults
        // to ALL.
        //
        // * XFF_IP: The first IP address (i.e. the originating client IP address) specified
        // in the list of IPs under X-Forwarded-For HTTP header. If no such header is
        // present or the value is not a valid IP, the key defaults to the source IP address
        // of the request i.e. key type IP.
        //
        // * HTTP_COOKIE: The value of the HTTP cookie whose name is configured under
        // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
        // cookie value. If no such cookie is present in the request, the key type defaults
        // to ALL.
        //
        // * HTTP_PATH: The URL path of the HTTP request. The key value is truncated to the
        // first 128 bytes.
        //
        // * SNI: Server name indication in the TLS session of the HTTPS request. The key
        // value is truncated to the first 128 bytes. The key type defaults to ALL on a HTTP
        // session.
        //
        // * REGION_CODE: The country/region from which the request originates.
        //
        // * TLS_JA3_FINGERPRINT: JA3 TLS/SSL fingerprint if the client connects using
        // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
        //
        // * TLS_JA4_FINGERPRINT: JA4 TLS/SSL fingerprint if the client connects using
        // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
        //
        // * USER_IP: The IP address of the originating client, which is resolved based on
        // "userIpRequestHeaders" configured with the security policy. If there is no
        // "userIpRequestHeaders" configuration or an IP address cannot be resolved from it,
        // the key type defaults to IP. Possible values: ["ALL", "IP", "HTTP_HEADER",
        // "XFF_IP", "HTTP_COOKIE", "HTTP_PATH", "SNI", "REGION_CODE",
        // "TLS_JA3_FINGERPRINT", "TLS_JA4_FINGERPRINT", "USER_IP"]
        optional string enforce_on_key_type = 2 [json_name = "enforce_on_key_type"];
      }

      message RateLimitThreshold {
        // Number of HTTP(S) requests for calculating the threshold.
        optional int64 count = 1;

        // Interval over which the threshold is computed.
        optional int64 interval_sec = 2 [json_name = "interval_sec"];
      }
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message UserDefinedFields {
    // The base relative to which 'offset' is measured. Possible values are:
    //
    // - IPV4: Points to the beginning of the IPv4 header.
    //
    // - IPV6: Points to the beginning of the IPv6 header.
    //
    // - TCP: Points to the beginning of the TCP header, skipping over any IPv4 options
    // or IPv6 extension headers. Not present for non-first fragments.
    //
    // - UDP: Points to the beginning of the UDP header, skipping over any IPv4 options
    // or IPv6 extension headers. Not present for non-first fragments. Possible values:
    // ["IPV4", "IPV6", "TCP", "UDP"]
    optional string base = 1;

    // If specified, apply this mask (bitwise AND) to the field to ignore bits before
    // matching.
    //
    // Encoded as a hexadecimal number (starting with "0x").
    //
    // The last byte of the field (in network byte order) corresponds to the least
    // significant byte of the mask.
    optional string mask = 2;

    // The name of this field. Must be unique within the policy.
    optional string name = 3;

    // Offset of the first byte of the field (in network byte order) relative to 'base'.
    optional int64 offset = 4;

    // Size of the field in bytes. Valid values: 1-4.
    optional int64 size = 5;
  }
}

// GoogleComputeRegionSecurityPolicyRule version is 0
message GoogleComputeRegionSecurityPolicyRule {
  // The Action to perform when the rule is matched. The following are the valid
  // actions:
  //
  //
  // * allow: allow access to target.
  //
  //
  // * deny(STATUS): deny access to target, returns the HTTP response code specified.
  // Valid values for STATUS are 403, 404, and 502.
  //
  //
  // * rate_based_ban: limit client traffic to the configured threshold and ban the
  // client if the traffic exceeds the threshold. Configure parameters for this action
  // in RateLimitOptions. Requires rateLimitOptions to be set.
  //
  //
  // * redirect: redirect to a different target. This can either be an internal
  // reCAPTCHA redirect, or an external URL-based redirect via a 302 response.
  // Parameters for this action can be configured via redirectOptions. This action is
  // only supported in Global Security Policies of type CLOUD_ARMOR.
  //
  //
  // * throttle: limit client traffic to the configured threshold. Configure
  // parameters for this action in rateLimitOptions. Requires rateLimitOptions to be
  // set for this.
  optional string action = 1;

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 3;

  optional string id = 4;

  // If set to true, the specified action is not enforced.
  optional bool preview = 5;

  // An integer indicating the priority of a rule in the list.
  //
  // The priority must be a positive value between 0 and 2147483647.
  //
  // Rules are evaluated from highest to lowest priority where 0 is the highest
  // priority and 2147483647 is the lowest priority.
  optional int64 priority = 6;

  optional string project = 7;

  // The Region in which the created Region Security Policy rule should reside.
  optional string region = 8;

  // The name of the security policy this rule belongs to.
  optional string security_policy = 9 [json_name = "security_policy"];

  Match match = 10;

  NetworkMatch network_match = 11 [json_name = "network_match"];

  PreconfiguredWafConfig preconfigured_waf_config = 12 [json_name = "preconfigured_waf_config"];

  RateLimitOptions rate_limit_options = 13 [json_name = "rate_limit_options"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Match {
    // Preconfigured versioned expression. If this field is specified, config must also
    // be specified.
    //
    // Available preconfigured expressions along with their requirements are: SRC_IPS_V1
    // - must specify the corresponding srcIpRange field in config. Possible values:
    // ["SRC_IPS_V1"]
    optional string versioned_expr = 1 [json_name = "versioned_expr"];

    Config config = 2;

    Expr expr = 3;

    message Config {
      // CIDR IP address range. Maximum number of srcIpRanges allowed is 10.
      repeated string src_ip_ranges = 1 [json_name = "src_ip_ranges"];
    }

    message Expr {
      // Textual representation of an expression in Common Expression Language syntax. The
      // application context of the containing message determines which well-known feature
      // set of CEL is supported.
      optional string expression = 1;
    }
  }

  message NetworkMatch {
    // Destination IPv4/IPv6 addresses or CIDR prefixes, in standard text format.
    repeated string dest_ip_ranges = 1 [json_name = "dest_ip_ranges"];

    // Destination port numbers for TCP/UDP/SCTP. Each element can be a 16-bit unsigned
    // decimal number (e.g. "80") or range (e.g. "0-1023").
    repeated string dest_ports = 2 [json_name = "dest_ports"];

    // IPv4 protocol / IPv6 next header (after extension headers). Each element can be
    // an 8-bit unsigned decimal number (e.g. "6"), range (e.g. "253-254"), or one of
    // the following protocol names: "tcp", "udp", "icmp", "esp", "ah", "ipip", or
    // "sctp".
    repeated string ip_protocols = 3 [json_name = "ip_protocols"];

    // BGP Autonomous System Number associated with the source IP address.
    repeated int64 src_asns = 4 [json_name = "src_asns"];

    // Source IPv4/IPv6 addresses or CIDR prefixes, in standard text format.
    repeated string src_ip_ranges = 5 [json_name = "src_ip_ranges"];

    // Source port numbers for TCP/UDP/SCTP. Each element can be a 16-bit unsigned
    // decimal number (e.g. "80") or range (e.g. "0-1023").
    repeated string src_ports = 6 [json_name = "src_ports"];

    // Two-letter ISO 3166-1 alpha-2 country code associated with the source IP address.
    repeated string src_region_codes = 7 [json_name = "src_region_codes"];

    repeated UserDefinedFields user_defined_fields = 8 [json_name = "user_defined_fields"];

    message UserDefinedFields {
      // Name of the user-defined field, as given in the definition.
      optional string name = 1;

      // Matching values of the field. Each element can be a 32-bit unsigned decimal or
      // hexadecimal (starting with "0x") number (e.g. "64") or range (e.g.
      // "0x400-0x7ff").
      repeated string values = 2;
    }
  }

  message PreconfiguredWafConfig {
    repeated Exclusion exclusion = 1;

    message Exclusion {
      // A list of target rule IDs under the WAF rule set to apply the preconfigured WAF
      // exclusion.
      //
      // If omitted, it refers to all the rule IDs under the WAF rule set.
      repeated string target_rule_ids = 1 [json_name = "target_rule_ids"];

      // Target WAF rule set to apply the preconfigured WAF exclusion.
      optional string target_rule_set = 2 [json_name = "target_rule_set"];

      repeated RequestCookie request_cookie = 3 [json_name = "request_cookie"];

      repeated RequestHeader request_header = 4 [json_name = "request_header"];

      repeated RequestQueryParam request_query_param = 5 [json_name = "request_query_param"];

      repeated RequestUri request_uri = 6 [json_name = "request_uri"];

      message RequestCookie {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value. Possible
        // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestHeader {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value. Possible
        // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestQueryParam {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value. Possible
        // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestUri {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value. Possible
        // values: ["CONTAINS", "ENDS_WITH", "EQUALS", "EQUALS_ANY", "STARTS_WITH"]
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }
    }
  }

  message RateLimitOptions {
    // Can only be specified if the action for the rule is "rate_based_ban".
    //
    // If specified, determines the time (in seconds) the traffic will continue to be
    // banned by the rate limit after the rate falls below the threshold.
    optional int64 ban_duration_sec = 1 [json_name = "ban_duration_sec"];

    // Action to take for requests that are under the configured rate limit threshold.
    //
    // Valid option is "allow" only.
    optional string conform_action = 2 [json_name = "conform_action"];

    // Determines the key to enforce the rateLimitThreshold on. Possible values are:
    //
    // * ALL: A single rate limit threshold is applied to all the requests matching this
    // rule. This is the default value if "enforceOnKey" is not configured.
    //
    // * IP: The source IP address of the request is the key. Each IP has this limit
    // enforced separately.
    //
    // * HTTP_HEADER: The value of the HTTP header whose name is configured under
    // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
    // header value. If no such header is present in the request, the key type defaults
    // to ALL.
    //
    // * XFF_IP: The first IP address (i.e. the originating client IP address) specified
    // in the list of IPs under X-Forwarded-For HTTP header. If no such header is
    // present or the value is not a valid IP, the key defaults to the source IP address
    // of the request i.e. key type IP.
    //
    // * HTTP_COOKIE: The value of the HTTP cookie whose name is configured under
    // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
    // cookie value. If no such cookie is present in the request, the key type defaults
    // to ALL.
    //
    // * HTTP_PATH: The URL path of the HTTP request. The key value is truncated to the
    // first 128 bytes.
    //
    // * SNI: Server name indication in the TLS session of the HTTPS request. The key
    // value is truncated to the first 128 bytes. The key type defaults to ALL on a HTTP
    // session.
    //
    // * REGION_CODE: The country/region from which the request originates.
    //
    // * TLS_JA3_FINGERPRINT: JA3 TLS/SSL fingerprint if the client connects using
    // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
    //
    // * TLS_JA4_FINGERPRINT: JA4 TLS/SSL fingerprint if the client connects using
    // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
    //
    // * USER_IP: The IP address of the originating client, which is resolved based on
    // "userIpRequestHeaders" configured with the security policy. If there is no
    // "userIpRequestHeaders" configuration or an IP address cannot be resolved from it,
    // the key type defaults to IP. Possible values: ["ALL", "IP", "HTTP_HEADER",
    // "XFF_IP", "HTTP_COOKIE", "HTTP_PATH", "SNI", "REGION_CODE",
    // "TLS_JA3_FINGERPRINT", "TLS_JA4_FINGERPRINT", "USER_IP"]
    optional string enforce_on_key = 3 [json_name = "enforce_on_key"];

    // Rate limit key name applicable only for the following key types:
    //
    // HTTP_HEADER -- Name of the HTTP header whose value is taken as the key value.
    //
    // HTTP_COOKIE -- Name of the HTTP cookie whose value is taken as the key value.
    optional string enforce_on_key_name = 4 [json_name = "enforce_on_key_name"];

    // Action to take for requests that are above the configured rate limit threshold,
    // to deny with a specified HTTP response code.
    //
    // Valid options are deny(STATUS), where valid values for STATUS are 403, 404, 429,
    // and 502.
    optional string exceed_action = 5 [json_name = "exceed_action"];

    BanThreshold ban_threshold = 6 [json_name = "ban_threshold"];

    repeated EnforceOnKeyConfigs enforce_on_key_configs = 7 [json_name = "enforce_on_key_configs"];

    RateLimitThreshold rate_limit_threshold = 8 [json_name = "rate_limit_threshold"];

    message BanThreshold {
      // Number of HTTP(S) requests for calculating the threshold.
      optional int64 count = 1;

      // Interval over which the threshold is computed.
      optional int64 interval_sec = 2 [json_name = "interval_sec"];
    }

    message EnforceOnKeyConfigs {
      // Rate limit key name applicable only for the following key types:
      //
      // HTTP_HEADER -- Name of the HTTP header whose value is taken as the key value.
      //
      // HTTP_COOKIE -- Name of the HTTP cookie whose value is taken as the key value.
      optional string enforce_on_key_name = 1 [json_name = "enforce_on_key_name"];

      // Determines the key to enforce the rateLimitThreshold on. Possible values are:
      //
      // * ALL: A single rate limit threshold is applied to all the requests matching this
      // rule. This is the default value if "enforceOnKeyConfigs" is not configured.
      //
      // * IP: The source IP address of the request is the key. Each IP has this limit
      // enforced separately.
      //
      // * HTTP_HEADER: The value of the HTTP header whose name is configured under
      // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
      // header value. If no such header is present in the request, the key type defaults
      // to ALL.
      //
      // * XFF_IP: The first IP address (i.e. the originating client IP address) specified
      // in the list of IPs under X-Forwarded-For HTTP header. If no such header is
      // present or the value is not a valid IP, the key defaults to the source IP address
      // of the request i.e. key type IP.
      //
      // * HTTP_COOKIE: The value of the HTTP cookie whose name is configured under
      // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
      // cookie value. If no such cookie is present in the request, the key type defaults
      // to ALL.
      //
      // * HTTP_PATH: The URL path of the HTTP request. The key value is truncated to the
      // first 128 bytes.
      //
      // * SNI: Server name indication in the TLS session of the HTTPS request. The key
      // value is truncated to the first 128 bytes. The key type defaults to ALL on a HTTP
      // session.
      //
      // * REGION_CODE: The country/region from which the request originates.
      //
      // * TLS_JA3_FINGERPRINT: JA3 TLS/SSL fingerprint if the client connects using
      // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
      //
      // * TLS_JA4_FINGERPRINT: JA4 TLS/SSL fingerprint if the client connects using
      // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
      //
      // * USER_IP: The IP address of the originating client, which is resolved based on
      // "userIpRequestHeaders" configured with the security policy. If there is no
      // "userIpRequestHeaders" configuration or an IP address cannot be resolved from it,
      // the key type defaults to IP. Possible values: ["ALL", "IP", "HTTP_HEADER",
      // "XFF_IP", "HTTP_COOKIE", "HTTP_PATH", "SNI", "REGION_CODE",
      // "TLS_JA3_FINGERPRINT", "TLS_JA4_FINGERPRINT", "USER_IP"]
      optional string enforce_on_key_type = 2 [json_name = "enforce_on_key_type"];
    }

    message RateLimitThreshold {
      // Number of HTTP(S) requests for calculating the threshold.
      optional int64 count = 1;

      // Interval over which the threshold is computed.
      optional int64 interval_sec = 2 [json_name = "interval_sec"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionSslCertificate version is 0
message GoogleComputeRegionSslCertificate {
  // The certificate in PEM format.
  //
  // The certificate chain must be no greater than 5 certs long.
  //
  // The chain must include at least one intermediate cert.
  optional string certificate = 1;

  // The unique identifier for the resource.
  optional int64 certificate_id = 2 [json_name = "certificate_id"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 5;

  // Expire time of the certificate in RFC3339 text format.
  optional string expire_time = 6 [json_name = "expire_time"];

  optional string id = 7;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  //
  //
  // These are in the same namespace as the managed SSL certificates.
  optional string name = 8;

  // Creates a unique name beginning with the specified prefix. Conflicts with name.
  optional string name_prefix = 9 [json_name = "name_prefix"];

  // The write-only private key in PEM format.
  optional string private_key = 10 [json_name = "private_key"];

  // The write-only private key in PEM format.
  optional string private_key_wo = 11 [json_name = "private_key_wo"];

  // Triggers update of 'private_key_wo' write-only. Increment this value when an
  // update to 'private_key_wo' is needed. For more info see [updating write-only
  // ders/google/guides/using_write_only_arguments.html#updating-write-only-arguments)
  optional string private_key_wo_version = 12 [json_name = "private_key_wo_version"];

  optional string project = 13;

  // The Region in which the created regional ssl certificate should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 14;

  optional string self_link = 15 [json_name = "self_link"];

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionSslPolicy version is 0
message GoogleComputeRegionSslPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // A list of features enabled when the selected profile is CUSTOM. The
  //
  // method returns the set of features that can be specified in this
  //
  // list. This field must be empty if the profile is not CUSTOM.
  //
  //
  // See the [official
  // /cloud.google.com/compute/docs/load-balancing/ssl-policies#profilefeaturesupport)
  //
  // for which ciphers are available to use. **Note**: this argument
  //
  // *must* be present when using the 'CUSTOM' profile. This argument
  //
  // *must not* be present when using any other profile.
  repeated string custom_features = 2 [json_name = "custom_features"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  // The list of features enabled in the SSL policy.
  repeated string enabled_features = 5 [json_name = "enabled_features"];

  // Fingerprint of this resource. A hash of the contents stored in this
  //
  // object. This field is used in optimistic locking.
  optional string fingerprint = 6;

  optional string id = 7;

  // The minimum version of SSL protocol that can be used by the clients
  //
  // to establish a connection with the load balancer. When set to
  //
  // 'TLS_1_3', the profile field must be set to 'RESTRICTED'. Default value:
  // "TLS_1_0" Possible values: ["TLS_1_0", "TLS_1_1", "TLS_1_2", "TLS_1_3"]
  optional string min_tls_version = 8 [json_name = "min_tls_version"];

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 9;

  // One of 'DEFAULT', 'ENABLED', or 'DEFERRED'. Controls whether the load balancer
  // negotiates
  //
  // X25519MLKEM768 key exchange when clients advertise support for it.
  //
  // When set to 'DEFAULT', or if no SSL Policy is attached to
  //
  // the target proxy, the load balancer disallows X25519MLKEM768 key
  //
  // exchange before October 2026, and allows it afterward. When set to
  //
  // 'ENABLED', the load balancer allows X25519MLKEM768 key
  //
  // exchange. When set to 'DEFERRED', the load balancer
  //
  // disallows X25519MLKEM768 key exchange until October 2027, and allows
  //
  // it afterward. Possible values: ["DEFAULT", "ENABLED", "DEFERRED"]
  optional string post_quantum_key_exchange = 10 [json_name = "post_quantum_key_exchange"];

  // Profile specifies the set of SSL features that can be used by the
  //
  // load balancer when negotiating SSL with clients. If using 'CUSTOM',
  //
  // the set of SSL features to enable must be specified in the
  //
  // 'customFeatures' field.
  //
  //
  // See the [official
  // /cloud.google.com/compute/docs/load-balancing/ssl-policies#profilefeaturesupport)
  //
  // for information on what cipher suites each profile provides. If
  //
  // 'CUSTOM' is used, the 'custom_features' attribute **must be set**.
  //
  // If set to 'FIPS_202205', 'minTlsVersion' must also be set to
  //
  // 'TLS_1_2'. Default value: "COMPATIBLE" Possible values: ["COMPATIBLE", "MODERN",
  // "RESTRICTED", "CUSTOM", "FIPS_202205"]
  optional string profile = 11;

  optional string project = 12;

  // The region where the regional SSL policy resides.
  optional string region = 13;

  optional string self_link = 14 [json_name = "self_link"];

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  optional int32 count = 18;

  optional string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionTargetHttpProxy version is 0
message GoogleComputeRegionTargetHttpProxy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Specifies how long to keep a connection open, after completing a response,
  //
  // while there is no matching traffic (in seconds). If an HTTP keepalive is
  //
  // not specified, a default value (600 seconds) will be used. For Regional
  //
  // HTTP(S) load balancer, the minimum allowed value is 5 seconds and the
  //
  // maximum allowed value is 600 seconds.
  optional int64 http_keep_alive_timeout_sec = 4 [json_name = "http_keep_alive_timeout_sec"];

  optional string id = 5;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 6;

  optional string project = 7;

  // The unique identifier for the resource.
  optional int64 proxy_id = 8 [json_name = "proxy_id"];

  // The Region in which the created target https proxy should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 9;

  optional string self_link = 10 [json_name = "self_link"];

  // A reference to the RegionUrlMap resource that defines the mapping from URL
  //
  // to the BackendService.
  optional string url_map = 11 [json_name = "url_map"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionTargetHttpsProxy version is 0
message GoogleComputeRegionTargetHttpsProxy {
  // URLs to certificate manager certificate resources that are used to authenticate
  // connections between users and the load balancer.
  //
  // sslCertificates and certificateManagerCertificates can't be defined together.
  //
  // Accepted format is
  // ogleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}'
  // or just the self_link
  // 'projects/{project}/locations/{location}/certificates/{resourceName}'
  repeated string certificate_manager_certificates = 1 [json_name = "certificate_manager_certificates"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  // Specifies how long to keep a connection open, after completing a response,
  //
  // while there is no matching traffic (in seconds). If an HTTP keepalive is
  //
  // not specified, a default value (600 seconds) will be used. For Regioanl
  //
  // HTTP(S) load balancer, the minimum allowed value is 5 seconds and the
  //
  // maximum allowed value is 600 seconds.
  optional int64 http_keep_alive_timeout_sec = 5 [json_name = "http_keep_alive_timeout_sec"];

  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 7;

  optional string project = 8;

  // The unique identifier for the resource.
  optional int64 proxy_id = 9 [json_name = "proxy_id"];

  // The Region in which the created target https proxy should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 10;

  optional string self_link = 11 [json_name = "self_link"];

  // A URL referring to a networksecurity.ServerTlsPolicy
  //
  // resource that describes how the proxy should authenticate inbound
  //
  // traffic. serverTlsPolicy only applies to a global TargetHttpsProxy
  //
  // attached to globalForwardingRules with the loadBalancingScheme
  //
  // set to INTERNAL_SELF_MANAGED or EXTERNAL or EXTERNAL_MANAGED.
  //
  // For details which ServerTlsPolicy resources are accepted with
  //
  // INTERNAL_SELF_MANAGED and which with EXTERNAL, EXTERNAL_MANAGED
  //
  // loadBalancingScheme consult ServerTlsPolicy documentation.
  //
  // If left blank, communications are not encrypted.
  //
  //
  // If you remove this field from your configuration at the same time as
  //
  // deleting or recreating a referenced ServerTlsPolicy resource, you will
  //
  // receive a resourceInUseByAnotherResource error. Use
  // lifecycle.create_before_destroy
  //
  // within the ServerTlsPolicy resource to avoid this.
  optional string server_tls_policy = 12 [json_name = "server_tls_policy"];

  // URLs to SslCertificate resources that are used to authenticate connections
  // between users and the load balancer.
  //
  // At least one SSL certificate must be specified. Currently, you may specify up to
  // 15 SSL certificates.
  //
  // sslCertificates do not apply when the load balancing scheme is set to
  // INTERNAL_SELF_MANAGED.
  repeated string ssl_certificates = 13 [json_name = "ssl_certificates"];

  // A reference to the Region SslPolicy resource that will be associated with
  //
  // the TargetHttpsProxy resource. If not set, the TargetHttpsProxy
  //
  // resource will not have any SSL policy configured.
  optional string ssl_policy = 14 [json_name = "ssl_policy"];

  // A reference to the RegionUrlMap resource that defines the mapping from URL
  //
  // to the RegionBackendService.
  optional string url_map = 15 [json_name = "url_map"];

  repeated Timeouts timeouts = 16;

  google.protobuf.Value for_each = 17 [json_name = "for_each"];

  repeated string depends_on = 18 [json_name = "depends_on"];

  optional int32 count = 19;

  optional string provider = 20;

  terraform.v1.Lifecycle lifecycle = 21;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRegionTargetTcpProxy version is 0
message GoogleComputeRegionTargetTcpProxy {
  // A reference to the BackendService resource. This field is optional when
  //
  // the loadBalancingScheme (available in beta) is specified.
  optional string backend_service = 1 [json_name = "backend_service"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  optional string id = 5;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 6;

  optional string project = 7;

  // This field only applies when the forwarding rule that references
  //
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  optional bool proxy_bind = 8 [json_name = "proxy_bind"];

  // Specifies the type of proxy header to append before sending data to
  //
  // the backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
  optional string proxy_header = 9 [json_name = "proxy_header"];

  // The unique identifier for the resource.
  optional int64 proxy_id = 10 [json_name = "proxy_id"];

  // The Region in which the created target TCP proxy should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 11;

  optional string self_link = 12 [json_name = "self_link"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRegionUrlMap version is 0
message GoogleComputeRegionUrlMap {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // The full or partial URL of the defaultService resource to which traffic is
  // directed if
  //
  // none of the hostRules match. If defaultRouteAction is additionally specified,
  // advanced
  //
  // routing actions like URL Rewrites, etc. take effect prior to sending the request
  // to the
  //
  // backend. However, if defaultService is specified, defaultRouteAction cannot
  // contain any
  //
  // weightedBackendServices. Conversely, if routeAction specifies any
  //
  // weightedBackendServices, service must not be specified.  Only one of
  // defaultService,
  //
  // defaultUrlRedirect or defaultRouteAction.weightedBackendService must be set.
  optional string default_service = 2 [json_name = "default_service"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource.
  optional string description = 4;

  // Fingerprint of this resource. This field is used internally during
  //
  // updates of this resource.
  optional string fingerprint = 5;

  optional string id = 6;

  // The unique identifier for the resource.
  optional int64 map_id = 7 [json_name = "map_id"];

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 8;

  optional string project = 9;

  // The Region in which the url map should reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 10;

  optional string self_link = 11 [json_name = "self_link"];

  DefaultRouteAction default_route_action = 12 [json_name = "default_route_action"];

  DefaultUrlRedirect default_url_redirect = 13 [json_name = "default_url_redirect"];

  HeaderAction header_action = 14 [json_name = "header_action"];

  repeated HostRule host_rule = 15 [json_name = "host_rule"];

  repeated PathMatcher path_matcher = 16 [json_name = "path_matcher"];

  repeated Test test = 17;

  repeated Timeouts timeouts = 18;

  google.protobuf.Value for_each = 19 [json_name = "for_each"];

  repeated string depends_on = 20 [json_name = "depends_on"];

  optional int32 count = 21;

  optional string provider = 22;

  terraform.v1.Lifecycle lifecycle = 23;

  message DefaultRouteAction {
    CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

    FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

    RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

    RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

    Timeout timeout = 5;

    UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

    repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

    message CorsPolicy {
      // In response to a preflight request, setting this to true indicates that the
      // actual request can include user credentials. This field translates to the
      // Access-Control-Allow-Credentials header.
      //
      // Default is false.
      optional bool allow_credentials = 1 [json_name = "allow_credentials"];

      // Specifies the content for the Access-Control-Allow-Headers header.
      repeated string allow_headers = 2 [json_name = "allow_headers"];

      // Specifies the content for the Access-Control-Allow-Methods header.
      repeated string allow_methods = 3 [json_name = "allow_methods"];

      // Specifies the regualar expression patterns that match allowed origins. For
      // regular expression grammar
      //
      // please see en.cppreference.com/w/cpp/regex/ecmascript
      //
      // An origin is allowed if it matches either an item in allowOrigins or an item in
      // allowOriginRegexes.
      repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

      // Specifies the list of origins that will be allowed to do CORS requests.
      //
      // An origin is allowed if it matches either an item in allowOrigins or an item in
      // allowOriginRegexes.
      repeated string allow_origins = 5 [json_name = "allow_origins"];

      // If true, the setting specifies the CORS policy is disabled. The default value of
      // false, which indicates that the CORS policy is in effect.
      optional bool disabled = 6;

      // Specifies the content for the Access-Control-Expose-Headers header.
      repeated string expose_headers = 7 [json_name = "expose_headers"];

      // Specifies how long results of a preflight request can be cached in seconds.
      //
      // This translates to the Access-Control-Max-Age header.
      optional int64 max_age = 8 [json_name = "max_age"];
    }

    message FaultInjectionPolicy {
      Abort abort = 1;

      Delay delay = 2;

      message Abort {
        // The HTTP status code used to abort the request.
        //
        // The value must be between 200 and 599 inclusive.
        optional int64 http_status = 1 [json_name = "http_status"];

        // The percentage of traffic (connections/operations/requests) which will be aborted
        // as part of fault injection.
        //
        // The value must be between 0.0 and 100.0 inclusive.
        optional int64 percentage = 2;
      }

      message Delay {
        // The percentage of traffic (connections/operations/requests) on which delay will
        // be introduced as part of fault injection.
        //
        // The value must be between 0.0 and 100.0 inclusive.
        optional int64 percentage = 1;

        FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

        message FixedDelay {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are
          //
          // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
          // 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          //
          // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
          // days/year * 10000 years
          optional string seconds = 2;
        }
      }
    }

    message RequestMirrorPolicy {
      // The full or partial URL to the RegionBackendService resource being mirrored to.
      //
      // The backend service configured for a mirroring policy must reference backends
      // that are of the same type as the original backend service matched in the URL map.
      //
      // Serverless NEG backends are not currently supported as a mirrored backend
      // service.
      optional string backend_service = 1 [json_name = "backend_service"];
    }

    message RetryPolicy {
      // Specifies the allowed number retries. This number must be > 0. If not specified,
      // defaults to 1.
      optional int64 num_retries = 1 [json_name = "num_retries"];

      // Specifies one or more conditions when this retry policy applies.
      //
      // Valid values are listed below. Only the following codes are supported when the
      // URL map is bound to target gRPC proxy that has validateForProxyless field set to
      // true: cancelled, deadline-exceeded, internal, resource-exhausted, unavailable.
      //
      // - 5xx : retry is attempted if the instance or endpoint responds with any 5xx
      // response code, or if the instance or endpoint does not respond at all. For
      // example, disconnects, reset, read timeout, connection failure, and refused
      // streams.
      //
      // - gateway-error : Similar to 5xx, but only applies to response codes 502, 503 or
      // 504.
      //
      // - connect-failure : a retry is attempted on failures connecting to the instance
      // or endpoint. For example, connection timeouts.
      //
      // - retriable-4xx : a retry is attempted if the instance or endpoint responds with
      // a 4xx response code. The only error that you can retry is error code 409.
      //
      // - refused-stream : a retry is attempted if the instance or endpoint resets the
      // stream with a REFUSED_STREAM error code. This reset type indicates that it is
      // safe to retry.
      //
      // - cancelled : a retry is attempted if the gRPC status code in the response header
      // is set to cancelled.
      //
      // - deadline-exceeded : a retry is attempted if the gRPC status code in the
      // response header is set to deadline-exceeded.
      //
      // - internal :  a retry is attempted if the gRPC status code in the response header
      // is set to internal.
      //
      // - resource-exhausted : a retry is attempted if the gRPC status code in the
      // response header is set to resource-exhausted.
      //
      // - unavailable : a retry is attempted if the gRPC status code in the response
      // header is set to unavailable.
      repeated string retry_conditions = 2 [json_name = "retry_conditions"];

      PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

      message PerTryTimeout {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations
        // less than one second are
        //
        // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
        // 999,999,999 inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        //
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
        // days/year * 10000 years
        optional string seconds = 2;
      }
    }

    message Timeout {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      // less than one second are represented with a 0 seconds field and a positive nanos
      // field. Must be from 0 to 999,999,999 inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      // inclusive. Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24
      // hr/day * 365.25 days/year * 10000 years
      optional string seconds = 2;
    }

    message UrlRewrite {
      // Before forwarding the request to the selected service, the request's host header
      // is replaced with contents of hostRewrite.
      //
      // The value must be from 1 to 255 characters.
      optional string host_rewrite = 1 [json_name = "host_rewrite"];

      // Before forwarding the request to the selected backend service, the matching
      // portion of the request's path is replaced by pathPrefixRewrite.
      //
      // The value must be from 1 to 1024 characters.
      optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
    }

    message WeightedBackendServices {
      // The full or partial URL to the default BackendService resource. Before forwarding
      // the request to backendService, the load balancer applies any relevant
      // headerActions specified as part of this backendServiceWeight.
      optional string backend_service = 1 [json_name = "backend_service"];

      // Specifies the fraction of traffic sent to a backend service, computed as weight /
      // (sum of all weightedBackendService weights in routeAction) .
      //
      // The selection of a backend service is determined only for new traffic. Once a
      // user's request has been directed to a backend service, subsequent requests are
      // sent to the same backend service as determined by the backend service's session
      // affinity policy.
      //
      // The value must be from 0 to 1000.
      optional int64 weight = 2;

      HeaderAction header_action = 3 [json_name = "header_action"];

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request
        // before forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response
        // before sending the response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header. If true, headerValue is set for the header, discarding any values that
          // were set for that header.
          //
          // The default value is false.
          optional bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header. If true, headerValue is set for the header, discarding any values that
          // were set for that header.
          //
          // The default value is false.
          optional bool replace = 3;
        }
      }
    }
  }

  message DefaultUrlRedirect {
    // The host that will be used in the redirect response instead of the one that was
    //
    // supplied in the request. The value must be between 1 and 255 characters.
    optional string host_redirect = 1 [json_name = "host_redirect"];

    // If set to true, the URL scheme in the redirected request is set to https. If set
    // to
    //
    // false, the URL scheme of the redirected request will remain the same as that of
    // the
    //
    // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
    //
    // true for TargetHttpsProxy is not permitted. The default is set to false.
    optional bool https_redirect = 2 [json_name = "https_redirect"];

    // The path that will be used in the redirect response instead of the one that was
    //
    // supplied in the request. pathRedirect cannot be supplied together with
    //
    // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of
    // the
    //
    // original request will be used for the redirect. The value must be between 1 and
    // 1024
    //
    // characters.
    optional string path_redirect = 3 [json_name = "path_redirect"];

    // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
    //
    // retaining the remaining portion of the URL before redirecting the request.
    //
    // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
    //
    // neither. If neither is supplied, the path of the original request will be used
    // for
    //
    // the redirect. The value must be between 1 and 1024 characters.
    optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

    // The HTTP Status code to use for this RedirectAction. Supported values are:
    //
    //
    // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
    //
    //
    // * FOUND, which corresponds to 302.
    //
    //
    // * SEE_OTHER which corresponds to 303.
    //
    //
    // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
    //
    // will be retained.
    //
    //
    // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
    //
    // the request method will be retained. Possible values: ["FOUND",
    // "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER",
    // "TEMPORARY_REDIRECT"]
    optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

    // If set to true, any accompanying query portion of the original URL is removed
    // prior
    //
    // to redirecting the request. If set to false, the query portion of the original
    // URL is
    //
    // retained.
    //
    // This field is required to ensure an empty block is not set. The normal default
    // value is false.
    optional bool strip_query = 6 [json_name = "strip_query"];
  }

  message HeaderAction {
    // A list of header names for headers that need to be removed from the request
    // before forwarding the request to the backendService.
    repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

    // A list of header names for headers that need to be removed from the response
    // before sending the response back to the client.
    repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

    repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

    repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

    message RequestHeadersToAdd {
      // The name of the header.
      optional string header_name = 1 [json_name = "header_name"];

      // The value of the header to add.
      optional string header_value = 2 [json_name = "header_value"];

      // If false, headerValue is appended to any values that already exist for the
      // header. If true, headerValue is set for the header, discarding any values that
      // were set for that header.
      //
      // The default value is false.
      optional bool replace = 3;
    }

    message ResponseHeadersToAdd {
      // The name of the header.
      optional string header_name = 1 [json_name = "header_name"];

      // The value of the header to add.
      optional string header_value = 2 [json_name = "header_value"];

      // If false, headerValue is appended to any values that already exist for the
      // header. If true, headerValue is set for the header, discarding any values that
      // were set for that header.
      //
      // The default value is false.
      optional bool replace = 3;
    }
  }

  message HostRule {
    // An optional description of this HostRule. Provide this property
    //
    // when you create the resource.
    optional string description = 1;

    // The list of host patterns to match. They must be valid
    //
    // hostnames, except * will match any string of ([a-z0-9-.]*). In
    //
    // that case, * must be the first character and must be followed in
    //
    // the pattern by either - or ..
    repeated string hosts = 2;

    // The name of the PathMatcher to use to match the path portion of
    //
    // the URL if the hostRule matches the URL's host portion.
    optional string path_matcher = 3 [json_name = "path_matcher"];
  }

  message PathMatcher {
    // A reference to a RegionBackendService resource. This will be used if
    //
    // none of the pathRules defined by this PathMatcher is matched by
    //
    // the URL's path portion.
    optional string default_service = 1 [json_name = "default_service"];

    // An optional description of this resource.
    optional string description = 2;

    // The name to which this PathMatcher is referred by the HostRule.
    optional string name = 3;

    DefaultRouteAction default_route_action = 4 [json_name = "default_route_action"];

    DefaultUrlRedirect default_url_redirect = 5 [json_name = "default_url_redirect"];

    HeaderAction header_action = 6 [json_name = "header_action"];

    repeated PathRule path_rule = 7 [json_name = "path_rule"];

    repeated RouteRules route_rules = 8 [json_name = "route_rules"];

    message DefaultRouteAction {
      CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

      FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

      MaxStreamDuration max_stream_duration = 3 [json_name = "max_stream_duration"];

      RequestMirrorPolicy request_mirror_policy = 4 [json_name = "request_mirror_policy"];

      RetryPolicy retry_policy = 5 [json_name = "retry_policy"];

      Timeout timeout = 6;

      UrlRewrite url_rewrite = 7 [json_name = "url_rewrite"];

      repeated WeightedBackendServices weighted_backend_services = 8 [json_name = "weighted_backend_services"];

      message CorsPolicy {
        // In response to a preflight request, setting this to true indicates that the
        // actual request can include user credentials.
        //
        // This translates to the Access-Control-Allow-Credentials header.
        optional bool allow_credentials = 1 [json_name = "allow_credentials"];

        // Specifies the content for the Access-Control-Allow-Headers header.
        repeated string allow_headers = 2 [json_name = "allow_headers"];

        // Specifies the content for the Access-Control-Allow-Methods header.
        repeated string allow_methods = 3 [json_name = "allow_methods"];

        // Specifies the regular expression patterns that match allowed origins. For regular
        // expression grammar
        //
        // please see en.cppreference.com/w/cpp/regex/ecmascript
        //
        // An origin is allowed if it matches either an item in allowOrigins or an item in
        // allowOriginRegexes.
        repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

        // Specifies the list of origins that will be allowed to do CORS requests.
        //
        // An origin is allowed if it matches either an item in allowOrigins or an item in
        // allowOriginRegexes.
        repeated string allow_origins = 5 [json_name = "allow_origins"];

        // If true, specifies the CORS policy is disabled. The default value is false, which
        // indicates that the CORS policy is in effect.
        optional bool disabled = 6;

        // Specifies the content for the Access-Control-Expose-Headers header.
        repeated string expose_headers = 7 [json_name = "expose_headers"];

        // Specifies how long results of a preflight request can be cached in seconds.
        //
        // This translates to the Access-Control-Max-Age header.
        optional int64 max_age = 8 [json_name = "max_age"];
      }

      message FaultInjectionPolicy {
        Abort abort = 1;

        Delay delay = 2;

        message Abort {
          // The HTTP status code used to abort the request.
          //
          // The value must be between 200 and 599 inclusive.
          optional int64 http_status = 1 [json_name = "http_status"];

          // The percentage of traffic (connections/operations/requests) which will be aborted
          // as part of fault injection.
          //
          // The value must be between 0.0 and 100.0 inclusive.
          optional int64 percentage = 2;
        }

        message Delay {
          // The percentage of traffic (connections/operations/requests) on which delay will
          // be introduced as part of fault injection.
          //
          // The value must be between 0.0 and 100.0 inclusive.
          optional int64 percentage = 1;

          FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

          message FixedDelay {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            // less than one second are
            //
            // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
            // 999,999,999 inclusive.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            //
            // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
            // days/year * 10000 years
            optional string seconds = 2;
          }
        }
      }

      message MaxStreamDuration {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations
        // less than one second are represented
        //
        // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999
        // inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        //
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
        // days/year * 10000 years
        optional string seconds = 2;
      }

      message RequestMirrorPolicy {
        // The full or partial URL to the BackendService resource being mirrored to.
        optional string backend_service = 1 [json_name = "backend_service"];
      }

      message RetryPolicy {
        // Specifies the allowed number retries. This number must be > 0. If not specified,
        // defaults to 1.
        optional int64 num_retries = 1 [json_name = "num_retries"];

        // Specfies one or more conditions when this retry rule applies. Valid values are:
        //
        //
        // * 5xx: Loadbalancer will attempt a retry if the backend service responds with any
        // 5xx response code,
        //
        // or if the backend service does not respond at all, example: disconnects, reset,
        // read timeout,
        //
        // * connection failure, and refused streams.
        //
        // * gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or
        // 504.
        //
        // * connect-failure: Loadbalancer will retry on failures connecting to backend
        // services,
        //
        // for example due to connection timeouts.
        //
        // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
        //
        // Currently the only retriable error supported is 409.
        //
        // * refused-stream:Loadbalancer will retry if the backend service resets the stream
        // with a REFUSED_STREAM error code.
        //
        // This reset type indicates that it is safe to retry.
        //
        // * cancelled: Loadbalancer will retry if the gRPC status code in the response
        // header is set to cancelled
        //
        // * deadline-exceeded: Loadbalancer will retry if the gRPC status code in the
        // response header is set to deadline-exceeded
        //
        // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the
        // response header is set to resource-exhausted
        //
        // * unavailable: Loadbalancer will retry if the gRPC status code in the response
        // header is set to unavailable
        repeated string retry_conditions = 2 [json_name = "retry_conditions"];

        PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

        message PerTryTimeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are
          //
          // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
          // 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          //
          // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
          // days/year * 10000 years
          optional string seconds = 2;
        }
      }

      message Timeout {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations
        // less than one second are represented
        //
        // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999
        // inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        //
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
        // days/year * 10000 years
        optional string seconds = 2;
      }

      message UrlRewrite {
        // Prior to forwarding the request to the selected service, the request's host
        // header is replaced
        //
        // with contents of hostRewrite.
        //
        //
        // The value must be between 1 and 255 characters.
        optional string host_rewrite = 1 [json_name = "host_rewrite"];

        // Prior to forwarding the request to the selected backend service, the matching
        // portion of the
        //
        // request's path is replaced by pathPrefixRewrite.
        //
        //
        // The value must be between 1 and 1024 characters.
        optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];

        // If specified, the pattern rewrites the URL path (based on the :path header) using
        // the HTTP template syntax.
        //
        //
        // A corresponding pathTemplateMatch must be specified. Any template variables must
        // exist in the pathTemplateMatch field.
        //
        //
        // * At least one variable must be specified in the pathTemplateMatch field
        //
        // * You can omit variables from the rewritten URL
        //
        // * The * and ** operators cannot be matched unless they have a corresponding
        // variable name - e.g. {format=*} or {var=**}.
        //
        //
        // For example, a pathTemplateMatch of /static/{format=**} could be rewritten as
        // /static/content/{format} to prefix
        //
        // /content to the URL. Variables can also be re-ordered in a rewrite, so that
        // /{country}/{format}/{suffix=**} can be
        //
        // rewritten as /content/{format}/{country}/{suffix}.
        //
        //
        // At least one non-empty routeRules[].matchRules[].path_template_match is required.
        //
        //
        // Only one of pathPrefixRewrite or pathTemplateRewrite may be specified.
        optional string path_template_rewrite = 3 [json_name = "path_template_rewrite"];
      }

      message WeightedBackendServices {
        // The full or partial URL to the default BackendService resource. Before forwarding
        // the
        //
        // request to backendService, the loadbalancer applies any relevant headerActions
        //
        // specified as part of this backendServiceWeight.
        optional string backend_service = 1 [json_name = "backend_service"];

        // Specifies the fraction of traffic sent to backendService, computed as
        //
        // weight / (sum of all weightedBackendService weights in routeAction) .
        //
        //
        // The selection of a backend service is determined only for new traffic. Once a
        // user's request
        //
        // has been directed to a backendService, subsequent requests will be sent to the
        // same backendService
        //
        // as determined by the BackendService's session affinity policy.
        //
        //
        // The value must be between 0 and 1000
        optional int64 weight = 2;

        HeaderAction header_action = 3 [json_name = "header_action"];

        message HeaderAction {
          // A list of header names for headers that need to be removed from the request prior
          // to
          //
          // forwarding the request to the backendService.
          repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

          // A list of header names for headers that need to be removed from the response
          // prior to sending the
          //
          // response back to the client.
          repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

          repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

          repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

          message RequestHeadersToAdd {
            // The name of the header to add.
            optional string header_name = 1 [json_name = "header_name"];

            // The value of the header to add.
            optional string header_value = 2 [json_name = "header_value"];

            // If false, headerValue is appended to any values that already exist for the
            // header.
            //
            // If true, headerValue is set for the header, discarding any values that were set
            // for that header.
            optional bool replace = 3;
          }

          message ResponseHeadersToAdd {
            // The name of the header to add.
            optional string header_name = 1 [json_name = "header_name"];

            // The value of the header to add.
            optional string header_value = 2 [json_name = "header_value"];

            // If false, headerValue is appended to any values that already exist for the
            // header.
            //
            // If true, headerValue is set for the header, discarding any values that were set
            // for that header.
            optional bool replace = 3;
          }
        }
      }
    }

    message DefaultUrlRedirect {
      // The host that will be used in the redirect response instead of the one that was
      //
      // supplied in the request. The value must be between 1 and 255 characters.
      optional string host_redirect = 1 [json_name = "host_redirect"];

      // If set to true, the URL scheme in the redirected request is set to https. If set
      // to
      //
      // false, the URL scheme of the redirected request will remain the same as that of
      // the
      //
      // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
      //
      // true for TargetHttpsProxy is not permitted. The default is set to false.
      optional bool https_redirect = 2 [json_name = "https_redirect"];

      // The path that will be used in the redirect response instead of the one that was
      //
      // supplied in the request. pathRedirect cannot be supplied together with
      //
      // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of
      // the
      //
      // original request will be used for the redirect. The value must be between 1 and
      // 1024
      //
      // characters.
      optional string path_redirect = 3 [json_name = "path_redirect"];

      // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
      //
      // retaining the remaining portion of the URL before redirecting the request.
      //
      // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
      //
      // neither. If neither is supplied, the path of the original request will be used
      // for
      //
      // the redirect. The value must be between 1 and 1024 characters.
      optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

      // The HTTP Status code to use for this RedirectAction. Supported values are:
      //
      //
      // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
      //
      //
      // * FOUND, which corresponds to 302.
      //
      //
      // * SEE_OTHER which corresponds to 303.
      //
      //
      // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
      //
      // will be retained.
      //
      //
      // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
      //
      // the request method will be retained. Possible values: ["FOUND",
      // "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER",
      // "TEMPORARY_REDIRECT"]
      optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

      // If set to true, any accompanying query portion of the original URL is removed
      // prior
      //
      // to redirecting the request. If set to false, the query portion of the original
      // URL is
      //
      // retained.
      //
      // This field is required to ensure an empty block is not set. The normal default
      // value is false.
      optional bool strip_query = 6 [json_name = "strip_query"];
    }

    message HeaderAction {
      // A list of header names for headers that need to be removed from the request
      // before forwarding the request to the backendService.
      repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

      // A list of header names for headers that need to be removed from the response
      // before sending the response back to the client.
      repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

      repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

      repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

      message RequestHeadersToAdd {
        // The name of the header.
        optional string header_name = 1 [json_name = "header_name"];

        // The value of the header to add.
        optional string header_value = 2 [json_name = "header_value"];

        // If false, headerValue is appended to any values that already exist for the
        // header. If true, headerValue is set for the header, discarding any values that
        // were set for that header.
        //
        // The default value is false.
        optional bool replace = 3;
      }

      message ResponseHeadersToAdd {
        // The name of the header.
        optional string header_name = 1 [json_name = "header_name"];

        // The value of the header to add.
        optional string header_value = 2 [json_name = "header_value"];

        // If false, headerValue is appended to any values that already exist for the
        // header. If true, headerValue is set for the header, discarding any values that
        // were set for that header.
        //
        // The default value is false.
        optional bool replace = 3;
      }
    }

    message PathRule {
      // The list of path patterns to match. Each must start with / and the only place a
      //
      // \* is allowed is at the end following a /. The string fed to the path matcher
      //
      // does not include any text after the first ? or #, and those chars are not
      //
      // allowed here.
      repeated string paths = 1;

      // The region backend service resource to which traffic is
      //
      // directed if this rule is matched. If routeAction is additionally specified,
      //
      // advanced routing actions like URL Rewrites, etc. take effect prior to sending
      //
      // the request to the backend. However, if service is specified, routeAction cannot
      //
      // contain any weightedBackendService s. Conversely, if routeAction specifies any
      //
      // weightedBackendServices, service must not be specified. Only one of urlRedirect,
      //
      // service or routeAction.weightedBackendService must be set.
      optional string service = 2;

      RouteAction route_action = 3 [json_name = "route_action"];

      UrlRedirect url_redirect = 4 [json_name = "url_redirect"];

      message RouteAction {
        CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

        RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

        Timeout timeout = 5;

        UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          //
          // actual request can include user credentials. This translates to the Access-
          //
          // Control-Allow-Credentials header. Defaults to false.
          optional bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          //
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          //
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          //
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          optional bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          //
          // translates to the content for the Access-Control-Max-Age header.
          optional int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            //
            // and 599 inclusive.
            optional int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            //
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            //
            // inclusive.
            optional int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            //
            // be introduced as part of fault injection. The value must be between 0.0 and
            //
            // 100.0 inclusive.
            optional int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              //
              // less than one second are represented with a 0 'seconds' field and a positive
              //
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              optional int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              //
              // inclusive.
              optional string seconds = 2;
            }
          }
        }

        message RequestMirrorPolicy {
          // The RegionBackendService resource being mirrored to.
          optional string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          optional int64 num_retries = 1 [json_name = "num_retries"];

          // Specifies one or more conditions when this retry rule applies. Valid values are:
          //
          //
          // - 5xx: Loadbalancer will attempt a retry if the backend service responds with
          //
          // any 5xx response code, or if the backend service does not respond at all,
          //
          // for example: disconnects, reset, read timeout, connection failure, and refused
          //
          // streams.
          //
          // - gateway-error: Similar to 5xx, but only applies to response codes
          //
          // 502, 503 or 504.
          //
          // - connect-failure: Loadbalancer will retry on failures
          //
          // connecting to backend services, for example due to connection timeouts.
          //
          // - retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          //
          // Currently the only retriable error supported is 409.
          //
          // - refused-stream: Loadbalancer will retry if the backend service resets the
          // stream with a
          //
          // REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          //
          // - cancelled: Loadbalancer will retry if the gRPC status code in the response
          //
          // header is set to cancelled
          //
          // - deadline-exceeded: Loadbalancer will retry if the
          //
          // gRPC status code in the response header is set to deadline-exceeded
          //
          // - resource-exhausted: Loadbalancer will retry if the gRPC status code in the
          // response
          //
          // header is set to resource-exhausted
          //
          // - unavailable: Loadbalancer will retry if
          //
          // the gRPC status code in the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            //
            // less than one second are represented with a 0 'seconds' field and a positive
            //
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            //
            // inclusive.
            optional string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          //
          // less than one second are represented with a 0 'seconds' field and a positive
          //
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          //
          // inclusive.
          optional string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          //
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          //
          // 255 characters.
          optional string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          //
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          //
          // be between 1 and 1024 characters.
          optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
        }

        message WeightedBackendServices {
          // The default RegionBackendService resource. Before
          //
          // forwarding the request to backendService, the loadbalancer applies any relevant
          //
          // headerActions specified as part of this backendServiceWeight.
          optional string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          //
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          //
          // backend service is determined only for new traffic. Once a user's request has
          //
          // been directed to a backendService, subsequent requests will be sent to the same
          //
          // backendService as determined by the BackendService's session affinity policy.
          //
          // The value must be between 0 and 1000
          optional int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            //
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            //
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one
        //
        // that was supplied in the request. The value must be between 1 and 255
        //
        // characters.
        optional string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https.
        //
        // If set to false, the URL scheme of the redirected request will remain the
        //
        // same as that of the request. This must only be set for UrlMaps used in
        //
        // TargetHttpProxys. Setting this true for TargetHttpsProxy is not
        //
        // permitted. The default is set to false.
        optional bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one
        //
        // that was supplied in the request. pathRedirect cannot be supplied
        //
        // together with prefixRedirect. Supply one alone or neither. If neither is
        //
        // supplied, the path of the original request will be used for the redirect.
        //
        // The value must be between 1 and 1024 characters.
        optional string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the
        //
        // HttpRouteRuleMatch, retaining the remaining portion of the URL before
        //
        // redirecting the request. prefixRedirect cannot be supplied together with
        //
        // pathRedirect. Supply one alone or neither. If neither is supplied, the
        //
        // path of the original request will be used for the redirect. The value
        //
        // must be between 1 and 1024 characters.
        optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        //
        // * FOUND, which corresponds to 302.
        //
        //
        // * SEE_OTHER which corresponds to 303.
        //
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
        //
        // will be retained.
        //
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
        //
        // the request method will be retained. Possible values: ["FOUND",
        // "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER",
        // "TEMPORARY_REDIRECT"]
        optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is removed
        //
        // prior to redirecting the request. If set to false, the query portion of the
        //
        // original URL is retained.
        //
        // This field is required to ensure an empty block is not set. The normal default
        // value is false.
        optional bool strip_query = 6 [json_name = "strip_query"];
      }
    }

    message RouteRules {
      // For routeRules within a given pathMatcher, priority determines the order
      //
      // in which load balancer will interpret routeRules. RouteRules are evaluated
      //
      // in order of priority, from the lowest to highest number. The priority of
      //
      // a rule decreases as its number increases (1, 2, 3, N+1). The first rule
      //
      // that matches the request is applied.
      //
      //
      // You cannot configure two or more routeRules with the same priority.
      //
      // Priority for each rule must be set to a number between 0 and
      //
      // 2147483647 inclusive.
      //
      //
      // Priority numbers can have gaps, which enable you to add or remove rules
      //
      // in the future without affecting the rest of the rules. For example,
      //
      // 1, 2, 3, 4, 5, 9, 12, 16 is a valid series of priority numbers to which
      //
      // you could add rules numbered from 6 to 8, 10 to 11, and 13 to 15 in the
      //
      // future without any impact on existing rules.
      optional int64 priority = 1;

      // The region backend service resource to which traffic is
      //
      // directed if this rule is matched. If routeAction is additionally specified,
      //
      // advanced routing actions like URL Rewrites, etc. take effect prior to sending
      //
      // the request to the backend. However, if service is specified, routeAction cannot
      //
      // contain any weightedBackendService s. Conversely, if routeAction specifies any
      //
      // weightedBackendServices, service must not be specified. Only one of urlRedirect,
      //
      // service or routeAction.weightedBackendService must be set.
      optional string service = 2;

      HeaderAction header_action = 3 [json_name = "header_action"];

      repeated MatchRules match_rules = 4 [json_name = "match_rules"];

      RouteAction route_action = 5 [json_name = "route_action"];

      UrlRedirect url_redirect = 6 [json_name = "url_redirect"];

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request
        //
        // prior to forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response
        //
        // prior to sending the response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          //
          // header. If true, headerValue is set for the header, discarding any values that
          //
          // were set for that header.
          optional bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          //
          // header. If true, headerValue is set for the header, discarding any values that
          //
          // were set for that header.
          optional bool replace = 3;
        }
      }

      message MatchRules {
        // For satisfying the matchRule condition, the path of the request must exactly
        //
        // match the value specified in fullPathMatch after removing any query parameters
        //
        // and anchor that may be part of the original URL. FullPathMatch must be between 1
        //
        // and 1024 characters. Only one of prefixMatch, fullPathMatch or regexMatch must
        //
        // be specified.
        optional string full_path_match = 1 [json_name = "full_path_match"];

        // Specifies that prefixMatch and fullPathMatch matches are case sensitive.
        //
        // Defaults to false.
        optional bool ignore_case = 2 [json_name = "ignore_case"];

        // For satisfying the matchRule condition, the path of the request
        //
        // must match the wildcard pattern specified in pathTemplateMatch
        //
        // after removing any query parameters and anchor that may be part
        //
        // of the original URL.
        //
        //
        // pathTemplateMatch must be between 1 and 255 characters
        //
        // (inclusive).  The pattern specified by pathTemplateMatch may
        //
        // have at most 5 wildcard operators and at most 5 variable
        //
        // captures in total.
        optional string path_template_match = 3 [json_name = "path_template_match"];

        // For satisfying the matchRule condition, the request's path must begin with the
        //
        // specified prefixMatch. prefixMatch must begin with a /. The value must be
        //
        // between 1 and 1024 characters. Only one of prefixMatch, fullPathMatch or
        //
        // regexMatch must be specified.
        optional string prefix_match = 4 [json_name = "prefix_match"];

        // For satisfying the matchRule condition, the path of the request must satisfy the
        //
        // regular expression specified in regexMatch after removing any query parameters
        //
        // and anchor supplied with the original URL. For regular expression grammar please
        //
        // see en.cppreference.com/w/cpp/regex/ecmascript  Only one of prefixMatch,
        //
        // fullPathMatch or regexMatch must be specified.
        optional string regex_match = 5 [json_name = "regex_match"];

        repeated HeaderMatches header_matches = 6 [json_name = "header_matches"];

        repeated MetadataFilters metadata_filters = 7 [json_name = "metadata_filters"];

        repeated QueryParameterMatches query_parameter_matches = 8 [json_name = "query_parameter_matches"];

        message HeaderMatches {
          // The value should exactly match contents of exactMatch. Only one of exactMatch,
          //
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          optional string exact_match = 1 [json_name = "exact_match"];

          // The name of the HTTP header to match. For matching against the HTTP request's
          //
          // authority, use a headerMatch with the header name ":authority". For matching a
          //
          // request's method, use the headerName ":method".
          optional string header_name = 2 [json_name = "header_name"];

          // If set to false, the headerMatch is considered a match if the match criteria
          //
          // above are met. If set to true, the headerMatch is considered a match if the
          //
          // match criteria above are NOT met. Defaults to false.
          optional bool invert_match = 3 [json_name = "invert_match"];

          // The value of the header must start with the contents of prefixMatch. Only one of
          //
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          //
          // must be set.
          optional string prefix_match = 4 [json_name = "prefix_match"];

          // A header with the contents of headerName must exist. The match takes place
          //
          // whether or not the request's header has a value or not. Only one of exactMatch,
          //
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          optional bool present_match = 5 [json_name = "present_match"];

          // The value of the header must match the regular expression specified in
          //
          // regexMatch. For regular expression grammar, please see:
          //
          // en.cppreference.com/w/cpp/regex/ecmascript  For matching against a port
          //
          // specified in the HTTP request, use a headerMatch with headerName set to PORT and
          //
          // a regular expression that satisfies the RFC2616 Host header's port specifier.
          //
          // Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or
          //
          // rangeMatch must be set.
          optional string regex_match = 6 [json_name = "regex_match"];

          // The value of the header must end with the contents of suffixMatch. Only one of
          //
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          //
          // must be set.
          optional string suffix_match = 7 [json_name = "suffix_match"];

          RangeMatch range_match = 8 [json_name = "range_match"];

          message RangeMatch {
            // The end of the range (exclusive).
            optional int64 range_end = 1 [json_name = "range_end"];

            // The start of the range (inclusive).
            optional int64 range_start = 2 [json_name = "range_start"];
          }
        }

        message MetadataFilters {
          // Specifies how individual filterLabel matches within the list of filterLabels
          //
          // contribute towards the overall metadataFilter match. Supported values are:
          //
          //
          // * MATCH_ANY: At least one of the filterLabels must have a matching label in the
          //
          // provided metadata.
          //
          // * MATCH_ALL: All filterLabels must have matching labels in
          //
          // the provided metadata. Possible values: ["MATCH_ALL", "MATCH_ANY"]
          optional string filter_match_criteria = 1 [json_name = "filter_match_criteria"];

          repeated FilterLabels filter_labels = 2 [json_name = "filter_labels"];

          message FilterLabels {
            // Name of metadata label. The name can have a maximum length of 1024 characters
            //
            // and must be at least 1 character long.
            optional string name = 1;

            // The value of the label must match the specified value. value can have a maximum
            //
            // length of 1024 characters.
            optional string value = 2;
          }
        }

        message QueryParameterMatches {
          // The queryParameterMatch matches if the value of the parameter exactly matches
          //
          // the contents of exactMatch. Only one of presentMatch, exactMatch and regexMatch
          //
          // must be set.
          optional string exact_match = 1 [json_name = "exact_match"];

          // The name of the query parameter to match. The query parameter must exist in the
          //
          // request, in the absence of which the request match fails.
          optional string name = 2;

          // Specifies that the queryParameterMatch matches if the request contains the query
          //
          // parameter, irrespective of whether the parameter has a value or not. Only one of
          //
          // presentMatch, exactMatch and regexMatch must be set.
          optional bool present_match = 3 [json_name = "present_match"];

          // The queryParameterMatch matches if the value of the parameter matches the
          //
          // regular expression specified by regexMatch. For the regular expression grammar,
          //
          // please see en.cppreference.com/w/cpp/regex/ecmascript  Only one of presentMatch,
          //
          // exactMatch and regexMatch must be set.
          optional string regex_match = 4 [json_name = "regex_match"];
        }
      }

      message RouteAction {
        CorsPolicy cors_policy = 1 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 2 [json_name = "fault_injection_policy"];

        RequestMirrorPolicy request_mirror_policy = 3 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 4 [json_name = "retry_policy"];

        Timeout timeout = 5;

        UrlRewrite url_rewrite = 6 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 7 [json_name = "weighted_backend_services"];

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          //
          // actual request can include user credentials. This translates to the Access-
          //
          // Control-Allow-Credentials header. Defaults to false.
          optional bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          //
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          //
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          //
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          //
          // which indicates that the CORS policy is in effect. Defaults to false.
          optional bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          //
          // translates to the content for the Access-Control-Max-Age header.
          optional int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            //
            // and 599 inclusive.
            optional int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            //
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            //
            // inclusive.
            optional int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            //
            // be introduced as part of fault injection. The value must be between 0.0 and
            //
            // 100.0 inclusive.
            optional int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              //
              // less than one second are represented with a 0 'seconds' field and a positive
              //
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              optional int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              //
              // inclusive.
              optional string seconds = 2;
            }
          }
        }

        message RequestMirrorPolicy {
          // The RegionBackendService resource being mirrored to.
          optional string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          optional int64 num_retries = 1 [json_name = "num_retries"];

          // Specifies one or more conditions when this retry rule applies. Valid values are:
          //
          //
          // * 5xx: Loadbalancer will attempt a retry if the backend service responds with
          //
          // any 5xx response code, or if the backend service does not respond at all,
          //
          // for example: disconnects, reset, read timeout, connection failure, and refused
          //
          // streams.
          //
          // * gateway-error: Similar to 5xx, but only applies to response codes
          //
          // 502, 503 or 504.
          //
          // * connect-failure: Loadbalancer will retry on failures
          //
          // connecting to backend services, for example due to connection timeouts.
          //
          // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          //
          // Currently the only retriable error supported is 409.
          //
          // * refused-stream: Loadbalancer will retry if the backend service resets the
          // stream with a
          //
          // REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          //
          // * cancelled: Loadbalancer will retry if the gRPC status code in the response
          //
          // header is set to cancelled
          //
          // * deadline-exceeded: Loadbalancer will retry if the
          //
          // gRPC status code in the response header is set to deadline-exceeded
          //
          // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the
          // response
          //
          // header is set to resource-exhausted
          //
          // * unavailable: Loadbalancer will retry if the gRPC status code in
          //
          // the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            //
            // less than one second are represented with a 0 'seconds' field and a positive
            //
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            //
            // inclusive.
            optional string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          //
          // less than one second are represented with a 0 'seconds' field and a positive
          //
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          //
          // inclusive.
          optional string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          //
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          //
          // 255 characters.
          optional string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          //
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          //
          // be between 1 and 1024 characters.
          optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];

          // Prior to forwarding the request to the selected origin, if the
          //
          // request matched a pathTemplateMatch, the matching portion of the
          //
          // request's path is replaced re-written using the pattern specified
          //
          // by pathTemplateRewrite.
          //
          //
          // pathTemplateRewrite must be between 1 and 255 characters
          //
          // (inclusive), must start with a '/', and must only use variables
          //
          // captured by the route's pathTemplate matchers.
          //
          //
          // pathTemplateRewrite may only be used when all of a route's
          //
          // MatchRules specify pathTemplate.
          //
          //
          // Only one of pathPrefixRewrite and pathTemplateRewrite may be
          //
          // specified.
          optional string path_template_rewrite = 3 [json_name = "path_template_rewrite"];
        }

        message WeightedBackendServices {
          // The default RegionBackendService resource. Before
          //
          // forwarding the request to backendService, the loadbalancer applies any relevant
          //
          // headerActions specified as part of this backendServiceWeight.
          optional string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          //
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          //
          // backend service is determined only for new traffic. Once a user's request has
          //
          // been directed to a backendService, subsequent requests will be sent to the same
          //
          // backendService as determined by the BackendService's session affinity policy.
          //
          // The value must be between 0 and 1000
          optional int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            //
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            //
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one
        //
        // that was supplied in the request. The value must be between 1 and 255
        //
        // characters.
        optional string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https.
        //
        // If set to false, the URL scheme of the redirected request will remain the
        //
        // same as that of the request. This must only be set for UrlMaps used in
        //
        // TargetHttpProxys. Setting this true for TargetHttpsProxy is not
        //
        // permitted. The default is set to false.
        optional bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one
        //
        // that was supplied in the request. pathRedirect cannot be supplied
        //
        // together with prefixRedirect. Supply one alone or neither. If neither is
        //
        // supplied, the path of the original request will be used for the redirect.
        //
        // The value must be between 1 and 1024 characters.
        optional string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the
        //
        // HttpRouteRuleMatch, retaining the remaining portion of the URL before
        //
        // redirecting the request. prefixRedirect cannot be supplied together with
        //
        // pathRedirect. Supply one alone or neither. If neither is supplied, the
        //
        // path of the original request will be used for the redirect. The value
        //
        // must be between 1 and 1024 characters.
        optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        //
        // * FOUND, which corresponds to 302.
        //
        //
        // * SEE_OTHER which corresponds to 303.
        //
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
        //
        // will be retained.
        //
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
        //
        // the request method will be retained. Possible values: ["FOUND",
        // "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER",
        // "TEMPORARY_REDIRECT"]
        optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is
        //
        // removed prior to redirecting the request. If set to false, the query
        //
        // portion of the original URL is retained. The default value is false.
        optional bool strip_query = 6 [json_name = "strip_query"];
      }
    }
  }

  message Test {
    // Description of this test case.
    optional string description = 1;

    // Host portion of the URL.
    optional string host = 2;

    // Path portion of the URL.
    optional string path = 3;

    // A reference to expected RegionBackendService resource the given URL should be
    // mapped to.
    optional string service = 4;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeReservation version is 0
message GoogleComputeReservation {
  // List of all reservation block names in the parent reservation.
  repeated string block_names = 1 [json_name = "block_names"];

  // Full or partial URL to a parent commitment. This field displays for
  //
  // reservations that are tied to a commitment.
  optional string commitment = 2;

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Absolute time in future when the reservation will be auto-deleted by Compute
  // Engine. Timestamp is represented in RFC3339 text format.
  //
  // Cannot be used with delete_after_duration.
  optional string delete_at_time = 4 [json_name = "delete_at_time"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 5 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 6;

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string id = 7;

  // Type of the resource. Always compute#reservations for reservations.
  optional string kind = 8;

  // Full or partial URL to parent commitments. This field displays for reservations
  // that are tied to multiple commitments.
  repeated string linked_commitments = 9 [json_name = "linked_commitments"];

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 10;

  optional string project = 11;

  // Status information for Reservation resource.
  repeated ResourceStatus resource_status = 12 [json_name = "resource_status"];

  // Reserved for future use.
  optional bool satisfies_pzs = 13 [json_name = "satisfies_pzs"];

  optional string self_link = 14 [json_name = "self_link"];

  // When set to true, only VMs that target this reservation by name can
  //
  // consume this reservation. Otherwise, it can be consumed by VMs with
  //
  // affinity for any reservation. Defaults to false.
  optional bool specific_reservation_required = 15 [json_name = "specific_reservation_required"];

  // The status of the reservation.
  optional string status = 16;

  // The zone where the reservation is made.
  optional string zone = 17;

  DeleteAfterDuration delete_after_duration = 18 [json_name = "delete_after_duration"];

  Params params = 19;

  ReservationSharingPolicy reservation_sharing_policy = 20 [json_name = "reservation_sharing_policy"];

  ShareSettings share_settings = 21 [json_name = "share_settings"];

  SpecificReservation specific_reservation = 22 [json_name = "specific_reservation"];

  repeated Timeouts timeouts = 23;

  google.protobuf.Value for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  optional int32 count = 26;

  optional string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message ResourceStatus {
    // health_info: list of object
    repeated HealthInfo health_info = 1 [json_name = "health_info"];

    // reservation_block_count: number
    optional int64 reservation_block_count = 2 [json_name = "reservation_block_count"];

    // reservation_maintenance: list of object
    repeated ReservationMaintenance reservation_maintenance = 3 [json_name = "reservation_maintenance"];

    // specific_sku_allocation: list of object
    repeated SpecificSkuAllocation specific_sku_allocation = 4 [json_name = "specific_sku_allocation"];

    message HealthInfo {
      // degraded_block_count: number
      optional int64 degraded_block_count = 1 [json_name = "degraded_block_count"];

      // health_status: string
      optional string health_status = 2 [json_name = "health_status"];

      // healthy_block_count: number
      optional int64 healthy_block_count = 3 [json_name = "healthy_block_count"];
    }

    message ReservationMaintenance {
      // instance_maintenance_ongoing_count: number
      optional int64 instance_maintenance_ongoing_count = 1 [json_name = "instance_maintenance_ongoing_count"];

      // instance_maintenance_pending_count: number
      optional int64 instance_maintenance_pending_count = 2 [json_name = "instance_maintenance_pending_count"];

      // maintenance_ongoing_count: number
      optional int64 maintenance_ongoing_count = 3 [json_name = "maintenance_ongoing_count"];

      // maintenance_pending_count: number
      optional int64 maintenance_pending_count = 4 [json_name = "maintenance_pending_count"];

      // scheduling_type: string
      optional string scheduling_type = 5 [json_name = "scheduling_type"];

      // subblock_infra_maintenance_ongoing_count: number
      optional int64 subblock_infra_maintenance_ongoing_count = 6 [
        json_name = "subblock_infra_maintenance_ongoing_count"
      ];

      // subblock_infra_maintenance_pending_count: number
      optional int64 subblock_infra_maintenance_pending_count = 7 [
        json_name = "subblock_infra_maintenance_pending_count"
      ];

      // upcoming_group_maintenance: list of object
      repeated UpcomingGroupMaintenance upcoming_group_maintenance = 8 [json_name = "upcoming_group_maintenance"];

      message UpcomingGroupMaintenance {
        // can_reschedule: bool
        optional bool can_reschedule = 1 [json_name = "can_reschedule"];

        // latest_window_start_time: string
        optional string latest_window_start_time = 2 [json_name = "latest_window_start_time"];

        // maintenance_on_shutdown: bool
        optional bool maintenance_on_shutdown = 3 [json_name = "maintenance_on_shutdown"];

        // maintenance_reasons: list of string
        repeated string maintenance_reasons = 4 [json_name = "maintenance_reasons"];

        // maintenance_status: string
        optional string maintenance_status = 5 [json_name = "maintenance_status"];

        // type: string
        optional string type = 6;

        // window_end_time: string
        optional string window_end_time = 7 [json_name = "window_end_time"];

        // window_start_time: string
        optional string window_start_time = 8 [json_name = "window_start_time"];
      }
    }

    message SpecificSkuAllocation {
      // source_instance_template_id: string
      optional string source_instance_template_id = 1 [json_name = "source_instance_template_id"];

      // utilizations: map of string
      map<string, string> utilizations = 2;
    }
  }

  message DeleteAfterDuration {
    // Number of nanoseconds for the auto-delete duration.
    optional int64 nanos = 1;

    // Number of seconds for the auto-delete duration.
    optional string seconds = 2;
  }

  message Params {
    // Resource manager tags to be bound to the reservation. Tag keys and values have
    // the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message ReservationSharingPolicy {
    // Sharing config for all Google Cloud services. Possible values: ["ALLOW_ALL",
    // "DISALLOW_ALL"]
    optional string service_share_type = 1 [json_name = "service_share_type"];
  }

  message ShareSettings {
    // Type of sharing for this shared-reservation Possible values: ["LOCAL",
    // "SPECIFIC_PROJECTS"]
    optional string share_type = 1 [json_name = "share_type"];

    repeated ProjectMap project_map = 2 [json_name = "project_map"];

    message ProjectMap {
      optional string id = 1;

      // The project id/number, should be same as the key of this project config in the
      // project map.
      optional string project_id = 2 [json_name = "project_id"];
    }
  }

  message SpecificReservation {
    // Indicates how many instances are actually usable currently.
    optional int64 assured_count = 1 [json_name = "assured_count"];

    // The number of resources that are allocated.
    optional int64 count = 2;

    // How many instances are in use.
    optional int64 in_use_count = 3 [json_name = "in_use_count"];

    // Specifies the instance template to create the reservation. If you use this field,
    // you must exclude the
    //
    // instanceProperties field.
    optional string source_instance_template = 4 [json_name = "source_instance_template"];

    InstanceProperties instance_properties = 5 [json_name = "instance_properties"];

    message InstanceProperties {
      // An opaque location hint used to place the allocation close to other resources.
      // This field is for use by internal tools that use the public API.
      optional string location_hint = 1 [json_name = "location_hint"];

      // The name of the machine type to reserve.
      optional string machine_type = 2 [json_name = "machine_type"];

      // The minimum CPU platform for the reservation. For example,
      //
      // '"Intel Skylake"'. See
      //
      // the CPU platform availability
      // /cloud.google.com/compute/docs/instances/specify-min-cpu-platform#availablezones)
      //
      // for information on available CPU platforms.
      optional string min_cpu_platform = 3 [json_name = "min_cpu_platform"];

      repeated GuestAccelerators guest_accelerators = 4 [json_name = "guest_accelerators"];

      repeated LocalSsds local_ssds = 5 [json_name = "local_ssds"];

      message GuestAccelerators {
        // The number of the guest accelerator cards exposed to
        //
        // this instance.
        optional int64 accelerator_count = 1 [json_name = "accelerator_count"];

        // The full or partial URL of the accelerator type to
        //
        // attach to this instance. For example:
        //
        // 'projects/my-project/zones/us-central1-c/acceleratorTypes/nvidia-tesla-p100'
        //
        //
        // If you are creating an instance template, specify only the accelerator name.
        optional string accelerator_type = 2 [json_name = "accelerator_type"];
      }

      message LocalSsds {
        // The size of the disk in base-2 GB.
        optional int64 disk_size_gb = 1 [json_name = "disk_size_gb"];

        // The disk interface to use for attaching this disk. Default value: "SCSI" Possible
        // values: ["SCSI", "NVME"]
        optional string interface = 2;
      }
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeResizeRequest version is 0
message GoogleComputeResizeRequest {
  // The creation timestamp for this resize request in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resize-request.
  optional string description = 3;

  optional string id = 4;

  // The reference of the instance group manager this ResizeRequest is a part of.
  optional string instance_group_manager = 5 [json_name = "instance_group_manager"];

  // The name of this resize request. The name must be 1-63 characters long, and
  // comply with RFC1035.
  optional string name = 6;

  optional string project = 7;

  // The number of instances to be created by this resize request. The group's target
  // size will be increased by this number.
  optional int64 resize_by = 8 [json_name = "resize_by"];

  // Current state of the request.
  optional string state = 9;

  // Status of the request.
  repeated Status status = 10;

  // The reference of the compute zone scoping this request. If it is not provided,
  // the provider zone is used.
  optional string zone = 11;

  RequestedRunDuration requested_run_duration = 12 [json_name = "requested_run_duration"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Status {
    // error: list of object
    repeated Error error = 1;

    // last_attempt: list of object
    repeated LastAttempt last_attempt = 2 [json_name = "last_attempt"];

    message Error {
      // errors: list of object
      repeated Errors errors = 1;

      message Errors {
        // code: string
        optional string code = 1;

        // error_details: list of object
        repeated ErrorDetails error_details = 2 [json_name = "error_details"];

        // location: string
        optional string location = 3;

        // message: string
        optional string message = 4;

        message ErrorDetails {
          // error_info: list of object
          repeated ErrorInfo error_info = 1 [json_name = "error_info"];

          // help: list of object
          repeated Help help = 2;

          // localized_message: list of object
          repeated LocalizedMessage localized_message = 3 [json_name = "localized_message"];

          // quota_info: list of object
          repeated QuotaInfo quota_info = 4 [json_name = "quota_info"];

          message ErrorInfo {
            // domain: string
            optional string domain = 1;

            // metadatas: map of string
            map<string, string> metadatas = 2;

            // reason: string
            optional string reason = 3;
          }

          message Help {
            // links: list of object
            repeated Links links = 1;

            message Links {
              // description: string
              optional string description = 1;

              // url: string
              optional string url = 2;
            }
          }

          message LocalizedMessage {
            // locale: string
            optional string locale = 1;

            // message: string
            optional string message = 2;
          }

          message QuotaInfo {
            // dimensions: map of string
            map<string, string> dimensions = 1;

            // future_limit: number
            optional int64 future_limit = 2 [json_name = "future_limit"];

            // limit: number
            optional int64 limit = 3;

            // limit_name: string
            optional string limit_name = 4 [json_name = "limit_name"];

            // metric_name: string
            optional string metric_name = 5 [json_name = "metric_name"];

            // rollout_status: string
            optional string rollout_status = 6 [json_name = "rollout_status"];
          }
        }
      }
    }

    message LastAttempt {
      // error: list of object
      repeated Error error = 1;

      message Error {
        // errors: list of object
        repeated Errors errors = 1;

        message Errors {
          // code: string
          optional string code = 1;

          // error_details: list of object
          repeated ErrorDetails error_details = 2 [json_name = "error_details"];

          // location: string
          optional string location = 3;

          // message: string
          optional string message = 4;

          message ErrorDetails {
            // error_info: list of object
            repeated ErrorInfo error_info = 1 [json_name = "error_info"];

            // help: list of object
            repeated Help help = 2;

            // localized_message: list of object
            repeated LocalizedMessage localized_message = 3 [json_name = "localized_message"];

            // quota_info: list of object
            repeated QuotaInfo quota_info = 4 [json_name = "quota_info"];

            message ErrorInfo {
              // domain: string
              optional string domain = 1;

              // metadatas: map of string
              map<string, string> metadatas = 2;

              // reason: string
              optional string reason = 3;
            }

            message Help {
              // links: list of object
              repeated Links links = 1;

              message Links {
                // description: string
                optional string description = 1;

                // url: string
                optional string url = 2;
              }
            }

            message LocalizedMessage {
              // locale: string
              optional string locale = 1;

              // message: string
              optional string message = 2;
            }

            message QuotaInfo {
              // dimensions: map of string
              map<string, string> dimensions = 1;

              // future_limit: number
              optional int64 future_limit = 2 [json_name = "future_limit"];

              // limit: number
              optional int64 limit = 3;

              // limit_name: string
              optional string limit_name = 4 [json_name = "limit_name"];

              // metric_name: string
              optional string metric_name = 5 [json_name = "metric_name"];

              // rollout_status: string
              optional string rollout_status = 6 [json_name = "rollout_status"];
            }
          }
        }
      }
    }
  }

  message RequestedRunDuration {
    // Span of time that's a fraction of a second at nanosecond resolution. Durations
    // less than one second are represented with a 0 seconds field and a positive nanos
    // field. Must be from 0 to 999,999,999 inclusive.
    optional int64 nanos = 1;

    // Span of time at a resolution of a second. Must be from 600 to 604800 inclusive.
    // Note: minimum and maximum allowed range for requestedRunDuration is 10 minutes
    // (600 seconds) and 7 days(604800 seconds) correspondingly.
    optional string seconds = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeResourcePolicy version is 0
message GoogleComputeResourcePolicy {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 2;

  optional string id = 3;

  // The name of the resource, provided by the client when initially creating
  //
  // the resource. The resource name must be 1-63 characters long, and comply
  //
  // with RFC1035. Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])'? which means the
  //
  // first character must be a lowercase letter, and all following characters
  //
  // must be a dash, lowercase letter, or digit, except the last character,
  //
  // which cannot be a dash.
  optional string name = 4;

  optional string project = 5;

  // Region where resource policy resides.
  optional string region = 6;

  optional string self_link = 7 [json_name = "self_link"];

  DiskConsistencyGroupPolicy disk_consistency_group_policy = 8 [json_name = "disk_consistency_group_policy"];

  GroupPlacementPolicy group_placement_policy = 9 [json_name = "group_placement_policy"];

  InstanceSchedulePolicy instance_schedule_policy = 10 [json_name = "instance_schedule_policy"];

  SnapshotSchedulePolicy snapshot_schedule_policy = 11 [json_name = "snapshot_schedule_policy"];

  repeated Timeouts timeouts = 12;

  WorkloadPolicy workload_policy = 13 [json_name = "workload_policy"];

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message DiskConsistencyGroupPolicy {
    // Enable disk consistency on the resource policy.
    optional bool enabled = 1;
  }

  message GroupPlacementPolicy {
    // The number of availability domains instances will be spread across. If two
    // instances are in different
    //
    // availability domain, they will not be put in the same low latency network
    optional int64 availability_domain_count = 1 [json_name = "availability_domain_count"];

    // Collocation specifies whether to place VMs inside the same availability domain on
    // the same low-latency network.
    //
    // Specify 'COLLOCATED' to enable collocation. Can only be specified with
    // 'vm_count'. If compute instances are created
    //
    // with a COLLOCATED policy, then exactly 'vm_count' instances must be created at
    // the same time with the resource policy
    //
    // attached. Possible values: ["COLLOCATED"]
    optional string collocation = 2;

    // Specifies the shape of the GPU slice, in slice based GPU families eg. A4X.
    optional string gpu_topology = 3 [json_name = "gpu_topology"];

    // Number of VMs in this placement group. Google does not recommend that you use
    // this field
    //
    // unless you use a compact policy and you want your policy to work only if it
    // contains this
    //
    // exact number of VMs.
    optional int64 vm_count = 4 [json_name = "vm_count"];
  }

  message InstanceSchedulePolicy {
    // The expiration time of the schedule. The timestamp is an RFC3339 string.
    optional string expiration_time = 1 [json_name = "expiration_time"];

    // The start time of the schedule. The timestamp is an RFC3339 string.
    optional string start_time = 2 [json_name = "start_time"];

    // Specifies the time zone to be used in interpreting the schedule. The value of
    // this field must be a time zone name
    //
    // from the tz database: http://en.wikipedia.org/wiki/Tz_database.
    optional string time_zone = 3 [json_name = "time_zone"];

    VmStartSchedule vm_start_schedule = 4 [json_name = "vm_start_schedule"];

    VmStopSchedule vm_stop_schedule = 5 [json_name = "vm_stop_schedule"];

    message VmStartSchedule {
      // Specifies the frequency for the operation, using the unix-cron format.
      optional string schedule = 1;
    }

    message VmStopSchedule {
      // Specifies the frequency for the operation, using the unix-cron format.
      optional string schedule = 1;
    }
  }

  message SnapshotSchedulePolicy {
    RetentionPolicy retention_policy = 1 [json_name = "retention_policy"];

    Schedule schedule = 2;

    SnapshotProperties snapshot_properties = 3 [json_name = "snapshot_properties"];

    message RetentionPolicy {
      // Maximum age of the snapshot that is allowed to be kept.
      optional int64 max_retention_days = 1 [json_name = "max_retention_days"];

      // Specifies the behavior to apply to scheduled snapshots when
      //
      // the source disk is deleted. Default value: "KEEP_AUTO_SNAPSHOTS" Possible values:
      // ["KEEP_AUTO_SNAPSHOTS", "APPLY_RETENTION_POLICY"]
      optional string on_source_disk_delete = 2 [json_name = "on_source_disk_delete"];
    }

    message Schedule {
      DailySchedule daily_schedule = 1 [json_name = "daily_schedule"];

      HourlySchedule hourly_schedule = 2 [json_name = "hourly_schedule"];

      WeeklySchedule weekly_schedule = 3 [json_name = "weekly_schedule"];

      message DailySchedule {
        // Defines a schedule with units measured in days. The value determines how many
        // days pass between the start of each cycle. Days in cycle for snapshot schedule
        // policy must be 1.
        optional int64 days_in_cycle = 1 [json_name = "days_in_cycle"];

        // This must be in UTC format that resolves to one of
        //
        // 00:00, 04:00, 08:00, 12:00, 16:00, or 20:00. For example,
        //
        // both 13:00-5 and 08:00 are valid.
        optional string start_time = 2 [json_name = "start_time"];
      }

      message HourlySchedule {
        // The number of hours between snapshots.
        optional int64 hours_in_cycle = 1 [json_name = "hours_in_cycle"];

        // Time within the window to start the operations.
        //
        // It must be in an hourly format "HH:MM",
        //
        // where HH : [00-23] and MM : [00] GMT. eg: 21:00
        optional string start_time = 2 [json_name = "start_time"];
      }

      message WeeklySchedule {
        repeated DayOfWeeks day_of_weeks = 1 [json_name = "day_of_weeks"];

        message DayOfWeeks {
          // The day of the week to create the snapshot. e.g. MONDAY Possible values:
          // ["MONDAY", "TUESDAY", "WEDNESDAY", "THURSDAY", "FRIDAY", "SATURDAY", "SUNDAY"]
          optional string day = 1;

          // Time within the window to start the operations.
          //
          // It must be in format "HH:MM", where HH : [00-23] and MM : [00-00] GMT.
          optional string start_time = 2 [json_name = "start_time"];
        }
      }
    }

    message SnapshotProperties {
      // Creates the new snapshot in the snapshot chain labeled with the
      //
      // specified name. The chain name must be 1-63 characters long and comply
      //
      // with RFC1035.
      optional string chain_name = 1 [json_name = "chain_name"];

      // Whether to perform a 'guest aware' snapshot.
      optional bool guest_flush = 2 [json_name = "guest_flush"];

      // A set of key-value pairs.
      map<string, string> labels = 3;

      // Cloud Storage bucket location to store the auto snapshot
      //
      // (regional or multi-regional)
      repeated string storage_locations = 4 [json_name = "storage_locations"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message WorkloadPolicy {
    // The accelerator topology. This field can be set only when the workload policy
    // type is HIGH_THROUGHPUT
    //
    // and cannot be set if max topology distance is set.
    optional string accelerator_topology = 1 [json_name = "accelerator_topology"];

    // The maximum topology distance. This field can be set only when the workload
    // policy type is HIGH_THROUGHPUT
    //
    // and cannot be set if accelerator topology or accelerator topology mode is set.
    // Possible values: ["BLOCK", "CLUSTER", "SUBBLOCK"]
    optional string max_topology_distance = 2 [json_name = "max_topology_distance"];

    // The type of workload policy. Possible values: ["HIGH_AVAILABILITY",
    // "HIGH_THROUGHPUT"]
    optional string type = 3;
  }
}

// GoogleComputeResourcePolicyAttachment version is 0
message GoogleComputeResourcePolicyAttachment {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The name of the instance in which the resource policies are attached to.
  optional string instance = 3;

  // The resource policy to be attached to the instance for scheduling start/stop
  //
  // operations. Do not specify the self link.
  optional string name = 4;

  optional string project = 5;

  // A reference to the zone where the instance resides.
  optional string zone = 6;

  repeated Timeouts timeouts = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRolloutPlan version is 0
message GoogleComputeRolloutPlan {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 2;

  optional string id = 3;

  // The location scope of the rollout plan. Possible values:
  // ["LOCATION_SCOPE_UNSPECIFIED", "ZONAL", "REGIONAL"]
  optional string location_scope = 4 [json_name = "location_scope"];

  // Name of the resource. Provided by the client when the resource is created.
  //
  // The name must be 1-63 characters long, and comply with RFC1035.
  optional string name = 5;

  optional string project = 6;

  optional string self_link = 7 [json_name = "self_link"];

  repeated Timeouts timeouts = 8;

  repeated Waves waves = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  optional int32 count = 12;

  optional string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }

  message Waves {
    // The display name of this wave of the rollout plan.
    optional string display_name = 1 [json_name = "display_name"];

    // The wave number.
    optional int64 number = 2;

    OrchestrationOptions orchestration_options = 3 [json_name = "orchestration_options"];

    repeated Selectors selectors = 4;

    Validation validation = 5;

    message OrchestrationOptions {
      // Maximum number of locations to be orchestrated in parallel.
      optional int64 max_concurrent_locations = 1 [json_name = "max_concurrent_locations"];

      // Maximum number of resources to be orchestrated per location in parallel.
      optional int64 max_concurrent_resources_per_location = 2 [
        json_name = "max_concurrent_resources_per_location"
      ];

      repeated Delays delays = 3;

      message Delays {
        // Controls whether the delay should only be added between batches of projects
        // corresponding to different locations, or also between batches of projects
        // corresponding to the same location. Possible values: ["DELIMITER_UNSPECIFIED",
        // "DELIMITER_LOCATION", "DELIMITER_BATCH"]
        optional string delimiter = 1;

        // The duration of the delay, if any, to be added between batches of projects.
        optional string duration = 2;

        // Controls whether the specified duration is to be added at the end of each batch,
        // or if the total processing time for each batch will be padded if needed to meet
        // the specified duration. Possible values: ["TYPE_UNSPECIFIED", "TYPE_OFFSET",
        // "TYPE_MINIMUM"]
        optional string type = 3;
      }
    }

    message Selectors {
      LocationSelector location_selector = 1 [json_name = "location_selector"];

      ResourceHierarchySelector resource_hierarchy_selector = 2 [json_name = "resource_hierarchy_selector"];

      message LocationSelector {
        // Example: "us-central1-a"
        repeated string included_locations = 1 [json_name = "included_locations"];
      }

      message ResourceHierarchySelector {
        // Format: "folders/{folder_id}"
        repeated string included_folders = 1 [json_name = "included_folders"];

        // Format: "organizations/{organization_id}"
        repeated string included_organizations = 2 [json_name = "included_organizations"];

        // Format: "projects/{project_id}"
        repeated string included_projects = 3 [json_name = "included_projects"];
      }
    }

    message Validation {
      // The type of the validation. Possible values:
      //
      // "manual": The system waits for an end-user approval API before progressing to the
      // next wave.
      //
      // "time": The system waits for a user specified duration before progressing to the
      // next wave.
      optional string type = 1;

      TimeBasedValidationMetadata time_based_validation_metadata = 2 [json_name = "time_based_validation_metadata"];

      message TimeBasedValidationMetadata {
        // The duration that the system waits in between waves. This wait starts
        //
        // after all changes in the wave are rolled out.
        optional string wait_duration = 1 [json_name = "wait_duration"];
      }
    }
  }
}

// GoogleComputeRoute version is 0
message GoogleComputeRoute {
  repeated AsPaths as_paths = 1 [json_name = "as_paths"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property
  //
  // when you create the resource.
  optional string description = 4;

  // The destination range of outgoing packets that this route applies to.
  //
  // Only IPv4 is supported.
  optional string dest_range = 5 [json_name = "dest_range"];

  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the
  //
  // last character, which cannot be a dash.
  optional string name = 7;

  // The network that this route applies to.
  optional string network = 8;

  // URL to a gateway that should handle matching packets.
  //
  // Currently, you can only specify the internet gateway, using a full or
  //
  // partial valid URL:
  //
  // gleapis.com/compute/v1/projects/project/global/gateways/default-internet-gateway'
  //
  // * 'projects/project/global/gateways/default-internet-gateway'
  //
  // * 'global/gateways/default-internet-gateway'
  //
  // * The string 'default-internet-gateway'.
  optional string next_hop_gateway = 9 [json_name = "next_hop_gateway"];

  // The hub network that should handle matching packets, which should conform to
  // RFC1035.
  optional string next_hop_hub = 10 [json_name = "next_hop_hub"];

  // The IP address or URL to a forwarding rule of type
  //
  // loadBalancingScheme=INTERNAL that should handle matching
  //
  // packets.
  //
  //
  // With the GA provider you can only specify the forwarding
  //
  // rule as a partial or full URL. For example, the following
  //
  // are all valid values:
  //
  // * 10.128.0.56
  //
  // pis.com/compute/v1/projects/project/regions/region/forwardingRules/forwardingRule
  //
  // * regions/region/forwardingRules/forwardingRule
  //
  //
  // When the beta provider, you can also specify the IP address
  //
  // of a forwarding rule from the same VPC or any peered VPC.
  //
  //
  // Note that this can only be used when the destinationRange is
  //
  // a public (non-RFC 1918) IP CIDR range.
  optional string next_hop_ilb = 11 [json_name = "next_hop_ilb"];

  // URL to an instance that should handle matching packets.
  //
  // You can specify this as a full or partial URL. For example:
  //
  // s://www.googleapis.com/compute/v1/projects/project/zones/zone/instances/instance'
  //
  // * 'projects/project/zones/zone/instances/instance'
  //
  // * 'zones/zone/instances/instance'
  //
  // * Just the instance name, with the zone in 'next_hop_instance_zone'.
  optional string next_hop_instance = 12 [json_name = "next_hop_instance"];

  // The zone of the instance specified in next_hop_instance. Omit if
  // next_hop_instance is specified as a URL.
  optional string next_hop_instance_zone = 13 [json_name = "next_hop_instance_zone"];

  // Internal fixed region-to-region cost that Google Cloud calculates based on
  // factors such as network performance, distance, and available bandwidth between
  // regions.
  optional string next_hop_inter_region_cost = 14 [json_name = "next_hop_inter_region_cost"];

  // Network IP address of an instance that should handle matching packets.
  optional string next_hop_ip = 15 [json_name = "next_hop_ip"];

  // Multi-Exit Discriminator, a BGP route metric that indicates the desirability of a
  // particular route in a network.
  optional string next_hop_med = 16 [json_name = "next_hop_med"];

  // URL to a Network that should handle matching packets.
  optional string next_hop_network = 17 [json_name = "next_hop_network"];

  // Indicates the origin of the route. Can be IGP (Interior Gateway Protocol), EGP
  // (Exterior Gateway Protocol), or INCOMPLETE.
  optional string next_hop_origin = 18 [json_name = "next_hop_origin"];

  // The network peering name that should handle matching packets, which should
  // conform to RFC1035.
  optional string next_hop_peering = 19 [json_name = "next_hop_peering"];

  // URL to a VpnTunnel that should handle matching packets.
  optional string next_hop_vpn_tunnel = 20 [json_name = "next_hop_vpn_tunnel"];

  // The priority of this route. Priority is used to break ties in cases
  //
  // where there is more than one matching route of equal prefix length.
  //
  //
  // In the case of two routes with equal prefix length, the one with the
  //
  // lowest-numbered priority value wins.
  //
  //
  // Default value is 1000. Valid range is 0 through 65535.
  optional int64 priority = 21;

  optional string project = 22;

  // The status of the route, which can be one of the following values:
  //
  // - 'ACTIVE' for an active route
  //
  // - 'INACTIVE' for an inactive route
  optional string route_status = 23 [json_name = "route_status"];

  // The type of this route, which can be one of the following values:
  //
  // - 'TRANSIT' for a transit route that this router learned from another Cloud
  // Router and will readvertise to one of its BGP peers
  //
  // - 'SUBNET' for a route from a subnet of the VPC
  //
  // - 'BGP' for a route learned from a BGP peer of this router
  //
  // - 'STATIC' for a static route
  optional string route_type = 24 [json_name = "route_type"];

  optional string self_link = 25 [json_name = "self_link"];

  // A list of instance tags to which this route applies.
  repeated string tags = 26;

  // If potential misconfigurations are detected for this route, this field will be
  // populated with warning messages.
  repeated Warnings warnings = 27;

  Params params = 28;

  repeated Timeouts timeouts = 29;

  google.protobuf.Value for_each = 30 [json_name = "for_each"];

  repeated string depends_on = 31 [json_name = "depends_on"];

  optional int32 count = 32;

  optional string provider = 33;

  terraform.v1.Lifecycle lifecycle = 34;

  message AsPaths {
    // as_lists: list of number
    repeated int64 as_lists = 1 [json_name = "as_lists"];

    // path_segment_type: string
    optional string path_segment_type = 2 [json_name = "path_segment_type"];
  }

  message Warnings {
    // code: string
    optional string code = 1;

    // data: list of object
    repeated Data data = 2;

    // message: string
    optional string message = 3;

    message Data {
      // key: string
      optional string key = 1;

      // value: string
      optional string value = 2;
    }
  }

  message Params {
    // Resource manager tags to be bound to the route. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456. The field is ignored when empty.
    //
    // The field is immutable and causes resource replacement when mutated. This field
    // is only
    //
    // set at create time and modifying this field after creation will trigger
    // recreation.
    //
    // To apply tags to an existing resource, see the google_tags_tag_binding resource.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRouter version is 0
message GoogleComputeRouter {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Indicates if a router is dedicated for use with encrypted VLAN
  //
  // attachments (interconnectAttachments).
  optional bool encrypted_interconnect_router = 4 [json_name = "encrypted_interconnect_router"];

  optional string id = 5;

  // Name of the resource. The name must be 1-63 characters long, and
  //
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  //
  // which means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 6;

  // A URI of an NCC Gateway spoke
  optional string ncc_gateway = 7 [json_name = "ncc_gateway"];

  // A reference to the network to which this router belongs.
  optional string network = 8;

  optional string project = 9;

  // Region where the router resides.
  optional string region = 10;

  optional string self_link = 11 [json_name = "self_link"];

  Bgp bgp = 12;

  Md5AuthenticationKeys md5_authentication_keys = 13 [json_name = "md5_authentication_keys"];

  Params params = 14;

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  optional int32 count = 18;

  optional string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message Bgp {
    // User-specified flag to indicate which mode to use for advertisement. Default
    // value: "DEFAULT" Possible values: ["DEFAULT", "CUSTOM"]
    optional string advertise_mode = 1 [json_name = "advertise_mode"];

    // User-specified list of prefix groups to advertise in custom mode.
    //
    // This field can only be populated if advertiseMode is CUSTOM and
    //
    // is advertised to all peers of the router. These groups will be
    //
    // advertised in addition to any specified prefixes. Leave this field
    //
    // blank to advertise no custom groups.
    //
    //
    // This enum field has the one valid value: ALL_SUBNETS
    repeated string advertised_groups = 2 [json_name = "advertised_groups"];

    // Local BGP Autonomous System Number (ASN). Must be an RFC6996
    //
    // private ASN, either 16-bit or 32-bit. The value will be fixed for
    //
    // this router resource. All VPN tunnels that link to this router
    //
    // will have the same local ASN.
    optional int64 asn = 3;

    // Explicitly specifies a range of valid BGP Identifiers for this Router.
    //
    // It is provided as a link-local IPv4 range (from 169.254.0.0/16), of
    //
    // size at least /30, even if the BGP sessions are over IPv6. It must
    //
    // not overlap with any IPv4 BGP session ranges. Other vendors commonly
    //
    // call this router ID.
    optional string identifier_range = 4 [json_name = "identifier_range"];

    // The interval in seconds between BGP keepalive messages that are sent
    //
    // to the peer. Hold time is three times the interval at which keepalive
    //
    // messages are sent, and the hold time is the maximum number of seconds
    //
    // allowed to elapse between successive keepalive messages that BGP
    //
    // receives from a peer.
    //
    //
    // BGP will use the smaller of either the local hold time value or the
    //
    // peer's hold time value as the hold time for the BGP connection
    //
    // between the two peers. If set, this value must be between 20 and 60.
    //
    // The default is 20.
    optional int64 keepalive_interval = 5 [json_name = "keepalive_interval"];

    repeated AdvertisedIpRanges advertised_ip_ranges = 6 [json_name = "advertised_ip_ranges"];

    message AdvertisedIpRanges {
      // User-specified description for the IP range.
      optional string description = 1;

      // The IP range to advertise. The value must be a
      //
      // CIDR-formatted string.
      optional string range = 2;
    }
  }

  message Md5AuthenticationKeys {
    // Value of the key used for MD5 authentication.
    optional string key = 1;

    // Name used to identify the key. Must be unique within a router.
    //
    // Must be referenced by exactly one bgpPeer. Must comply with RFC1035.
    optional string name = 2;
  }

  message Params {
    // Resource manager tags to be bound to the router. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRouterInterface version is 0
message GoogleComputeRouterInterface {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The name or resource link to the VLAN interconnect for this interface. Changing
  // this forces a new interface to be created. Only one of interconnect_attachment,
  // subnetwork or vpn_tunnel can be specified.
  optional string interconnect_attachment = 3 [json_name = "interconnect_attachment"];

  // The IP address and range of the interface. The IP range must be in the RFC3927
  // link-local IP space. Changing this forces a new interface to be created.
  optional string ip_range = 4 [json_name = "ip_range"];

  // IP version of this interface.
  optional string ip_version = 5 [json_name = "ip_version"];

  // A unique name for the interface, required by GCE. Changing this forces a new
  // interface to be created.
  optional string name = 6;

  // The regional private internal IP address that is used to establish BGP sessions
  // to a VM instance acting as a third-party Router Appliance. Changing this forces a
  // new interface to be created.
  optional string private_ip_address = 7 [json_name = "private_ip_address"];

  // The ID of the project in which this interface's router belongs. If it is not
  // provided, the provider project is used. Changing this forces a new interface to
  // be created.
  optional string project = 8;

  // The name of the interface that is redundant to this interface. Changing this
  // forces a new interface to be created.
  optional string redundant_interface = 9 [json_name = "redundant_interface"];

  // The region this interface's router sits in. If not specified, the project region
  // will be used. Changing this forces a new interface to be created.
  optional string region = 10;

  // The name of the router this interface will be attached to. Changing this forces a
  // new interface to be created.
  optional string router = 11;

  // The URI of the subnetwork resource that this interface belongs to, which must be
  // in the same region as the Cloud Router. Changing this forces a new interface to
  // be created. Only one of subnetwork, interconnect_attachment or vpn_tunnel can be
  // specified.
  optional string subnetwork = 12;

  // The name or resource link to the VPN tunnel this interface will be linked to.
  // Changing this forces a new interface to be created. Only one of vpn_tunnel,
  // interconnect_attachment or subnetwork can be specified.
  optional string vpn_tunnel = 13 [json_name = "vpn_tunnel"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeRouterNamedSet version is 0
message GoogleComputeRouterNamedSet {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of the Named Set.
  optional string description = 2;

  // The fingerprint used for optimistic locking of this resource. Used
  //
  // internally during updates.
  optional string fingerprint = 3;

  optional string id = 4;

  // The name of the Named Set, which must be a resource ID segment and unique
  //
  // within all named sets owned by the Router.
  optional string name = 5;

  optional string project = 6;

  // Region where the router resides.
  optional string region = 7;

  // The name of the Cloud Router in which this Named Set will be configured.
  optional string router = 8;

  // The type of the Named Set. Possible values: ["NAMED_SET_TYPE_PREFIX",
  // "NAMED_SET_TYPE_COMMUNITY"]
  optional string type = 9;

  repeated Elements elements = 10;

  repeated Timeouts timeouts = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  optional int32 count = 14;

  optional string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Elements {
    // Description of the expression.
    optional string description = 1;

    // Textual representation of an expression in Common Expression Language syntax.
    optional string expression = 2;

    // String indicating the location of the expression for error reporting, e.g. a file
    // name and a position in the file.
    optional string location = 3;

    // Title for the expression, i.e. a short string describing its purpose.
    optional string title = 4;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRouterNat version is 0
message GoogleComputeRouterNat {
  // The network tier to use when automatically reserving NAT IP addresses.
  //
  // Must be one of: PREMIUM, STANDARD. If not specified, then the current
  //
  // project-level default tier is used. Possible values: ["PREMIUM", "STANDARD"]
  optional string auto_network_tier = 1 [json_name = "auto_network_tier"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // A list of URLs of the IP resources to be drained. These IPs must be
  //
  // valid static external IPs that have been assigned to the NAT.
  repeated string drain_nat_ips = 3 [json_name = "drain_nat_ips"];

  // Enable Dynamic Port Allocation.
  //
  // If minPortsPerVm is set, minPortsPerVm must be set to a power of two greater than
  // or equal to 32.
  //
  // If minPortsPerVm is not set, a minimum of 32 ports will be allocated to a VM from
  // this NAT config.
  //
  // If maxPortsPerVm is set, maxPortsPerVm must be set to a power of two greater than
  // minPortsPerVm.
  //
  // If maxPortsPerVm is not set, a maximum of 65536 ports will be allocated to a VM
  // from this NAT config.
  //
  //
  // Mutually exclusive with enableEndpointIndependentMapping.
  optional bool enable_dynamic_port_allocation = 4 [json_name = "enable_dynamic_port_allocation"];

  // Enable endpoint independent mapping.
  //
  // For more information see the [official
  // documentation](https://docs.cloud.google.com/nat/docs/public-nat#specs-rfcs).
  optional bool enable_endpoint_independent_mapping = 5 [json_name = "enable_endpoint_independent_mapping"];

  // Specifies the endpoint Types supported by the NAT Gateway.
  //
  // Supported values include:
  //
  // 'ENDPOINT_TYPE_VM', 'ENDPOINT_TYPE_SWG',
  //
  // 'ENDPOINT_TYPE_MANAGED_PROXY_LB'.
  repeated string endpoint_types = 6 [json_name = "endpoint_types"];

  // Timeout (in seconds) for ICMP connections. Defaults to 30s if not set.
  optional int64 icmp_idle_timeout_sec = 7 [json_name = "icmp_idle_timeout_sec"];

  optional string id = 8;

  // Self-links of NAT IPs to be used as initial value for creation alongside a
  // RouterNatAddress resource.
  //
  // Conflicts with natIps and drainNatIps. Only valid if natIpAllocateOption is set
  // to MANUAL_ONLY.
  repeated string initial_nat_ips = 9 [json_name = "initial_nat_ips"];

  // Maximum number of ports allocated to a VM from this NAT.
  //
  // This field can only be set when enableDynamicPortAllocation is enabled.
  optional int64 max_ports_per_vm = 10 [json_name = "max_ports_per_vm"];

  // Minimum number of ports allocated to a VM from this NAT. Defaults to 64 for
  // static port allocation and 32 dynamic port allocation if not set.
  optional int64 min_ports_per_vm = 11 [json_name = "min_ports_per_vm"];

  // Name of the NAT service. The name must be 1-63 characters long and
  //
  // comply with RFC1035.
  optional string name = 12;

  // How external IPs should be allocated for this NAT. Valid values are
  //
  // 'AUTO_ONLY' for only allowing NAT IPs allocated by Google Cloud
  //
  // Platform, or 'MANUAL_ONLY' for only user-allocated NAT IP addresses. Possible
  // values: ["MANUAL_ONLY", "AUTO_ONLY"]
  optional string nat_ip_allocate_option = 13 [json_name = "nat_ip_allocate_option"];

  // Self-links of NAT IPs. Only valid if natIpAllocateOption
  //
  // is set to MANUAL_ONLY.
  //
  // If this field is used alongside with a count created list of address resources
  // 'google_compute_address.foobar.*.self_link',
  //
  // the access level resource for the address resource must have a 'lifecycle' block
  // with 'create_before_destroy = true' so
  //
  // the number of resources can be increased/decreased without triggering the
  // 'resourceInUseByAnotherResource' error.
  repeated string nat_ips = 14 [json_name = "nat_ips"];

  optional string project = 15;

  // Region where the router and NAT reside.
  optional string region = 16;

  // The name of the Cloud Router in which this NAT will be configured.
  optional string router = 17;

  // How NAT should be configured per Subnetwork.
  //
  // If 'ALL_SUBNETWORKS_ALL_IP_RANGES', all of the
  //
  // IP ranges in every Subnetwork are allowed to Nat.
  //
  // If 'ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES', all of the primary IP
  //
  // ranges in every Subnetwork are allowed to Nat.
  //
  // 'LIST_OF_SUBNETWORKS': A list of Subnetworks are allowed to Nat
  //
  // (specified in the field subnetwork below). Note that if this field
  //
  // contains ALL_SUBNETWORKS_ALL_IP_RANGES or
  //
  // ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES, then there should not be any
  //
  // other RouterNat section in any Router for this network in this region. Possible
  // values: ["ALL_SUBNETWORKS_ALL_IP_RANGES",
  // "ALL_SUBNETWORKS_ALL_PRIMARY_IP_RANGES", "LIST_OF_SUBNETWORKS"]
  optional string source_subnetwork_ip_ranges_to_nat = 18 [json_name = "source_subnetwork_ip_ranges_to_nat"];

  // Specify the Nat option for NAT64, which can take one of the following values:
  //
  // ALL_IPV6_SUBNETWORKS: All of the IP ranges in every Subnetwork are allowed to
  // Nat.
  //
  // LIST_OF_IPV6_SUBNETWORKS: A list of Subnetworks are allowed to Nat (specified in
  // the field nat64Subnetwork below).
  //
  // Note that if this field contains NAT64_ALL_V6_SUBNETWORKS no other Router.Nat
  // section in this region can also enable NAT64 for any Subnetworks in this network.
  //
  // Other Router.Nat sections can still be present to enable NAT44 only. Possible
  // values: ["ALL_IPV6_SUBNETWORKS", "LIST_OF_IPV6_SUBNETWORKS"]
  optional string source_subnetwork_ip_ranges_to_nat64 = 19 [json_name = "source_subnetwork_ip_ranges_to_nat64"];

  // Timeout (in seconds) for TCP established connections.
  //
  // Defaults to 1200s if not set.
  optional int64 tcp_established_idle_timeout_sec = 20 [json_name = "tcp_established_idle_timeout_sec"];

  // Timeout (in seconds) for TCP connections that are in TIME_WAIT state.
  //
  // Defaults to 120s if not set.
  optional int64 tcp_time_wait_timeout_sec = 21 [json_name = "tcp_time_wait_timeout_sec"];

  // Timeout (in seconds) for TCP transitory connections.
  //
  // Defaults to 30s if not set.
  optional int64 tcp_transitory_idle_timeout_sec = 22 [json_name = "tcp_transitory_idle_timeout_sec"];

  // Indicates whether this NAT is used for public or private IP translation.
  //
  // If unspecified, it defaults to PUBLIC.
  //
  // If 'PUBLIC' NAT used for public IP translation.
  //
  // If 'PRIVATE' NAT used for private IP translation. Default value: "PUBLIC"
  // Possible values: ["PUBLIC", "PRIVATE"]
  optional string type = 23;

  // Timeout (in seconds) for UDP connections. Defaults to 30s if not set.
  optional int64 udp_idle_timeout_sec = 24 [json_name = "udp_idle_timeout_sec"];

  LogConfig log_config = 25 [json_name = "log_config"];

  repeated Nat64Subnetwork nat64_subnetwork = 26 [json_name = "nat64_subnetwork"];

  repeated Rules rules = 27;

  repeated Subnetwork subnetwork = 28;

  repeated Timeouts timeouts = 29;

  google.protobuf.Value for_each = 30 [json_name = "for_each"];

  repeated string depends_on = 31 [json_name = "depends_on"];

  optional int32 count = 32;

  optional string provider = 33;

  terraform.v1.Lifecycle lifecycle = 34;

  message LogConfig {
    // Indicates whether or not to export logs.
    optional bool enable = 1;

    // Specifies the desired filtering of logs on this NAT. Possible values:
    // ["ERRORS_ONLY", "TRANSLATIONS_ONLY", "ALL"]
    optional string filter = 2;
  }

  message Nat64Subnetwork {
    // Self-link of the subnetwork resource that will use NAT64
    optional string name = 1;
  }

  message Rules {
    // An optional description of this rule.
    optional string description = 1;

    // CEL expression that specifies the match condition that egress traffic from a VM
    // is evaluated against.
    //
    // If it evaluates to true, the corresponding action is enforced.
    //
    //
    // The following examples are valid match expressions for public NAT:
    //
    //
    // "inIpRange(destination.ip, '1.1.0.0/16') || inIpRange(destination.ip,
    // '2.2.0.0/16')"
    //
    //
    // "destination.ip == '1.1.0.1' || destination.ip == '8.8.8.8'"
    //
    //
    // The following example is a valid match expression for private NAT:
    //
    //
    // "nexthop.hub ==
    // etworkconnectivity.googleapis.com/v1alpha1/projects/my-project/global/hub/hub-1'"
    optional string match = 2;

    // An integer uniquely identifying a rule in the list.
    //
    // The rule number must be a positive value between 0 and 65000, and must be unique
    // among rules within a NAT.
    optional int64 rule_number = 3 [json_name = "rule_number"];

    Action action = 4;

    message Action {
      // A list of URLs of the IP resources used for this NAT rule.
      //
      // These IP addresses must be valid static external IP addresses assigned to the
      // project.
      //
      // This field is used for public NAT.
      repeated string source_nat_active_ips = 1 [json_name = "source_nat_active_ips"];

      // A list of URLs of the subnetworks used as source ranges for this NAT Rule.
      //
      // These subnetworks must have purpose set to PRIVATE_NAT.
      //
      // This field is used for private NAT.
      repeated string source_nat_active_ranges = 2 [json_name = "source_nat_active_ranges"];

      // A list of URLs of the IP resources to be drained.
      //
      // These IPs must be valid static external IPs that have been assigned to the NAT.
      //
      // These IPs should be used for updating/patching a NAT rule only.
      //
      // This field is used for public NAT.
      repeated string source_nat_drain_ips = 3 [json_name = "source_nat_drain_ips"];

      // A list of URLs of subnetworks representing source ranges to be drained.
      //
      // This is only supported on patch/update, and these subnetworks must have
      // previously been used as active ranges in this NAT Rule.
      //
      // This field is used for private NAT.
      repeated string source_nat_drain_ranges = 4 [json_name = "source_nat_drain_ranges"];
    }
  }

  message Subnetwork {
    // Self-link of subnetwork to NAT
    optional string name = 1;

    // List of the secondary ranges of the subnetwork that are allowed
    //
    // to use NAT. This can be populated only if
    //
    // 'LIST_OF_SECONDARY_IP_RANGES' is one of the values in
    //
    // sourceIpRangesToNat
    repeated string secondary_ip_range_names = 2 [json_name = "secondary_ip_range_names"];

    // List of options for which source IPs in the subnetwork
    //
    // should have NAT enabled. Supported values include:
    //
    // 'ALL_IP_RANGES', 'LIST_OF_SECONDARY_IP_RANGES',
    //
    // 'PRIMARY_IP_RANGE'.
    repeated string source_ip_ranges_to_nat = 3 [json_name = "source_ip_ranges_to_nat"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRouterNatAddress version is 0
message GoogleComputeRouterNatAddress {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // A list of URLs of the IP resources to be drained. These IPs must be
  //
  // valid static external IPs that have been assigned to the NAT.
  repeated string drain_nat_ips = 2 [json_name = "drain_nat_ips"];

  optional string id = 3;

  // Self-links of NAT IPs to be used in a Nat service. Only valid if the referenced
  // RouterNat
  //
  // natIpAllocateOption is set to MANUAL_ONLY.
  repeated string nat_ips = 4 [json_name = "nat_ips"];

  optional string project = 5;

  // Region where the NAT service reside.
  optional string region = 6;

  // The name of the Cloud Router in which the referenced NAT service is configured.
  optional string router = 7;

  // The name of the Nat service in which this address will be configured.
  optional string router_nat = 8 [json_name = "router_nat"];

  repeated Timeouts timeouts = 9;

  google.protobuf.Value for_each = 10 [json_name = "for_each"];

  repeated string depends_on = 11 [json_name = "depends_on"];

  optional int32 count = 12;

  optional string provider = 13;

  terraform.v1.Lifecycle lifecycle = 14;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRouterPeer version is 0
message GoogleComputeRouterPeer {
  // User-specified flag to indicate which mode to use for advertisement.
  //
  // Valid values of this enum field are: 'DEFAULT', 'CUSTOM' Default value: "DEFAULT"
  // Possible values: ["DEFAULT", "CUSTOM"]
  optional string advertise_mode = 1 [json_name = "advertise_mode"];

  // User-specified list of prefix groups to advertise in custom
  //
  // mode, which currently supports the following option:
  //
  //
  // * 'ALL_SUBNETS': Advertises all of the router's own VPC subnets.
  //
  // This excludes any routes learned for subnets that use VPC Network
  //
  // Peering.
  //
  //
  //
  // Note that this field can only be populated if advertiseMode is 'CUSTOM'
  //
  // and overrides the list defined for the router (in the "bgp" message).
  //
  // These groups are advertised in addition to any specified prefixes.
  //
  // Leave this field blank to advertise no custom groups.
  repeated string advertised_groups = 2 [json_name = "advertised_groups"];

  // The priority of routes advertised to this BGP peer.
  //
  // Where there is more than one matching route of maximum
  //
  // length, the routes with the lowest priority value win.
  optional int64 advertised_route_priority = 3 [json_name = "advertised_route_priority"];

  // The user-defined custom learned route priority for a BGP session.
  //
  // This value is applied to all custom learned route ranges for the session. You can
  // choose a value
  //
  // from 0 to 65335. If you don't provide a value, Google Cloud assigns a priority of
  // 100 to the ranges.
  optional int64 custom_learned_route_priority = 4 [json_name = "custom_learned_route_priority"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 5 [json_name = "deletion_policy"];

  // The status of the BGP peer connection. If set to false, any active session
  //
  // with the peer is terminated and all associated routing information is removed.
  //
  // If set to true, the peer connection can be established with routing information.
  //
  // The default is true.
  optional bool enable = 6;

  // Enable IPv4 traffic over BGP Peer. It is enabled by default if the peerIpAddress
  // is version 4.
  optional bool enable_ipv4 = 7 [json_name = "enable_ipv4"];

  // Enable IPv6 traffic over BGP Peer. If not specified, it is disabled by default.
  optional bool enable_ipv6 = 8 [json_name = "enable_ipv6"];

  // routers.list of export policies applied to this peer, in the order they must be
  // evaluated.
  //
  // The name must correspond to an existing policy that has ROUTE_POLICY_TYPE_EXPORT
  // type.
  repeated string export_policies = 9 [json_name = "export_policies"];

  optional string id = 10;

  // routers.list of import policies applied to this peer, in the order they must be
  // evaluated.
  //
  // The name must correspond to an existing policy that has ROUTE_POLICY_TYPE_IMPORT
  // type.
  repeated string import_policies = 11 [json_name = "import_policies"];

  // Name of the interface the BGP peer is associated with.
  optional string interface = 12;

  // IP address of the interface inside Google Cloud Platform.
  //
  // Only IPv4 is supported.
  optional string ip_address = 13 [json_name = "ip_address"];

  // IPv4 address of the interface inside Google Cloud Platform.
  optional string ipv4_nexthop_address = 14 [json_name = "ipv4_nexthop_address"];

  // IPv6 address of the interface inside Google Cloud Platform.
  //
  // The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64.
  //
  // If you do not specify the next hop addresses, Google Cloud automatically
  //
  // assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range
  // for you.
  optional string ipv6_nexthop_address = 15 [json_name = "ipv6_nexthop_address"];

  // An internal boolean field for provider use for zero_advertised_route_priority.
  optional bool is_advertised_route_priority_set = 16 [json_name = "is_advertised_route_priority_set"];

  // An internal boolean field for provider use.
  optional bool is_custom_learned_priority_set = 17 [json_name = "is_custom_learned_priority_set"];

  // The resource that configures and manages this BGP peer.
  //
  //
  // * 'MANAGED_BY_USER' is the default value and can be managed by
  //
  // you or other users
  //
  // * 'MANAGED_BY_ATTACHMENT' is a BGP peer that is configured and
  //
  // managed by Cloud Interconnect, specifically by an
  //
  // InterconnectAttachment of type PARTNER. Google automatically
  //
  // creates, updates, and deletes this type of BGP peer when the
  //
  // PARTNER InterconnectAttachment is created, updated,
  //
  // or deleted.
  optional string management_type = 18 [json_name = "management_type"];

  // Name of this BGP peer. The name must be 1-63 characters long,
  //
  // and comply with RFC1035. Specifically, the name must be 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which
  //
  // means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 19;

  // Peer BGP Autonomous System Number (ASN).
  //
  // Each BGP interface may use a different value.
  optional int64 peer_asn = 20 [json_name = "peer_asn"];

  // IP address of the BGP interface outside Google Cloud Platform.
  //
  // Only IPv4 is supported. Required if 'ip_address' is set.
  optional string peer_ip_address = 21 [json_name = "peer_ip_address"];

  // IPv4 address of the BGP interface outside Google Cloud Platform.
  optional string peer_ipv4_nexthop_address = 22 [json_name = "peer_ipv4_nexthop_address"];

  // IPv6 address of the BGP interface outside Google Cloud Platform.
  //
  // The address must be in the range 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64.
  //
  // If you do not specify the next hop addresses, Google Cloud automatically
  //
  // assigns unused addresses from the 2600:2d00:0:2::/64 or 2600:2d00:0:3::/64 range
  // for you.
  optional string peer_ipv6_nexthop_address = 23 [json_name = "peer_ipv6_nexthop_address"];

  optional string project = 24;

  // Region where the router and BgpPeer reside.
  //
  // If it is not provided, the provider region is used.
  optional string region = 25;

  // The name of the Cloud Router in which this BgpPeer will be configured.
  optional string router = 26;

  // The URI of the VM instance that is used as third-party router appliances
  //
  // such as Next Gen Firewalls, Virtual Routers, or Router Appliances.
  //
  // The VM instance must be located in zones contained in the same region as
  //
  // this Cloud Router. The VM instance is the peer side of the BGP session.
  optional string router_appliance_instance = 27 [json_name = "router_appliance_instance"];

  // Force the advertised_route_priority to be 0.
  optional bool zero_advertised_route_priority = 28 [json_name = "zero_advertised_route_priority"];

  // Force the custom_learned_route_priority to be 0.
  optional bool zero_custom_learned_route_priority = 29 [json_name = "zero_custom_learned_route_priority"];

  repeated AdvertisedIpRanges advertised_ip_ranges = 30 [json_name = "advertised_ip_ranges"];

  Bfd bfd = 31;

  repeated CustomLearnedIpRanges custom_learned_ip_ranges = 32 [json_name = "custom_learned_ip_ranges"];

  Md5AuthenticationKey md5_authentication_key = 33 [json_name = "md5_authentication_key"];

  repeated Timeouts timeouts = 34;

  google.protobuf.Value for_each = 35 [json_name = "for_each"];

  repeated string depends_on = 36 [json_name = "depends_on"];

  optional int32 count = 37;

  optional string provider = 38;

  terraform.v1.Lifecycle lifecycle = 39;

  message AdvertisedIpRanges {
    // User-specified description for the IP range.
    optional string description = 1;

    // The IP range to advertise. The value must be a
    //
    // CIDR-formatted string.
    optional string range = 2;
  }

  message Bfd {
    // The minimum interval, in milliseconds, between BFD control packets
    //
    // received from the peer router. The actual value is negotiated
    //
    // between the two routers and is equal to the greater of this value
    //
    // and the transmit interval of the other router. If set, this value
    //
    // must be between 1000 and 30000.
    optional int64 min_receive_interval = 1 [json_name = "min_receive_interval"];

    // The minimum interval, in milliseconds, between BFD control packets
    //
    // transmitted to the peer router. The actual value is negotiated
    //
    // between the two routers and is equal to the greater of this value
    //
    // and the corresponding receive interval of the other router. If set,
    //
    // this value must be between 1000 and 30000.
    optional int64 min_transmit_interval = 2 [json_name = "min_transmit_interval"];

    // The number of consecutive BFD packets that must be missed before
    //
    // BFD declares that a peer is unavailable. If set, the value must
    //
    // be a value between 5 and 16.
    optional int64 multiplier = 3;

    // The BFD session initialization mode for this BGP peer.
    //
    // If set to 'ACTIVE', the Cloud Router will initiate the BFD session
    //
    // for this BGP peer. If set to 'PASSIVE', the Cloud Router will wait
    //
    // for the peer router to initiate the BFD session for this BGP peer.
    //
    // If set to 'DISABLED', BFD is disabled for this BGP peer. Possible values:
    // ["ACTIVE", "DISABLED", "PASSIVE"]
    optional string session_initialization_mode = 4 [json_name = "session_initialization_mode"];
  }

  message CustomLearnedIpRanges {
    // The IP range to learn. The value must be a
    //
    // CIDR-formatted string.
    optional string range = 1;
  }

  message Md5AuthenticationKey {
    // Value of the key.
    optional string key = 1;

    // [REQUIRED] Name used to identify the key.
    //
    // Must be unique within a router. Must be referenced by exactly one bgpPeer. Must
    // comply with RFC1035.
    optional string name = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeRouterRoutePolicy version is 0
message GoogleComputeRouterRoutePolicy {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string fingerprint = 2;

  optional string id = 3;

  // Name of the route policy. This policy's name, which must be a resource ID segment
  // and unique within all policies owned by the Router
  optional string name = 4;

  optional string project = 5;

  // Region where the router and NAT reside.
  optional string region = 6;

  // The name of the Cloud Router in which this route policy will be configured.
  optional string router = 7;

  // This is policy's type, which is one of IMPORT or EXPORT Possible values:
  // ["ROUTE_POLICY_TYPE_IMPORT", "ROUTE_POLICY_TYPE_EXPORT"]
  optional string type = 8;

  repeated Terms terms = 9;

  repeated Timeouts timeouts = 10;

  google.protobuf.Value for_each = 11 [json_name = "for_each"];

  repeated string depends_on = 12 [json_name = "depends_on"];

  optional int32 count = 13;

  optional string provider = 14;

  terraform.v1.Lifecycle lifecycle = 15;

  message Terms {
    // The evaluation priority for this term, which must be between 0 (inclusive) and
    // 2147483648 (exclusive), and unique within the list.
    optional int64 priority = 1;

    repeated Actions actions = 2;

    Match match = 3;

    message Actions {
      // Description of the expression
      optional string description = 1;

      // Textual representation of an expression in Common Expression
      //
      // Language syntax.
      optional string expression = 2;

      // String indicating the location of the expression for error
      //
      // reporting, e.g. a file name and a position in the file
      optional string location = 3;

      // Title for the expression, i.e. a short string describing its
      //
      // purpose.
      optional string title = 4;
    }

    message Match {
      // Description of the expression
      optional string description = 1;

      // Textual representation of an expression in Common Expression Language syntax.
      optional string expression = 2;

      // String indicating the location of the expression for error reporting, e.g. a file
      // name and a position in the file
      optional string location = 3;

      // Title for the expression, i.e. a short string describing its purpose.
      optional string title = 4;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeSecurityPolicy version is 0
message GoogleComputeSecurityPolicy {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // An optional description of this security policy. Max size is 2048.
  optional string description = 2;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 3 [json_name = "effective_labels"];

  // Fingerprint of this resource.
  optional string fingerprint = 4;

  optional string id = 5;

  // The unique fingerprint of the labels.
  optional string label_fingerprint = 6 [json_name = "label_fingerprint"];

  // Labels to apply to this address.  A list of key->value pairs.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 7;

  // The name of the security policy.
  optional string name = 8;

  // The project in which the resource belongs. If it is not provided, the provider
  // project is used.
  optional string project = 9;

  // The URI of the created resource.
  optional string self_link = 10 [json_name = "self_link"];

  // The combination of labels configured directly on the resource and default labels
  // configured on the provider.
  map<string, string> terraform_labels = 11 [json_name = "terraform_labels"];

  // The type indicates the intended use of the security policy. CLOUD_ARMOR - Cloud
  // Armor backend security policies can be configured to filter incoming HTTP
  // requests targeting backend services. They filter requests before they hit the
  // origin servers. CLOUD_ARMOR_EDGE - Cloud Armor edge security policies can be
  // configured to filter incoming HTTP requests targeting backend services (including
  // Cloud CDN-enabled) as well as backend buckets (Cloud Storage). They filter
  // requests before the request is served from Google's cache.
  optional string type = 12;

  AdaptiveProtectionConfig adaptive_protection_config = 13 [json_name = "adaptive_protection_config"];

  AdvancedOptionsConfig advanced_options_config = 14 [json_name = "advanced_options_config"];

  RecaptchaOptionsConfig recaptcha_options_config = 15 [json_name = "recaptcha_options_config"];

  repeated Rule rule = 16;

  repeated Timeouts timeouts = 17;

  google.protobuf.Value for_each = 18 [json_name = "for_each"];

  repeated string depends_on = 19 [json_name = "depends_on"];

  optional int32 count = 20;

  optional string provider = 21;

  terraform.v1.Lifecycle lifecycle = 22;

  message AdaptiveProtectionConfig {
    Layer7DdosDefenseConfig layer_7_ddos_defense_config = 1 [json_name = "layer_7_ddos_defense_config"];

    message Layer7DdosDefenseConfig {
      // If set to true, enables CAAP for L7 DDoS detection.
      optional bool enable = 1;

      // Rule visibility. Supported values include: "STANDARD", "PREMIUM".
      optional string rule_visibility = 2 [json_name = "rule_visibility"];

      repeated ThresholdConfigs threshold_configs = 3 [json_name = "threshold_configs"];

      message ThresholdConfigs {
        optional int64 auto_deploy_confidence_threshold = 1 [json_name = "auto_deploy_confidence_threshold"];

        optional int64 auto_deploy_expiration_sec = 2 [json_name = "auto_deploy_expiration_sec"];

        optional int64 auto_deploy_impacted_baseline_threshold = 3 [
          json_name = "auto_deploy_impacted_baseline_threshold"
        ];

        optional int64 auto_deploy_load_threshold = 4 [json_name = "auto_deploy_load_threshold"];

        optional int64 detection_absolute_qps = 5 [json_name = "detection_absolute_qps"];

        optional int64 detection_load_threshold = 6 [json_name = "detection_load_threshold"];

        optional int64 detection_relative_to_baseline_qps = 7 [json_name = "detection_relative_to_baseline_qps"];

        // The name must be 1-63 characters long, and comply with RFC1035. The name must be
        // unique within the security policy.
        optional string name = 8;

        repeated TrafficGranularityConfigs traffic_granularity_configs = 9 [json_name = "traffic_granularity_configs"];

        message TrafficGranularityConfigs {
          // If enabled, traffic matching each unique value for the specified type constitutes
          // a separate traffic unit. It can only be set to true if value is empty.
          optional bool enable_each_unique_value = 1 [json_name = "enable_each_unique_value"];

          // Type of this configuration.
          optional string type = 2;

          // Requests that match this value constitute a granular traffic unit.
          optional string value = 3;
        }
      }
    }
  }

  message AdvancedOptionsConfig {
    // JSON body parsing. Supported values include: "DISABLED", "STANDARD".
    optional string json_parsing = 1 [json_name = "json_parsing"];

    // Logging level. Supported values include: "NORMAL", "VERBOSE".
    optional string log_level = 2 [json_name = "log_level"];

    // The maximum request size chosen by the customer with Waf enabled. Values
    // supported are "8KB", "16KB, "32KB", "48KB" and "64KB". Values are case
    // insensitive.
    optional string request_body_inspection_size = 3 [json_name = "request_body_inspection_size"];

    // An optional list of case-insensitive request header names to use for resolving
    // the callers client IP address.
    repeated string user_ip_request_headers = 4 [json_name = "user_ip_request_headers"];

    JsonCustomConfig json_custom_config = 5 [json_name = "json_custom_config"];

    message JsonCustomConfig {
      // A list of custom Content-Type header values to apply the JSON parsing.
      repeated string content_types = 1 [json_name = "content_types"];
    }
  }

  message RecaptchaOptionsConfig {
    // A field to supply a reCAPTCHA site key to be used for all the rules using the
    // redirect action with the type of GOOGLE_RECAPTCHA under the security policy. The
    // specified site key needs to be created from the reCAPTCHA API. The user is
    // responsible for the validity of the specified site key. If not specified, a
    // Google-managed site key is used.
    optional string redirect_site_key = 1 [json_name = "redirect_site_key"];
  }

  message Rule {
    // Action to take when match matches the request.
    optional string action = 1;

    // An optional description of this rule. Max size is 64.
    optional string description = 2;

    // When set to true, the action specified above is not enforced. Stackdriver logs
    // for requests that trigger a preview action are annotated as such.
    optional bool preview = 3;

    // An unique positive integer indicating the priority of evaluation for a rule.
    // Rules are evaluated from highest priority (lowest numerically) to lowest priority
    // (highest numerically) in order.
    optional int64 priority = 4;

    HeaderAction header_action = 5 [json_name = "header_action"];

    Match match = 6;

    PreconfiguredWafConfig preconfigured_waf_config = 7 [json_name = "preconfigured_waf_config"];

    RateLimitOptions rate_limit_options = 8 [json_name = "rate_limit_options"];

    RedirectOptions redirect_options = 9 [json_name = "redirect_options"];

    message HeaderAction {
      repeated RequestHeadersToAdds request_headers_to_adds = 1 [json_name = "request_headers_to_adds"];

      message RequestHeadersToAdds {
        // The name of the header to set.
        optional string header_name = 1 [json_name = "header_name"];

        // The value to set the named header to.
        optional string header_value = 2 [json_name = "header_value"];
      }
    }

    message Match {
      // Predefined rule expression. If this field is specified, config must also be
      // specified. Available options:   SRC_IPS_V1: Must specify the corresponding
      // src_ip_ranges field in config.
      optional string versioned_expr = 1 [json_name = "versioned_expr"];

      Config config = 2;

      Expr expr = 3;

      ExprOptions expr_options = 4 [json_name = "expr_options"];

      message Config {
        // Set of IP addresses or ranges (IPV4 or IPV6) in CIDR notation to match against
        // inbound traffic. There is a limit of 10 IP ranges per rule. A value of '*'
        // matches all IPs (can be used to override the default behavior).
        repeated string src_ip_ranges = 1 [json_name = "src_ip_ranges"];
      }

      message Expr {
        // Textual representation of an expression in Common Expression Language syntax. The
        // application context of the containing message determines which well-known feature
        // set of CEL is supported.
        optional string expression = 1;
      }

      message ExprOptions {
        RecaptchaOptions recaptcha_options = 1 [json_name = "recaptcha_options"];

        message RecaptchaOptions {
          // A list of site keys to be used during the validation of reCAPTCHA action-tokens.
          // The provided site keys need to be created from reCAPTCHA API under the same
          // project where the security policy is created
          repeated string action_token_site_keys = 1 [json_name = "action_token_site_keys"];

          // A list of site keys to be used during the validation of reCAPTCHA session-tokens.
          // The provided site keys need to be created from reCAPTCHA API under the same
          // project where the security policy is created.
          repeated string session_token_site_keys = 2 [json_name = "session_token_site_keys"];
        }
      }
    }

    message PreconfiguredWafConfig {
      repeated Exclusion exclusion = 1;

      message Exclusion {
        // A list of target rule IDs under the WAF rule set to apply the preconfigured WAF
        // exclusion. If omitted, it refers to all the rule IDs under the WAF rule set.
        repeated string target_rule_ids = 1 [json_name = "target_rule_ids"];

        // Target WAF rule set to apply the preconfigured WAF exclusion.
        optional string target_rule_set = 2 [json_name = "target_rule_set"];

        repeated RequestCookie request_cookie = 3 [json_name = "request_cookie"];

        repeated RequestHeader request_header = 4 [json_name = "request_header"];

        repeated RequestQueryParam request_query_param = 5 [json_name = "request_query_param"];

        repeated RequestUri request_uri = 6 [json_name = "request_uri"];

        message RequestCookie {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value. Available options: EQUALS: The operator matches if the field value
          // equals the specified value. STARTS_WITH: The operator matches if the field value
          // starts with the specified value. ENDS_WITH: The operator matches if the field
          // value ends with the specified value. CONTAINS: The operator matches if the field
          // value contains the specified value. EQUALS_ANY: The operator matches if the field
          // value is any value.
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation. The field value must be given if the field
          // operator is not EQUALS_ANY, and cannot be given if the field operator is
          // EQUALS_ANY.
          optional string value = 2;
        }

        message RequestHeader {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value. Available options: EQUALS: The operator matches if the field value
          // equals the specified value. STARTS_WITH: The operator matches if the field value
          // starts with the specified value. ENDS_WITH: The operator matches if the field
          // value ends with the specified value. CONTAINS: The operator matches if the field
          // value contains the specified value. EQUALS_ANY: The operator matches if the field
          // value is any value.
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation. The field value must be given if the field
          // operator is not EQUALS_ANY, and cannot be given if the field operator is
          // EQUALS_ANY.
          optional string value = 2;
        }

        message RequestQueryParam {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value. Available options: EQUALS: The operator matches if the field value
          // equals the specified value. STARTS_WITH: The operator matches if the field value
          // starts with the specified value. ENDS_WITH: The operator matches if the field
          // value ends with the specified value. CONTAINS: The operator matches if the field
          // value contains the specified value. EQUALS_ANY: The operator matches if the field
          // value is any value.
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation. The field value must be given if the field
          // operator is not EQUALS_ANY, and cannot be given if the field operator is
          // EQUALS_ANY.
          optional string value = 2;
        }

        message RequestUri {
          // You can specify an exact match or a partial match by using a field operator and a
          // field value. Available options: EQUALS: The operator matches if the field value
          // equals the specified value. STARTS_WITH: The operator matches if the field value
          // starts with the specified value. ENDS_WITH: The operator matches if the field
          // value ends with the specified value. CONTAINS: The operator matches if the field
          // value contains the specified value. EQUALS_ANY: The operator matches if the field
          // value is any value.
          optional string operator = 1;

          // A request field matching the specified value will be excluded from inspection
          // during preconfigured WAF evaluation. The field value must be given if the field
          // operator is not EQUALS_ANY, and cannot be given if the field operator is
          // EQUALS_ANY.
          optional string value = 2;
        }
      }
    }

    message RateLimitOptions {
      // Can only be specified if the action for the rule is "rate_based_ban". If
      // specified, determines the time (in seconds) the traffic will continue to be
      // banned by the rate limit after the rate falls below the threshold.
      optional int64 ban_duration_sec = 1 [json_name = "ban_duration_sec"];

      // Action to take for requests that are under the configured rate limit threshold.
      // Valid option is "allow" only.
      optional string conform_action = 2 [json_name = "conform_action"];

      // Determines the key to enforce the rateLimitThreshold on
      optional string enforce_on_key = 3 [json_name = "enforce_on_key"];

      // Rate limit key name applicable only for the following key types: HTTP_HEADER --
      // Name of the HTTP header whose value is taken as the key value. HTTP_COOKIE --
      // Name of the HTTP cookie whose value is taken as the key value.
      optional string enforce_on_key_name = 4 [json_name = "enforce_on_key_name"];

      // Action to take for requests that are above the configured rate limit threshold,
      // to either deny with a specified HTTP response code, or redirect to a different
      // endpoint. Valid options are "deny()" where valid values for status are 403, 404,
      // 429, and 502, and "redirect" where the redirect parameters come from
      // exceedRedirectOptions below.
      optional string exceed_action = 5 [json_name = "exceed_action"];

      BanThreshold ban_threshold = 6 [json_name = "ban_threshold"];

      repeated EnforceOnKeyConfigs enforce_on_key_configs = 7 [json_name = "enforce_on_key_configs"];

      ExceedRedirectOptions exceed_redirect_options = 8 [json_name = "exceed_redirect_options"];

      RateLimitThreshold rate_limit_threshold = 9 [json_name = "rate_limit_threshold"];

      message BanThreshold {
        // Number of HTTP(S) requests for calculating the threshold.
        optional int64 count = 1;

        // Interval over which the threshold is computed.
        optional int64 interval_sec = 2 [json_name = "interval_sec"];
      }

      message EnforceOnKeyConfigs {
        // Rate limit key name applicable only for the following key types: HTTP_HEADER --
        // Name of the HTTP header whose value is taken as the key value. HTTP_COOKIE --
        // Name of the HTTP cookie whose value is taken as the key value.
        optional string enforce_on_key_name = 1 [json_name = "enforce_on_key_name"];

        // Determines the key to enforce the rate_limit_threshold on
        optional string enforce_on_key_type = 2 [json_name = "enforce_on_key_type"];
      }

      message ExceedRedirectOptions {
        // Target for the redirect action. This is required if the type is EXTERNAL_302 and
        // cannot be specified for GOOGLE_RECAPTCHA.
        optional string target = 1;

        // Type of the redirect action.
        optional string type = 2;
      }

      message RateLimitThreshold {
        // Number of HTTP(S) requests for calculating the threshold.
        optional int64 count = 1;

        // Interval over which the threshold is computed.
        optional int64 interval_sec = 2 [json_name = "interval_sec"];
      }
    }

    message RedirectOptions {
      // Target for the redirect action. This is required if the type is EXTERNAL_302 and
      // cannot be specified for GOOGLE_RECAPTCHA.
      optional string target = 1;

      // Type of the redirect action. Available options: EXTERNAL_302: Must specify the
      // corresponding target field in config. GOOGLE_RECAPTCHA: Cannot specify target
      // field in config.
      optional string type = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeSecurityPolicyRule version is 0
message GoogleComputeSecurityPolicyRule {
  // The Action to perform when the rule is matched. The following are the valid
  // actions:
  //
  //
  // * allow: allow access to target.
  //
  //
  // * deny(STATUS): deny access to target, returns the HTTP response code specified.
  // Valid values for STATUS are 403, 404, and 502.
  //
  //
  // * rate_based_ban: limit client traffic to the configured threshold and ban the
  // client if the traffic exceeds the threshold. Configure parameters for this action
  // in RateLimitOptions. Requires rateLimitOptions to be set.
  //
  //
  // * redirect: redirect to a different target. This can either be an internal
  // reCAPTCHA redirect, or an external URL-based redirect via a 302 response.
  // Parameters for this action can be configured via redirectOptions. This action is
  // only supported in Global Security Policies of type CLOUD_ARMOR.
  //
  //
  // * throttle: limit client traffic to the configured threshold. Configure
  // parameters for this action in rateLimitOptions. Requires rateLimitOptions to be
  // set for this.
  optional string action = 1;

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 3;

  optional string id = 4;

  // If set to true, the specified action is not enforced.
  optional bool preview = 5;

  // An integer indicating the priority of a rule in the list.
  //
  // The priority must be a positive value between 0 and 2147483647.
  //
  // Rules are evaluated from highest to lowest priority where 0 is the highest
  // priority and 2147483647 is the lowest priority.
  optional int64 priority = 6;

  optional string project = 7;

  // The name of the security policy this rule belongs to.
  optional string security_policy = 8 [json_name = "security_policy"];

  HeaderAction header_action = 9 [json_name = "header_action"];

  Match match = 10;

  PreconfiguredWafConfig preconfigured_waf_config = 11 [json_name = "preconfigured_waf_config"];

  RateLimitOptions rate_limit_options = 12 [json_name = "rate_limit_options"];

  RedirectOptions redirect_options = 13 [json_name = "redirect_options"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message HeaderAction {
    repeated RequestHeadersToAdds request_headers_to_adds = 1 [json_name = "request_headers_to_adds"];

    message RequestHeadersToAdds {
      // The name of the header to set.
      optional string header_name = 1 [json_name = "header_name"];

      // The value to set the named header to.
      optional string header_value = 2 [json_name = "header_value"];
    }
  }

  message Match {
    // Preconfigured versioned expression. If this field is specified, config must also
    // be specified.
    //
    // Available preconfigured expressions along with their requirements are: SRC_IPS_V1
    // - must specify the corresponding srcIpRange field in config. Possible values:
    // ["SRC_IPS_V1"]
    optional string versioned_expr = 1 [json_name = "versioned_expr"];

    Config config = 2;

    Expr expr = 3;

    ExprOptions expr_options = 4 [json_name = "expr_options"];

    message Config {
      // CIDR IP address range. Maximum number of srcIpRanges allowed is 10.
      repeated string src_ip_ranges = 1 [json_name = "src_ip_ranges"];
    }

    message Expr {
      // Textual representation of an expression in Common Expression Language syntax. The
      // application context of the containing message determines which well-known feature
      // set of CEL is supported.
      optional string expression = 1;
    }

    message ExprOptions {
      RecaptchaOptions recaptcha_options = 1 [json_name = "recaptcha_options"];

      message RecaptchaOptions {
        // A list of site keys to be used during the validation of reCAPTCHA action-tokens.
        // The provided site keys need to be created from reCAPTCHA API under the same
        // project where the security policy is created.
        repeated string action_token_site_keys = 1 [json_name = "action_token_site_keys"];

        // A list of site keys to be used during the validation of reCAPTCHA session-tokens.
        // The provided site keys need to be created from reCAPTCHA API under the same
        // project where the security policy is created.
        repeated string session_token_site_keys = 2 [json_name = "session_token_site_keys"];
      }
    }
  }

  message PreconfiguredWafConfig {
    repeated Exclusion exclusion = 1;

    message Exclusion {
      // A list of target rule IDs under the WAF rule set to apply the preconfigured WAF
      // exclusion.
      //
      // If omitted, it refers to all the rule IDs under the WAF rule set.
      repeated string target_rule_ids = 1 [json_name = "target_rule_ids"];

      // Target WAF rule set to apply the preconfigured WAF exclusion.
      optional string target_rule_set = 2 [json_name = "target_rule_set"];

      repeated RequestCookie request_cookie = 3 [json_name = "request_cookie"];

      repeated RequestHeader request_header = 4 [json_name = "request_header"];

      repeated RequestQueryParam request_query_param = 5 [json_name = "request_query_param"];

      repeated RequestUri request_uri = 6 [json_name = "request_uri"];

      message RequestCookie {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestHeader {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestQueryParam {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }

      message RequestUri {
        // You can specify an exact match or a partial match by using a field operator and a
        // field value.
        //
        // Available options:
        //
        // EQUALS: The operator matches if the field value equals the specified value.
        //
        // STARTS_WITH: The operator matches if the field value starts with the specified
        // value.
        //
        // ENDS_WITH: The operator matches if the field value ends with the specified value.
        //
        // CONTAINS: The operator matches if the field value contains the specified value.
        //
        // EQUALS_ANY: The operator matches if the field value is any value.
        optional string operator = 1;

        // A request field matching the specified value will be excluded from inspection
        // during preconfigured WAF evaluation.
        //
        // The field value must be given if the field operator is not EQUALS_ANY, and cannot
        // be given if the field operator is EQUALS_ANY.
        optional string value = 2;
      }
    }
  }

  message RateLimitOptions {
    // Can only be specified if the action for the rule is "rate_based_ban".
    //
    // If specified, determines the time (in seconds) the traffic will continue to be
    // banned by the rate limit after the rate falls below the threshold.
    optional int64 ban_duration_sec = 1 [json_name = "ban_duration_sec"];

    // Action to take for requests that are under the configured rate limit threshold.
    //
    // Valid option is "allow" only.
    optional string conform_action = 2 [json_name = "conform_action"];

    // Determines the key to enforce the rateLimitThreshold on. Possible values are:
    //
    // * ALL: A single rate limit threshold is applied to all the requests matching this
    // rule. This is the default value if "enforceOnKey" is not configured.
    //
    // * IP: The source IP address of the request is the key. Each IP has this limit
    // enforced separately.
    //
    // * HTTP_HEADER: The value of the HTTP header whose name is configured under
    // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
    // header value. If no such header is present in the request, the key type defaults
    // to ALL.
    //
    // * XFF_IP: The first IP address (i.e. the originating client IP address) specified
    // in the list of IPs under X-Forwarded-For HTTP header. If no such header is
    // present or the value is not a valid IP, the key defaults to the source IP address
    // of the request i.e. key type IP.
    //
    // * HTTP_COOKIE: The value of the HTTP cookie whose name is configured under
    // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
    // cookie value. If no such cookie is present in the request, the key type defaults
    // to ALL.
    //
    // * HTTP_PATH: The URL path of the HTTP request. The key value is truncated to the
    // first 128 bytes.
    //
    // * SNI: Server name indication in the TLS session of the HTTPS request. The key
    // value is truncated to the first 128 bytes. The key type defaults to ALL on a HTTP
    // session.
    //
    // * REGION_CODE: The country/region from which the request originates.
    //
    // * TLS_JA3_FINGERPRINT: JA3 TLS/SSL fingerprint if the client connects using
    // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
    //
    // * TLS_JA4_FINGERPRINT: JA4 TLS/SSL fingerprint if the client connects using
    // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
    //
    // * USER_IP: The IP address of the originating client, which is resolved based on
    // "userIpRequestHeaders" configured with the security policy. If there is no
    // "userIpRequestHeaders" configuration or an IP address cannot be resolved from it,
    // the key type defaults to IP. Possible values: ["ALL", "IP", "HTTP_HEADER",
    // "XFF_IP", "HTTP_COOKIE", "HTTP_PATH", "SNI", "REGION_CODE",
    // "TLS_JA3_FINGERPRINT", "TLS_JA4_FINGERPRINT", "USER_IP"]
    optional string enforce_on_key = 3 [json_name = "enforce_on_key"];

    // Rate limit key name applicable only for the following key types:
    //
    // HTTP_HEADER -- Name of the HTTP header whose value is taken as the key value.
    //
    // HTTP_COOKIE -- Name of the HTTP cookie whose value is taken as the key value.
    optional string enforce_on_key_name = 4 [json_name = "enforce_on_key_name"];

    // Action to take for requests that are above the configured rate limit threshold,
    // to either deny with a specified HTTP response code, or redirect to a different
    // endpoint.
    //
    // Valid options are deny(STATUS), where valid values for STATUS are 403, 404, 429,
    // and 502.
    optional string exceed_action = 5 [json_name = "exceed_action"];

    BanThreshold ban_threshold = 6 [json_name = "ban_threshold"];

    repeated EnforceOnKeyConfigs enforce_on_key_configs = 7 [json_name = "enforce_on_key_configs"];

    ExceedRedirectOptions exceed_redirect_options = 8 [json_name = "exceed_redirect_options"];

    RateLimitThreshold rate_limit_threshold = 9 [json_name = "rate_limit_threshold"];

    message BanThreshold {
      // Number of HTTP(S) requests for calculating the threshold.
      optional int64 count = 1;

      // Interval over which the threshold is computed.
      optional int64 interval_sec = 2 [json_name = "interval_sec"];
    }

    message EnforceOnKeyConfigs {
      // Rate limit key name applicable only for the following key types:
      //
      // HTTP_HEADER -- Name of the HTTP header whose value is taken as the key value.
      //
      // HTTP_COOKIE -- Name of the HTTP cookie whose value is taken as the key value.
      optional string enforce_on_key_name = 1 [json_name = "enforce_on_key_name"];

      // Determines the key to enforce the rateLimitThreshold on. Possible values are:
      //
      // * ALL: A single rate limit threshold is applied to all the requests matching this
      // rule. This is the default value if "enforceOnKeyConfigs" is not configured.
      //
      // * IP: The source IP address of the request is the key. Each IP has this limit
      // enforced separately.
      //
      // * HTTP_HEADER: The value of the HTTP header whose name is configured under
      // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
      // header value. If no such header is present in the request, the key type defaults
      // to ALL.
      //
      // * XFF_IP: The first IP address (i.e. the originating client IP address) specified
      // in the list of IPs under X-Forwarded-For HTTP header. If no such header is
      // present or the value is not a valid IP, the key defaults to the source IP address
      // of the request i.e. key type IP.
      //
      // * HTTP_COOKIE: The value of the HTTP cookie whose name is configured under
      // "enforceOnKeyName". The key value is truncated to the first 128 bytes of the
      // cookie value. If no such cookie is present in the request, the key type defaults
      // to ALL.
      //
      // * HTTP_PATH: The URL path of the HTTP request. The key value is truncated to the
      // first 128 bytes.
      //
      // * SNI: Server name indication in the TLS session of the HTTPS request. The key
      // value is truncated to the first 128 bytes. The key type defaults to ALL on a HTTP
      // session.
      //
      // * REGION_CODE: The country/region from which the request originates.
      //
      // * TLS_JA3_FINGERPRINT: JA3 TLS/SSL fingerprint if the client connects using
      // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
      //
      // * TLS_JA4_FINGERPRINT: JA4 TLS/SSL fingerprint if the client connects using
      // HTTPS, HTTP/2 or HTTP/3. If not available, the key type defaults to ALL.
      //
      // * USER_IP: The IP address of the originating client, which is resolved based on
      // "userIpRequestHeaders" configured with the security policy. If there is no
      // "userIpRequestHeaders" configuration or an IP address cannot be resolved from it,
      // the key type defaults to IP. Possible values: ["ALL", "IP", "HTTP_HEADER",
      // "XFF_IP", "HTTP_COOKIE", "HTTP_PATH", "SNI", "REGION_CODE",
      // "TLS_JA3_FINGERPRINT", "TLS_JA4_FINGERPRINT", "USER_IP"]
      optional string enforce_on_key_type = 2 [json_name = "enforce_on_key_type"];
    }

    message ExceedRedirectOptions {
      // Target for the redirect action. This is required if the type is EXTERNAL_302 and
      // cannot be specified for GOOGLE_RECAPTCHA.
      optional string target = 1;

      // Type of the redirect action.
      optional string type = 2;
    }

    message RateLimitThreshold {
      // Number of HTTP(S) requests for calculating the threshold.
      optional int64 count = 1;

      // Interval over which the threshold is computed.
      optional int64 interval_sec = 2 [json_name = "interval_sec"];
    }
  }

  message RedirectOptions {
    // Target for the redirect action. This is required if the type is EXTERNAL_302 and
    // cannot be specified for GOOGLE_RECAPTCHA.
    optional string target = 1;

    // Type of the redirect action.
    optional string type = 2;
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeServiceAttachment version is 0
message GoogleComputeServiceAttachment {
  // An array of the consumer forwarding rules connected to this service
  //
  // attachment.
  repeated ConnectedEndpoints connected_endpoints = 1 [json_name = "connected_endpoints"];

  // The connection preference to use for this service attachment. Valid
  //
  // values include "ACCEPT_AUTOMATIC", "ACCEPT_MANUAL".
  optional string connection_preference = 2 [json_name = "connection_preference"];

  // An array of projects that are not allowed to connect to this service
  //
  // attachment.
  repeated string consumer_reject_lists = 3 [json_name = "consumer_reject_lists"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 5;

  // If specified, the domain name will be used during the integration between
  //
  // the PSC connected endpoints and the Cloud DNS. For example, this is a
  //
  // valid domain name: "p.mycompany.com.". Current max number of domain names
  //
  // supported is 1.
  repeated string domain_names = 6 [json_name = "domain_names"];

  // If true, enable the proxy protocol which is for supplying client TCP/IP
  //
  // address data in TCP connections that traverse proxies on their way to
  //
  // destination servers.
  optional bool enable_proxy_protocol = 7 [json_name = "enable_proxy_protocol"];

  // Fingerprint of this resource. This field is used internally during
  //
  // updates of this resource.
  optional string fingerprint = 8;

  optional string id = 9;

  // Name of the resource. The name must be 1-63 characters long, and
  //
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  //
  // which means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 10;

  // An array of subnets that is provided for NAT in this service attachment.
  repeated string nat_subnets = 11 [json_name = "nat_subnets"];

  optional string project = 12;

  // The number of consumer spokes that connected Private Service Connect endpoints
  // can be propagated to through Network Connectivity Center.
  //
  // This limit lets the service producer limit how many propagated Private Service
  // Connect connections can be established to this service attachment from a single
  // consumer.
  //
  //
  // If the connection preference of the service attachment is ACCEPT_MANUAL, the
  // limit applies to each project or network that is listed in the consumer accept
  // list.
  //
  // If the connection preference of the service attachment is ACCEPT_AUTOMATIC, the
  // limit applies to each project that contains a connected endpoint.
  //
  //
  // If unspecified, the default propagated connection limit is 250. To explicitly
  // send a zero value, set 'send_propagated_connection_limit_if_zero = true'.
  optional int64 propagated_connection_limit = 13 [json_name = "propagated_connection_limit"];

  // An 128-bit global unique ID of the PSC service attachment.
  repeated PscServiceAttachmentId psc_service_attachment_id = 14 [json_name = "psc_service_attachment_id"];

  // This flag determines whether a consumer accept/reject list change can reconcile
  // the statuses of existing ACCEPTED or REJECTED PSC endpoints.
  //
  //
  // If false, connection policy update will only affect existing PENDING PSC
  // endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched regardless
  // how the connection policy is modified .
  //
  // If true, update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For
  // example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project is
  // added to the reject list.
  optional bool reconcile_connections = 15 [json_name = "reconcile_connections"];

  // URL of the region where the resource resides.
  optional string region = 16;

  optional string self_link = 17 [json_name = "self_link"];

  // Controls the behavior of propagated_connection_limit.
  //
  // When false, setting propagated_connection_limit to zero causes the provider to
  // use to the API's default value.
  //
  // When true, the provider will set propagated_connection_limit to zero.
  //
  // Defaults to false.
  optional bool send_propagated_connection_limit_if_zero = 18 [
    json_name = "send_propagated_connection_limit_if_zero"
  ];

  // NOTE: This field is temporarily non-functional due to an underlying API issue.
  //
  // Any value provided here will be ignored until the API issue is resolved, expected
  // around 2026-03.
  //
  // [If true, show NAT IPs of all connected endpoints.]
  optional bool show_nat_ips = 19 [json_name = "show_nat_ips"];

  // The URL of a service serving the endpoint identified by this service attachment.
  optional string target_service = 20 [json_name = "target_service"];

  repeated ConsumerAcceptLists consumer_accept_lists = 21 [json_name = "consumer_accept_lists"];

  repeated Timeouts timeouts = 22;

  google.protobuf.Value for_each = 23 [json_name = "for_each"];

  repeated string depends_on = 24 [json_name = "depends_on"];

  optional int32 count = 25;

  optional string provider = 26;

  terraform.v1.Lifecycle lifecycle = 27;

  message ConnectedEndpoints {
    // consumer_network: string
    optional string consumer_network = 1 [json_name = "consumer_network"];

    // endpoint: string
    optional string endpoint = 2;

    // nat_ips: list of string
    repeated string nat_ips = 3 [json_name = "nat_ips"];

    // propagated_connection_count: number
    optional int64 propagated_connection_count = 4 [json_name = "propagated_connection_count"];

    // psc_connection_id: string
    optional string psc_connection_id = 5 [json_name = "psc_connection_id"];

    // status: string
    optional string status = 6;
  }

  message PscServiceAttachmentId {
    // high: string
    optional string high = 1;

    // low: string
    optional string low = 2;
  }

  message ConsumerAcceptLists {
    // The number of consumer forwarding rules the consumer project can
    //
    // create.
    optional int64 connection_limit = 1 [json_name = "connection_limit"];

    // The endpoint that is allowed to connect to this service attachment.
    //
    // Only one of project_id_or_num, network_url and endpoint_url may be set.
    optional string endpoint_url = 2 [json_name = "endpoint_url"];

    // The network that is allowed to connect to this service attachment.
    //
    // Only one of project_id_or_num and network_url may be set.
    optional string network_url = 3 [json_name = "network_url"];

    // A project that is allowed to connect to this service attachment.
    //
    // Only one of project_id_or_num and network_url may be set.
    optional string project_id_or_num = 4 [json_name = "project_id_or_num"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeSharedVpcHostProject version is 0
message GoogleComputeSharedVpcHostProject {
  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  optional string id = 2;

  // The ID of the project that will serve as a Shared VPC host project
  optional string project = 3;

  repeated Timeouts timeouts = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  optional int32 count = 7;

  optional string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeSharedVpcServiceProject version is 0
message GoogleComputeSharedVpcServiceProject {
  // The deletion policy for the shared VPC service. Setting ABANDON allows the
  // resource
  //
  // 				to be abandoned rather than deleted. Possible values are: "ABANDON".
  optional string deletion_policy = 1 [json_name = "deletion_policy"];

  // The ID of a host project to associate.
  optional string host_project = 2 [json_name = "host_project"];

  optional string id = 3;

  // The ID of the project that will serve as a Shared VPC service project.
  optional string service_project = 4 [json_name = "service_project"];

  repeated Timeouts timeouts = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeSnapshot version is 0
message GoogleComputeSnapshot {
  // Creates the new snapshot in the snapshot chain labeled with the
  //
  // specified name. The chain name must be 1-63 characters long and
  //
  // comply with RFC1035. This is an uncommon option only for advanced
  //
  // service owners who needs to create separate snapshot chains, for
  //
  // example, for chargeback tracking.  When you describe your snapshot
  //
  // resource, this field is visible only if it has a non-empty value.
  optional string chain_name = 1 [json_name = "chain_name"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  // Size of the snapshot, specified in GB.
  optional int64 disk_size_gb = 5 [json_name = "disk_size_gb"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 6 [json_name = "effective_labels"];

  optional string id = 7;

  // The fingerprint used for optimistic locking of this resource. Used
  //
  // internally during updates.
  optional string label_fingerprint = 8 [json_name = "label_fingerprint"];

  // Labels to apply to this Snapshot.
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 9;

  // A list of public visible licenses that apply to this snapshot. This
  //
  // can be because the original image had licenses attached (such as a
  //
  // Windows image).  snapshotEncryptionKey nested object Encrypts the
  //
  // snapshot using a customer-supplied encryption key.
  repeated string licenses = 10;

  // Name of the resource; provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 11;

  optional string project = 12;

  optional string self_link = 13 [json_name = "self_link"];

  // The unique identifier for the resource.
  optional int64 snapshot_id = 14 [json_name = "snapshot_id"];

  // Indicates the type of the snapshot. Possible values: ["ARCHIVE", "STANDARD"]
  optional string snapshot_type = 15 [json_name = "snapshot_type"];

  // A reference to the disk used to create this snapshot.
  optional string source_disk = 16 [json_name = "source_disk"];

  // A reference to the instant snapshot used to create this snapshot.
  optional string source_instant_snapshot = 17 [json_name = "source_instant_snapshot"];

  // A size of the storage used by the snapshot. As snapshots share
  //
  // storage, this number is expected to change with snapshot
  //
  // creation/deletion.
  optional int64 storage_bytes = 18 [json_name = "storage_bytes"];

  // Cloud Storage bucket storage location of the snapshot (regional or
  // multi-regional).
  repeated string storage_locations = 19 [json_name = "storage_locations"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 20 [json_name = "terraform_labels"];

  // A reference to the zone where the disk is hosted.
  optional string zone = 21;

  Params params = 22;

  SnapshotEncryptionKey snapshot_encryption_key = 23 [json_name = "snapshot_encryption_key"];

  SourceDiskEncryptionKey source_disk_encryption_key = 24 [json_name = "source_disk_encryption_key"];

  repeated Timeouts timeouts = 25;

  google.protobuf.Value for_each = 26 [json_name = "for_each"];

  repeated string depends_on = 27 [json_name = "depends_on"];

  optional int32 count = 28;

  optional string provider = 29;

  terraform.v1.Lifecycle lifecycle = 30;

  message Params {
    // Resource manager tags to be bound to the snapshot. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message SnapshotEncryptionKey {
    // The name of the encryption key that is stored in Google Cloud KMS.
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    //
    // If absent, the Compute Engine Service Agent service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies an encryption key stored in Google Cloud KMS, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];

    // The RFC 4648 base64 encoded SHA-256 hash of the customer-supplied
    //
    // encryption key that protects this resource.
    optional string sha256 = 5;
  }

  message SourceDiskEncryptionKey {
    // The name of the encryption key that is stored in Google Cloud KMS.
    optional string kms_key_self_link = 1 [json_name = "kms_key_self_link"];

    // The service account used for the encryption request for the given KMS key.
    //
    // If absent, the Compute Engine Service Agent service account is used.
    optional string kms_key_service_account = 2 [json_name = "kms_key_service_account"];

    // Specifies a 256-bit customer-supplied encryption key, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string raw_key = 3 [json_name = "raw_key"];

    // Specifies an encryption key stored in Google Cloud KMS, encoded in
    //
    // RFC 4648 base64 to either encrypt or decrypt this resource.
    optional string rsa_encrypted_key = 4 [json_name = "rsa_encrypted_key"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeSnapshotIamBinding version is 0
message GoogleComputeSnapshotIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeSnapshotIamMember version is 0
message GoogleComputeSnapshotIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  Condition condition = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  optional int32 count = 10;

  optional string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeSnapshotIamPolicy version is 0
message GoogleComputeSnapshotIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  google.protobuf.Value for_each = 6 [json_name = "for_each"];

  repeated string depends_on = 7 [json_name = "depends_on"];

  optional int32 count = 8;

  optional string provider = 9;

  terraform.v1.Lifecycle lifecycle = 10;
}

// GoogleComputeSnapshotSettings version is 0
message GoogleComputeSnapshotSettings {
  optional string id = 1;

  optional string project = 2;

  StorageLocation storage_location = 3 [json_name = "storage_location"];

  repeated Timeouts timeouts = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  optional int32 count = 7;

  optional string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message StorageLocation {
    // The chosen location policy Possible values: ["NEAREST_MULTI_REGION",
    // "LOCAL_REGION", "SPECIFIC_LOCATIONS"]
    optional string policy = 1;

    repeated Locations locations = 2;

    message Locations {
      optional string location = 1;

      // Name of the location. It should be one of the Cloud Storage buckets.
      //
      // Only one location can be specified. (should match location)
      optional string name = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeSslCertificate version is 0
message GoogleComputeSslCertificate {
  // The certificate in PEM format.
  //
  // The certificate chain must be no greater than 5 certs long.
  //
  // The chain must include at least one intermediate cert.
  optional string certificate = 1;

  // The unique identifier for the resource.
  optional int64 certificate_id = 2 [json_name = "certificate_id"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 5;

  // Expire time of the certificate in RFC3339 text format.
  optional string expire_time = 6 [json_name = "expire_time"];

  optional string id = 7;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  //
  //
  // These are in the same namespace as the managed SSL certificates.
  optional string name = 8;

  // Creates a unique name beginning with the specified prefix. Conflicts with name.
  optional string name_prefix = 9 [json_name = "name_prefix"];

  // The write-only private key in PEM format.
  optional string private_key = 10 [json_name = "private_key"];

  // The write-only private key in PEM format.
  optional string private_key_wo = 11 [json_name = "private_key_wo"];

  // Triggers update of 'private_key_wo' write-only. Increment this value when an
  // update to 'private_key_wo' is needed. For more info see [updating write-only
  // ders/google/guides/using_write_only_arguments.html#updating-write-only-arguments)
  optional string private_key_wo_version = 12 [json_name = "private_key_wo_version"];

  optional string project = 13;

  optional string self_link = 14 [json_name = "self_link"];

  repeated Timeouts timeouts = 15;

  google.protobuf.Value for_each = 16 [json_name = "for_each"];

  repeated string depends_on = 17 [json_name = "depends_on"];

  optional int32 count = 18;

  optional string provider = 19;

  terraform.v1.Lifecycle lifecycle = 20;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeSslPolicy version is 0
message GoogleComputeSslPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Profile specifies the set of SSL features that can be used by the
  //
  // load balancer when negotiating SSL with clients. This can be one of
  //
  // 'COMPATIBLE', 'MODERN', 'RESTRICTED', or 'CUSTOM'. If using 'CUSTOM',
  //
  // the set of SSL features to enable must be specified in the
  //
  // 'customFeatures' field.
  //
  //
  // See the [official
  // /cloud.google.com/compute/docs/load-balancing/ssl-policies#profilefeaturesupport)
  //
  // for which ciphers are available to use. **Note**: this argument
  //
  // *must* be present when using the 'CUSTOM' profile. This argument
  //
  // *must not* be present when using any other profile.
  repeated string custom_features = 2 [json_name = "custom_features"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  // The list of features enabled in the SSL policy.
  repeated string enabled_features = 5 [json_name = "enabled_features"];

  // Fingerprint of this resource. A hash of the contents stored in this
  //
  // object. This field is used in optimistic locking.
  optional string fingerprint = 6;

  optional string id = 7;

  // The minimum version of SSL protocol that can be used by the clients
  //
  // to establish a connection with the load balancer. When set to'
  //
  // TLS_1_3', the profile field must be set to 'RESTRICTED'. Default value: "TLS_1_0"
  // Possible values: ["TLS_1_0", "TLS_1_1", "TLS_1_2", "TLS_1_3"]
  optional string min_tls_version = 8 [json_name = "min_tls_version"];

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 9;

  // One of 'DEFAULT', 'ENABLED', or 'DEFERRED'. Controls whether the load balancer
  //
  // negotiates X25519MLKEM768 key exchange when clients advertise support for it.
  //
  // When set to 'DEFAULT', or if no SSL Policy is attached to
  //
  // the target proxy, the load balancer disallows X25519MLKEM768 key
  //
  // exchange before October 2026, and allows it afterward. When set to
  //
  // 'ENABLED', the load balancer allows X25519MLKEM768 key
  //
  // exchange. When set to 'DEFERRED', the load balancer
  //
  // disallows X25519MLKEM768 key exchange until October 2027, and allows
  //
  // it afterward. Possible values: ["DEFAULT", "ENABLED", "DEFERRED"]
  optional string post_quantum_key_exchange = 10 [json_name = "post_quantum_key_exchange"];

  // Profile specifies the set of SSL features that can be used by the
  //
  // load balancer when negotiating SSL with clients. If using 'CUSTOM',
  //
  // the set of SSL features to enable must be specified in the
  //
  // 'customFeatures' field.
  //
  //
  // See the [official
  // /cloud.google.com/compute/docs/load-balancing/ssl-policies#profilefeaturesupport)
  //
  // for information on what cipher suites each profile provides. If
  //
  // 'CUSTOM' is used, the 'custom_features' attribute **must be set**.
  //
  // If set to 'FIPS_202205', 'minTlsVersion' must also be set to
  //
  // 'TLS_1_2'. Default value: "COMPATIBLE" Possible values: ["COMPATIBLE", "MODERN",
  // "RESTRICTED", "CUSTOM", "FIPS_202205"]
  optional string profile = 11;

  optional string project = 12;

  optional string self_link = 13 [json_name = "self_link"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeStoragePool version is 0
message GoogleComputeStoragePool {
  // Provisioning type of the byte capacity of the pool. Possible values: ["STANDARD",
  // "ADVANCED"]
  optional string capacity_provisioning_type = 1 [json_name = "capacity_provisioning_type"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // Whether Terraform will be prevented from destroying the StoragePool.
  //
  // When the field is set to true or unset in Terraform state, a 'terraform apply'
  //
  // or 'terraform destroy' that would delete the StoragePool will fail.
  //
  // When the field is set to false, deleting the StoragePool is allowed.
  optional bool deletion_protection = 4 [json_name = "deletion_protection"];

  // A description of this resource. Provide this property when you create the
  // resource.
  optional string description = 5;

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 6 [json_name = "effective_labels"];

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string id = 7;

  // Type of the resource.
  optional string kind = 8;

  // The fingerprint used for optimistic locking of this resource.
  //
  // Used internally during updates.
  optional string label_fingerprint = 9 [json_name = "label_fingerprint"];

  // Labels to apply to this storage pool. These can be later modified by the
  // setLabels method.
  //
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 10;

  // Name of the resource. Provided by the client when the resource is created.
  //
  // The name must be 1-63 characters long, and comply with RFC1035.
  //
  // Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'
  //
  // which means the first character must be a lowercase letter,
  //
  // and all following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 11;

  // Provisioning type of the performance-related parameters of the pool, such as
  // throughput and IOPS. Possible values: ["STANDARD", "ADVANCED"]
  optional string performance_provisioning_type = 12 [json_name = "performance_provisioning_type"];

  // Size, in GiB, of the storage pool. For more information about the size limits,
  //
  // see https://cloud.google.com/compute/docs/disks/storage-pools.
  optional string pool_provisioned_capacity_gb = 13 [json_name = "pool_provisioned_capacity_gb"];

  // Provisioned IOPS of the storage pool.
  //
  // Only relevant if the storage pool type is 'hyperdisk-balanced'.
  optional string pool_provisioned_iops = 14 [json_name = "pool_provisioned_iops"];

  // Provisioned throughput, in MB/s, of the storage pool.
  //
  // Only relevant if the storage pool type is 'hyperdisk-balanced' or
  // 'hyperdisk-throughput'.
  optional string pool_provisioned_throughput = 15 [json_name = "pool_provisioned_throughput"];

  optional string project = 16;

  // Status information for the storage pool resource.
  repeated ResourceStatus resource_status = 17 [json_name = "resource_status"];

  // Status information for the storage pool resource.
  repeated Status status = 18;

  // Type of the storage pool. For example, the
  //
  // following are valid values:
  //
  //
  // ompute/v1/projects/{project_id}/zones/{zone}/storagePoolTypes/hyperdisk-balanced'
  //
  // * 'hyperdisk-throughput'
  optional string storage_pool_type = 19 [json_name = "storage_pool_type"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 20 [json_name = "terraform_labels"];

  // A reference to the zone where the storage pool resides.
  optional string zone = 21;

  Params params = 22;

  repeated Timeouts timeouts = 23;

  google.protobuf.Value for_each = 24 [json_name = "for_each"];

  repeated string depends_on = 25 [json_name = "depends_on"];

  optional int32 count = 26;

  optional string provider = 27;

  terraform.v1.Lifecycle lifecycle = 28;

  message ResourceStatus {
    // disk_count: string
    optional string disk_count = 1 [json_name = "disk_count"];

    // last_resize_timestamp: string
    optional string last_resize_timestamp = 2 [json_name = "last_resize_timestamp"];

    // max_total_provisioned_disk_capacity_gb: string
    optional string max_total_provisioned_disk_capacity_gb = 3 [
      json_name = "max_total_provisioned_disk_capacity_gb"
    ];

    // pool_used_capacity_bytes: string
    optional string pool_used_capacity_bytes = 4 [json_name = "pool_used_capacity_bytes"];

    // pool_used_iops: string
    optional string pool_used_iops = 5 [json_name = "pool_used_iops"];

    // pool_used_throughput: string
    optional string pool_used_throughput = 6 [json_name = "pool_used_throughput"];

    // pool_user_written_bytes: string
    optional string pool_user_written_bytes = 7 [json_name = "pool_user_written_bytes"];

    // total_provisioned_disk_capacity_gb: string
    optional string total_provisioned_disk_capacity_gb = 8 [json_name = "total_provisioned_disk_capacity_gb"];

    // total_provisioned_disk_iops: string
    optional string total_provisioned_disk_iops = 9 [json_name = "total_provisioned_disk_iops"];

    // total_provisioned_disk_throughput: string
    optional string total_provisioned_disk_throughput = 10 [json_name = "total_provisioned_disk_throughput"];
  }

  message Status {
    // disk_count: string
    optional string disk_count = 1 [json_name = "disk_count"];

    // last_resize_timestamp: string
    optional string last_resize_timestamp = 2 [json_name = "last_resize_timestamp"];

    // max_total_provisioned_disk_capacity_gb: string
    optional string max_total_provisioned_disk_capacity_gb = 3 [
      json_name = "max_total_provisioned_disk_capacity_gb"
    ];

    // pool_used_capacity_bytes: string
    optional string pool_used_capacity_bytes = 4 [json_name = "pool_used_capacity_bytes"];

    // pool_used_iops: string
    optional string pool_used_iops = 5 [json_name = "pool_used_iops"];

    // pool_used_throughput: string
    optional string pool_used_throughput = 6 [json_name = "pool_used_throughput"];

    // pool_user_written_bytes: string
    optional string pool_user_written_bytes = 7 [json_name = "pool_user_written_bytes"];

    // total_provisioned_disk_capacity_gb: string
    optional string total_provisioned_disk_capacity_gb = 8 [json_name = "total_provisioned_disk_capacity_gb"];

    // total_provisioned_disk_iops: string
    optional string total_provisioned_disk_iops = 9 [json_name = "total_provisioned_disk_iops"];

    // total_provisioned_disk_throughput: string
    optional string total_provisioned_disk_throughput = 10 [json_name = "total_provisioned_disk_throughput"];
  }

  message Params {
    // Resource manager tags to be bound to the storage pool. Tag keys and values have
    // the
    //
    // same definition as resource manager tags. Keys and values can be either in
    // numeric format,
    //
    // such as tagKeys/{tag_key_id} and tagValues/{tag_value_id} or in namespaced format
    // such as
    //
    // {org_id|projectId}/{tag_key_short_name} and {tag_value_short_name}. The field is
    // ignored when empty.
    //
    // The field is immutable and causes resource replacement when mutated. This field
    // is only
    //
    // set at create time and modifying this field after creation will trigger
    // recreation.
    //
    // To apply tags to an existing resource, see the google_tags_tag_binding resource.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeStoragePoolIamBinding version is 0
message GoogleComputeStoragePoolIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeStoragePoolIamMember version is 0
message GoogleComputeStoragePoolIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string name = 4;

  optional string project = 5;

  optional string role = 6;

  optional string zone = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeStoragePoolIamPolicy version is 0
message GoogleComputeStoragePoolIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string name = 3;

  optional string policy_data = 4 [json_name = "policy_data"];

  optional string project = 5;

  optional string zone = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeSubnetwork version is 0
message GoogleComputeSubnetwork {
  // Typically packets destined to IPs within the subnetwork range that do not match
  //
  // existing resources are dropped and prevented from leaving the VPC.
  //
  // Setting this field to true will allow these packets to match dynamic routes
  // injected
  //
  // via BGP even if their destinations match existing subnet ranges.
  optional bool allow_subnet_cidr_routes_overlap = 1 [json_name = "allow_subnet_cidr_routes_overlap"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when
  //
  // you create the resource. This field can be set only at resource
  //
  // creation time.
  optional string description = 4;

  // The range of external IPv6 addresses that are owned by this subnetwork.
  optional string external_ipv6_prefix = 5 [json_name = "external_ipv6_prefix"];

  // Fingerprint of this resource. This field is used internally during updates of
  // this resource.
  optional string fingerprint = 6;

  // The gateway address for default routes to reach destination addresses
  //
  // outside this subnetwork.
  optional string gateway_address = 7 [json_name = "gateway_address"];

  optional string id = 8;

  // The internal IPv6 address range that is assigned to this subnetwork.
  optional string internal_ipv6_prefix = 9 [json_name = "internal_ipv6_prefix"];

  // The range of internal addresses that are owned by this subnetwork.
  //
  // Provide this property when you create the subnetwork. For example,
  //
  // 10.0.0.0/8 or 192.168.0.0/16. Ranges must be unique and
  //
  // non-overlapping within a network. Only IPv4 is supported.
  //
  // Field is optional when 'reserved_internal_range' is defined, otherwise required.
  optional string ip_cidr_range = 10 [json_name = "ip_cidr_range"];

  // Resource reference of a PublicDelegatedPrefix. The PDP must be a sub-PDP
  //
  // in EXTERNAL_IPV6_SUBNETWORK_CREATION or INTERNAL_IPV6_SUBNETWORK_CREATION
  //
  // mode. Use one of the following formats to specify a sub-PDP when creating
  //
  // a dual stack or IPv6-only subnetwork using BYOIP:
  //
  // Full resource URL, as in:
  //
  // ojects/{{projectId}}/regions/{{region}}/publicDelegatedPrefixes/{{sub-pdp-name}}'
  //
  // Partial URL, as in:
  //
  // 'projects/{{projectId}}/regions/region/publicDelegatedPrefixes/{{sub-pdp-name}}'
  //
  // * 'regions/{{region}}/publicDelegatedPrefixes/{{sub-pdp-name}}'
  optional string ip_collection = 11 [json_name = "ip_collection"];

  // The access type of IPv6 address this subnet holds. It's immutable and can only be
  // specified during creation
  //
  // or the first time the subnet is updated into IPV4_IPV6 dual stack. If the
  // ipv6_type is EXTERNAL then this subnet
  //
  // cannot enable direct path. Possible values: ["EXTERNAL", "INTERNAL"]
  optional string ipv6_access_type = 12 [json_name = "ipv6_access_type"];

  // The range of internal IPv6 addresses that are owned by this subnetwork.
  optional string ipv6_cidr_range = 13 [json_name = "ipv6_cidr_range"];

  // Possible endpoints of this subnetwork. It can be one of the following:
  //
  // * VM_ONLY: The subnetwork can be used for creating instances and IPv6 addresses
  // with VM endpoint type. Such a subnetwork
  //
  // gets external IPv6 ranges from a public delegated prefix and cannot be used to
  // create NetLb.
  //
  // * VM_AND_FR: The subnetwork can be used for creating both VM instances and
  // Forwarding Rules. It can also be used to reserve
  //
  // IPv6 addresses with both VM and FR endpoint types. Such a subnetwork gets its
  // IPv6 range from Google IP Pool directly.
  optional string ipv6_gce_endpoint = 14 [json_name = "ipv6_gce_endpoint"];

  // The name of the resource, provided by the client when initially
  //
  // creating the resource. The name must be 1-63 characters long, and
  //
  // comply with RFC1035. Specifically, the name must be 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which
  //
  // means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 15;

  // The network this subnet belongs to.
  //
  // Only networks that are in the distributed mode can have subnetworks.
  optional string network = 16;

  // When enabled, VMs in this subnetwork without external IP addresses can
  //
  // access Google APIs and services by using Private Google Access.
  optional bool private_ip_google_access = 17 [json_name = "private_ip_google_access"];

  // The private IPv6 google access type for the VMs in this subnet.
  optional string private_ipv6_google_access = 18 [json_name = "private_ipv6_google_access"];

  optional string project = 19;

  // The purpose of the resource. This field can be either 'PRIVATE',
  // 'REGIONAL_MANAGED_PROXY', 'GLOBAL_MANAGED_PROXY', 'PRIVATE_SERVICE_CONNECT',
  // 'PEER_MIGRATION' or
  // 'PRIVATE_NAT'([Beta](../guides/provider_versions.html.markdown)).
  //
  // A subnet with purpose set to 'REGIONAL_MANAGED_PROXY' is a user-created
  // subnetwork that is reserved for regional Envoy-based load balancers.
  //
  // A subnetwork in a given region with purpose set to 'GLOBAL_MANAGED_PROXY' is a
  // proxy-only subnet and is shared between all the cross-regional Envoy-based load
  // balancers.
  //
  // A subnetwork with purpose set to 'PRIVATE_SERVICE_CONNECT' reserves the subnet
  // for hosting a Private Service Connect published service.
  //
  // A subnetwork with purpose set to 'PEER_MIGRATION' is a user created subnetwork
  // that is reserved for migrating resources from one peered network to another.
  //
  // A subnetwork with purpose set to 'PRIVATE_NAT' is used as source range for
  // Private NAT gateways.
  //
  // Note that 'REGIONAL_MANAGED_PROXY' is the preferred setting for all regional
  // Envoy load balancers.
  //
  // If unspecified, the purpose defaults to 'PRIVATE'.
  optional string purpose = 20;

  // The GCP region for this subnetwork.
  optional string region = 21;

  // The ID of the reserved internal range. Must be prefixed with
  // 'networkconnectivity.googleapis.com'
  //
  // E.g.
  // vity.googleapis.com/projects/{project}/locations/global/internalRanges/{rangeId}'
  optional string reserved_internal_range = 22 [json_name = "reserved_internal_range"];

  // 'Configures subnet mask resolution for this subnetwork.' Possible values:
  // ["ARP_ALL_RANGES", "ARP_PRIMARY_RANGE", "ARP_BROADCAST_PRIMARY_RANGE",
  // "ARP_BROADCAST_PRIMARY_RANGE_WITH_LEARNING"]
  optional string resolve_subnet_mask = 23 [json_name = "resolve_subnet_mask"];

  // The role of subnetwork.
  //
  // Currently, this field is only used when 'purpose' is 'REGIONAL_MANAGED_PROXY'.
  //
  // The value can be set to 'ACTIVE' or 'BACKUP'.
  //
  // An 'ACTIVE' subnetwork is one that is currently being used for Envoy-based load
  // balancers in a region.
  //
  // A 'BACKUP' subnetwork is one that is ready to be promoted to 'ACTIVE' or is
  // currently draining. Possible values: ["ACTIVE", "BACKUP"]
  optional string role = 24;

  optional string self_link = 25 [json_name = "self_link"];

  // Controls the removal behavior of secondary_ip_range.
  //
  // When false, removing secondary_ip_range from config will not produce a diff as
  //
  // the provider will default to the API's value.
  //
  // When true, the provider will treat removing secondary_ip_range as sending an
  //
  // empty list of secondary IP ranges to the API.
  //
  // Defaults to false.
  optional bool send_secondary_ip_range_if_empty = 26 [json_name = "send_secondary_ip_range_if_empty"];

  // The stack type for this subnet to identify whether the IPv6 feature is enabled or
  // not.
  //
  // If not specified IPV4_ONLY will be used. Possible values: ["IPV4_ONLY",
  // "IPV4_IPV6", "IPV6_ONLY"]
  optional string stack_type = 27 [json_name = "stack_type"];

  // 'The state of the subnetwork, which can be one of the following values:
  //
  // READY: Subnetwork is created and ready to use DRAINING: only applicable to
  // subnetworks that have the purpose
  //
  // set to INTERNAL_HTTPS_LOAD_BALANCER and indicates that connections to the load
  // balancer are being drained.
  //
  // A subnetwork that is draining cannot be used or modified until it reaches a
  // status of READY'
  optional string state = 28;

  // The unique identifier number for the resource. This identifier is defined by the
  // server.
  optional int64 subnetwork_id = 29 [json_name = "subnetwork_id"];

  LogConfig log_config = 30 [json_name = "log_config"];

  Params params = 31;

  repeated SecondaryIpRange secondary_ip_range = 32 [json_name = "secondary_ip_range"];

  repeated Timeouts timeouts = 33;

  google.protobuf.Value for_each = 34 [json_name = "for_each"];

  repeated string depends_on = 35 [json_name = "depends_on"];

  optional int32 count = 36;

  optional string provider = 37;

  terraform.v1.Lifecycle lifecycle = 38;

  message LogConfig {
    // Can only be specified if VPC flow logging for this subnetwork is enabled.
    //
    // Toggles the aggregation interval for collecting flow logs. Increasing the
    //
    // interval time will reduce the amount of generated flow logs for long
    //
    // lasting connections. Default is an interval of 5 seconds per connection. Default
    // value: "INTERVAL_5_SEC" Possible values: ["INTERVAL_5_SEC", "INTERVAL_30_SEC",
    // "INTERVAL_1_MIN", "INTERVAL_5_MIN", "INTERVAL_10_MIN", "INTERVAL_15_MIN"]
    optional string aggregation_interval = 1 [json_name = "aggregation_interval"];

    // Export filter used to define which VPC flow logs should be logged, as as CEL
    // expression. See
    //
    // https://cloud.google.com/vpc/docs/flow-logs#filtering for details on how to
    // format this field.
    //
    // The default value is 'true', which evaluates to include everything.
    optional string filter_expr = 2 [json_name = "filter_expr"];

    // Can only be specified if VPC flow logging for this subnetwork is enabled.
    //
    // The value of the field must be in [0, 1]. Set the sampling rate of VPC
    //
    // flow logs within the subnetwork where 1.0 means all collected logs are
    //
    // reported and 0.0 means no logs are reported. Default is 0.5 which means
    //
    // half of all collected logs are reported.
    optional int64 flow_sampling = 3 [json_name = "flow_sampling"];

    // Can only be specified if VPC flow logging for this subnetwork is enabled.
    //
    // Configures whether metadata fields should be added to the reported VPC
    //
    // flow logs. Default value: "INCLUDE_ALL_METADATA" Possible values:
    // ["EXCLUDE_ALL_METADATA", "INCLUDE_ALL_METADATA", "CUSTOM_METADATA"]
    optional string metadata = 4;

    // List of metadata fields that should be added to reported logs.
    //
    // Can only be specified if VPC flow logs for this subnetwork is enabled and
    // "metadata" is set to CUSTOM_METADATA.
    repeated string metadata_fields = 5 [json_name = "metadata_fields"];
  }

  message Params {
    // Resource manager tags to be bound to the subnetwork. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456. The field is ignored when empty.
    //
    // The field is immutable and causes resource replacement when mutated. This field
    // is only
    //
    // set at create time and modifying this field after creation will trigger
    // recreation.
    //
    // To apply tags to an existing resource, see the google_tags_tag_binding resource.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message SecondaryIpRange {
    // The range of IP addresses belonging to this subnetwork secondary
    //
    // range. Provide this property when you create the subnetwork.
    //
    // Ranges must be unique and non-overlapping with all primary and
    //
    // secondary IP ranges within a network. Only IPv4 is supported.
    //
    // Field is optional when 'reserved_internal_range' is defined, otherwise required.
    optional string ip_cidr_range = 1 [json_name = "ip_cidr_range"];

    // The name associated with this subnetwork secondary range, used
    //
    // when adding an alias IP range to a VM instance. The name must
    //
    // be 1-63 characters long, and comply with RFC1035. The name
    //
    // must be unique within the subnetwork.
    optional string range_name = 2 [json_name = "range_name"];

    // The ID of the reserved internal range. Must be prefixed with
    // 'networkconnectivity.googleapis.com'
    //
    // E.g.
    // vity.googleapis.com/projects/{project}/locations/global/internalRanges/{rangeId}'
    optional string reserved_internal_range = 3 [json_name = "reserved_internal_range"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeSubnetworkIamBinding version is 0
message GoogleComputeSubnetworkIamBinding {
  optional string etag = 1;

  optional string id = 2;

  repeated string members = 3;

  optional string project = 4;

  optional string region = 5;

  optional string role = 6;

  optional string subnetwork = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeSubnetworkIamMember version is 0
message GoogleComputeSubnetworkIamMember {
  optional string etag = 1;

  optional string id = 2;

  optional string member = 3;

  optional string project = 4;

  optional string region = 5;

  optional string role = 6;

  optional string subnetwork = 7;

  Condition condition = 8;

  google.protobuf.Value for_each = 9 [json_name = "for_each"];

  repeated string depends_on = 10 [json_name = "depends_on"];

  optional int32 count = 11;

  optional string provider = 12;

  terraform.v1.Lifecycle lifecycle = 13;

  message Condition {
    optional string description = 1;

    optional string expression = 2;

    optional string title = 3;
  }
}

// GoogleComputeSubnetworkIamPolicy version is 0
message GoogleComputeSubnetworkIamPolicy {
  optional string etag = 1;

  optional string id = 2;

  optional string policy_data = 3 [json_name = "policy_data"];

  optional string project = 4;

  optional string region = 5;

  optional string subnetwork = 6;

  google.protobuf.Value for_each = 7 [json_name = "for_each"];

  repeated string depends_on = 8 [json_name = "depends_on"];

  optional int32 count = 9;

  optional string provider = 10;

  terraform.v1.Lifecycle lifecycle = 11;
}

// GoogleComputeTargetGrpcProxy version is 0
message GoogleComputeTargetGrpcProxy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Fingerprint of this resource. A hash of the contents stored in
  //
  // this object. This field is used in optimistic locking. This field
  //
  // will be ignored when inserting a TargetGrpcProxy. An up-to-date
  //
  // fingerprint must be provided in order to patch/update the
  //
  // TargetGrpcProxy; otherwise, the request will fail with error
  //
  // 412 conditionNotMet. To see the latest fingerprint, make a get()
  //
  // request to retrieve the TargetGrpcProxy. A base64-encoded string.
  optional string fingerprint = 4;

  optional string id = 5;

  // Name of the resource. Provided by the client when the resource
  //
  // is created. The name must be 1-63 characters long, and comply
  //
  // with RFC1035. Specifically, the name must be 1-63 characters long
  //
  // and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which
  //
  // means the first character must be a lowercase letter, and all
  //
  // following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 6;

  optional string project = 7;

  optional string self_link = 8 [json_name = "self_link"];

  // Server-defined URL with id for the resource.
  optional string self_link_with_id = 9 [json_name = "self_link_with_id"];

  // URL to the UrlMap resource that defines the mapping from URL to
  //
  // the BackendService. The protocol field in the BackendService
  //
  // must be set to GRPC.
  optional string url_map = 10 [json_name = "url_map"];

  // If true, indicates that the BackendServices referenced by
  //
  // the urlMap may be accessed by gRPC applications without using
  //
  // a sidecar proxy. This will enable configuration checks on urlMap
  //
  // and its referenced BackendServices to not allow unsupported features.
  //
  // A gRPC application must use "xds:///" scheme in the target URI
  //
  // of the service it is connecting to. If false, indicates that the
  //
  // BackendServices referenced by the urlMap will be accessed by gRPC
  //
  // applications via a sidecar proxy. In this case, a gRPC application
  //
  // must not use "xds:///" scheme in the target URI of the service
  //
  // it is connecting to
  optional bool validate_for_proxyless = 11 [json_name = "validate_for_proxyless"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeTargetHttpProxy version is 0
message GoogleComputeTargetHttpProxy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Fingerprint of this resource. A hash of the contents stored in this object. This
  // field is used in optimistic locking.
  //
  // This field will be ignored when inserting a TargetHttpProxy. An up-to-date
  // fingerprint must be provided in order to
  //
  // patch/update the TargetHttpProxy; otherwise, the request will fail with error 412
  // conditionNotMet.
  //
  // To see the latest fingerprint, make a get() request to retrieve the
  // TargetHttpProxy.
  //
  // A base64-encoded string.
  optional string fingerprint = 4;

  // Specifies how long to keep a connection open, after completing a response,
  //
  // while there is no matching traffic (in seconds). If an HTTP keepalive is
  //
  // not specified, a default value will be used. For Global
  //
  // external HTTP(S) load balancer, the default value is 610 seconds, the
  //
  // minimum allowed value is 5 seconds and the maximum allowed value is 1200
  //
  // seconds. For cross-region internal HTTP(S) load balancer, the default
  //
  // value is 600 seconds, the minimum allowed value is 5 seconds, and the
  //
  // maximum allowed value is 600 seconds. For Global external HTTP(S) load
  //
  // balancer (classic), this option is not available publicly.
  optional int64 http_keep_alive_timeout_sec = 5 [json_name = "http_keep_alive_timeout_sec"];

  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 7;

  optional string project = 8;

  // This field only applies when the forwarding rule that references
  //
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  optional bool proxy_bind = 9 [json_name = "proxy_bind"];

  // The unique identifier for the resource.
  optional int64 proxy_id = 10 [json_name = "proxy_id"];

  optional string self_link = 11 [json_name = "self_link"];

  // A reference to the UrlMap resource that defines the mapping from URL
  //
  // to the BackendService.
  optional string url_map = 12 [json_name = "url_map"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeTargetHttpsProxy version is 0
message GoogleComputeTargetHttpsProxy {
  // URLs to certificate manager certificate resources that are used to authenticate
  // connections between users and the load balancer.
  //
  // Certificate manager certificates only apply when the load balancing scheme is set
  // to INTERNAL_MANAGED.
  //
  // For EXTERNAL and EXTERNAL_MANAGED, use certificate_map instead.
  //
  // sslCertificates and certificateManagerCertificates fields can not be defined
  // together.
  //
  // Accepted format is
  // ogleapis.com/projects/{project}/locations/{location}/certificates/{resourceName}'
  // or just the self_link
  // 'projects/{project}/locations/{location}/certificates/{resourceName}'
  repeated string certificate_manager_certificates = 1 [json_name = "certificate_manager_certificates"];

  // A reference to the CertificateMap resource uri that identifies a certificate map
  //
  // associated with the given target proxy. This field is only supported for EXTERNAL
  // and EXTERNAL_MANAGED load balancing schemes.
  //
  // For INTERNAL_MANAGED, use certificate_manager_certificates instead.
  //
  // Accepted format is
  // apis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}'.
  optional string certificate_map = 2 [json_name = "certificate_map"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 5;

  // Fingerprint of this resource. A hash of the contents stored in this object. This
  // field is used in optimistic locking.
  //
  // This field will be ignored when inserting a TargetHttpsProxy. An up-to-date
  // fingerprint must be provided in order to
  //
  // patch the TargetHttpsProxy; otherwise, the request will fail with error 412
  // conditionNotMet.
  //
  // To see the latest fingerprint, make a get() request to retrieve the
  // TargetHttpsProxy.
  //
  // A base64-encoded string.
  optional string fingerprint = 6;

  // Specifies how long to keep a connection open, after completing a response,
  //
  // while there is no matching traffic (in seconds). If an HTTP keepalive is
  //
  // not specified, a default value will be used. For Global
  //
  // external HTTP(S) load balancer, the default value is 610 seconds, the
  //
  // minimum allowed value is 5 seconds and the maximum allowed value is 1200
  //
  // seconds. For cross-region internal HTTP(S) load balancer, the default
  //
  // value is 600 seconds, the minimum allowed value is 5 seconds, and the
  //
  // maximum allowed value is 600 seconds. For Global external HTTP(S) load
  //
  // balancer (classic), this option is not available publicly.
  optional int64 http_keep_alive_timeout_sec = 7 [json_name = "http_keep_alive_timeout_sec"];

  optional string id = 8;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 9;

  optional string project = 10;

  // This field only applies when the forwarding rule that references
  //
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  optional bool proxy_bind = 11 [json_name = "proxy_bind"];

  // The unique identifier for the resource.
  optional int64 proxy_id = 12 [json_name = "proxy_id"];

  // Specifies the QUIC override policy for this resource. This determines
  //
  // whether the load balancer will attempt to negotiate QUIC with clients
  //
  // or not. Can specify one of NONE, ENABLE, or DISABLE. If NONE is
  //
  // specified, Google manages whether QUIC is used. Default value: "NONE" Possible
  // values: ["NONE", "ENABLE", "DISABLE"]
  optional string quic_override = 13 [json_name = "quic_override"];

  optional string self_link = 14 [json_name = "self_link"];

  // A URL referring to a networksecurity.ServerTlsPolicy
  //
  // resource that describes how the proxy should authenticate inbound
  //
  // traffic. serverTlsPolicy only applies to a global TargetHttpsProxy
  //
  // attached to globalForwardingRules with the loadBalancingScheme
  //
  // set to INTERNAL_SELF_MANAGED or EXTERNAL or EXTERNAL_MANAGED.
  //
  // For details which ServerTlsPolicy resources are accepted with
  //
  // INTERNAL_SELF_MANAGED and which with EXTERNAL, EXTERNAL_MANAGED
  //
  // loadBalancingScheme consult ServerTlsPolicy documentation.
  //
  // If left blank, communications are not encrypted.
  //
  //
  // If you remove this field from your configuration at the same time as
  //
  // deleting or recreating a referenced ServerTlsPolicy resource, you will
  //
  // receive a resourceInUseByAnotherResource error. Use
  // lifecycle.create_before_destroy
  //
  // within the ServerTlsPolicy resource to avoid this.
  optional string server_tls_policy = 15 [json_name = "server_tls_policy"];

  // URLs to SslCertificate resources that are used to authenticate connections
  // between users and the load balancer.
  //
  // Currently, you may specify up to 15 SSL certificates. sslCertificates do not
  // apply when the load balancing scheme is set to INTERNAL_SELF_MANAGED.
  //
  // sslCertificates and certificateManagerCertificates can not be defined together.
  repeated string ssl_certificates = 16 [json_name = "ssl_certificates"];

  // A reference to the SslPolicy resource that will be associated with
  //
  // the TargetHttpsProxy resource. If not set, the TargetHttpsProxy
  //
  // resource will not have any SSL policy configured.
  optional string ssl_policy = 17 [json_name = "ssl_policy"];

  // Specifies whether TLS 1.3 0-RTT Data (“Early Data”) should be accepted for this
  // service.
  //
  // Early Data allows a TLS resumption handshake to include the initial application
  // payload
  //
  // (a HTTP request) alongside the handshake, reducing the effective round trips to
  // “zero”.
  //
  // This applies to TLS 1.3 connections over TCP (HTTP/2) as well as over UDP
  // (QUIC/h3). Possible values: ["STRICT", "PERMISSIVE", "UNRESTRICTED", "DISABLED"]
  optional string tls_early_data = 18 [json_name = "tls_early_data"];

  // A reference to the UrlMap resource that defines the mapping from URL
  //
  // to the BackendService.
  optional string url_map = 19 [json_name = "url_map"];

  repeated Timeouts timeouts = 20;

  google.protobuf.Value for_each = 21 [json_name = "for_each"];

  repeated string depends_on = 22 [json_name = "depends_on"];

  optional int32 count = 23;

  optional string provider = 24;

  terraform.v1.Lifecycle lifecycle = 25;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeTargetInstance version is 0
message GoogleComputeTargetInstance {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  optional string id = 4;

  // The Compute instance VM handling traffic for this target instance.
  //
  // Accepts the instance self-link, relative path
  //
  // (e.g. 'projects/project/zones/zone/instances/instance') or name. If
  //
  // name is given, the zone will default to the given zone or
  //
  // the provider-default zone and the project will default to the
  //
  // provider-level project.
  optional string instance = 5;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 6;

  // NAT option controlling how IPs are NAT'ed to the instance.
  //
  // Currently only NO_NAT (default value) is supported. Default value: "NO_NAT"
  // Possible values: ["NO_NAT"]
  optional string nat_policy = 7 [json_name = "nat_policy"];

  optional string project = 8;

  optional string self_link = 9 [json_name = "self_link"];

  // URL of the zone where the target instance resides.
  optional string zone = 10;

  repeated Timeouts timeouts = 11;

  google.protobuf.Value for_each = 12 [json_name = "for_each"];

  repeated string depends_on = 13 [json_name = "depends_on"];

  optional int32 count = 14;

  optional string provider = 15;

  terraform.v1.Lifecycle lifecycle = 16;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeTargetPool version is 0
message GoogleComputeTargetPool {
  // URL to the backup target pool. Must also set failover_ratio.
  optional string backup_pool = 1 [json_name = "backup_pool"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // Textual description field.
  optional string description = 3;

  // Ratio (0 to 1) of failed nodes before using the backup pool (which must also be
  // set).
  optional int64 failover_ratio = 4 [json_name = "failover_ratio"];

  // List of zero or one health check name or self_link. Only legacy
  // google_compute_http_health_check is supported.
  repeated string health_checks = 5 [json_name = "health_checks"];

  optional string id = 6;

  // List of instances in the pool. They can be given as URLs, or in the form of
  // "zone/name". Note that the instances need not exist at the time of target pool
  // creation, so there is no need to use the Terraform interpolators to create a
  // dependency on the instances from the target pool.
  repeated string instances = 7;

  // A unique name for the resource, required by GCE. Changing this forces a new
  // resource to be created.
  optional string name = 8;

  // The ID of the project in which the resource belongs. If it is not provided, the
  // provider project is used.
  optional string project = 9;

  // Where the target pool resides. Defaults to project region.
  optional string region = 10;

  // The URI of the created resource.
  optional string self_link = 11 [json_name = "self_link"];

  // How to distribute load. Options are "NONE" (no affinity). "CLIENT_IP" (hash of
  // the source/dest addresses / ports), and "CLIENT_IP_PROTO" also includes the
  // protocol (default "NONE").
  optional string session_affinity = 12 [json_name = "session_affinity"];

  repeated Timeouts timeouts = 13;

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeTargetSslProxy version is 0
message GoogleComputeTargetSslProxy {
  // A reference to the BackendService resource.
  optional string backend_service = 1 [json_name = "backend_service"];

  // A reference to the CertificateMap resource uri that identifies a certificate map
  //
  // associated with the given target proxy. This field can only be set for global
  // target proxies.
  //
  // Accepted format is
  // apis.com/projects/{project}/locations/{location}/certificateMaps/{resourceName}'.
  optional string certificate_map = 2 [json_name = "certificate_map"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 3 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 5;

  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 7;

  optional string project = 8;

  // Specifies the type of proxy header to append before sending data to
  //
  // the backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
  optional string proxy_header = 9 [json_name = "proxy_header"];

  // The unique identifier for the resource.
  optional int64 proxy_id = 10 [json_name = "proxy_id"];

  optional string self_link = 11 [json_name = "self_link"];

  // A list of SslCertificate resources that are used to authenticate
  //
  // connections between users and the load balancer. At least one
  //
  // SSL certificate must be specified.
  repeated string ssl_certificates = 12 [json_name = "ssl_certificates"];

  // A reference to the SslPolicy resource that will be associated with
  //
  // the TargetSslProxy resource. If not set, the TargetSslProxy
  //
  // resource will not have any SSL policy configured.
  optional string ssl_policy = 13 [json_name = "ssl_policy"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeTargetTcpProxy version is 0
message GoogleComputeTargetTcpProxy {
  // A reference to the BackendService resource. This field is optional when
  //
  // the loadBalancingScheme (available in beta) is set to INTERNAL_MANAGED.
  optional string backend_service = 1 [json_name = "backend_service"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 4;

  optional string id = 5;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035. Specifically, the name must be 1-63 characters long and match
  //
  // the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means the
  //
  // first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 6;

  optional string project = 7;

  // This field only applies when the forwarding rule that references
  //
  // this target proxy has a loadBalancingScheme set to INTERNAL_SELF_MANAGED.
  optional bool proxy_bind = 8 [json_name = "proxy_bind"];

  // Specifies the type of proxy header to append before sending data to
  //
  // the backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"]
  optional string proxy_header = 9 [json_name = "proxy_header"];

  // The unique identifier for the resource.
  optional int64 proxy_id = 10 [json_name = "proxy_id"];

  optional string self_link = 11 [json_name = "self_link"];

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeUrlMap version is 0
message GoogleComputeUrlMap {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // The backend service or backend bucket to use when none of the given rules match.
  optional string default_service = 2 [json_name = "default_service"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 3 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  //
  // the resource.
  optional string description = 4;

  // Fingerprint of this resource. A hash of the contents stored in this object. This
  //
  // field is used in optimistic locking.
  optional string fingerprint = 5;

  optional string id = 6;

  // The unique identifier for the resource.
  optional int64 map_id = 7 [json_name = "map_id"];

  // Name of the resource. Provided by the client when the resource is created. The
  //
  // name must be 1-63 characters long, and comply with RFC1035. Specifically, the
  //
  // name must be 1-63 characters long and match the regular expression
  //
  // '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character must be a lowercase
  //
  // letter, and all following characters must be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 8;

  optional string project = 9;

  optional string self_link = 10 [json_name = "self_link"];

  DefaultCustomErrorResponsePolicy default_custom_error_response_policy = 11 [json_name = "default_custom_error_response_policy"];

  DefaultRouteAction default_route_action = 12 [json_name = "default_route_action"];

  DefaultUrlRedirect default_url_redirect = 13 [json_name = "default_url_redirect"];

  HeaderAction header_action = 14 [json_name = "header_action"];

  repeated HostRule host_rule = 15 [json_name = "host_rule"];

  repeated PathMatcher path_matcher = 16 [json_name = "path_matcher"];

  repeated Test test = 17;

  repeated Timeouts timeouts = 18;

  google.protobuf.Value for_each = 19 [json_name = "for_each"];

  repeated string depends_on = 20 [json_name = "depends_on"];

  optional int32 count = 21;

  optional string provider = 22;

  terraform.v1.Lifecycle lifecycle = 23;

  message DefaultCustomErrorResponsePolicy {
    // The full or partial URL to the BackendBucket resource that contains the custom
    // error content. Examples are:
    //
    //
    // .googleapis.com/compute/v1/projects/project/global/backendBuckets/myBackendBucket
    //
    // compute/v1/projects/project/global/backendBuckets/myBackendBucket
    //
    // global/backendBuckets/myBackendBucket
    //
    //
    // If errorService is not specified at lower levels like pathMatcher, pathRule and
    // routeRule, an errorService specified at a higher level in the UrlMap will be
    // used. If UrlMap.defaultCustomErrorResponsePolicy contains one or more
    // errorResponseRules[], it must specify errorService.
    //
    // If load balancer cannot reach the backendBucket, a simple Not Found Error will be
    // returned, with the original response code (or overrideResponseCode if
    // configured).
    optional string error_service = 1 [json_name = "error_service"];

    repeated ErrorResponseRule error_response_rule = 2 [json_name = "error_response_rule"];

    message ErrorResponseRule {
      // Valid values include:
      //
      // - A number between 400 and 599: For example 401 or 503, in which case the load
      // balancer applies the policy if the error code exactly matches this value.
      //
      // - 5xx: Load Balancer will apply the policy if the backend service responds with
      // any response code in the range of 500 to 599.
      //
      // - 4xx: Load Balancer will apply the policy if the backend service responds with
      // any response code in the range of 400 to 499.
      //
      // Values must be unique within matchResponseCodes and across all errorResponseRules
      // of CustomErrorResponsePolicy.
      repeated string match_response_codes = 1 [json_name = "match_response_codes"];

      // The HTTP status code returned with the response containing the custom error
      // content.
      //
      // If overrideResponseCode is not supplied, the same response code returned by the
      // original backend bucket or backend service is returned to the client.
      optional int64 override_response_code = 2 [json_name = "override_response_code"];

      // The full path to a file within backendBucket. For example:
      // /errors/defaultError.html
      //
      // path must start with a leading slash. path cannot have trailing slashes.
      //
      // If the file is not available in backendBucket or the load balancer cannot reach
      // the BackendBucket, a simple Not Found Error is returned to the client.
      //
      // The value must be from 1 to 1024 characters.
      optional string path = 3;
    }
  }

  message DefaultRouteAction {
    CachePolicy cache_policy = 1 [json_name = "cache_policy"];

    CorsPolicy cors_policy = 2 [json_name = "cors_policy"];

    FaultInjectionPolicy fault_injection_policy = 3 [json_name = "fault_injection_policy"];

    MaxStreamDuration max_stream_duration = 4 [json_name = "max_stream_duration"];

    RequestMirrorPolicy request_mirror_policy = 5 [json_name = "request_mirror_policy"];

    RetryPolicy retry_policy = 6 [json_name = "retry_policy"];

    Timeout timeout = 7;

    UrlRewrite url_rewrite = 8 [json_name = "url_rewrite"];

    repeated WeightedBackendServices weighted_backend_services = 9 [json_name = "weighted_backend_services"];

    message CachePolicy {
      // Bypass the cache when the specified request headers are matched by name,
      //
      // e.g. Pragma or Authorization headers. Values are case-insensitive. Up to 5
      //
      // header names can be specified. The cache is bypassed for all cacheMode
      //
      // values.
      repeated string cache_bypass_request_header_names = 1 [json_name = "cache_bypass_request_header_names"];

      // Specifies the cache setting for all responses from this route. If not
      //
      // specified, Cloud CDN uses CACHE_ALL_STATIC mode. Possible values:
      // ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
      optional string cache_mode = 2 [json_name = "cache_mode"];

      // Negative caching allows per-status code TTLs to be set, in order to apply
      //
      // fine-grained caching for common errors or redirects. This can reduce the
      //
      // load on your origin and improve end-user experience by reducing response
      //
      // latency. When the cacheMode is set to CACHE_ALL_STATIC or
      //
      // USE_ORIGIN_HEADERS, negative caching applies to responses with the
      //
      // specified response code that lack any Cache-Control, Expires, or
      //
      // Pragma: no-cache directives. When the cacheMode is set to
      //
      // FORCE_CACHE_ALL, negative caching applies to all responses with the
      //
      // specified response code, and overrides any caching headers. By default,
      //
      // Cloud CDN applies the following TTLs to these HTTP status codes:
      //
      // * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m
      //
      // * 404 (Not Found), 410 (Gone), 451 (Unavailable For Legal Reasons): 120s
      //
      // * 405 (Method Not Found), 501 (Not Implemented): 60s
      //
      // These defaults can be overridden in negativeCachingPolicy. If not
      //
      // specified, Cloud CDN applies negative caching by default.
      optional bool negative_caching = 3 [json_name = "negative_caching"];

      // If true then Cloud CDN will combine multiple concurrent cache fill
      //
      // requests into a small number of requests to the origin. If not specified,
      //
      // Cloud CDN applies request coalescing by default.
      optional bool request_coalescing = 4 [json_name = "request_coalescing"];

      CacheKeyPolicy cache_key_policy = 5 [json_name = "cache_key_policy"];

      ClientTtl client_ttl = 6 [json_name = "client_ttl"];

      DefaultTtl default_ttl = 7 [json_name = "default_ttl"];

      MaxTtl max_ttl = 8 [json_name = "max_ttl"];

      repeated NegativeCachingPolicy negative_caching_policy = 9 [json_name = "negative_caching_policy"];

      ServeWhileStale serve_while_stale = 10 [json_name = "serve_while_stale"];

      message CacheKeyPolicy {
        // Names of query string parameters to exclude in cache keys. All other
        //
        // parameters will be included. Either specify excludedQueryParameters
        //
        // or includedQueryParameters, not both. '&' and '=' will be percent
        //
        // encoded and not treated as delimiters. Note: This field applies to
        //
        // routes that use backend services. Attempting to set it on a route that
        //
        // points exclusively to Backend Buckets will result in a configuration
        //
        // error. For routes that point to a Backend Bucket, use
        //
        // includedQueryParameters to define which parameters should be part of
        //
        // the cache key.
        repeated string excluded_query_parameters = 1 [json_name = "excluded_query_parameters"];

        // If true, requests to different hosts will be cached separately. Note:
        //
        // This setting is only applicable to routes that use a Backend Service.
        //
        // It does not affect requests served by a Backend Bucket, as the host is
        //
        // never included in a Backend Bucket's cache key. Attempting to set it on
        //
        // a route that points exclusively to Backend Buckets will result in a
        //
        // configuration error.
        optional bool include_host = 2 [json_name = "include_host"];

        // If true, http and https requests will be cached separately. Note: This
        //
        // setting is only applicable to routes that use a Backend Service. It
        //
        // does not affect requests served by a Backend Bucket, as the protocol is
        //
        // never included in a Backend Bucket's cache key. Attempting to set on a
        //
        // route that points exclusively to Backend Buckets will result in a
        //
        // configuration error.
        optional bool include_protocol = 3 [json_name = "include_protocol"];

        // If true, include query string parameters in the cache key according to
        //
        // includedQueryParameters and excludedQueryParameters. If neither is
        //
        // set, the entire query string will be included. If false, the query
        //
        // string will be excluded from the cache key entirely. Note: This field
        //
        // applies to routes that use backend services. Attempting to set it on a
        //
        // route that points exclusively to Backend Buckets will result in a
        //
        // configuration error. For routes that point to a Backend Bucket, use
        //
        // includedQueryParameters to define which parameters should be part of
        //
        // the cache key.
        optional bool include_query_string = 4 [json_name = "include_query_string"];

        // Allows HTTP cookies (by name) to be used in the cache key. The
        //
        // name=value pair will be used in the cache key Cloud CDN generates.
        //
        // Note: This setting is only applicable to routes that use a Backend
        //
        // Service. It does not affect requests served by a Backend Bucket.
        //
        // Attempting to set it on a route that points exclusively to Backend
        //
        // Buckets will result in a configuration error. Up to 5 cookie names can
        //
        // be specified.
        repeated string included_cookie_names = 5 [json_name = "included_cookie_names"];

        // Allows HTTP request headers (by name) to be used in the cache key.
        repeated string included_header_names = 6 [json_name = "included_header_names"];

        // Names of query string parameters to include in cache keys. All other
        //
        // parameters will be excluded. Either specify includedQueryParameters
        //
        // or excludedQueryParameters, not both. '&' and '=' will be percent
        //
        // encoded and not treated as delimiters.
        repeated string included_query_parameters = 7 [json_name = "included_query_parameters"];
      }

      message ClientTtl {
        // Span of time that's a fraction of a second at nanosecond resolution.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        optional string seconds = 2;
      }

      message DefaultTtl {
        // Span of time that's a fraction of a second at nanosecond resolution.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        optional string seconds = 2;
      }

      message MaxTtl {
        // Span of time that's a fraction of a second at nanosecond resolution.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        optional string seconds = 2;
      }

      message NegativeCachingPolicy {
        // The HTTP status code to define a TTL against. Only HTTP status codes
        //
        // 300, 301, 302, 307, 308, 404, 405, 410, 421, 451 and 501 can be
        //
        // specified as values, and you cannot specify a status code more than
        //
        // once.
        optional int64 code = 1;

        Ttl ttl = 2;

        message Ttl {
          // Span of time that's a fraction of a second at nanosecond resolution.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          optional string seconds = 2;
        }
      }

      message ServeWhileStale {
        // Span of time that's a fraction of a second at nanosecond resolution.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        optional string seconds = 2;
      }
    }

    message CorsPolicy {
      // In response to a preflight request, setting this to true indicates that the
      // actual request can include user credentials.
      //
      // This translates to the Access-Control-Allow-Credentials header.
      optional bool allow_credentials = 1 [json_name = "allow_credentials"];

      // Specifies the content for the Access-Control-Allow-Headers header.
      repeated string allow_headers = 2 [json_name = "allow_headers"];

      // Specifies the content for the Access-Control-Allow-Methods header.
      repeated string allow_methods = 3 [json_name = "allow_methods"];

      // Specifies the regular expression patterns that match allowed origins. For regular
      // expression grammar
      //
      // please see en.cppreference.com/w/cpp/regex/ecmascript
      //
      // An origin is allowed if it matches either an item in allowOrigins or an item in
      // allowOriginRegexes.
      repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

      // Specifies the list of origins that will be allowed to do CORS requests.
      //
      // An origin is allowed if it matches either an item in allowOrigins or an item in
      // allowOriginRegexes.
      repeated string allow_origins = 5 [json_name = "allow_origins"];

      // If true, specifies the CORS policy is disabled. The default value is false, which
      // indicates that the CORS policy is in effect.
      optional bool disabled = 6;

      // Specifies the content for the Access-Control-Expose-Headers header.
      repeated string expose_headers = 7 [json_name = "expose_headers"];

      // Specifies how long results of a preflight request can be cached in seconds.
      //
      // This translates to the Access-Control-Max-Age header.
      optional int64 max_age = 8 [json_name = "max_age"];
    }

    message FaultInjectionPolicy {
      Abort abort = 1;

      Delay delay = 2;

      message Abort {
        // The HTTP status code used to abort the request.
        //
        // The value must be between 200 and 599 inclusive.
        optional int64 http_status = 1 [json_name = "http_status"];

        // The percentage of traffic (connections/operations/requests) which will be aborted
        // as part of fault injection.
        //
        // The value must be between 0.0 and 100.0 inclusive.
        optional int64 percentage = 2;
      }

      message Delay {
        // The percentage of traffic (connections/operations/requests) on which delay will
        // be introduced as part of fault injection.
        //
        // The value must be between 0.0 and 100.0 inclusive.
        optional int64 percentage = 1;

        FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

        message FixedDelay {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are
          //
          // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
          // 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          //
          // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
          // days/year * 10000 years
          optional string seconds = 2;
        }
      }
    }

    message MaxStreamDuration {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      // less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999
      // inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      // inclusive.
      //
      // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
      // days/year * 10000 years
      optional string seconds = 2;
    }

    message RequestMirrorPolicy {
      // The full or partial URL to the BackendService resource being mirrored to.
      optional string backend_service = 1 [json_name = "backend_service"];
    }

    message RetryPolicy {
      // Specifies the allowed number retries. This number must be > 0. If not specified,
      // defaults to 1.
      optional int64 num_retries = 1 [json_name = "num_retries"];

      // Specfies one or more conditions when this retry rule applies. Valid values are:
      //
      //
      // * 5xx: Loadbalancer will attempt a retry if the backend service responds with any
      // 5xx response code,
      //
      // or if the backend service does not respond at all, example: disconnects, reset,
      // read timeout,
      //
      // * connection failure, and refused streams.
      //
      // * gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or
      // 504.
      //
      // * connect-failure: Loadbalancer will retry on failures connecting to backend
      // services,
      //
      // for example due to connection timeouts.
      //
      // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
      //
      // Currently the only retriable error supported is 409.
      //
      // * refused-stream:Loadbalancer will retry if the backend service resets the stream
      // with a REFUSED_STREAM error code.
      //
      // This reset type indicates that it is safe to retry.
      //
      // * cancelled: Loadbalancer will retry if the gRPC status code in the response
      // header is set to cancelled
      //
      // * deadline-exceeded: Loadbalancer will retry if the gRPC status code in the
      // response header is set to deadline-exceeded
      //
      // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the
      // response header is set to resource-exhausted
      //
      // * unavailable: Loadbalancer will retry if the gRPC status code in the response
      // header is set to unavailable
      repeated string retry_conditions = 2 [json_name = "retry_conditions"];

      PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

      message PerTryTimeout {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations
        // less than one second are
        //
        // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
        // 999,999,999 inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        //
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
        // days/year * 10000 years
        optional string seconds = 2;
      }
    }

    message Timeout {
      // Span of time that's a fraction of a second at nanosecond resolution. Durations
      // less than one second are represented
      //
      // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999
      // inclusive.
      optional int64 nanos = 1;

      // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
      // inclusive.
      //
      // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
      // days/year * 10000 years
      optional string seconds = 2;
    }

    message UrlRewrite {
      // Prior to forwarding the request to the selected service, the request's host
      // header is replaced
      //
      // with contents of hostRewrite.
      //
      //
      // The value must be between 1 and 255 characters.
      optional string host_rewrite = 1 [json_name = "host_rewrite"];

      // Prior to forwarding the request to the selected backend service, the matching
      // portion of the
      //
      // request's path is replaced by pathPrefixRewrite.
      //
      //
      // The value must be between 1 and 1024 characters.
      optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
    }

    message WeightedBackendServices {
      // The full or partial URL to the default BackendService resource. Before forwarding
      // the
      //
      // request to backendService, the loadbalancer applies any relevant headerActions
      //
      // specified as part of this backendServiceWeight.
      optional string backend_service = 1 [json_name = "backend_service"];

      // Specifies the fraction of traffic sent to backendService, computed as
      //
      // weight / (sum of all weightedBackendService weights in routeAction) .
      //
      //
      // The selection of a backend service is determined only for new traffic. Once a
      // user's request
      //
      // has been directed to a backendService, subsequent requests will be sent to the
      // same backendService
      //
      // as determined by the BackendService's session affinity policy.
      //
      //
      // The value must be between 0 and 1000
      optional int64 weight = 2;

      HeaderAction header_action = 3 [json_name = "header_action"];

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request prior
        // to
        //
        // forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response
        // prior to sending the
        //
        // response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header to add.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header.
          //
          // If true, headerValue is set for the header, discarding any values that were set
          // for that header.
          optional bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header to add.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          // header.
          //
          // If true, headerValue is set for the header, discarding any values that were set
          // for that header.
          optional bool replace = 3;
        }
      }
    }
  }

  message DefaultUrlRedirect {
    // The host that will be used in the redirect response instead of the one that was
    //
    // supplied in the request. The value must be between 1 and 255 characters.
    optional string host_redirect = 1 [json_name = "host_redirect"];

    // If set to true, the URL scheme in the redirected request is set to https. If set
    // to
    //
    // false, the URL scheme of the redirected request will remain the same as that of
    // the
    //
    // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
    //
    // true for TargetHttpsProxy is not permitted. The default is set to false.
    optional bool https_redirect = 2 [json_name = "https_redirect"];

    // The path that will be used in the redirect response instead of the one that was
    //
    // supplied in the request. pathRedirect cannot be supplied together with
    //
    // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of
    // the
    //
    // original request will be used for the redirect. The value must be between 1 and
    // 1024
    //
    // characters.
    optional string path_redirect = 3 [json_name = "path_redirect"];

    // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
    //
    // retaining the remaining portion of the URL before redirecting the request.
    //
    // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
    //
    // neither. If neither is supplied, the path of the original request will be used
    // for
    //
    // the redirect. The value must be between 1 and 1024 characters.
    optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

    // The HTTP Status code to use for this RedirectAction. Supported values are:
    //
    //
    // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
    //
    //
    // * FOUND, which corresponds to 302.
    //
    //
    // * SEE_OTHER which corresponds to 303.
    //
    //
    // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
    //
    // will be retained.
    //
    //
    // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
    //
    // the request method will be retained. Possible values: ["FOUND",
    // "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER",
    // "TEMPORARY_REDIRECT"]
    optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

    // If set to true, any accompanying query portion of the original URL is removed
    // prior
    //
    // to redirecting the request. If set to false, the query portion of the original
    // URL is
    //
    // retained. The default is set to false.
    //
    // This field is required to ensure an empty block is not set. The normal default
    // value is false.
    optional bool strip_query = 6 [json_name = "strip_query"];
  }

  message HeaderAction {
    // A list of header names for headers that need to be removed from the request
    //
    // prior to forwarding the request to the backendService.
    repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

    // A list of header names for headers that need to be removed from the response
    //
    // prior to sending the response back to the client.
    repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

    repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

    repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

    message RequestHeadersToAdd {
      // The name of the header.
      optional string header_name = 1 [json_name = "header_name"];

      // The value of the header to add.
      optional string header_value = 2 [json_name = "header_value"];

      // If false, headerValue is appended to any values that already exist for the
      //
      // header. If true, headerValue is set for the header, discarding any values that
      //
      // were set for that header.
      optional bool replace = 3;
    }

    message ResponseHeadersToAdd {
      // The name of the header.
      optional string header_name = 1 [json_name = "header_name"];

      // The value of the header to add.
      optional string header_value = 2 [json_name = "header_value"];

      // If false, headerValue is appended to any values that already exist for the
      //
      // header. If true, headerValue is set for the header, discarding any values that
      //
      // were set for that header.
      optional bool replace = 3;
    }
  }

  message HostRule {
    // An optional description of this resource. Provide this property when you create
    //
    // the resource.
    optional string description = 1;

    // The list of host patterns to match. They must be valid hostnames, except * will
    //
    // match any string of ([a-z0-9-.]*). In that case, * must be the first character
    //
    // and must be followed in the pattern by either - or ..
    repeated string hosts = 2;

    // The name of the PathMatcher to use to match the path portion of the URL if the
    //
    // hostRule matches the URL's host portion.
    optional string path_matcher = 3 [json_name = "path_matcher"];
  }

  message PathMatcher {
    // The backend service or backend bucket to use when none of the given paths match.
    optional string default_service = 1 [json_name = "default_service"];

    // An optional description of this resource. Provide this property when you create
    //
    // the resource.
    optional string description = 2;

    // The name to which this PathMatcher is referred by the HostRule.
    optional string name = 3;

    DefaultCustomErrorResponsePolicy default_custom_error_response_policy = 4 [json_name = "default_custom_error_response_policy"];

    DefaultRouteAction default_route_action = 5 [json_name = "default_route_action"];

    DefaultUrlRedirect default_url_redirect = 6 [json_name = "default_url_redirect"];

    HeaderAction header_action = 7 [json_name = "header_action"];

    repeated PathRule path_rule = 8 [json_name = "path_rule"];

    repeated RouteRules route_rules = 9 [json_name = "route_rules"];

    message DefaultCustomErrorResponsePolicy {
      // The full or partial URL to the BackendBucket resource that contains the custom
      // error content. Examples are:
      //
      // .googleapis.com/compute/v1/projects/project/global/backendBuckets/myBackendBucket
      //
      // compute/v1/projects/project/global/backendBuckets/myBackendBucket
      //
      // global/backendBuckets/myBackendBucket
      //
      // If errorService is not specified at lower levels like pathMatcher, pathRule and
      // routeRule, an errorService specified at a higher level in the UrlMap will be
      // used. If UrlMap.defaultCustomErrorResponsePolicy contains one or more
      // errorResponseRules[], it must specify errorService.
      //
      // If load balancer cannot reach the backendBucket, a simple Not Found Error will be
      // returned, with the original response code (or overrideResponseCode if
      // configured).
      optional string error_service = 1 [json_name = "error_service"];

      repeated ErrorResponseRule error_response_rule = 2 [json_name = "error_response_rule"];

      message ErrorResponseRule {
        // Valid values include:
        //
        // - A number between 400 and 599: For example 401 or 503, in which case the load
        // balancer applies the policy if the error code exactly matches this value.
        //
        // - 5xx: Load Balancer will apply the policy if the backend service responds with
        // any response code in the range of 500 to 599.
        //
        // - 4xx: Load Balancer will apply the policy if the backend service responds with
        // any response code in the range of 400 to 499.
        //
        // Values must be unique within matchResponseCodes and across all errorResponseRules
        // of CustomErrorResponsePolicy.
        repeated string match_response_codes = 1 [json_name = "match_response_codes"];

        // The HTTP status code returned with the response containing the custom error
        // content.
        //
        // If overrideResponseCode is not supplied, the same response code returned by the
        // original backend bucket or backend service is returned to the client.
        optional int64 override_response_code = 2 [json_name = "override_response_code"];

        // The full path to a file within backendBucket . For example:
        // /errors/defaultError.html
        //
        // path must start with a leading slash. path cannot have trailing slashes.
        //
        // If the file is not available in backendBucket or the load balancer cannot reach
        // the BackendBucket, a simple Not Found Error is returned to the client.
        //
        // The value must be from 1 to 1024 characters
        optional string path = 3;
      }
    }

    message DefaultRouteAction {
      CachePolicy cache_policy = 1 [json_name = "cache_policy"];

      CorsPolicy cors_policy = 2 [json_name = "cors_policy"];

      FaultInjectionPolicy fault_injection_policy = 3 [json_name = "fault_injection_policy"];

      MaxStreamDuration max_stream_duration = 4 [json_name = "max_stream_duration"];

      RequestMirrorPolicy request_mirror_policy = 5 [json_name = "request_mirror_policy"];

      RetryPolicy retry_policy = 6 [json_name = "retry_policy"];

      Timeout timeout = 7;

      UrlRewrite url_rewrite = 8 [json_name = "url_rewrite"];

      repeated WeightedBackendServices weighted_backend_services = 9 [json_name = "weighted_backend_services"];

      message CachePolicy {
        // Bypass the cache when the specified request headers are matched by name,
        //
        // e.g. Pragma or Authorization headers. Values are case-insensitive. Up to 5
        //
        // header names can be specified. The cache is bypassed for all cacheMode
        //
        // values.
        repeated string cache_bypass_request_header_names = 1 [json_name = "cache_bypass_request_header_names"];

        // Specifies the cache setting for all responses from this route. If not
        //
        // specified, Cloud CDN uses CACHE_ALL_STATIC mode. Possible values:
        // ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
        optional string cache_mode = 2 [json_name = "cache_mode"];

        // Negative caching allows per-status code TTLs to be set, in order to apply
        //
        // fine-grained caching for common errors or redirects. This can reduce the
        //
        // load on your origin and improve end-user experience by reducing response
        //
        // latency. When the cacheMode is set to CACHE_ALL_STATIC or
        //
        // USE_ORIGIN_HEADERS, negative caching applies to responses with the
        //
        // specified response code that lack any Cache-Control, Expires, or
        //
        // Pragma: no-cache directives. When the cacheMode is set to
        //
        // FORCE_CACHE_ALL, negative caching applies to all responses with the
        //
        // specified response code, and overrides any caching headers. By default,
        //
        // Cloud CDN applies the following TTLs to these HTTP status codes:
        //
        // * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m
        //
        // * 404 (Not Found), 410 (Gone), 451 (Unavailable For Legal Reasons): 120s
        //
        // * 405 (Method Not Found), 501 (Not Implemented): 60s
        //
        // These defaults can be overridden in negativeCachingPolicy. If not
        //
        // specified, Cloud CDN applies negative caching by default.
        optional bool negative_caching = 3 [json_name = "negative_caching"];

        // If true then Cloud CDN will combine multiple concurrent cache fill
        //
        // requests into a small number of requests to the origin. If not specified,
        //
        // Cloud CDN applies request coalescing by default.
        optional bool request_coalescing = 4 [json_name = "request_coalescing"];

        CacheKeyPolicy cache_key_policy = 5 [json_name = "cache_key_policy"];

        ClientTtl client_ttl = 6 [json_name = "client_ttl"];

        DefaultTtl default_ttl = 7 [json_name = "default_ttl"];

        MaxTtl max_ttl = 8 [json_name = "max_ttl"];

        repeated NegativeCachingPolicy negative_caching_policy = 9 [json_name = "negative_caching_policy"];

        ServeWhileStale serve_while_stale = 10 [json_name = "serve_while_stale"];

        message CacheKeyPolicy {
          // Names of query string parameters to exclude in cache keys. All other
          //
          // parameters will be included. Either specify excludedQueryParameters
          //
          // or includedQueryParameters, not both. '&' and '=' will be percent
          //
          // encoded and not treated as delimiters. Note: This field applies to
          //
          // routes that use backend services. Attempting to set it on a route that
          //
          // points exclusively to Backend Buckets will result in a configuration
          //
          // error. For routes that point to a Backend Bucket, use
          //
          // includedQueryParameters to define which parameters should be part of
          //
          // the cache key.
          repeated string excluded_query_parameters = 1 [json_name = "excluded_query_parameters"];

          // If true, requests to different hosts will be cached separately. Note:
          //
          // This setting is only applicable to routes that use a Backend Service.
          //
          // It does not affect requests served by a Backend Bucket, as the host is
          //
          // never included in a Backend Bucket's cache key. Attempting to set it on
          //
          // a route that points exclusively to Backend Buckets will result in a
          //
          // configuration error.
          optional bool include_host = 2 [json_name = "include_host"];

          // If true, http and https requests will be cached separately. Note: This
          //
          // setting is only applicable to routes that use a Backend Service. It
          //
          // does not affect requests served by a Backend Bucket, as the protocol is
          //
          // never included in a Backend Bucket's cache key. Attempting to set on a
          //
          // route that points exclusively to Backend Buckets will result in a
          //
          // configuration error.
          optional bool include_protocol = 3 [json_name = "include_protocol"];

          // If true, include query string parameters in the cache key according to
          //
          // includedQueryParameters and excludedQueryParameters. If neither is
          //
          // set, the entire query string will be included. If false, the query
          //
          // string will be excluded from the cache key entirely. Note: This field
          //
          // applies to routes that use backend services. Attempting to set it on a
          //
          // route that points exclusively to Backend Buckets will result in a
          //
          // configuration error. For routes that point to a Backend Bucket, use
          //
          // includedQueryParameters to define which parameters should be part of
          //
          // the cache key.
          optional bool include_query_string = 4 [json_name = "include_query_string"];

          // Allows HTTP cookies (by name) to be used in the cache key. The
          //
          // name=value pair will be used in the cache key Cloud CDN generates.
          //
          // Note: This setting is only applicable to routes that use a Backend
          //
          // Service. It does not affect requests served by a Backend Bucket.
          //
          // Attempting to set it on a route that points exclusively to Backend
          //
          // Buckets will result in a configuration error. Up to 5 cookie names can
          //
          // be specified.
          repeated string included_cookie_names = 5 [json_name = "included_cookie_names"];

          // Allows HTTP request headers (by name) to be used in the cache key.
          repeated string included_header_names = 6 [json_name = "included_header_names"];

          // Names of query string parameters to include in cache keys. All other
          //
          // parameters will be excluded. Either specify includedQueryParameters
          //
          // or excludedQueryParameters, not both. '&' and '=' will be percent
          //
          // encoded and not treated as delimiters.
          repeated string included_query_parameters = 7 [json_name = "included_query_parameters"];
        }

        message ClientTtl {
          // Span of time that's a fraction of a second at nanosecond resolution.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          optional string seconds = 2;
        }

        message DefaultTtl {
          // Span of time that's a fraction of a second at nanosecond resolution.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          optional string seconds = 2;
        }

        message MaxTtl {
          // Span of time that's a fraction of a second at nanosecond resolution.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          optional string seconds = 2;
        }

        message NegativeCachingPolicy {
          // The HTTP status code to define a TTL against. Only HTTP status codes
          //
          // 300, 301, 302, 307, 308, 404, 405, 410, 421, 451 and 501 can be
          //
          // specified as values, and you cannot specify a status code more than
          //
          // once.
          optional int64 code = 1;

          Ttl ttl = 2;

          message Ttl {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }
        }

        message ServeWhileStale {
          // Span of time that's a fraction of a second at nanosecond resolution.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          optional string seconds = 2;
        }
      }

      message CorsPolicy {
        // In response to a preflight request, setting this to true indicates that the
        // actual request can include user credentials.
        //
        // This translates to the Access-Control-Allow-Credentials header.
        optional bool allow_credentials = 1 [json_name = "allow_credentials"];

        // Specifies the content for the Access-Control-Allow-Headers header.
        repeated string allow_headers = 2 [json_name = "allow_headers"];

        // Specifies the content for the Access-Control-Allow-Methods header.
        repeated string allow_methods = 3 [json_name = "allow_methods"];

        // Specifies the regular expression patterns that match allowed origins. For regular
        // expression grammar
        //
        // please see en.cppreference.com/w/cpp/regex/ecmascript
        //
        // An origin is allowed if it matches either an item in allowOrigins or an item in
        // allowOriginRegexes.
        repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

        // Specifies the list of origins that will be allowed to do CORS requests.
        //
        // An origin is allowed if it matches either an item in allowOrigins or an item in
        // allowOriginRegexes.
        repeated string allow_origins = 5 [json_name = "allow_origins"];

        // If true, specifies the CORS policy is disabled. The default value is false, which
        // indicates that the CORS policy is in effect.
        optional bool disabled = 6;

        // Specifies the content for the Access-Control-Expose-Headers header.
        repeated string expose_headers = 7 [json_name = "expose_headers"];

        // Specifies how long results of a preflight request can be cached in seconds.
        //
        // This translates to the Access-Control-Max-Age header.
        optional int64 max_age = 8 [json_name = "max_age"];
      }

      message FaultInjectionPolicy {
        Abort abort = 1;

        Delay delay = 2;

        message Abort {
          // The HTTP status code used to abort the request.
          //
          // The value must be between 200 and 599 inclusive.
          optional int64 http_status = 1 [json_name = "http_status"];

          // The percentage of traffic (connections/operations/requests) which will be aborted
          // as part of fault injection.
          //
          // The value must be between 0.0 and 100.0 inclusive.
          optional int64 percentage = 2;
        }

        message Delay {
          // The percentage of traffic (connections/operations/requests) on which delay will
          // be introduced as part of fault injection.
          //
          // The value must be between 0.0 and 100.0 inclusive.
          optional int64 percentage = 1;

          FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

          message FixedDelay {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            // less than one second are
            //
            // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
            // 999,999,999 inclusive.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            //
            // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
            // days/year * 10000 years
            optional string seconds = 2;
          }
        }
      }

      message MaxStreamDuration {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations
        // less than one second are represented
        //
        // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999
        // inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        //
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
        // days/year * 10000 years
        optional string seconds = 2;
      }

      message RequestMirrorPolicy {
        // The full or partial URL to the BackendService resource being mirrored to.
        optional string backend_service = 1 [json_name = "backend_service"];
      }

      message RetryPolicy {
        // Specifies the allowed number retries. This number must be > 0. If not specified,
        // defaults to 1.
        optional int64 num_retries = 1 [json_name = "num_retries"];

        // Specfies one or more conditions when this retry rule applies. Valid values are:
        //
        //
        // * 5xx: Loadbalancer will attempt a retry if the backend service responds with any
        // 5xx response code,
        //
        // or if the backend service does not respond at all, example: disconnects, reset,
        // read timeout,
        //
        // * connection failure, and refused streams.
        //
        // * gateway-error: Similar to 5xx, but only applies to response codes 502, 503 or
        // 504.
        //
        // * connect-failure: Loadbalancer will retry on failures connecting to backend
        // services,
        //
        // for example due to connection timeouts.
        //
        // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
        //
        // Currently the only retriable error supported is 409.
        //
        // * refused-stream:Loadbalancer will retry if the backend service resets the stream
        // with a REFUSED_STREAM error code.
        //
        // This reset type indicates that it is safe to retry.
        //
        // * cancelled: Loadbalancer will retry if the gRPC status code in the response
        // header is set to cancelled
        //
        // * deadline-exceeded: Loadbalancer will retry if the gRPC status code in the
        // response header is set to deadline-exceeded
        //
        // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the
        // response header is set to resource-exhausted
        //
        // * unavailable: Loadbalancer will retry if the gRPC status code in the response
        // header is set to unavailable
        repeated string retry_conditions = 2 [json_name = "retry_conditions"];

        PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

        message PerTryTimeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          // less than one second are
          //
          // represented with a 0 seconds field and a positive nanos field. Must be from 0 to
          // 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          // inclusive.
          //
          // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
          // days/year * 10000 years
          optional string seconds = 2;
        }
      }

      message Timeout {
        // Span of time that's a fraction of a second at nanosecond resolution. Durations
        // less than one second are represented
        //
        // with a 0 seconds field and a positive nanos field. Must be from 0 to 999,999,999
        // inclusive.
        optional int64 nanos = 1;

        // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
        // inclusive.
        //
        // Note: these bounds are computed from: 60 sec/min * 60 min/hr * 24 hr/day * 365.25
        // days/year * 10000 years
        optional string seconds = 2;
      }

      message UrlRewrite {
        // Prior to forwarding the request to the selected service, the request's host
        // header is replaced
        //
        // with contents of hostRewrite.
        //
        //
        // The value must be between 1 and 255 characters.
        optional string host_rewrite = 1 [json_name = "host_rewrite"];

        // Prior to forwarding the request to the selected backend service, the matching
        // portion of the
        //
        // request's path is replaced by pathPrefixRewrite.
        //
        //
        // The value must be between 1 and 1024 characters.
        optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
      }

      message WeightedBackendServices {
        // The full or partial URL to the default BackendService resource. Before forwarding
        // the
        //
        // request to backendService, the loadbalancer applies any relevant headerActions
        //
        // specified as part of this backendServiceWeight.
        optional string backend_service = 1 [json_name = "backend_service"];

        // Specifies the fraction of traffic sent to backendService, computed as
        //
        // weight / (sum of all weightedBackendService weights in routeAction) .
        //
        //
        // The selection of a backend service is determined only for new traffic. Once a
        // user's request
        //
        // has been directed to a backendService, subsequent requests will be sent to the
        // same backendService
        //
        // as determined by the BackendService's session affinity policy.
        //
        //
        // The value must be between 0 and 1000
        optional int64 weight = 2;

        HeaderAction header_action = 3 [json_name = "header_action"];

        message HeaderAction {
          // A list of header names for headers that need to be removed from the request prior
          // to
          //
          // forwarding the request to the backendService.
          repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

          // A list of header names for headers that need to be removed from the response
          // prior to sending the
          //
          // response back to the client.
          repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

          repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

          repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

          message RequestHeadersToAdd {
            // The name of the header to add.
            optional string header_name = 1 [json_name = "header_name"];

            // The value of the header to add.
            optional string header_value = 2 [json_name = "header_value"];

            // If false, headerValue is appended to any values that already exist for the
            // header.
            //
            // If true, headerValue is set for the header, discarding any values that were set
            // for that header.
            optional bool replace = 3;
          }

          message ResponseHeadersToAdd {
            // The name of the header to add.
            optional string header_name = 1 [json_name = "header_name"];

            // The value of the header to add.
            optional string header_value = 2 [json_name = "header_value"];

            // If false, headerValue is appended to any values that already exist for the
            // header.
            //
            // If true, headerValue is set for the header, discarding any values that were set
            // for that header.
            optional bool replace = 3;
          }
        }
      }
    }

    message DefaultUrlRedirect {
      // The host that will be used in the redirect response instead of the one that was
      //
      // supplied in the request. The value must be between 1 and 255 characters.
      optional string host_redirect = 1 [json_name = "host_redirect"];

      // If set to true, the URL scheme in the redirected request is set to https. If set
      // to
      //
      // false, the URL scheme of the redirected request will remain the same as that of
      // the
      //
      // request. This must only be set for UrlMaps used in TargetHttpProxys. Setting this
      //
      // true for TargetHttpsProxy is not permitted. The default is set to false.
      optional bool https_redirect = 2 [json_name = "https_redirect"];

      // The path that will be used in the redirect response instead of the one that was
      //
      // supplied in the request. pathRedirect cannot be supplied together with
      //
      // prefixRedirect. Supply one alone or neither. If neither is supplied, the path of
      // the
      //
      // original request will be used for the redirect. The value must be between 1 and
      // 1024
      //
      // characters.
      optional string path_redirect = 3 [json_name = "path_redirect"];

      // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
      //
      // retaining the remaining portion of the URL before redirecting the request.
      //
      // prefixRedirect cannot be supplied together with pathRedirect. Supply one alone or
      //
      // neither. If neither is supplied, the path of the original request will be used
      // for
      //
      // the redirect. The value must be between 1 and 1024 characters.
      optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

      // The HTTP Status code to use for this RedirectAction. Supported values are:
      //
      //
      // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
      //
      //
      // * FOUND, which corresponds to 302.
      //
      //
      // * SEE_OTHER which corresponds to 303.
      //
      //
      // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
      //
      // will be retained.
      //
      //
      // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
      //
      // the request method will be retained. Possible values: ["FOUND",
      // "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER",
      // "TEMPORARY_REDIRECT"]
      optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

      // If set to true, any accompanying query portion of the original URL is removed
      // prior
      //
      // to redirecting the request. If set to false, the query portion of the original
      // URL is
      //
      // retained.
      //
      // This field is required to ensure an empty block is not set. The normal default
      // value is false.
      optional bool strip_query = 6 [json_name = "strip_query"];
    }

    message HeaderAction {
      // A list of header names for headers that need to be removed from the request
      //
      // prior to forwarding the request to the backendService.
      repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

      // A list of header names for headers that need to be removed from the response
      //
      // prior to sending the response back to the client.
      repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

      repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

      repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

      message RequestHeadersToAdd {
        // The name of the header.
        optional string header_name = 1 [json_name = "header_name"];

        // The value of the header to add.
        optional string header_value = 2 [json_name = "header_value"];

        // If false, headerValue is appended to any values that already exist for the
        //
        // header. If true, headerValue is set for the header, discarding any values that
        //
        // were set for that header.
        optional bool replace = 3;
      }

      message ResponseHeadersToAdd {
        // The name of the header.
        optional string header_name = 1 [json_name = "header_name"];

        // The value of the header to add.
        optional string header_value = 2 [json_name = "header_value"];

        // If false, headerValue is appended to any values that already exist for the
        //
        // header. If true, headerValue is set for the header, discarding any values that
        //
        // were set for that header.
        optional bool replace = 3;
      }
    }

    message PathRule {
      // The list of path patterns to match. Each must start with / and the only place a
      //
      // \* is allowed is at the end following a /. The string fed to the path matcher
      //
      // does not include any text after the first ? or #, and those chars are not
      //
      // allowed here.
      repeated string paths = 1;

      // The backend service or backend bucket to use if any of the given paths match.
      optional string service = 2;

      CustomErrorResponsePolicy custom_error_response_policy = 3 [json_name = "custom_error_response_policy"];

      RouteAction route_action = 4 [json_name = "route_action"];

      UrlRedirect url_redirect = 5 [json_name = "url_redirect"];

      message CustomErrorResponsePolicy {
        // The full or partial URL to the BackendBucket resource that contains the custom
        // error content. Examples are:
        //
        //
        // .googleapis.com/compute/v1/projects/project/global/backendBuckets/myBackendBucket
        //
        // compute/v1/projects/project/global/backendBuckets/myBackendBucket
        //
        // global/backendBuckets/myBackendBucket
        //
        //
        // If errorService is not specified at lower levels like pathMatcher, pathRule and
        // routeRule, an errorService specified at a higher level in the UrlMap will be
        // used. If UrlMap.defaultCustomErrorResponsePolicy contains one or more
        // errorResponseRules[], it must specify errorService.
        //
        // If load balancer cannot reach the backendBucket, a simple Not Found Error will be
        // returned, with the original response code (or overrideResponseCode if
        // configured).
        optional string error_service = 1 [json_name = "error_service"];

        repeated ErrorResponseRule error_response_rule = 2 [json_name = "error_response_rule"];

        message ErrorResponseRule {
          // Valid values include:
          //
          //
          // - A number between 400 and 599: For example 401 or 503, in which case the load
          // balancer applies the policy if the error code exactly matches this value.
          //
          // - 5xx: Load Balancer will apply the policy if the backend service responds with
          // any response code in the range of 500 to 599.
          //
          // - 4xx: Load Balancer will apply the policy if the backend service responds with
          // any response code in the range of 400 to 499.
          //
          //
          // Values must be unique within matchResponseCodes and across all errorResponseRules
          // of CustomErrorResponsePolicy.
          repeated string match_response_codes = 1 [json_name = "match_response_codes"];

          // The HTTP status code returned with the response containing the custom error
          // content.
          //
          // If overrideResponseCode is not supplied, the same response code returned by the
          // original backend bucket or backend service is returned to the client.
          optional int64 override_response_code = 2 [json_name = "override_response_code"];

          // The full path to a file within backendBucket . For example:
          // /errors/defaultError.html
          //
          // path must start with a leading slash. path cannot have trailing slashes.
          //
          // If the file is not available in backendBucket or the load balancer cannot reach
          // the BackendBucket, a simple Not Found Error is returned to the client.
          //
          // The value must be from 1 to 1024 characters
          optional string path = 3;
        }
      }

      message RouteAction {
        CachePolicy cache_policy = 1 [json_name = "cache_policy"];

        CorsPolicy cors_policy = 2 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 3 [json_name = "fault_injection_policy"];

        MaxStreamDuration max_stream_duration = 4 [json_name = "max_stream_duration"];

        RequestMirrorPolicy request_mirror_policy = 5 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 6 [json_name = "retry_policy"];

        Timeout timeout = 7;

        UrlRewrite url_rewrite = 8 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 9 [json_name = "weighted_backend_services"];

        message CachePolicy {
          // Bypass the cache when the specified request headers are matched by name,
          //
          // e.g. Pragma or Authorization headers. Values are case-insensitive. Up to 5
          //
          // header names can be specified. The cache is bypassed for all cacheMode
          //
          // values.
          repeated string cache_bypass_request_header_names = 1 [json_name = "cache_bypass_request_header_names"];

          // Specifies the cache setting for all responses from this route. If not
          //
          // specified, Cloud CDN uses CACHE_ALL_STATIC mode. Possible values:
          // ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
          optional string cache_mode = 2 [json_name = "cache_mode"];

          // Negative caching allows per-status code TTLs to be set, in order to apply
          //
          // fine-grained caching for common errors or redirects. This can reduce the
          //
          // load on your origin and improve end-user experience by reducing response
          //
          // latency. When the cacheMode is set to CACHE_ALL_STATIC or
          //
          // USE_ORIGIN_HEADERS, negative caching applies to responses with the
          //
          // specified response code that lack any Cache-Control, Expires, or
          //
          // Pragma: no-cache directives. When the cacheMode is set to
          //
          // FORCE_CACHE_ALL, negative caching applies to all responses with the
          //
          // specified response code, and overrides any caching headers. By default,
          //
          // Cloud CDN applies the following TTLs to these HTTP status codes:
          //
          // * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m
          //
          // * 404 (Not Found), 410 (Gone), 451 (Unavailable For Legal Reasons): 120s
          //
          // * 405 (Method Not Found), 501 (Not Implemented): 60s
          //
          // These defaults can be overridden in negativeCachingPolicy. If not
          //
          // specified, Cloud CDN applies negative caching by default.
          optional bool negative_caching = 3 [json_name = "negative_caching"];

          // If true then Cloud CDN will combine multiple concurrent cache fill
          //
          // requests into a small number of requests to the origin. If not specified,
          //
          // Cloud CDN applies request coalescing by default.
          optional bool request_coalescing = 4 [json_name = "request_coalescing"];

          CacheKeyPolicy cache_key_policy = 5 [json_name = "cache_key_policy"];

          ClientTtl client_ttl = 6 [json_name = "client_ttl"];

          DefaultTtl default_ttl = 7 [json_name = "default_ttl"];

          MaxTtl max_ttl = 8 [json_name = "max_ttl"];

          repeated NegativeCachingPolicy negative_caching_policy = 9 [json_name = "negative_caching_policy"];

          ServeWhileStale serve_while_stale = 10 [json_name = "serve_while_stale"];

          message CacheKeyPolicy {
            // Names of query string parameters to exclude in cache keys. All other
            //
            // parameters will be included. Either specify excludedQueryParameters
            //
            // or includedQueryParameters, not both. '&' and '=' will be percent
            //
            // encoded and not treated as delimiters. Note: This field applies to
            //
            // routes that use backend services. Attempting to set it on a route that
            //
            // points exclusively to Backend Buckets will result in a configuration
            //
            // error. For routes that point to a Backend Bucket, use
            //
            // includedQueryParameters to define which parameters should be part of
            //
            // the cache key.
            repeated string excluded_query_parameters = 1 [json_name = "excluded_query_parameters"];

            // If true, requests to different hosts will be cached separately. Note:
            //
            // This setting is only applicable to routes that use a Backend Service.
            //
            // It does not affect requests served by a Backend Bucket, as the host is
            //
            // never included in a Backend Bucket's cache key. Attempting to set it on
            //
            // a route that points exclusively to Backend Buckets will result in a
            //
            // configuration error.
            optional bool include_host = 2 [json_name = "include_host"];

            // If true, http and https requests will be cached separately. Note: This
            //
            // setting is only applicable to routes that use a Backend Service. It
            //
            // does not affect requests served by a Backend Bucket, as the protocol is
            //
            // never included in a Backend Bucket's cache key. Attempting to set on a
            //
            // route that points exclusively to Backend Buckets will result in a
            //
            // configuration error.
            optional bool include_protocol = 3 [json_name = "include_protocol"];

            // If true, include query string parameters in the cache key according to
            //
            // includedQueryParameters and excludedQueryParameters. If neither is
            //
            // set, the entire query string will be included. If false, the query
            //
            // string will be excluded from the cache key entirely. Note: This field
            //
            // applies to routes that use backend services. Attempting to set it on a
            //
            // route that points exclusively to Backend Buckets will result in a
            //
            // configuration error. For routes that point to a Backend Bucket, use
            //
            // includedQueryParameters to define which parameters should be part of
            //
            // the cache key.
            optional bool include_query_string = 4 [json_name = "include_query_string"];

            // Allows HTTP cookies (by name) to be used in the cache key. The
            //
            // name=value pair will be used in the cache key Cloud CDN generates.
            //
            // Note: This setting is only applicable to routes that use a Backend
            //
            // Service. It does not affect requests served by a Backend Bucket.
            //
            // Attempting to set it on a route that points exclusively to Backend
            //
            // Buckets will result in a configuration error. Up to 5 cookie names can
            //
            // be specified.
            repeated string included_cookie_names = 5 [json_name = "included_cookie_names"];

            // Allows HTTP request headers (by name) to be used in the cache key.
            repeated string included_header_names = 6 [json_name = "included_header_names"];

            // Names of query string parameters to include in cache keys. All other
            //
            // parameters will be excluded. Either specify includedQueryParameters
            //
            // or excludedQueryParameters, not both. '&' and '=' will be percent
            //
            // encoded and not treated as delimiters.
            repeated string included_query_parameters = 7 [json_name = "included_query_parameters"];
          }

          message ClientTtl {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }

          message DefaultTtl {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }

          message MaxTtl {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }

          message NegativeCachingPolicy {
            // The HTTP status code to define a TTL against. Only HTTP status codes
            //
            // 300, 301, 302, 307, 308, 404, 405, 410, 421, 451 and 501 can be
            //
            // specified as values, and you cannot specify a status code more than
            //
            // once.
            optional int64 code = 1;

            Ttl ttl = 2;

            message Ttl {
              // Span of time that's a fraction of a second at nanosecond resolution.
              optional int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              // inclusive.
              optional string seconds = 2;
            }
          }

          message ServeWhileStale {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }
        }

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          //
          // actual request can include user credentials. This translates to the Access-
          //
          // Control-Allow-Credentials header. Defaults to false.
          optional bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          //
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          //
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          //
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          optional bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          //
          // translates to the content for the Access-Control-Max-Age header.
          optional int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            //
            // and 599 inclusive.
            optional int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            //
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            //
            // inclusive.
            optional int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            //
            // be introduced as part of fault injection. The value must be between 0.0 and
            //
            // 100.0 inclusive.
            optional int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              //
              // less than one second are represented with a 0 'seconds' field and a positive
              //
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              optional int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              //
              // inclusive.
              optional string seconds = 2;
            }
          }
        }

        message MaxStreamDuration {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          //
          // less than one second are represented with a 0 'seconds' field and a positive
          //
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          //
          // inclusive.
          optional string seconds = 2;
        }

        message RequestMirrorPolicy {
          // The BackendService resource being mirrored to.
          optional string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          optional int64 num_retries = 1 [json_name = "num_retries"];

          // Specifies one or more conditions when this retry rule applies. Valid values are:
          //
          //
          // * 5xx: Loadbalancer will attempt a retry if the backend service responds with
          //
          // any 5xx response code, or if the backend service does not respond at all,
          //
          // for example: disconnects, reset, read timeout, connection failure, and refused
          //
          // streams.
          //
          // * gateway-error: Similar to 5xx, but only applies to response codes
          //
          // 502, 503 or 504.
          //
          // * connect-failure: Loadbalancer will retry on failures
          //
          // connecting to backend services, for example due to connection timeouts.
          //
          // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          //
          // Currently the only retriable error supported is 409.
          //
          // * refused-stream: Loadbalancer will retry if the backend service resets the
          // stream with a
          //
          // REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          //
          // * cancelled: Loadbalancer will retry if the gRPC status code in the response
          //
          // header is set to cancelled
          //
          // * deadline-exceeded: Loadbalancer will retry if the
          //
          // gRPC status code in the response header is set to deadline-exceeded
          //
          // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the
          // response
          //
          // header is set to resource-exhausted
          //
          // * unavailable: Loadbalancer will retry if
          //
          // the gRPC status code in the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            //
            // less than one second are represented with a 0 'seconds' field and a positive
            //
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            //
            // inclusive.
            optional string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          //
          // less than one second are represented with a 0 'seconds' field and a positive
          //
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          //
          // inclusive.
          optional string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          //
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          //
          // 255 characters.
          optional string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          //
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          //
          // be between 1 and 1024 characters.
          optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];
        }

        message WeightedBackendServices {
          // The default BackendService resource. Before
          //
          // forwarding the request to backendService, the loadbalancer applies any relevant
          //
          // headerActions specified as part of this backendServiceWeight.
          optional string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          //
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          //
          // backend service is determined only for new traffic. Once a user's request has
          //
          // been directed to a backendService, subsequent requests will be sent to the same
          //
          // backendService as determined by the BackendService's session affinity policy.
          //
          // The value must be between 0 and 1000
          optional int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            //
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            //
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one
        //
        // that was supplied in the request. The value must be between 1 and 255
        //
        // characters.
        optional string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https.
        //
        // If set to false, the URL scheme of the redirected request will remain the
        //
        // same as that of the request. This must only be set for UrlMaps used in
        //
        // TargetHttpProxys. Setting this true for TargetHttpsProxy is not
        //
        // permitted. The default is set to false.
        optional bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one
        //
        // that was supplied in the request. pathRedirect cannot be supplied
        //
        // together with prefixRedirect. Supply one alone or neither. If neither is
        //
        // supplied, the path of the original request will be used for the redirect.
        //
        // The value must be between 1 and 1024 characters.
        optional string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the
        //
        // HttpRouteRuleMatch, retaining the remaining portion of the URL before
        //
        // redirecting the request. prefixRedirect cannot be supplied together with
        //
        // pathRedirect. Supply one alone or neither. If neither is supplied, the
        //
        // path of the original request will be used for the redirect. The value
        //
        // must be between 1 and 1024 characters.
        optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        //
        // * FOUND, which corresponds to 302.
        //
        //
        // * SEE_OTHER which corresponds to 303.
        //
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
        //
        // will be retained.
        //
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case,
        //
        // the request method will be retained. Possible values: ["FOUND",
        // "MOVED_PERMANENTLY_DEFAULT", "PERMANENT_REDIRECT", "SEE_OTHER",
        // "TEMPORARY_REDIRECT"]
        optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is
        //
        // removed prior to redirecting the request. If set to false, the query
        //
        // portion of the original URL is retained.
        //
        // This field is required to ensure an empty block is not set. The normal default
        // value is false.
        optional bool strip_query = 6 [json_name = "strip_query"];
      }
    }

    message RouteRules {
      // For routeRules within a given pathMatcher, priority determines the order
      //
      // in which load balancer will interpret routeRules. RouteRules are evaluated
      //
      // in order of priority, from the lowest to highest number. The priority of
      //
      // a rule decreases as its number increases (1, 2, 3, N+1). The first rule
      //
      // that matches the request is applied.
      //
      //
      // You cannot configure two or more routeRules with the same priority.
      //
      // Priority for each rule must be set to a number between 0 and
      //
      // 2147483647 inclusive.
      //
      //
      // Priority numbers can have gaps, which enable you to add or remove rules
      //
      // in the future without affecting the rest of the rules. For example,
      //
      // 1, 2, 3, 4, 5, 9, 12, 16 is a valid series of priority numbers to which
      //
      // you could add rules numbered from 6 to 8, 10 to 11, and 13 to 15 in the
      //
      // future without any impact on existing rules.
      optional int64 priority = 1;

      // The backend service resource to which traffic is
      //
      // directed if this rule is matched. If routeAction is additionally specified,
      //
      // advanced routing actions like URL Rewrites, etc. take effect prior to sending
      //
      // the request to the backend. However, if service is specified, routeAction cannot
      //
      // contain any weightedBackendService s. Conversely, if routeAction specifies any
      //
      // weightedBackendServices, service must not be specified. Only one of urlRedirect,
      //
      // service or routeAction.weightedBackendService must be set.
      optional string service = 2;

      CustomErrorResponsePolicy custom_error_response_policy = 3 [json_name = "custom_error_response_policy"];

      HeaderAction header_action = 4 [json_name = "header_action"];

      repeated MatchRules match_rules = 5 [json_name = "match_rules"];

      RouteAction route_action = 6 [json_name = "route_action"];

      UrlRedirect url_redirect = 7 [json_name = "url_redirect"];

      message CustomErrorResponsePolicy {
        // The full or partial URL to the BackendBucket resource that contains the custom
        // error content. Examples are:
        //
        //
        // .googleapis.com/compute/v1/projects/project/global/backendBuckets/myBackendBucket
        //
        // compute/v1/projects/project/global/backendBuckets/myBackendBucket
        //
        // global/backendBuckets/myBackendBucket
        //
        //
        // If errorService is not specified at lower levels like pathMatcher, pathRule and
        // routeRule, an errorService specified at a higher level in the UrlMap will be
        // used. If UrlMap.defaultCustomErrorResponsePolicy contains one or more
        // errorResponseRules[], it must specify errorService.
        //
        // If load balancer cannot reach the backendBucket, a simple Not Found Error will be
        // returned, with the original response code (or overrideResponseCode if
        // configured).
        optional string error_service = 1 [json_name = "error_service"];

        repeated ErrorResponseRule error_response_rule = 2 [json_name = "error_response_rule"];

        message ErrorResponseRule {
          // Valid values include:
          //
          //
          // - A number between 400 and 599: For example 401 or 503, in which case the load
          // balancer applies the policy if the error code exactly matches this value.
          //
          // - 5xx: Load Balancer will apply the policy if the backend service responds with
          // any response code in the range of 500 to 599.
          //
          // - 4xx: Load Balancer will apply the policy if the backend service responds with
          // any response code in the range of 400 to 499.
          //
          //
          // Values must be unique within matchResponseCodes and across all errorResponseRules
          // of CustomErrorResponsePolicy.
          repeated string match_response_codes = 1 [json_name = "match_response_codes"];

          // The HTTP status code returned with the response containing the custom error
          // content.
          //
          // If overrideResponseCode is not supplied, the same response code returned by the
          // original backend bucket or backend service is returned to the client.
          optional int64 override_response_code = 2 [json_name = "override_response_code"];

          // The full path to a file within backendBucket . For example:
          // /errors/defaultError.html
          //
          // path must start with a leading slash. path cannot have trailing slashes.
          //
          // If the file is not available in backendBucket or the load balancer cannot reach
          // the BackendBucket, a simple Not Found Error is returned to the client.
          //
          // The value must be from 1 to 1024 characters
          optional string path = 3;
        }
      }

      message HeaderAction {
        // A list of header names for headers that need to be removed from the request
        //
        // prior to forwarding the request to the backendService.
        repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

        // A list of header names for headers that need to be removed from the response
        //
        // prior to sending the response back to the client.
        repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

        repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

        repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

        message RequestHeadersToAdd {
          // The name of the header.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          //
          // header. If true, headerValue is set for the header, discarding any values that
          //
          // were set for that header.
          optional bool replace = 3;
        }

        message ResponseHeadersToAdd {
          // The name of the header.
          optional string header_name = 1 [json_name = "header_name"];

          // The value of the header to add.
          optional string header_value = 2 [json_name = "header_value"];

          // If false, headerValue is appended to any values that already exist for the
          //
          // header. If true, headerValue is set for the header, discarding any values that
          //
          // were set for that header.
          optional bool replace = 3;
        }
      }

      message MatchRules {
        // For satisfying the matchRule condition, the path of the request must exactly
        //
        // match the value specified in fullPathMatch after removing any query parameters
        //
        // and anchor that may be part of the original URL. FullPathMatch must be between 1
        //
        // and 1024 characters. Only one of prefixMatch, fullPathMatch or regexMatch must
        //
        // be specified.
        optional string full_path_match = 1 [json_name = "full_path_match"];

        // Specifies that prefixMatch and fullPathMatch matches are case sensitive.
        //
        // Defaults to false.
        optional bool ignore_case = 2 [json_name = "ignore_case"];

        // For satisfying the matchRule condition, the path of the request
        //
        // must match the wildcard pattern specified in pathTemplateMatch
        //
        // after removing any query parameters and anchor that may be part
        //
        // of the original URL.
        //
        //
        // pathTemplateMatch must be between 1 and 255 characters
        //
        // (inclusive).  The pattern specified by pathTemplateMatch may
        //
        // have at most 5 wildcard operators and at most 5 variable
        //
        // captures in total.
        optional string path_template_match = 3 [json_name = "path_template_match"];

        // For satisfying the matchRule condition, the request's path must begin with the
        //
        // specified prefixMatch. prefixMatch must begin with a /. The value must be
        //
        // between 1 and 1024 characters. Only one of prefixMatch, fullPathMatch or
        //
        // regexMatch must be specified.
        optional string prefix_match = 4 [json_name = "prefix_match"];

        // For satisfying the matchRule condition, the path of the request must satisfy the
        //
        // regular expression specified in regexMatch after removing any query parameters
        //
        // and anchor supplied with the original URL. For regular expression grammar please
        //
        // see en.cppreference.com/w/cpp/regex/ecmascript  Only one of prefixMatch,
        //
        // fullPathMatch or regexMatch must be specified.
        optional string regex_match = 5 [json_name = "regex_match"];

        repeated HeaderMatches header_matches = 6 [json_name = "header_matches"];

        repeated MetadataFilters metadata_filters = 7 [json_name = "metadata_filters"];

        repeated QueryParameterMatches query_parameter_matches = 8 [json_name = "query_parameter_matches"];

        message HeaderMatches {
          // The value should exactly match contents of exactMatch. Only one of exactMatch,
          //
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          optional string exact_match = 1 [json_name = "exact_match"];

          // The name of the HTTP header to match. For matching against the HTTP request's
          //
          // authority, use a headerMatch with the header name ":authority". For matching a
          //
          // request's method, use the headerName ":method".
          optional string header_name = 2 [json_name = "header_name"];

          // If set to false, the headerMatch is considered a match if the match criteria
          //
          // above are met. If set to true, the headerMatch is considered a match if the
          //
          // match criteria above are NOT met. Defaults to false.
          optional bool invert_match = 3 [json_name = "invert_match"];

          // The value of the header must start with the contents of prefixMatch. Only one of
          //
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          //
          // must be set.
          optional string prefix_match = 4 [json_name = "prefix_match"];

          // A header with the contents of headerName must exist. The match takes place
          //
          // whether or not the request's header has a value or not. Only one of exactMatch,
          //
          // prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch must be set.
          optional bool present_match = 5 [json_name = "present_match"];

          // The value of the header must match the regular expression specified in
          //
          // regexMatch. For regular expression grammar, please see:
          //
          // en.cppreference.com/w/cpp/regex/ecmascript  For matching against a port
          //
          // specified in the HTTP request, use a headerMatch with headerName set to PORT and
          //
          // a regular expression that satisfies the RFC2616 Host header's port specifier.
          //
          // Only one of exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or
          //
          // rangeMatch must be set.
          optional string regex_match = 6 [json_name = "regex_match"];

          // The value of the header must end with the contents of suffixMatch. Only one of
          //
          // exactMatch, prefixMatch, suffixMatch, regexMatch, presentMatch or rangeMatch
          //
          // must be set.
          optional string suffix_match = 7 [json_name = "suffix_match"];

          RangeMatch range_match = 8 [json_name = "range_match"];

          message RangeMatch {
            // The end of the range (exclusive).
            optional int64 range_end = 1 [json_name = "range_end"];

            // The start of the range (inclusive).
            optional int64 range_start = 2 [json_name = "range_start"];
          }
        }

        message MetadataFilters {
          // Specifies how individual filterLabel matches within the list of filterLabels
          //
          // contribute towards the overall metadataFilter match. Supported values are:
          //
          // - MATCH_ANY: At least one of the filterLabels must have a matching label in the
          //
          // provided metadata.
          //
          // - MATCH_ALL: All filterLabels must have matching labels in
          //
          // the provided metadata. Possible values: ["MATCH_ALL", "MATCH_ANY"]
          optional string filter_match_criteria = 1 [json_name = "filter_match_criteria"];

          repeated FilterLabels filter_labels = 2 [json_name = "filter_labels"];

          message FilterLabels {
            // Name of metadata label. The name can have a maximum length of 1024 characters
            //
            // and must be at least 1 character long.
            optional string name = 1;

            // The value of the label must match the specified value. value can have a maximum
            //
            // length of 1024 characters.
            optional string value = 2;
          }
        }

        message QueryParameterMatches {
          // The queryParameterMatch matches if the value of the parameter exactly matches
          //
          // the contents of exactMatch. Only one of presentMatch, exactMatch and regexMatch
          //
          // must be set.
          optional string exact_match = 1 [json_name = "exact_match"];

          // The name of the query parameter to match. The query parameter must exist in the
          //
          // request, in the absence of which the request match fails.
          optional string name = 2;

          // Specifies that the queryParameterMatch matches if the request contains the query
          //
          // parameter, irrespective of whether the parameter has a value or not. Only one of
          //
          // presentMatch, exactMatch and regexMatch must be set.
          optional bool present_match = 3 [json_name = "present_match"];

          // The queryParameterMatch matches if the value of the parameter matches the
          //
          // regular expression specified by regexMatch. For the regular expression grammar,
          //
          // please see en.cppreference.com/w/cpp/regex/ecmascript  Only one of presentMatch,
          //
          // exactMatch and regexMatch must be set.
          optional string regex_match = 4 [json_name = "regex_match"];
        }
      }

      message RouteAction {
        CachePolicy cache_policy = 1 [json_name = "cache_policy"];

        CorsPolicy cors_policy = 2 [json_name = "cors_policy"];

        FaultInjectionPolicy fault_injection_policy = 3 [json_name = "fault_injection_policy"];

        MaxStreamDuration max_stream_duration = 4 [json_name = "max_stream_duration"];

        RequestMirrorPolicy request_mirror_policy = 5 [json_name = "request_mirror_policy"];

        RetryPolicy retry_policy = 6 [json_name = "retry_policy"];

        Timeout timeout = 7;

        UrlRewrite url_rewrite = 8 [json_name = "url_rewrite"];

        repeated WeightedBackendServices weighted_backend_services = 9 [json_name = "weighted_backend_services"];

        message CachePolicy {
          // Bypass the cache when the specified request headers are matched by name,
          //
          // e.g. Pragma or Authorization headers. Values are case-insensitive. Up to 5
          //
          // header names can be specified. The cache is bypassed for all cacheMode
          //
          // values.
          repeated string cache_bypass_request_header_names = 1 [json_name = "cache_bypass_request_header_names"];

          // Specifies the cache setting for all responses from this route. If not
          //
          // specified, Cloud CDN uses CACHE_ALL_STATIC mode. Possible values:
          // ["USE_ORIGIN_HEADERS", "FORCE_CACHE_ALL", "CACHE_ALL_STATIC"]
          optional string cache_mode = 2 [json_name = "cache_mode"];

          // Negative caching allows per-status code TTLs to be set, in order to apply
          //
          // fine-grained caching for common errors or redirects. This can reduce the
          //
          // load on your origin and improve end-user experience by reducing response
          //
          // latency. When the cacheMode is set to CACHE_ALL_STATIC or
          //
          // USE_ORIGIN_HEADERS, negative caching applies to responses with the
          //
          // specified response code that lack any Cache-Control, Expires, or
          //
          // Pragma: no-cache directives. When the cacheMode is set to
          //
          // FORCE_CACHE_ALL, negative caching applies to all responses with the
          //
          // specified response code, and overrides any caching headers. By default,
          //
          // Cloud CDN applies the following TTLs to these HTTP status codes:
          //
          // * 300 (Multiple Choice), 301, 308 (Permanent Redirects): 10m
          //
          // * 404 (Not Found), 410 (Gone), 451 (Unavailable For Legal Reasons): 120s
          //
          // * 405 (Method Not Found), 501 (Not Implemented): 60s
          //
          // These defaults can be overridden in negativeCachingPolicy. If not
          //
          // specified, Cloud CDN applies negative caching by default.
          optional bool negative_caching = 3 [json_name = "negative_caching"];

          // If true then Cloud CDN will combine multiple concurrent cache fill
          //
          // requests into a small number of requests to the origin. If not specified,
          //
          // Cloud CDN applies request coalescing by default.
          optional bool request_coalescing = 4 [json_name = "request_coalescing"];

          CacheKeyPolicy cache_key_policy = 5 [json_name = "cache_key_policy"];

          ClientTtl client_ttl = 6 [json_name = "client_ttl"];

          DefaultTtl default_ttl = 7 [json_name = "default_ttl"];

          MaxTtl max_ttl = 8 [json_name = "max_ttl"];

          repeated NegativeCachingPolicy negative_caching_policy = 9 [json_name = "negative_caching_policy"];

          ServeWhileStale serve_while_stale = 10 [json_name = "serve_while_stale"];

          message CacheKeyPolicy {
            // Names of query string parameters to exclude in cache keys. All other
            //
            // parameters will be included. Either specify excludedQueryParameters
            //
            // or includedQueryParameters, not both. '&' and '=' will be percent
            //
            // encoded and not treated as delimiters. Note: This field applies to
            //
            // routes that use backend services. Attempting to set it on a route that
            //
            // points exclusively to Backend Buckets will result in a configuration
            //
            // error. For routes that point to a Backend Bucket, use
            //
            // includedQueryParameters to define which parameters should be part of
            //
            // the cache key.
            repeated string excluded_query_parameters = 1 [json_name = "excluded_query_parameters"];

            // If true, requests to different hosts will be cached separately. Note:
            //
            // This setting is only applicable to routes that use a Backend Service.
            //
            // It does not affect requests served by a Backend Bucket, as the host is
            //
            // never included in a Backend Bucket's cache key. Attempting to set it on
            //
            // a route that points exclusively to Backend Buckets will result in a
            //
            // configuration error.
            optional bool include_host = 2 [json_name = "include_host"];

            // If true, http and https requests will be cached separately. Note: This
            //
            // setting is only applicable to routes that use a Backend Service. It
            //
            // does not affect requests served by a Backend Bucket, as the protocol is
            //
            // never included in a Backend Bucket's cache key. Attempting to set on a
            //
            // route that points exclusively to Backend Buckets will result in a
            //
            // configuration error.
            optional bool include_protocol = 3 [json_name = "include_protocol"];

            // If true, include query string parameters in the cache key according to
            //
            // includedQueryParameters and excludedQueryParameters. If neither is
            //
            // set, the entire query string will be included. If false, the query
            //
            // string will be excluded from the cache key entirely. Note: This field
            //
            // applies to routes that use backend services. Attempting to set it on a
            //
            // route that points exclusively to Backend Buckets will result in a
            //
            // configuration error. For routes that point to a Backend Bucket, use
            //
            // includedQueryParameters to define which parameters should be part of
            //
            // the cache key.
            optional bool include_query_string = 4 [json_name = "include_query_string"];

            // Allows HTTP cookies (by name) to be used in the cache key. The
            //
            // name=value pair will be used in the cache key Cloud CDN generates.
            //
            // Note: This setting is only applicable to routes that use a Backend
            //
            // Service. It does not affect requests served by a Backend Bucket.
            //
            // Attempting to set it on a route that points exclusively to Backend
            //
            // Buckets will result in a configuration error. Up to 5 cookie names can
            //
            // be specified.
            repeated string included_cookie_names = 5 [json_name = "included_cookie_names"];

            // Allows HTTP request headers (by name) to be used in the cache key.
            repeated string included_header_names = 6 [json_name = "included_header_names"];

            // Names of query string parameters to include in cache keys. All other
            //
            // parameters will be excluded. Either specify includedQueryParameters
            //
            // or excludedQueryParameters, not both. '&' and '=' will be percent
            //
            // encoded and not treated as delimiters.
            repeated string included_query_parameters = 7 [json_name = "included_query_parameters"];
          }

          message ClientTtl {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }

          message DefaultTtl {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }

          message MaxTtl {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }

          message NegativeCachingPolicy {
            // The HTTP status code to define a TTL against. Only HTTP status codes
            //
            // 300, 301, 302, 307, 308, 404, 405, 410, 421, 451 and 501 can be
            //
            // specified as values, and you cannot specify a status code more than
            //
            // once.
            optional int64 code = 1;

            Ttl ttl = 2;

            message Ttl {
              // Span of time that's a fraction of a second at nanosecond resolution.
              optional int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              // inclusive.
              optional string seconds = 2;
            }
          }

          message ServeWhileStale {
            // Span of time that's a fraction of a second at nanosecond resolution.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            // inclusive.
            optional string seconds = 2;
          }
        }

        message CorsPolicy {
          // In response to a preflight request, setting this to true indicates that the
          //
          // actual request can include user credentials. This translates to the Access-
          //
          // Control-Allow-Credentials header. Defaults to false.
          optional bool allow_credentials = 1 [json_name = "allow_credentials"];

          // Specifies the content for the Access-Control-Allow-Headers header.
          repeated string allow_headers = 2 [json_name = "allow_headers"];

          // Specifies the content for the Access-Control-Allow-Methods header.
          repeated string allow_methods = 3 [json_name = "allow_methods"];

          // Specifies the regular expression patterns that match allowed origins. For
          //
          // regular expression grammar please see en.cppreference.com/w/cpp/regex/ecmascript
          //
          // An origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origin_regexes = 4 [json_name = "allow_origin_regexes"];

          // Specifies the list of origins that will be allowed to do CORS requests. An
          //
          // origin is allowed if it matches either allow_origins or allow_origin_regex.
          repeated string allow_origins = 5 [json_name = "allow_origins"];

          // If true, specifies the CORS policy is disabled.
          //
          // which indicates that the CORS policy is in effect. Defaults to false.
          optional bool disabled = 6;

          // Specifies the content for the Access-Control-Expose-Headers header.
          repeated string expose_headers = 7 [json_name = "expose_headers"];

          // Specifies how long the results of a preflight request can be cached. This
          //
          // translates to the content for the Access-Control-Max-Age header.
          optional int64 max_age = 8 [json_name = "max_age"];
        }

        message FaultInjectionPolicy {
          Abort abort = 1;

          Delay delay = 2;

          message Abort {
            // The HTTP status code used to abort the request. The value must be between 200
            //
            // and 599 inclusive.
            optional int64 http_status = 1 [json_name = "http_status"];

            // The percentage of traffic (connections/operations/requests) which will be
            //
            // aborted as part of fault injection. The value must be between 0.0 and 100.0
            //
            // inclusive.
            optional int64 percentage = 2;
          }

          message Delay {
            // The percentage of traffic (connections/operations/requests) on which delay will
            //
            // be introduced as part of fault injection. The value must be between 0.0 and
            //
            // 100.0 inclusive.
            optional int64 percentage = 1;

            FixedDelay fixed_delay = 2 [json_name = "fixed_delay"];

            message FixedDelay {
              // Span of time that's a fraction of a second at nanosecond resolution. Durations
              //
              // less than one second are represented with a 0 'seconds' field and a positive
              //
              // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
              optional int64 nanos = 1;

              // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
              //
              // inclusive.
              optional string seconds = 2;
            }
          }
        }

        message MaxStreamDuration {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          //
          // less than one second are represented with a 0 'seconds' field and a positive
          //
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          //
          // inclusive.
          optional string seconds = 2;
        }

        message RequestMirrorPolicy {
          // The BackendService resource being mirrored to.
          optional string backend_service = 1 [json_name = "backend_service"];
        }

        message RetryPolicy {
          // Specifies the allowed number retries. This number must be > 0.
          optional int64 num_retries = 1 [json_name = "num_retries"];

          // Specfies one or more conditions when this retry rule applies. Valid values are:
          //
          //
          // * 5xx: Loadbalancer will attempt a retry if the backend service responds with
          //
          // any 5xx response code, or if the backend service does not respond at all,
          //
          // for example: disconnects, reset, read timeout, connection failure, and refused
          //
          // streams.
          //
          // * gateway-error: Similar to 5xx, but only applies to response codes
          //
          // 502, 503 or 504.
          //
          // * connect-failure: Loadbalancer will retry on failures
          //
          // connecting to backend services, for example due to connection timeouts.
          //
          // * retriable-4xx: Loadbalancer will retry for retriable 4xx response codes.
          //
          // Currently the only retriable error supported is 409.
          //
          // * refused-stream: Loadbalancer will retry if the backend service resets the
          // stream with a
          //
          // REFUSED_STREAM error code. This reset type indicates that it is safe to retry.
          //
          // * cancelled: Loadbalancer will retry if the gRPC status code in the response
          //
          // header is set to cancelled
          //
          // * deadline-exceeded: Loadbalancer will retry if the
          //
          // gRPC status code in the response header is set to deadline-exceeded
          //
          // * resource-exhausted: Loadbalancer will retry if the gRPC status code in the
          // response
          //
          // header is set to resource-exhausted
          //
          // * unavailable: Loadbalancer will retry if the gRPC status code in
          //
          // the response header is set to unavailable
          repeated string retry_conditions = 2 [json_name = "retry_conditions"];

          PerTryTimeout per_try_timeout = 3 [json_name = "per_try_timeout"];

          message PerTryTimeout {
            // Span of time that's a fraction of a second at nanosecond resolution. Durations
            //
            // less than one second are represented with a 0 'seconds' field and a positive
            //
            // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
            optional int64 nanos = 1;

            // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
            //
            // inclusive.
            optional string seconds = 2;
          }
        }

        message Timeout {
          // Span of time that's a fraction of a second at nanosecond resolution. Durations
          //
          // less than one second are represented with a 0 'seconds' field and a positive
          //
          // 'nanos' field. Must be from 0 to 999,999,999 inclusive.
          optional int64 nanos = 1;

          // Span of time at a resolution of a second. Must be from 0 to 315,576,000,000
          //
          // inclusive.
          optional string seconds = 2;
        }

        message UrlRewrite {
          // Prior to forwarding the request to the selected service, the request's host
          //
          // header is replaced with contents of hostRewrite. The value must be between 1 and
          //
          // 255 characters.
          optional string host_rewrite = 1 [json_name = "host_rewrite"];

          // Prior to forwarding the request to the selected backend service, the matching
          //
          // portion of the request's path is replaced by pathPrefixRewrite. The value must
          //
          // be between 1 and 1024 characters.
          optional string path_prefix_rewrite = 2 [json_name = "path_prefix_rewrite"];

          // Prior to forwarding the request to the selected origin, if the
          //
          // request matched a pathTemplateMatch, the matching portion of the
          //
          // request's path is replaced re-written using the pattern specified
          //
          // by pathTemplateRewrite.
          //
          //
          // pathTemplateRewrite must be between 1 and 255 characters
          //
          // (inclusive), must start with a '/', and must only use variables
          //
          // captured by the route's pathTemplate matchers.
          //
          //
          // pathTemplateRewrite may only be used when all of a route's
          //
          // MatchRules specify pathTemplate.
          //
          //
          // Only one of pathPrefixRewrite and pathTemplateRewrite may be
          //
          // specified.
          optional string path_template_rewrite = 3 [json_name = "path_template_rewrite"];
        }

        message WeightedBackendServices {
          // The default BackendService resource. Before
          //
          // forwarding the request to backendService, the loadbalancer applies any relevant
          //
          // headerActions specified as part of this backendServiceWeight.
          optional string backend_service = 1 [json_name = "backend_service"];

          // Specifies the fraction of traffic sent to backendService, computed as weight /
          //
          // (sum of all weightedBackendService weights in routeAction) . The selection of a
          //
          // backend service is determined only for new traffic. Once a user's request has
          //
          // been directed to a backendService, subsequent requests will be sent to the same
          //
          // backendService as determined by the BackendService's session affinity policy.
          //
          // The value must be between 0 and 1000
          optional int64 weight = 2;

          HeaderAction header_action = 3 [json_name = "header_action"];

          message HeaderAction {
            // A list of header names for headers that need to be removed from the request
            //
            // prior to forwarding the request to the backendService.
            repeated string request_headers_to_remove = 1 [json_name = "request_headers_to_remove"];

            // A list of header names for headers that need to be removed from the response
            //
            // prior to sending the response back to the client.
            repeated string response_headers_to_remove = 2 [json_name = "response_headers_to_remove"];

            repeated RequestHeadersToAdd request_headers_to_add = 3 [json_name = "request_headers_to_add"];

            repeated ResponseHeadersToAdd response_headers_to_add = 4 [json_name = "response_headers_to_add"];

            message RequestHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }

            message ResponseHeadersToAdd {
              // The name of the header.
              optional string header_name = 1 [json_name = "header_name"];

              // The value of the header to add.
              optional string header_value = 2 [json_name = "header_value"];

              // If false, headerValue is appended to any values that already exist for the
              //
              // header. If true, headerValue is set for the header, discarding any values that
              //
              // were set for that header.
              optional bool replace = 3;
            }
          }
        }
      }

      message UrlRedirect {
        // The host that will be used in the redirect response instead of the one that was
        //
        // supplied in the request. The value must be between 1 and 255 characters.
        optional string host_redirect = 1 [json_name = "host_redirect"];

        // If set to true, the URL scheme in the redirected request is set to https. If set
        //
        // to false, the URL scheme of the redirected request will remain the same as that
        //
        // of the request. This must only be set for UrlMaps used in TargetHttpProxys.
        //
        // Setting this true for TargetHttpsProxy is not permitted. Defaults to false.
        optional bool https_redirect = 2 [json_name = "https_redirect"];

        // The path that will be used in the redirect response instead of the one that was
        //
        // supplied in the request. Only one of pathRedirect or prefixRedirect must be
        //
        // specified. The value must be between 1 and 1024 characters.
        optional string path_redirect = 3 [json_name = "path_redirect"];

        // The prefix that replaces the prefixMatch specified in the HttpRouteRuleMatch,
        //
        // retaining the remaining portion of the URL before redirecting the request.
        optional string prefix_redirect = 4 [json_name = "prefix_redirect"];

        // The HTTP Status code to use for this RedirectAction. Supported values are:
        //
        //
        // * MOVED_PERMANENTLY_DEFAULT, which is the default value and corresponds to 301.
        //
        //
        // * FOUND, which corresponds to 302.
        //
        //
        // * SEE_OTHER which corresponds to 303.
        //
        //
        // * TEMPORARY_REDIRECT, which corresponds to 307. In this case, the request method
        // will be retained.
        //
        //
        // * PERMANENT_REDIRECT, which corresponds to 308. In this case, the request method
        // will be retained. Possible values: ["FOUND", "MOVED_PERMANENTLY_DEFAULT",
        // "PERMANENT_REDIRECT", "SEE_OTHER", "TEMPORARY_REDIRECT"]
        optional string redirect_response_code = 5 [json_name = "redirect_response_code"];

        // If set to true, any accompanying query portion of the original URL is removed
        //
        // prior to redirecting the request. If set to false, the query portion of the
        //
        // original URL is retained. Defaults to false.
        optional bool strip_query = 6 [json_name = "strip_query"];
      }
    }
  }

  message Test {
    // Description of this test case.
    optional string description = 1;

    // The expected output URL evaluated by the load balancer containing the scheme,
    // host, path and query parameters.
    //
    //
    // For rules that forward requests to backends, the test passes only when
    // expectedOutputUrl matches the request forwarded by the load balancer to backends.
    // For rules with urlRewrite, the test verifies that the forwarded request matches
    // hostRewrite and pathPrefixRewrite in the urlRewrite action. When service is
    // specified, expectedOutputUrl's scheme is ignored.
    //
    //
    // For rules with urlRedirect, the test passes only if expectedOutputUrl matches the
    // URL in the load balancer's redirect response. If urlRedirect specifies
    // httpsRedirect, the test passes only if the scheme in expectedOutputUrl is also
    // set to HTTPS. If urlRedirect specifies stripQuery, the test passes only if
    // expectedOutputUrl does not contain any query parameters.
    //
    //
    // expectedOutputUrl is optional when service is specified.
    optional string expected_output_url = 2 [json_name = "expected_output_url"];

    // For rules with urlRedirect, the test passes only if expectedRedirectResponseCode
    // matches the HTTP status code in load balancer's redirect response.
    //
    //
    // expectedRedirectResponseCode cannot be set when service is set.
    optional int64 expected_redirect_response_code = 3 [json_name = "expected_redirect_response_code"];

    // Host portion of the URL.
    optional string host = 4;

    // Path portion of the URL.
    optional string path = 5;

    // The backend service or backend bucket link that should be matched by this test.
    optional string service = 6;

    repeated Headers headers = 7;

    message Headers {
      // Header name.
      optional string name = 1;

      // Header value.
      optional string value = 2;
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeVpnGateway version is 0
message GoogleComputeVpnGateway {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // The unique identifier for the resource.
  optional int64 gateway_id = 4 [json_name = "gateway_id"];

  optional string id = 5;

  // Name of the resource. Provided by the client when the resource is
  //
  // created. The name must be 1-63 characters long, and comply with
  //
  // RFC1035.  Specifically, the name must be 1-63 characters long and
  //
  // match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  //
  // the first character must be a lowercase letter, and all following
  //
  // characters must be a dash, lowercase letter, or digit, except the last
  //
  // character, which cannot be a dash.
  optional string name = 6;

  // The network this VPN gateway is accepting traffic for.
  optional string network = 7;

  optional string project = 8;

  // The region this gateway should sit in.
  optional string region = 9;

  optional string self_link = 10 [json_name = "self_link"];

  Params params = 11;

  repeated Timeouts timeouts = 12;

  google.protobuf.Value for_each = 13 [json_name = "for_each"];

  repeated string depends_on = 14 [json_name = "depends_on"];

  optional int32 count = 15;

  optional string provider = 16;

  terraform.v1.Lifecycle lifecycle = 17;

  message Params {
    // Resource manager tags to be bound to the Vpn Gateway. Tag keys and values have
    // the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;
  }
}

// GoogleComputeVpnTunnel version is 0
message GoogleComputeVpnTunnel {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // Detailed status message for the VPN tunnel.
  optional string detailed_status = 4 [json_name = "detailed_status"];

  // All of labels (key/value pairs) present on the resource in GCP, including the
  // labels configured through Terraform, other clients and services.
  map<string, string> effective_labels = 5 [json_name = "effective_labels"];

  optional string id = 6;

  // IKE protocol version to use when establishing the VPN tunnel with
  //
  // peer VPN gateway.
  //
  // Acceptable IKE versions are 1 or 2. Default version is 2.
  optional int64 ike_version = 7 [json_name = "ike_version"];

  // The fingerprint used for optimistic locking of this resource.  Used
  //
  // internally during updates.
  optional string label_fingerprint = 8 [json_name = "label_fingerprint"];

  // Labels to apply to this VpnTunnel.
  //
  //
  // **Note**: This field is non-authoritative, and will only manage the labels
  // present in your configuration.
  //
  // Please refer to the field 'effective_labels' for all of the labels present on the
  // resource.
  map<string, string> labels = 9;

  // Local traffic selector to use when establishing the VPN tunnel with
  //
  // peer VPN gateway. The value should be a CIDR formatted string,
  //
  // for example '192.168.0.0/16'. The ranges should be disjoint.
  //
  // Only IPv4 is supported.
  repeated string local_traffic_selector = 10 [json_name = "local_traffic_selector"];

  // Name of the resource. The name must be 1-63 characters long, and
  //
  // comply with RFC1035. Specifically, the name must be 1-63
  //
  // characters long and match the regular expression
  //
  // '[a-z]([-a-z0-9]*[a-z0-9])?' which means the first character
  //
  // must be a lowercase letter, and all following characters must
  //
  // be a dash, lowercase letter, or digit,
  //
  // except the last character, which cannot be a dash.
  optional string name = 11;

  // URL of the peer side external VPN gateway to which this VPN tunnel is connected.
  optional string peer_external_gateway = 12 [json_name = "peer_external_gateway"];

  // The interface ID of the external VPN gateway to which this VPN tunnel is
  // connected.
  optional int64 peer_external_gateway_interface = 13 [json_name = "peer_external_gateway_interface"];

  // URL of the peer side HA GCP VPN gateway to which this VPN tunnel is connected.
  //
  // If provided, the VPN tunnel will automatically use the same vpn_gateway_interface
  //
  // ID in the peer GCP VPN gateway.
  //
  // This field must reference a 'google_compute_ha_vpn_gateway' resource.
  optional string peer_gcp_gateway = 14 [json_name = "peer_gcp_gateway"];

  // IP address of the peer VPN gateway. Only IPv4 is supported.
  optional string peer_ip = 15 [json_name = "peer_ip"];

  optional string project = 16;

  // The region where the tunnel is located. If unset, is set to the region of
  // 'target_vpn_gateway'.
  optional string region = 17;

  // Remote traffic selector to use when establishing the VPN tunnel with
  //
  // peer VPN gateway. The value should be a CIDR formatted string,
  //
  // for example '192.168.0.0/16'. The ranges should be disjoint.
  //
  // Only IPv4 is supported.
  repeated string remote_traffic_selector = 18 [json_name = "remote_traffic_selector"];

  // URL of router resource to be used for dynamic routing.
  optional string router = 19;

  optional string self_link = 20 [json_name = "self_link"];

  // Shared secret used to set the secure session between the Cloud VPN
  //
  // gateway and the peer VPN gateway.
  optional string shared_secret = 21 [json_name = "shared_secret"];

  // Hash of the shared secret.
  optional string shared_secret_hash = 22 [json_name = "shared_secret_hash"];

  // Shared secret used to set the secure session between the Cloud VPN
  //
  // gateway and the peer VPN gateway.
  optional string shared_secret_wo = 23 [json_name = "shared_secret_wo"];

  // Triggers update of 'shared_secret_wo' write-only. Increment this value when an
  // update to 'shared_secret_wo' is needed. For more info see [updating write-only
  // ders/google/guides/using_write_only_arguments.html#updating-write-only-arguments)
  optional string shared_secret_wo_version = 24 [json_name = "shared_secret_wo_version"];

  // URL of the Target VPN gateway with which this VPN tunnel is
  //
  // associated.
  optional string target_vpn_gateway = 25 [json_name = "target_vpn_gateway"];

  // The combination of labels configured directly on the resource
  //
  // and default labels configured on the provider.
  map<string, string> terraform_labels = 26 [json_name = "terraform_labels"];

  // The unique identifier for the resource. This identifier is defined by the server.
  optional string tunnel_id = 27 [json_name = "tunnel_id"];

  // URL of the VPN gateway with which this VPN tunnel is associated.
  //
  // This must be used if a High Availability VPN gateway resource is created.
  //
  // This field must reference a 'google_compute_ha_vpn_gateway' resource.
  optional string vpn_gateway = 28 [json_name = "vpn_gateway"];

  // The interface ID of the VPN gateway with which this VPN tunnel is associated.
  optional int64 vpn_gateway_interface = 29 [json_name = "vpn_gateway_interface"];

  CipherSuite cipher_suite = 30 [json_name = "cipher_suite"];

  Params params = 31;

  repeated Timeouts timeouts = 32;

  google.protobuf.Value for_each = 33 [json_name = "for_each"];

  repeated string depends_on = 34 [json_name = "depends_on"];

  optional int32 count = 35;

  optional string provider = 36;

  terraform.v1.Lifecycle lifecycle = 37;

  message CipherSuite {
    Phase1 phase1 = 1;

    Phase2 phase2 = 2;

    message Phase1 {
      // Diffie-Hellman groups.
      repeated string dh = 1;

      // Encryption algorithms.
      repeated string encryption = 2;

      // Integrity algorithms.
      repeated string integrity = 3;

      // Pseudo-random functions.
      repeated string prf = 4;
    }

    message Phase2 {
      // Encryption algorithms.
      repeated string encryption = 1;

      // Integrity algorithms.
      repeated string integrity = 2;

      // Perfect forward secrecy groups.
      repeated string pfs = 3;
    }
  }

  message Params {
    // Resource manager tags to be bound to the Vpn Tunnel. Tag keys and values have the
    //
    // same definition as resource manager tags. Keys must be in the format
    // tagKeys/{tag_key_id},
    //
    // and values are in the format tagValues/456.
    map<string, string> resource_manager_tags = 1 [json_name = "resource_manager_tags"];
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}

// GoogleComputeWireGroup version is 0
message GoogleComputeWireGroup {
  // Indicates whether the wire group is administratively enabled.
  optional bool admin_enabled = 1 [json_name = "admin_enabled"];

  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 2 [json_name = "creation_timestamp"];

  // Required cross site network to which wire group belongs.
  optional string cross_site_network = 3 [json_name = "cross_site_network"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 4 [json_name = "deletion_policy"];

  // An optional description of this resource. Provide this property when you create
  // the resource.
  optional string description = 5;

  optional string id = 6;

  // Name of the resource. Provided by the client when the resource is created. The
  // name must be
  //
  // 1-63 characters long, and comply with RFC1035. Specifically, the name must be
  // 1-63 characters
  //
  // long and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which means
  // the first
  //
  // character must be a lowercase letter, and all following characters must be a
  // dash,
  //
  // lowercase letter, or digit, except the last character, which cannot be a dash.
  optional string name = 7;

  optional string project = 8;

  // Topology details for the wire group configuration.
  repeated Topology topology = 9;

  // The single/redundant wire(s) managed by the wire group.
  repeated Wires wires = 10;

  repeated Endpoints endpoints = 11;

  repeated Timeouts timeouts = 12;

  WireProperties wire_properties = 13 [json_name = "wire_properties"];

  google.protobuf.Value for_each = 14 [json_name = "for_each"];

  repeated string depends_on = 15 [json_name = "depends_on"];

  optional int32 count = 16;

  optional string provider = 17;

  terraform.v1.Lifecycle lifecycle = 18;

  message Topology {
    // endpoints: list of object
    repeated Endpoints endpoints = 1;

    message Endpoints {
      // city: string
      optional string city = 1;

      // label: string
      optional string label = 2;
    }
  }

  message Wires {
    // admin_enabled: bool
    optional bool admin_enabled = 1 [json_name = "admin_enabled"];

    // endpoints: list of object
    repeated Endpoints endpoints = 2;

    // label: string
    optional string label = 3;

    // wire_properties: list of object
    repeated WireProperties wire_properties = 4 [json_name = "wire_properties"];

    message Endpoints {
      // interconnect: string
      optional string interconnect = 1;

      // vlan_tag: number
      optional int64 vlan_tag = 2 [json_name = "vlan_tag"];
    }

    message WireProperties {
      // bandwidth_unmetered: number
      optional int64 bandwidth_unmetered = 1 [json_name = "bandwidth_unmetered"];

      // fault_response: string
      optional string fault_response = 2 [json_name = "fault_response"];
    }
  }

  message Endpoints {
    optional string endpoint = 1;

    repeated Interconnects interconnects = 2;

    message Interconnects {
      optional string interconnect = 1;

      optional string interconnect_name = 2 [json_name = "interconnect_name"];

      // VLAN tags for the interconnect.
      repeated int64 vlan_tags = 3 [json_name = "vlan_tags"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }

  message WireProperties {
    // The configuration of a wire's bandwidth allocation.
    //
    // ALLOCATE_PER_WIRE: configures a separate unmetered bandwidth allocation (and
    // associated charges) for each wire in the group.
    //
    // SHARED_WITH_WIRE_GROUP: this is the default behavior, which configures one
    // unmetered bandwidth allocation for the wire group. The unmetered bandwidth is
    // divided equally across each wire in the group, but dynamic
    //
    // throttling reallocates unused unmetered bandwidth from unused or underused wires
    // to other wires in the group.
    optional string bandwidth_allocation = 1 [json_name = "bandwidth_allocation"];

    // The unmetered bandwidth setting.
    optional int64 bandwidth_unmetered = 2 [json_name = "bandwidth_unmetered"];

    // Response when a fault is detected in a pseudowire:
    //
    // NONE: default.
    //
    // DISABLE_PORT: set the port line protocol down when inline probes detect a fault.
    // This setting is only permitted on port mode pseudowires.
    optional string fault_response = 3 [json_name = "fault_response"];
  }
}

// GoogleComputeZoneVmExtensionPolicy version is 0
message GoogleComputeZoneVmExtensionPolicy {
  // Creation timestamp in RFC3339 text format.
  optional string creation_timestamp = 1 [json_name = "creation_timestamp"];

  // Whether Terraform will be prevented from destroying the instance. Defaults to
  // "DELETE".
  //
  // When a 'terraform destroy' or 'terraform apply' would delete the instance,
  //
  // the command will fail if this field is set to "PREVENT" in Terraform state.
  //
  // When set to "ABANDON", the command will remove the resource from Terraform
  //
  // management without updating or deleting the resource in the API.
  //
  // When set to "DELETE", deleting the resource is allowed.
  optional string deletion_policy = 2 [json_name = "deletion_policy"];

  // An optional description of this resource.
  optional string description = 3;

  // The unique identifier for the resource.
  optional string id = 4;

  // Type of the resource.
  optional string kind = 5;

  // Indicates if this policy is managed by a global policy.
  optional bool managed_by_global = 6 [json_name = "managed_by_global"];

  // Name of the resource. Provided by the client when the resource is created.
  optional string name = 7;

  // Priority of this policy.
  optional int64 priority = 8;

  optional string project = 9;

  // Current state of the policy.
  optional string state = 10;

  // Name of the zone for this request.
  optional string zone = 11;

  repeated ExtensionPolicies extension_policies = 12 [json_name = "extension_policies"];

  repeated InstanceSelectors instance_selectors = 13 [json_name = "instance_selectors"];

  repeated Timeouts timeouts = 14;

  google.protobuf.Value for_each = 15 [json_name = "for_each"];

  repeated string depends_on = 16 [json_name = "depends_on"];

  optional int32 count = 17;

  optional string provider = 18;

  terraform.v1.Lifecycle lifecycle = 19;

  message ExtensionPolicies {
    optional string extension_name = 1 [json_name = "extension_name"];

    // The specific version of the extension to install.
    optional string pinned_version = 2 [json_name = "pinned_version"];

    // String-based configuration data for the extension.
    optional string string_config = 3 [json_name = "string_config"];
  }

  message InstanceSelectors {
    LabelSelector label_selector = 1 [json_name = "label_selector"];

    message LabelSelector {
      // A map of key-value pairs representing VM labels.
      map<string, string> inclusion_labels = 1 [json_name = "inclusion_labels"];
    }
  }

  message Timeouts {
    optional string create = 1;

    optional string delete = 2;

    optional string update = 3;
  }
}
