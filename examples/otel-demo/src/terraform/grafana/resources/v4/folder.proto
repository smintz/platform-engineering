syntax = "proto3";

// Provider: grafana 4
package terraform.grafana.resources.v4;

import "google/protobuf/struct.proto";

import "terraform/v1/meta.proto";

// GrafanaFolder version is 0
message GrafanaFolder {
  string id = 1;

  // The Organization ID. If not set, the Org ID defined in the provider block will be
  // used.
  string org_id = 2 [json_name = "org_id"];

  // The uid of the parent folder. If set, the folder will be nested. If not set, the
  // folder will be created in the root folder. Note: This requires the nestedFolders
  // feature flag to be enabled on your Grafana instance.
  string parent_folder_uid = 3 [json_name = "parent_folder_uid"];

  // Prevent deletion of the folder if it is not empty (contains dashboards or alert
  // rules). This feature requires Grafana 10.2 or later. Defaults to `false`.
  bool prevent_destroy_if_not_empty = 4 [json_name = "prevent_destroy_if_not_empty"];

  // The title of the folder.
  string title = 5;

  // Unique identifier.
  string uid = 6;

  // The full URL of the folder.
  string url = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}

// GrafanaFolderPermission version is 0
message GrafanaFolderPermission {
  // The UID of the folder.
  string folder_uid = 1 [json_name = "folder_uid"];

  string id = 2;

  // The Organization ID. If not set, the default organization is used for basic
  // authentication, or the one that owns your service account for token
  // authentication.
  string org_id = 3 [json_name = "org_id"];

  repeated Permissions permissions = 4;

  google.protobuf.Value for_each = 5 [json_name = "for_each"];

  repeated string depends_on = 6 [json_name = "depends_on"];

  int32 count = 7;

  string provider = 8;

  terraform.v1.Lifecycle lifecycle = 9;

  message Permissions {
    // Permission to associate with item. Options: View, Edit, Admin.
    string permission = 1;

    // Name of the basic role to manage permissions for. Options: `Viewer`, `Editor` or
    // `Admin`.
    string role = 2;

    // ID of the team to manage permissions for.
    string team_id = 3 [json_name = "team_id"];

    // ID of the user or service account to manage permissions for.
    string user_id = 4 [json_name = "user_id"];
  }
}

// GrafanaFolderPermissionItem version is 0
message GrafanaFolderPermissionItem {
  // The UID of the folder.
  string folder_uid = 1 [json_name = "folder_uid"];

  string id = 2;

  // The Organization ID. If not set, the default organization is used for basic
  // authentication, or the one that owns your service account for token
  // authentication.
  string org_id = 3 [json_name = "org_id"];

  // the permission to be assigned
  string permission = 4;

  // the role onto which the permission is to be assigned
  string role = 5;

  // the team onto which the permission is to be assigned
  string team = 6;

  // the user or service account onto which the permission is to be assigned
  string user = 7;

  google.protobuf.Value for_each = 8 [json_name = "for_each"];

  repeated string depends_on = 9 [json_name = "depends_on"];

  int32 count = 10;

  string provider = 11;

  terraform.v1.Lifecycle lifecycle = 12;
}
