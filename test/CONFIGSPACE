platform=remote_repo(label="platform", url="..", branch="main")

# One module per driver: importing Grafana should not drag in ECS, and a second
# monitoring or CI driver is a new repo rather than a change to an existing one.
github_actions=remote_repo(label="github_actions", url="../drivers/cicd/github_actions", branch="main")
grafana=remote_repo(label="grafana", url="../drivers/monitoring/grafana", branch="main")
ecs=remote_repo(label="ecs", url="../drivers/runtime/ecs", branch="main")
kubernetes=remote_repo(label="kubernetes", url="../drivers/runtime/kubernetes", branch="main")
terraform_state=remote_repo(label="terraform_state", url="../drivers/state/terraform", branch="main")
