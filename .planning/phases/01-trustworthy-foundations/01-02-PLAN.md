---
phase: 01-trustworthy-foundations
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/platform/core.pinc
  - src/platform/platform.pinc
  - test/src/select_test.mpconf
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FOUND-02, FOUND-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A platform author writes `platform.WithLabels(tier = \"cache\")` on a component and the pair lands in `Component.Metadata.labels` (D-07, FOUND-02)."
    - "`WithLabels` constructs `Component.Metadata` before writing into it, so it works on a component that has none — `component.metadata` is unset until assigned."
    - "A platform author calls `platform.SelectComponents(root_msg, predicate)` and receives every component in the dependency graph the predicate accepts (D-11, FOUND-03)."
    - "`SelectComponents` returns a Starlark list in the same post-order the existing traversal produces — upstreams before the component that declared them."
    - "Two components with identical content collapse to a single entry: a component reached down two arms of a diamond is returned once, not twice (D-11). Components differing in domain or in any hook do not collapse."
    - "`SelectComponents` returns an empty list when nothing matches, and does not fail — Phase 5's SEC-02 needs that result in order to raise its own error."
    - "`GetConfigs` produces byte-identical output after the traversal is lifted out of it: recompiling the reference stack leaves every golden file unchanged (D-12)."
    - "One traversal exists, with two consumers — `GetConfigs` and `SelectComponents` share it rather than each owning a walk that could disagree about what the graph is (D-10)."
    - "Both symbols are reachable from a call site the same way every other platform symbol is, through the `platform` facade struct (D-13), with no new load edge and the module graph still acyclic."
    - "The de-duplication, ordering and empty-match guarantees are asserted by a committed probe that `make test` compiles on every build and that materializes nothing."
    - "No planning artifact still names a `Component.tags` field that D-07 decided will not exist: REQUIREMENTS.md FOUND-02 and both ROADMAP references name `Component.Metadata.labels`, so Phase 5's SEC-01 is planned against the field the code actually has."
  artifacts:
    - src/platform/core.pinc
    - src/platform/platform.pinc
    - test/src/select_test.mpconf
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "`GetConfigs` -> module-level `walk_upstreams` — the call replaces the nested closure at core.pinc:205-212; `GetConfigs` becomes a consumer, not the owner."
    - "`SelectComponents` -> the same `walk_upstreams`, passing a `collect` closure exactly as `GetConfigs` passes `collect_configs`."
    - "`WithLabels` -> `component_proto.Metadata()` -> `component.metadata.labels` — the nil-guard shape copied from the `Upstreams()` guard at core.pinc:159-160."
    - "`src/platform/platform.pinc` load list and `platform` struct -> the two new `core.pinc` symbols. `platform.pinc` already loads `core.pinc` and `core.pinc` loads nothing back."
    - "`test/src/select_test.mpconf` -> `platform.SelectComponents` / `platform.WithLabels` — the only executable assertion of either, and it runs inside `make test`."
  prohibitions:
    - "No second graph traversal is written — `SelectComponents` reuses the lifted `walk_upstreams` (D-10)."
    - "No `Component.tags` field is added to `src/platform/v1/platform.proto`; the schema is not edited at all (D-07)."
    - "`Metadata.tags` (the repeated-string field) stays unwritten and unread — one shape vocabulary, `labels` (D-08)."
    - "`GetConfigs`' observable output does not change (D-12)."
    - "`WithLabels` is an ordinary `component_filter` hook, not an `Inherit(...)` hook and not a new marker kind (D-09). Whether labels should inherit is a Phase 2 decision."
    - "`SelectComponents` does not de-duplicate by a `<domain>/<name>` string key — that silently asserts a uniqueness nothing enforces."
    - "No `print(...)` survives into a commit in any `.pinc` or `.mpconf`."
  stated_assumptions:
    - "FOUND-02, zero-argument call: no source artifact specifies what `WithLabels()` with no keyword arguments should do. The implementation below constructs an empty `Metadata` and writes nothing, which is harmless — a `Component` is never materialized, only its configs are — but it is an assumption, not a decision."
---

<objective>
Close FOUND-02 and FOUND-03: give a component declared, selectable shape, and give a
platform author one call that retrieves every component in a dependency graph matching a
predicate.

Purpose: this is the surface every later stakeholder fan-out selects against. Phase 2 writes
cost centres into it (COST-01/02/03) and Phase 5 targets policies at it (SEC-01/02/03). It is
deliberately built on the field that already exists (`Component.Metadata.labels`) and the
traversal already proven correct against the reference stack — two shape vocabularies, or
two walks, are two things that can disagree, and a selector nobody can audit is worse than
none.

Output: three symbols in `src/platform/core.pinc`, two facade exports, and one committed
probe that asserts all of it on every build.
</objective>

<execution_context>
@/Users/smintz/git/platform-engineering/.claude/gsd-core/workflows/execute-plan.md
@/Users/smintz/git/platform-engineering/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trustworthy-foundations/01-CONTEXT.md
@.planning/phases/01-trustworthy-foundations/01-RESEARCH.md
@.planning/phases/01-trustworthy-foundations/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: A platform author can declare a component's shape and select on it</name>

  <files>src/platform/core.pinc, src/platform/platform.pinc, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>

  <read_first>
    - `.claude/skills/protoconf-dev/SKILL.md` — invoke the `protoconf-dev` skill before editing any `.pinc`. It carries the Starlark/proto/compile mechanics, the `load()` path rules, and the reminder that a debugging print must not survive into a commit.
    - `src/platform/core.pinc` in full — the file being modified. Specifically: `WithDescription` at 101-107 (the hook shape to copy), `WithFailureDomain` at 118-126 (the inheriting shape NOT to copy), the `Upstreams()` nil-guard at 159-160 (the shape `WithLabels` copies), and `GetConfigs` at 180-214 with its nested walk at 205-212.
    - `src/platform/platform.pinc` in full — 72 lines, one shape throughout: an alphabetically sorted `load(...)` list and a `struct` grouped by `#` comment sections.
    - `src/platform/v1/platform.proto` lines 17 and 27-30 — `Metadata metadata = 5;` and `message Metadata { repeated string tags = 1; map<string, string> labels = 2; }`. Read only; this file is NOT edited.
    - `.planning/phases/01-trustworthy-foundations/01-RESEARCH.md` sections "Code Examples / 3" and "Code Examples / 4" — verified implementations of all three symbols, compiled against the reference stack, plus the facade edits. "Common Pitfalls" items 4 and 5 explain why each line is the shape it is.
    - `.planning/phases/01-trustworthy-foundations/01-PATTERNS.md` — the analog assignments and, critically, the placement warning about `GetConfigs`' doc comment.
    - `.planning/codebase/CONVENTIONS.md` section "Comment style" — the strongest convention in this repo. Every public symbol carries a prose block above it explaining why the design is this shape and naming the alternative rejected. A new hook without a rationale comment is out of place here.
  </read_first>

  <precondition>`protoconf` is on PATH and `git status --porcelain -- test/materialized_config test/outputs .github/workflows/terraform.yaml` is empty before starting — the acceptance test for this task is an EMPTY diff on those trees, which only means something from a clean base.</precondition>

  <action>
Three additions to `src/platform/core.pinc` and two to `src/platform/platform.pinc`. Use the
verified bodies in RESEARCH.md Code Examples 3 and 4 rather than re-deriving them; they were
compiled against the reference stack and left all ten golden files byte-identical.

1. Lift the traversal to module level (D-10). `GetConfigs` currently owns the walk as a
nested closure at `core.pinc:205-212`. Move it out to a module-level function named
`walk_upstreams`, taking `(component, next)` — keep the `next`-callback shape, which is the
convention every hook in this file follows and which lets `SelectComponents` pass a `collect`
closure exactly as `GetConfigs` passes `collect_configs`. The body is unchanged: recurse into
`component.upstreams.components` when `component.upstreams` is set, then call
`next(component)`. `GetConfigs` keeps its `collect_configs` closure and its
`walk_upstreams(component, collect_configs)` call; only the definition moves. This is D-10:
the traversal already proven correct against the reference stack gets a second consumer, not
a second implementation.

Placement. `GetConfigs`' four-line doc comment sits directly above its `def` at
`core.pinc:180-183`. Insert the new functions ABOVE that comment, not between the comment and
the `def` — otherwise the comment silently reattaches to the lifted traversal. RESEARCH.md
flags this as a mistake its own scratch patch made: it compiles fine and reads wrong.

2. Add `SelectComponents(component, predicate)` (D-11, FOUND-03). Build a local `dict`,
define a `collect(c)` closure that sets `found[c] = True` when `predicate(c)` accepts, call
`walk_upstreams(component, collect)`, and `return found.keys()`.

Three properties this shape buys, all verified in RESEARCH.md Pitfall 5, and all of which a
different shape would lose:
  - The de-duplication is by VALUE, not by reference. `Component` calls each dependency
    factory once per parent (`core.pinc:161-164`), so a diamond's repeated upstream is two
    distinct objects with equal content. Proto messages in this Starlark are hashable and
    compare structurally, so the dict collapses them; reference identity would collapse
    nothing. Do not de-duplicate by a domain-and-name string key — that works, but it
    silently asserts a uniqueness nothing in the schema enforces.
  - `found.keys()` returns a Starlark `list`, satisfying D-11's return type.
  - Insertion order is post-order and an empty match returns an empty list rather than
    failing, which is what Phase 5's SEC-02 needs in order to raise its own zero-match error.

The predicate receives the `platform.v1.Component` MESSAGE, not the `module` wrapper —
`upstreams.components` holds messages, so that is what the traversal yields. The root
argument is likewise a message, matching how `test/src/core_test.mpconf:112-114` already
calls `platform.GetConfigs(component.msg)`.

3. Add `WithLabels(**labels)` (D-07, D-09, FOUND-02). An ordinary `WithX` hook factory
following `WithDescription` at `core.pinc:101-107`: an inner `do(component, next)` that
mutates and calls `next`, returning `component_filter(do)`. It must NOT wrap that return in
`Inherit(...)` — that is `WithFailureDomain`'s shape and D-09 asks for an ordinary hook here.
Whether labels should propagate down the dependency subtree is a Phase 2 question (COST-01),
and the inheriting variant is a one-word change when Phase 2 needs it.

The body iterates `labels.items()` and assigns each pair into `component.metadata.labels`,
guarded by a nil-check first: `component.metadata` is unset until assigned, and writing into
it without constructing it raises a NoneType field error at compile (RESEARCH.md Pitfall 4).
Copy the exact guard shape from `core.pinc:159-160`, which does the same thing for
`Upstreams()`. `component_proto` is already loaded at `core.pinc:1`, so
`component_proto.Metadata()` needs no new load line.

Write into `Metadata.labels`, the `map<string, string>` field. Do NOT add a `Component.tags`
field to the proto — the field already exists under a different name and is written by
nothing today (D-07). Do NOT write the repeated-string field on `Metadata`; one keyed
vocabulary can answer "which components matched, and on what value" and a bare set cannot,
and two vocabularies answering the same question can disagree (D-08). Values are strings only
— the project constraint that every value the platform produces is expressible as a proto
message (`01-RESEARCH.md` §"Project Constraints", row 5) means a policy selector value carries
no nested structure.

Each of the three gets a prose block comment above it in the register of the surrounding
file: why the design is this shape, and what the rejected alternative would cost.

4. Export through the facade (D-13). Two edits in `src/platform/platform.pinc`, both
following the file's own single shape. In the `load("//platform/core.pinc", ...)` list at
lines 1-21, which is alphabetically sorted with PascalCase before snake_case, add
`"SelectComponents"` between `"MAIN_CONFIG"` and `"TerraformConfig"`, and `"WithLabels"`
between `"WithFailureDomain"` and `"chain"`. In the `platform = struct(...)` at 42-72, which
is grouped by `#` comment section rather than alphabetised within a group, add
`SelectComponents = SelectComponents` in the `# components and their configs` group beside
`GetConfigs` — it is the second consumer of the same traversal — and `WithLabels = WithLabels`
beside `WithDescription` and `WithFailureDomain`. No new load edge is created:
`platform.pinc` already loads `core.pinc` and `core.pinc` loads nothing back, so the acyclic
module graph the project requires is preserved by construction
(`.planning/codebase/ARCHITECTURE.md:172`, "Module graph is acyclic by construction";
`01-RESEARCH.md` §"Project Constraints", row 1).

5. Sync the three upstream lines that still name the field D-07 decided against. D-07 is
locked and this task honours it, but `REQUIREMENTS.md` and `ROADMAP.md` still describe
`Component.tags` — a field that will not exist after this plan. Phase 5's SEC-01 is planned
directly off those lines, so left alone this is the same prose-versus-code drift the phase
exists to close, one phase downstream. Three edits, prose only, no requirement IDs renumbered
and no success criterion changed in substance:

  - `.planning/REQUIREMENTS.md`, the FOUND-02 bullet: replace
    ``(`platform.v1.Component.tags`)`` with ``(`platform.v1.Component.Metadata.labels`)``.
  - `.planning/ROADMAP.md`, Phase 1 Success Criterion 2: replace ``a predicate over
    `Component.tags` `` with ``a predicate over `Component.Metadata.labels` ``.
  - `.planning/ROADMAP.md`, the Overview paragraph: replace ``it needs `Component.tags` to
    exist`` with ``it needs `Component.Metadata.labels` to be populated``, and the following
    ``populating meaningful tags`` with ``populating meaningful labels``.

Do not restate the old field name anywhere in the amended text, not even parenthetically — the
acceptance gate is a zero-count grep for `Component.tags` across both files, and a "formerly
called X" aside would defeat it. The decision record already lives in `01-CONTEXT.md` D-07,
which is where the history belongs.

Run `protoconf fmt -w` (or the root `make fmt`) before finishing, and strip any debugging
output added during the loop. The tight loop here is `cd test && protoconf compile .`, since
`test/.protoconf_cache/platform` is a symlink into the working tree and picks up uncommitted
edits with no re-sync.
  </action>

  <verify>
    <automated>R=$(git rev-parse --show-toplevel) && cd "$R/test" && make test && cd "$R" && git add -AN -- test/materialized_config test/outputs .github/workflows/terraform.yaml && git diff --exit-code -- test/materialized_config test/outputs .github/workflows/terraform.yaml</automated>
    <fails_when>`make test` exits non-zero, or `git diff --exit-code` reports ANY change under `test/materialized_config`, `test/outputs` or `.github/workflows/terraform.yaml` — D-12 requires this refactor to be byte-for-byte behaviour-preserving, so a single changed line means `GetConfigs` no longer produces what it produced.</fails_when>

    <automated>R=$(git rev-parse --show-toplevel) && grep -qE '^def walk_upstreams\(component, next\):' "$R/src/platform/core.pinc" && grep -qE '^def SelectComponents\(component, predicate\):' "$R/src/platform/core.pinc" && grep -qE '^def WithLabels\(\*\*labels\):' "$R/src/platform/core.pinc" && grep -q 'component_proto.Metadata()' "$R/src/platform/core.pinc" && grep -q 'walk_upstreams(component, collect_configs)' "$R/src/platform/core.pinc"</automated>
    <fails_when>any of the three symbols is missing from module level in `src/platform/core.pinc`, or the `Metadata()` nil-guard is absent, or `GetConfigs` no longer calls the lifted traversal — meaning the walk was duplicated rather than shared.</fails_when>

    <automated>R=$(git rev-parse --show-toplevel) && test "$(sed -n '/^def GetConfigs/,/^def [A-Za-z_]/p' "$R/src/platform/core.pinc" | grep -c 'def walk_upstreams')" -eq 0 && test "$(grep -c 'def walk_upstreams' "$R/src/platform/core.pinc")" -eq 1</automated>
    <fails_when>the traversal is still defined inside the `GetConfigs` body, or exists more than once in the file — region-scoped on purpose: it must appear exactly once at module level and zero times inside `GetConfigs`.</fails_when>

    <automated>R=$(git rev-parse --show-toplevel) && grep -q '"SelectComponents",' "$R/src/platform/platform.pinc" && grep -q '"WithLabels",' "$R/src/platform/platform.pinc" && grep -q 'SelectComponents = SelectComponents,' "$R/src/platform/platform.pinc" && grep -q 'WithLabels = WithLabels,' "$R/src/platform/platform.pinc" && test "$(grep -c '"//platform/platform.pinc"' "$R/src/platform/core.pinc")" -eq 0</automated>
    <fails_when>either symbol is missing from the facade's `load(...)` list or from the `platform` struct, or `src/platform/core.pinc` gained a load of the facade — which would make the module graph cyclic and break the acyclic-module-graph constraint at `.planning/codebase/ARCHITECTURE.md:172`.</fails_when>

    <automated>R=$(git rev-parse --show-toplevel) && test "$(grep -c 'Component\.tags' "$R/.planning/REQUIREMENTS.md")" -eq 0 && test "$(grep -c 'Component\.tags' "$R/.planning/ROADMAP.md")" -eq 0 && grep -q 'platform\.v1\.Component\.Metadata\.labels' "$R/.planning/REQUIREMENTS.md" && grep -q 'Component\.Metadata\.labels' "$R/.planning/ROADMAP.md"</automated>
    <fails_when>either planning artifact still names `Component.tags`, or neither names `Component.Metadata.labels` — meaning Phase 5's SEC-01 would be planned against a field D-07 decided will not exist.</fails_when>

    <automated>R=$(git rev-parse --show-toplevel) && test -z "$(git -C "$R" diff HEAD --name-only -- src/platform/v1/platform.proto)" && test "$(grep -c 'Inherit(component_filter(do))' "$R/src/platform/core.pinc")" -eq 1 && test "$(grep -c 'metadata.tags' "$R/src/platform/core.pinc")" -eq 0 && test "$(grep -c 'print(' "$R/src/platform/core.pinc")" -eq 0 && test "$(grep -c 'print(' "$R/src/platform/platform.pinc")" -eq 0</automated>
    <fails_when>`src/platform/v1/platform.proto` was modified (D-07 forbids adding a tags field — the schema needs no edit), or a second inheriting hook appeared (meaning `WithLabels` was made to inherit against D-09), or the repeated-string metadata field was written (D-08), or debugging output survived into either edited file.</fails_when>
  </verify>

  <acceptance_criteria>
    - `src/platform/core.pinc` defines `walk_upstreams(component, next)` at module level, exactly once in the file, and zero times inside the `GetConfigs` body.
    - `GetConfigs` calls `walk_upstreams(component, collect_configs)` and its own `collect_configs` closure is unchanged.
    - `src/platform/core.pinc` defines `SelectComponents(component, predicate)` at module level, which collects into a `dict` keyed by the component message and returns `found.keys()`.
    - `src/platform/core.pinc` defines `WithLabels(**labels)` at module level, which guards on `component.metadata`, constructs `component_proto.Metadata()` before writing, and returns `component_filter(do)` with no `Inherit(...)` wrapper.
    - `grep -c 'metadata.tags' src/platform/core.pinc` returns 0.
    - `src/platform/v1/platform.proto` is unmodified.
    - `src/platform/platform.pinc` lists `"SelectComponents"` and `"WithLabels"` in its `load(...)` block and binds both in the `platform` struct.
    - `src/platform/core.pinc` contains no load of the facade module (`grep -c '"//platform/platform.pinc"' src/platform/core.pinc` returns 0 — the local-root load form, which is distinct from the `@platform//` cross-module form a call site uses).
    - Each of the three new symbols carries a prose block comment above its `def` explaining why the design is this shape.
    - `cd test && make test` exits 0 and produces an EMPTY diff on `test/materialized_config`, `test/outputs` and `.github/workflows/terraform.yaml`.
    - `grep -c 'Component\.tags' .planning/REQUIREMENTS.md` and the same over `.planning/ROADMAP.md` both return 0, and both files name `Component.Metadata.labels` instead.
    - No requirement ID was renumbered and no ROADMAP success criterion changed in substance — the amendment renames a field, nothing else.
    - No debugging print remains in either edited `.pinc` file.
  </acceptance_criteria>

  <done>`platform.WithLabels(tier = "cache")` writes into `Component.Metadata.labels`, `platform.SelectComponents(root_msg, predicate)` returns the matching components of a graph as a de-duplicated post-order list, and the reference stack compiles to byte-identical output — the refactor is proven behaviour-preserving by the only assertion mechanism this repo has.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The de-duplication, ordering and empty-match guarantees are asserted on every build</name>

  <files>test/src/select_test.mpconf</files>

  <behavior>
    Given a diamond `top -> {mid1, mid2} -> leaf`, labelled edge / app / app / cache, plus a
    fifth component that declares no labels at all as an upstream of one mid — five distinct
    components in the graph:
    - An always-true predicate returns FIVE names: one entry per distinct component, with the
      leaf appearing exactly once despite being reached down both arms, in post-order
      (upstreams before the component that declared them). The unlabelled component is one of
      the five — it has no `metadata`, and it is still selected.
    - `tier == "app"` returns the two mid names, in post-order.
    - `tier == "cache"` returns exactly the leaf name.
    - A `tier` value no component declares returns an empty list — an empty result, not a failure.
    - A component that declared no labels has `metadata` unset, and a predicate that reads labels survives that without raising.
  </behavior>

  <read_first>
    - `.claude/skills/protoconf-dev/SKILL.md` — invoke the `protoconf-dev` skill. Note in particular that `.pinc` files materialize nothing and only `.mpconf`/`.pconf` produce output, and that `load()` uses `@label//` for another module.
    - `src/platform/core.pinc` as edited by Task 1 — the three symbols being asserted.
    - `test/src/core_test.mpconf` lines 1-13, 47-50 and 111-114 — the `load(...)` ordering convention (cross-module `@label//` first, alphabetical by path), the `XComponent(*hooks)` factory shape, and how `main()` calls into the platform with `.msg`.
    - `.planning/phases/01-trustworthy-foundations/01-RESEARCH.md` section "Code Examples / 4" for the verified expected results (ALL, APPS, CACHES, NONE), section "Code Examples / 8" for the shape of a probe that materializes nothing, and section "Don't Hand-Roll" last row for why a committed `.mpconf` is the right vehicle rather than a shell script grepping compiler stderr.
    - `.planning/codebase/TESTING.md` section "Coverage" — the named gap this begins to close, and section "Adding Coverage" for how a new mechanism is meant to show up.
  </read_first>

  <action>
Create `test/src/select_test.mpconf`, a probe whose `main()` returns an empty dict. An
`.mpconf` returning an empty dict writes no entry under `test/outputs/` or
`test/materialized_config/` (verified in RESEARCH.md), so this assertion costs zero
golden-file surface while running as part of every `cd test && make test`. That is the whole
reason it is an `.mpconf` and not a shell script: it runs inside the build that already
exists.

It needs one load line: `load("@platform//platform/platform.pinc", "platform")`. No runtime
driver, no state driver, no backend — a component with no `WithState` still exists and still
carries upstreams and labels, and nothing here calls `GetConfigs`.

Build a diamond so the de-duplication case is real rather than hypothetical. Four factory
functions in the style of `RedisComponent`/`ApiTaskComponent` — each takes `*hooks` and
returns `platform.Component(...)` — with dependencies passed UNINSTANTIATED to
`platform.WithDeps(...)`, which is what makes the two mids each build their own leaf:

  - a leaf factory — `platform.WithLabels(tier = "cache")`, no deps
  - two mid factories with distinct slugs — each `platform.WithLabels(tier = "app")` and
    `platform.WithDeps(<leaf factory>)`
  - a top factory — `platform.WithLabels(tier = "edge")` and
    `platform.WithDeps(<mid1 factory>, <mid2 factory>)`

The two mids must differ in something other than their labels — distinct component slugs is
enough — because de-duplication is by content, and two components identical in every field
would legitimately collapse into one.

Add a fifth component that declares NO labels at all, as an upstream of one mid. It exists so
the unset-metadata branch of the predicate is actually exercised rather than merely written,
and so the always-true assertion covers a component whose `metadata` is unset.

In `main()`, take the root as `<top factory>().msg` and assert four selections by comparing a
`[c.name for c in ...]` list against a literal, calling `fail(...)` with a message naming both
the expected and the actual list when they differ. Follow the repo's error convention: the
message is prefixed with what raised it and explains the fix, not just the fault.

All four `fail(...)` messages MUST begin with the literal prefix `SelectComponents: expected `,
before naming which selection tripped and what it got. That prefix is load-bearing, not
cosmetic: the negative check below greps the compiler log for it, and it is the only string in
the log that a tripped assertion produces and an unrelated failure — a syntax error, a missing
load, a NoneType field error, all of which name `select_test.mpconf` in their traceback — does
not. The
always-true assertion is the important one — it covers post-order AND the diamond
de-duplication in a single comparison, because the leaf is reached down both arms and must
appear exactly once.

The label predicate must short-circuit on unset metadata: `component.metadata` is unset on a
component that declared none, so write it as a truthiness check on the component's metadata
before reading `metadata.labels.get(...)`.

Head the file with a prose comment in the register of the rest of the repo: what this file
asserts, and why it asserts it here rather than in a golden file — the golden files cannot
see `SelectComponents` at all, because nothing materialises a `Component`.

`main()` returns an empty dict. Add no output key. Strip any debugging output before
finishing, and run `protoconf fmt -w`.
  </action>

  <verify>
    <automated>R=$(git rev-parse --show-toplevel) && cd "$R/test" && make test && cd "$R" && git add -AN -- test/materialized_config test/outputs .github/workflows/terraform.yaml && git diff --exit-code -- test/materialized_config test/outputs .github/workflows/terraform.yaml</automated>
    <fails_when>`make test` exits non-zero — which for this file means one of the four assertions in `select_test.mpconf` tripped, and the compiler traceback names which — or the probe wrote an output entry, showing as a non-empty diff on the golden trees.</fails_when>

    <automated>R=$(git rev-parse --show-toplevel) && test -z "$(find "$R/test/outputs" "$R/test/materialized_config" -name 'select_test*' -print -quit)" && test "$(grep -c 'print(' "$R/test/src/select_test.mpconf")" -eq 0 && grep -q 'return {}' "$R/test/src/select_test.mpconf" && test "$(grep -c 'platform.SelectComponents' "$R/test/src/select_test.mpconf")" -ge 4 && test "$(grep -c 'platform.WithLabels' "$R/test/src/select_test.mpconf")" -ge 3 && test "$(grep -c 'fail(' "$R/test/src/select_test.mpconf")" -ge 4</automated>
    <fails_when>the probe materialized a file under either golden tree, or debugging output survived, or `main()` does not return an empty dict, or fewer than four selections / three label declarations / four assertions are present — meaning the diamond, the ordering, the label filter or the empty-match case is not actually being asserted.</fails_when>

    <automated>R=$(git rev-parse --show-toplevel) && cd "$R/test" && cp src/select_test.mpconf "${TMPDIR:-/tmp}/select_test.bak" && sed 's/tier = "cache"/tier = "app"/' "${TMPDIR:-/tmp}/select_test.bak" > src/select_test.mpconf; protoconf compile . > "${TMPDIR:-/tmp}/select_neg.log" 2>&1; RC=$?; cp "${TMPDIR:-/tmp}/select_test.bak" src/select_test.mpconf; test $RC -ne 0 && grep -q 'SelectComponents: expected' "${TMPDIR:-/tmp}/select_neg.log"</automated>
    <fails_when>`protoconf compile` exits 0 after the leaf's label is relabelled to match the mids, or the compiler output does not contain the literal `SelectComponents: expected` — which is emitted only by this probe's own assertions. A bare case-insensitive grep for `select` would match any failure inside a file called `select_test.mpconf` — a syntax error, a missing load, a NoneType field error — and so would not distinguish "the assertion tripped" from "the file did not compile", which is the property this check exists to establish. The original file is restored before the assertion runs, so a failure here leaves the worktree clean.</fails_when>
  </verify>

  <acceptance_criteria>
    - `test/src/select_test.mpconf` exists, is git-tracked, and its `main()` returns an empty dict.
    - Compiling it writes no file under `test/outputs/` or `test/materialized_config/` — `find` for `select_test*` in either tree returns nothing.
    - It declares a diamond in which one component is reached down two distinct arms, plus at least one component that declares no labels.
    - It contains at least four `platform.SelectComponents` calls and at least four `fail(` assertions.
    - The always-true selection is asserted against a literal list whose length equals the number of distinct components in the graph — proving the diamond's shared upstream appears once.
    - One assertion covers a predicate that matches nothing and expects an empty list.
    - `cd test && make test` exits 0 and produces an EMPTY diff on the three drift-asserted trees.
    - Every `fail(...)` message in the file begins with the literal `SelectComponents: expected `.
    - Mutating the leaf's label so it no longer distinguishes the leaf makes `protoconf compile .` exit non-zero AND emits `SelectComponents: expected` into the compiler log — the assertion demonstrably tripped, as distinct from the file merely failing to compile.
    - No debugging print remains in the file.
  </acceptance_criteria>

  <done>Every build of this repo now proves that `SelectComponents` de-duplicates a diamond, preserves post-order, filters on `Metadata.labels`, and returns an empty list rather than failing on no match — the first executable assertion in a repo whose only prior test was a golden diff.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols, files and paths created across all three plans of Phase 1. This plan produces rows
3-7; the rest are listed so an executor of any plan knows the full surface.

| Artifact | Kind | Home | Plan |
|----------|------|------|------|
| `.github/workflows/drift.yaml` | hand-written CI workflow | `.github/workflows/` | 01-01 |
| root `outputs/`, `materialized_config/` | DELETED (4 tracked files) | repo root | 01-01 |
| `walk_upstreams(component, next)` | module-level traversal, lifted out of `GetConfigs` | `src/platform/core.pinc` | 01-02 |
| `SelectComponents(component, predicate)` | public selector, returns a de-duplicated list | `src/platform/core.pinc` | 01-02 |
| `WithLabels(**labels)` | `component_filter` hook factory writing `Component.Metadata.labels` | `src/platform/core.pinc` | 01-02 |
| `platform.SelectComponents`, `platform.WithLabels` | facade re-exports (load list + struct) | `src/platform/platform.pinc` | 01-02 |
| `test/src/select_test.mpconf` | assertion probe, materializes nothing | `test/src/` | 01-02 |
| `_producers(configs)` | module-private helper extracted from `dependencies` | `drivers/cicd/github_actions/src/github_actions.pinc` | 01-03 |
| `_check_remote_backends(configs)` | module-private FOUND-04 compile gate | `drivers/cicd/github_actions/src/github_actions.pinc` | 01-03 |
| `_check_reads_are_produced(configs)` | module-private FOUND-05 compile gate | `drivers/cicd/github_actions/src/github_actions.pinc` | 01-03 |
| `test/src/handshake_test.mpconf` | cross-state handshake probe + negative-test switch points | `test/src/` | 01-03 |
| redis S3 backend | changed golden output (2 files) | `test/{outputs,materialized_config}/core_test/us-east-1/redis/infra/` | 01-03 |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declared component shape -> policy selection | Labels declared by one author decide which hardening, cost and policy hooks a component receives in Phases 2 and 5. A wrong selection result is a silently wrong policy application. |
| Starlark call site -> compiled artifact | Anything written into a component may end up in a committed, world-readable materialized file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | `SelectComponents` de-duplication | medium | mitigate | A component reached down two arms of a diamond returned twice means a Phase 5 hardening hook applied twice — silent, and in a mutating hook, wrong. De-duplicated by structural equality in a `dict`, and asserted by the always-true selection in `test/src/select_test.mpconf`, which runs on every build. |
| T-01-07 | Tampering | `SelectComponents` completeness | medium | mitigate | A selector that misses a component means a security policy that silently does not apply to it (SEC-01/SEC-03). D-10 reuses the one traversal already proven correct against the reference stack rather than writing a second walk that could disagree; D-12's empty-golden-diff assertion proves the lift changed nothing. |
| T-01-08 | Information Disclosure | `Component.Metadata.labels` | low | accept | `labels` is a free-form `map<string, string>` and an author could put a credential in it. Accepted rather than mitigated in code: `.planning/codebase/ARCHITECTURE.md:218` already states credentials never appear in configuration and are minted in CI, `Component` itself is never materialized (only its configs are), and a value-shape restriction on a `map<string,string>` is not expressible in the schema. The Phase 2 and Phase 5 plans that write labels carry the same constraint. |
| T-01-09 | Denial of Service | `walk_upstreams` recursion | low | accept | Unbounded recursion on a cyclic component graph would hang the compiler. Not reachable: `Component` builds each dependency eagerly from an uninstantiated factory at construction time, so a component cycle cannot be constructed in the first place, and remote-state cycles are caught separately by `_check_acyclic`. |
| T-01-SC | Tampering | package-manager installs | n/a | accept | This plan installs no npm/PyPI/crates package and adds no dependency of any kind. RESEARCH.md's Package Legitimacy Audit found no `[ASSUMED]`, `[SUS]` or `[SLOP]` package anywhere in this phase, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `cd test && make test` exits 0, and the diff on `test/materialized_config`,
   `test/outputs` and `.github/workflows/terraform.yaml` is EMPTY. This is D-12 and it is the
   single most important check in this plan: the whole refactor is only correct if it is
   invisible.
2. All five `<automated>` gates in Task 1 and all three in Task 2 pass.
3. `test/src/select_test.mpconf` compiles inside `make test` and materializes nothing.
4. The `drift` workflow added by plan 01-01 stays green on this change.

<human-check>
Read the diff on `src/platform/core.pinc`. Confirm that `GetConfigs`' four-line doc comment
still sits directly above `def GetConfigs` and has not been orphaned onto the lifted
traversal, and that each of the three new symbols carries a rationale comment in the register
of the file around it. Both are correctness-of-reading issues no grep can catch.
</human-check>
</verification>

<success_criteria>
- FOUND-02 satisfied: a component carries declared key/value shape, written through a
  supported hook, in the field the schema already had.
- FOUND-03 satisfied: an exported `platform.SelectComponents` returns every component in a
  dependency graph matching a predicate, once each, in a defined order.
- D-12 satisfied: the reference stack's generated output is byte-identical.
- The module graph is still acyclic and the proto schema is unchanged.
</success_criteria>

<edge_coverage_notes>
Resolved edges from the specless probe, lifted into `must_haves.truths` above:

| Requirement | Category | Resolution |
|-------------|----------|------------|
| FOUND-03 | adjacency | Value-equal components MERGE into one entry (dict keyed by the message, structural equality). Components differing in domain or in any hook do not merge. RESEARCH.md Pitfall 5. |
| FOUND-03 | empty | No match returns an empty list and does not fail; a root with no upstreams that matches returns just that root. Asserted in `select_test.mpconf`. |
| FOUND-03 | ordering | Post-order (upstreams before the component that declared them), deterministic; a de-duplicated component keeps its FIRST-occurrence position. |
| FOUND-02 | unclassified | `component.metadata` is unset until constructed, so `WithLabels` constructs `Metadata()` before writing. Labels are `map<string,string>`; values carry no nested structure. |

Unresolved, stated rather than guessed:

- FOUND-02, zero-argument call. Nothing in ROADMAP, REQUIREMENTS, CONTEXT or RESEARCH says
  what `WithLabels()` with no keyword arguments should do. The implementation constructs an
  empty `Metadata` and writes nothing. This is an assumption; if Phase 2 needs it to be an
  error, that is a Phase 2 decision.
</edge_coverage_notes>

<output>
Create `.planning/phases/01-trustworthy-foundations/01-02-SUMMARY.md` when done.
</output>
